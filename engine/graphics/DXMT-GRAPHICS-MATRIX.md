# DXMT Graphics Matrix

Date: 2026-06-05
Scope: owned graphics/DXMT work only. GPU/Wine smokes were not run while HK Lane A holds the GPU lane.

| Track | Entry Point | Status | Evidence |
|---|---|---|---|
| HK zero-latency present | `engine/graphics/scripts/run_hk_zero_latency_present_harness.sh x86_64` | Ready, not run | `engine/dxmt/tests/dx11/dx11_hk_present_probe.cpp` creates local-DXMT D3D11 device, HWND swapchain, one present, and staging readback. x86_64 llvm-mingw syntax check: PASS. |
| Phase 6 hardening | `engine/graphics/scripts/run_dxmt_phase6_hardening_suite.sh`; CI wrapper `engine/graphics/scripts/run_dxmt_d3d11_stability_smoke.sh` | Ready, not run | Wrappers require stability, residency, leak, command-batch, and multithread markers across repeated long runs. `DXMT_STABILITY_LEAK_BUDGET_KB` now forwards to `DXMT_SMOKE_LEAK_BUDGET_KB` and requires `UnityLeakProbe result=PASS`. |
| D3D8/9 to DXMT(D3D11) seed | `engine/graphics/scripts/run_d3d9_dxmt_headless_smoke.sh` | CPU PASS | D3D8 RenderWare-style triangle list and indexed triangle-strip plus D3D9 device/swapchain/FVF/render-state/sampler-state/texture-stage/indexed-triangle/indexed-triangle-strip/depth/cull/scissor/present, FFP WVP transform, texture modulate, texture-alpha combiner, `MODULATE4X`, signed add combiners (`ADDSIGNED`, `ADDSIGNED2X`, `ADDSMOOTH`), `DOTPRODUCT3`, `BLENDDIFFUSEALPHA`, `BLENDFACTORALPHA`, `D3DTA_COMPLEMENT` / `D3DTA_ALPHAREPLICATE` arg modifiers, alpha-test cutout, output-merger color write mask, `D3DFMT_INDEX32`, expanded legacy formats (`X8B8G8R8`, `A8P8`, `A4L4`, signed bump maps, `D16_LOCKABLE`, `D24FS8`, `DF16/DF24`, YUV aliases), `DrawIndexedPrimitive` range and `BaseVertexIndex` semantics, non-indexed `DrawPrimitive` range semantics including triangle strips, XNA programmable sprite, XNA alpha-blend sprite, and D3D9 format sweep translate to D3D11-like render state. Pytest: 57 passed. Mock smoke: 28 traces PASS, `present_count=1` each, MODULATE4X trace `center_pixel=(129,255,64)` / `non_background_pixels=1682`, BLENDDIFFUSEALPHA trace `center_pixel=(100,0,100)` / `non_background_pixels=1682`, BLENDFACTORALPHA trace `center_pixel=(100,0,100)` / `non_background_pixels=1682`, DOTPRODUCT3 trace `center_pixel=(255,255,255)` / `non_background_pixels=1682`, signed combiner trace `center_pixel=(64,128,192)` / `non_background_pixels=1682`, D3D8 RenderWare strip trace `non_background_pixels=2601`, legacy format trace `non_background_pixels=1152`, scissor trace `non_background_pixels=1024`, center pixel `(26,255,51)`, outside pixel `(4,8,16)`, color-write mask trace `center_pixel=(255,64,16)` / `non_background_pixels=1682`, FFP modifier trace `center_pixel=(191,127,63)` / `non_background_pixels=1682`, D3D9 indexed and non-indexed triangle-strip traces `non_background_pixels=2704`, non-indexed draw range trace `non_background_pixels=605`, center pixel remains background `(4,8,16)`, `unsupported_calls=0`. |
| D3D8/9 to Metal guarded lane | `engine/graphics/scripts/run_d3d9_metal_headless_smoke.sh` | Request PASS, GPU not run | Reuses the same twenty-eight D3D8/9 traces with `--backend metal`; guarded by `ALLOW_GPU_SMOKE=1` for real GPU execution. CPU-only `D3D9_METAL_REQUEST_ONLY=1` validates twenty-eight `.metal-request.json` contracts carrying `source_api=d3d8/d3d9`, render/depth formats, shader mode, D3D render states including cull mode, scissor-test enable, color write mask, and texture factor, texture-stage states and arg modifiers, sampler address/filter state, depth compare/write state, WVP transform matrix, expanded texture/render/depth formats, `uint16/uint32` index format, effective ranged/base-vertex indices or sliced non-indexed vertices, primitive topology including D3D8 RenderWare strips, draw range metadata, present count, and texture pixels. Native helper consumes request geometry/texture/sampler/depth/WVP/index-format/topology data, maps triangle strips to `MTLPrimitiveTypeTriangleStrip`, maps D3D color write masks to `MTLColorWriteMask`, maps enabled D3D9 scissor rects to Metal scissor state, maps D3D point/linear and clamp/wrap/mirror to Metal sampler state, maps D3D compare funcs to Metal depth state, maps D3D cull mode to Metal winding/cull state, chooses `MTLIndexTypeUInt16` or `MTLIndexTypeUInt32`, and emits Metal shader variants for programmable texture*diffuse plus FFP `MODULATE`, `MODULATE2X`, `MODULATE4X`, `ADD`, `ADDSIGNED`, `ADDSIGNED2X`, `ADDSMOOTH`, `DOTPRODUCT3`, `SUBTRACT`, `BLENDDIFFUSEALPHA`, `BLENDTEXTUREALPHA`, `BLENDFACTORALPHA`, `D3DTA_COMPLEMENT`, `D3DTA_ALPHAREPLICATE`, alpha-test discard, and alpha-blend output-merger state. Request smoke: PASS; native helper build-only: PASS. |

Next GPU-permitted evidence:

1. Run HK present harness once Lane A reaches D3D11CreateDevice and releases GPU contention.
2. Run Phase 6 hardening suite and record leak delta, presented frames, elapsed ms, residency result, batch result, and multithread result.
3. When GPU is free, run `ALLOW_GPU_SMOKE=1 engine/graphics/scripts/run_d3d9_metal_headless_smoke.sh` to collect real Metal PPM/report evidence for the twenty-eight D3D8/9 traces. Until then, use `D3D9_METAL_REQUEST_ONLY=1` for CPU-only Metal request contract checks.
