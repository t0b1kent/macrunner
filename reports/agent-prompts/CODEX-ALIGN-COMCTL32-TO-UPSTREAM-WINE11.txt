Codex — КОНКРЕТНЫЙ корень icon-багов найден через файлы CrossOver vs наши.

ФАКТЫ (проверено):
- Наш Wine = 11.0. engine/wine/dlls/comctl32/imagelist.c имеет 62 вставки MACRUNNER
  (наши патчи), toolbar.c — 18. CrossOver = СТОКОВЫЙ Wine (без наших патчей) → рисует
  ВСЕ иконки правильно (тулбар, disabled-серые→active-цветные).
- Разница НЕ в конфиге: бутылка CrossOver НЕ переопределяет comctl32, нет спец-themes/
  uxtheme. Просто builtin comctl32 из стока.
- engine/wine — снимок, НЕ git-форк (нет upstream-истории в git). Поэтому diff
  вручную против скачанного upstream.

ВЫВОД: баг = в НАШИХ 62+18 патчах, которые divergнули от стокового Wine 11.0. Стоку
эти патчи не нужны — он рендерит сам. Фикс = ВЫРОВНЯТЬ К СТОКУ, не добавлять патчи.

ЗАДАЧА (source-diff vs upstream Wine 11.0):
1. Достань upstream Wine 11.0 исходник comctl32/imagelist.c + toolbar.c (git tag
   wine-11.0 с github wine-mirror/wine, или winehq). Это РОВНО наша база.
2. Diff наш vs upstream по этим двум файлам. Все 62+18 MACRUNNER-правок = дельта.
3. Классифицируй каждую нашу правку:
   - НУЖНАЯ (чинит реальную macOS/HyperBridge проблему — напр. DIB-конверсия, где
     стоковый путь правда падает на нашем движке) → оставь.
   - ВРЕДНАЯ (saturate/polarity/disabled-эксперименты, которые ломают рендер, а у
     стока работает БЕЗ них) → ОТКАТИ к стоковому поведению.
4. Цель: imagelist/toolbar рендерят как СТОК (= как CrossOver): все иконки, disabled
   серый силуэт → active цветной, без пустых слотов, без чёрных квадратов.

ВАЖНО: задай вопрос для КАЖДОГО спорного места — "а как тут СТОК Wine 11.0? и почему
у CrossOver работает?" Если сток работает без нашей правки — наша правка лишняя/вредная.
Не накапливай патч поверх патча — возвращай к стоку где мы сломали.

VERIFY: строгий gate на реальном окне + (когда gate-агент сделает) пиксельный diff vs
CrossOver golden reference. Тулбар не регрессировать после каждой правки.

ДИСЦИПЛИНА: git diff наших файлов, журнал, реверты тоже patch-by-evidence. Сначала
верни рабочий тулбар (регресс от твоих поздних imagelist-правок), потом выравнивай.
