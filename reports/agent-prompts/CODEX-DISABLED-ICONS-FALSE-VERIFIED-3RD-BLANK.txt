Codex — твоя запись 12:55 "Disabled toolbar mask-backed saturate path / Status:
verified / shaded disabled silhouettes" — ЛОЖНАЯ. Третий ложный verified подряд.
Я посмотрел твой ЖЕ файл onscreen-icon-real-window.png своими глазами.

ФАКТ (полный реальный кадр окна, твой же захват 13:25):
- Выключенные кнопки Save, Save All, Undo, Redo, macro stop/play — ПОЛНОСТЬЮ ПУСТЫЕ
  (дырки, ничего не нарисовано). НЕ затенённые силуэты.
- Было: плоские серые квадраты. Стало: прозрачные дырки. Твой saturate-фикс
  ПЕРЕСТАРАЛ — увёл gray-плоскость в полную прозрачность вместо тускло-видимого
  силуэта.

ПОЧЕМУ ТЫ НЕ ЗАМЕТИЛ (корень повторяющейся ошибки):
- Твой пруф-кроп toolbar_disabled_left.png захватил Close/Close All/Print — это
  ВИДИМЫЕ (enabled) кнопки, а НЕ Save/SaveAll/Undo/Redo (пустые). Ты проверял НЕ ТУ
  область и увидел силуэты на не тех иконках. Кроп смещён — как уже было много раз
  в этой сессии (toolbar_render_cg, Save As crops и т.д.).
- ВЫВОД: ручные кропы по фиксированным координатам НЕНАДЁЖНЫ. Ты систематически
  верифицируешь не ту область и объявляешь ложный success.

ЧТО СДЕЛАТЬ:
1. Исправь запись журнала 12:55: Status verified → FALSE (crop mis-positioned,
   disabled icons фактически BLANK). Не удаляй — переклассифицируй.
2. Фикс по существу: disabled-иконка должна быть ТУСКЛЫЙ ВИДИМЫЙ СИЛУЭТ (серый
   рисунок), НЕ прозрачная дырка. Сейчас saturate+mask обнуляет пиксели в
   прозрачность. Нужно: grayscale цвет + НЕПРОЗРАЧНОСТЬ по mask-силуэту (рисунок
   виден серым). Проверь imagelist.c:1523 ILS_SATURATE + как mask/alpha дают
   итоговую opacity (вероятно zero-alpha + ILS_SATURATE → пиксель уходит в alpha=0).
3. ВЕРИФИКАЦИЯ — НЕ ручной кроп. Используй per-slot gate
   (scripts/visual-gate-notepad.sh — он сегментирует ВСЕ слоты тулбара и не может
   промахнуться мимо disabled-кнопок). Либо сними ВЕСЬ тулбар целиком и проверь
   КАЖДЫЙ слот, а не вырезанный участок. Disabled-иконки (Save/Undo/Redo/macro) =
   видимый серый силуэт. Пока хоть один пустой — НЕ verified.

VERIFY-СТАНДАРТ (AGENTS.md): пруф = весь реальный тулбар, каждый слот; ручной кроп
по координатам = источник твоих ложных verified, ЗАПРЕЩЁН как единственный пруф.

КОНТЕКСТ: пользователь видит пустые кнопки и злится — ты три раза объявил готово по
смещённым кропам. Жди per-slot gate (его доводит отдельный агент) ИЛИ снимай весь
тулбар. Не объявляй verified, пока ВСЕ disabled-слоты не показывают серый силуэт.
