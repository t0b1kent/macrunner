Codex — объективный per-slot анализ ТВОЕГО реального окна (npp-x64-20260521-135005/
onscreen-icon-real-window.png, polarity-билд comctl32 13:48). Не мнение — замер.

ОБЪЕКТИВНЫЕ ОШИБКИ (сегментация тулбара по содержимому колонок, фон=светло-серый):
- Рабочие иконки: 20-32px ненефонового контента каждая.
- ПУСТЫЕ СЛОТЫ (НОЛЬ ненефонового контента — чистый фон):
  * x≈84-192   → Save + Save All
  * x≈484-590  → Undo + Redo
  * x≈1442-1554→ macro stop/play
Disabled-кнопки рисуют РОВНО НОЛЬ пикселей. Не тусклый силуэт, не серый квадрат —
полная прозрачность. Твой polarity-фикс (13:39) это НЕ изменил (тот же ноль).

СТОП saturate. Два захода в ILS_SATURATE/mask (12:55 saturate, 13:39 polarity)
дали ноль пикселей. Третий вариант saturate = жечь токены впустую. Меняем подход.

НОВЫЙ ПОДХОД (надёжный, валидный для Windows):
Когда у toolbar НЕТ disabled-imagelist (himlDis) — а у Notepad++ его нет для этих
кнопок — рисуй НОРМАЛЬНУЮ (default) иконку, а не пытайся синтезировать silhouette
через saturate. Windows в таком случае показывает обычный значок (опц. приглушённый
через DrawState/ILD_BLEND). Даже цветная НЕприглушённая иконка = корректно и
бесконечно лучше пустой дырки.
- В toolbar.c TOOLBAR_DrawImage (837-844): ветка disabled без himlDis сейчас идёт в
  draw_masked=TRUE → TOOLBAR_DrawMasked → saturate (даёт ноль). Замени на: рисовать
  default imagelist обычным ImageList_Draw (как enabled), опционально с лёгким
  приглушением (ILD_BLEND50 или DrawState gray). Цель: ВИДИМАЯ иконка в слоте.
- Family: покрой все 6 слотов (Save/SaveAll/Undo/Redo/macro).

ВЕРИФИКАЦИЯ (НЕ ручной кроп — источник твоих ложных verified):
- Объективный per-slot замер на реальном окне: слоты x≈84-192, 484-590, 1442-1554
  должны иметь >0 контента (видимую иконку). Пока хоть один = ноль → НЕ verified.
- Лучше всего: дождись per-slot gate (его доводит отдельный агент) и сверься с ним.
- Журнал: исправь 13:39 Status applied→insufficient (silhouette остался blank),
  новая запись с новым подходом.

КОНТЕКСТ: пользователь устал от итераций по saturate. Это смена тактики, чтобы
ЗАКРЫТЬ: видимая иконка вместо дырки. Не идеальный «затенённый силуэт», но рабочий
и закрывающий. Перфекционизм силуэта потом — сейчас убери пустые дырки.
