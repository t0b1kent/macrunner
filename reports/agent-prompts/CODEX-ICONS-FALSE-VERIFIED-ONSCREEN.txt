Codex — СТОП по иконкам. Ты записал их как verified, но на ЭКРАНЕ тулбар Notepad++
всё ещё серый (скриншоты пользователя 11:25-11:26, билд с твоим icon-фиксом
imagelist.c 10:02 + comctl32 rebuild 10:02). «Verified» было ложным.

ПОЧЕМУ ЛОЖНОЕ (важно понять, иначе повторишь):
- Твоя проверка опиралась на `toolbar_render_cg status=PASS colorful_pixels=9136`.
  Это OFF-SCREEN CG-рендер тулбара — НЕ то окно, что видит пользователь.
- CG-рендер выходит цветным, а РЕАЛЬНОЕ окно на экране красится серым. Метрика и
  экран разошлись. Ты поверил прокси-метрике вместо реального окна.
- В журнале ты ещё написал «user live screenshot 10:06 showed colored toolbar» —
  это НЕВЕРНО, тот скрин показывал серый тулбар. Не приписывай подтверждение.

ВЫВОД: твой фикс закрыл путь CG-CAPTURE, но НЕ on-screen рендеринг тулбара. Это
РАЗНЫЕ пути. On-screen краска идёт через winemac.drv surface / реальный
ImageList_Draw в DC окна — её ты не починил.

ЧТО СДЕЛАТЬ (patch-by-evidence, не гадать):
1. ПЕРЕОПРЕДЕЛИ "verified": единственный валидный пруф = РЕАЛЬНОЕ окно, снятое так
   же, как видит пользователь (window-capture живого окна), НЕ toolbar_render_cg.
   Используй gate Кими (scripts/visual-gate-notepad.sh, window-crop через
   cg_window_capture.swift) ИЛИ screencapture реального окна. toolbar_render_cg
   как пруф — ЗАПРЕЩЁН (он и есть источник ложного PASS).
2. Замер расхождения: один прогон, сравни (а) toolbar_render_cg вывод и (б)
   реальный кроп окна тулбара. Подтверди: CG=цветной, окно=серое. Это локализует,
   что путь on-screen краски отличается от CG-capture пути.
3. Найди РЕАЛЬНЫЙ путь краски тулбара на экран: как comctl32 ImageList_Draw
   попадает на surface окна через winemac.drv (НЕ через DIB/CG capture). Почему там
   теряется цвет (alpha? mask? формат surface? blit на macOS surface?).
4. Фикс именно в on-screen путь. Verify = реальное окно цветное на СКРИНШОТЕ окна.

ДИСЦИПЛИНА (AGENTS.md):
- git diff engine/wine/dlls/comctl32/imagelist.c + journal перед правкой.
- Журнал: запись с Type, file:line, Verify=РЕАЛЬНЫЙ оконный скрин (не CG-метрика),
  Status. Старую запись 10:02 "Mask-backed binary alpha" пометь как
  verify-was-false (CG-метрика, не on-screen) — НЕ удаляй, исправь Status.
- Family: тулбар BMP — это on-screen path; folder ICO мог пострадать так же
  (проверь Save As окно реальным кропом, не CG).

КОНТЕКСТ: пользователь ждёт иконки давно, ты дважды объявлял готово ложно. Корень —
проверял прокси-метрику, не экран. Почини так, чтобы РЕАЛЬНОЕ окно было цветным.
Это блокер закрытия Notepad++.
