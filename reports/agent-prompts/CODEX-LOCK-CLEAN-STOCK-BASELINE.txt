Codex — ты верно заметил: дерево СМЕШАННОЕ (toolbar.c=сток, но imagelist.c=174 строки
дельты, cursoricon.c=10 вставок). Из-за этого визуальные результаты двусмысленны и
кажется «ничего не меняется» — мы меряем на дрейфующей базе. Зафиксируй чистую базу.

ШАГ 1 — ЗАЛОЧИТЬ pure-stock Wine baseline (для чистой атрибуции):
- imagelist.c → верни к чистому upstream: cp reports/upstream-wine-11.0-reference/
  comctl32/imagelist.c engine/wine/dlls/comctl32/imagelist.c
- cursoricon.c → достань upstream wine-11.0 (raw github wine-mirror/wine
  dlls/user32/cursoricon.c) и поставь чистый.
- toolbar.c уже сток.
- Пересобери comctl32+user32, relink ntdll.so, verify-build-freshness.
- ТЕПЕРЬ дерево = ЧИСТЫЙ сток Wine + только HyperBridge. Держи стабильно, НЕ
  возвращай Wine-render патчи во время охоты за иконками.

ШАГ 2 — на этой ЧИСТОЙ базе сделай пинпойнт (из CODEX-PINPOINT-EXACT-ZERO-
INSTRUCTION-STOP-GUESSING.txt):
- Запусти стоковый Notepad++. Иконки чёрные? Теперь это ОДНОЗНАЧНО проблема
  HyperBridge/нижнего слоя (не остаточные патчи).
- Инструментируй ТОЧКУ обнуления: source-buffer ДО (цветной?) → операция → dest
  ПОСЛЕ (ноль?). Дампни PC + опкод-байты + source-адрес + guest-read-vs-host-память.
- Развилка: (A) memory mapping virtual.c зануляет guest-read; (B) инструкция-копир
  мистранслируется (назови опкод); (C) не тот source-буфер. Назови ОДНУ.

ШАГ 3 — почему чистая база критична:
- Сейчас «scrollbar стал лучше» могло быть от Wine-патча, а не HyperBridge — на
  чистом стоке узнаешь правду по КАЖДОМУ элементу.
- Перестань добавлять инструкции по гипотезе (CMPS/LODS visual-unchanged — это тоже
  гадание). Сначала ПИНПОЙНТ на чистой базе, потом фикс подтверждённого.

ПРИНЦИП (AGENTS.md): корректный транслятор гонит СТОКОВЫЙ Wine. Чистый сток = база
истины. Wine-render патчи (imagelist/cursoricon) — костыли, в чистой базе их нет.
После того как HyperBridge починен и сток рисует — патчи выкидываются совсем.

НЕ дрейфуй между стоком и патчами — это и создаёт «ничего не меняется». Залочь сток.
