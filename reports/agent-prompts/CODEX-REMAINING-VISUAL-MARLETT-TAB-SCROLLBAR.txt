Codex — disabled-иконки тулбара ЗАКРЫТЫ (14:24 verified, 14:32 cleanup). Отлично.
Следующее: оставшиеся ВИДИМЫЕ дефекты Notepad++. Все подтверждены на реальном окне
(captures npp-x64-20260521-142539/manual-onscreen-window.png). Приоритет — Marlett.

ВАЖНО про верификацию: старый gate/punchlist помечает Marlett "PASS" — это ЛОЖНО
(lenient non-black проходит на пустой рамке). Реальное окно показывает ПУСТЫЕ
КВАДРАТЫ. Не верь агрегатному non-black. Пруф = строгий per-element gate (его сейчас
ужесточает отдельный агент: глиф в ЦЕНТРЕ кнопки, не периметр) ИЛИ твой зум на центр
caption-кнопок. ЗАПРЕЩЕНО объявлять verified по lenient-метрике (это уже было 3 раза).

=== ДЕФЕКТ 1 (приоритет) — Marlett кнопки окна = ПУСТЫЕ КВАДРАТЫ ===
Min/max/close в правом верхнем углу = пустые рамки, БЕЗ глифа. Ты 11:10 убрал
fallback-overlay и решил, что native Marlett работает — но глиф так и НЕ появился.
Значит native Marlett TextOut реально не рисует символы 0/1/2/r в caption.

МЕТОД (AGENTS.md source-search-FIRST, НЕ слепая трасса):
- grep по именам в caption/NC-paint пути: TextOut/ExtTextOut с Marlett, символы
  '0'/'1'/'2'/'r', NC paint кнопок (win32u/defwnd.c nonclient, user32/nonclient.c,
  uitools.c). Найди где ДОЛЖЕН рисоваться глиф и почему не рисует (шрифт не
  выбирается? glyph index? цвет=фон? clip?).
- Сверь с твоей же 11:10 правкой (git diff/journal): не сломал ли revert overlay
  что-то нужное.
- Гипотеза из кода → ОДНА трасса подтвердить → фикс. Family: все 3-4 кнопки.

=== ДЕФЕКТ 2 — чёрный квадрат во вкладке ===
Во вкладке "new 1" между текстом и крестиком — маленький чёрный квадрат (артефакт).
grep: comctl32 tab owner-draw / close-button imagelist (comctl32/tab.c paint). Тот же
класс icon/imagelist, что ты уже знаешь. Фикс → видимый значок/чистый таб, без чёрноты.

=== ДЕФЕКТ 3 — скроллбар (артефакт + функционал) ===
На пустом доке скроллбара нет. Нужен контент. Проверь: при длинном файле скроллбар
рисуется без чёрных квадратов (частая болезнь — чёрные стрелки), И функционально
двигает viewport. grep: comctl32 scrollbar paint + GetSysColor(COLOR_SCROLLBAR).

=== БАТЧ ===
По batch-diagnostics: если нужна трасса — собери ВСЕ нужные точки (caption paint +
tab + scrollbar) за ОДИН прогон, не по одной. Но сначала grep-локализация каждого.

=== VERIFY (строго, по AGENTS.md) ===
- Пруф = реальное окно, per-element. Marlett: глиф в центре каждой кнопки. Tab: нет
  чёрного квадрата. Scrollbar: визуал + viewport реально двигается.
- Используй строгий gate отдельного агента (он доводит marlett-центр/tab/scrollbar
  чеки). НЕ объявляй verified по lenient non-black.
- Журнал: запись на каждый фикс (file:line, Why, Verify=реальное окно, Status).

КОНТЕКСТ: тулбар закрыт, это последние видимые дефекты до визуально-зелёного
Notepad++. Marlett — самый заметный (в каждом окне). Не торопись с verified —
3 ложных уже было, пользователь это ловит глазом.
