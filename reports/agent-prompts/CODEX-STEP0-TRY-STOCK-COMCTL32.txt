Codex — ШАГ 0 перед разбором 62 патчей: РЕШАЮЩИЙ эксперимент со стоковым comctl32.
Быстрее, чем диффить вручную — сразу скажет, где корень.

ГИПОТЕЗА: CrossOver (стоковый Wine comctl32, без наших патчей) рисует иконки
правильно. Наш imagelist.c = 62 MACRUNNER-патча, toolbar.c = 18. Тест: убрать наши
патчи → стоковый Wine 11.0 comctl32 → собрать → запустить на НАШЕМ HyperBridge.

КАК:
1. Достань upstream Wine 11.0 исходник: dlls/comctl32/imagelist.c, toolbar.c
   (git tag wine-11.0, github wine-mirror/wine). Это РОВНО наша база версии.
2. Сохрани наши текущие (git уже трекает — git diff покажет дельту; сделай ветку/
   копию чтобы вернуть). Замени наши imagelist.c + toolbar.c на СТОКОВЫЕ wine-11.0.
3. Собери comctl32 (как обычно) → запусти Notepad++ → сними тулбар реального окна.

ЧТО СМОТРЕТЬ:
- Стоковый рисует ВСЕ иконки (тулбар полный, disabled серые)? → наши 62+18 патчей И
  ЕСТЬ баг. Дальше: верни наши патчи, классифицируй, откати вредные (саму задачу
  align-to-upstream). Возможно проще: начать со стока и добавить ТОЛЬКО реально
  нужные патчи (для macOS DIB), а не наоборот.
- Стоковый ВСЁ РАВНО сломан (пустые/чёрные)? → корень ГЛУБЖЕ comctl32: win32u DIB-
  конверсия / winemac.drv / HyperBridge нижний слой. Тогда трасса этого слоя, а
  comctl32-патчи были band-aids. Чини реальный слой.

ВАЖНО:
- Это ДИАГНОСТИКА, не финальный фикс. Не теряй наши патчи (git — можно вернуть).
- Часть наших патчей может быть НУЖНА (стоковый Wine падал на нашем DIB-пути изначально
  — потому их и добавили). Тест покажет, нужны ли они ещё или мы их переросли.
- Verify на РЕАЛЬНОМ окне (gate / скриншот окна), не off-screen метрика.
- Сначала всё равно верни рабочий тулбар если он в регрессе, чтобы baseline был чистый.

РЕЗУЛЬТАТ в журнал: "stock comctl32 на HyperBridge → [рисует/не рисует]" + вывод где
корень. Это определит всю дальнейшую стратегию (откатывать патчи vs чинить нижний слой).
