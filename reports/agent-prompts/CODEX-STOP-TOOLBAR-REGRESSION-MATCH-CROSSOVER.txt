Codex — КРИТИЧНО: ты регрессируешь тулбар. Останови.

ФАКТ: на gate 17:00 toolbar был PASS (490k цветных, failed_slots=[]). Сейчас (скрин
18:50) тулбар СНОВА с пустыми иконками. Причина — твои правки comctl32/imagelist.c
ПОСЛЕ 17:00: 17:24 indexed-icon SetDIBits, 18:08 revert, 18:28 remove-GetPixel, и ещё
в 18:59. Ты чинишь marlett/scrollbar/indexed в ОБЩЕМ imagelist.c и ломаешь рабочий
тулбар. imagelist.c — общий путь отрисовки ВСЕХ иконок; правка для одного ломает другое.

ЭТАЛОН (важно): CrossOver на СТОКОВОМ Wine 26 рисует ВСЕ иконки Notepad++ правильно
(пользователь подтвердил) — тулбар полный, disabled серые, после правки становятся
цветными. Значит баг — в РАСХОЖДЕНИИ нашего форка Wine/HyperBridge от upstream Wine,
а не в принципе. Это твоя зацепка.

ПОРЯДОК (анти-трэшинг, не правь дальше вслепую):
1. ВЕРНИ рабочий тулбар. git diff engine/wine/dlls/comctl32/imagelist.c — найди что
   изменилось от состояния, где toolbar был PASS (17:00). Изолируй: твои marlett/
   scrollbar правки НЕ должны трогать toolbar draw path. Откати/огради регресс
   (git checkout конкретных кусков если нужно). После — toolbar снова PASS, не блокер.
2. СВЕРЬ С UPSTREAM WINE (source-search-first, CrossOver = доказательство что работает):
   сравни наш imagelist.c ImageList_DrawIndirect / DIB-конверсию / alpha-blend против
   upstream Wine (github wine-mirror). Найди ГДЕ наш форк divergнул и ломает рендер.
   Не переизобретай — найди расхождение и почини его, чтобы было как у стокового Wine.

ЦЕЛЕВОЕ ПОВЕДЕНИЕ (как CrossOver / настоящий Windows):
- ВСЕ иконки тулбара рисуются (ноль пустых слотов).
- disabled-кнопки (Save без изменений, Undo/Redo, macro) = СЕРЫЙ затенённый силуэт
  (виден рисунок), НЕ пустой квадрат, НЕ полноцвет.
- После правки документа disabled→active: иконка становится ЦВЕТНОЙ. Этот переход
  состояния должен работать (enabled imagelist цветной, disabled — серый из него).
- Чёрный квадрат (в табе ~(1000,324) и где ещё) — убрать. Scrollbar: сам скролл
  работает (пользователь подтвердил), но чёрный квадрат на нём НЕ исправлен — убрать.

VERIFY (строго, не трэшить):
- После КАЖДОЙ правки imagelist.c — убедись gate toolbar остаётся PASS (per-slot,
  реальное окно). Регресс тулбара = откат, не накопление.
- Строгий gate (новый агент перепрогонит): toolbar PASS + marlett_center + tab_black
  + disabled-серый-силуэт. НЕ lenient non-black, НЕ off-screen.
- Журнал: каждая правка + явно отметь "toolbar still PASS" в Verify.

ГЛАВНОЕ: перестань ломать уже зелёное. Тулбар работал — верни. Потом изолированно
доведи marlett/tab/disabled-состояние, сверяясь с upstream Wine (CrossOver доказал
что это рендерится). После каждого шага — gate, чтобы не было новых регрессий.
