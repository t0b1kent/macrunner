Codex — SIMD-семья добавлена (174 теста pass, отлично), но твой вердикт 00:59:
иконки ВСЁ ЕЩЁ чёрные, и новая арифметика НЕ исполняется. Активный blit-путь =
ВЕКТОРНАЯ ПАМЯТЬ: MOVD, PUNPCK, XMM LOAD/STORE (MOVDQA/MOVDQU). Багов missing-опкодов
нет (faults нет). Значит корень — НЕ арифметика. Сузить до: (A) vector-memory ops
дают неверные байты БЕЗ fault, либо (B) данные иконки уже нулевые ДО SIMD (нижний
слой: PE-ресурс → DIB load → surface).

МЕТОД — трассировка ДАННЫХ (не опкодов), differential vs Rosetta:
1. Используй свой MACRUNNER_HB_TRACE_SIMD: на blit-пути иконки дампи XMM-регистры
   ДО и ПОСЛЕ каждого MOVDQU/MOVDQA/MOVD/PUNPCK — реальные байты.
2. Параллельно дампи SOURCE-буфер (откуда читает blit) и DEST-буфер (куда пишет):
   - Если SOURCE уже нули/мусор ДО SIMD → корень НИЖЕ (resource decode / DIB load /
     как гость видит память иконки). Это не SIMD — копай win32u DIB / загрузку
     ресурса / маппинг памяти.
   - Если SOURCE правильный, а после MOVDQU/PUNPCK DEST стал нули → mistranslation
     именно vector-memory op (subtle: выравнивание, порядок байт, 128-bit lane,
     частичная запись). Сверь с sse2neon/Rosetta-выводом на тех же байтах.
3. Differential: тот же blit-код прогнать под Rosetta (CrossOver, правильно) и нами,
   сравнить байты на каждом шаге → где первый раз разошлись = там баг.

ГИПОТЕЗЫ ПО ПРИОРИТЕТУ:
- MOVDQU (unaligned 128-bit) — частичная/смещённая загрузка/запись на unaligned
  буфере иконки (наиболее вероятно для "vector memory").
- PUNPCK — неверный interleave lane (премультипликация alpha распаковкой байт).
- MOVD XMM<->mem — неверный перенос scalar.
- НИЖНИЙ слой: гость читает память иконки неверно (HyperBridge memory mapping /
  win32u DIB), тогда SIMD ни при чём.

VERIFY (сток comctl32 = target, патчи НЕ возвращать):
- Найди где байты впервые портятся → почини (decoder/lift/interpreter ИЛИ нижний
  слой). Сток Notepad++ → иконки цветные = транслятор/слой починен.
- Журнал: где именно портятся данные + фикс.

ВАЖНО: не предполагай что это SIMD — твой же трейс говорит "vector memory ИЛИ ниже".
Сначала ОТВЕТЬ на вопрос: source-данные иконки правильные до SIMD или нет? Это
развилка. dump буфера решает за один прогон.
