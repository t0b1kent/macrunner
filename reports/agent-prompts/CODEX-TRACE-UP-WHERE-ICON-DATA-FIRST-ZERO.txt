Codex — отличная работа: вердикт 03:58 доказал, что vector-memory чист (load/store
ok=186/232, bad=0), и данные иконки УЖЕ НУЛЕВЫЕ ДО SIMD. Корень = до SIMD, в GDI/DIB
слое или более ранней x64-загрузке. Теперь трассируй ВВЕРХ — где данные ВПЕРВЫЕ нули.

ВОПРОС: на каком шаге пиксели иконки ещё правильные, а на каком стали нули?
Пайплайн (трассируй снизу вверх от уже доказанного blit):
  PE-ресурс иконки → декод/LoadImage → CreateDIBSection/DIB буфер → ЗАПОЛНЕНИЕ
  (StretchDIBits/SetDIBits/dibdrv copy source→dest) → blit (доказано ок) → surface.

МЕТОД (тот же data-trace, но РАНЬШЕ blit):
1. Найди где DIB-буфер иконки ЗАПОЛНЯЕТСЯ (win32u/dibdrv copy, SetDIBits, StretchBlt).
   Дампи source (пиксели иконки из ресурса) и dest (DIB) ДО и ПОСЛЕ заполнения.
2. Развилка:
   - SOURCE (ресурс иконки) уже нулевой при чтении → проблема ещё раньше: декод
     ресурса / LoadImage / как гость МАППИТ память ресурса (HyperBridge virtual.c).
   - SOURCE правильный, а после fill DEST нулевой → проблема в fill-коде. А fill —
     это ГОСТЕВОЙ x86 (win32u dibdrv через HyperBridge). Значит ОПЯТЬ возможна
     мистрансляция инструкции, но в FILL-пути (не в финальном blit). Трасса покажет
     какая инструкция в fill даёт нули.
3. Differential vs Rosetta на том же fill-коде: где первый раз байты разошлись.

ДВЕ ГИПОТЕЗЫ (обе проверяемы дампом source/dest на fill-шаге):
A. Native GDI/DIB: win32u dibdrv fill пишет нули (наш патч-band-aid или сток-баг на
   нашем движке). Сверь наш win32u/dibdrv/dib с upstream-11.0.
B. Гостевая инструкция в FILL-пути мистранслируется (как PACKUSWB была, но раньше).
   Включи opcode-fault + SIMD-трассу на fill-диапазоне PC.

КООРДИНАЦИЯ: отдельный агент делает СТАТИЧЕСКИЙ аудит нижнего слоя (icon DIB/blit/
memory путь, virtual.c 82 патча, win32u dib vs сток) → ICON-LOWER-LAYER-AUDIT.md.
Свяжи свою runtime-трассу с его картой — где он флагнул подозрительный код, дампи там.

VERIFY: сток comctl32+win32u = target (патчи не возвращать). Нашёл точку обнуления →
почини (decoder/lift ЕСЛИ гостевая инструкция, ИЛИ нижний слой/маппинг). Сток рисует
иконки = готово. Журнал: ГДЕ именно данные впервые нули + фикс.

ВАЖНО: ты уже отлично сужаешь по байтам — продолжай тем же методом, на шаг раньше.
Ответь: source иконки нулевой при чтении или становится нулём при fill? Это развилка.
