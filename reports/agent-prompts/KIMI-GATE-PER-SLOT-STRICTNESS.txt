Кими — gate заработал (RESULT.md/RESULT-annotated.png, 12:05), оконный захват ок,
молодец. НО он выдал toolbar PASS, хотя на реальном окне остались ПУСТЫЕ серые
квадраты (Save/Undo/Redo/macro disabled-кнопки — без силуэта иконки). Gate их
пропустил → ложный PASS. Это тот же капкан ложной уверенности, теперь в самом gate.

ПРИЧИНА: текущий toolbar-чек меряет АГРЕГАТ (colorful=27094, black_ratio=0.015 по
всей полосе). Пока есть достаточно цветных пикселей суммарно — PASS, даже если часть
слотов пустые. Нужно мерять ПО КАЖДОЙ ИКОНКЕ, а не по сумме.

ЗАДАЧА: ужесточить gate до per-slot проверки тулбара. Disjoint (tools/visual_
regression + profile), read-only по движку. Wine сериализуй (Codex сейчас правит
disabled-иконки — не запускай параллельно, жди очереди / используй wait_for_queue).

ЧТО СДЕЛАТЬ:
1. Сегментируй полосу тулбара на отдельные icon-слоты (фиксированный шаг кнопки,
   напр. ~26px на macOS-рендере, или детектируй разделители/группы). Дай каждому
   слоту bbox.
2. Для КАЖДОГО слота классифицируй:
   - OK: есть содержимое — либо цветное (enabled), либо ЗАТЕНЁННЫЙ силуэт (disabled:
     серый, НО с вариативностью/краями — не плоская заливка).
   - FAIL: пустой/плоский квадрат — однородная заливка без краёв (low variance,
     нет edge-пикселей). Это битая иконка (то, что есть сейчас на disabled).
   Метрика на слот: std/variance яркости + доля edge-пикселей (Sobel/градиент).
   Плоский серый квадрат = низкая variance + ~0 edges → FAIL.
3. toolbar PASS только если ВСЕ непустые слоты OK. Любой плоский слот → toolbar FAIL
   + укажи индексы провалившихся слотов в RESULT.md.
4. Аннотируй RESULT-annotated.png: подсветь FAIL-слоты рамкой (чтобы глазом видно
   какие именно пустые).

ВАЖНО различать (это и есть суть):
- Нормальный disabled (Windows): серый, НО силуэт иконки виден → variance/edges есть → OK.
- Битый (сейчас): плоский серый квадрат, силуэта нет → variance≈0, edges≈0 → FAIL.

VERIFY GATE: прогони на текущем билде. Ожидаемо: toolbar теперь FAIL с указанием
слотов Save/Undo/Redo/macro (подтверди, что совпадает с тем, что видно глазом на
RESULT-annotated.png). Когда Codex починит disabled-путь — те же слоты станут OK.

РАСПРОСТРАНИ принцип per-slot и на остальные иконочные чеки где уместно (folder ICO
в Save As — тоже по слотам, не агрегат). tofu/bands можно оставить как есть.

МЕТОДОЛОГИЯ (AGENTS.md): пруф = реальное окно; gate должен ловить то, что видит
глаз, а не маскировать агрегатом. Family-aware. Wine не параллельно с Codex.
