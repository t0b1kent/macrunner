Продолжение gate-работы. Пользователь поймал 3 визуальных дефекта, которые gate
сейчас пропускает (ложные PASS) или не проверяет. Root: /Volumes/MacOS/MacRunner.
Read-only по движку (НЕ трогай engine/**/*.c — фиксы делает Codex). Wine сериализуй
(pgrep notepad++/wine, wait_for_queue). Пруф = реальное окно (window-capture).

КОНТЕКСТ ПОБЕДЫ: disabled-иконки тулбара ПОЧИНЕНЫ (Codex 14:24 — draw from default
imagelist). Тулбар полный. Перепрогони gate → toolbar должен стать PASS (был FAIL
[5,23-39]; не забудь тюнинг хвоста из прошлого задания).

=== ДЕФЕКТ 1 — Marlett кнопки окна = ПУСТЫЕ КВАДРАТЫ (ложный PASS) ===
Сейчас marlett-чек проходит, если non_black>min. Но кнопки min/max/close — это
ПУСТЫЕ КВАДРАТЫ (только контур-рамка, без глифа _ ◻ ✕ внутри). Контур даёт non_black
пиксели → ложный PASS.
Почини проверку:
- Раздели caption-область на 3 слота (min/max/close). Для КАЖДОГО проверь, есть ли
  ГЛИФ В ЦЕНТРЕ (не только рамка по краю). Метрика: контент в центральной зоне слота
  (внутренние ~60%), а не по периметру. Пустой квадрат = только периметр → FAIL.
- Так gate отличит реальный Marlett-глиф от пустой рамки. Сейчас они = пустые рамки
  → marlett ДОЛЖЕН быть FAIL.

=== ДЕФЕКТ 2 — чёрный квадрат во вкладке ===
Во вкладке "new 1" между текстом и крестиком-закрытия есть маленький ЧЁРНЫЙ КВАДРАТ
(артефакт). Добавь проверку tab-полосы: детектить чёрные заливки (black_ratio высокий
в маленькой зоне) в области вкладки. Чёрный квадрат = FAIL с координатой.

=== ДЕФЕКТ 3 — скроллбар (визуал + функционал) ===
Сейчас документ пустой → скроллбара нет, проверить нельзя. Нужно дать контент:
- В прогоне: вставь/открой достаточно строк, чтобы появился вертикальный скроллбар
  (напр. файл на 200+ строк, или ввод). Используй существующие run-обёртки.
- ВИЗУАЛ: вертикальный скроллбар появился, есть thumb (ползунок), НЕТ чёрного
  квадрата/артефактов (частая болезнь — чёрные квадраты на стрелках скролла).
- ФУНКЦИОНАЛ: проверь, что скроллинг РАБОТАЕТ — после scroll-вниз (PgDn/колесо/
  перетаскивание thumb) верхняя строка уходит из вида (viewport реально двигается).
  Сравни два кадра: до и после скролла — содержимое viewport изменилось = PASS.
  Не изменилось = FAIL (скроллбар нарисован, но не двигает).

=== ВЫХОД ===
- Обновлённый gate, прогнан → RESULT.md с честными вердиктами:
  toolbar PASS (фикс лёг), marlett FAIL (пустые рамки), tab FAIL (чёрный квадрат),
  scrollbar PASS/FAIL (визуал + функционал).
- Все найденные дефекты → swarm/inbox-for-codex/NOTEPAD-FULL-VISUAL-PUNCHLIST.md
  с file:line гипотезой (для Codex — он чинит):
  * Marlett пустые рамки: native Marlett glyph не рисуется в caption (win32u/defwnd
    nonclient paint; Codex 11:10 убрал fallback overlay, но реальный глиф так и не
    появился — значит TextOut Marlett 0/1/2 не рисует).
  * Tab чёрный квадрат: comctl32 tab owner-draw / close-icon imagelist.
  * Scrollbar артефакт: comctl32 scrollbar paint + GetSysColor.

МЕТОД (AGENTS.md): пруф = реальное окно, per-element, центр-зона для глифов (не
периметр), функционал отдельно от визуала. Family-aware. Disjoint output, ноль
правок движка.
