Отличная работа — per-slot gate работает и честно ловит пустые toolbar-слоты
(RESULT 14:16: toolbar FAIL [5,23-39], остальное PASS). Два хвоста до полного,
надёжного покрытия. Root: /Volumes/MacOS/MacRunner. Read-only по движку (engine/**/*.c
НЕ трогай — это лейн Codex). Wine сериализуй: перед запуском pgrep notepad++/wine,
жди если Codex гоняет (wait_for_queue уже есть).

=== ЗАДАЧА 1 — gate должен ТЕСТИРОВАТЬ Save As (сейчас folder_icons SKIP) ===
folder_icons и dialog_* проверки сейчас SKIP, потому что диалог Save As не открыт во
время прогона. Надо открывать его и снимать.
- В прогоне gate: после старта Notepad++ программно открыть Save As (она уже
  поддержана — см. MACRUNNER_NPP_DIALOG_TARGET="saveas" / MACRUNNER_NPP_DIALOG_HOLD_MS
  в существующих run-обёртках; используй их, не изобретай).
- Снять окно диалога (window-capture), и прогнать на нём: folder_icons (иконки папок
  в дереве — цветные, не чёрные/пустые), dialog_listview_header (вертикальные
  разделители колонок File|Size|Type), dialog_treeview.
- Если диалог реально не открылся — это FAIL, не SKIP (отличай "не смог открыть" от
  "не проверяли").

=== ЗАДАЧА 2 — подтюнить toolbar per-slot (ложные FAIL на хвосте) ===
Слоты [23-39] в FAIL — похоже включают ПУСТУЮ область справа от последней реальной
кнопки (тулбар шире, чем число кнопок). Это ложные срабатывания.
- Определи последний РЕАЛЬНЫЙ кнопочный слот (по последней группе с контентом /
  известному числу кнопок NPP) и не проверяй пустой хвост за ним.
- Также отдели легитимные разделители (separators) от пустых слотов.
- Цель: FAIL только на слотах, где ДОЛЖНА быть кнопка, но она пустая (напр. slot 5 =
  Save). Сейчас slot 5 FAIL — это ВЕРНО (disabled Save пуст). Хвост 23-39 — проверь,
  не ложь ли.

=== ВАЖНЫЙ КОНТЕКСТ ===
- Disabled-иконки тулбара (slot 5 и т.п.) РЕШЕНО ПАРКОВАТЬ как known-minor (Codex
  4 попытки, on-screen compositing не даётся, это наименее важный элемент). Так что
  gate ПРАВИЛЬНО показывает их FAIL — это ожидаемый known-minor, не блокер. Не пытайся
  их чинить (ты движок не трогаешь). Просто чтобы gate точно их атрибутировал.
- Можешь пометить known-minor слоты в профиле (напр. "expected_fail"/"known_minor"
  список), чтобы Overall мог быть "PASS-with-known-minor" — это поможет видеть, когда
  ВСЁ ОСТАЛЬНОЕ зелёное. Но FAIL-факт по слоту сохраняй честно.

=== ВЫХОД ===
- Обновлённый gate, прогнанный → RESULT.md с folder_icons + header РЕАЛЬНО
  проверенными в Save As, и без ложных toolbar-FAIL на хвосте.
- Если что-то новое всплыло визуально → допиши в
  swarm/inbox-for-codex/NOTEPAD-FULL-VISUAL-PUNCHLIST.md.

МЕТОД (AGENTS.md): пруф = реальное окно/диалог (window-capture), per-slot/per-icon,
честный confidence. Family-aware. Disjoint output (tools/+reports/+profile), ноль
правок движка.
