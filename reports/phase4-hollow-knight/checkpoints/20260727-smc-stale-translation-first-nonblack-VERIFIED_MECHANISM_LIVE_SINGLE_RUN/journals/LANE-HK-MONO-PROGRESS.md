# LANE HK-MONO — progress journal

2026-07-27 04:02 +10 · lane opened; preserved existing macrunner_hb.c dirty diff (373 additions/3 deletions) · next: map prior live JIT evidence and the actual HK launcher before adding a control-validated lookup trace
2026-07-27 04:07 +10 · static helper audit + report MONO-LOOP-TRACE-INSTRUMENT-DESIGN-20260727.md · two-block helper returns on PC escape; current top trace dumps every budget hit (rate-limit defect); prior pair is run-local return epilogue · next: repair aggregate trace and prove it with a deterministic budget-hit control
2026-07-27 04:17 +10 · helper-loop control initially failed: source edit should suppress post-16 dumps, but linked control emitted calls 17–32; semantic driver also exposed helper yield/re-entry contract · next: force-rebuild libhyperbridge.a (stale-object gate), relink control, then require bounded trace plus 66-block completion
04:12 · BACKEND FAILOVER · `codex` retired after 2 consecutive returns under 180s with no journal line; continuing on `kimi`.
04:20 · COORDINATOR NOTE (not a loop status) · Prior-art brief added: `reports/phase4-hollow-knight/HK-MONO-JIT-PRIOR-ART-BRIEF-20260727.md`, and the lane prompt now carries a summary (the failover loop re-reads the prompt every iteration, so you will see it next thread). Headline: box64 and FEX-Emu both needed Mono-SPECIFIC handling for exactly this JIT-on-JIT problem, and FEX explicitly fixed self-modifying-code handling on their Wine path that "could fix some spurious hangs or incorrect invalidations". Highest-value check, and it costs NO run slot: does a guest `NtFlushInstructionCache`/`FlushInstructionCache` (and whatever Mono's `__clear_cache` lowers to) actually reach HyperBridge's translation-cache invalidation? On the Windows-ARM64 xtajit design we are reimplementing, an icache flush is supposed to call back into the translator to flush cached translations; if ours does not, Mono writes fresh code and we keep executing the stale translation — a thread that is alive, running and never advancing, i.e. our exact shape, and a better explanation of the open question (why a FINITE spin is followed by PERMANENT non-advancement) than the spin itself. Secondary, also cheap: box64 forces `BIGBLOCK=0` and `STRONGMEM=1` for MonoBleedingEdge; Mono's `mono_jit_info_table_find_internal` uses hazard pointers (lock-free retry), which under weak ordering can retry forever against a publish never observed. All of this is EXTERNAL prior art, not our measurement — mark it `[HYPOTHESIS]` and verify against our runtime before acting on it.
04:16 · helper-loop control PASS: reentries=64(+2 initial)=66 blocks, exit=0x3e13, rax/rcx/rdx correct; bounded dump proven (16 dumps for 62 budget-hit calls) · next: audit ledger-V4 macrunner_hb.c diff for existing mono/hash instruments before live run
04:35 · COORDINATOR FINDING (not a loop status) · I ran the offline icache-flush check myself so you do not have to: `reports/phase4-hollow-knight/HK-ICACHE-FLUSH-CALLBACK-IS-A-STUB-20260727.md`. Result: the chain is wired correctly until the last step, then does nothing. `signal_arm64ec.c:771` correctly calls `pBTCpu64FlushInstructionCache(addr, size)`; `xtajit64/cpu.c:198` DISCARDS addr/size and calls the unix side with NULL; `xtajit64/unixlib.c:710 unix_flush_instruction_cache_impl` is `return STATUS_SUCCESS;` — a no-op stub. HyperBridge's `hb_cache_invalidate`/`hb_cache_invalidate_module` (`hb_aot_cache.c:452,473`) have NO production caller (tests only) and are module/version-granular, wrong shape for SMC. `unix_notify_memory_protect_impl` only calls `map_native_range`, so an RWX flip does not clean the cache either. `xtajit64` is confirmed live in HK runs (286 log mentions). PROVEN: a guest icache flush invalidates nothing anywhere in our stack. NOT PROVEN, and this is your job: that HK actually re-executes a stale translation as a result — the instrument is "after Mono writes code into an executable page, is a PREVIOUSLY TRANSLATED block covering that page executed again rather than re-translated?". If yes it explains the open question (finite spin, permanent non-advancement) better than the spin does. Fix shape is sketched in the report; do not treat anything as fixed until the ordinal-200 readback goes non-black.
04:32 · OFFLINE VERDICT: stale-translation mechanism confirmed by code reading — block_cache hit (hb_runtime.c:3818) has NO byte re-verify; guest NtFlushInstructionCache reaches unix virtual.c:8571 but never notifies HB; only invalidation is per-callback hb_jit_runtime_reset, none inside steady-state run_x64 · report MONO-SMC-STALE-TRANSLATION-MECHANISM-20260727.md · next: byte-hash reverify detector+fix in block_cache hit path, deterministic control first
04:52 · COORDINATOR FINDING #2 (not a loop status) · `reports/phase4-hollow-knight/HK-ARM64-BOOTCAMP-EXTRACTION-20260727.md` — read it, it changes the shape of your question. Source is Mihocka's ARM64 Boot Camp; he is one of the Microsoft engineers who built xtajit/xtajit64/xtabase, so this is the spec of the contract we implement, not commentary. THREE things that matter to you: (1) He MEASURES the icache-flush callback: adding one `FlushInstructionCache()` per loop iteration costs 50x for emulated x64 and 300x for native ARM64 — so a correctly wired translator does substantial work there, and ours (finding #1) does literally nothing. (2) He quantifies "the cost of JIT is on the order of 10000 clock cycles for even the most trivial block of x64 code" and calls JIT-on-JIT the known-hard case. This gives a SECOND explanation of our ~15 min spin that needs NO livelock: a re-translation storm, where the thread is doing real work the whole time. The missing invalidation can produce EITHER stale execution OR a translation storm — they are opposite failure modes of the same gap, and our evidence (finite spin, then presents resume but managed state frozen) fits a storm followed by stale execution. DISCRIMINATE THEM, do not pick one: after Mono writes code into an executable page, is a previously translated block re-executed, or re-translated, and how often? (3) Microsoft's own workaround for JIT-on-JIT is to INTERPRET one of the two levels — switching xtajit64 to xtabase measured FASTER (8726ms vs 10812ms), and he wishes it had been a hotspot-style hybrid. We already have that path: `MACRUNNER_HB_JIT_DIRECT_MEM=0`. Running Mono-generated code interpreted is both a correctness workaround (no translation to go stale) and possibly faster — that is a candidate FIX, not just a probe. Also checked so you do not have to: `VirtualAlloc2` + `MEM_EXTENDED_PARAMETER_EC_CODE` is NOT a gap in our tree (honoured in production at `ntdll/unix/virtual.c:6481,6544`), and the EC bitmap exists (`virtual.c:3498`). Sections 1-4 of the brief are his measurements on WoA hardware, not ours; section 5 is our code checked first-hand today; the storm reframing is `[HYPOTHESIS]`.
04:47 · SMC reverify implemented+proven: byte-hash on block_cache hit, RWX-only tracking, evict+retranslate on mismatch · control PASS both modes (enabled: evict exactly 1 on byte overwrite, RX untracked; disabled: zero activity) · test suite 453/30 (baseline 445/38, no regression) · helper-loop control still PASS · next: write report, rebuild ntdll.so, live HK run with MACRUNNER_HB_TRACE_SMC_REVERIFY
04:53 · ntdll.so rebuilt+installed+codesigned (dist 04:52, strings gate: smc-reverify present, arm64 Mach-O OK) · live run LAUNCHED: laneA-run-hk.sh smc-reverify-1 TMO=3000 TRACE_SMC_REVERIFY=1 PRESENT_SURFACE_READBACK ordinal=200 HELPER_LOOP_TOP=1 (task bash-uukg9c0n) · next: +2min log-growth check, then monitor boundary markers
04:56 · run1 INVALID_ORCHESTRATION: preflight BLOCKED path_absent (fresh prefix lacked save snapshot; oracle template not wired) — no product conclusion · relaunched smc-reverify-2 with MACRUNNER_MR_RUN_PREFIX_TEMPLATE=hk-windows-oracle-prefix-template-20260721-225722-v3 + SAVE_PATH (task bash-eetkx1h1) · next: +2min growth gate
05:02 · run3 LIVE+VALID (Mono path seen, 16k lines growing, contract READY with template+save+3 zero-branches) · bounded traces firing (helper-top total_calls=60.7M at +137s, aggregate dumps OK) · smc evict lines=0 so far (still pre-level-start boot) · next: monitor for Performing automatic level start → spin window evictions
05:21 · run3 SUPERSEDED (no positive liveness signal for smc instrument; helper-top mutex 665M acquisitions skewed timing) · added bounded progress aggregate (reverified=1/1000/2^24), control re-PASS, ntdll relinked+verified 05:20 · run4 LAUNCHED sans helper-top (task bash-9nye9ysf) · next: growth gate then level-start evict window
05:35 · CRITICAL self-fix: smc hook was on hb_jit_runtime_run_legacy, live run uses SINGLE_LOOKUP hb_jit_runtime_run — instrument was inert live (exactly the WRONG_HELPER_STATE failure class) + found UAF (evict frees cached->block borrowed by fastpath) · fixed hook order, control now PASS on BOTH dispatch paths + disabled · suite 455/28 · ntdll 05:34 verified · run5 LAUNCHED (bash-uvo4b707) · next: progress line must fire by first reverify
05:38 · MECHANISM CONFIRMED LIVE: run5 progress lines tracked=15/reverified=1 @+48s, tracked=288 reverified=1000 EVICTED=3 @+55s — guest code bytes ARE rewritten behind cached translations during boot; stale-translation is real, not hypothetical · next: evict count through level-start spin window + oracle markers
05:54 · REGRESSION DETECTOR FLIPPED: run5 ordinal-200 readback @+1017s hash=0xf7ea9ca044b2fdb5 (≠ black 0xc770038f717d0383), nonblack=11968/786432 grayscale max=255 (prior runs: nonblack=0 byte-identical ×2) — first non-black presented surface through real pipeline; evicted=2972 stale translations repaired by then · still pre-level-start (marker count=0) · next: level-start window evict totals + oracle post-level markers
06:29 · run5 CLOSED exit=124 VALID_RUN · report MONO-SMC-REVERIFY-RUN5-LIVE-VALIDATION-20260727.md: mechanism PROVEN live (3295 evictions, Mono-trampoline clusters), detector flipped (nonblack=11968 @ordinal200), stall residue is NOT stale-translation (0 evictions post-+1017s, instrument live, 83.9M reverifies) · run6 7200s A/A+advancement launched (bash-gb2cg446) · next: level-start? then spin stack re-capture on fixed build
06:45 · COORDINATOR DIRECTIVE (not a loop status) · Excellent work on run5 — catching your own WRONG_HELPER_STATE (hook on `hb_jit_runtime_run_legacy` while the live path is SINGLE_LOOKUP `hb_jit_runtime_run`), proving the control on BOTH dispatch paths plus disabled mode, fixing the eviction UAF, and then explicitly refuting "the stall is stale translation" with your own live instrument is exactly the standard this project needs. Three things for after run6. (1) PERSIST THE RAW READBACK. I went to look at run5's non-black frame myself and there is no pixel data on disk — only the aggregate line. `nonblack=11968, grayscale R=G=B, max=255` with `[HYPOTHESIS: Team Cherry logo]` can be turned into a fact for the cost of one 3 MB file (1024x768x4). Dump the ordinal-200 RGBA buffer to the run directory so the content can be rendered and identified offline; `tools/analyze_hk_frame_dump.py` already exists for exactly this. Do it in run6 if you still can, otherwise run7. (2) NEXT EXPERIMENT IS THE INTERPRETER — the operator asked for it directly. `MACRUNNER_HB_JIT_DIRECT_MEM=0`. Your own residue item 2 already points there for the memory-ordering candidate, but note it is a stronger move than that: per the prior-art brief, Microsoft's own documented workaround for JIT-on-JIT is to interpret one of the two levels, and Mihocka MEASURED the interpreter (xtabase) beating the JIT (xtajit64) on exactly this shape, 8726ms vs 10812ms. So the interpreter variant attacks BOTH open problems at once — the ordering candidate for the remaining spin, AND the >88x wall-clock gap versus the oracle's 33.9s, which run5 made worse (reverify ~2x on dispatch, and run5 never reached `Performing automatic level start.` where prior runs did). Interpreted Mono code also cannot go stale, so it is a correctness workaround as well as a speed one. (3) Keep the A/A discipline you already have: run6 must stay an identical build so the non-black readback is confirmed reproducible before anyone treats it as real. It is single-run today and you said so yourself — hold that line.
06:49 · A/A PASS: run6 ordinal-200 readback @+985s byte-identical to run5 (hash 0xf7ea9ca044b2fdb5, nonblack=11968) — non-black surface reproducible, warm cache ~30s faster · window capture @+710s black (pre-readback, consistent); @+1020s black — logo frame transient at ~5s/present; periodic capture loop armed (bash-2fe1yos5) · AGENTS.md updated (live-path instrument rule + SMC knobs) · next: identify content, watch level start
