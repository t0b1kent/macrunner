# DXMT D3D11/DXGI Coverage Matrix

Generated by Lane D on 2026-06-02 from `engine/dxmt/**` and `engine/graphics/**`.
Updated on 2026-06-04 after the isolated DXMT smoke prefix covered Unity device
probes, resource breadth, shader/draw/dispatch, per-instance input, indirect
calls, expanded state/query probes, deferred resource command-lists, MSAA
render/resolve, shader corpus coverage, Hollow Knight DXBC corpus extraction,
asset-aware Hollow Knight Unity ShaderProgram DXBC extraction, bitblt/flip
sequential/discard swapchain variants, live-window present, visible fullscreen enter/restore with
display capture, per-swapchain `ResizeTarget` plus resize/readback, append-UAV counter copy,
multi-UAV, buffer/texture SRV-to-UAV, texture-array UAV, and groupshared atomic compute readbacks, two-pass long-run stability, and vkd3d prefix deployment. The x64
guest binding smoke now defaults to strict native `winemetal=n`, rejects the old
Unixlib `c0000135` failure signature, and still records the post-TSO runtime
timeout before D3D/DXGI markers.
Status is evidence-based: `Implemented` means an entry point exists and routes to
DXMT/Metal code; `Partial` means the path exists with known limits; `Gap` means
the current code returns `E_NOTIMPL`, `DXGI_ERROR_*`, or has no owned smoke yet.
Partial/Gap audit on 2026-06-04: no matrix table rows remain in `Partial` or
`Gap` status; `Clean unsupported`, `device-gated`, and `filter-only` rows are
intentional API/device semantics, not open user-mode implementation gaps.

## Phase 1 Gate

| Gate | Status | Evidence |
|---|---:|---|
| `D3D11CreateDevice` | Implemented | `src/d3d11/d3d11.cpp` creates DXGI factory/adapter, selects FL, returns device/context. |
| Feature levels 10_0-11_0 | Implemented | Default probe list includes 11_0, 10_1, 10_0. |
| Feature level 11_1 | Implemented | Current headless smoke returns `feature_level=0xb100`; `UnityFeatureLevelProbe request=11_1 chosen=0xb100`, with 11_0/10_1/10_0 fallbacks green. Cap override remains available via `d3d11.maxFeatureLevel`. |
| `CreateDXGIFactory*` | Implemented | `src/dxgi/dxgi_factory.cpp` exports `CreateDXGIFactory`, `1`, `2`. |
| Adapter enumeration | Implemented | `EnumAdapters/EnumAdapters1` enumerate `WMT::CopyAllDevices()`. |
| WARP adapter | Implemented | `EnumWarpAdapter` returns the default DXMT Metal adapter as the WARP fallback; this is not a CPU WARP rasterizer. The smoke now also creates a `D3D_DRIVER_TYPE_WARP` device through that fallback (`UnityWarpProbe result=PASS`, FL 11_1) in `artifacts/dxmt-smoke-logs/lane-d-warp-device-20260604-140424.outer.log`. |
| `CreateSwapChain` / `ForHwnd` | Implemented | Legacy desc converts to desc1 and routes through `IMTLDXGIDevice::CreateSwapChain`. |
| CoreWindow/composition swapchains | Clean unsupported | `CreateSwapChainForCoreWindow` and `CreateSwapChainForComposition` return `DXGI_ERROR_UNSUPPORTED` with null swapchains for valid owned-device inputs and `DXGI_ERROR_INVALID_CALL` for invalid inputs; smoke reports `unsupported_swapchains=PASS`. |
| Backbuffer `GetBuffer` | Implemented | Valid logical indices below `BufferCount` query the DXMT backing texture; out-of-range indices return `DXGI_ERROR_INVALID_CALL` with null output. Bitblt and flip smoke verify `GetBuffer(1) hr=0x00000000` and `GetBuffer(2) hr=0x887a0001`. |
| RTV clear | Implemented | `ClearRenderTargetView` emits DXMT render clear commands. |
| Present | Implemented | `IDXGISwapChain::Present` routes to `Present1`/Presenter. |
| Pixel readback smoke | Added | `tests/dx11/dx11_headless_smoke.cpp` copies backbuffer to staging and maps pixel 0. |

## Phase 1 Runtime Evidence

| Probe | Result | Evidence |
|---|---:|---|
| DXMT smoke build | PASS | `engine/graphics/scripts/run_dxmt_d3d11_headless_smoke.sh aarch64` builds `dx11_headless_smoke.exe`, `d3d11.dll`, `dxgi.dll`, `winemetal.dll`, and `winemetal.so`. |
| Isolated prefix install | PASS | Runner owns `artifacts/dxmt-smoke-prefix/`, installs built DLLs into `system32`, the app dir, and a prefix-local builtin overlay. |
| Mixed DXMT binding | PASS | `bind_mode=mixed`, `WINEDLLOVERRIDES=d3d11,dxgi=n;winemetal=b,n`; loaded paths point to the prefix overlay for `winemetal.dll`, `dxgi.dll`, and `d3d11.dll`. |
| x64 DXMT PE binding | PASS | `engine/graphics/scripts/run_dxmt_x64_binding_smoke.sh` now defaults to `WINEDLLOVERRIDES=d3d11,dxgi,d3d10core,winemetal=n`; readiness recheck `artifacts/dxmt-x64-binding/run-20260604-165927/dxmt-x64-binding.log` reports `dxmt_sync=PASS`, native `winemetal.dll`/`DXGI.DLL`/`d3d11.dll` loads, `unixlib_binding=PASS`, and `present_reached=NO` with timeout exit before D3D/DXGI markers. Current runtime still stops before present in HyperBridge, outside Lane D. |
| DXGI factory/adapter probe | PASS | `CreateDXGIFactory1(probe) hr=0x00000000`; `IDXGIFactory::EnumAdapters(0 probe) hr=0x00000000`; `IDXGIFactory4::EnumWarpAdapter hr=0x00000000` returns a non-null adapter and `GetDesc hr=0x00000000`. |
| Adapter/output enumeration | PASS | `EnumAdapters1`, `GetDesc1`, `EnumOutputs`, `GetDesc`, and `GetDisplayModeList` pass; first mode `640x480`, fetched `3` modes. |
| Output gamma/fullscreen state probes | PASS | `UnityGammaProbe` covers gamma capabilities, set/get/restore gamma ramp, display-surface unsupported cases, `GetFullscreenDesc`, `GetFullscreenState`, `SetFullscreenState(FALSE)`, and opt-in forced-message `SetFullscreenState(TRUE)` plus restore. |
| D3D11 device probe | PASS | `D3D11CreateDevice hr=0x00000000`; `feature_level=0xb100`. |
| BGRA render-target support | PASS | `CheckFormatSupport(B8G8R8A8_UNORM) hr=0x00000000`; `format_support=0x02fef3f3`. |
| Unity core format probes | PASS | `R8G8B8A8`, `B8G8R8A8`, `R16G16B16A16_FLOAT`, `R11G11B10_FLOAT`, `R32_FLOAT`, `R16_FLOAT`, `R8_UNORM`, `D24S8`, and `D32_FLOAT` meet required Texture2D/sample/RT/depth flags. |
| Unity optional BC probes | PASS | All optional BC1-BC7 variants report Texture2D + shader-sample support on this Metal device. |
| Unity optional YUV 4:2:2 probes | PASS | `R8G8_B8G8_UNORM` and `G8R8_G8B8_UNORM` report Texture2D + shader-sample support (`support=0x00d233f1`, required `0x00000220`) in `artifacts/dxmt-smoke-logs/lane-d-yuv422-20260603-212449.outer.log`. |
| Unity sRGB encode/decode probes | PASS | `UnitySRGBProbe` verifies sRGB RTV clear encode (`staging_rgba=137,0,255,255`); `UnitySRGBSampleProbe` verifies sRGB SRV shader decode to a linear target (`sampled_rgba=63,63,63,255`) in `artifacts/dxmt-smoke-logs/dx11-headless-aarch64.log`. |
| Unity feature probes | PASS | `THREADING`, expanded `D3D11_OPTIONS`, `ARCHITECTURE_INFO`, `DOUBLES`, `D3D10_X_HARDWARE_OPTIONS`, and MSAA quality matrix now fail the smoke if advertised required bits drift; `artifacts/dxmt-smoke-logs/lane-d-cbuffer-offset-20260604-132736.outer.log` reports `UnityFeatureProbe result=PASS`, D3D11.1 option bits including `cb_offset=1`, `cb_partial=1`, `cb_no_overwrite=1`, `copy_overlap=1`, and 1/2/4x MSAA quality with 0 quality for 8x on this Metal device. |
| Unity advanced interface probes | PASS | Two-pass stability run `artifacts/dxmt-smoke-logs/lane-d-stability-20260604-125404.outer.log` has both per-run logs reporting `UnityAdapterNotificationProbe`, `UnityOutputCapabilityProbe`, `D3D11On12Probe`, `UnityDeviceInterfaceProbe`, `UnityFenceProbe`, `UnityTiledProbe`, `UnityResidencyProbe`, and `UnityMRTProbe` as `PASS`; notable checks include native unsupported semantics for D3D11On12/tiled-resource calls, fence signal/wait, residency offer/reclaim, and MRT red/blue readbacks. |
| Unity resource probes | PASS | Color texture + SRV/RTV/update/copy/readback, dynamic vertex buffer map + `WRITE_NO_OVERWRITE`, dynamic constant buffer map + `WRITE_NO_OVERWRITE`, default constant-buffer partial `UpdateSubresource` boxed update + staging readback, same-buffer overlap `CopySubresourceRegion` + staging readback, immutable constant buffer, depth DSV, R32_UINT/R32_FLOAT UAV clears with staging readback, 3D texture SRV, cubemap SRV, Texture2DArray SRV/RTV, Texture2DArray mip-slice update/copy/readback, and structured buffer SRV/UAV all return success; both runs in `artifacts/dxmt-smoke-logs/lane-d-stability-uav-clear-20260604-161143.outer.log` report `uav_clear_uint_word=0x1234abcd` and `uav_clear_float_value=2.500`. |
| Depth format family probe | PASS | Resource smoke creates DSVs and clears `D24_UNORM_S8_UINT`, `D16_UNORM`, `D32_FLOAT`, `D32_FLOAT_S8X24_UINT`, plus typeless `R24G8_TYPELESS` with DSV/SRV views; advertised D3D11.1 depth-only `ClearView` full/rect DSV calls pass in both runs of `artifacts/dxmt-smoke-logs/lane-d-stability-refresh-20260604-135717.outer.log`. |
| BC texture family probe | PASS | Resource smoke keeps the detailed BC1 copy matrix and now creates/SRVs/copies to staging/maps/byte-verifies every advertised BC1 sRGB, BC2, BC3, BC4, BC5, BC6H, and BC7 variant in `artifacts/dxmt-smoke-logs/lane-d-bc-family-summary-20260603-213548.outer.log`. |
| MSAA resolve family probe | PASS | 2x/4x `R8G8B8A8_UNORM`, `B8G8R8A8_UNORM`, and `R16G16B16A16_FLOAT` create MSAA RTVs, resolve to single-sample textures, copy to staging, and byte-verify in `artifacts/dxmt-smoke-logs/lane-d-msaa-family-20260603-214144.outer.log`; 4x color+depth resolve/readback remains green. |
| Unity shader/draw probes | PASS | Dynamic `d3dcompiler_47` load, VS/PS/GS compile, GS stream-output creation, shader creation, input layout with per-instance slot, vertex/index/instance/indirect/SO buffers, fullscreen draw, texture-sample draw, sRGB sample/decode draw, present, `SOGetTargets` bind/unbind state roundtrip, stream-output readback, and green-pixel readback pass. Evidence: `artifacts/dxmt-smoke-logs/lane-d-so-roundtrip-20260604-130834.outer.log`. |
| Unity shader corpus probe | PASS | `UnityShaderCorpusProbe` compiles/creates `vs_vertex_id`, `vs_instance_matrix`, `ps_texture_array_clip`, `ps_derivative_frontface`, and `cs_texture_intrinsics`; this also covers DXBC `UNDEFINED` interpolation as flat for system-value inputs. |
| Hollow Knight DXBC corpus smoke | PASS | `engine/graphics/scripts/run_hk_dxbc_airconv_corpus_smoke.sh aarch64` extracted 24 standalone DXBC containers from Unity default resources and translated all 24 through native `airconv -S`; HK `resources.assets`, `sharedassets0.assets`, and `globalgamemanagers.assets` contain DXBC markers but need Unity asset-aware extraction before they count as real-game shader corpus coverage. |
| Hollow Knight Unity shader DXBC smoke | PASS | `engine/graphics/scripts/run_hk_unity_shader_dxbc_smoke.sh aarch64` parses Unity SerializedFile v22 Shader objects, LZ4-decompresses D3D11 platform blocks, extracts DXBC from ShaderSubProgram program code, and validates 128 unique HK `resources.assets` DXBC blobs through native `airconv -S` with 0 failures. |
| Unity state/draw variants | PASS | Alpha/opaque/independent blend, cull-none/cull-back/wireframe-front rasterizers including no-depthclip, depth/stencil and disabled depth, linear/comparison/anisotropic samplers, viewport/scissor set-get, and state-reset isolation pass; `Draw`, `DrawIndexed`, `DrawInstanced`, `DrawIndexedInstanced`, `DrawInstancedIndirect`, and `DrawIndexedInstancedIndirect` execute with `IASetVertexBuffers(instance_slot)=PASS` before present/readback. |
| Unity compute/dispatch probes | PASS | CS `cs_5_0` texture UAV, typed-buffer UAV, structured-buffer UAV, multi-UAV structured-buffer, structured SRV-to-UAV, texture SRV-to-UAV, texture-array UAV, groupshared/interlocked structured UAV, and constant-buffer offset shaders compile/create; direct `Dispatch`, `DispatchIndirect`, `CSSetShaderResources(t0)`, `CSSetConstantBuffers1(first=2,count=1)` + `CSGetConstantBuffers1`, UAV-to-staging copy/map, append-UAV hidden counter set plus `CopyStructureCount`, `staging_value=42`, `typed_buffer_values=100,101,102,103`, `structured_buffer_values=200,201,202,203`, `multi_uav0_values=400,401,402,403`, `multi_uav1_values=500,501,502,503`, `srv_uav_values=617,618,619,620`, `texture_srv_uav_value=707`, `texture_array_uav_values=800,801`, `groupshared_atomic_values=10,65261,48879,4660`, `constant_offset_values=300,301,302,303`, and `append_buffer_count=4` all pass in both runs of `artifacts/dxmt-smoke-logs/lane-d-stability-texture-array-uav-20260604-204240.outer.log`. |
| vkd3d HLSL structured UAV store | PASS | Active D3D12 build source is `engine/vkd3d-proton`, which has no Wine `hlsl_codegen.c` structured-store fixme; `engine/graphics/scripts/run_vkd3d_prefix_sync_smoke.sh` records this as `structured_store_source=PASS mode=active_source_no_hlsl_codegen` in `artifacts/vkd3d-prefix-sync/run-20260604-165527`. Fallback `engine/vkd3d` and the Wine vendored vkd3d compiler now lower whole-element `RWStructuredBuffer<T>` writes to `VSIR_OP_STORE_STRUCTURED` with a zero byte offset and include the matching TPF `STORE_STRUCTURED` encoder case; rebuilt `d3dcompiler_47.dll`/`wined3d.dll` dist binaries carry no stale structured-store fixme, and the headless smoke compiles `cs_structured_buffer_5_0` with `hr=0x00000000`. |
| vkd3d HLSL append-counter syntax | Compiler backlog | API-level append UAV counter creation, initial-count set, and `CopyStructureCount` are green in the DXMT runtime smoke. Shader-level append/consume counter syntax is not in the always-on smoke yet because vkd3d HLSL rejects the current probes before runtime: `AppendStructuredBuffer<uint>` failed in `artifacts/dxmt-smoke-logs/lane-d-append-buffer-20260604-162953.outer.log` with `Unknown modifier "AppendStructuredBuffer"`, and `RWStructuredBuffer<uint>.IncrementCounter()` failed in `artifacts/dxmt-smoke-logs/lane-d-append-counter-20260604-163126.outer.log` with `Method 'IncrementCounter' is not defined on type 'RWStructuredBuffer<uint>'`. TPF/VSIR already has `IMM_ATOMIC_ALLOC`/`IMM_ATOMIC_CONSUME`, so the next root fix is HLSL parser/method/lowering work, not a DXMT Metal runtime failure. |
| Unity tessellation/class linkage probes | PASS | `UnityTessellationProbe` compiles `hs_5_0`, `ds_5_0`, `vs_5_0`, and `ps_5_0`, creates VS/HS/DS/PS objects, and exercises class linkage/name queries. |
| Unity query/deferred probes | PASS | `CreateQuery(EVENT)`, `GetData(EVENT)`, `CreateQuery(TIMESTAMP)`, timestamp readback, `CreateQuery(OCCLUSION)`, occlusion `GetData=0`, `CreatePredicate(OCCLUSION)`, predication bind/clear, `CreateDeferredContext`, `FinishCommandList`, and `ExecuteCommandList` all pass. |
| Unity deferred resource probes | PASS | `UnityDeferredResourceProbe` records `UpdateSubresource` texture/box/default-buffer commands, dynamic `Map`/`Unmap`, copy-to-staging, `FinishCommandList`, immediate `ExecuteCommandList`, and post-execute readbacks. |
| Unity deferred draw probe | PASS | Deferred context records `ClearRenderTargetView`, finishes/executes command list, and readback returns `pixel0_bgra=255,0,64,255`; deferred full draw command recording/execution returns green `draw_pixel0_bgra=0,255,0,255`. |
| Unity MSAA/depth resolve probe | PASS | `UnityMSAAProbe` creates 4x color/depth textures, creates an MSAA DSV, clears color/depth, resolves to single-sample texture, and validates staging readback. |
| Swapchain variant probes | PASS | `DXGI_SWAP_EFFECT_DISCARD`, `SEQUENTIAL`, `FLIP_SEQUENTIAL`, and `FLIP_DISCARD` smoke swapchains all create, pass `Present(DXGI_PRESENT_TEST)`, `Present1(DXGI_PRESENT_TEST + dirty rect)`, `Present1(dirty)`, read back `pixel0_bgra=0,255,0,255`, `ResizeTarget(60hz)`, null `ResizeTarget` invalid-call validation, then `ResizeBuffers(half)` and post-resize present/readback with `resized_pixel0_bgra=255,0,0,255`; all variants report `result=PASS` in `artifacts/dxmt-smoke-logs/dx11-headless-aarch64.log`. |
| Live-window present smoke | PASS | `engine/graphics/scripts/run_dxmt_d3d11_live_window_smoke.sh aarch64` reaches real HWND present/readback in the owned prefix; CG capture confirms a nonblack host window shell on this host. |
| Visible fullscreen display smoke | PASS | `engine/graphics/scripts/run_dxmt_d3d11_fullscreen_smoke.sh aarch64` reaches visible HWND, `SetFullscreenState(TRUE) hr=0x00000000`, `fullscreen_after_true=1`, restore success, `pixel_readback=PASS`, and a display capture of `3024x1964` with nonblack/colorful pixels. |
| Phase 6 stability smoke | PASS | Expanded suite after Texture2DArray mip/slice update-copy, sRGB sample/decode, typed-buffer + structured-buffer + groupshared/interlocked compute, constant-buffer offsetting, partial constant-buffer update, overlap buffer copy, UAV clear readbacks, append-UAV counter copy, query lifecycle pending checks, depth-only `ClearView`, swapchain-effect/`ResizeTarget`/resize variants, present-test, and state-breadth additions ran 2 passes with 60s minimum stability each in `artifacts/dxmt-smoke-logs/lane-d-stability-groupshared-atomic-20260604-162315.outer.log`; both per-run logs passed `UnityWarpProbe result=PASS`, `groupshared_atomic_values=10,65261,48879,4660`, `UnityComputeProbe result=PASS`, final readback/frame statistics, and `UnityStabilityProbe result=PASS`; the outer summary ends with `exit_code=0`. |
| vkd3d prefix deployment | PASS | `engine/graphics/scripts/run_vkd3d_prefix_sync_smoke.sh` verifies `d3d12.dll`, `d3d12core.dll`, `dxgi.dll`, and `winemetal.dll` in isolated aarch64/x64 prefixes byte-match `engine/graphics/dist`; run `artifacts/vkd3d-prefix-sync/run-20260604-165527` also asserts active source selection, PE machine architecture for vkd3d plus DXMT dependency DLLs, `dxgi.dll` factory exports, `d3d12core.dll` exports, and the `d3d12.dll` D3D12 factory/root-signature exports. The same run includes an aarch64 strict-prefix Wine runtime loader gate with `d3d12,d3d12core=n`, verifies copied native `d3d12core.dll` binding, and exercises legacy plus versioned D3D12 root signature semantic roundtrips: `D3D12SerializeRootSignature`, `D3D12CreateRootSignatureDeserializer`, `D3D12SerializeVersionedRootSignature`, and `D3D12CreateVersionedRootSignatureDeserializer` preserve a v1.1 constants + static-sampler signature (`vkd3d_runtime_versioned_rootsig_result=PASS`). The runtime probe still has an opt-in `VKD3D_RUNTIME_PROBE_DEVICE=1` device diagnostic; current opt-in evidence `artifacts/vkd3d-prefix-sync/run-20260604-135307/runtime-loader-aarch64-windows.log` shows `D3D12CreateDevice hr=0x80004005` through builtin `winevulkan`, so device bring-up remains backend work, not a native-DLL binding regression. |
| Headless Metal layer fallback | PASS | When Wine macdrv Metal-view symbols are unavailable, `winemetal` creates a retained fallback `CAMetalLayer` for the smoke HWND. |
| Swapchain/present | PASS | Message HWND path prints `CreateSwapChainForHwnd hr=0x00000000`, `IDXGISwapChain::GetBuffer hr=0x00000000`, `Present hr=0x00000000`. |
| RTV clear/readback | PASS | Swapchain backbuffer clears to RGBA `(0.125,0.5,0.875,1.0)`; readback prints `pixel0_bgra=223,128,32,255`, `pixel_readback=PASS`. |
| Strict native `winemetal=n` | Implemented | x64 guest binding is strict-native green: the Lane D runner skips automatic prefix-update wineboot for this focused probe, loads the synchronized x64 DXMT PE DLLs with `winemetal=n`, and parser-gates against `winemetal_init_unix_call`, `__wine_init_unix_call`, `c0000135`, and `LoadLibraryExW(winemetal)` regressions. Evidence: `artifacts/dxmt-x64-binding/run-20260604-165927/dxmt-x64-binding.log`. |
| Top-level HWND creation | Implemented | Owned live/fullscreen smokes create non-null overlapped Wine HWNDs and present through them; stale `GetLastError=1400` after a successful `CreateWindowExW` is ignored. Message-HWND fallback remains available for headless coverage. |

## D3D11 Device Surface

| Family | Status | Evidence / gap |
|---|---:|---|
| Buffers | Implemented | DEFAULT/IMMUTABLE/DYNAMIC/STAGING creation paths, including dynamic vertex and dynamic constant-buffer `WRITE_NO_OVERWRITE` maps, default constant-buffer partial update/readback, same-buffer overlap copy/readback, and shader-visible constant-buffer offsetting via `CSSetConstantBuffers1`. |
| Textures 1D/2D/3D | Implemented | Texture creation routes through device, staging, linear, dynamic helpers. |
| SRV/RTV/DSV/UAV | Implemented | View creation normalizes descriptors and creates Metal texture views. |
| VS/PS/CS | Implemented | Pipeline cache add paths exist. |
| GS/HS/DS | Implemented | Headless smoke compiles/creates GS, GS+stream-output, HS, and DS shaders; GS draw passes, HS/DS patch draw readback returns `pixel0_bgra=0,255,0,255`, and standalone Unity/HK D3D11 DXBC corpora translate through `airconv`. |
| Stream output | Implemented | Smoke creates SO layout/buffer, binds SO targets, verifies `SOGetTargets(bound)` returns the target, draws, unbinds, verifies `SOGetTargets(unbound)` is null, copies to staging, and verifies output with `StreamOutputVerify=PASS first_word=0xbf800000` in `artifacts/dxmt-smoke-logs/lane-d-so-roundtrip-20260604-130834.outer.log`. |
| Blend/rasterizer/depth-stencil/sampler states | Implemented | Smoke covers alpha/opaque/independent blend, cull-none/cull-back/wireframe-front rasterizers with scissor/no-depthclip variants, depth/stencil/disabled depth, linear/comparison/anisotropic samplers, viewport/scissor get paths, and explicit state reset before later draw probes. |
| Queries/counters | Implemented | Event, timestamp, timestamp-disjoint, occlusion, pipeline statistics, SO statistics, SO overflow predicates, deferred query playback, and occlusion predicate smoke pass; pipeline/SO/overflow stats now enforce `Begin` pending (`S_FALSE`) before deterministic `End` data in both runs of `artifacts/dxmt-smoke-logs/lane-d-stability-refresh-20260604-135717.outer.log`; device-dependent counters return clean unsupported semantics (`DXGI_ERROR_UNSUPPORTED` with zeroed strings/counters), not `E_NOTIMPL`. |
| Deferred/context methods | Implemented | Empty command lists, deferred RTV clear/readback, deferred draw recording/playback, deferred query playback, and deferred resource update/map/copy/readback pass; `UnityDeferredResourceProbe result=PASS`. |
| Multithread/render stability | Implemented | `ID3D10Multithread` protection, two worker-thread deferred command lists, command batching, and two 60s expanded-suite stability runs pass; `artifacts/dxmt-smoke-logs/lane-d-stability-refresh-20260604-135717.outer.log` reports both runs with `UnityMultithreadProbe result=PASS`, `UnityBatchProbe result=PASS`, `UnityStabilityProbe result=PASS`, and final `exit_code=0`. |
| Draw/DrawIndexed/instanced/indirect | Implemented | Smoke covers direct, indexed, instanced, indexed-instanced, and indirect variants. |
| Dispatch/DispatchIndirect | Implemented | Smoke covers direct and indirect compute dispatch with texture UAV readback value `42`, typed-buffer UAV readback values `100,101,102,103`, structured-buffer UAV readback values `200,201,202,203`, and groupshared/interlocked structured UAV readback values `10,65261,48879,4660`. |
| Copy/resolve/mips | Implemented | `CopyResource`, `ResolveSubresource`, `GenerateMips`, and Texture2DArray mip/slice subresource paths are covered; `artifacts/dxmt-smoke-logs/lane-d-stability-20260604-125404.outer.log` plus per-run logs report `array_slice0_mip1_rgba=96,80,65,255`, `array_slice1_mip1_rgba=255,128,64,255`, `array_slice0_mip1_copy_rgba=68,136,204,255`, `array_slice1_mip1_copy_rgba=68,136,204,255`, and `UnityResourceProbe result=PASS`. |

## DXGI Format Coverage

| Format family | Status | Evidence / gap |
|---|---:|---|
| `R8G8B8A8` / `B8G8R8A8` | Implemented | Mapped in `dxmt_format.cpp`; BGRA marked backbuffer-capable. |
| `R10G10B10A2` | Implemented | Mapped and marked backbuffer-capable. |
| `R16G16B16A16_FLOAT` | Implemented | Mapped and marked backbuffer-capable. |
| 16/32-bit float/int scalar/vector | Implemented | Generic mappings plus Metal capability inspector. |
| BC1-BC7 | Implemented (device-gated) | Mapped when Metal device reports BC texture compression; smoke validates detailed BC1 copy paths plus create/SRV/default-to-staging copy/map/byte readback for every advertised BC1 sRGB, BC2, BC3, BC4, BC5, BC6H, and BC7 variant on this host. |
| D16/D24S8/D32/D32S8 | Implemented | Smoke creates textures/DSVs and clears `D16_UNORM`, `D24_UNORM_S8_UINT`, `D32_FLOAT`, and `D32_FLOAT_S8X24_UINT`; D24 remains internally emulated over Depth32Float_Stencil8. |
| YUV 4:2:2 | Implemented (filter-only) | `R8G8_B8G8_UNORM`/`G8R8_G8B8_UNORM` map to BGRG/GBGR 4:2:2 and smoke-verify Texture2D + shader-sample/filter-only support; no RT/storage capability is advertised. |
| MSAA | Implemented (device-gated) | `R8G8B8A8`, `B8G8R8A8`, and `R16G16B16A16_FLOAT` report 1/2/4x quality=1 and 8x quality=0; smoke covers 4x color/depth render plus 2x/4x color resolve/staging byte readback across those formats. |
| `CheckFormatSupport` | Implemented | Computes D3D11 flags from `MTLQueryDXGIFormat` and capability bits. |
| `CheckFormatSupport2` | Implemented | UAV/load/store/atomic/tiled flags populated from Metal capability bits. |

## Phase 2 Backlog From This Matrix

| Probe | Current state | Next evidence needed |
|---|---:|---|
| Unity FL 11_0 + BGRA support | PASS | `D3D11CreateDevice hr=0x00000000`, `feature_level=0xb100`, BGRA flags `0x02fef3f3`. |
| Unity format probes | PASS | Core Unity color/depth formats plus optional BC1-BC7 and YUV 4:2:2 probes pass in the headless smoke. |
| Phase 3 resource probes | PASS | Texture/buffer/view/update/map/copy/UAV, 3D texture, cubemap, and structured SRV/UAV probes pass in the headless smoke. |
| Phase 4 shaders/draw | PASS | Embedded HLSL VS/PS/CS compiles via `d3dcompiler_47`; shader/input-layout creation succeeds with per-instance input data; direct/indirect draw and dispatch variants pass; expanded state matrix, event/timestamp/occlusion query data, predicate, deferred command list, deferred clear, and deferred draw pass; readback returns `pixel0_bgra=0,255,0,255`, deferred clear returns `255,0,64,255`, deferred draw returns green, and compute returns texture, typed-buffer, structured-buffer, multi-UAV structured-buffer, structured SRV-to-UAV, texture SRV-to-UAV, texture-array UAV, and append-UAV counter readbacks. |
| Phase 5 Unity probe expansion | PASS | State object matrix, texture sampling, gamma/output state, forced-message fullscreen enter/restore, visible fullscreen display capture, per-swapchain `ResizeTarget`, `ResizeBuffers`, post-resize RTV/present/readback, indexed/indirect variants, query/predicate, empty deferred playback, 2x/4x MSAA resolve family, and bitblt/flip sequential/discard swapchains are green. |
| Swapchain variants | PASS | Basic headless message-HWND swapchain/present, resize, `DISCARD`, `SEQUENTIAL`, `FLIP_SEQUENTIAL`, and `FLIP_DISCARD` variant present/readback are green; all variants expose logical `GetBuffer(1)` and reject out-of-range `GetBuffer(2)`. |
| Adapter/output enumeration | PASS | `EnumOutputs`, monitor desc, and display mode list pass in the smoke. |
| State object breadth | PASS | Alpha/opaque/independent blend, cull-none/cull-back/wireframe-front rasterizer, scissor/no-depthclip, depth/stencil/disabled depth, linear/comparison/anisotropic sampler, viewport/scissor set-get, and state-reset variants pass. |
| Real Unity present gate | Lane A/C gated | Runtime shader capture and real Unity present are ready to resume once the x64 guest reaches `CreateDXGIFactory` / `D3D11CreateDevice`; strict-native x64 binding evidence `artifacts/dxmt-x64-binding/run-20260604-165927/dxmt-x64-binding.log` still does not reach present markers, while owned DXMT substitutes remain green. |
