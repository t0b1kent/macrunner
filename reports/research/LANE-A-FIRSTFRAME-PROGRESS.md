# LANE A — FIRST-FRAME PROGRESS / HEARTBEAT (autoloop resume anchor)

**Mission:** `reports/research/LANE-A-FIRSTFRAME-MEGATASK.md` — drive **Hollow Knight** (x64 Unity
6000.0.61f1 under ARM64EC/HyperBridge + ARM64 Wine + DXMT→Metal) to its **first visible frame**.
Silent frame counts; sound is follow-up.
**Discipline:** EMPIRICAL verify-first (pin every wall with trace/disasm/profile/heartbeat BEFORE
theorizing; keep a refuted-scorecard; declare "root" only after a fix MOVES the boot). Bulk ISA gaps.
Commit each real win — NAMED files only, golden wine-dist UNTOUCHED, scoped `wineserver -k` only,
force-add gitignored fix files, commit AFTER a re-run verifies. No ultracode bursts.
**Done:** write `LOOP-STATUS: GOAL` (frame rendered) or `LOOP-STATUS: BLOCKED <reason+next probe>`
at column 0; else keep going wall-by-wall.

## Refuted-scorecard for the rank-7 wall (empirical, this session)
| hypothesis | verdict | evidence |
|---|---|---|
| x18-fault storm | REFUTED | 0% across all `sample` snapshots (951s) |
| getenv/environ-lock contention | real but MINOR — FIX#1 `c021d33` | `_os_unfair_lock_lock_slow`→0 after fix, boot unchanged |
| WaitOnAddress sync hang | REFUTED | 36 calls/600s all correct (equal=1 block, wakes found+alerted); workers idle-by-design; c0000005=0 |
| JIT block-cache exhaustion | SYMPTOM not cause — FIX#2a `ca56289` | cache-full 8000→0 after fix, boot STILL pinned rank-7 |
| **software-audio mixer spin** | **ROOT (pinned)** | heartbeat: 35506/58646 hits @ UnityPlayer.dll rva 0x17b2bb0 = float→int16 saturating PCM loop; "FMOD failed to init any audio device → software output" |

## Committed this session (all real engine wins, GfxDevice-crash → Mono managed-runtime init)
- `cf52a9a` graphics: HK drives real DXMT Metal device (Direct3D 11.0 / Apple M1 Pro)
- `c76033c` jit: legacy CMPPS (0F C2) all 4 variants × 8 predicates
- `19431a1` jit: x86 TSO load LDAR→LDR+DMB ISHLD (unaligned-safe; fixes Mono SIGBUS)
- `287bb5e` jit: double→int converts CVTPD2DQ/CVTTPD2DQ/CVTSS2SI/CVTSD2SI (fallbacks 8000→2)
- `c021d33` jit: FIX#1 — cache interpreter trace-gate env vars once (kills per-instruction getenv environ-lock)
- `ca56289` jit: FIX#2a — block-cache 65536→524288 + de-stick code_cache_full latch (cache-full 8000→0)

## Current wall: rank-7 software-audio spin (ROOT pinned, fix domain = AUDIO)
Unity's bundled FMOD finds no audio device → unthrottled software PCM mixer (float→int16 saturating
loop @ `UnityPlayer.dll RVA 0x17b2bb0`, base 0x87efc510000) monopolizes a core under emulation → main
thread never advances past `Begin MonoManager ReloadAssembly` (log frozen ~219KB, 120% CPU, 10+min).
`mmdevapi.dll` present in guest but device ENUMERATION returns nothing. Fastest path to a FRAME (goal,
not sound): throttle/stub the no-device mixer OR fix mmdevapi→winecoreaudio enumeration so FMOD gets a
paced device. Probe artifacts: `reports/phase4-hollow-knight/laneA-hbprobe-try1-112017`.

---
2026-06-17 11:20 · milestone · graphics(DXMT Metal device) + 5 JIT/engine fixes committed to main carried HK from GfxDevice/CreateBuffer crash → D3D11 device → Mono managed-runtime init · furthest = rank-7 (Begin MonoManager ReloadAssembly) · next: characterize the rank-7 livelock empirically
2026-06-17 · diagnose · profiled run (16 samples/951s): NOT x18 (0%); __ulock_wait2 dominance = parked threads not contention; 120% CPU is ONE thread in guest code · refuted x18 · next: instrument the suspected WaitOnAddress path
2026-06-17 · diagnose · FIX#1 (getenv-cache) committed c021d33 — env-lock contention eliminated (_os_unfair_lock→0) but boot trajectory UNCHANGED · getenv-lock refuted as gate · next: instrument WaitOnAddress wait/wake
2026-06-17 · diagnose · WAITADDR probe (macrunner_hb.c, env-gated): 36 wait calls/600s, all equal=1 (correct block), wakes found+alerted, 27 workers idle-by-design · WaitOnAddress REFUTED · next: mine the log for the actual hot path
2026-06-17 · diagnose · log line-freq: "JIT code cache full" = 7998/9614 lines (block-cache 65536/65536, sticky latch; exec buffer only 18% used) · candidate root = JIT-cache exhaustion · next: FIX#2a grow+de-stick
2026-06-17 · fix · FIX#2a committed ca56289 (HB_BLOCK_CACHE_SIZE 65536→524288 + de-stick block-cache latch) — cache-full 8000→0 VERIFIED · BUT boot STILL rank-7 → JIT-cache was a SYMPTOM (the 1.74MB "burst" was log-spam, real progress always ~217KB) · next: heartbeat guest-PC probe of the silent spin
2026-06-17 11:20 · ROOT · heartbeat probe (MACRUNNER_HB_TRACE_HEARTBEAT+_MODULE): spin = UnityPlayer.dll RVA 0x17b2bb0 (35506/58646 hits), disasm = float→int16 saturating PCM conversion loop (mulss·cvttss2si·clamp±32767·movw·loop) = SOFTWARE AUDIO MIXER; cause = "FMOD failed to init any audio device → software output", unthrottled · rank-7 root PINNED, fix domain = AUDIO · next: pin WHY device enumeration returns nothing (mmdevapi→winecoreaudio) per MEGATASK STEP 1, choose fastest unblock (throttle/stub vs fix enum)
2026-06-17 · audio-probe · x64 MMDevice/WASAPI ENUM fixture (audio_device_enum_x64.exe) now PASSES (rc=0, "audio enum ok") — the x64-guest→native enum bridge is UNBLOCKED by this session's Lane A fixes (audit's 2026-06-13 "BLOCKED needs Lane A" is STALE). BUT enum-only, does NOT exercise IAudioClient render chain · enum is NOT the break · next: test the full x64 render bridge
2026-06-17 · audio-probe · x64 full-render fixtures: DSound PASS (DSOUND PCM ch2 44100 16, checksum 0x27645FE8, PACED end-to-end → winecoreaudio RENDER bridge WORKS for x64-guest); XAudio2 FAIL (rc=5, crashes ~10s). FMOD default = WASAPI (AUTODETECT). So enum✓ + DSound-render✓ ⇒ the gap is **WASAPI device-INIT/render specifically** (IAudioClient::Initialize/GetService/Start, post-enum) — the only path with NO x64 fixture · refined wall: not enum, not the general render bridge · next: EITHER (A) pin HK's FMOD WASAPI init failure directly (HK run + WINEDEBUG=+mmdevapi,+winecoreaudio, capture which IAudioClient call fails) and fix x64-guest winecoreaudio WASAPI render-init [Audio-lane: engine/wine/dlls/winecoreaudio.drv + mmdevapi], OR (B) FASTEST-to-frame: force FMOD→DSound (proven working+paced) so it stops the software-mixer spin. Both are AUDIO-domain; DSound-works is a strong asset (bridge can render paced audio → fix is narrow).
2026-06-17 · wasapi-pin · HK + WINEDEBUG=+mmdevapi,+coreaudio,+ole → ZERO mmdevapi/coreaudio RUNTIME lines (audio fails BEFORE reaching mmdevapi); sole audio-fail evidence = `err:dsound:get_mmdevenum CoCreateInstance failed: 800401f0` = **CO_E_NOTINITIALIZED**. Read dsound_main.c:154 get_mmdevenum: it ALREADY calls CoInitialize(NULL) (:158) before CoCreateInstance (:160) → NOT a missing-CoInit bug; **CoInitialize is INEFFECTIVE** (apartment not established) → CoCreateInstance(MMDeviceEnumerator) returns NOTINITIALIZED → FMOD finds no device → unthrottled software mixer spin. · ROOT REFINED: the break is **COM-apartment activation (combase/ole32), upstream of mmdevapi** — OFF the scoped winecoreaudio/mmdevapi path; same TEB/apartment-coherence family as x18/host-TEB. The narrow winecoreaudio render fix does NOT apply. · next: see BLOCKED.
LOOP-STATUS: BLOCKED rank-7 audio = CO_E_NOTINITIALIZED: CoInitialize() ineffective for the x64-guest audio thread under ARM64EC (combase/ole32 COM-apartment not established; TEB-coherence class) → FMOD no device → software-mixer spin. NOT a winecoreaudio/mmdevapi bug (fails upstream at COM activation), so the megatask's scoped audio fix does not apply; this is a combase/ole32 + TEB engine investigation. CHEAPEST DECISIVE NEXT PROBE: re-run with WINEDEBUG=+ole (or +combase) filtered to the audio tid to capture CoInitialize()'s RETURN + apartment state — distinguishes (i) CoInitialize returns an error = combase load/init bug (contained, fixable) vs (ii) returns S_OK but the apartment is lost by CoCreateInstance = TEB-apartment-coherence (deep, x18-family). THROTTLE-ALTERNATIVE (for a silent frame) is NOT yet actionable: needs the thread-topology question answered first — is the boot thread CPU-STARVED by the spinner (→ yield/throttle helps; candidate: find_region_normalized at 29% of running may take a GLOBAL region-table lock the spinner hammers, serializing all threads' memory accesses) or PARKED-WAITING on an audio-ready signal (→ only making init succeed helps). REFUTED-SCORECARD (rank-7): x18 0% · getenv/environ-lock (FIX#1, minor) · WaitOnAddress healthy · JIT-cache (FIX#2a, symptom) · audio-ENUM works · DSound-RENDER works · XAudio2 fails · → narrowed to COM-apartment-init (CO_E_NOTINITIALIZED).
2026-06-17 · dsound-MTA (#1) · overlay dsound.dll: log init_hr + CoInitializeEx(MTA)-retry on CO_E_NOTINITIALIZED; deployed; re-run WINEDEBUG=-all,+dsound. RESULT: `init_hr=00000001 (S_FALSE: COM ALREADY init'd STA) → CoCreateInstance 800401f0 (CO_E_NOTINITIALIZED)`; MTA-retry `CoInitializeEx=S_OK → CoCreateInstance 80040154 (REGDB_E_CLASSNOTREG)`. ⇒ **COM init WORKS (S_FALSE/S_OK) → NOT TEB-apartment-deep**; MTA did NOT unblock (HK still rank-7, log 217KB, no new markers). The real error is REGDB_E_CLASSNOTREG, YET the standalone x64 enum fixture passes with the same prefix → a SUBTLE combase apartment/class-resolution issue specific to HK's audio thread (tid 0054), not a quick fix.
2026-06-17 · throttle-precheck (#2) · find_region_normalized (hb_memory.c:1043) is LOCK-FREE (per-context mem->hot[4] MRU + linear scan of mem->regions; no global/shared lock). ⇒ the audio spinner does NOT serialize other threads → **throttling it will NOT wake a PARKED main thread** (global-lock-starvation hypothesis REFUTED). · REFRAME / RED-HERRING RISK: FMOD init COMPLETED ("software output"), so the main thread likely got PAST audio init; the float→int16 mixer spins on a SEPARATE, lock-free thread (non-blocking CPU noise). The boot freeze may NOT be caused by audio — the main/boot thread may be PARKED on a DIFFERENT post-audio-init dependency. The audio-spin is the most VISIBLE consumer, not necessarily the boot-blocker.
LOOP-STATUS: BLOCKED rank-7 — neither branch of UPDATE-2 applies: MTA did NOT unblock (REGDB_E_CLASSNOTREG, but COM init WORKS so NOT TEB-deep — a subtle combase class/apartment issue, fixture-passes-HK-fails), AND #2-throttle is moot (audio spinner is lock-free → can't be starving a parked main thread). CAUSALITY UNRESOLVED & must be settled before more audio work: is the boot frozen because (a) the main thread is parked WAITING-ON-AUDIO (→ only a real device/COM fix unblocks — deep) or (b) the main thread is parked on a DIFFERENT post-FMOD-init dependency and audio is a RED HERRING (→ find that wait; audio is a sound-only follow-up)? CHEAPEST DECISIVE NEXT PROBE: thread-aware — identify the main/boot thread (the one that logged `Begin MonoManager ReloadAssembly` / the Mono main thread) and capture its wait state during the freeze (heartbeat/`sample` per-tid, or instrument the wait it's parked in) → distinguishes (a) vs (b). REFUTED-SCORECARD (rank-7): x18 0% · getenv-lock (FIX#1) · WaitOnAddress healthy · JIT-cache (FIX#2a symptom) · audio-enum✓ · DSound-render✓ · XAudio2✗ · COM init WORKS (not TEB-deep) · MTA-retry→CLASSNOTREG (didn't unblock) · find_region_normalized lock-free (throttle won't help parked thread). dsound MTA-retry change uncommitted (harmless + good diagnostic; revert or keep before any commit).
2026-06-17 · thread-aware probe (UPDATE-3) · per-tid `sample` of guest pid during freeze (45 threads). Full Unity thread pool UP + idle-parked (Job/BG Workers, GfxDeviceWorker, AsyncRead, Finalizer, AssetGC×7 — all NtWaitForAlertByThreadId/NtWaitForSingleObject; 27 WaitOnAddress parkers cur=cmp=0xff800001 never-woken = idle, no work dispatched). Audio mixer (Thread_355582) = lone CPU hog (float loop), lock-free, non-blocking. **MAIN thread = Thread_354443: NOT parked — RUNNING Mono ReloadAssembly (lift_one_block/ir_cache_put/exec_instr = JIT-translating Mono's x64) but spending ~47% (1699/3591) in `macrunner_hb_module_from_pc → mach_msg2_trap` + __psynch_mutex.** ⇒ (b) CONFIRMED: audio is a RED HERRING; boot is THROTTLED, not waiting-on-audio.
2026-06-17 · ROOT (b) · macrunner_hb_module_from_pc (macrunner_hb.c:671): on LDR-miss it does a LINEAR DOWNWARD page-scan up to **0x100000 (1,048,576) pages** (line 702), calling macrunner_hb_read_local_memory each = **`mach_vm_read_overwrite` MACH SYSCALL per page** (line 606, Apple). Mono's JIT-compiled C# lives in ANONYMOUS exec regions (not PE modules / not in LDR) → every Mono-JIT PC misses LDR → triggers the giant mach-scan. Neg-cache is per-PAGE, only 128 entries (linear) → Mono's many JIT pages THRASH it → nearly every Mono-JIT PC re-scans. = the real boot-throttle (~half the main thread). FIX DOMAIN = Lane A (macrunner_hb.c, overlay-buildable). Fix options: (1) cap the 1M-page scan to a sane bound (real non-LDR PEs are tiny; UnityPlayer-class are in the LDR, caught at :687); (2) RANGE-based neg-cache (when the scan finds the nearest PE base `b` below, the span (b+size, page] provably has no PE module → neg-cache the whole range → Mono region cached after ONE scan) — safe given the downward-first-DOS+NT-hit semantics; (3) or replace the memory-scan fallback with an LDR-list nearest-module iteration (no mach syscalls; returns NULL for Mono code = faster AND more correct than the current best-effort nearest-DLL).
LOOP-STATUS: BLOCKED(soft, fix-design steer) rank-7 boot-throttle ROOT-CAUSED → (b) audio RED HERRING. Main thread (Mono ReloadAssembly) is RUNNING-but-throttled ~47% in macrunner_hb_module_from_pc's 1M-page mach_vm_read downward scan (macrunner_hb.c:671/702/606), triggered because Mono JIT code isn't a PE module (LDR-miss) and the per-page 128-entry neg-cache thrashes. NOT a wait/deadlock; NOT audio; NOT x18/getenv/JIT-cache. NEXT = fix module_from_pc (cap scan + range-neg-cache, OR LDR-iterate fallback) in our overlay, rebuild, re-run → expect the main thread to accelerate through ReloadAssembly toward PlayerLoop/scene/swapchain. Correctness-sensitive hot path → recommend the safe range-neg-cache (option 2) or LDR-iterate (option 3). REFUTED-SCORECARD (rank-7): x18 0% / getenv-lock(FIX#1) / WaitOnAddress healthy / JIT-cache(FIX#2a symptom) / audio enum✓+DSound-render✓ / COM-init works(not TEB-deep) / find_region_normalized lock-free / **ROOT = module_from_pc mach-scan throttle (b)**.
2026-06-17 · FIX module_from_pc (UPDATE-3 1+2) COMMITTED 5126bcb · cap 1M-page scan to 0x10000 + range-cache the hit (span [base, query-page]->base, provably identical via downward-first-hit) + grow pos/neg caches 64/128→256/512. VERIFIED: module_from_pc 47%→0%, ~90M blocks/235s (was stuck), 0 new faults, 0 cache-full. But boot STILL rank-7 → next throttle exposed.
2026-06-17 · FIX find_region_normalized COMMITTED 3f53170 · O(log n) treap walk for x64 (reuse existing region_tree, non-overlapping→identical to linear scan) instead of MRU(4)+O(n) linear scan. VERIFIED: find_region_normalized 33.5%→1.5%, boot reaches rank-7 cleanly, 0 new faults.
2026-06-17 · STRATEGIC PICTURE · After 2 throttle fixes the rank-7 "wall" is confirmed THROUGHPUT-LIMITED (double-emulation: HyperBridge emulating Mono's JIT-compilation of HK's entire C# during ReloadAssembly), NOT a discrete blocker/deadlock. Running breakdown now: macrunner_hb_run_x64 43% (dispatch = ~irreducible emulation floor), **macrunner_hb_ir_cache_put 30.6%** (NEXT fixable throttle: the IR cache is MACRUNNER_HB_IR_CACHE_SIZE=8192 with open-addressing linear probe up to full size at macrunner_hb.c:4059; Mono's ReloadAssembly working set > 8192 → probe degrades to ~O(8192) per put = re-lift/thrash, same FIX#2a class one layer up → grow the IR cache), find_region 1.5%, try_ntdll_version_semantic 4%. Boot has NOT passed rank-7 in any run (5-15min) despite the throughput gains → ReloadAssembly is a huge emulated-JIT cost (or an IR-cache re-lift thrash). PEELED throttles: module_from_pc 47%→0%, find_region 33.5%→1.5%. REMAINING: ir_cache_put 30% (fixable, grow IR cache), then run_x64 43% (dispatch floor).
LOOP-STATUS: BLOCKED(soft, STRATEGY steer) rank-7 is throughput-limited double-emulation of Mono ReloadAssembly, NOT a discrete blocker. 2 pathological throttles fixed+committed (module_from_pc 47%→0% 5126bcb, find_region 33.5%→1.5% 3f53170). NEXT fixable throttle = ir_cache_put 30.6% (grow IR cache 8192, FIX#2a pattern, 3rd cache-size win). After that, run_x64 dispatch (43%) is the ~irreducible emulation floor → the frame requires ReloadAssembly to COMPLETE, which even fully-optimized may need a LONG run (or the ir_cache_put 30% IS a re-lift thrash livelock that, once fixed, lets it complete). STRATEGY FORK for operator: (a) grow IR cache (likely last big helper win) + then a LONG-HAUL run (30+min) to test if ReloadAssembly completes → frame; (b) confirm-first whether ir_cache_put is re-lift-thrash (livelock) vs genuine-new-block translation (slow-but-progressing); (c) reassess (multi-hour boot acceptable for "first frame"? faster path?). REFUTED-SCORECARD (rank-7): x18 0% / getenv(FIX#1) / WaitOnAddress healthy / JIT-cache(FIX#2a) / audio RED-HERRING (COM-init works, mixer lock-free) / module_from_pc FIXED / find_region FIXED / → throughput-limited (run_x64 dispatch floor + ir_cache_put 8192-thrash).
2026-06-17 · FIX IR-cache (a)+(b) COMMITTED e23140f · grow MACRUNNER_HB_IR_CACHE_SIZE 8192→262144 + rate-limited full-fail counter (macrunner-hb-ircache-FULL) in ir_cache_put. The IR cache is eviction-free open-addressing; at 8192 it filled under Mono ReloadAssembly → uncached blocks re-lifted every exec (find+put O(size) when full) = translation thrash. VERIFIED long-haul: **ircache-FULL=0 (cache FITS at 262144, thrash GONE)**, ir_cache_put 30.6%→out-of-hot-list, **CPU 120%→26%**, 0 regression. (b) DECISIVE: it WAS thrash, the grow eliminated it.
2026-06-17 · ★ STATE CHANGE — rank-7 is now a WAIT-DEADLOCK, not throughput. Post-thrash per-tid sample (45 threads, CPU 26%): the ENTIRE process is PARKED — workers (Job/BG/GfxDeviceWorker/AsyncRead) idle in WaitOnAddress (NtWaitForAlertByThreadId); Finalizer + AssetGC×7 idle in NtWaitForSingleObject; **MAIN/coordinator threads 460362 + 460360 BLOCKED in macrunner_hb_run_x64 → macrunner_hb_try_kernel32_handle_semantic → NtWaitForSingleObject → server_wait = WaitForSingleObject on a kernel object via wineserver.** NOTHING runs guest code (log frozen 217KB). ⇒ Mono ReloadAssembly COMPLETED (that's why CPU dropped 120→26); the boot now blocks at the NEXT sync point — the main thread waits on a kernel HANDLE that is never signaled = a genuine wait-dependency deadlock/missed-signal. NOT throughput, NOT audio (red herring), NOT the dispatch floor. The throughput fixes UNMASKED this real wall.
LOOP-STATUS: BLOCKED rank-7 = WAIT-DEADLOCK (exposed after fixing all throughput throttles). Main/coordinator threads (460362/460360) blocked in kernel32 WaitForSingleObject → NtWaitForSingleObject → server_wait on a kernel object that is never signaled; full Unity thread pool parked idle; ReloadAssembly completed (CPU 120%→26%). This is the SAME class as the pre-thrash "355592 WaitForSingleObject+NtSetTimer" sighting, now clean. CHEAPEST DECISIVE NEXT PROBE: identify the WAITED-ON HANDLE + who should signal it — instrument macrunner_hb_try_kernel32_handle_semantic's WaitForSingleObject path (log the HANDLE value + object type/name + timeout for tid 460362/460360 during the freeze), then find the SetEvent/Release/thread-exit that should signal it and why it doesn't (a worker job-done event a parked worker never posts? an async-load completion? a thread/process handle for something that didn't start?). SESSION COMMITS (7): 287bb5e CVT, c021d33 FIX#1 getenv-cache, ca56289 FIX#2a block-cache, 5126bcb module_from_pc, 3f53170 find_region, e23140f IR-cache; (+19431a1 LDAR, c76033c CMPPS pre-this-task). REFUTED-SCORECARD rank-7: x18 0% / getenv(FIX#1) / WaitOnAddress-prim healthy / JIT-cache(FIX#2a) / audio RED-HERRING / module_from_pc FIXED / find_region FIXED / IR-cache-thrash FIXED → **now a WaitForSingleObject wait-deadlock (main thread, unsignaled kernel object)**.
2026-06-17 · WaitForSingleObject-handle probe (UPDATE) · enabled existing wait-semantic trace (MACRUNNER_HB_TRACE_WAIT_SEMANTIC=1 BUDGET=2M, zero code change) + correlated wait handles vs SetEvent/Release signals. FINDINGS: group (b) handles 0x68-0xb0,0xec (caller 0x87efca87c92) are signaled=1 = healthy idle worker/Finalizer/GC CYCLIC waits (red herring). The MAIN/boot thread (caller **0x87efdc4f244** = UnityPlayer code) hits handles **0x1d0/0x1dc via WaitForSingleObjectEx 40487 before / 40486 after** = a LOOP (NOT a stuck single block — corrects the initial "(a) never-signaled deadlock" read; before≈after, only 1 in-flight). INFINITE timeout, NOT SetEvent-signaled (signaled=0) → alertable/APC- or I/O-completion-driven. So the main thread is in an **alertable WaitForSingleObjectEx LOOP** on 0x1d0/0x1dc, waking ~40K times yet NO boot progress, while the worker pool is idle (0xff800001, no work dispatched). = a higher-level LIVELOCK ((a)-shape: awaited completion/job never produced), not a clean dead wait. NEXT: map+disasm caller 0x87efdc4f244 (UnityPlayer fn, ~rva 0x173f244) → what condition the loop polls + what 0x1d0/0x1dc are + WHY the completion never arrives (which producer/dispatch never runs → connects to the idle 0xff800001 workers).
LOOP-STATUS: BLOCKED rank-7 = main/boot thread (UnityPlayer caller 0x87efdc4f244) in an alertable WaitForSingleObjectEx LOOP on handles 0x1d0/0x1dc (40K iters, INFINITE, not SetEvent-signaled, no boot progress) while the Unity worker pool is idle (0xff800001 = no work dispatched). A livelock: the main thread waits (alertably) for an async completion/job that is never produced. NOT throughput (all 4 throttles fixed: CPU 120%→26%), NOT audio (red herring), NOT a clean never-signaled single-wait. CHEAPEST DECISIVE NEXT PROBE: map caller 0x87efdc4f244 to UnityPlayer rva + DISASM the wait-loop (what flag/condition it polls between WaitForSingleObjectEx calls) + identify 0x1d0/0x1dc (NtQueryObject type/name OR their CreateEvent) + trace who SHOULD complete the awaited job and why it doesn't (dispatch never reaches the idle workers? an async I/O that never completes under emulation?). SESSION COMMITS(7): CVT 287bb5e, FIX#1 c021d33, FIX#2a ca56289, module_from_pc 5126bcb, find_region 3f53170, IR-cache e23140f (+ LDAR 19431a1, CMPPS c76033c prior).
2026-06-17 · caller-disasm pin · UnityPlayer base 0x87efc510000 → caller 0x87efdc4f244 = UnityPlayer.dll rva 0x173f244 (VMA 0x18173f244, in UnityMain2 region). DISASM: the loop is the canonical **`while (WaitForSingleObjectEx(event, INFINITE, bAlertable=TRUE) == WAIT_IO_COMPLETION) ;`** — handle loaded from [rbx], INFINITE, alertable; exit on WAIT_OBJECT_0(0=signaled); re-wait while WAIT_IO_COMPLETION(0xC0=APC ran); error path on other. ANSWERS: (1) polled condition = the EVENT being signaled (WAIT_OBJECT_0); the 40K iters are WAIT_IO_COMPLETION (APCs ARE firing) — it loops because the event is NEVER signaled. (2) 0x1d0/0x1dc = an EVENT (WaitForSingleObjectEx + WAIT_OBJECT_0 + SetEvent-expected; alertable=also pumps APCs = a completion/async wait), NOT IOCP. (3) WHICH: APCs ARE delivered → option-(3) REFUTED; the event's PRODUCER never SetEvents it (signaled=0 whole run) + worker pool idle (0xff800001 never woken) → **option (1): the awaited completion/job is never PRODUCED — the worker that should SetEvent never runs because dispatch/wake never reaches the idle worker pool**. So rank-7 = main thread pumps APCs forever waiting for a completion Event a worker should signal, but the worker is never woken to do the work. FINAL PIN (next): confirm 0x1d0/0x1dc never SetEvent'd + NtQueryObject its name; trace the worker-dispatch/job-done path → why the WakeByAddress/dispatch never wakes the 0xff800001 worker (the actual fixable bug → boot → present → frame).
2026-06-17 · producer/dispatch pin DONE (WAIT_SEMANTIC + WAITADDR run, dispatch-try1-144021) · (1) Event 0x1d0/0x1dc = NEVER SetEvent'd (set list = 0xc8,0x19c,0x1c4,0x1d4,0x6c..0xb4,0xf4 — NOT 0x1d0/0x1dc) → main's completion never produced. (2) Workers: WaitOnAddress addr=0x320xxxxx comparand=**0xff800001**, NEVER woken (27/28; only cycling lock 0x3c0414c60 woken) → **no WakeByAddress ever issued for worker addrs**. Unity uses semaphores too (CreateSemaphoreExW by worker-pool caller 0x87efca87e9d; ReleaseSemaphore by 0x87efd9e1f30). VERDICT: rank-7 deadlock = the **Unity job-system worker-WAKE is broken under emulation** — dispatch never wakes the WaitOnAddress-parked workers → jobs never run → completion Event never SetEvent → main pumps APCs forever. Operator's (a)-family (pool never dispatched/woken). REMAINING AMBIGUITY: WakeByAddress NEVER called for worker addrs (→ pure-(a) missing wake) BUT ReleaseSemaphore IS called (→ possible (b): dispatch wakes via a SEMAPHORE while workers park in WaitOnAddress = primitive MISMATCH under emulation, wake not delivered). FINAL DISAMBIGUATION: disasm the worker-park (caller 0x87efca87c92 — does it wait on the semaphore or WaitOnAddress, and does it FALL BACK between them?) + the dispatch-wake (caller 0x87efd9e1f30 ReleaseSemaphore — is that meant to wake these workers?) → does the dispatch's primitive match the worker's park? If mismatch (sem-release vs WaitOnAddress-park) → fix the emulation so the job-dispatch wake reaches the parked workers. Then: workers wake → job done → Event set → main proceeds → boot → present → CAMetalLayer → fold laneD winemac present → FRAME.
LOOP-STATUS: BLOCKED rank-7 = Unity job-system worker-wake broken under emulation. Confirmed: main's completion Event 0x1d0/0x1dc NEVER SetEvent; 27 job workers parked in WaitOnAddress(cmp=0xff800001) NEVER woken (no WakeByAddress for them); ReleaseSemaphore IS called (semaphore path exercised). So dispatched jobs never run → completion never produced → main loops WaitForSingleObjectEx(alertable) forever. (a)-family. CHEAPEST NEXT: disasm worker-park (0x87efca87c92) + dispatch-wake (0x87efd9e1f30) to confirm whether it's (a) WakeByAddress-never-called or (b) semaphore-release-vs-WaitOnAddress-park MISMATCH (dispatch wakes a primitive the worker isn't parked on) → the exact emulation fix. SESSION COMMITS(7): CVT 287bb5e, FIX#1 c021d33, FIX#2a ca56289, module_from_pc 5126bcb, find_region 3f53170, IR-cache e23140f (+LDAR 19431a1, CMPPS c76033c).
2026-06-17 · final disasm · worker-pool fn UnityPlayer rva 0x577c60: worker = `ReleaseSemaphore(doneSem[+0x60]); loop { WaitForSingleObjectEx(jobSem[+0x58],INFINITE,nonalert); call job[+0x68](arg[+0x70]); ReleaseSemaphore(doneSem[+0x60]); }`. Dispatch-wake wrapper rva 0x14d1f10 = `ReleaseSemaphore(h,count,NULL)` (+GetLastError retry). ⇒ this pool PARKS on a SEMAPHORE via WaitForSingleObjectEx + is dispatched/woken by ReleaseSemaphore = operator's (b)-shape. In Wine: sem-wait→NtWaitForSingleObject→wineserver server_wait; ReleaseSemaphore→NtReleaseSemaphore→wineserver. ★ TWO-POOL NUANCE: this is the SEMAPHORE pool; the 27 never-woken workers in WAITADDR park on RtlWaitOnAddress(0xff800001) = Unity's SEPARATE lock-free Job.Worker pool (woken by WakeByAddress, never called). Which pool gates the main's never-set Event 0x1d0/0x1dc decides the fix: SEMAPHORE pool → fix = wineserver NtReleaseSemaphore not waking the NtWaitForSingleObject waiter (b); JOB.WORKER pool → fix = the WakeByAddress the job-enqueue never issues (a). DECISIVE FINAL TEST: trace one jobSem ([this+0x58]) handle — is it ever ReleaseSemaphore'd, and does the WaitForSingleObjectEx(jobSem) worker WAKE? If released-but-worker-stuck → wineserver semaphore-release-wake bug (b, the fix). If never released → dispatch never signals (a). Same for the Job.Worker WakeByAddress. (Marathon depth — multi-pool; the fix is one wake-delivery test away.)
2026-06-17 · WAKE-DELIVERY TEST — clean re-analysis of dispatch run.log via ctx (ReleaseSemaphore DOES log handle=; earlier "(b)" was a +4 handle mis-correlation: jobSem 0x68 vs doneSem 0x6c, adjacent pair per worker). HARD DATA whole-boot ~250s: (1) SEMAPHORE pool worker(caller 0x87efca87c92) waits 7 jobSems 0x68/0x74/0x80/0x8c/0x98/0xa4/0xb0 — each after=0 relSem=0 setEvt=0 = NEVER SIGNALED ⇒ (a). (2) JOB.WORKER RtlWaitOnAddress pool: 28 wait-addrs, ONLY 0x3c0414c60 ever woken; worker 0xff800001 slots NEVER ⇒ (a). (3) DELIVERY PROVEN OK: 0x3c0414c60 wakes; main(0x87efdc4f244) waits on EVENTS 0x1d0(15419/after15418)+0x1dc(24832/after24831) RETURN fine (alertable APC pump, NOT hard-blocked) but events NEVER SetEvent (setEvt=0). ⇒ (b) wineserver-delivery REFUTED — core wine fine, do NOT stop/surface. ★ TWO CORRECTIONS: NOT the try_wait_address_semantic flavor (waitaddr-wake log is OUR handler; guest only ever WakeByAddress 0x3c0414c60, never a worker → guest never ISSUES the wake; nothing to route-fix). AND signaling near-silent: 15 ReleaseSemaphore + 3 SetEvent in WHOLE boot (healthy Unity = thousands) ⇒ producer/coordinator never drives the job system. REFRAME: furthest marker = rank-7 D3D11CreateDevice; main pumps completion-events 0x1d0/0x1dc never set ⇒ gating thread = whoever sets them, most likely the GfxDevice/render thread INSIDE D3D11 device creation ⇒ ROOT MAY BE LANE D (DXMT D3D11CreateDevice stall), worker-park = symptom. a1-alt (upstream atomic-visibility) argued-against by near-zero dispatch (not a per-dispatch miss). 
LOOP-STATUS: BLOCKED rank-7 — VERDICT (a) the wake/dispatch is NEVER ISSUED (sem jobSems relSem=0; Job.Worker slots WakeByAddress=0); (b) wineserver delivery REFUTED (0x3c0414c60 wakes, main waits return) → core wine untouched, no stop. CORRECTION: not a try_wait_address_semantic routing fix (guest never issues the worker wake). Near-zero whole-boot signaling (15 ReleaseSemaphore/3 SetEvent) ⇒ producer never drives dispatch; boot parked at rank-7 D3D11CreateDevice, main pumps never-set completion events 0x1d0/0x1dc. LEADING ROOT = LANE D (GfxDevice/render thread stalled in DXMT D3D11CreateDevice), worker-park downstream. NEXT DIAGNOSTIC (decides exact file, cheap, diagnostic-only): id the GfxDevice/render thread + creator/setter of 0x1d0/0x1dc — parked-awaiting-dispatch → Lane A sync (hb_arm64_codegen.c atomic/fence, a1) vs stuck-inside-DXMT-D3D11CreateDevice → Lane D (engine/dxmt). COMMITS(7): CVT/FIX#1/FIX#2a/module_from_pc/find_region/IR-cache (+LDAR,CMPPS).
2026-06-17 · GfxDevice-thread DIAGNOSTIC (mined dispatch run.log via ctx) — ★OVERTURNS Lane-D AND the sync hypotheses. FACTS: (1) GRAPHICS FULLY WORKS — L1707 GfxDevice creating device client; L1713 dxmt-D3D11CreateDevice ENTER → L1733-38 device CREATED (Direct3D 11.0 [level 11.1], Renderer Apple M1 Pro, VRAM 12779MB); L1739-83 dxmt-createbuffer-call n=1..11 (buffers flow — also OBSOLETES old CreateBuffer-fail memory [[project_hk_nullcall_wndproc]]). (2) THE REAL WALL = a SINGLE hard thread-death: macrunner-hb-run-exit total=1, reason=native-chpe-image, status=c000007b STATUS_INVALID_IMAGE_FORMAT, L16112, DEEP in Mono ReloadAssembly (began L2014). (3) CAUSALLY COUPLED to main's wait: next line L16114 = main(caller 0x87efdc4f244) WaitForSingleObjectEx(0x1dc) returns status=0 WAIT_OBJECT_0 (dead thread's exit fired the paired event) but real work undone → main re-blocks on 0x1d0; NO graphics progress after L16112 = terminal. (4) faulting last_bytes `...ff 25 52 2a 03 00...` = `jmp qword[rip+0x32a52]` = an IMPORT-THUNK tail-jump landing at pc=0x169340fcc in a ~0x1693xxxxx region HB classifies native/CHPE(ARM64EC) + REFUSES to dispatch. VERDICT: NOT (b)-wineserver, NOT Lane-D graphics — it's a LANE A / HyperBridge ARM64EC CHPE-DISPATCH thread-death: a Mono worker/loader thread tail-jumps through an import into an ARM64EC/CHPE region HB won't route → c000007b → thread dies → completion never produced → main hangs on 0x1d0/0x1dc. EXACT FILE: engine/wine/dlls/ntdll/unix/macrunner_hb.c:19376-19404 (the `module_machine!=AMD64 || get_arm64x_metadata` branch: tries label_allows_direct_native(label) && pc_is_native_pe_builtin(pc)→call_direct_native_target; FALSE here → refusal :19398-403). Our domain, overlay, SAFE. The marathon's "worker-wake/sync" framing was the SYMPTOM (everyone waits on the thread that died); root is the CHPE-dispatch refusal.
LOOP-STATUS: BLOCKED rank-7 — VERDICT: NOT graphics (DXMT D3D11 device + 11 buffers created OK on Apple M1 Pro), NOT (b)-wineserver. ROOT = LANE A HyperBridge ARM64EC CHPE-dispatch: ONE thread dies c000007b/native-chpe-image (macrunner_hb.c:19403) deep in Mono ReloadAssembly, doing a `jmp [rip+disp]` import-thunk tail-call into a ~0x1693xxxxx ARM64EC/CHPE region HB refuses (pc_is_native_pe_builtin=false) → never signals completion → main pumps 0x1d0/0x1dc forever. FIX (our domain, overlay): make the :19376-19404 native/CHPE dispatch recognize+route that tail-jump target (read pc_is_native_pe_builtin / label_allows_direct_native / call_direct_native_target + id the 0x1693xxxxx region). NEXT: scope the 3 recognition fns + the target region → design the dispatch fix → implement → re-run → expect thread survives → completion → boot past ReloadAssembly → present → FRAME. COMMITS(7): CVT/FIX#1/FIX#2a/module_from_pc/find_region/IR-cache (+LDAR,CMPPS).
2026-06-17 · CHPE-REFUSE PROBE (env-gated macrunner_hb.c:19398, built+deployed both dist, ran 360s) — ★CORRECTS the fix direction. ONE probe line at the refusal: pc=0x14cd90fcc image_base=0x87efe630000(=kernelbase, AMD64+arm64x, image_exec=0) ≠ mod=0x14cce0000(mod_machine=014c=i386!, arm64x=0, mod_exec=0, sec=none, ldr=0, dladdr=none) is_native_pe_builtin=0 label_allows=1. FAULT bytes `48 8d a4 24 00 00 00 00 / ff 25 52 2a 03 00` = `lea rsp,[rsp+0]; jmp qword[rip+0x32a52]` = ARM64X x64 ENTRY-THUNK / fast-forward sequence for a kernelbase export (last_block 0x87eff36e2f0 = kernelbase x64-thunk range). The redirection slot resolved to 0x14cd90fcc = GARBAGE (no LDR/no exec-section/no dladdr; only a phantom i386 signature at 0x14cce0000 that is NOT a real loaded module). ⇒ pc_is_native_pe_builtin=0 is CORRECT — HB rightly refuses to execute garbage. NOT a recognition bug (patching pc_is_native_pe_builtin to accept 0x14cd90fcc would run garbage). ROOT = UPSTREAM: a kernelbase x64 ENTRY-THUNK's ARM64X redirection (jmp [rip+disp]) resolves to a BAD pointer instead of the real ARM64 function = the SAME ARM64X entry-thunk family as the committed window-server keystone (signal_arm64.c macrunner_hb_redirect_arm64x_hexpthk_sigill: strip +2 tag, redirect ffwd-thunk→native ARM64) BUT hit on the x64 JIT-DISPATCH path (hb_run_x64 jmp [rip+disp]), which that SIGILL-path fix does NOT cover. Secondary: image_base was STALE (kernelbase, the jumping module) ≠ module_from_pc(pc) — minor, not the cause. Markers reached identical (engine-version→subsystems→GfxDevice→D3D11CreateDevice→device-created→createbuffer n=1..11→ReloadAssembly→[c000007b]; no SwapChain/Present) = deterministic repro.
LOOP-STATUS: BLOCKED rank-7 — ROOT PINNED: a kernelbase x64 ARM64X ENTRY-THUNK (`lea rsp,[rsp]; jmp [rip+0x32a52]`) resolves its redirection slot to GARBAGE (0x14cd90fcc — no LDR/exec-section/dladdr) → HB correctly refuses (c000007b native-chpe-image) → the Mono worker/loader thread dies → completion event 0x1d0/0x1dc never set → main hangs. NOT a pc_is_native_pe_builtin recognition bug (it's right to refuse garbage); NOT graphics (DXMT D3D11 device+11 buffers OK); NOT wineserver. SAME ARM64X entry-thunk family as committed keystone (signal_arm64.c hexpthk redirect) but on the x64-JIT-dispatch path it doesn't cover. FIX: make the x64-dispatch entry-thunk redirection resolve to the real ARM64 target (the redirection table / .hexpthk for kernelbase) instead of garbage — in macrunner_hb.c hb_run_x64 / the arm64x thunk-resolution, mirroring the signal_arm64.c keystone. NEXT DIAGNOSTIC (pins exact import + intended target): enrich probe to log last_block module name + the IAT/redirection slot addr ([insn_end]+0x32a52) + its stored value + the intended ARM64 target; THEN design the redirection fix → implement → re-run. Probe is env-gated+deployed (overlay, golden untouched). COMMITS(7): CVT/FIX#1/FIX#2a/module_from_pc/find_region/IR-cache (+LDAR,CMPPS).
2026-06-17 · PROBE2 + ROOT PINNED + FIX IMPLEMENTED. probe2 (chpe-refuse-probe2): last_block=0x87eff36e2f0 lb_name=kernel32.dll (x64 forward thunk `lea rsp,[rsp];jmp [rip+0x32a52]`), slot=0x87eff3a0d50 in kernel32 IAT, **stored=0x87efe660c68 (valid kernelbase ARM64 target) — IAT is CORRECT, stored_eq_pc=0**. So refused pc=0x17fd20fcc is DOWNSTREAM, NOT the IAT target. ★TRUE ROOT (read hb_run_x64 19262-19403 + call_direct_native_target 18631/finish_import 18764): (1) Mono-JIT'd x64 code (anonymous exec, module_from_pc=NULL) CALLs kernel32!fn (return addr = Mono-JIT pc 0x17fd20fcc pushed). (2) kernel32 thunk jmp→kernelbase native 0x87efe660c68; loop computes image_base=kernelbase(arm64x); CHPE branch dispatches it via call_direct_native_target; finish_import sets ctx->pc=ret_addr=0x17fd20fcc; continue. (3) next iter ctx->pc=0x17fd20fcc is a registered DYNAMIC-EXEC region → the 19267-19274 fast-path `goto in_guest_image` **WITHOUT updating image_base** → stale image_base=kernelbase(arm64x) → CHPE check 19376 fires → pc_is_native_pe_builtin(0x17fd20fcc)=FALSE → refuses the Mono-JIT RETURN ADDRESS with c000007b. Only triggers when the prior module was ARM64X (kernelbase); Mono→Mono returns leave non-arm64x image_base = why ReloadAssembly mostly worked. NOT a bad IAT, NOT a thunk-redirection bug, NOT graphics/wineserver — a STALE-image_base misroute of Mono-JIT code into the CHPE-refusal. ★FIX (macrunner_hb.c:19376, minimal+safe): guard the CHPE-refusal with `&& !macrunner_hb_x64_dynamic_exec_contains_no_lock(ctx->pc)` — dynamic-exec (Mono-JIT) pcs are never native CHPE images, so they fall through to the normal x64-JIT path that already runs them; real CHPE images (not dynamic-exec) still refused. Short-circuited (only evaluated when image_base is non-AMD64/arm64x). Built+deployed both dist+codesigned. VERIFY RUN in flight (chpe-fix, 420s) — expect c000007b gone + boot past ReloadAssembly → SwapChain/Present/FRAME.
2026-06-17 · VERIFY RUN (chpe-fix, 420s) — ★FIX CONFIRMED + boot ADVANCED to a new wall. native-chpe-image=0 (was the fatal c000007b), chpe-refuse-probe=0 → the stale-image_base CHPE-misroute is FIXED. Boot now reaches: createbuffer n=1..17 (was 1..11), a 2nd dxmt-D3D11CreateDevice (Flags=0x820), and GpuFence::Create. NEW WALL (still hung to cap, exit=143): (1) `GpuFence::Create(): Failed to create ID3D11Fence, error 0x80004005` (DXMT missing ID3D11Fence/CreateFence = LANE D), immediately followed by (2) a DIFFERENT c000007b reason=**non-application-target** (path :19349, NOT the CHPE path I fixed) at Mono-JIT pc=0x16b52c833 (rip==pc, real x64 method bytes `cmp [rbp-0x20],r14;jz;mov...`; q8/q10 in same 0x16b5 Mono-JIT region) → x64_dynamic_exec_contains(0x16b52c833)=FALSE so it fell through 19267→19349. = SAME Mono-JIT-not-recognized family as the fixed bug but on the non-application-target path (dynamic-exec tracking gap), sitting right after a Lane-D GpuFence failure. Only ONE run-exit (one thread death) → same hang pattern (worker dies→main hangs). FIX is solid (only touched CHPE path :19376, clearly working) — this is a NEW unmasked wall, not a regression.
2026-06-17 · CHPE-guard fix COMMITTED 2f9b135 (incl env-gated chpe-refuse probe). nonapp-probe run (laneA-nonapp): ★MILESTONE — boot now reaches INPUT INIT (furthest ever). NO crash from the non-application-target path (run-exit=0, nonapp-probe=0) ⇒ that c000007b is STOCHASTIC (Mono-JIT dynamic-exec registration RACE, not deterministic). Boot trajectory: createbuffer n=1..21 (was 17), GfxDevice OK, GpuFence::Create still E_FAIL 0x80004005 (DXMT no ID3D11Fence — Lane D, non-fatal so far), then `<RI> Initializing input.` + Unity XInput fallback chatter ("XInput1_4.dll not found, trying 1_3" — but ALL xinput DLLs ARE present in prefix, so that's Unity probe noise not a missing file). DIED rc=5 (exit=5, NOT a hang) at a NULL-call: seh-host-boundary pc=0x0 lr=0x87EFAAA8994 exception=c0000005 tid=0144; the crashing thread = ldr-init index 48 entry=0x87EFAAA8900, faults at entry+0x94 calling pc=0x0 = a thread entry fn calling through a NULL function pointer (uninit callback / unresolved import), created right after input init. nullcall-iat probe: UnityPlayer not found at probe addrs. = the [[project_hk_nullcall_wndproc]] execute-at-0 FAMILY but on an input-init thread (different from the old CreateBuffer-fail one; CreateBuffer now works n=21).
LOOP-STATUS: ADVANCED (milestone) — CHPE-guard fix COMMITTED 2f9b135 + verified; HK boot leaped from "dies in Mono ReloadAssembly" to GRAPHICS-FULLY-INIT + INPUT-INIT-STARTED. Three walls remain, in deterministic order: (1) [furthest, deterministic, rc=5] NULL-call c0000005 on an input-init thread (entry 0x87EFAAA8900, calls pc=0x0 at +0x94, tid 0144) — execute-at-0 family, likely an uninit XInput/input fn-ptr or unresolved import; (2) [stochastic] non-application-target Mono-JIT c000007b (dynamic-exec registration race; didn't repro) — fix = make dynamic-exec registration race-free / guard :19349; (3) [Lane D, non-fatal so far] GpuFence::Create E_FAIL = DXMT missing ID3D11Fence. NEXT (decide): pin the NULL-call (#1, furthest deterministic) — disasm thread-entry 0x87EFAAA8900 / id the NULL fn-ptr (XInput? input callback?) — vs address the stochastic race (#2) vs Lane D fence (#3). probes (chpe-refuse/probe2 committed; nonapp-refuse uncommitted) env-gated. COMMITS(8): +2f9b135 CHPE-guard (CVT/FIX#1/FIX#2a/module_from_pc/find_region/IR-cache +LDAR,CMPPS).
2026-06-17 · NULL-CALL PIN (operator chose #1). The existing PE-side SEH-host-boundary nullcall handler (engine/wine/dlls/ntdll/signal_arm64.c:1661+) is SPECIALIZED for the OLD UnityPlayer-WndProc case (probes x5/x1/x19 for UnityPlayer base, dumps USER32/dxgi IAT) → for THIS native-thread crash the probes are stack ptrs → "UnityPlayer module not found", uninformative. ENRICHED the else-branch (uncommitted): LdrFindEntryForAddress(Lr)→caller module+rva, decode the faulting `blr/br Xn` (0xD63F0000|n<<5 / 0xD61F0000|n<<5) → which reg held NULL + its value, + dump thread-entry insns at Lr-0x94. ★PE-BUILD RECIPE (was the blocker): PE-side ntdll.dll uses the llvm-mingw cross-compiler `aarch64-w64-mingw32-clang` which is NOT on PATH by default (the .so build uses native gcc) → `export PATH=engine/toolchain/llvm-mingw-20260505-ucrt-macos-universal/bin:$PATH` then `make -C engine/wine/build-arm64ec-spike dlls/ntdll/aarch64-windows/ntdll.dll` (arch-specific target; the bare `dlls/ntdll/ntdll.dll` target does NOT rebuild the aarch64 PE obj). Built ARM64X (winegcc -b arm64ec-windows -marm64x), deployed to dist lib/wine/aarch64-windows/ntdll.dll (PE, no codesign). nullpin run (MACRUNNER_HB_TRACE_NULLCALL=1) in flight — expect nullcall-native (caller module+rva+blr reg) + nullcall-entry (thread-entry module+insns) → identifies the input-thread + the NULL fn-ptr.
NULL-CALL FULLY PINNED via the nullpin run (enriched probe): nullcall-native lr=0x87EFAAA8994 module=**sechost.dll** rva=0x18994 callins=d63f0100=`blr x8`; entry rva 0x18900 = **sechost!device_notify_proc** (disasm). The faulting call (rva 0x18990): `adrp x8,.data(0x60000); ldr x8,[x8,#0x238]; blr x8` with x0=0 x1=&buf x2=0 x3=&buf x4=0 x5=&buf = the **RpcStringBindingComposeW(ObjUuid,ProtSeq,NetAddr,Endpoint,Options,&out)** signature. **[0x60238] = __imp_RpcStringBindingComposeW** (sechost .data sec4 = the rpcrt4 DELAY-import IAT, slots 0x60208-0x60248 = RpcBindingFree/RpcStringBindingComposeW/RpcStringFreeW/...). ROOT: rpcrt4 IS loaded (its imports resolve), but the **delay-import binding NEVER ran** (delay-load/snap=0 in log) → sechost's rpcrt4 delay-IAT slots are NULL → device_notify_proc directly `blr`s the NULL slot. TRIGGER = Unity `Windows.Gaming.Input` init (Gaming.Input:1) spins up sechost's device-notify RPC thread. Unhandled c0000005 → HK exits rc=5. = DELAY-IMPORT BINDING GAP in the ARM64EC loader (wine loader.c delay-descriptor processing / delay-IAT thunk init), Lane A, would affect ANY builtin's delay imports.
2026-06-17 · delay-import path investigation — ★ROOT REFINED to ARM64X .data-coherence (not a plain delay-bind gap). On-disk sechost .data: 0x60218-0x60228 = delay-IAT slots WITH thunk ptrs (0x18001094c etc.); 0x60230+ = STRING data ("security"/"service"); __hybrid_auxiliary_delayload_iat symbols + .a64xrm section present. The native-ARM64 device_notify_proc reads [0x60238] expecting the ARM64-VIEW .data (a delay-IAT thunk ptr), but the file/base view there is string bytes ⇒ the ARM64-view .data is produced by the .a64xrm DYNAMIC RELOCATIONS, and at runtime that slot is NULL ⇒ this is the **ARM64X .data VIEW-COHERENCE gap**: sechost's ARM64-view .data (incl. its delay/auxiliary IAT) isn't established. sechost base = 0x87EFA90000 (entry 0x18900); slot @ base+0x60238. Connects to the KNOWN deferred issue [[arm64x-sync-stopgaps]] (per-twin .data two-copy sync; kernelbase locale DONE; sechost NOT covered; proper fix = general ARM64X .data coherence). Earlier "delay-import binding gap" framing was incomplete — the deeper root is .data view-coherence. Loader machinery exists (LdrResolveDelayLoadedAPI :8495, macrunner_hb_delay_load_* :3611, unix relocate_pe delay-dir build :1452, __hybrid_auxiliary_delayload_iat) but the ARM64-view .data isn't coherent.
2026-06-17 · .data-dump probe (nulldata run) — ★CORRECTS the diagnosis AGAIN (twice now): the delay-IAT slot is NOT NULL. Live ARM64-view sechost .data (base=0x87EFAA90000): [0x60208..0x60248] = a sequence of ptrs incrementing by 0xC into .text 0x1DC24+ = the DELAY-LOAD THUNK STUBS (correctly populated). [0x60238]=0x87EFAAADC6C = a valid ptr to thunk#7. Disasm: each 12-byte thunk (e.g. 0x1DC6C) = `adrp x17,.data; add x17,#0x238(=&IATslot); b 0x1DC90` (common helper); helper 0x1DC90 saves args, `blr` the resolver (__delayLoadHelper2→ResolveDelayLoadedAPI), `mov x16,x0`, later tail-`br x16`. So `blr x8`(device_notify_proc) jumps INTO the thunk; the pc=0x0 fault is the helper tail-`br x16` where x16=resolver-result=0. ⇒ NOT a .data/coherence/slot bug — the **delay-load RESOLVER returns NULL**. LdrResolveDelayLoadedAPI (loader.c:8497) returns NULL only via the `fail` path (LdrGetProcedureAddress failed, nts!=0, then dllhook/syshook→NULL); macrunner_hb_maybe_register_dynamic_import_thunk is a PASSTHROUGH for native-ARM64 importers (sechost) so it's not that. ⇒ ROOT = **LdrGetProcedureAddress(rpcrt4,"RpcStringBindingComposeW") FAILS under ARM64EC** for an already-loaded rpcrt4 → delay-resolve returns NULL → tail-br NULL → rc=5. (My ARM64X-.data-coherence + delay-bind framings were BOTH wrong — disconfirmed by the .data dump showing valid thunk ptrs.)
2026-06-17 · delayresolve probe (instrumented LdrResolveDelayLoadedAPI) — ★ROOT PINNED (4th/final, prior 3 corrected). Only 4 delay-resolves logged, ALL ok (shell32 SHGetDesktopFolder, ole32 CoInitialize/CoRegisterClassObject, user32 GetDpiForMonitorInternal; nts=0) — **rpcrt4 NOT among them**, so LdrResolveDelayLoadedAPI is NEVER reached for RpcStringBindingComposeW, yet sechost still crashes. Disasm of the resolver path: thunk 0x1DC6C→helper 0x1DC90 (sets x0=delay-desc 0x4b96c, x1=thunk, blr 0x17cdc)→**__delayLoadHelper2 @ 0x17cdc**: `adrp x6,0x40000; ldr x6,[x6,#0x148]; ...; br x6` = tail-branch to **[sechost __imp_ResolveDelayLoadedAPI @ rva 0x40148]** which is **NULL** → br 0 → pc=0x0 → rc=5 (BEFORE the real resolver, hence no log). sechost imports `ResolveDelayLoadedAPI` from **kernelbase.dll** (kernelbase.spec:1393 `@ stdcall -import ResolveDelayLoadedAPI(...) LdrResolveDelayLoadedAPI` = a FORWARDER → ntdll.LdrResolveDelayLoadedAPI; ntdll itself only exports the Ldr-prefixed name). So the bug = **sechost's forwarder import kernelbase!ResolveDelayLoadedAPI→ntdll.LdrResolveDelayLoadedAPI is left NULL in sechost's ARM64-view IAT** (native ARM64X builtin forwarded-export binding gap; x64 guest shell32/ole32/user32 bound theirs fine → their delay-loads work). ALL prior framings (NULL-slot, .data-coherence, resolver-returns-NULL) corrected: the delay-IAT + thunks are fine; the missing link is the __imp_ResolveDelayLoadedAPI forwarder slot.
2026-06-17 · STEP 1 (import-bind DIAGs) — ★OVERTURNS fix-(A) premise. prefix-DIAG EMPTY (ResolveDelayLoadedAPI NOT in the ARM64X prefix set → the continue-skip path is NOT involved). mainloop-DIAG: `sechost.dll dll=kernelbase.dll import=ResolveDelayLoadedAPI find_named=0x87EFE723AF0 FINAL=0x87FFF913670` → the binding SUCCEEDS: find_named_export DOES follow the kernelbase→ntdll forwarder, IAT slot written 0x87FFF913670 (=native ARM64 ntdll.LdrResolveDelayLoadedAPI, NON-NULL); binds twice (2nd pass writes 0x87FFF8241AC). ⇒ the forwarded-export binding is NOT broken — fix-(A)-as-scoped (make binding resolve forwarders) is MOOT. YET runtime __delayLoadHelper2 tail-`br [__imp_ResolveDelayLoadedAPI @ rva 0x40148]` still reads NULL (crash pc=0x0; rpcrt4 never reaches resolver, delayresolve total=4 no rpcrt4). ⇒ the runtime ARM64-VIEW IAT slot at 0x40148 is NULL despite the binding writing a valid address = an ARM64X IAT VIEW-COHERENCE problem (bound value ≠ what native code reads at 0x40148), NOT a binding-logic gap. Connects [[arm64x-sync-stopgaps]] (per-twin .data/.rdata two-copy sync; kernelbase done, sechost not). STEP 1 saved a wrong fix.
2026-06-17 · runtime-IAT-dump attempts (iatdump, iatdump2) — ★the input-init/RPC stage is STOCHASTIC: runs hit DIFFERENT walls. iatdump: crash via UNIX segv path (nullcall-site source=segv-exec0 guest pc=0x87efaaa8900=sechost device_notify_proc entry) → PE-side dump didn't fire (wrong handler). Added the IAT+helper dump to the UNIX nullcall-site path too (mach_vm_read [base+0x40100..0x40180] + helper@0x17cdc). iatdump2: hit a THIRD wall — nullcall-site source=primary-segv host_pc=0x87fff9522e0 (ntdll) fault=0x60 (near-NULL struct deref) tid=92, "no registered x64 ctx" (returns before the dump), run HUNG exit=143 (NOT the sechost rc=5). So across runs the walls vary: sechost device_notify_proc exec-at-0 (rc=5) / ntdll fault-0x60 (hang) / non-application-target — = a RACE / systemic ARM64X fragility at input-init (Windows.Gaming.Input→RPC/sechost), not one deterministic slot. Couldn't capture the sechost IAT dump (crash didn't recur). DURABLE STEP-1 RESULT STANDS: forwarded-export binding WORKS (non-NULL) → fix-(A)-as-scoped MOOT; sechost crash (when it occurs) = runtime IAT NULL despite bind = ARM64X .rdata view-coherence (unconfirmed via runtime dump due to stochasticity).
LOOP-STATUS: BLOCKED at input-init (STOCHASTIC) — STEP 1 overturned fix-(A): forwarded-export binding SUCCEEDS (sechost!ResolveDelayLoadedAPI→0x87FFF913670 non-NULL). Crash class varies per-run (sechost exec-at-0 rc=5 / ntdll fault-0x60 hang / non-application-target) = race/systemic ARM64X fragility at Windows.Gaming.Input→RPC/sechost, NOT one clean binding gap. The sechost variant = runtime ARM64-view IAT [0x40148] NULL despite valid bind (ARM64X view-coherence) but couldn't pin the runtime value (stochastic, didn't recur). NEXT APPROACH (rethink needed): deterministic repro of the input-init stage (pin one wall) OR systemic ARM64X .rdata IAT coherence (sync bound IAT into the ARM64 view for all builtins, [[arm64x-sync-stopgaps]]) OR neutralize the Windows.Gaming.Input path (device_notify_proc non-essential).
2026-06-17 · OPTION 2 (neutralize the flaky device-notify path). WGI-only stub (windows.gaming.input/main.c skip RegisterDeviceNotificationW) = INSUFFICIENT: re-run still crashed (lr=0x87EFAAA8994=sechost device_notify_proc, exit=5) because when WGI fails Unity FALLS BACK TO XINPUT, and xinput1_3/main.c:706 ALSO calls RegisterDeviceNotificationW → same I_ScRegisterDeviceNotification → same sechost device_notify_proc. ⇒ moved the stub to the SINGLE COMMON SINK: sechost/service.c:2172 I_ScRegisterDeviceNotification — skip spawning device_notify_thread (registration still returns a valid `notify` handle so all callers proceed; device HOTPLUG non-essential for first frame). Catches WGI + XInput + any registrant. WGI main.c reverted to original (untracked anyway — engine/ gitignored, only force-added sources tracked). Built clean WGI (both arches) + stubbed sechost (both arches) + deployed. NB sechost/service.c is UNTRACKED → must `git add -f` it to commit (per track-load-bearing-sources rule).
2026-06-17 · ★SECHOST STUB VERIFIED + COMMITTED a2401d9. Re-run (sechoststub): the input-init device_notify_proc crash is GONE (no run-exit, no 0x87EFAAA8994), and the input subsystem now FULLY INITIALIZES: `Using XInput` → `<RI> Input initialized.` (WGI fails→XInput fallback completes). Boot ADVANCED past the stochastic input-init wall. service.c force-added (was untracked). ★NEW WALL: after "Input initialized" the boot produces NO further output and HANGS to the 420s cap (exit=143) — a SILENT hang (wait/deadlock), NOT a crash, BEFORE SwapChain/PlayerLoop/scene/render. GpuFence::Create still E_FAIL (non-fatal). NEXT: characterize the post-input-init hang — monitored sample run (per-thread states / what the main thread waits on) OR wait-semantic trace — determine deterministic-wait vs stochastic. Furthest point now = INPUT-INITIALIZED (past input-init). COMMITS(9): +a2401d9 sechost-stub, +2f9b135 CHPE-guard.
2026-06-17 · POST-INPUT-INIT HANG CHARACTERIZED (monitored profiler hang-char, per-thread sample t0337). NOT a deadlock — a DETERMINISTIC THROUGHPUT SPIN: of 50 threads, 47 idle-parked (mach_msg/ulock_wait/workq), main thread idle in CFRunLoopRun→mach_msg; ONLY Thread_930813 busy (n=1438/1443, CPU~122% whole hang). Its stack: macrunner_hb_run_x64→hb_jit_runtime_run→JIT helpers (hb_jit_helper_exec_binop/extend_operand_lazy)→hb_memory_read→**macrunner_hb_special_read→mach_vm_region(:3768)+mach_vm_read_overwrite(:3788)** per guest read + macrunner_hb_trace_mono_probe_any_ + update_current_x64_context. = guest Mono-JIT code whose memory reads MISS the fast path (find_region) and fall to special_read = 2 MACH syscalls/read (special_read HAS a region cache cache_live_region:3783 but it's still thrashing/missing). SAME CLASS as the prior rank-7 Mono-grind throughput wins (module_from_pc/find_region/IR-cache). Log stops (no markers) because Mono C# execution emits none. OPEN: progressing-slowly vs stuck-spin (e.g., Mono polling a never-true condition — note GpuFence::Create E_FAIL could make Unity wait, but the spinner is in MONO not render code). Per operator tree: DETERMINISTIC → empirical loop. NEXT: (a) cheap LONGER run (1200s) to confirm progressing-vs-stuck; (b) attack special_read overhead — why do Mono reads miss find_region→special_read (register Mono regions / fast-path them / cache) + check if macrunner_hb_trace_mono_probe_any_ is always-on hot-path overhead to gate. GpuFence/ID3D11Fence (Lane D) possible compounding.
2026-06-17 · LONG-HAUL (1200s) VERDICT — progressing-vs-stuck RESOLVED. The 122% Mono grind PROGRESSED + COMPLETED (~t=368s, CPU 122%→30%, reached `<RI> Input initialized.`) — it was Mono ReloadAssembly finishing, NOT the wall. AFTER input-init the boot STALLS: t=460→1200 d+=0 (no markers), CPU ~30%. Per-thread sample @ t=1109: 51/53 parked; only 2 threads active in macrunner_hb_run_x64 running ntdll/kernelbase GUEST code (try_ntdll_version / pc_is_x64_guest_code / is_registered_x64 / update_current_x64_context) + RECURRING callback-exception-frame-map (frame_pc ntdll 0x74ed4/0x754f4, frame_lr **kernelbase+0x7954c**). = the X18-FAULT-STORM (macOS wipes x18 → guest kernelbase TEB/sync access faults → heal → repeat) bottlenecking a guest kernelbase SYNC/WAIT loop; d+=0 for 800s ⇒ effectively STUCK, not throughput. = the KNOWN deep Lane-A x18/host-TEB NEEDS (memory: "residual stochastic spin = x18-fault-storm slowness"), NOT a quick fix. NEXT (fresh): guest-PC heartbeat probe to pin the exact kernelbase wait the 2 threads spin on (which sync primitive / what they await — possibly Unity job/render completion tied to GpuFence E_FAIL), then tackle x18/host-TEB OR the awaited producer. COMMITS(9): a2401d9 sechost-stub, 2f9b135 CHPE-guard (+prior).
2026-06-17 · POST-INPUT-INIT STALL probe (operator: empirical, don't assume x18; KEY LEAD = GpuFence-tied?). ★Disasm of the recurring fault lr kernelbase+0x7954c = **SetWaitableTimer+0x2c** (fn @ rva 0x79520; the return after its inner blr x9). So the 2 stalled threads loop through SetWaitableTimer = a TIMED-WAIT/POLL loop; the recurring callback-exception-frame-map = the timer-paced callback delivery (ntdll Tp* thread-pool nearby at pc rva 0x754f4). STRONGLY fits the GpuFence lead: a timed poll awaiting a render/fence completion that never comes (GpuFence::Create E_FAIL → DXMT missing ID3D11Fence). Heartbeat guest-PC probe is COMMITTED+env-gated (MACRUNNER_HB_TRACE_HEARTBEAT[_MODULE]) — NO rebuild. Run (heartbeat, 480s) in flight to capture the guest PC the spinners loop on (the UnityPlayer/Mono caller of the timed wait) → disasm → confirm what they await + GpuFence-tied. If GpuFence-tied → fix = ID3D11Fence in DXMT (Lane D, targeted, needed for present anyway). If not → genuine x18/host-TEB systemic.
2026-06-17 · HEARTBEAT + per-thread sample VERDICT — ★GpuFence lead REFUTED; it's a MONO LOCK-FREE LIVELOCK. Module @0x87ef0890000 = **mono-2.0-bdwgc.dll** (HK Mono+Boehm GC). Heartbeat (post-input-init) dominated by: UnityPlayer 0x17b2bb0 (FMOD sw-mixer, RED HERRING) + mono 0x14df30-4f. Disasm of mono 0x14df30: a HAZARD-POINTER ACQUIRE spin — `mov [r9+8],rax`(publish) `lock or [rsp],ebp`(FENCE) `mov rcx,[rdx+0x18]`(re-read) `cmp;je exit` else re-publish+retry; exits only when re-read observes the published ptr. Operator PCs 0x4c1f9b/0xc4da5 = Mono fn return-epilogues (hot call loop). Per-thread sample (t1109): NO thread in DXMT/Metal/fence wait; 21 Unity Job.Workers PARKED (NtWaitForAlertByThreadId/WaitOnAddress); main idle CFRunLoop; AssetGC×7/Finalizer idle (NtWaitForSingleObject); only FMOD mixer + 2 Mono threads active. ⇒ NOT GpuFence (nothing waits on the failed fence; it was handled non-fatal) → did NOT implement ID3D11Fence. REAL WALL = Mono lock-free hazard-pointer spin LIVELOCKS (800s zero progress) = a STORE→LOAD ordering loop (publish-store; fence; re-read); PRIME SUSPECT = HyperBridge not lowering `lock or [rsp],ebp` (lock-prefixed RMW-as-fence) to a full DMB → re-read sees stale → never converges. = LANE A MEMORY-ORDERING (hb_arm64_codegen.c lock-prefix/atomic lowering; adjacent to commit 19431a1 "x86 TSO load lowering...fixes Mono SIGBUS livelock"). NEXT: verify how the JIT lowers `lock or [mem],reg` (does it emit DMB ISH?) — if the barrier is missing/weak, that's the fix; connects to the LDAR/TSO work. NOT x18 (x18=0% throughout, refuted), NOT GpuFence, NOT FMOD (red herring).
2026-06-17 · ROOT CONFIRMED + FIX DESIGNED (JIT codegen read). emit_atomic_ir_helper (hb_arm64_codegen.c:3020) DOES bracket atomic ops with emit_dmb_ish before+after — BUT block_has_atomic_ir (:3030) only recognizes HB_IR_CMPXCHG/CMPXCHG8B/XCHG/XADD as atomic. AND hb_ir_instr_t (hb_ir.h:558) has NO lock/atomic flag → `lock or [mem],reg` and plain `or [mem]` decode to the SAME HB_IR_OR; the decoder only maps XCHG/CMPXCHG/XADD to distinct atomic IR. ⇒ lock-prefixed RMW used as FENCES (lock or/and/add/sub/inc/dec/bt*/btc/bts/btr) carry NO barrier through the JIT. Mono's hazard-pointer loop uses `lock or [rsp],ebp` purely as a full memory FENCE for store([r9+8])→load([rdx+0x18]) ordering; without DMB, ARM64 weak memory reorders → re-read stale → validate never converges → LIVELOCK. ★FIX (Lane A memory-ordering, scoped, our domain): (1) hb_ir.h add a `lock`/`is_atomic` bool to hb_ir_instr_t; (2) hb_interpreter.c decoder: set it when the 0xF0 LOCK prefix is present on a memory-RMW op; (3) hb_arm64_codegen.c: block_has_atomic_ir (or a per-instr check) → treat lock-flagged instrs as atomic → route through emit_atomic_ir_helper (DMB ISH before+after) OR at minimum emit DMB around them. Connects to commit 19431a1 (LDAR/TSO "fixes Mono SIGBUS livelock") — same Mono memory-ordering family, different primitive (store→load fence vs acquire-load). VERIFY: re-run → hazard-pointer loop converges → boot advances past the Mono stall. Marathon checkpoint: 2 committed HK-boot fixes (2f9b135, a2401d9); this is the next fix, fully designed.
2026-06-17 · LOCK-PREFIX BARRIER FIX IMPLEMENTED (operator GO). 4 files: (1) hb_ir.h — added `bool is_locked` to hb_ir_instr_t; (2) hb_decoder.h — added `bool lock_prefix` to hb_decoded_t; (3) hb_decode_x64.c — prefix loop captures 0xF0 → out->lock_prefix; (4) hb_lift_x64.c main lift loop — stamps is_locked on all IR instrs of a lock-prefixed x86 op (block->instrs[pre..count]); (5) hb_arm64_codegen.c — block-codegen loop (8344) + public codegen_instr wrapper (8049) bracket is_locked instrs with emit_dmb_ish (DMB ISH) before+after. So `lock or [rsp],r` (Mono hazard-ptr fence) now gets a full barrier → store→load ordering enforced → loop should converge. Built libhyperbridge.a clean (-Werror passed), relinked ntdll.so (rm+make, libhyperbridge not a make prereq), deployed+codesigned both dist. VERIFY RUN (lockfix profiler, 600s) in flight — expect Mono spin clears → boot advances past the post-input-init stall to new markers/scene/PlayerLoop/SwapChain/Present. NB interpreter path (hb_interpreter.c) locked-op barrier NOT added (spin is JIT; interpreter is fallback) — note for completeness.
2026-06-17 · VERIFY (lockfix) — fix did NOT clear the stall; boot trajectory IDENTICAL (Mono grind 122%→Input initialized ~t372→CPU 32% stall, d+=0 t435-622, no markers). 2 possibilities: (1) BARRIER BYPASSED — block_has_atomic_ir (gates loop-optimizers emit_two_block_loop_helper/etc.) only checks CMPXCHG/XCHG/XADD, NOT is_locked → a hot lock-containing loop matched by a loop-optimizer bypasses codegen_instr (where the DMB wrap is) → no barrier; (2) the Mono hazard-ptr spin is NOT the blocker (red herring like FMOD) — real stall = 21 parked Job.Workers (NtWaitForAlertByThreadId, never woken) + main waiting on the job system (earlier worker-wake pattern). Fix UNCOMMITTED (unverified). NEXT: close #1 — make block_has_atomic_ir also return true for is_locked instrs (loop-optimizers skip → route through barriered codegen_instr) + re-verify; if STILL no change → diagnosis #2 (Mono spin = red herring; attack the job-system worker-wake / main-thread wait instead).
2026-06-17 · RE-VERIFY (lockfix2, block_has_atomic_ir+=is_locked) — STILL IDENTICAL trajectory (Mono grind→Input initialized t372→32% stall→d+=0→cap). ★VERDICT: the lock-prefix barrier fix is REFUTED for this wall — the Mono hazard-pointer spin is a RED HERRING (boot independent of its convergence). My diagnosis was WRONG. REAL BLOCKER (per the per-thread sample, already in hand): 21 Unity Job.Workers PARKED on NtWaitForAlertByThreadId (WaitOnAddress, NEVER woken) + main idle CFRunLoop = the JOB-SYSTEM WORKER-WAKE pattern — same (a)-family "wake never issued" as the earlier rank-7 producer/dispatch investigation, recurring post-input-init. Mono-spin + FMOD = busy background red herrings. LOCK-PREFIX FIX (hb_ir.h/hb_decoder.h/hb_decode_x64.c/hb_lift_x64.c/hb_arm64_codegen.c, all UNCOMMITTED, deployed in dist ntdll.so) = correct-in-principle (x86 LOCK must be a full barrier — real mem-ordering gap, builds clean -Werror, no observed harm) BUT unverified-for-this-wall → per "commit after verify" do NOT commit as-is; DISPOSITION TBD (revert clean / commit as correctness fix / keep deployed-uncommitted). ★NEXT (real wall): job-system worker-wake — why the 21 Job.Workers parked on WaitOnAddress(0xff800001-style) are never woken post-input-init + what the main thread awaits (connect to the earlier producer/dispatch pin: WakeByAddress never issued for worker slots). MARATHON CHECKPOINT.
2026-06-17 · ★CHECKPOINT (operator: commit lock-prefix as correctness + checkpoint). Lock-prefix barrier fix COMMITTED d7e30f7 (correctness fix — closes real x86-LOCK full-barrier gap; clearly labeled does-NOT-clear-this-wall; x86-32 decoder not yet covered). SESSION TOTAL: 3 committed HK-boot fixes — 2f9b135 CHPE-guard (Mono-ReloadAssembly-death→graphics-full), a2401d9 sechost-stub (→input-full), d7e30f7 lock-prefix-barrier (mem-ordering correctness). Boot frontier: Mono-death→GRAPHICS-FULL(D3D11 11.0/M1 Pro/21 buffers)→INPUT-FULL(Using XInput)→post-input-init JOB-SYSTEM-WORKER-WAKE stall. dist=these 3 fixes; tree clean; no orphans. ★NEXT SESSION (the real wall, fresh): job-system worker-wake — 21 Job.Workers parked NtWaitForAlertByThreadId never woken + main idle CFRunLoop. Reuse the earlier producer/dispatch findings (WakeByAddress never issued for the 0xff800001 worker slots; the wake is never PRODUCED). Mono-spin + FMOD = confirmed red herrings (boot independent of them). Probe approach: id the thread that SHOULD wake the workers (the job dispatcher) + why it doesn't post-input-init — atomic-visibility (now that lock-RMW barriers exist, re-check) OR a producer stuck upstream OR a missing WakeByAddress on the enqueue path. CHPE-guard COMMITTED 2f9b135 (durable; boot Mono-death→graphics+input). Diagnostics uncommitted (env-gated/filtered); delayresolve now env-gated. COMMITS(8).
2026-06-17 · CONSOLIDATED (operator's call). Reverted ALL uncommitted diagnostic probes (git checkout macrunner_hb.c/signal_arm64.c/loader.c) → source tree = HEAD (2f9b135). Rebuilt CLEAN ntdll.so (native gcc, CHPE fix) + ntdll.dll (toolchain-on-PATH, no diagnostics) + redeployed both (codesigned .so). dist now = the verified-good 2f9b135 state (CHPE fix → graphics+input; NO diagnostic spam). No orphans. Probe TECHNIQUES + findings all preserved here (re-addable): chpe-refuse (committed in 2f9b135), nonapp-refuse, nullcall-native/entry/data + IAT/helper dump (both PE seh-host-boundary AND unix nullcall-site paths), delayresolve, prefix/mainloop bind DIAGs. NEXT SESSION (fresh approach, NOT the binding — that works): the input-init wall is STOCHASTIC (sechost exec-at-0 rc=5 / ntdll fault-0x60 hang / non-application-target) = race/systemic ARM64X fragility at Windows.Gaming.Input→RPC/sechost. Best attacks: (1) deterministic probe of sechost [0x40148] at FIXED points (post-bind + device_notify_proc thread-start) to confirm/refute 'slot NULL after valid bind' without the stochastic crash; (2) neutralize/stub the WGI init that spawns device_notify_proc (non-essential for HK) to remove the wall + expose the next deterministic one; (3) systemic ARM64X .rdata IAT coherence ([[arm64x-sync-stopgaps]]). The slot is sechost's import of **kernelbase!ResolveDelayLoadedAPI** (a -import FORWARDER→ntdll.LdrResolveDelayLoadedAPI; kernelbase.spec:1393) that DID NOT BIND in sechost's native ARM64X IAT. Triggered by Unity Windows.Gaming.Input→sechost device_notify_proc delay-loading rpcrt4. FIX OPTIONS: (A) fix wine import binding so native ARM64X builtins resolve forwarded kernelbase exports (the kernelbase→ntdll forwarder) — find_forwarded_export/fixup_imports under ARM64EC; (B) add a direct ntdll `ResolveDelayLoadedAPI` alias export so the import resolves without the kernelbase forwarder; (C) probe the import-binding to see why this forwarder slot is NULL for sechost (vs working modules). device_notify_proc non-essential but unhandled crash kills process. CHECKPOINT: marathon (4 probe runs this sub-wall, 3 self-corrections); CHPE-guard COMMITTED 2f9b135 (boot Mono-death→graphics+input-init). Diagnostic probes uncommitted (env-gated/rate-limited). COMMITS(8).
2026-06-18 · ★(c) REFUTED, (b) CONFIRMED — job-dispatcher wake probe (operator GO: pin dispatcher, (b)/(c)). Built-in MACRUNNER_HB_TRACE_WAITADDR run (TMO=450, reached stall): wake path HEALTHY — **13/13 wakes DELIVERED (found>0), ZERO lost (found=0)**. Only **44 total WaitOnAddress calls in 450s** (all under the 64 rate-cap = ALL): workers park ONCE (INFINITE, cmp=0x0, on 0x87ef249xxxx UnityPlayer region; tids 0054/011c/0120) + the 0xff800004 semaphore-counter wait (tid 0090 @0x3202ef380) + a poller (tid 0110) cycling its OWN timed addr 0x3c0414c60. After input-init (log line 1646): only 2 waits/1 wake. ⇒ WakeByAddress emulation is NOT the gap. ARCH: HB intercepts x64 WaitOnAddress/WakeByAddress* → its OWN private registry macrunner_hb_wait_addr_queues[256] (hash (addr>>4)%256); wait+wake-single+wake-all share it, internally consistent (verified, macrunner_hb.c:12609-12848). com.apple.main-thread = WINE Mac CFRunLoop (__wine_main loader.c:3476→CFRunLoopRun→mach_msg2_trap), NOT Unity main (Wine runs the app on a separate pthread). Census: 23 Job.Workers+UnityGfxDeviceWorker+callbacks parked on WaitOnAddress; 8 AssetGCHelper+Finalizer+2 pollers (1095425/1095426) on **NtWaitForSingleObject (kernel objects via wineserver)**; pollers do tiny JIT bursts (alive). VERDICT: **(b)** — 13 wakes/450s = Unity main/boot thread NEVER reaches job-enqueue; workers idle CORRECTLY awaiting work never produced. Real wall is UPSTREAM on a KERNEL OBJECT, not address-wait. There is NO "missing wake" to fix at the WaitOnAddress layer. NEXT (run in-flight): MACRUNNER_HB_TRACE_WAIT_SEMANTIC=1 BUDGET=150000 — kernel32-handle-semantic trace (macrunner_hb.c:15250 WaitForSingleObject/Ex + :15296 WaitForMultipleObjects) logs handle+timeout+caller PC/module, before/after paired (stuck=before-without-after). Pin which thread = Unity main/boot driver blocks forever on which handle + who should signal it. COMMITS(8).
2026-06-18 · ★STUCK WAIT PINNED + FMOD-spin REFUTED — WAIT_SEMANTIC run (BUDGET=150000, reached stall L109720, 138k lines/28.7MB, triage=PRESENT_MISSING+secondary WAIT_DEADLOCK). KERNEL HANDLES: 68055 WaitForSingleObjectEx, **68035 return status=0 (WAIT_OBJECT_0 = signaled-immediately)**, only 10 TIMEOUT. Traffic = 2 handles (0x1dc 40k×, 0x1d0 28k×) BOTH from guest caller 0x87efdc4f244, INFINITE, returning signaled — a SPIN, not a block. BUT it spins from **line 1792 (early boot) continuously THROUGH the stall** (pre=53893/post=14126) = present even while boot advanced ⇒ **FMOD software-mixer background poll, BENIGN red herring** (matches prior FMOD finding; log: 'FMOD…emulated software output'). = the 2 'busy poller' threads 1095425/1095426. ADDRESS WAITS (caller PCs): (a) caller **0x87efca2e443** = 23 Job.Workers parking (23 distinct semaphore addrs 0x3002xxxxx + 0x87ef249xxxx), all pre-stall, never re-wait; (b) caller 0x87efc7bee71/…beebb = live poller pair (wait+wake on 0x3a0414c60, Mono GC/timer, still cycling post-stall); (c) ★caller **0x87efeef8409 = the BOOT/MAIN thread** — ran a SEQUENCE of WaitOnAddress(0x87ef249xxxx fence, INFINITE, cmp=0) steps successfully through input-init, then at **L109846 (just after stall onset) WaitOnAddress(0x87ef2496e48) and NEVER returns** (last addr-wait in the run). Post-stall the ONLY wake issued = to the poller, **NEVER to a worker**. ⇒ TERMINAL LOST-WAKEUP: boot thread blocks on a final job/fence at 0x87ef2496e48 that no worker is woken to fulfil; wake MECHANISM is healthy so it's not a delivery gap — no worker-wake is ISSUED for this job. FULL CENSUS @stall: every thread parked/idle EXCEPT the 2 FMOD spinners — UnityGfxDeviceWorker parked on WaitOnAddress (awaiting render cmds), winemetal SharedEventListener×2 idle CFRunLoop, wine_xinput_hid_update in NtUserMsgWaitForMultipleObjectsEx, AssetGC×8+Finalizer on NtWaitForSingleObject. ★OPEN FORK (needs disasm of caller 0x87efeef8409 + identity of fence 0x87ef2496e48; needs this-run module base map — get via heartbeat-module probe or loader dump): is 0x87ef2496e48 a JOB-system completion fence (→ lost-wakeup/memory-ordering race on the enqueue↔park, Lane A, connects to d7e30f7/19431a1 TSO work) OR a RENDER/PRESENT/async-IO fence (→ Lane D graphics, matches PRESENT_MISSING triage — boot thread awaiting first render/present that DXMT never completes)? Could NOT compute module base for 0x87efc/0x87efe callers from this run.log (UnityPlayer base not logged; DXGI@0x87ef2530000, d3d11@0x87ef2970000 known). COMMITS(8).
2026-06-18 · ★FORK RESOLVED → GRAPHICS (Lane D), NOT Lane A. (operator GO: resolve fork first.) modmap run (HEARTBEAT_MODULE=1 + WAIT_SEMANTIC=1, reached stall): heartbeat-module gave guest bases — UnityPlayer.dll@0x87efc510000, mono-2.0-bdwgc@0x87ef0890000, **kernelbase.dll@0x87efee90000**, d3d11-wg.dll@0x87ef1cd0000, dxgi-wg@0x87ef2530000, dxgi@0x87ef2820000, d3d11@0x87ef2970000. Mapping the stuck boot caller 0x87efeef8409 → **kernelbase.dll+0x68409**; disasm of dist x86_64 kernelbase.dll rva 0x68409 = **kernelbase!WaitOnAddress+0x29** (return after `callq *__imp_RtlWaitOnAddress`; fn @0x1740683e0). So the boot/main thread is blocked in the PUBLIC `WaitOnAddress` API (cmp=0, INFINITE) on heap sync objects 0x87ef249xxxx. CONVERGENT EVIDENCE it's GRAPHICS: (1) boot uses public kernelbase!WaitOnAddress = C++/DXMT style, NOT Unity Baselib (workers call RtlWaitOnAddress DIRECTLY from UnityPlayer+0x51e443); (2) heartbeat shows a thread executing **d3d11-wg.dll** code (pc 0x87ef1e3818c) at the EXACT stall onset (L387080-387324), interleaved with the boot WaitOnAddress (L387090/387145); (3) wait objects 0x87ef249xxxx sit in the heap cluster adjacent to the graphics DLLs (d3d11-wg/dxgi-wg); (4) triage independently = PRESENT_MISSING; (5) UnityGfxDeviceWorker parked awaiting render cmds. ⇒ Unity MAIN thread deadlocks inside the d3d11 (DXMT) layer on a WaitOnAddress for a graphics sync object (GPU fence / command-buffer completion / present sync) that DXMT never signals → no first present. NOT a Lane A job-system memory-ordering race. ★STRATEGIC: the Lane A engine fixes (CHPE 2f9b135, sechost a2401d9, lock-prefix d7e30f7) carry HK boot ALL THE WAY to the first graphics-present wait — Lane A has done its part for the boot path; the HK first-frame wall is now LANE D (DXMT present/fence path), matching CLAUDE.md 'remaining path is ALL graphics'. Disasm of on-disk x86_64 DXMT d3d11 @rva 0x16818c hit libc++ init_wam_pm ⇒ loaded d3d11-wg is a DIFFERENT layout (likely the ARM64EC/aarch64 DXMT d3d11 x64-CHPE side) → can't name the exact fn from the x86_64 file. AIRTIGHT REFINEMENT (optional, for Lane D): add `outer` deeper-stack capture to the address-wait trace (the WaitForSingleObject path already logs outer via macrunner_hb_trace_stack_address(ctx,6)) → 1 run → names the UnityPlayer/DXMT fn that called WaitOnAddress + the exact DXMT entry. COMMITS(8).
2026-06-18 · ONE-LANE DXMT FIX (operator GO: option 1 then continue in-terminal, no handoff, drive to first frame). CONFIRMED graphics root via DXMT source map: DXMT uses **CpuFence = std::atomic<uint64_t>::wait()/notify_all()** (util_cpu_fence.hpp); libc++ std::atomic::wait→WaitOnAddress (8-byte, cmp=0, INFINITE) EXACTLY matches the boot thread → boot thread stuck in a DXMT CpuFence::wait (NOT ready_.wait which is atomic<bool>/1-byte). Signal chain: main CommitCurrentChunk(ready_for_encode++) → encodeThread commits Metal cmdbuf (CommitChunkInternal, cmdbuf.commit(); NB line 137 cmdbuf.encodeWaitForEvent(resource-initializer MTLSharedEvent) — cmdbuf waits on GPU for the initializer event) → ready_for_commit++ → finishThread waitUntilCompleted → cpu_coherent.signal + frame_latency_fence_.signal (dxmt_command_queue.cpp:148-205). DXMT log (wstack run): D3D11CreateDevice ENTER (threaded) + Direct3D 11.0 + 11x CreateBuffer, then DXMT activity STOPS — hang is during resource/pipeline init, BEFORE first present (no swapchain/Present/nextDrawable). Boot thread progresses through several CpuFence waits then hangs on ONE → signaling works generally; a SPECIFIC op never completes. Stall point VARIES run-to-run (wstack: ~L15148 graphics-init; modmap: L387145 input-init) ⇒ intermittent completion race (Metal cmdbuf completion / finish-thread signal sometimes doesn't fire). ARM64EC x64↔ARM64 stack split → raw rsp stack-walk gave zeros (can't get DXMT caller from stack). Module bases (wstack run): UnityPlayer@0x87efc510000, kernelbase@0x87efee90000, d3d11-wg@0x87ef1cd0000 (the EXECUTING d3d11, in x64 JIT = x86_64 emulated DXMT), dxgi-wg@0x87ef2530000. ★INSTRUMENTED DXMT (engine/dxmt, uncommitted): util_cpu_fence.hpp CpuFence::wait logs `dxmt-diag: cpufence-block site/fence/cur/target` (via __builtin_return_address); dxmt_command_queue.cpp logs encode-commit/finish-enter/finish-completed/finish-signaled. Built dxmt-x86_64 d3d11.dll (targeted `ninja src/d3d11/d3d11.dll` — skips broken airconv_cli native-tool link; diag verified in dll), deployed to engine/graphics/dist/dxmt/x86_64-windows/d3d11.dll. NB aarch64 d3d11 build BROKEN pre-existing (undefined __wine_dbg_output from prior macrunner createbuffer/D3D11CreateDevice tracing in d3d11_device.cpp/d3d11.cpp) — but x86_64 is the executing one (heartbeat). DIAG RUN in flight (TMO=300, no heartbeat→small log): will show WHERE the pipeline breaks (encode-commit w/o finish-completed = Metal cmdbuf never completes; finish-signaled but main still blocks = wrong fence value) + the cpufence-block site → the fix. COMMITS(8).
2026-06-18 · ★DXMT BUILD LESSON + which-d3d11-runs. (1) The x64 app's d3d11 calls REDIRECT to the ARM64EC NATIVE counterpart (IAT: D3D11CreateDevice native_cp=1 target=ARM64 d3d11) ⇒ the **aarch64-windows DXMT d3d11 (d3d11-wg)** is what actually runs the CpuFence/command-queue code, NOT the x86_64 one. (2) ★The plain `engine/graphics/build/dxmt-aarch64` build is INCOMPLETE — produces a **4.96MB** d3d11.dll MISSING the embedded Metal shaders (airconv shader-gen didn't run; airconv_cli won't link: llvm::cl vtable undefined). Deploying it → HK `exit(3)` (ucrtbase abort/std::terminate) right at `GfxDevice: creating device client`, BEFORE D3D11CreateDevice ENTER (no shaders → can't build present/blit pipelines). DETERMINISTIC (2 runs). (3) ★The COMPLETE build is `engine/graphics/build/dxmt-aarch64-hk-present` (+ `dxmt-x86_64-hk-present`) — **33MB** d3d11 with shaders embedded (Jun 13). Incremental `ninja -C dxmt-aarch64-hk-present src/d3d11/d3d11.dll` recompiles my changed TUs + relinks reusing shaders → complete d3d11 + diag. BUILD RECIPE (no airconv needed): `export PATH=engine/toolchain/llvm-mingw-.../bin:$PATH; ninja -C engine/graphics/build/dxmt-{aarch64,x86_64}-hk-present src/d3d11/d3d11.dll` → cp to engine/graphics/dist/dxmt/{aarch64,x86_64}-windows/d3d11.dll (PE, no codesign). (4) BUILD FIX (uncommitted, needed for aarch64 link): d3d11.cpp + d3d11_device.cpp MR_DXMT_OUT macro changed __wine_dbg_output→fprintf(stderr) (the __wine_dbg_output path didn't link for ARM64EC). NB: dist d3d11 NOT git-tracked; I overwrote it — now = hk-present 33MB + diag (good/complete). (5) ★FLAKY BOOT / timing race: runs WITH heartbeat reach FURTHER (input-init) than WITHOUT (loader-init/pre-device) — heartbeat slows JIT threads past an early multi-threaded-init race. So diag runs use HEARTBEAT=1 + NO_AUTOTRIAGE=1 (avoid the classify_run choke on big logs — operator lesson). DIAG RUN (dxmtdiag4, complete d3d11 + heartbeat) in flight → expect to pass device creation → reach graphics CpuFence stall → dxmt-diag (encode-commit/finish-*/cpufence-block) fires → pinpoint the unsignaled fence + broken link → fix. COMMITS(8).
2026-06-18 · ★★ROOT CAUSE CONFIRMED + FIX APPLIED — DXMT lost-wakeup on std::atomic::wait/notify under HB emulation. Progressive diag (complete hk-present d3d11): dxmtdiag4 → main blocks in CpuFence::wait(cur=0,target=1) + encode-commit=0 (no Metal cmdbuf ever committed). dxmtdiag6 (encode-thread diag) DECISIVE: `encode-thread-start` FIRED (encode thread started, ready_for_encode=1) + `commit-current-chunk` FIRED (main: ready_for_encode 1→2 + notify_one) but **`encode-woke`=0** — the encode thread's `ready_for_encode.wait(1)` NEVER returned despite value now=2 + notify_one. ⇒ **libc++ std::atomic<uint64_t>::notify_*() wakes are LOST under HyperBridge** (the contention-table WakeByAddress path doesn't reliably deliver to a thread blocked in std::atomic::wait) → DXMT's GPU-submission pipeline deadlocks at the FIRST chunk: encode thread never commits → finish thread never signals cpu_coherent → main's CpuFence::wait(1) (a Map/WaitCPUFence-style GPU-coherency wait after the 1st CreateBuffer) blocks forever → no present. (NB this is the SAME wake-delivery class as the earlier Unity-worker investigation, but the lost wake is in DXMT's own threads, in libc++ atomic wait/notify, not Unity Baselib.) ★FIX (engine/dxmt, operator GO one-lane, UNCOMMITTED): make DXMT's atomic-waits SELF-HEALING — replace the fragile untimed std::atomic::wait with a poll-the-value + 25us bounded sleep backstop (producer still advances the value correctly; only the wakeup is unreliable). 4 sites: util_cpu_fence.hpp CpuFence::wait; dxmt_command_queue.cpp EncodingThread (ready_for_encode), WaitForFinishThread (ready_for_commit), CommitCurrentChunk (chunk_ongoing). +added <chrono>/<thread>. Built+deployed both hk-present d3d11 (aarch64 33.28MB + x86_64 33.9MB). FIX-TEST RUN (dxmtfix, heartbeat) in flight → expect encode-commit/finish-completed/finish-signaled to NOW fire + boot to advance PAST the CpuFence stall → CreateSwapChain → Present → CAMetalLayer/presentDrawable/winemetal-present markers (currently 0) = first frame. Diag fprintf still in (flush1/stagingmap-wait/commit-chunk/encode-thread-start/encode-commit/finish-*) — CLEAN UP before commit. ROOT (HB-side, deeper): why libc++ atomic notify_all/WakeByAddress loses wakes for the contention-table path — defer; the DXMT poll-backstop unblocks the frame. COMMITS(8).
2026-06-18 · ★FIX v1 WORKED (deadlock broken) but my sleep_for hit an x87 JIT gap → fix v2 (yield). dxmtfix run (poll w/ std::this_thread::sleep_for(25us)): boot ADVANCED 1→16 CreateBuffers + 'Begin MonoManager ReloadAssembly' (was: 1 CreateBuffer→instant deadlock) ⇒ the lost-wake fix is CORRECT. BUT new crash: `macrunner-hb-runtime-fail ... module=d3d11.dll rva=0x5e7fa` `status=c000007b reason=runtime q0/q8=MEMORY_FAULT` on the polling threads + `GpuFence::Create: Failed to create ID3D11Fence 0x80004005`. ★Disasm d3d11 rva 0x5e7fa = inside `std::chrono::duration_cast<duration<long double,micro>>` — x87 80-bit FPU ops (fildll/fstpt/fldt). ROOT: **HyperBridge JIT cannot translate x87 80-bit long-double FPU instructions** → c000007b. The trigger is libc++ `std::this_thread::sleep_for`'s overflow guard `constexpr duration<long double> __max = nanoseconds::max()` (NOT DXMT's statistics, which use integer clock::duration — verified dxmt_statistics.hpp). ★FIX v2 (UNCOMMITTED): replace sleep_for(chrono) with `std::this_thread::yield()` (→SwitchToThread, no chrono/x87) at all 4 poll sites (util_cpu_fence.hpp + 3 in dxmt_command_queue.cpp); dropped <chrono>. Built+deployed both hk-present d3d11. dxmtfix2 run in flight → expect encode-commit/finish-completed/finish-signaled to fire + boot past the x87 crash → present. NOTE: x87-long-double JIT gap is a real HB engine gap (will bite any long-double/printf-%Lf path); deferred unless it re-blocks. GpuFence::Create E_FAIL (DXMT no ID3D11Fence) appears non-fatal (boot continued past it). NOTE perf: yield()-spin at TIME_CRITICAL burns CPU while idle — fine for first-frame, tune later. COMMITS(8).
2026-06-18 · ★FIX v2 (yield) VERIFIED CLEAN — boot reaches FURTHEST EVER. dxmtfix2 run (yield, heartbeat, TMO=420): **NO c000007b/x87 crash** (yield removed the sleep_for long-double trigger) + boot progressed through: early graphics (D3D11CreateDevice + 11 CreateBuffers) → **`Loaded All Assemblies, in 317.053 seconds`** (Mono assembly load COMPLETED — the long grind) → `Finished resetting the current domain 6.3s` → **`[Physics::Module] PhysX Multi-Threaded`** (Physics init). Then TMO=420 ran out (Mono alone ate 317s) — did NOT deadlock, ran out of TIME. So the DXMT lost-wake fix (v2 yield) is GOOD: unblocks the GPU-pipeline deadlock + no x87. commit-current-chunk=0 this run (rendering/present comes after scene-load, not yet reached). ★KEY REFRAME: the earlier no-heartbeat early-stalls (loader-init/pre-device) were my BROKEN 4.96MB dxmt-aarch64 d3d11 (exit3), NOT a timing race — so with complete hk-present d3d11 + fix, no-heartbeat should run FASTER (no heartbeat overhead slowing the 317s Mono). NEXT (in flight): dxmtframe run — NO heartbeat, TMO=900 (Mono ~150-250s without heartbeat overhead + post-Physics input-init/scene/present) → expect to reach first present (CAMetalLayer/presentDrawable). If it stalls AFTER Physics = a new (later) wall to chase; if it presents = FIRST FRAME. COMMITS(8).
2026-06-18 · NEXT WALL after the DXMT fix = win32u/x18 callback-storm (heartbeat-masked, pre-existing). dxmtframe (NO heartbeat, TMO=900): boot reached 17 CreateBuffers → 'Loaded All Assemblies' (Mono done) → PhysX init → `GpuFence::Create E_FAIL 0x80004005` → then a RECURRING `macrunner-hb-callback-exception` storm in **win32u.dll rva 0x2dc84** (frame_pc=0x87ffd49dc84) for the remaining ~600s, NEVER reaching input-init. = the KNOWN x18-fault-storm / win32u callback livelock (deep Lane A x18/host-TEB, pre-existing — NOT caused by the DXMT fix; the modmap run WITH heartbeat got PAST this to input-init). ⇒ WITHOUT heartbeat the boot livelocks at win32u/x18 BEFORE input-init; WITH heartbeat (thread-slowing) it survives to input-init (where the now-FIXED DXMT GPU-deadlock used to be). So the DXMT fix is GOOD (consistently reaches PhysX, far past the old wall) but the path to first-frame still crosses the win32u/x18 timing-livelock. BEST-SHOT run (dxmtframe2, HEARTBEAT + DXMT-fix + TMO=900) in flight → heartbeat masks win32u/x18+early-races, DXMT-fix clears input-init GPU-deadlock, 900s covers Mono(317s)+scene+present → expect first present (CAMetalLayer/presentDrawable) OR a stall that names the next wall. NB also exercising the harness hardening (auto NO_AUTOTRIAGE on heartbeat). Eventual heartbeat-free first-frame needs the x18-storm fix (deep, deferred). COMMITS(8).
2026-06-18 · ★MILESTONE + DEEP WALL. dxmtframe2 (HEARTBEAT + DXMT-fix + TMO=900, 915k lines/243MB; harness auto-NO_AUTOTRIAGE WORKED — no classify_run choke): boot reached Mono-done + PhysX, then STALLED at the SAME point — `GpuFence::Create E_FAIL 0x80004005` (L681205) → busy-spin livelock, NEVER reached input-init. Heartbeat did NOT reliably pass it this run (it's a race). Spin signature (heartbeat block counts): Mono 0x87ef132xxxx **204M blocks**, UnityPlayer 0x87efc7c4144 **765M steps**, d3d11 rva 0x5e1f9 227K (= my CpuFence::wait yield-poll idling — minor) + 44 callback-exceptions in **ntdll 0x74a94/0x74c74/0x75174 + win32u 0x2c944/0x2dc84** = the KNOWN **x18-fault-storm + Mono-spin livelock** (deep Lane A x18/host-TEB, pre-existing — the wall the EARLIER session was stuck on). ⇒ ★The DXMT lost-wake fix ACHIEVED its target (boot now consistently reaches PhysX, far past the old GPU-pipeline deadlock) but the first frame is blocked by the deeper x18-storm/Mono-spin (reached pre-input-init). NB commit-current-chunk=0 (no GPU chunk submitted — rendering is post-scene, never reached). NB my yield()-poll leaves idle DXMT encode/finish threads busy-spinning (CPU burn) — refine to a non-chrono low-CPU delay (Win32 Sleep(1)? util_win32_compat lacks Sleep — check windows.h) before commit; also clean the diag fprintf. GpuFence::Create E_FAIL (DXMT no ID3D11Fence) sits RIGHT at the stall onset in every run — candidate: implement ID3D11Fence in DXMT may avoid the Unity fallback path that triggers the storm (re-examine; earlier 'refuted' was a different boot point). ★FORK for operator: (a) implement ID3D11Fence in DXMT (tractable, may sidestep the storm); (b) tackle the deep x18-fault-storm (the earlier wall); (c) refine yield-poll + commit the DXMT fix as a standalone win. COMMITS(8) — DXMT fix UNCOMMITTED (diag still in).
2026-06-18 · ★COMMITTED (operator: lock in DXMT fix, then ID3D11Fence). Cleaned ALL dxmt-diag fprintf + refined yield()→Sleep(1) (low-CPU, non-chrono; util_cpu_fence.hpp forward-declares Win32 Sleep). Sanity run (dxmtsanity): 0 diag, 0 c000007b/x87, 0 abort(3), boot reaches 17 CreateBuffers→Mono-done→PhysX = no regression. COMMITS: **engine/dxmt SUB-REPO (separate git repo, NOT a main submodule) 84be732** = lost-wake fix (dxmt_command_queue.cpp + util_cpu_fence.hpp, self-healing Sleep(1) poll at CpuFence::wait/EncodingThread/WaitForFinishThread/chunk_ongoing); **main repo 1c3de1e** = harness hardening (laneA-run-hk.sh: 60s wait cap + scoped force-clean orphans; auto NO_AUTOTRIAGE on heartbeat). LEFT UNCOMMITTED (working-tree only, entangled w/ pre-existing debug): d3d11.cpp + d3d11_device.cpp (__wine_dbg_output→fprintf build-fix needed for aarch64 link w/ the pre-existing createbuffer/D3D11CreateDevice MR_DXMT_OUT tracing) — note: aarch64 dxmt d3d11 build REQUIRES this fix while that tracing is present. dxmt build = engine/graphics/build/dxmt-{aarch64,x86_64}-hk-present (33MB w/ shaders; plain dxmt-aarch64 is INCOMPLETE/4.96MB→exit3). NEXT (operator GO): implement ID3D11Fence in DXMT (GpuFence::Create E_FAIL 0x80004005 at the stall onset; may sidestep the Unity fallback that triggers the x18/Mono livelock). COMMITS(main=9 incl 1c3de1e; dxmt-subrepo=84be732).
2026-06-18 · ★★MILESTONE: boot reaches INPUT-INIT (furthest ever). ID3D11Fence degrade fix (d3d11_fence.cpp): CreateFence(SHARED) needs WDDM/D3DKMT (unavailable under Wine) → was E_FAIL; now DEGRADES to a local MTLSharedEvent-backed fence (local_kmt=0) + returns S_OK. COMMITTED **dxmt-subrepo af237cc**. Fence run (heartbeat TMO=900): boot ADVANCED past the post-PhysX x18/Mono stall → **`<RI> Input initialized.` + `Using XInput`** (prior runs stalled at PhysX!). Fix confirmed in deployed dlls (degrade-string present). 1 residual `GpuFence::Create E_FAIL` survives (before input-init; Unity log artifact, boot proceeds past it). ★NEW WALL = SEH/unwind: rc=5 (app EXITED, not timeout) right after input-init — `macrunner-hb-arm64-unwind-unsafe-boundary pc=0x87ef0d9f0bc image=0x87ef0d10000` (x64 module, machine=8664, rva 0x8f0bc) → the ARM64 unwinder hit an unsafe boundary unwinding an exception → exit(5). = HyperBridge SEH/x64→ARM64 unwind issue (the c0000026/SEH-host-boundary family, Lane A engine — different area from DXMT). LAYER PROGRESSION this session: CreateBuffer-deadlock(lost-wake) → PhysX-stall(x18/Mono) → input-init(GpuFence) → SEH-unwind-exit5. ★CAVEAT: x18-storm is a RACE; input-init reach may be partly variance — re-run to confirm reproducibility. NEXT FORK: (a) SEH/unwind exit5 (identify module 0x87ef0d10000+rva 0x8f0bc, the unsafe-boundary unwind); (b) confirm fence-fix reproducibility; (c) the x18-storm (still the slow underlying drag). COMMITS: dxmt-subrepo 84be732+af237cc, main 1c3de1e.
2026-06-18 · ★VARIANCE IS THE META-OBSTACLE (operator GO: map+attack SEH-unwind). SEH-unwind mechanism understood: signal_arm64.c macrunner_hb_try_arm64_unwind_methods(1016)→macrunner_hb_pc_unsafe_for_arm64_unwind(803): a PC is 'unsafe' (→stop_unwind_at_boundary(243), which nulls dispatch + makes NO pc/sp progress → unwinder terminates → unhandled exception → exit) when code_range_kind==0 (x64-CHPE code in an ARM64X module) OR pc<HOST_BOUNDARY_MAX OR sp out-of-bounds. So the exit5 = an exception unwinds into an x64-CHPE frame in an ARM64X module, ARM64 unwinder can't proceed → exception dies → exit(5). MAP RUN (heartbeat+HEARTBEAT_MODULE, TMO=900) FAILED to map it: the _MODULE overhead SLOWED the boot → STALLED at PhysX (timeout), NEVER reached input-init/exit5 (unlike the fence run which had heartbeat-only + reached input-init+exit5). This run's only unsafe-boundary = the BENIGN teardown one (pc=0x87fff36e600 image=0x87fff330000 rva=0x3e600, appears at EVERY run's exit). Dominant spin = FMOD sw-mixer (UnityPlayer rva 0x17b2xxx, 207M blocks, red herring). ⇒ CATCH-22: mapping the exit5 module needs HEARTBEAT_MODULE, but its overhead prevents reaching the exit5. ROOT = x18-fault-storm: makes the boot RACY (PhysX-stall vs input-init+exit5 vary run-to-run) + SLOW (Mono 317s) + likely causes the unwind exceptions. ★To map exit5 without _MODULE overhead: add a TARGETED module-name lookup to the unsafe-boundary MESSAGE (signal_arm64.c:1029) + run heartbeat-only (lighter, reaches input-init ~50%). ★STRATEGIC: 3 fixes committed + boot to input-init = major win; remaining (SEH-unwind + x18-storm) are deep + gated by x18-storm variance. x18-storm is the deep root (known hard Lane-A x18/host-TEB). COMMITS: dxmt-subrepo 84be732+af237cc, main 1c3de1e.
2026-06-18 · ★SEH-unwind exit5 MAPPED → DXGI.DLL x64-CHPE + it's an x18-storm symptom. Added module-name to the unsafe-boundary log (signal_arm64.c:1029, LdrFindEntryForAddress→BaseDllName+rva); rebuilt+deployed PE ntdll.dll (aarch64-windows, 7.86MB). sehmap run (heartbeat-only, caught the exit5, rc=5, reached input-init): unsafe-boundary pc=0x87ef0d9f0bc image=0x87ef0d10000 kind=0 — module= came back EMPTY (LdrFindEntryForAddress fails for the x64-CHPE view), BUT iatentry names base 0x87ef0d10000 = **DXGI.DLL** (imach=aa64 tmach=8664). So exit5 = an exception in **DXGI.DLL x64-CHPE code rva 0x8f0bc** → ARM64 unwinder can't unwind the x64-CHPE frame (code_range_kind==0) → stop_unwind → unhandled → exit(5). The exit5 sequence shows x18-fault SIGNAL-CALLBACKS in **d3d11.dll** (x64-signal-callback heartbeats rva 0x41240/0x8c20/0xa7583) + FMOD/Mono spins → the dxgi fault escalates (instead of healing) → unwind-fail → exit. ⇒ The SEH-unwind exit5 is an **x18-fault-storm fault in the graphics x64-CHPE code that escalates instead of healing**. ★ROOT CONFIRMED: the x18-fault-storm is the COMMON root of ALL post-PhysX walls — slowness (Mono 317s), run-to-run variance (PhysX-stall vs input-init+exit5), AND this SEH-unwind exit5. (disasm of standalone x86_64/aarch64 dxgi @rva 0x8f0bc = libc++ locale, doesn't match the ARM64X x64-CHPE merged layout — can't disasm the exact faulting insn from standalone builds.) ★FORK: (a) x18-fault-storm ROOT (heal x18 in graphics x64-CHPE so faults don't escalate; fixes slowness+variance+exit5 — the real fix, deep Lane-A x18/host-TEB); (b) SEH-unwind band-aid (make the ARM64 unwinder handle/cross the dxgi x64-CHPE frame so the one escalated fault is handled not exit5 — but x18-storm slowness continues); (c) checkpoint (3 commits + input-init = major win). COMMITS: dxmt-subrepo 84be732+af237cc, main 1c3de1e; PE ntdll.dll has uncommitted unsafe-boundary module-name diag.
2026-06-18 · ★WHY DXGI ESCALATES vs D3D11 HEALS — diagnosed (operator GO). segv fault router `macrunner_hb_pc_is_x64_guest_code` (macrunner_hb.c:5821) recognizes x64-guest code via: (1) is_current_x64_guest_exec_address (dynamic-exec/JIT region), (2) is_registered_x64_guest_address + **module_machine==AMD64** + pc_in_executable_section, (3) PEB main-image AMD64 fallback. ★dxgi is an ARM64X module (machine=ARM64, NOT AMD64) → its x64-CHPE code at rva 0x8f0bc FAILS check #2 (requires AMD64 machine). d3d11's faults route correctly via #1 (their x64-CHPE is JIT-registered in the dynamic-exec region → x64-signal-callback → heal). dxgi's fault at 0x8f0bc isn't in the registered region + fails #2 → NOT recognized as x64-guest → falls through to ARM64 RtlDispatchException → code_range_kind==0 unsafe-boundary (signal_arm64.c:803) → stop_unwind → unhandled → exit5. ⇒ ASYMMETRY = the fault router doesn't recognize x64-CHPE code INSIDE ARM64X modules (only pure-AMD64 modules + the JIT region). ★PROPOSED FIX: make pc_is_x64_guest_code ALSO return TRUE for an ARM64X module's x64-CHPE range (arm64x_code_range_kind(module,pc)==0) → route the dxgi x64-CHPE fault to the x64 heal path like d3d11. CAVEATS: (1) needs a unix-side code_range_kind helper (the existing one is PE-side signal_arm64.c; unix macrunner_hb.c has macrunner_ec_module_metadata? verify) — cross-side; (2) UNVERIFIED — should probe pc_is_x64_guest_code's result for the dxgi fault before/after; (3) verification gated by x18-storm variance (exit5 ~50%). Deep cross-arch-SEH engine change. COMMITS: dxmt-subrepo 84be732+af237cc, main 1c3de1e.
2026-06-18 · ★SEH-UNWIND FIX IMPLEMENTED (operator GO: fix the dxgi-escalates). macrunner_hb.c (unix): added macrunner_hb_pc_in_arm64x_x64_range(module,pc) — walks the ARM64EC CodeMap (via macrunner_hb_get_arm64x_metadata + macrunner_hb_image_nt_header; NB use _image_nt_header NOT RtlImageNtHeader which is PE-only/undefined unix-side) → TRUE if pc is in a non-native (x64/CHPE) range. Added a clause to macrunner_hb_pc_is_x64_guest_code: if module_from_pc(pc) has an x64-CHPE range at pc + executable section → return TRUE. So a fault in dxgi/d3d11 x64-CHPE code (ARM64X, module_machine!=AMD64) now routes to the x64 heal/dispatch path instead of the ARM64 unwinder → should eliminate the post-input-init exit(5). Built ntdll.so clean, deployed+codesigned both aarch64-unix+windows. VERIFY RUN (sehfix, heartbeat-only TMO=900) in flight → expect: NO exit5 (rc!=5) IF boot reaches input-init → boot continues past it (toward scene/present, OR full-900s slow, OR next wall). Gated by x18-storm variance (~50% reaches input-init). PE ntdll.dll still has the (uncommitted) unsafe-boundary module-name diag — if the fix works, the dxgi unsafe-boundary should NOT fire. COMMITS: dxmt-subrepo 84be732+af237cc, main 1c3de1e (ntdll.so x64-CHPE-route fix UNCOMMITTED pending verify).
2026-06-18 · SEH-fix v1 MISSED THE VARIANT → v2. sehfix run: STILL exit5, SAME dxgi unsafe-boundary (pc=0x87ef0d9f0bc kind=0). ROOT of the miss: the segv fault-router `macrunner_hb_route_x64_callback_fault` (signal_arm64.c:1062) calls the **_no_lock** variants (pc_is_x64_guest_code_no_lock:5923), NOT the locking one I patched. AND _no_lock has a gate: `if (!is_registered_x64_guest_address(pc)) return FALSE` BEFORE the module check — dxgi's STATIC x64-CHPE range isn't "registered" (d3d11's faulting code IS, via the JIT dynamic-exec region → that's why d3d11 heals + dxgi doesn't!). ★FIX v2 (UNCOMMITTED): patched BOTH _no_lock (5923, additive: after the registered-gate, check pe_module_from_pc_no_lock + pc_in_arm64x_x64_range + exec-section → signal-safe, reads committed PE metadata, no locks) AND _module_no_lock (5888, accept AMD64-machine OR arm64x-x64-range). Built ntdll.so clean, deployed+signed both. sehfix2 run in flight → if route_x64_callback_fault now recognizes the dxgi x64-CHPE fault → routes to x64 heal (like d3d11) → NO exit5 → boot continues. IF still exit5: the fault is NOT a route_x64_callback_fault SIGSEGV (it's a genuine dispatched exception) → needs an unwinder-level fix (let ARM64 unwinder cross the x64-CHPE frame) → REPORT to operator, don't attempt v3 blindly. COMMITS: dxmt-subrepo 84be732+af237cc, main 1c3de1e.
2026-06-18 · ★SEH-fix v2 REGRESSED early boot → REVERTED. sehfix2: ALL_TRIES_FLAKED (both tries, ~21k lines, never reached Mono). NOT the normal boot-flake (no c0000135/exit53) — a RUNAWAY `x64-signal-callback` loop at a FIXED **ntdll** pc 0x87fff9521a8 (rva 0x821a8) with rsp CLIMBING monotonically = infinite recursion. ROOT: my arm64x-x64-range match was TOO BROAD — **ntdll itself is ARM64X and has legitimate x64-EC entry thunks**; pc_is_x64_guest_code_no_lock now returned TRUE for ntdll's OWN x64-CHPE code → ntdll host faults/callbacks routed into the x64-guest emulation path → recursion → early-boot death. ⇒ The fix MUST exclude system/Wine-builtin ARM64X modules (ntdll/kernelbase/etc. — LDR_WINE_INTERNAL or by the host-image set); only GENUINE guest graphics modules (dxgi/d3d11/DXMT, ARM64X-but-app-side) should get x64-CHPE-fault routing. REVERTED macrunner_hb.c + signal_arm64.c to HEAD (git checkout), rebuilt clean ntdll.so (1520376 bytes = verified-good size), redeployed+signed both. ★DIST NOW = VERIFIED-GOOD: 3 committed fixes (dxmt 84be732 lost-wake + af237cc fence, main 1c3de1e harness) reaching input-init; ntdll.so = HEAD (no my SEH changes). ★REPORTED TO OPERATOR — SEH-unwind exit5 fix needs a deliberate scoping decision (which modules count as "guest x64-CHPE"), NOT more autonomous iteration; the dxgi-escalates DIAGNOSIS is solid (routing gap: x64-CHPE-in-ARM64X not recognized by the fault router + the registered-address gate), the IMPLEMENTATION needs the host-module exclusion. COMMITS: dxmt-subrepo 84be732+af237cc, main 1c3de1e.
2026-06-18 · ★SEH-fix v3 — NAME-ALLOWLIST scope (operator: продолжай). Re-added (macrunner_hb.c, UNCOMMITTED): (1) macrunner_hb_pc_in_arm64x_x64_range(module,pc) — CodeMap walk, signal-safe; (2) macrunner_hb_pc_in_graphics_arm64x_x64_range(pc) — ldr_entry_from_pc (matches by FULL image range, finds dxgi for the x64-CHPE pc) → BaseDllName → **allowlist {dxgi,d3d11,d3d10core,d3d9}.dll** → arm64x-x64-range + exec-section. NB LDR_WINE_INTERNAL is NOT a usable discriminator (DXMT d3d11/dxgi are ALSO builtin-marked, like ntdll) → name allowlist is the safe scope: STRUCTURALLY cannot match ntdll/kernelbase (the v2 recursion). Added the graphics-clause to all 3 variants (pc_is_x64_guest_code + _module_no_lock + _no_lock; the _no_lock one is what route_x64_callback_fault actually calls). Built clean (1520488 bytes), deployed+signed. sehfix3 run (heartbeat, TMO=900, MAX=3 tries) in flight → expect: NO ntdll recursion (allowlist), AND if dxgi fault now routes to x64-heal → NO exit5 → boot past input-init. IF exit5 persists w/o recursion: fault isn't reaching route_x64_callback_fault (genuine dispatched exception) → needs unwinder-level fix → report. IF ntdll recursion returns: allowlist insufficient → revert+report. COMMITS: dxmt-subrepo 84be732+af237cc, main 1c3de1e.
2026-06-18 · ★★SEH-UNWIND FIX v3 VERIFIED + COMMITTED (main 8fb2ab0). sehfix3 (heartbeat TMO=900): **exit5=0, ntdll-recursion(rva0x821a8)=0, unsafe-boundary=0** — ALL clean. Boot reached input-init (L349340) + SURVIVED past it (ran full 900s timeout, no death). The name-allowlist scope worked: dxgi x64-CHPE fault now routes to the x64 heal path (like d3d11), ntdll/kernelbase structurally excluded. Committed macrunner_hb.c → main 8fb2ab0 (60 insertions: macrunner_hb_pc_in_arm64x_x64_range CodeMap-walk + macrunner_hb_pc_in_graphics_arm64x_x64_range allowlist gate + clauses in all 3 pc_is_x64_guest_code variants). ★SESSION TOTAL = 4 COMMITTED FIXES: dxmt-subrepo 84be732 (lost-wake), af237cc (ID3D11Fence degrade); main 1c3de1e (harness), 8fb2ab0 (x64-CHPE SEH-route). Boot frontier: CreateBuffer-deadlock → PhysX → input-init → (exit5 KILLED) → now SURVIVES past input-init, grinding under the x18-storm (no present in 900s — present is post-scene, gated by the x18-storm SLOWNESS which is the last deep root). ★REMAINING = x18-fault-storm (the slow drag: Mono 317s + the post-input-init grind never reaches scene/present in the timeout). To reach FIRST FRAME: either (a) speed up past the x18-storm (deep x18/host-TEB heal-cost reduction) OR (b) a much longer TMO to see if it eventually presents OR (c) confirm where post-input-init it now stalls/grinds (new probe). COMMITS: dxmt 84be732+af237cc, main 1c3de1e+8fb2ab0.
2026-06-18 · ★(c) POST-INPUT-INIT PROBE → REDIRECTS (a). Wrote /tmp/hk_postii_probe.py (launch HK heartbeat → forward delta-scan for 'Input initialized' [tail-scan misses it, buried under heartbeat spam] → settle 40s → sample busiest HK proc top-of-stack ×4). VERDICT — the post-input-init grind is NOT x18-heal and NOT a code wall: (1) **x18-fault storm GONE post-ii** — callback-exception(x18 sig) after-input-init=**0** (vs 34 before-ii, vs HUNDREDS pre-SEH-fix) ⇒ the SEH-unwind + earlier fixes effectively KILLED the x18-storm at this stage; (2) NOT a fixed near-wall rva; (3) it's the **FMOD software-mixer BUSY-LOOP** — UnityPlayer rva **0x17b2bb0-0x17b2c7c** (9 PCs cycling) = **88.8%** of post-ii UnityPlayer guest execution (top-10 share 89%), the KNOWN FMOD red herring ("FMOD failed to init audio devices → emulated software output" → spins forever mixing silence, eats the ~135% CPU). Process-wide: ~99% thread-states PARKED (ulock_wait 57% + wineserver-read 22% + mach_msg/select/workq); the 1 "busy" thread is 92% mach_msg + 8% HB mem-mgmt (tree_insert/map_live_address_space/malloc). Real boot progress = the THIN ~11% non-FMOD UnityPlayer exec (~2000 hits/250 rvas) — slow. ★⇒ (a) x18-Part-2 is OFF-TARGET (x18-storm already gone). The FMOD sw-mixer spin is a CPU-draining red herring that may STARVE the main thread's JIT throughput. NEW LEVERS for first-frame: (i) NEUTRALIZE the FMOD sw-mixer spin (free the core → main thread progresses faster; operator did similar 'option 2' neutralizations for WGI); (ii) much-longer run to see if the thin real-progress reaches scene-load/present; (iii) MAIN-thread-only probe (exclude the FMOD spinner) to see what the boot driver actually awaits. REPORTED — (c) refutes (a); awaiting operator's pick of lever. COMMITS: dxmt 84be732+af237cc, main 1c3de1e+8fb2ab0.
2026-06-18 · ★★(iii) MAIN-THREAD PROBE → REDIRECTS BOTH (a) AND (i). Re-analyzed the 4 captured samples (no new run). EXCLUDING the FMOD spinner: EVERY other thread is PARKED (Job.Worker×23+UnityGfxDeviceWorker on __ulock_wait2; AssetGC×7+Finalizer on wineserver read; dxmt-encode/finish×2 on __select; NSEvent/winemetal-SharedEventListener on mach_msg). The Unity MAIN/driver thread (Thread_2253162, the only guest-active one) is NOT parked and NOT FMOD-starved — its '92% mach_msg' DECODES to: macrunner_hb_run_x64 → **macrunner_hb_map_live_address_space (93%)** → **mach_vm_region** → mach_msg2_trap. ★ROOT: macrunner_hb_map_live_address_space (macrunner_hb.c:3958) ENUMERATES THE ENTIRE USER VM MAP (0→0x7fffffff0000, one mach_vm_region syscall PER REGION; Unity+Mono+DXMT+heaps = thousands of regions). Called from the run_x64 hot loop at **:19344**: when the guest PC enters a region HB hasn't mapped (mark_x64_image_exec_sections → NOT_FOUND/MEMORY_FAULT), it does a FULL-MAP RE-ENUMERATION + retry. During scene-load the main thread constantly enters new code regions (Mono JIT / Unity managed / DXMT cross-module) → CONSTANT full-VM rescans → 93% main-thread time in mach_vm_region. = an O(regions)-per-new-region-access perf bug (effectively O(N²) over scene-load). ⇒ NEITHER (a) x18 (storm gone) NOR (i) FMOD-throttle (main isn't FMOD-starved; it's map-rescan-bound — freeing a core won't unblock it). ★REAL FIX (high-leverage): make the remap INCREMENTAL — on a PC/addr fault, query+map ONLY the region containing ctx->pc (one mach_vm_region call with the faulting addr) instead of walking 0→max_addr. Likely a single-region helper + use it at :19344 (and the 2 other call sites 17417/19066 can stay full-scan = one-time init). Target: main-thread throughput ↑ → scene-load → presentDrawable. REPORTED — recommending the VM-map incremental fix OVER (i) FMOD-throttle. COMMITS: dxmt 84be732+af237cc, main 1c3de1e+8fb2ab0.
2026-06-18 · ★INCREMENTAL VM-MAP FIX IMPLEMENTED (operator GO). Snapshot first (.tmp/snap-vmmap-20260618/ntdll.so.verifiedgood + macrunner_hb.c.verifiedgood; HEAD=8fb2ab0 clean). CORRECTNESS CHECK done: (1) mark_x64_image_exec_sections (macrunner_hb.c:4009) hb_memory_protect's EVERY exec section of the module → needs the whole MODULE mapped (not just ctx->pc's 1 region) → scope = module range, not single-region; (2) init sites 17455/19104 install special_read/write/grow handlers → DATA accesses to unmapped regions served on-demand by special handlers, NOT by map_live → narrowing the per-fault scan does NOT lose data coverage; (3) replay_virtual_regions (4992) replays HB's OWN recorded virtual/JIT regions → scan-scope-independent. So no downstream dep on the full refresh at the per-fault site → no periodic full-scan needed. IMPL: refactored macrunner_hb_map_live_address_space → _range(mem,lo,hi) helper + thin full wrapper (0..max) + new macrunner_hb_map_live_module_range(mem,module) [base..base+SizeOfImage]. Swapped ONLY :19389 (was :19344, the 93%-sink per-fault path) to module_range; init sites :17455/:19104 keep full scan. Built ntdll.so clean (1520616), deployed+signed both. VERIFY: post-input-init probe re-run in flight → expect (gate1) reaches input-init [LADDER≥input-init], (gate2-perf) main-thread NO LONGER 93% in map_live_address_space/mach_vm_region. If good → 8-boot reliability gate + long run for scene-load/present. COMMITS: dxmt 84be732+af237cc, main 1c3de1e+8fb2ab0 (VM-map fix UNCOMMITTED pending verify).
2026-06-18 · ★VERIFY → WRONG SITE FIXED; real sink = 19104 (per-callback), NOT 19344. Post-fix probe: gate1 ✓ reached input-init (el=496s). BUT gate2 FAILED — the 93% sink PERSISTS. Per-thread parse of the busy guest thread (Thread_2294129) shows the dominant stack is `dispatch_x64_callback+664 → macrunner_hb_run_x64+2672 (macrunner_hb.c:**19104**) → macrunner_hb_map_live_address_space_range(FULL 0..max, +132 @3992) → mach_vm_region → mach_msg2_trap` = 2897/3117 (93%). ★ROOT (corrected): line 19104 is in run_x64 right AFTER hb_context_create(19070)+hb_memory_create(19077) — so EVERY x64-callback dispatch (dispatch_x64_callback→run_x64) creates a FRESH ctx+memory and FULL-scans the entire VM map. It's PER-CALLBACK, not one-time init. During scene-load Unity makes constant x64 callbacks → constant full scans = the 93% sink. My fix targeted 19344/19389 (per-fault remap) which is NOT the dominant path; 19389 fix is correct-but-not-hot (KEEP, harmless). The operator's (and my) assumption that 19066/19104 = init was WRONG — it's the hot per-callback path. ★PROPOSED: apply the same incremental map at 19104 — map only the ENTRY module's range (entry/image_base available at 19104) instead of full scan; data via special_read/write handlers (set at 19100-19102), other code via the per-fault remap. RISK: 19104 is per-callback ctx-init the operator explicitly said keep-full; entry-module-only is a behavioral change (deeper fix = CACHE hb_memory per-thread vs fresh-per-callback). REPORTED — confirming before overriding the explicit keep-19104-full instruction. VM-map fix (19389 + refactor) UNCOMMITTED; not committing (didn't move the needle yet). COMMITS: dxmt 84be732+af237cc, main 1c3de1e+8fb2ab0.
2026-06-18 · (A)+safety IMPLEMENTED (operator GO). Snapshot 2 (.tmp/snap-vmmap-20260618/ntdll.so.pre19104). At 19104 (the real per-callback sink): `ret = image_base ? map_live_module_range(mem,image_base) : map_live_address_space(mem)` + full-scan fallback if module-range fails (keeps the 19114 !HB_OK→STATUS_NO_MEMORY semantics). CORRECTNESS: image_base always non-zero (dispatch_x64_callback passes target_module ?: PEB ImageBase, :18961) + always a valid NT-header module → module-range maps it; mark_x64_image_exec_sections(image_base) @19128 needs exactly image_base's module (covered); replay_virtual_regions scan-independent. LAZY-FILL NET: code beyond entry-module → run-loop :19322 → per-fault remap :19389 (map_live_module_range, incremental); data → special_read/write handlers (19100-19102); dynamic/JIT → 19327. Committing 19389 WITH this (it IS the net). Built+deployed. 3-GATE VERIFY in flight: (1) LADDER≥input-init, (2) 8-boot consistency, (3) ★watch NEW exception class — if a callback enters an uncovered region the lazy path mishandles (e.g. dxgi x64-CHPE mid-callback: not entry-image, not dynamic, not AMD64-module at :19337 → could fall through) it surfaces as a NEW exception (not just slowness). On green → snapshot+commit verified-forward. ★(B) per-thread hb_memory CACHE = deferred clean-followup (needs staleness-invalidation: VM changes between callbacks) → recording in NEEDS. COMMITS: dxmt 84be732+af237cc, main 1c3de1e+8fb2ab0.
2026-06-18 · ★★(A) VERIFIED GREEN — VM-map sink ELIMINATED. Probe (laneA-postii-try1-210755): (gate1) reached input-init el=488s ✓; (gate2) busy-thread map_live/mach_vm_region = **93%→0.0%** ✓✓; (gate3) NO new exception class — 1 post-ii exception = c0000017/reason=stack-setup/pc=0x0 = pre-existing benign null-target callback (NOT live-map failure, NOT a guest-code fault → not from the lazy-fill) ✓; (★PERF) post-ii guest block-transitions in the comparable ~60s window **29,287 → 68,516 (~2.3× throughput)** ✓. Busy thread now 74% hb_jit_runtime_destroy + 15% mach_vm_deallocate = per-callback context TEARDOWN (the fresh-ctx-per-callback root = deferred (B); my fix removed the build-side full-scan, teardown is the next layer). NET: real verified-forward (sink gone, throughput 2.3×, no regression). COMMITTING macrunner_hb.c (refactor _range + map_live_module_range + :19389 per-fault + :19104 per-callback entry-module map). 8-boot consistency batch launching as final confirmatory gate. COMMITS: dxmt 84be732+af237cc, main 1c3de1e+8fb2ab0 + (pending) VM-map.
2026-06-19 · ★★VM-MAP FIX COMMITTED + ALL GATES GREEN. main **4c4f1c4** (incremental VM map). 8-BOOT CONSISTENCY GATE: **8/8 reached input-init** (all try1, no flake, ~435K lines, ~626s each, near-identical) → PASS. New-exc per boot = only `GpuFence=1` (the known-benign ID3D11Fence-degrade WARN, boot proceeds) — ZERO c0000005/c000007b/unwind-unsafe/runtime-fail → no new fault class from the lazy-fill ✓. Combined with the probe gates (sink 93%→0%, ~2.3x throughput, gate3 clean) the VM-map fix is FULLY VERIFIED-FORWARD. ★SESSION TOTAL = 5 COMMITTED FIXES: dxmt 84be732 (lost-wake), af237cc (ID3D11Fence degrade); main 1c3de1e (harness), 8fb2ab0 (x64-CHPE SEH-route), 4c4f1c4 (incremental VM-map). Boot frontier: CreateBuffer-deadlock→PhysX→input-init→(exit5 killed)→survives+input-init reliably (8/8), main-thread throughput 2.3x. ★REMAINING to presentDrawable: (1) per-callback ctx TEARDOWN hb_jit_runtime_destroy ~74% (deferred (B) per-thread hb_memory cache, LANE-A-NEEDS — needs staleness-invalidation); (2) does the 2.3x throughput now reach scene-load/present in a LONG run (TMO 1800+)? — UNKNOWN, all runs so far killed ~600-900s before present. FMOD sw-mixer spin (rva 0x17b2bb0-c7c) = separate CPU drain red herring. NEXT FORK: (a) ONE long run (TMO 1800+) to see if it now presents / where it next stalls; (b) implement (B) per-thread cache to further cut per-callback overhead; (c) checkpoint. COMMITS: dxmt 84be732+af237cc, main 1c3de1e+8fb2ab0+4c4f1c4.
2026-06-19 · (a) LONG RUN toward presentDrawable (operator GO). Snapshot .tmp/snap-vmmap-20260618/ntdll.so.verified5commits. /tmp/hk_longrun_monitor.py: lane TMO=2100, MACRUNNER_HB_TRACE_HEARTBEAT=1 + WINEMETAL_TRACE_HWND=1, NO DXMT_HEADLESS/ORPHAN (real window allowed) + parallel scripts/window-capture-verdict.sh --watch --name 'Hollow Knight' (CGWindowList poll→screencapture). Watches run.log for present markers (presentDrawable|CAMetalLayer|nextDrawable|dxmt[present]|macdrv_view_create_metal_view|winemetal-present), samples busiest-HK top-of-stack every 120s after input-init for stall diagnosis. OUTCOMES: present/non_background_pixels>0 → ★WINDOW rung13 (commit+CG-capture, celebrate); stall → top-of-stack: hb_jit_runtime_destroy-dominant → confirms (B) per-thread cache as next lever; new rva pin (e.g. win32u 0xd448 seen in heartbeat) → near wall, fix it. Also watch dxmt[present] nil-drawable + macdrv_view_create_metal_view (success vs orphan) = did present-path reach the real layer. In flight (~35min). COMMITS: dxmt 84be732+af237cc, main 1c3de1e+8fb2ab0+4c4f1c4.
2026-06-19 · ★(a) LONG-RUN VERDICT: STALL before scene-load — a WAIT-wall, NOT (B) teardown, NOT throughput. Ran full TMO=2100 (input-init @t=480s → 1665s post-ii grind → lane exit rc=0). NO present: presentDrawable/CAMetalLayer/nextDrawable/dxmt[present]/macdrv_view/winemetal-present ALL **0** (present path NEVER engaged; only WINEMETAL_TRACE_HWND hwnd= ×2). Boot ladder STOPS at input-init+Using XInput — **NO PlayerLoop / Loading scene / scene markers** in 1665s. Process-wide top-of-stack STATIONARY for 25min: __ulock_wait2 57% (job workers parked) | read 22% (wineserver) | mach_msg 8% | __select 6% | workq 4% | **hb_jit_runtime_destroy only 1%**. Post-ii UnityPlayer guest exec = **89% FMOD sw-mixer** (rva 0x17b2bxxx/cxxx) + ~11% scattered (not advancing). ⇒ The boot reaches input-init, the main thread does a BURST of x64 callbacks (per-callback teardown) then PARKS waiting → STALL before PlayerLoop. = a WAIT-WALL (process 99% parked, job-workers idle) — echoes the ORIGINAL job-system worker-wake pattern. ★(B) per-thread cache is NOT the present-blocker (hb_jit_runtime_destroy 1% process-wide; it was 74% of the main thread only in the brief post-ii callback BURST, before it parks). ★REAL WALL = what the main/boot thread AWAITS between input-init and PlayerLoop (job-system producer? render/audio handshake? FMOD-starvation?). NEXT: PER-THREAD sample of the main/boot thread DURING the stall (process-wide is swamped by parked threads) to name the wait; OR neutralize FMOD (if it starves/blocks main). REPORTED — (a) shows a wait-wall before scene-load, redirects from (B). 5 commits stand (verified-forward each). COMMITS: dxmt 84be732+af237cc, main 1c3de1e+8fb2ab0+4c4f1c4.
2026-06-19 · (1) MAIN-THREAD WAIT PROBE — stderr traces inadequate, pivot to per-thread sample. Suspect docs read: GATE-DIAGNOSIS-20260603 (worker-pool semaphores 0x48-0x90 WORK; guest reached CreateEventW 0xb8(init0)/0xbc(init1) + DuplicateHandle 0xb4→0xc0 = render/GfxDevice handshake events, pc~0x6f0..44f0; later UPDATE superseded→TSO/Boehm memory-ordering — both PRE-device-creation, now passed) + CHATGPT-REVIEW-macrunner-wait-signal.c (our WaitOnAddress/WakeByAddress hashed-queue layer = lost-wake suspect). mainwait run (WAIT_SEMANTIC budget=120000 + WAITADDR + heartbeat): WAIT_SEMANTIC budget EXHAUSTED before input-init (ii at line 478972/637955 — Mono-load does >120k wait/wake events first) → ZERO post-ii wait-semantic. WAITADDR is rate-limited (first 64 + every 8192nd) AND threads park-once-and-stay → caught only tid 0110 poller (addr 0x3a0414c80, woken). WakeByAddress found>0=14 found=0(LOST)=0 (pre-ii) — no lost-wake evidence but no post-ii coverage. ⇒ stderr traces can't catch the main thread's SINGLE forever-park. PIVOT: live PER-THREAD `sample` DEEP in the stall (SETTLE=160s post-ii, past the callback burst) → the main/boot thread's parked stack shows the exact primitive (NtWaitForAlertByThreadId/WaitOnAddress/NtWaitForSingleObject/Multiple) + call chain + what it awaits. /tmp/hk_deepstall_probe.py in flight. Then cross-ref handle/addr vs 0xb8/0xbc render-handshake + check producer/wake. COMMITS: dxmt 84be732+af237cc, main 1c3de1e+8fb2ab0+4c4f1c4.
2026-06-19 · ★★(1) ROOT NAMED = (B) per-callback JIT-context lifecycle, NOT a wait. Deep-stall per-thread sample (SETTLE=160s post-ii, 54 threads): the main/boot thread = **Thread_2813221**, the ONLY non-infra non-FMOD thread, and it is NOT PARKED — it's busy in a per-callback dispatch loop: `dispatch_x64_callback → run_x64` → **hb_jit_runtime_destroy ~58%** (per-callback JIT teardown → mach_vm_deallocate) + `run_x64 → replay_virtual_regions → sync_virtual_region → getenv/__findenv_locked ~13%`. Every other candidate parked: winemetal SharedEventListener CFRunLoop×2 (mach_msg), NtWaitForAlertByThreadId waiters, wineserver read (NtWaitForSingleObject), workq idle. ⇒ main is NOT wait-blocked: NOT lost-wake (our wake layer healthy, found0=0), NOT an unsignaled event/handshake (0xb8/0xbc never reached — that was pre-device, now passed), NOT FMOD priority-inversion (main isn't waiting). It's CPU-BOUND on the per-callback context lifecycle. Cross-ref long-run: post-ii non-FMOD guest exec scattered across MANY distinct rvas (0x173xx/0x174xx, each <1%) = VARIED code = PROGRESSING through scene-load, but PATHOLOGICALLY SLOW (each managed-code transition = an x64 callback that rebuilds+tears-down a full JIT context). Scene-load never completes in 1665s. ★VERDICT: (B) IS THE FRAME-BLOCKER (not a post-frame nicety). VM-map fix 4c4f1c4 removed the build-side (map_live); teardown (hb_jit_runtime_destroy) + replay_virtual_regions/getenv remain per-callback. FIX = (B) per-thread context+JIT+memory CACHE (create once per thread, reuse across callbacks → eliminates build+teardown+replay per call). Needs staleness-invalidation (VM changes between callbacks). Cheaper interim candidates: pool/reuse the JIT runtime (skip destroy+recreate); memoize the getenv in sync_virtual_region (observer tax); make replay_virtual_regions incremental. NEXT (operator call): implement (B) (or interim teardown-pool) → main-thread per-callback cost ↓ → scene-load completes → presentDrawable. COMMITS: dxmt 84be732+af237cc, main 1c3de1e+8fb2ab0+4c4f1c4.
2026-06-19 · (B-interim) SMC DUE-DILIGENCE (operator gate) → literal reuse UNSAFE; SAFE realization = buffer-pool+regenerate. Snapshot .tmp/snap-vmmap-20260618/ntdll.so.pre-Binterim. ARCH: run_x64 per callback: hb_context_create(19070)+hb_memory_create(19077)+map_live(19104,fixed)+ir_cache=calloc(262144 entries, 19275, LOCAL)+hb_jit_runtime_create(19290) → run → hb_jit_runtime_destroy(20640)+ir_cache_destroy+ctx_destroy. hb_jit_runtime_create (hb_runtime.c:1015) = calloc(rt)+hb_jit_buffer_create→**mmap(MAP_JIT, RWX)** (hb_jit.c:79-82); _destroy = hb_jit_buffer_destroy→**munmap** + free. ⇒ the 58% = per-callback mmap/munmap of the MAP_JIT exec arena (+ pthread_jit_write_protect toggles). ★SMC CHECK: ir_cache + jit_mem are FRESH PER CALLBACK (no thread-local persistence) → NO cross-callback invalidation exists (none needed today). hb cache HAS invalidation machinery (hb_cache_invalidate/_module, notify_memory_protect, dirty_invalidations) but PRISM §5 marks SMC as [UNKNOWN]/CURRENT-GATE (not confirmed wired to guest code-mod). Mono SELF-MODIFIES (code-mempool address reuse) → literal translation-PERSISTENCE-reuse across callbacks = STALE-TRANSLATION c0000005. ★SAFE REALIZATION (sidesteps invalidation entirely): POOL the buffers (jit_mem MAP_JIT arena + ir_cache) per-thread, but RESET per callback (bump-ptr→0, ir_cache logical-clear) → translations REGENERATED each callback from current code (NO persistence → NO staleness → SMC-safe) while eliminating the mmap/munmap churn (the 58%). Re-translation stays (but it's NOT the bottleneck — lift/codegen absent from the 58% profile; the munmap IS). Needs: hyperbridge hb_jit.c/hb_runtime.c reset API (reuse arena) + macrunner_hb.c thread-local jit_rt/ir_cache reset-not-recreate. PLUS safe edits 2 (memoize getenv in sync_virtual_region) + 3 (incremental replay_virtual_regions). ★FORK: diverges from literal "reuse" + touches the LIB → reporting before the multi-file change. COMMITS: dxmt 84be732+af237cc, main 1c3de1e+8fb2ab0+4c4f1c4.
2026-06-19 · ★★OPERATOR REDIRECT → JIT FALLBACK-LOOP (the real HK blocker; graphics ready, D3D11CreateDevice passes, static-airconv debunked). B-interim built+deployed (unverified, harmless) but the BLOCKER is engine ISA. ROOT (confirmed ground-truth): codegen_instr (hb_arm64_codegen.c:3842) routes ~14 data ops to emit_interp_ir_helper (inline per-op interp call that executes+advances) BUT its **default: returned HB_ERR_UNSUPPORTED_OPCODE** (4513) → whole block FAILS → block-level interp fallback (macrunner_hb.c:20263) → when the unsupported op is FIRST in the block (steps=0) PC never advances → re-lift same pc → re-fail → ∞ LOOP (the HK boot livelock; observed marker `macrunner-hb-jit-fallback out=UNSUPPORTED_OPCODE steps=0 gbytes=41 d1 d8`=RCR). RCR/RCL: LIFTED (hb_lift_x64.c:1440→HB_IR_RCL/RCR) + INTERPRETED (hb_interpreter.c:4670) but NO codegen emitter → falls to default → loop. = pure CODEGEN gap (lift+interp OK), exactly the interp-helper class. Control-flow ops (CALL/RET/JMP/LOOP) are EXPLICITLY cased → default = data ops only → routing default→interp-helper is SAFE (no branch mis-exec). ★FIX (1, hb_arm64_codegen.c:4513): default → `return emit_interp_ir_helper(buf, instr)` (kept HB_IR_UNSUPPORTED/FAULT=genuine lift-gaps returning UNSUPPORTED). Any lifted+interp-supported op with no ARM64 emitter (RCR/RCL, SSE-compare family, ...) now interpreted INLINE + block advances → loop GONE, HK proceeds via interp path. (2) bulk codegen DROPPED per operator (speed followup, not correctness). Block codegen path confirmed = codegen_instr (called at :8372 from block_with_cfg + :8072 public wrapper). emit_interp_ir_helper = established (14 uses) → reg-sync handled. Rebuilt libhyperbridge.a + ntdll.so, deployed. GATE in flight: HK headless TMO=1800 → expect boot PAST the loop → PhysX → scene-load/PlayerLoop/present; watch jit-fallback marker (RCR should now interp not loop) + c0000005 stale-exec (gate-3) + 8-boot. Snapshot .pre-Binterim covers revert. COMMITS: dxmt 84be732+af237cc, main 1c3de1e+8fb2ab0+4c4f1c4 (+pending fallback-fix + B-interim).
2026-06-19 · ★★★FALLBACK FIX VERIFIED + COMMITTED dfc2148 — BOOT ADVANCES PAST input-init TO SCENE-LOAD. Gate run (headless, TMO=1800): **jit-fallback markers=0** (RCR loop GONE), **c0000005 stale-exec=0** (gate-3 clean, fix correct by construction), boot timeline D3D11→Mono→PhysX→input-init(L322245)→**UnloadTime 362ms (L594691, scene-load/asset-mgmt, POST-input-init)** — NEW marker, NEVER reached before (every prior run ceilinged at input-init). The fallback-loop WAS the wall; removing it let the boot proceed into scene loading. ★NEW WALL (post-scene-load, L597620): `macrunner-hb-run-exit status=c000007b reason=non-application-target pc=0x17fff43eb q0=0x0/MEMORY_FAULT` = a guest indirect call to a bad/unrecognized target (NOT stale-exec from my fix — c0000005=0; a SEPARATE next blocker). Boot continued (not fatal) but no present in TMO. ★COORDINATION DISCOVERY: git HEAD is on **CODEX's line (b92cbc6 "reset-pool"/DXMT/ID3D11Fence/Mono-reload commits)** — my session's 5 commits (84be732/af237cc/1c3de1e/8fb2ab0/4c4f1c4) are NOT in this HEAD. Codex landed the B-interim/reset-pool (tls_jit_rt ×5, hb_jit_runtime_reset, hb_jit_buffer_reset) + DXMT work IN PARALLEL; my working-tree macrunner_hb.c/hb_jit.c/hb_runtime.c == HEAD (converged). My UNIQUE validated contribution = the FALLBACK FIX (hb_arm64_codegen.c default→emit_interp_ir_helper + hb_codegen.h decl) → committed dfc2148 on top of Codex's HEAD (additive, non-conflicting). ⇒ Lanes converged; the reset-pool/DXMT fixes ARE in HEAD (via Codex), + my fallback fix. NEXT: (a) the c000007b non-application-target @0x17fff43eb (new wall, post-scene-load) — map module+rva, why the indirect target is bad; (b) 8-boot consistency on dfc2148; (c) does it reach present with more time. COMMITS(HEAD line): ...b92cbc6 + dfc2148 (fallback fix).
2026-06-20 · ★KIMI WINEMAC get_win_data WORK (operator handoff: Kimi's analysis correct, exec stuck → I build/run/diagnose). LOCKED dfc2148 (git tag hk-fallback-fix-dfc2148 + snapshot ntdll.so.fallbackfix-validated; dfc2148 IS in HEAD 23ea8ce ancestry; HEAD = Codex line, now docs 'CreateFence S_OK + macdrv get_win_data NULL blocker' milestone). ★Single-lane rule (operator: ONE engine terminal — Codex+I dup'd reset-pool, converged) ⇒ winemac-run and 8-boot can't parallel; winemac (visible-frame gate) prioritized, 8-boot queued after. BINDING CHAIN (mapped): DXMT swapchain → unix-call _CreateMetalViewFromHWND (winemetal_unix.c:1706) → pfn_get_win_data(hwnd) @1731 → my_get_win_data (winemac.drv/d3dmetal.c:100). If win_data NULL → 'winemetal[HWND]: get_win_data=NULL' → abort/fallback (no DXMT_HEADLESS/ORPHAN) → NO CAMetalLayer → NO visible present. ★Kimi step1 (replace ERR→fprintf instrument) ALREADY IN SOURCE (d3dmetal.c:114-124 logs hwnd/owner_tid[NtUserGetWindowThread]/current_tid[GetCurrentThreadId]/same/root/is_root). I did step2: rebuilt winemac.so (unix side; d3dmetal.o, instrument verified in .so) + deployed+signed to dist aarch64-unix. (a) STATIC: c000007b 'non-application-target' (macrunner_hb.c:19549) = run-loop refused an indirect-call target ctx->pc not recognized as guest-app-image / registered-x64+exec / dynamic-exec-JIT / native-counterpart (same classifier family as c0000026/SEH; q0=0x0/MEMORY_FAULT hints data-as-code garbage-ptr vs missed-thunk — symbolize 0x17fff43eb from live map). RUN (getwindata, heartbeat+WINEMETAL_TRACE_HWND, NO headless, TMO=1500) in flight → grep macrunner-get-win-data (owner_tid vs current_tid = thread-affinity test) + winemetal[HWND] + symbolize the c000007b @0x17fff43eb. HYPOTHESIS (Kimi): HK creates window on thread A, present-binding runs on thread B → owner≠current → wine get_win_data returns NULL → fix = marshal binding to owner thread / thread-safe lookup. COMMITS(HEAD): ...dfc2148.
2026-06-20 · ★★get_win_data DIAGNOSIS CONCLUSIVE — THREAD-AFFINITY HYPOTHESIS REFUTED; root = window-not-realized-at-bind (ORDERING). getwindata run markers (L320934, POST-input-init): `macrunner-get-win-data: hwnd=0x20054 owner_tid=84 current_tid=84 **same=1** root=0x20054 **is_root=1**` then `winemetal[HWND]: hwnd=0x20054 get_win_data=NULL` → `real-NSWindow fallback window=0x737c91180 layer=0x76f02a1f0 frame=1512x982 scale=2.0` (the ORPHAN/black window — DXMT presents to a detached NSWindow, NOT the game HWND → no visible frame in-game). ★same=1 ⇒ NOT cross-thread (Kimi's hypothesis WRONG); is_root=1 ⇒ NOT a child-hwnd. ROOT (corrected): get_win_data is a global thread-safe dict lookup (window.c:236, NO thread gate); win_data is CREATED lazily by macdrv_create_win_data (window.c:580) ONLY from macdrv_WindowPosChanging (:1678) = the window's first reposition/show. At swapchain-bind time the game window 0x20054 has NOT yet been positioned/shown → no WindowPosChanging → no win_data → NULL → orphan fallback. = an ORDERING problem ON THE OWNER THREAD: DXMT creates+binds the swapchain BEFORE Unity realizes/shows the window. ★FIX (proposed, d3dmetal.c my_get_win_data): on get_win_data==NULL for a top-level window on the owner thread (the verified case), FORCE realization — NtUserSetWindowPos(hwnd,0,0,0,0,0, SWP_NOMOVE|NOSIZE|NOZORDER|NOACTIVATE) (NO SWP_NOSENDCHANGING) → triggers WindowPosChanging → macdrv_create_win_data → win_data+cocoa_window created → retry get_win_data → bind to REAL window. We're on the owner thread (same=1) so synchronous + safe; first get_win_data returns NULL with mutex UNLOCKED (window.c:244) so no deadlock. Hidden-window OK (cocoa window created regardless of visibility; content appears on Unity's ShowWindow). ★(a) c000007b SYMBOLIZED: pc=**0xbc3b77610** (~47GB = WILD/garbage addr, no module) last_block=0x87ef11f6e79 (d3d11/DXMT region) last_bytes=`49 8b 47 10 4d 8b c5`=`mov rax,[r15+0x10]; mov r8,r13` = indirect call through a CORRUPTED/uninit fn-ptr loaded from [r15+0x10] in DXMT = DATA-AS-CODE (garbage vtable slot, q0=MEMORY_FAULT confirmed) — a LATER secondary wall (L596377), likely downstream of the half-init orphan present path. NOT a missed-thunk classifier gap. ★REPORTING before the present-path edit: hypothesis refuted, real root = ordering/unrealized-window, proposed fix = force-realize-on-owner-thread. 8-boot still queued (lane free now). COMMITS(HEAD): ...dfc2148.
2026-06-20 · ★FORCE-REALIZE FIX IMPLEMENTED (operator GO: force-realize in my_get_win_data). Snapshot winemac.so.pre-forcerealize. d3dmetal.c my_get_win_data: on get_win_data()==NULL, IF NtUserGetWindowThread(hwnd)==GetCurrentThreadId() (owner thread, the diagnosed case) → NtUserSetWindowPos(hwnd,0,0,0,0,0, SWP_NOMOVE|NOSIZE|NOZORDER|NOACTIVATE|NOOWNERZORDER|NOREDRAW) → triggers macdrv_WindowPosChanging→macdrv_create_win_data (win_data+cocoa window) → retry get_win_data once + log `macrunner-get-win-data: force-realize hwnd=.. retry_data=..`. Owner-thread-guarded (sync, no cross-thread deadlock); NULL path already released win_data_mutex (no held lock). NtUserSetWindowPos+SWP_* confirmed available (used in window.c same dll). Built winemac.so clean (force-realize string in .so) + deployed+signed dist aarch64-unix. VERIFY RUN (forcerealize monitor: HK no-headless + WINEMETAL_TRACE_HWND + window-capture-verdict --watch CG-capture, TMO=1500) in flight. SUCCESS = retry_data non-NULL + macdrv_view_create_metal_view (not orphan fallback) + CAMetalLayer/presentDrawable + WINDOW_VERDICT PASS non_background_pixels>0 = ★FIRST VISIBLE FRAME (rung 13). If retry_data still NULL → window un-realizable at bind time (deeper ordering) → reassess. COMMITS(HEAD): ...dfc2148 + (pending winemac force-realize).
2026-06-20 · force-realize v1 FIRED but retry_data=0x0 (still NULL) → the no-op SetWindowPos (NOMOVE|NOSIZE|NOZORDER) was SHORT-CIRCUITED by win32u, never reached macdrv_WindowPosChanging (confirmed: WindowPosChanging:1678 DOES create win_data if reached → NULL means not reached; same thread tid92, is_root=1 unchanged). ★FIX v2: added **SWP_FRAMECHANGED** to the NtUserSetWindowPos flags (forces win32u to run the full reposition + call the driver's WindowPosChanging even with no geometry change) + log the BOOL swp return. Built+deployed. Re-verify (forcerealize2 monitor) in flight. If retry_data now non-NULL → win_data created → macdrv_view bind to REAL window → present → visible frame. If swp=0 (SetWindowPos failed) or retry still NULL despite FRAMECHANGED → macdrv_create_win_data hits its own NULL path (line 600 desktop-parent / 608 alloc) — would need macdrv-secondary trace to pin; reassess. COMMITS(HEAD): ...dfc2148.
2026-06-20 · force-realize v2 (SWP_FRAMECHANGED): **swp=1 (SetWindowPos SUCCEEDED) but retry_data=0x0 STILL NULL** → macdrv_create_win_data returned NULL via its OWN path (not thread — same tid84). Boot then orphan-fallback again → eventually exit=1 (win32u callback-exception). The existing macdrv create-trace is WS_POPUP-gated (misses an overlapped main window) → added UNCONDITIONAL (env MACRUNNER_TRACE_SECONDARY_WINDOW, NO style gate) `macrunner-winrealize` markers (window.c): WindowPosChanging entry (had_data), create_enter (style/tids), create_NULL_desktop (GA_PARENT==NULL), create_NULL_alloc, create_OK (parent/is_desktop_parent). Built+deployed. DIAG RUN (winrealize, TMO=700) in flight → pins exactly why win_data NULL for game hwnd 0x20054: (i) WindowPosChanging NOT logged → my SetWindowPos didn't reach driver; (ii) create_NULL_desktop → window GA_PARENT==NULL (message-only/odd parent); (iii) create_OK but retry NULL → re-entrancy/data-released race. Then fix accordingly. COMMITS(HEAD): ...dfc2148.
2026-06-20 · ★★ROOT EXPOSED — force-realize REFUTED; the game window is NEVER realized by macdrv. winrealize diag run: (1) game window **0x20054 has ZERO macdrv lifecycle events** in 471K lines — no WindowPosChanging EVER, no ShowWindow. (2) **tid 84 (Unity main) NEVER realizes ANY window via macdrv** — only wine-internal tids 240/244/248 do (desktop/system windows 0x10020/28/32... → those hit create_NULL_desktop[GA_PARENT==NULL] or create_OK). (3) My forced NtUserSetWindowPos(0x20054, SWP_FRAMECHANGED) returned **swp=1 but did NOT trigger macdrv_WindowPosChanging** (no 0x20054 marker) → calling SetWindowPos from the UNIX-CALL binding context (winemetal _CreateMetalViewFromHWND → d3dmetal my_get_win_data, a nested unix call) CANNOT drive the PE-side window/driver machinery synchronously. ⇒ ★force-realize approach has a FUNDAMENTAL context limitation (operator's pick is refuted by data). ★DEEP ROOT: Unity creates window 0x20054 on thread 84 (win32u HAS it — DXMT got the hwnd) but NEVER shows/positions it through macdrv before binding the swapchain → CHICKEN-AND-EGG: window not realized → win_data NULL → DXMT orphan-fallback → real window never gets content → Unity may defer ShowWindow until after first present (which is orphaned). Also possible: tid-84 (emulated x64 Unity main under HB) window ops don't reach the PE-side macdrv driver hook at all. ★NEW FORK (force-realize dead): (A) DEFER/RETRY binding — winemetal: on get_win_data NULL, DON'T create orphan; retry on later presents until win_data appears (works IF Unity eventually shows the window). (B) Force-SHOW from PE side (NtUserShowWindow on the owner thread via a PE-side hook, not unix) — drives the real realize path. (C) Investigate WHY tid-84 window ops don't reach macdrv (HB×wine-driver interaction) — deepest. (D) longer run — does Unity EVER show 0x20054 (we ended at 471K still in scene-load)? Revert the d3dmetal force-realize (dead end) — keep winrealize diag for now. REPORTING — operator's force-realize refuted, new root = window-never-realized, need direction on (A)/(B)/(C)/(D). COMMITS(HEAD): ...dfc2148.
2026-06-20 · (D) LONG OBSERVATION RUN (operator GO: does Unity EVER show 0x20054?). REVERTED the dead-end d3dmetal force-realize (kept get-win-data instrument + window.c winrealize driver-side traces). Built+deployed reverted winemac.so. Run (winshow, TMO=2400, heartbeat+WINEMETAL_TRACE_HWND+MACRUNNER_TRACE_SECONDARY_WINDOW) in flight. DISCRIMINATOR: does `winrealize: WindowPosChanging/create_OK hwnd=0x20054` EVER fire? IF YES (Unity realizes the window later, post scene-load) → defer/retry binding (A) is the clean fix (wait for win_data). IF NEVER in 2400s → chicken-and-egg confirmed (Unity defers ShowWindow until first present, which is orphaned) → must force-show (B, PE-side) or break the cycle. Decides the fix. COMMITS(HEAD): ...dfc2148.
2026-06-20 · ★★(D) VERDICT → REFRAME: present-binding is DOWNSTREAM; the real blocker = scene-load NEVER COMPLETES. Long run (winshow, 1M lines/2400s): 0x20054 **NEVER realized by macdrv** (WindowPosChanging=0, create_OK=0); tid-84 NEVER realizes ANY window (only wine-internal 240/244/248); DXMT calls get-win-data **ONCE** (no retry) → orphan permanent. ★Late-phase: **81% FMOD sw-mixer SPIN** (UnityPlayer 0x17b2cxxx/bxxx) + ~19% scattered (0x62bxx) + 1× **c000007b non-application-target @pc=0xcf9fd6100** (another WILD garbage indirect-call addr, `mov rax,[r15+0x10]` garbage-vtable, kills 1 thread, process continues) + recurring **win32u.dll callback-exceptions** (x18-storm, frame_pc 0x87ffd49c944/0x87ffd49dc84). NO scene-completion markers. ⇒ The boot reaches input-init + swapchain-creation (orphan bind) then STALLS in scene-load (FMOD-spin + throughput + c000007b + win32u-x18) → never reaches Unity's ShowWindow → window never realized → no present. ★REFRAME: get_win_data NULL / present-binding (defer-retry/force-show) is REAL but only matters AFTER scene-load completes — which it DOESN'T. The IMMEDIATE blocker to first-frame is **completing scene-load**. tid-84-never-reaches-macdrv is likely just because the boot never gets to window-management (not necessarily a driver gap). ★CRITICAL PATH = scene-load throughput/walls: (1) NEUTRALIZE FMOD sw-mixer spin (81% CPU → frees throughput; operator's earlier '(i)'), (2) PIN+fix the c000007b garbage-vtable indirect call (recurring; which DXMT/d3d11 call site loads the garbage fn-ptr from [r15+0x10], what r15 points to — likely an uninit vtable slot, possibly the SAME root each run), (3) win32u x18-storm + per-callback throughput (deep). winemac get_win_data work = diagnosed + parked (needs scene-load first). REPORTING reframe + critical-path fork. COMMITS(HEAD): ...dfc2148.
2026-06-20 · (operator GO: pin+fix c000007b garbage-vtable). Added `macrunner-hb-badtarget` instrument at the non-application-target rejection (macrunner_hb.c:19522): dumps r15 + guest-memory derefs [r15+0..0x28] (the disasm `mov rax,[r15+0x10]; call rax` loads the garbage fn-ptr from [r15+0x10], so r15 is the object/vtable). Dropped block_pc (uninit at that point, -Werror). Built+deployed ntdll.so (badtarget in .so). DIAG RUN (badtarget, HEARTBEAT+HEARTBEAT_MODULE, TMO=1100) in flight → captures: which vtable slot is garbage (expect [r15+0x10] = the wild target, others = valid vtable ptrs/fields → confirms uninit slot 0x10) + module-map to symbolize the caller (run-exit last_block 0x87ef11f6e79 = d3d11/DXMT region) + the call-site function. Then: identify the COM object whose vtable slot @0x10 is uninitialized in the DXMT/d3d11 path → fix (init the slot / route the call). Likely a missing DXMT method impl (NULL/garbage vtable entry) the Unity scene-load path calls. COMMITS(HEAD): ...dfc2148.
2026-06-20 · badtarget v1 run (heartbeat-MODULE) MISSED it — the heavy _MODULE trace changed timing → a DIFFERENT c000007b fired: `reason=runtime pc=0x87efc7c5605 (UnityPlayer rva 0x2b5605)` (a JIT/translation runtime failure w/ macrunner-hb-fail-bt backtrace, r15=0x320227198 q0/q8=0x0 — NOT garbage), not the non-application-target garbage-call. ⇒ ≥2 distinct c000007b modes (non-application-target garbage-call vs runtime JIT-fail), INTERMITTENT + timing-dependent. ★FIX: made the badtarget instrument SELF-SYMBOLIZE (MR_BTSYM: macrunner_hb_module_from_pc + get_export_module_name → module+rva for pc/r15/[r15+0x10]/[r15+0]) so NO heartbeat-module needed → lighter run → the garbage-call variant fires + faster boot. Built+deployed ntdll.so. badtarget2 run (plain HEARTBEAT, TMO=900, MAX=3 to catch the intermittent) in flight → expect macrunner-hb-badtarget with symbolized object/vtable/slot → pin the COM object + uninit slot @0x10 + caller. COMMITS(HEAD): ...dfc2148.
2026-06-20 · ★★c000007b = STATE-CORRUPTION SYMPTOM, NOT a uninit-vtable (hypothesis REFUTED). badtarget2 capture: `pc=0x1679c43eb r15=0x2b [r15+0]=0 [r15+8]=0x109 [r15+10]=0 q0/q8=MEMORY_FAULT`; last_block=0x1679c3bde last_bytes=`48 8b 0e 48 63 41 18`(mov rcx,[rsi]; movsxd rax,[rcx+0x18]). ★r15=0x2b (=43, a tiny INT not a pointer) → NOT an object/vtable. ★This run's pc/last_block/last_bytes are COMPLETELY DIFFERENT from prior runs (which had `49 8b 47 10`=mov rax,[r15+0x10], pc=0xcf9fd6100/0xbc3b77610). ⇒ the c000007b is a VARYING garbage target from DIFFERENT call sites each run, with CORRUPTED register state (garbage r15, MEMORY_FAULT vector regs) = guest-STATE-CORRUPTION symptom (JIT mistranslation OR x18-storm corrupting context during scene-load), NOT a discrete missing-DXMT-method/uninit-vtable. NON-FATAL (kills 1 thread, boot continues to 1M+ lines). ⇒ pinning/fixing it as a single bug is NOT tractable — it's downstream of the same deep instability (JIT/x18) that throttles scene-load. ★HONEST NEGATIVE RESULT redirecting: c000007b is a corruption symptom, not the fixable root. The scene-load blocker remains THROUGHPUT + STATE-INSTABILITY (FMOD 81% spin + per-callback + x18-storm + JIT-mistranslation). badtarget instrument (self-symbolizing) committed-worthy as a diagnostic but the FIX target moves. REASSESS w/ operator: (a) FMOD-neutralize (free 81% CPU), (b) deep JIT-correctness/x18-storm (the corruption root), (c) checkpoint. COMMITS(HEAD): ...dfc2148.
2026-06-20 · (operator GO: neutralize FMOD spin). Implemented FMOD-mixer THROTTLE in run_x64 (macrunner_hb.c:19967, env MACRUNNER_HB_FMOD_THROTTLE): when block_pc is in UnityPlayer rva 0x17b2bb0-0x17b2c80 (the sw-mixer spin), per-thread duty-cycle nanosleep(200us) every 128 blocks → ~10% duty (caps the 81% spin → frees ~70% of a core). Region resolved+cached from first UnityPlayer block (ASLR-safe via module_from_pc+get_export_module_name+strieq). Built+deployed ntdll.so. TEST RUN (fmodthr, heartbeat, MACRUNNER_HB_FMOD_THROTTLE=1, TMO=1500) in flight. GATE: (1) FMOD post-ii block-share DROPS (throttle works), (2) boot reaches FURTHER than baseline (baseline ceiling = UnloadTime then stall) → scene-complete/PlayerLoop/window-show. If further → FMOD was throttling scene-load → continue toward present. If NOT → FMOD is a parallel-core red herring (scene-load gated by main-thread throughput/state-corruption, not FMOD) → reassess. COMMITS(HEAD): ...dfc2148. [FMOD throttle entry below]
2026-06-20 · ★★FMOD THROTTLE = NEGATIVE RESULT (red herring CONFIRMED). fmodthr run (MACRUNNER_HB_FMOD_THROTTLE=1): throttle engaged (FMOD post-ii block share 81%→**75.5%**) BUT **boot ladder UNCHANGED** (UnloadTime ceiling, NO PlayerLoop/Scene/window-show — same as baseline). Freeing ~70% of a core did NOT advance scene-load. ⇒ FMOD is a PARALLEL-CORE red herring; scene-load is gated by the MAIN THREAD's own throughput/state-instability, NOT FMOD's core-theft. FMOD throttle = env-gated (off by default), harmless, kept as a lever. ★★★SESSION CONVERGED CONCLUSION (2 consecutive negative results — c000007b=corruption-symptom-not-bug + FMOD=red-herring): the remaining HK scene-load blocker is the DEEP main-thread THROUGHPUT + STATE-INSTABILITY (per-callback JIT lifecycle + x18-storm + JIT-mistranslation corruption) — peripherals (FMOD, c000007b, present-binding) are all downstream/parallel, NOT the gate. Completing scene-load needs main-thread speed+correctness, a MAJOR engine effort, not a quick fix. ★CHECKPOINT: dfc2148 fallback-loop fix (input-init→scene-load, biggest advance, LOCKED in HEAD) is the session's verified win; present-binding fully diagnosed+parked (orphan-window root, ordering); c000007b + FMOD ruled out as discrete fixes. NEXT (fresh, major): main-thread scene-load throughput/correctness (per-thread hb_memory cache=LANE-A-NEEDS [B-full]; x18-storm; JIT-mistranslation origin). Uncommitted diagnostics in working tree (badtarget self-symbolize, FMOD-throttle env-gated, winrealize traces, get-win-data instrument) — keep as diagnostics or revert per operator. COMMITS(HEAD): ...dfc2148.
2026-06-20 · THROUGHPUT TRACK — VERIFY-FIRST PROFILE (operator GO). Profiling the scene-load MAIN/driver thread BEFORE picking the lever (FMOD + c000007b both refuted by measuring — don't assume x18 either, it profiled 0% at rank-7 historically). KEY: HEAD now has Codex reset-pool (pools jit_rt across callbacks) — so the OLD profile (74% hb_jit_runtime_destroy, pre-reset-pool) may NO LONGER hold; need a FRESH profile of current binaries (HEAD reset-pool + dfc2148 + my inactive diagnostics). Probe: deepstall (SETTLE=160s into scene-load, per-thread sample x2, FMOD-throttle OFF=baseline). Will classify the driver thread's LEAF histogram into: per-callback TEARDOWN (hb_jit_runtime_destroy/mach_vm_deallocate/block_cache_destroy/ir_cache_destroy), per-callback SETUP (create/map_live/calloc), X18-FAULT (segv/x18/x64-signal-callback/route_x64_callback_fault), RE-TRANSLATION (hb_lift/hb_decode/codegen), INTERPRETER (hb_interpreter/exec_instr/exec_interp_ir — NB dfc2148 routes unsupported ops here, could be a NEW cost), REPLAY/SYNC (replay_virtual_regions/sync/getenv), guest-JIT-exec, PARKED. The dominator picks the lever: teardown→B-full per-thread hb_memory cache; x18→x18 work; re-translation→translation cache; interpreter→codegen the hot interp'd ops. In flight.
2026-06-20 · ★★★PROFILE RESULT (verify-first) — DOMINATOR = per-block PC-CLASSIFICATION, NOT teardown/x18/FMOD. Driver thread Thread_7100754 (scene-load main, 2 samples consistent): mach_msg2_trap 20% (mostly the 30% PARK) | macrunner_hb_run_x64 17-18% (dispatch loop) | **macrunner_hb_pc_in_graphics_arm64x_x64_range 9-9.7%** | __psynch_mutexdrop 7% + __psynch_mutexwait 3% (MUTEX CONTENTION ~10-11%) | macrunner_hb_try_ntdll_version_semantic 7% | pc_is_x64_guest_code_module_no_lock 5% | is_registered 2% | module_name_matches 1.3% | hb_jit_runtime_run 2-3% + exec_instr 1% (guest exec ~7%) | try_promote_hot_block 2%. ★TEARDOWN=0% (Codex reset-pool eliminated the old 74% — B-full cache NOT needed!), X18=0% on driver (exactly as operator warned — measured not assumed), INTERPRETER=1% (dfc2148 routing not a cost), RE-TRANSLATE~0%. ⇒ DOMINATOR = per-block PC-classification (~17%): pc_in_graphics(9.7%, MINE from the SEH fix — LDR-walk+strieq+CodeMap PER BLOCK) + module_no_lock(5%) + is_registered(2%). Plus mutex-contention(~11%) + try_ntdll_version_semantic(7%). ★FIX 1 (THIS pass): per-thread 6-entry module-classification CACHE in pc_in_graphics_arm64x_x64_range (cache [base,end)->is_gfx; non-graphics module=fast FALSE, graphics=precise) → collapses the 9.7% to a range-compare. Built+deployed. RE-PROFILE (profcache) in flight → confirm pc_in_graphics drops + driver does more useful work. NEXT passes if still hot: cache module_no_lock(5%), early-exit try_ntdll(7%), pin the contended mutex(11%). LEVER = per-block classification + mutex, NOT a deep x18/teardown rewrite. COMMITS(HEAD): ...dfc2148.
2026-06-20 · FIX 1 CONFIRMED + FIX 2 added (throughput track). RE-PROFILE (profcache, both samples): **pc_in_graphics_arm64x_x64_range GONE from top-12** (was 9-9.7% → ~0, cache worked) + input-init FASTER (el=400s vs 504s baseline). New post-fix-1 driver breakdown: run_x64 19-21% | mach_msg 22%(park) | **try_ntdll_version_semantic 8-9.6%** (now #1 non-park) | mutex psynch_drop+wait ~11% | module_no_lock 4% | find_region_normalized 3.5% | guest-exec ~5%. ★FIX 2: try_ntdll_version_semantic did an LDR-walk PER BLOCK but only applies if image_base IS amd64-ntdll (constant per run_x64; main thread's image_base=EXE → always FALSE = 9% pure waste). Gated the per-block call (macrunner_hb.c:19814) on a cached per-thread "is image_base ntdll" (vs_chk_base/vs_chk_res + goto skip_version_semantic) → non-ntdll threads skip it entirely. Both fixes ≈17% main-thread overhead removed (9.7%+9%). Built+deployed. ★VERIFY-ADVANCE run (thrufix, TMO=1800, heartbeat) in flight → does scene-load advance PAST the UnloadTime baseline ceiling (→ PlayerLoop/Scene/window-show)? If yes → throughput track is working, continue (mutex 11% + module_no_lock 4% next). If still UnloadTime ceiling → ~17% not enough alone; need the mutex + dispatch reductions too. NOTE: fixes are real perf wins regardless (committable). COMMITS(HEAD): ...dfc2148.
2026-06-20 · ★★VERIFY-ADVANCE (thrufix run, both throughput fixes) + ★HIDDEN LEVER FOUND. RESULT: boot reached input-init FASTER (L299128 vs baseline L320-336K — fixes work) BUT scene-load did NOT complete (stalled post-swapchain-bind at L299418, then 550K heartbeat lines + a win32u x18 callback-exception, no UnloadTime/PlayerLoop/present). = THIRD lever (FMOD, pc_in_graphics, try_ntdll) that reduced cost WITHOUT advancing scene-load → scene-load completion is NOT purely per-block-throughput-gated (also racy variance + deeper). ★★HIDDEN DOMINATOR: the "30% park" (mach_msg2_trap 22%) is NOT a wait — its PARENTS are **mach_vm_region (~10%) + mach_vm_read_overwrite (~10%) + mach_vm_write (~2%) = ~22%** = HyperBridge reading/writing GUEST MEMORY via slow MACH VM SYSCALLS instead of direct host memcpy (the special_read/find_region path for non-directly-accessible guest regions). This is the SINGLE BIGGEST remaining throughput lever, bigger than the classification just fixed, and was hidden under mach_msg2_trap. ★STATUS: 2 throughput fixes (pc_in_graphics cache + try_ntdll gate, ~17%) = VERIFIED perf wins (faster to input-init, costs dropped, no regression) — committable. Scene-load completion still elusive (throughput grind + variance). ★NEXT VERIFY-FIRST LEVER: the memory-access subsystem — make guest reads/writes DIRECT (memcpy) instead of mach_vm_read_overwrite/region/write syscalls (~22%). Biggest single remaining. OR checkpoint (scene-load completion is a multi-fix grind, may be engine-maturity-bound). COMMITS(HEAD): ...dfc2148 (throughput fixes uncommitted, verified-good).
2026-06-20 · THROUGHPUT WINS COMMITTED (main 51605cd: pc_in_graphics cache + try_ntdll gate, ~17%). Then ★MEMORY-ACCESS LEVER (~22%) implemented (operator GO). ROOT: special_read (macrunner_hb.c:3720) + special_write (3892) do mach_vm_region (find/check region) + mach_vm_read_overwrite / mach_vm_write (copy) = TWO Mach syscalls PER guest memory access; the region cache (cache_live_region→hb_memory_map) is POPULATED but NOT CHECKED before the syscall. FIX: fast path at top of special_read/write — hb_memory_find_region(addr); if cached + readable/writable + addr in [base,base+size) → DIRECT host memcpy (host_base translate for private mappings, else 1:1 addr) → skips BOTH syscalls. Cache MISS still uses the defensive mach_vm path (validates + populates) → safe (no fault-risk on first/uncached access; only hot repeat accesses go direct). Built+deployed. ★★DECISION GATE run (memgate, TMO=1800, heartbeat) + parallel mid-run sampler in flight. PER OPERATOR — distinguish: (A) scene-load progresses PAST the swapchain-bind/win32u-x18 stall → throughput WAS the gate → keep going. (B) scene-load STALLS AT THE SAME point (win32u x18 callback-exception, frame_pc 0x87ffd49dc84, no UnloadTime/present) → DISCRETE blocker, NOT throughput → PIVOT to the callback-exception (do NOT run a 5th throughput lever). HYPOTHESIS: the win32u x18 callback-exception may BE the SEH-unwind-through-x64 convergence wall (exception in win32u callback that can't unwind) — ABZU is building the x64-frame unwind fix; when it lands, test if it clears HK's stall too. Sampler confirms the memory fix engaged (mach_vm_read_overwrite/region should DROP from ~22%). COMMITS(HEAD): ...dfc2148, 51605cd.
2026-06-20 · ★★DECISION GATE = (B)-with-twist: stall point UNCHANGED + SEH-wall REFUTED. memgate run (memory-access fast paths): input-init REACHED (L306900), boot ladder IDENTICAL (Mono→PhysX→GfxDevice→D3D11→FMOD-fail→input-init), NO advance past swapchain-bind (no UnloadTime/PlayerLoop/present) → SAME stall point as the throughput-fix run → throughput is NOT the gate (4 levers now: FMOD + pc_in_graphics + try_ntdll + memory-access all reduced cost WITHOUT moving the stall). ★BUT the SEH-unwind-wall hypothesis is REFUTED: **unwind-unsafe-boundary=0**, callback-exceptions=18 TOTAL (mostly EARLY-boot ntdll, healed; only 1 win32u @0x87ffd49bfa4, healed, non-terminal) — NOT a storm, NOT a terminal SEH wall. So the win32u callback-exception is NOT the SEH-unwind-through-x64 convergence wall (ABZU's x64-unwind fix likely won't clear HK). ⇒ The blocker is NEITHER pure throughput (4 levers didn't move it) NOR a discrete crash/SEH-wall (none present) — it's scene-load GRINDING after swapchain-bind WITHOUT completing AND WITHOUT crashing (~144K dead heartbeat lines post-bind). NB memory-fix ENGAGEMENT unconfirmed (mid-run sampler failed to find the HK pid) — but gate (B) holds regardless (stall unchanged across multiple throughput attempts). ★HONEST SYNTHESIS: HK scene-load completion has NO single identifiable blocker — it's a deep grind (Mono asset-load + scene-init under emulation is enormous) and/or a subtle render/worker dependency, possibly just needs vastly more time. Memory-access fast-path fix HELD uncommitted (logically sound throughput win, engagement unverified, doesn't advance the goal). ★STATUS: dfc2148 (input-init→scene-load) + 51605cd (throughput, ~17%) = committed verified wins. Scene-load COMPLETION = open, deep, multi-session. NEXT (operator call): (a) much-longer run (>1hr) to see if scene-load EVER completes, (b) pin what the main thread does in the 144K-line post-bind grind (progress vs wait), (c) checkpoint — accept scene-load is engine-maturity-bound. COMMITS(HEAD): dfc2148, 51605cd.
2026-06-20 · (b) TERMINATION + FORWARD-VS-WAIT probe (operator GO). KEY operator insight: HK EXITED before 12min (sampler empty @t=720s, 451K-line log) = NOT an infinite hang → something TERMINATES it after the swapchain-bind grind. /tmp/hk_termination_probe.py: heartbeat run, sample main-thread top-of-stack at SETTLE=80s post-ii (forward-vs-wait), then let the lane die NATURALLY (no kill) to capture the termination, no prune. Will pin: (b.1) main thread MOVING through new code (asset-load/Mono/PhysX=forward, slow, consistent w/ ~22% mach_vm guest-mem) vs STUCK in a wait/sync primitive (WaitForSingleObject/spinlock/poll on a never-coming signal=dependency); (b.2) the TERMINATION — guest ExitProcess/NtTerminateProcess (Unity quitting on a failed asset/managed exception/assertion=CORRECTNESS bug not throughput) vs quiet crash vs harness timeout. DUAL-PURPOSE: the post-bind sample ALSO confirms memory-fix engagement (mach_vm_read_overwrite/region DROP from ~22%? → decides commit-vs-hold of the held memory-access fix). DECIDES NEXT: forward+clean-timeout→(a) longer run; wait→find the dependency; guest-abort/exception→fix the scene-load failure. Memory-access fix HELD uncommitted pending this engagement check (operator: don't ship unverified). COMMITS(HEAD): dfc2148, 51605cd.
2026-06-20 · ★★★(b) ANSWERED + memory-fix REVERTED (verify-first corrected a buggy bucket). termpin run: input-init t=368s (faster, throughput fixes help), HK ALIVE at t=448s (sampler found pid), lane killed by 900s harness TIMEOUT (rc=0). ★(b.2) TERMINATION = HARNESS TIMEOUT, NOT a guest abort/crash/self-exit — HK grinds ALIVE to the timeout. The memgate "exited before 12min" was a SAMPLER BUG (bash one-liner pid-detection failed), NOT HK dying. ★(b.1) FORWARD-progress, NOT a wait: driver thread explicit-WAIT-primitives = **0.0%** (no NtWaitForSingleObject/futex/server_wait) — it's BUSY, not blocked on a dependency. BUT efficiency is terrible: only **~4.5% actual guest-JIT-exec**; the other ~95% = per-callback OVERHEAD: run_x64-dispatch/abi 21% + classify 14% (pc_is_x64_guest_code_module_no_lock 7% still uncached + find_region) + **mach_vm memory-syscalls ~25%** + mutex-contention ~15% (under macrunner_hb_update_current_x64_context = per-callback x64-context-registration LOCK). ★MEMORY-FIX CORRECTION: my earlier leaf-bucket said mach_vm=0.1% — BUG (it bucketed the leaf mach_msg2_trap, not the parent mach_vm_region). PARENT-trace shows mach_vm STILL ~25% → the fast-path cache MISSES every time because the live-region cache lives in ctx->memory which is FRESH PER CALLBACK (not persisted) → never warms across calls; AND my added find_region check adds cost without hitting = NET NEGATIVE → REVERTED (back to 51605cd, rebuilt+redeployed clean). ⇒ ★ROOT = PER-CALLBACK OVERHEAD (95%): fresh-context-per-callback means each callback re-does dispatch + classification + memory-region-cache-warmup (mach_vm) + x64-context-registration (mutex). Scene-load IS progressing (forward) but at ~4.5% efficiency → grinds to timeout, never completes in budget. ★REAL LEVER = B-FULL per-thread context+memory CACHE (persist ctx+hb_memory+region-cache across callbacks): engages the mach_vm fast-path (region cache persists) + kills the per-callback context-registration mutex + dispatch warmup. This is the architectural fix the per-callback overhead demands (NB earlier "B-full not needed" was about TEARDOWN=0; SETUP/persistence is the real cost). DECISION: forward+clean-timeout → operator's tree says (a) longer run may finish — but at 4.5% efficiency it'd be very long; B-full is the real fix. COMMITS(HEAD): dfc2148, 51605cd (memory-fix reverted, not shipped).
2026-06-20 · ★PER-BLOCK FIXES (operator: targeted, not B-full — profile showed per-block/per-read dominate, B-full's per-callback premise refuted: update_current_x64_context is PER BLOCK @19414+20394, register_current_x64_context not in top leaves). LIFECYCLE STUDY findings: (1) the per-block update_current_x64_context global-lock (~15% psynch contention) refreshes a per-tid AMD64_CONTEXT snapshot read ONLY cross-thread (NtGet/SetContextThread/SEH, rare) — and those readers (get_x64_thread_context:1495, set:1649) RE-REFRESH the snapshot from entry->ctx on access → a missed per-block refresh is HARMLESS. Snapshot is MULTI-writer (owning thread per-block + cross-thread get/set) so a single-writer seqlock is unsafe → chose TRYLOCK-SKIP: update_current_x64_context never BLOCKS on the contended global lock (pthread_mutex_trylock; skip on contention, next uncontended block refreshes); register_current (critical per-callback seed) keeps its hard lock. (2) pc_is_x64_guest_code_module_no_lock (~7%, per-block LDR-walk+machine-check) → per-thread 6-entry module-classification cache (cached AMD64 module=precise exec-check, non-AMD64=fast FALSE), same pattern as the committed pc_in_graphics cache. Built+deployed. RE-PROFILE (perblock) in flight → verify BOTH AXES: (a) efficiency — mutex-contention ~15%→~0 + module_no_lock ~7%→~0 (driver thread) AND (b) correctness — reaches input-init + normal boot ladder, no divergence/new-fault. Ship only if both. Expected: ~22% main-thread overhead reclaimed (on top of the committed 51605cd ~17% + dfc2148). COMMITS(HEAD): dfc2148, 51605cd.
2026-06-20 · ★★PER-BLOCK FIXES VERIFIED + COMMITTED (main 2b9fbe8). RE-PROFILE (perblock, both axes GREEN): (a) MUTEX-contention 15%→**0.0%** (trylock-skip), module_no_lock 7%→**0.0%** (cache); (b) input-init el=**320s** (faster than 368s/504s baseline), normal ladder, no divergence/new-fault. ★CUMULATIVE THROUGHPUT: dfc2148 (fallback→scene-load) + 51605cd (~17%) + 2b9fbe8 (~22%) = ~39% main-thread overhead reclaimed, input-init 504s→320s (~37% faster to input-init). Driver-thread now: mach_msg 29.5% (=the ~22% mach_vm still, under it) + run_x64-dispatch 27.6% + fill_amd64 4.2% + find_region 3.3% + guest-exec 4.2%. ★REMAINING LEVERS: (1) mach_vm ~22% (the genuine B-full-MEMORY part: persist region-cache per-thread + fast-path + VM-change INVALIDATION — the riskier follow-up, needs the staleness correctness work); (2) run_x64 dispatch 27.6% (core JIT loop, harder). ★Scene-load COMPLETION still unverified (cumulative throughput up ~39% but mach_vm 22% + dispatch 27% remain + FMOD parallel drain). NEXT (operator call): (a) mach_vm ~22% lever (B-full-memory w/ invalidation), (b) LONG run to see if scene-load completes given cumulative ~39% gain, (c) checkpoint. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-20 · (b) FIRST-FRAME RUN (operator GO) — first-frame test + de-risk before the risky mach_vm staleness work. firstframe monitor: lane TMO=2400 (40min, bounded), heartbeat + WINEMETAL_TRACE_HWND, NO headless (real present path), + window-capture-verdict --watch (CG-capture if window appears) + present-marker scan (CAMetalLayer/presentDrawable/macdrv_view) + top-of-stack sample every 120s. Cumulative throughput now ~39% reclaimed (dfc2148+51605cd+2b9fbe8), input-init 504s→320s. THREE OUTCOMES: (1) scene-load COMPLETES + presents → ★FIRST FRAME (throughput track delivered); (2) major progress past UnloadTime→PlayerLoop/scene-complete → CLOSE → mach_vm ~22% (a) worth its staleness risk → do next; (3) still grinding at input-init/swapchain-bind → bigger bottleneck (dispatch 27%/FMOD/structural) → reassess BEFORE the risky mach_vm. Will report: total scene-load time at new speed + furthest rung + present markers. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-20 · ★★★(b) FIRST-FRAME RUN VERDICT = OUTCOME (3) + REFRAME: throughput is DEFINITIVELY NOT the scene-load gate; the c000007b kills a SCENE-LOAD WORKER. 40-min run (TMO=2400): input-init L292960 (FASTER — ~39% throughput gain works) but boot ladder STOPS at input-init (Mono→PhysX→input-init), NO UnloadTime/PlayerLoop/scene-complete, **PRESENT markers=0**, no window. SAME stall point as EVERY prior run regardless of throughput ⇒ ★throughput is NOT the gate (the ~39% faster boot reaches input-init quicker but stalls identically) → mach_vm ~22% (a) NOT worth its staleness risk. ★★KEY NEW INSIGHT — the late-phase sequence: input-init→orphan-bind(get-win-data NULL)→ThreadInit L461186 (worker starts)→**c000007b non-application-target L533511** (pc=0x66df16100, r15=0xe81c75be0, [r15+0]=0x0 — garbage-call KILLS a thread)→win32u callback-exception L594574→then GUEST GOES IDLE (late sample t=2329s: CPU 43%, run_x64=**0%**, mostly __ulock_wait2 parked). ⇒ the c000007b kills a thread, THEN the guest goes idle (main thread parks waiting). HYPOTHESIS: the c000007b kills a SCENE-LOAD WORKER thread → main thread waits forever for the dead worker → DEADLOCK/idle-stall. ★CORRELATION: runs WHERE c000007b fires → stall at input-init (this run); runs WITHOUT it (fallbackfix) → reach UnloadTime. So the c000007b (earlier PARKED as "non-fatal state-corruption symptom") may BE the intermittent GATE — by killing worker threads scene-load depends on. ★PIVOT (operator's tree: outcome-3 → reassess before risky mach_vm): the real blocker is likely the c000007b garbage-call killing scene-load workers, NOT throughput. Next = investigate: does the c000007b kill a scene-load worker + is the late-idle a deadlock-on-dead-worker? The c000007b root = guest-state-corruption (JIT-mistranslation/x18) producing the garbage indirect call — the deep ISA-correctness issue. ★3 throughput wins (dfc2148/51605cd/2b9fbe8) BANKED (faster boot for all guests) but scene-load completion is gated by the c000007b-worker-death, NOT throughput. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-20 · (1) PIN THE c000007b WORKER-KILL DEADLOCK (operator GO, verify-first before the deep root). /tmp/hk_deadlock_probe.py: launch HK heartbeat, watch run.log for the c000007b (run-exit non-application-target), capture the dying-thread context, wait 35s for the boot to go idle, then FULL per-thread sample (3 tries — c000007b is intermittent). Will pin: (1) the dying thread (c000007b regs/pc + correlate via heartbeat to thread NAME — Job.Worker/AssetGC/Loading.AsyncRead/etc.); (2) the MAIN thread's idle-wait stack (NtWaitForSingleObject/join on a handle/TID); (3) cross-ref: is the main thread blocked on the DEAD worker (a wait on a TID/handle that no longer exists) = deadlock-on-dead-worker, AND which expected worker thread is MISSING from the live-thread set. Confirms-or-refutes the mechanism BEFORE attacking the corruption ORIGIN (the deep ISA root: where guest state first goes garbage → the garbage indirect call). Mitigation angle noted: could main's wait-on-worker be made dead-worker-tolerant? (interim; real fix = corruption origin). COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-20 · ★★★DEADLOCK HYPOTHESIS REFUTED — boot is PROGRESSING, not deadlocked; no single gate. deadlock-probe (c000007b fired t=546s, idle-state full per-thread sample): (1) NO worker MISSING — all Job.Worker 0-6 + Background 0-15 + Loading.AsyncRead/PreloadManager + AssetGC×7 PRESENT. (2) NOT deadlocked — **7 threads ACTIVELY executing guest code** (hb_jit_runtime_run + exec_instr: dxmt-encode/finish×4 [rendering!] + 2 unnamed driver threads); 30 parked on NtWaitForAlertByThreadId (WaitOnAddress = idle workers awaiting work, NORMAL — producers are running) + 10 server_wait + 2 mach_msg. (3) FMOD-spin=0 (not even active). c000007b (r15=0x2b garbage, last_bytes `48 8b 0e 48 63 41 18`=mov rcx,[rsi];movsxd rax,[rcx+0x18] — the rsi/rcx variant) KILLS a thread but NOT a needed worker (all present) + does NOT deadlock (boot progresses). ⇒ deadlock-on-dead-worker REFUTED. ★★HONEST CONSOLIDATED VERDICT (after exhausting EVERY single-blocker hypothesis: throughput, FMOD, c000007b-crash, c000007b-deadlock, SEH-wall, present-binding — ALL refuted by measurement): HK scene-load has NO single gate. The boot reaches input-init reliably + PROGRESSES (7 threads executing guest code via JIT+interp-fallback) but is ENORMOUSLY SLOW under emulation (Mono JIT of all game C# + asset load + scene init) → doesn't complete in a 40-min budget. It's an ENGINE-MATURITY / cumulative-throughput grind, not a discrete fixable blocker. The c000007b IS a real ISA-correctness bug (JIT-mistranslation → garbage guest state, intermittent thread-death) worth the deep root fix, but it's NOT the scene-load gate. ★SESSION BANKED: dfc2148 (fallback→scene-load, the breakthrough) + 51605cd + 2b9fbe8 (throughput, ~39% reclaimed, boot ~37% faster to input-init). First frame requires sustained engine maturity (faster emulation + JIT correctness), beyond one more fix. RECOMMEND CHECKPOINT. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-20 · ★★★4h VALIDATION OVERTURNS THE VERDICT — HK HARD-STALLS at UnloadTime; concrete gate = win32u fault loop in the DESKTOP/MESSAGE path. The 4-HOUR no-heartbeat run produced only **1599 lines/0.2MB**: input-init(L1563)→get-win-data NULL→orphan window(L1565-67)→UnloadTime 176ms(L1572)→then **3h50m of ZERO progress** (only 27 lines, all repeating win32u callback-exceptions, same thread sp=0x10d92fbb0). present=False, no window. ⇒ ★the "engine-maturity / progresses-slowly" verdict is WRONG — HK does NOT progress; it HARD-STALLS at the first UnloadTime. Only a LONG run revealed it (shorter runs ended before the loop was obviously terminal — the validation earned its 4h). ★★THE GATE (concrete): right after UnloadTime, the DESKTOP/WINDOW-MESSAGE path faults in a loop — frame_pc=**win32u.dll rva 0x2c944/0x2cc84/0x2dc84** (the NtUser* message-loop fns), frame_lr=**explorer.exe (0x140012828 rva 0x12828) + user32.dll (rva 0x94fdc/0x6c328)** — NOT HK game code. = the wine desktop/message-processing (explorer+user32→win32u) hits a callback-exception loop. SPARSE (27 in 4h ⇒ threads mostly PARKED + occasional fault, a HARD HANG not a CPU-spin). ★STRONGLY connects to the ORPHAN WINDOW (get-win-data NULL, parked as "downstream") → the message-processing for the unrealized HK window 0x20054 faults in win32u → boot stuck → present-binding is likely UPSTREAM of the stall, not downstream. ★REVISED VERDICT: there IS a concrete gate — a win32u callback-exception fault loop in the desktop/message path after the first scene-unload, likely rooted in the orphan/unrealized window. NEXT: confirm WHY win32u rva 0x2c944-0x2dc84 faults (exception code + faulting instr — x18-fault-not-healing? NULL-deref on the orphan window? message-for-unrealized-window?). The reactive well is NOT dry — the validation found the gate. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-20 · DECISIVE SLOW-vs-STALL DIAGNOSTIC (operator GO). The live 4h process DIED before I could sample it (my validate-monitor's TMO mr-clean killed HK ~as the operator saw it ~35% CPU) — opportunity lost. BUT the frozen log already settles step-1(b): post-UnloadTime content REPEATS (27 win32u callback-exceptions, same sp=0x10d92fbb0 / win32u rva 0x2c944-0x2dc84, over 3h50m) + operator saw ~35% CPU ⇒ a CPU-CONSUMING win32u fault LOOP (callback-exception logging is rate-limited, so 35% CPU + 27 logged = tight loop) = STALL signature, NOT idle slow-grind. The stall sets in AT UnloadTime, so a fresh run reaches the identical stuck state in ~10min. RE-RUN (stallpin, heartbeat, sample 90s post-UnloadTime) in flight to get the operator's exact decisive test: the main-thread block_pc trace post-UnloadTime — CYCLING in a small region (STALL → discrete blocker = win32u loop, the frame-unblocking target) vs ADVANCING through new code (slow → engine-maturity speed work). COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-20 · ★★★DECISIVE VERDICT = SLOW-GRIND (forward progress), win32u-stall OVERTURN RETRACTED. stallpin re-run (heartbeat, 90s post-UnloadTime): label=thread **1315 DISTINCT block_pcs / 29174 hits (5% unique) = VARIED = ADVANCING through new code = FORWARD progress**; 0 callback-exceptions this run. ⇒ the boot IS progressing past UnloadTime, NOT a tight stall. The 4h run's 27 win32u callback-exceptions were SPORADIC side-faults (not a loop) in a NO-heartbeat log I over-read as a "win32u stall" — RETRACTED. ★PHASE PROFILE (sample t=612s): the 1315 distinct PCs are the AGGREGATE of all guest threads (the WORKERS executing scene-load); the MAIN thread is 76% in `NtWaitForSingleObject` (run_x64→try_kernel32_handle_semantic→server_wait→read) = COORDINATING, waiting on worker-completion = NORMAL parallel scene-load (main dispatches+waits, workers grind Mono-JIT+assets). ⇒ scene-load is genuinely SLOW under emulation (workers grinding the game's first-run Mono-JIT + asset processing), main thread waits — it WOULD complete given enough time, just enormously slow (>4h). ★FINAL: HK first-frame is gated by ENGINE SPEED (worker-thread execution of Mono-JIT'd game code + asset load under emulation), confirmed by measurement, NOT a discrete blocker. Targeted speed work = the worker/guest execution path (JIT/interp throughput for game code). The c000007b (correctness) + sporadic win32u side-faults are real bugs worth fixing but NOT the gate. dfc2148+51605cd+2b9fbe8 (~39% faster boot) banked. ★Verify-first corrected me TWICE here (over-read no-heartbeat log → win32u-stall, then heartbeat → forward-progress) — the discipline held. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-20 · TARGETED SPEED WORK opened (operator GO): profile the WORKER threads (not the main thread which is 76% WaitForSingleObject coordinating). workerprof run: reach post-UnloadTime scene-load, sample, then AGGREGATE all guest WORKER threads' EXECUTION (run_x64/exec_instr/hb_jit_runtime_run + callees), bucketed: JIT-TRANSLATION (lift/codegen — re-translating Mono-JIT'd code), INTERPRETER (exec_instr — dfc2148 fallback / interp backend), DISPATCH (run_x64/call_import/abi), MEMORY (mach_vm/special_read), classify/mutex. The top 1-3 hot spots in the worker loop = the speed targets; optimize highest-impact first, verify each (slice drops + scene-load still advances, no divergence), measure scene-load-time effect. NB workers alternate execute/wait → aggregate EXEC portion across all active workers (single-instant snapshots are noisy). COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-20 · ★★WORKER PROFILE → SPEED WORK REFRAMED: scene-load is SYNC/WAIT-BOUND, NOT worker-CPU-bound (3rd measurement correction this session). workerprof sample (post-UnloadTime): workers (AssetGC/Job) **95% blocked in NtWaitForSingleObject→server_select→wait_select_reply** (42350) on SYNC OBJECTS (jobs/events) via wineserver — NOT NtReadFile/asset-IO; main thread 76% WaitForSingleObject (on workers); dxmt threads on NtDelayExecution (the Sleep(1) lost-wake-fix polls, 15051). ZERO threads actively executing >30% at the sample instant; actual exec (~1.3M blocks/90s via heartbeat) is BURSTY+small vs the 95% wait. ⇒ everyone ~95% WAITING in a circular hand-off (main↔workers↔jobs), tiny work bursts. ★OPTIMIZING WORKER EXECUTION (JIT/interp/dispatch) WON'T HELP — workers barely execute, they WAIT. ★THE GATE = SYNC/WAKE LATENCY: each job hand-off = a wineserver round-trip (WaitForSingleObject/event-signal); scene-load = a long CHAIN of hand-offs → total ≈ many hand-offs × slow-round-trip-each = the emulation cost of wine SYNC PRIMITIVES under HB (ties to the lost-wake fragility patched earlier [DXMT std::atomic] + the Sleep(1) polls that ADD 1ms latency). NEW SPEED LEVER (reframed): the SYNC path — make WaitForSingleObject/event signal/wake FASTER under emulation (reduce wineserver round-trips / faster in-process sync for hot objects), NOT worker execution. NEXT: confirm sync-latency (measure sync-op rate + per-op latency) then target the hot-object sync path. dfc2148+51605cd+2b9fbe8 banked. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-20 · SYNC-LATENCY METER (operator GO, measure before fast-path). Added macrunner_hb_syncmeter_wfso (macrunner_hb.c ~349): wraps the guest WaitForSingleObject NtWaitForSingleObject round-trip, accumulates count+blocked-us, reports every 1s a single line "macrunner-hb-syncmeter: WaitForSingleObject ops_total/window_ops/rate=/s/avg_latency_us/window_blocked_ms" (env MACRUNNER_HB_TRACE_SYNCMETER, no per-op spam). Wired at the dominant sync op (42350 thread-time in the sample). Will tell: round-trip-OVERHEAD-bound (high rate, LOW avg_latency_us = μs) vs wait-DURATION-bound (LOW rate, HIGH avg = ms = lost-wake/slow-producer/Sleep-poll-gated signaling). (b) Sleep(1)/NtDelayExecution (DXMT native polls) = estimate from sample ratio (15051 NtDelayExecution vs 42350 WFSO ≈ 26:74) for now — precise (b) needs DXMT instrumentation. Build incremental (ntdll.so). NEXT: run post-UnloadTime ~120s, scan syncmeter lines → ops/sec + avg latency + (a)-vs-(b) split + the ceiling. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-20/21 · ★★★SYNC-METER RESULT = 10ms WAKE QUANTUM (smoking gun). Post-UnloadTime scene-load, 150 windows: WaitForSingleObject rate ~178/s (max 180), avg per-op latency PINNED at ~9.95ms (9.89-10.15ms, near-zero variance), ~1.67 thread-sec blocked/wall-sec. The ~10ms pin = a textbook QUANTUM (not variable slow-producer). Main thread 76% WFSO = ~76 ops/s × 10ms = 760ms/s ≈ 76% (ties out). ⇒ WAIT-DURATION-bound, specifically a 10ms wake granularity: sync wakes are NOT prompt (μs), every WFSO takes ~10ms. ★IF wakes were prompt → main thread largely unblocked → potentially HUGE scene-load speedup (the hand-off chain is 10ms-quantized). ENHANCED METER (status+timeout) building+rerun to pin (a) guest 10ms-poll-timeout-that-times-out [Unity job polling] vs (b) INFINITE-timeout woken at 10ms quantum [wineserver/wake-path bug] — timed_out vs signaled + infinite_to vs finite_to-avg. Then pick the fix. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8. (syncmeter = uncommitted diagnostic in macrunner_hb.c.)
2026-06-21 · ★★★ROOT CAUSE of the 10ms wake quantum = msync (macOS fast-sync) is DISABLED. Enhanced syncmeter: post-UnloadTime, 100% SIGNALED (not timed_out), 100% INFINITE-timeout, ~10.17ms avg latency ⇒ (b) wake-path quantum — events ARE signaled but waiters wake ~10ms late. TRACED: NtWaitForSingleObject/MultipleObjects try inproc_wait() first (sync.c 2499/2539), which on macOS → msync_wait_objs (os_sync_wait_on_address/__ulock = prompt). BUT inproc_wait gates `if (inproc_device_fd < 0) return STATUS_NOT_IMPLEMENTED` (sync.c 987) → falls to slow server_wait (~10ms). inproc_device_fd set ONLY if wineserver advertises reply->inproc_device (server.c 1692-94), which requires do_msync(). do_msync() (msync.c 417) = `getenv("WINEMSYNC") && atoi(...)`. ★WINEMSYNC is NOT set in laneA-run-hk.sh / mr-run.sh / config/env.sh ⇒ msync OFF ⇒ ALL event waits take the 10ms server path. FIX = set WINEMSYNC=1 (near-zero code, potentially massive: main thread 76% blocked on these). CAVEAT: sync.c has `#if defined(__APPLE__) && defined(__x86_64__)` blocks (1076-1391) — msync ARM64 port may be incomplete → VERIFY-FIRST (does msync engage on aarch64 + correctness). TESTING WINEMSYNC=1 now: syncmeter avg_latency should drop 10ms→μs; scene-load must still advance (markers) + no hang/divergence. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · msync PARTIALLY engages — most waits still slow. WINEMSYNC=1 (via probe env) reaches client (do_msync=1) AND server advertises inproc_device (diag: inproc_device_fd=10/8 >=0, first get_inproc_sync OK). Syncmeter with WINEMSYNC=1: median STILL 9.95ms BUT min=2us ⇒ msync engages for SOME objects (2us prompt) but MOST waits still hit the 10ms server path. So "enable msync" alone does NOT fix the gate. FORK being measured (counting diag in inproc_wait: calls/fd_bail/gis_fail/msync_ok every 4000): (1) most calls FALL BACK [gis_fail high → Unity job events not msync-registered → registration fix] vs (2) most REACH msync_wait_objs but still 10ms [msync wait/wake itself slow on aarch64 → wait-backend fix]. Note: WINEMSYNC runs did NOT reach UnloadTime in 900s (vs baseline ~516s) — possibly slower/variance, watch correctness. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8. (syncmeter+msync-diag uncommitted in macrunner_hb.c/sync.c.)
2026-06-21 · msync wait FULLY engaged (100%) but signal→wake = ~10ms; localized to the SIGNAL side. inproc_wait split: calls=108000, fd_bail=0%, gis_fail=0%, msync_ok=100% — EVERY wait uses msync_wait_objs → msync_wait_single → ulock_wait(UL_COMPARE_AND_WAIT_SHARED, obj_shm, val, ns_timeleft=0=INFINITE) (msync.c 301-331). So NOT a poll timeout, NOT registration — the waiter blocks on __ulock_wait until __ulock_wake. ⇒ the ~10ms is the SIGNAL→WAKE latency. NtSetEvent (sync.c 1284) tries inproc_set_event (fast, ulock_wake via msync_set_event_obj) THEN falls to SERVER_START_REQ(event_op). inproc_set_event (sync.c 874) requires get_inproc_sync(handle, INPROC_SYNC_**EVENT**, ...) — a TYPE match (wait used UNKNOWN), so the SIGNAL can fall to the server even when the WAIT uses msync. LAST FORK (measuring NtSetEvent fast-vs-server split): (a) server-fallback dominant → signal is server-mediated → fix = make SetEvent use msync fast-set (resolve the type/registration so inproc_set_event succeeds); (b) fast(msync) dominant but still 10ms → ulock_wake IS prompt → the 10ms is macOS WAKE→SCHEDULE latency (timer-coalescing/QoS of the woken thread) → fix = thread QoS / wake mechanism. NB if FEW SetEvent calls → hot signal is ReleaseSemaphore instead → instrument that. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8. (syncmeter+msync-diag uncommitted instrumentation.)
2026-06-21 · SIGNAL is NOT SetEvent → semaphore/server-signaled; wake is bimodal (5us fast-path vs ~10ms blocking). NtSetEvent diag: ZERO calls at the hot rate (no 4000-report) → the ~160/s waiters are NOT woken by SetEvent. inproc_wait still 100% msync_ok (144000). syncmeter median 9.96ms, min 5us = BIMODAL: fast-path (already-signaled) returns 5us; BLOCKING waits (ulock_wait until wake) take ~10ms. ⇒ the ~10ms is the BLOCKING-wait WAKE latency. Hot signal = likely NtReleaseSemaphore (Unity job system) OR server-internal signaling, NOT SetEvent. TWO leading fix candidates (need 1 more targeted diag — log msync_type of blocking waits + instrument ReleaseSemaphore fast-vs-server): (a) the SIGNAL (ReleaseSemaphore/server) doesn't do a prompt client ulock_wake → server-mediated wake = 10ms → fix the signal path to msync-fast-wake; (b) ulock_wake IS prompt but the woken worker thread isn't SCHEDULED for ~10ms = macOS timer-coalescing/QoS of non-RT threads under load → fix = thread QoS/scheduling. DECISION POINT: deep msync-backend investment — surface to operator. The full verified chain: no-frame→slow-grind→sync-bound(95% wait)→10ms wake quantum→msync was OFF(WINEMSYNC unset)→enabled→msync wait 100% engaged→signal/wake still 10ms. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8. (all msync instrumentation uncommitted, env-gated.)
2026-06-21 · REFRAME: the 10ms is the SERVER-REGISTERED multiple-wait path, not msync_wait_single. inproc_wait=148000 msync_ok=100%, but msync_wait_SINGLE ulock_wait BLOCK=0 → the waits do NOT block in single. get_inproc_sync CACHES (sync.c 774) → not the cost. ROOT: the syncmeter waits are ALERTABLE WaitForSingleObjectEx → alert_obj set → single_wait=0 (msync_wait_objs 819-827) → msync_wait_MULTIPLE (sync.c 939). msync_wait_multiple (msync.c 366): server_register_wait (mach_msg to wineserver, 378) → blocks on ulock_wait(addr=shm_tid_map+tid, 411) → woken when the WINESERVER writes the tid slot + ulock_wake (signal_all). ⇒ the wake is SERVER-MEDIATED (the wineserver wakes the waiter when an object signals) = ~10ms = option (a). Confirming with a MULTIPLE-BLOCK counter. Remaining 10ms WHY (after path confirmed): (a1) the wineserver's signal→wake latency (poll/timer/batch granularity), OR (b) macOS timer-coalescing/QoS of the ulock_wake-woken thread (10ms is a classic coalescing value, affects any ulock wake). NOTE the SIGNAL isn't SetEvent OR ReleaseSemaphore (both diag=0) → objects signaled by the WINESERVER internally (server-side state). COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8. (all msync instrumentation uncommitted, env-gated.)
2026-06-21 · ★★★MAJOR REFRAME — msync is NOT the latency fix; the 10ms is a SERVER-SIDE SIGNAL CADENCE. CONFIRMED path: alertable WaitForSingleObjectEx → msync_wait_multiple (server-registered wait, MULTIPLE-BLOCK count=100000, single BLOCK=0) → woken by the wineserver's __ulock_wake (server/msync.c 347/417 = DIRECT shm wake = PROMPT, not delayed). ★KEY: enabling msync did NOT lower the median (still ~10ms, SAME as pre-msync server_wait) ⇒ the 10ms is NOT the wait primitive (server vs msync) — BOTH wait for the wineserver to SIGNAL the object, which arrives on a ~10ms cadence. The wake is prompt; the SIGNAL is what's 10ms-late. Consistent ~10ms (tiny variance) = a TIMER, not jitter. ★LEADING HYPOTHESIS: the waited objects are signaled on a ~10ms cadence — most likely TIMER RESOLUTION (wine ~10ms/15.6ms vs the guest's timeBeginPeriod(1)=1ms expectation → all timed/paced ops 10ms-quantized), OR a 10ms-paced producer (Sleep/timer loop; ties to the dxmt NtDelayExecution Sleep(1) + the earlier Sleep-poll workarounds). ~10ms = classic Windows default timer tick. SIGNAL is NOT SetEvent/ReleaseSemaphore (both diag=0) → server-internal signaling (timer/message-queue?). ★FIX REFRAME: target the SIGNAL cadence / timer resolution, NOT the msync wake. ★WINEMSYNC: engaging it gave NO latency win (10ms is upstream) + correctness risk → RECOMMEND HOLD as lane default until the signal cadence is fixed (honest pushback on "make it default"). NEXT: identify what signals these objects + its cadence (timer-resolution check: NtSetTimerResolution / timeBeginPeriod honored? wineserver select-loop timeout granularity?). COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8. (all msync/syncmeter instrumentation uncommitted, env-gated.)
2026-06-21 · CADENCE METER (operator step 1): instrumented the wineserver signal_all_internal (server/msync.c 350 — the multiple-waiter wake trigger, woken via wake_tid→__ulock_wake, which is PROMPT) to log inter-signal avg/min gap every 2000 signals (env-gated, WINEMSYNC=1 active). Wake path traced: signal_all_internal→wake_tid (shm_tid_map[tid]=0 + __ulock_wake) = prompt; signal_all (412) = direct ulock_wake for single-waiters. Timer-resolution checked (operator step 2): wine NtSetTimerResolution is a SEMI-STUB — always claims 1ms (*current_res=10000=1ms), "no support for anything other than 1ms", doesn't track requests → guest timeBeginPeriod(1) is nominally satisfied (claimed). user_shared_data_timeout=16ms (server tick). NtDelayExecution: alertable→server_wait(timeout); non-alertable→select() loop. INTERPRETATION of cadence result: min_gap≈10ms (never closer) → a strict ~10ms TICK gates signals = server-side TIMER (a1) → hunt the timer. min_gap≈μs (bursty) → producer/coalescing-paced (b) → macOS timer-coalescing/QoS → QoS fix. Built+installed wineserver with the meter. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · ★★★ROOT CAUSE = macOS WAKE-TO-SCHEDULE latency (option b), NOT a server timer. Cadence meter (file-routed; wineserver stderr isn't in run.log) on signal_all_internal: 108000 calls, avg_gap ~6ms, **min_gap=1us** ⇒ signals are BURSTY/PROMPT, NOT a 10ms tick. The wake (signal_all_internal→wake_tid→__ulock_wake) is PROMPT. Yet each WaitForSingleObject = ~10ms (syncmeter). ⇒ the 10ms is AFTER the wake: the __ulock_wake'd thread is runnable promptly but macOS doesn't SCHEDULE it for ~10ms = TIMER-COALESCING/QoS of the woken thread (system only ~35% CPU → not contention). ★(a1 server-timer REFUTED; b CONFIRMED.) ~10ms = the default-QoS scheduling/coalescing window. ★FIX = make the woken wine threads schedule promptly: raise thread QoS to USER_INTERACTIVE and/or disable timer coalescing for the process. Quick zero-code confirm = `sudo sysctl -w kern.timer.coalescing_enabled=0` (system-wide, temporary) → if 10ms drops, b nailed. Code fix = pthread_set_qos_class_self_np(QOS_CLASS_USER_INTERACTIVE,0) at wine thread init (+ wineserver). VERIFY: latency 10ms→µs AND scene-load advances (re-run validation → does it COMPLETE → first frame?). COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8. (all instrumentation env-gated/uncommitted.)
2026-06-21 · QoS FIX-EXPERIMENT for (b): added env-gated pthread_set_qos_class_self_np(QOS_CLASS_USER_INTERACTIVE=0x21) in start_thread (thread.c 1142, per-worker-thread) — MACRUNNER_HB_HIQOS=1. Hypothesis: woken (__ulock_wake'd) threads at default QoS aren't scheduled for ~10ms (coalescing); USER_INTERACTIVE → prompt schedule. Test: WINEMSYNC=1 + HIQOS=1 + SYNCMETER. EXPECT: syncmeter avg_latency (mean) drops from 10ms — workers are ~55% of waits, so even worker-only QoS should drop the mean to ~4-5ms (clear signal). If it drops → (b) confirmed + QoS helps → extend to MAIN thread (76% WFSO, not via start_thread) + re-run validation (does scene-load COMPLETE → first frame?). If no drop → reconsider (main dominates / wineserver-side QoS / not the lever). COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · QoS experiment (HIQOS on start_thread workers) = NO latency drop (still ~10ms). Either (1) QoS doesn't fix the wake-to-schedule (b weakened — __ulock_wake is an EXPLICIT wake, not a timer, so coalescing may not even apply → woken thread should run promptly on a 35%-CPU system), OR (2) COVERAGE MISS: the guest x64 waiter threads run via macrunner_hb_x64_thread_entry (HB path) + the MAIN thread (76% WFSO) does NOT go through start_thread → my QoS injection missed them. ★DEFINITIVE coverage-independent test = `sudo sysctl -w kern.timer.coalescing_enabled=0` (system-wide) → re-run → if 10ms drops, (b) CONFIRMED regardless of which threads; if not, (b) REFUTED and the 10ms is genuine PRODUCER CADENCE (a real ~10ms-paced step in the guest scene-load, NOT a sync/schedule bug — would need the guest-side pacing identified). Needs operator sudo (can't run non-interactively). Revert after: coalescing_enabled=1. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · ★DECISIVE TEST: operator disabled kern.timer.coalescing_enabled=0. Re-measuring scene-load WaitForSingleObject latency (WINEMSYNC=1 + syncmeter, NO HIQOS = clean baseline vs the ~10ms). VERDICT MAP: avg_latency→µs = (b) CONFIRMED, macOS timer-coalescing of the woken thread was the 10ms gate → in-process fix = QoS time-constraint on the ACTUAL waiters (main thread + macrunner_hb_x64_thread_entry, which the start_thread-only QoS MISSED) + process coalescing opt-out, verify drops WITHOUT the global sysctl → re-run validation (FIRST FRAME?). avg_latency stays ~10ms = (b) REFUTED → genuine guest pacing (a Sleep/timer/frame-pacing wait in the scene-load loop) → characterize the pacing source + reducibility. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · ★★★VERDICT: (b) REFUTED — the 10ms is GENUINE GUEST PACING, not a wine/macOS bug. Coalescing OFF (kern.timer.coalescing_enabled=0, operator-set): WaitForSingleObject latency STAYED 9.52ms (range 9.48-9.61ms, ±0.06ms = razor-tight = a CLOCK). macOS coalescing ruled out. The ENTIRE engine/OS-side chain now ruled out by measurement: msync wait path (works, 100%), wineserver wake (prompt, __ulock_wake), thread QoS (no effect), timer coalescing (no effect). ⇒ each waiter's object is signaled ~9.5ms after it waits = a ~9.5ms-paced PRODUCER in the guest scene-load (everyone waits → server-signaled → very precise period = a guest TIMER, ~105Hz, or a frame-pace). NOT a fixable engine bug — it's the GAME pacing a loading step at ~9.5ms; scene-load = N steps × 9.5ms = slow. ★REFRAME of the whole 13-cycle descent: the sync/wake/schedule path is HEALTHY; the slowness is guest-side pacing. NEXT (characterize): is it a guest waitable/multimedia TIMER (~10ms period via the wineserver timer queue), a Sleep-yield in the loading loop, or frame-pacing? + reducible? (instrument wineserver set_timer period / NtDelayExecution durations / identify the producer). MAJOR DECISION POINT — guest-pacing is a different (guest-side) investigation than the engine bug we'd been chasing. ★msync/QoS/coalescing instrumentation = all env-gated, OFF by default, can stay or revert. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · ★CHARACTERIZED the guest pacing = ~5-10ms ONE-SHOT TIMERS (timed-wait/waitable-timer loop). timer_callback fired 156000+ times, ALL period_ms=0 (one-shot, re-armed), inter-fire gaps ~3-10ms (avg ~6ms). ~1:1 with the wait count ⇒ each ~9.5ms INFINITE WaitForSingleObject is gated by a guest-armed ~5-10ms one-shot timer = a FRAME-PACED/yield-based loading loop. The guest paces its scene-load to ~10ms/iteration (waitable timer / timed wait). CONFIRMS guest-pacing (not engine bug). ★REDUCIBILITY (operator's question): POSSIBLE but risky lever — clamp the wineserver one-shot timer due-times (server/timer.c, env-gated) to a small value during loading → IF loading is timer-gated (not work-gated), iterates faster → scene-load speeds up. RISK: changes timing-sensitive guest logic (could break game/physics). TESTABLE experiment. Caveat: the 4h run reached UnloadTime but not the next marker — pacing explains SLOWNESS but maybe not NON-COMPLETION (a separate stuck step / the win32u faults may also gate). ★FINAL REFRAME: the engine sync/wake/schedule is HEALTHY (msync/QoS/coalescing all ruled out); scene-load is gated by GUEST PACING (~10ms timed-wait loop) + raw emulation speed. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8. (all instrumentation env-gated/off.)
2026-06-21 · ★REDUCIBILITY TEST (operator GO): clamp guest one-shot waitable-timer due-times ~5-10ms→1ms (server/timer.c set_timer, env MACRUNNER_HB_TIMER_CLAMP=1, band 1-50ms one-shots only, reversible/off-default). Run: TMO=1800, TIMER_CLAMP=1 + WINEMSYNC=1 + SYNCMETER + WINEMETAL_TRACE_HWND + window-watcher; watches present (CAMetalLayer/presentDrawable) + scene markers PAST UnloadTime (PlayerLoop/Awake/Menu/...) + syncmeter latency. TWO OUTCOMES: (1) advances past UnloadTime / PRESENTS → pace WAS the gate → ★FIRST VISIBLE FRAME (render path x64-Unity→ARM64→DXMT→Metal proven E2E; HONEST: timing-ACCELERATED hack, not correct-speed — proves render path, correct-speed=raw-throughput follow-on). (2) latency drops (10ms→~1ms) but STILL stalls at UnloadTime → pace NOT the whole story → SEPARATE work-gated/stuck step = the real blocker → name it. Clamp = DIAGNOSTIC, REVERT after, never default. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · CLAMP test #1 (force 1-50ms→1ms): latency DROPPED 10ms→1.01ms (★PROVES the 10ms IS the guest waitable-timer pacing) BUT scene-load REGRESSED — UnloadTime NEVER reached in 31min (baseline ~8.7min), no present. The flatten-to-1ms DESTROYED relative timer ordering → broke timing-sensitive boot logic (ambiguous: pace-not-the-gate vs clamp-over-broke). CLAMP test #2 (gentler, ordering-PRESERVING: scale due-times by 1/4, floor 1ms) in flight to disambiguate: advances past UnloadTime/presents → pace WAS the gate (test#1 over-broke) → FIRST FRAME; still regresses/stalls → pace is timing-ESSENTIAL / not the gate → real blocker = work-gated (raw emulation throughput on the loading computation), not the wait. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · ★★★FINAL VERDICT (clamp test #2, gentler 1/4 ordering-preserving): the PACE is NOT the scene-load gate. latency dropped 10ms→2.39ms (4x, clamp engaged), boot reached UnloadTime NORMALLY at t=615s (NO regression — ordering preserved didn't break timing), but scene-load STILL did NOT progress past UnloadTime in the following 21min (present=false). ⇒ OUTCOME (2) CLEAN: reducing the pacing 4x without breaking the boot does NOT advance scene-load → the wait was a SYMPTOM; the real blocker is WORK-GATED (the emulated loading COMPUTATION past UnloadTime), not the wait/sync/pace. ★★FULL-CIRCLE CONCLUSION: (1) engine sync/wake/schedule is HEALTHY (msync works, wake prompt, QoS+coalescing no effect — all ruled out by measurement); (2) the ~10ms = guest waitable-timer pacing, NOT the gate (4x faster = no scene-load gain); (3) ★the REAL first-frame gate = RAW EMULATION THROUGHPUT on the loading WORK (the JIT/execution of the game's scene-load computation past UnloadTime). This is the broad engine-maturity throughput program (faster x86→ARM64 execution of game code), NOT a sync/pace/single-bug fix. CLAMP REVERTED (set_timer clean, wineserver rebuilt); read-only env-gated meters (syncmeter/cadence/timer-fire) left inert. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · THROUGHPUT PROGRAM (correctly targeted at the WORK). #3 _initterm clamp: CONFIRMED already in my dist — macrunner_hb.c 10928-10935 has the FIXED cap (0x400000, comment documents the old 0x10000/8192-entry cap that skipped large C++ static-init tables); I've rebuilt ntdll.so from this source many times → every recent run had the fix → it's NOT the missing piece (boot reaches UnloadTime + stalls on work WITH the fix). #1 loading-WORK profile in flight: run HK to UnloadTime, settle 30s into loading, take a 30s full sample (captures bursty execution), aggregate the EXECUTION subtree (run_x64→exec_instr/hb_jit_runtime_run/lift/codegen + callees, EXCLUDING wait leaves) across all threads → bucket JIT-translation/interpreter/dispatch/memory → top hot spots in the loading-work execution = the throughput targets. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · ★LOADING-WORK PROFILE (30s sample past UnloadTime, execution-subtree across all threads): the x86→ARM64 loading WORK splits ~ run_x64 dispatch loop 27% (core JIT, hard) + ★MEMORY ACCESS via mach_vm ~22-28% (special_read 4.6 + hb_memory_read 5.3 + find_region_normalized 4.4 + mach_vm_region 3.4 + mach_vm_read_overwrite 2.0 + hb_memory_write 2.3 + mach_msg2 6.0) + context/classify (fill_amd64+update_ctx+is_registered) 7% + JIT exec (hb_jit_runtime_run+helpers) 7% + interpreter (exec_instr) 4%. ★TOP OPTIMIZABLE = the guest-memory path: special_read/special_write do mach_vm_region + mach_vm_read_overwrite PER guest access (~22-28%). ★LEVER: MACRUNNER_HB_JIT_DIRECT_MEM is OFF by default (mr-run.sh :-0) → reads route through the slow Mach-syscall path. Testing DIRECT_MEM=1 (avoid mach_vm via direct guest-memory access) — env experiment, verify memory-slice drops + scene-load advances + no crash. Code-level fallback = special_read persistent region-cache + direct memcpy. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · DIRECT_MEM=1 THROUGHPUT EXPERIMENT (target = the ~22-28% mach_vm memory-access slice). Run HK with MACRUNNER_HB_JIT_DIRECT_MEM=1 (default off), heartbeat, reach UnloadTime + 30s sample. CHECK: (1) correctness — does boot reach UnloadTime, no crash/divergence? (DIRECT_MEM off-by-default may = stability reason); (2) speed — does the memory/mach_vm slice DROP in the sample (vs 22-28%)?; (3) impact — faster to UnloadTime (vs baseline ~540s)? If works+drops+faster → the mach_vm path IS the lever + DIRECT_MEM is the (env) fix. If crashes → DIRECT_MEM unstable → code-level special_read fast-path (persistent region-cache + direct memcpy) instead. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · DIRECT_MEM=1 = BROKEN (correctness) → env lever NOT usable. Boot NEVER reached input-init (TMO 960s vs baseline UnloadTime ~540s): c000007b runtime crash early (~DXGI/graphics init), garbage state (run-exit-indirect r15=0x24, q0/q8=MEMORY_FAULT) + a fail-bt through Hollow Knight.exe + UnityPlayer.dll. = direct guest-memory access reads WRONG → corrupted state → crash (the stability reason DIRECT_MEM is off-by-default). ⇒ the ~22-28% mach_vm memory path (the profile's #1 target) needs a CODE-LEVEL fix, NOT the env. Memory breakdown: region LOOKUP (find_region_normalized 4.4 + mach_vm_region 3.4 = ~7.8%) + actual Mach read (mach_vm_read_overwrite 2.0 + mach_msg2 6.0) + hb_memory dispatch (read 5.3 + write 2.3) + special_read entry 4.6. ★FIX PATH = special_read/special_write optimization: (a) PERSIST the region-lookup cache across callbacks (earlier attempt didn't engage = cache fresh per callback → find_region missed → cut the ~7.8% re-lookup) + (b) optional direct-memcpy ONLY for validated-mapped-readable regions (riskier; DIRECT_MEM shows naive direct-access crashes). Non-trivial + correctness-sensitive. DECISION POINT: invest in the special_read code fast-path (the #1 profile-confirmed throughput target) vs continue profiling. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · GUEST-SIDE hot-spot profile (operator ask: WHICH Unity function dominates the loading work, like the 0x17b2bb0/FMOD find — to coordinate with ABZU's same-class throughput grind). Re-running workprof (heartbeat ON, no DIRECT_MEM) to past UnloadTime; will aggregate heartbeat block_pcs past UnloadTime → resolve top hot guest PCs to module+rva (via pe-relocate/module-load lines) → name the hot Unity loading function. Complements the HB-side breakdown (mach_vm memory ~28% + dispatch 27%): the guest-side tells WHICH guest code generates that memory-heavy work → shared throughput target with ABZU. _initterm fix already in dist (confirmed). COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · GUEST-SIDE hot-spot profile (heartbeat, post-input-init grind, 335K samples): the dominant concentrated hot loop 0x87efdcc2bb0-c7c (9 PCs, ~38-50%) = the FMOD/audio sw-mixer SPIN (base 0x87efc510000 + rva 0x17b2bb0 — the EXACT rva refuted earlier; busy-loop, no audio device; heartbeat OVER-weights it = tight loop = many block-transitions/sec, but it's a background red herring that does NOT gate scene-load — throttle test earlier didn't move the stall). ★The REAL loading work (0x87ef1 range, ~45%) is BROAD — spread across many functions, NO single dominant hot guest spot (top non-spin PCs <1% each = Mono JIT + asset processing + scene init, many functions). ⇒ unlike FMOD, there is NO single guest function to optimize; the loading work is cross-cutting. ★THEREFORE the throughput lever = the HB-SIDE emulation overhead that's cross-cutting across the broad work: mach_vm MEMORY path (~22-28%) + dispatch loop (27%). RECONFIRMS the mach_vm memory path as the #1 shared throughput target (benefits ALL the broad loading work) — the target to coordinate with ABZU (same class). FMOD = top guest-CPU consumer but a known red herring (secondary: a FULL neutralization retest only if mach_vm doesn't pan out). COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · ★IMPLEMENTED special_read/write REGION CACHE (the #1 throughput lever). macrunner_hb.c: per-thread region cache (16 entries, round-robin) caching mach_vm_region bounds+prot; special_read/special_write check the cache before mach_vm_region (skip the syscall on hit; mach_vm_read_overwrite still does the real read = faults if region truly gone). CORRECTNESS via global generation counter macrunner_hb_vm_changed() (atomic inc) — on lookup, thread's cached gen != global → drop whole per-thread cache (stale region from free/remap/reprotect can NEVER be used). INVALIDATION HOOKS in virtual.c (already macrunner-aware): macrunner_hb_vm_changed() at success-return of NtAllocateVirtualMemory (6622) + NtAllocateVirtualMemoryEx (6765) + NtFreeVirtualMemory (free) + NtProtectVirtualMemory (protect) = the complete VM-change choke point (guest imports AND direct syscalls route through these). special_write hit gated on VM_PROT_WRITE. Env-gated MACRUNNER_HB_REGION_CACHE (default OFF) for safe A/B + rollout. Build clean. VERIFY run (REGION_CACHE=1, profile past UnloadTime): (a) speed = mach_vm_region/find_region slice drops; (b) correctness = reaches UnloadTime, no crash/divergence. Ship only if both hold. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · REGION CACHE — correctness run #1 (REGION_CACHE=1): boot progressed NORMALLY (DXGI→Mono→PhysX→input-init→grind 629K heartbeat lines), NO read-region/read-gap/write-region fault tags (= no stale/bad reads from the cache), only the KNOWN intermittent c000007b (baseline too) + normal early x18 callback-exceptions ⇒ correctness CLEAN so far (no cache-induced corruption). UnloadTime not reached = boot variance (2nd no-cache workprof didn't either). Added a direct hit-rate counter (macrunner_hb_rc_report: hits/miss/hit_rate/invalidations every 200K lookups, env SYNCMETER) for a clean SPEED measure (sampling unreliable w/ variance). Run #2 (REGION_CACHE=1 + SYNCMETER) in flight → hit_rate (how often mach_vm_region is skipped) + invalidation rate (VM-change churn). High hit_rate = the ~7.8% lever realized. COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · ★★★REGION CACHE VERIFIED — both axes pass. Run #2 (REGION_CACHE=1 + counter): hit_rate=100.0% (74,796,938 hits / 3,062 miss), invalidations=409, special-vm-FAULT tags=0, UnloadTime REACHED (L513478, boot advances normally). ⇒ SPEED: mach_vm_region (+find_region + the mach mechanism the hit also skips) ESSENTIALLY ELIMINATED = the ~7.8-14% mach_vm slice of the loading WORK realized at 100% hit. ⇒ CORRECTNESS: 0 fault tags across the full run (Mono/Unity hammer the allocator → staleness would surface fast), boot reaches UnloadTime, invalidation fired 409× under REAL VM churn with NO corruption = the generation/invalidation works under load. SHIP-WORTHY (the successor to the ~39% dfc2148/51605cd/2b9fbe8 wins). Implementation: per-thread 16-entry region cache (macrunner_hb.c) + generation-counter invalidation hooked at NtAllocate/AllocateEx/Free/Protect success-returns (virtual.c). Env MACRUNNER_HB_REGION_CACHE (currently default OFF). DECISION: flip default-ON (ship) now vs longer correctness soak first. Distinct from ABZU's _initterm runtime-reuse lever (no dup). COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8 (region cache uncommitted, pending ship decision).
2026-06-21 · ★SHIPPED the region cache (default-ON, kill-switch MACRUNNER_HB_REGION_CACHE=0). macrunner_hb_region_cache_enabled() flipped to default-on (verified 100% hit / 0 stale reads / invalidation works under churn). Default-on SOAK in flight (no REGION_CACHE env → tests the new default; SYNCMETER for the hit report) → confirm: rcache report fires (default-on works) + 0 fault tags (correctness) + boot reaches input-init/UnloadTime. The win is the successor to dfc2148/51605cd/2b9fbe8 (~39%): eliminates the ~7.8-14% mach_vm_region slice of the loading WORK. Files: macrunner_hb.c (cache + gen counter), virtual.c (4 invalidation hooks). Uncommitted (commit = operator's call). Distinct from ABZU's _initterm runtime-reuse (no dup; cross-pollinate; both shared-core HB throughput). COMMITS(HEAD): dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · ★COMMITTED region cache: 3eeefc6 "feat(hb): special_read/write mach_vm region cache (default-on, generation-invalidated)" (macrunner_hb.c + virtual.c, 322 ins/41 del, on main). Banked + reversible (kill-switch MACRUNNER_HB_REGION_CACHE=0). NOW (parallel): (2) correctness SOAK + (3) NEXT dominator = ~27% dispatch loop (run_x64), shared with ABZU. Combined run (TMO=1200, cache default-ON, sample at input-init+90s + continue to TMO): soak = 0 fault tags over 20min (extra staleness assurance); dispatch profile = run_x64 self-time + immediate-callee breakdown → the hot per-block pattern to optimize. Coordinate with ABZU: runtime-pool (theirs) + region-cache (3eeefc6) + dispatch-loop = complementary shared-core HB throughput levers; cross-pollinate + upstream together. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · ★SOAK PASS (correctness): region cache default-ON, 20min run, 0 special-vm-fault tags + 0 crash-sigs (c0000005/SIGSEGV/SIGBUS/Assertion) over 705K lines = strong staleness assurance under sustained real VM churn. ★DISPATCH-LOOP PROFILE (#3, clean wait-exclusion): the run_x64 subtree is 97% WAITS at this phase (input-init+90s, heavily paced). The EXECUTION dispatch breakdown: run_x64 SELF/loop-body 39.7% (core, already optimized 51605cd/2b9fbe8), hb_jit_runtime_run 29.4% (REAL guest code, not overhead), call_import_thunk 11.4% (host/import dispatch, non-wait), pc-classify+ctx-update 10.6% (already cached/trylocked, residual), now_us+fprintf 3.4% (heartbeat instrumentation, NOT a production cost). ★HONEST REFRAME: the "~27% dispatch loop" is 27% of EXECUTION, but execution is only ~3-25% of wall-time (wait-bound) → the dispatch loop is a SMALL wall-clock lever (~1-4%), DIMINISHING RETURNS (core loop already optimized; the rest is real guest code + instrumentation). The REGION CACHE (3eeefc6, the mach_vm memory slice) was the higher-value win. Next concrete dispatch sub-target = call_import_thunk per-call overhead (11.4%) but small impact. ⇒ scene-load is largely wait/pace-bound (guest's own ~10ms timer pacing); work optimizations (region cache) speed the work-part but the pacing is the guest's design. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · ★WARM-CACHE A/B (operator: ABZU's was DECISIVE — proved JIT throughput NOT the gate, lift cacheable, warm IDLES at the wall). HK's persistent translation cache (translation-cache.bin) = 16 bytes = bare header, NEVER populated despite many runs this session (each ntdll rebuild keys-invalidates it → every run COLD; + killed-at-timeout). A/B: cold #1 (does the file GROW = lift cacheable?) → warm #2 (same binary, same cache). Measures: (1) cache grows after #1 + warm #2 adds ~0 (lift cacheable); (2) input-init cold-vs-warm (warm faster = the 13,777-callback _initterm + boot lift cached); (3) furthest marker cold-vs-warm (warm reaches further toward present?); (4) late CPU on warm (IDLE = throughput NOT the gate, like ABZU → gate is wait/pace-bound or a block like ABZU's r14/0x58576f; BUSY = lift still mattered). VERDICT MAP: warm advances→present = throughput was a gate, present path de-risked → FIRST FRAME; warm idles same wall = throughput NOT my gate → stop optimizing throughput, find the real block. Don't grind the ~1-4% dispatch loop. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · ★★★WARM-CACHE A/B VERDICT (matches ABZU): THROUGHPUT IS NOT HK'S GATE. cache stayed 16 bytes (NEVER populated, +0 both runs) → (a) translation cache is BROKEN/non-writing (secondary bug: helps cold-start, NOT the frame) → couldn't produce a true warm run; (b) BOTH runs IDLE at ~27% CPU at the scene-load wall (~UnloadTime, present=0) — independent of the cache. ~27% CPU = WAIT-bound, NOT CPU-bound (spare CPU, process waiting not grinding). ⇒ throughput is NOT the gate (region cache 3eeefc6 banked, but no throughput work reaches the frame); the gate = wait/pace-bound loading or a BLOCK (HK's analog of ABZU's r14/0x58576f race). ★REDIRECT: STOP throughput optimization. FIND THE REAL BLOCK — investigate the IDLE-at-the-wall state: what is the scene-load main thread WAITING for that never resolves? (clamp test already ruled the 10ms pace out as the gate → it's a block: a lost-wake/dead-producer/missing-event, OR the c000007b intermittent corruption killing a needed thread). Earlier deadlock-probe was inconclusive (no worker missing) — re-investigate rigorously now that throughput is ruled out. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · FIND THE BLOCK (operator: lead with (1) idle-state wait analysis). Added a WAIT REGISTRY (macrunner_hb.c, env MACRUNNER_HB_TRACE_BLOCK): each thread records its current WaitForSingleObject (handle+caller+start) before the wait, clears after; other threads' frequent 10ms-paced returns scan for slots pending >3s = the BLOCK (the never-returning wait the syncmeter can't see, since it logs only after return) → logs tid/handle/caller/pending_ms. Run to the idle wall with BLOCK=1. NEXT: scan macrunner-hb-BLOCK lines = WHAT the scene-load threads block on (the wait target handle+caller); + cross-reference the c000007b (run-exit) thread-death — does the killed thread own the work main waits on? If block = c000007b killing the producer → fix the c000007b corruption-origin (priority) + cross-check ABZU's r14/0x58576f (shared race/visibility root?). Region cache 3eeefc6 stays; throughput ruled out. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · ★★★BLOCK FOUND = PRODUCER-DEATH DEADLOCK via the c000007b. Wait-registry (MACRUNNER_HB_TRACE_BLOCK) at the wall: 7 WORKER threads block the WHOLE run (889s) on consecutive job-system event handles 0x6c/0x78/0x84/0x90/0x9c/0xa8/0xb4 (one wait call-site caller=0x87efca87c92 = a thread POOL) + 1 COORDINATOR thread on handle 0xf0 (caller=0x87ef0ffccba) = CONSUMERS waiting for work that never comes. THIS run's c000007b killed a thread executing UnityPlayer LOADING code (last_block=0x87ef11f6e79, r15=0xea5873a20 garbage) = the PRODUCER. ⇒ c000007b kills the producer mid-task → the consumers' events never signal → permanent deadlock at the scene-load wall (the idle ~27% CPU = these threads parked forever). HK's analog of ABZU's r14/0x58576f. ★ROOT HYPOTHESIS (shared with ABZU): the c000007b = a garbage register loaded from memory ([rsi]/[r15]) → likely a MEMORY-ORDERING/TSO VISIBILITY bug (x86 strong TSO not fully enforced on ARM64 weak model → a load sees a not-yet-visible cross-thread write = garbage). Ties to recent 19431a1 (x86 TSO load lowering LDAR->LDR+DMB) — possibly incomplete. ★PRIORITY = c000007b corruption-origin. CROSS-CHECK ABZU's r14/0x58576f: if both are TSO-load/visibility corruptions → SHARED ROOT → ONE TSO fix covers both. Confirm causality: a no-c000007b run should NOT deadlock (producer survives). COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-21 · ORIGIN INVESTIGATION (#2, code-read while causality run #1 in flight). TSO hypothesis WEAKENED: with DIRECT_MEM=0 (the c000007b config), GPR loads → hb_jit_helper_exec_load_operand_lazy (codegen 4141), stores → hb_jit_helper_exec_store_operand_lazy. Those helpers DO apply real TSO fences: hb_jit_helper_acquire_after_operand_read = __atomic_thread_fence(ACQUIRE) after each load (4793), hb_jit_helper_release_before_operand_write = __atomic_thread_fence(RELEASE) before each store (4798). So the helper path HAS acquire/release ordering → the simple "missing TSO barrier" is largely NOT the gap. ★REMAINING ORIGIN CANDIDATES: (a) the fence is gated on hb_jit_operand_is_guest_mem(op) — a MISCLASSIFIED guest-mem operand (e.g., a specific addressing mode) would SKIP the fence = a TSO coverage gap; (b) REGISTER-PRESERVATION — r15/r14 are x86 callee-saved; if a guest reg in an ARM64 caller-saved reg isn't saved around a helper/import call → clobbered → garbage (but that'd be deterministic, not ~50% intermittent → less likely); (c) a genuine data-race/logic bug (producer writes garbage / consumer reads before any write). Intermittency (~50%) favors a RACE (a or c). NEXT: await causality (confirm c000007b⟺deadlock), then RUNTIME-trace where r15 first goes garbage (instrument upstream of the badtarget) to distinguish (a)/(b)/(c). COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-21/22 · ★CAUSALITY CONFIRMED (forward): c000007b ⟺ deadlock 3/3 runs (consumer-block 641-687s all). c000007b is NEAR-DETERMINISTIC (3/3, not ~50%) BUT garbage value varies (r15 differs/run) + fire location varies (0x17fX region earlier vs 0x87ef this run) = signature of a RACE the heavy loading ALWAYS hits (reads varying memory). No c000007b-absent run (definitive = fix-test). Race/TSO family stays alive despite barriers present → COVERAGE GAP (misclassified guest-mem operand skipping the fence) is the lead. Checking hb_jit_operand_is_guest_mem coverage. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · ★TSO HYPOTHESIS REFUTED (verify-first): hb_jit_operand_is_guest_mem(op) = (op->type==HB_OP_MEM) → the acquire fence fires on ALL loads + release on ALL stores (helper path, DIRECT_MEM off). TSO ordering is COMPLETE, no coverage gap. r15 (GPR) is loaded via the FENCED lazy helper → its garbage is NOT a missing-load-fence. ⇒ c000007b is NOT a TSO-barrier bug. Narrowed to: (b) REGISTER-PRESERVATION (r15/r14 are x86 callee-saved → clobbered across a helper/import call) — reliable-trigger(3/3) + varying-garbage(the clobber leftover) fits BETTER than a reordering race; OR (c) CONTEXT-CORRUPTION (a stray/mistranslated write to ctx->r15 upstream). Both → same measurement: WHERE r15 first goes garbage. NEXT: origin-trace = per-thread ring of recent (block_pc, r15) updated each block, dumped at the c000007b badtarget → localize the block where r15 transitions good→garbage → read that block's lowering. ABZU's 0x58576f (constructor-not-run) = likely DISTINCT from a register-clobber, but coordinate (if ABZU's branch is corruption-driven, shared). COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · ORIGIN-TRACE instrument built: per-thread r15 history ring (macrunner_hb.c, 16 entries of (block_pc, r15) updated each block in run_x64 at 20381, dumped at the c000007b badtarget; env MACRUNNER_HB_TRACE_BLOCK). Run to catch the c000007b + the macrunner-hb-r15ring dump → shows r15's value over the last 16 blocks before the garbage call → localizes the BLOCK where r15 transitions good→garbage. Then read that block's lowering: register-clobber (preservation bug) vs context-corruption vs the value source. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · TWO c000007b MODES found: (1) reason=non-application-target (r15=garbage → bad indirect call → the badtarget + deadlock cause, causality 3/3); (2) reason=runtime (this r15org run: pc=last_block=0x87efc7c5605, last_bytes=49 8b 84 f0... = mov rax,[r8+rsi*8+0x488]; cmp [rdx+8],rax; jae — and ★rbp=0 = a DIFFERENT register corrupted). ⇒ the corruption hits MULTIPLE registers (r15 in mode 1, rbp in mode 2) → points at CONTEXT/frame corruption, not r15-specific. My ring dump was gated on the badtarget (mode 1 only) → mode 2 this run → no dump. FIX: moved the r15-ring dump to the run-exit-indirect path (fires for BOTH modes). Re-running. NEXT: read the ring → where r15 (or the context) first goes garbage. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · r15org2 run: NO c000007b (didn't reach UnloadTime in 700s) → c000007b IS intermittent (needs to reach the loading phase where the producer runs+dies). Multi-attempt r15-catch probe (up to 4× TMO=800, BLOCK=1, early-stop on c000007b detection, ring dump now at run-exit-indirect = both modes) in flight → capture the r15 ring on the first c000007b → the corruption origin (the block where r15/context first goes garbage). COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · ★★★ORIGIN = UNINITIALIZED OBJECT VTABLE SLOT (constructor not run) = ABZU'S CLASS. r15-ring captured (attempt 1, reason=non-application-target): r15(OBJECT)=0x8a5875160 CONSISTENT across the hot loop (a real stable object, NOT random garbage); set at block 0x87ef11f67bf; loop 0x87ef0ffa↔0x87ef11f6; fault calling through it at 0x87ef11f6e79. The rejected target = [r15+0x10] (a vtable/fn-ptr slot) per the dispatch + the existing code comment (macrunner_hb.c 19946: "garbage fn-ptr loaded from [r15+0x10]... the UNINITIALIZED slot"). ⇒ NOT register-corruption, NOT TSO (refuted), NOT a clobber — a VALID OBJECT whose vtable/fn-ptr slot [r15+0x10] was NEVER INITIALIZED (the constructor/initializer that sets it didn't run) → call through the uninitialized slot → non-module ptr → rejected → thread dies → producer-death deadlock. ★MATCHES ABZU's 0x58576f (operator: "DETERMINISTIC producer-gap — a constructor not run"). SHARED ROOT → ONE FIX. Likely the static-init/_initterm path (the _initterm cap fix raised 0x10000→0x400000 is in dist, but may be incomplete OR a related init mechanism skips THIS constructor). ★COORDINATE WITH ABZU: they root-caused WHICH constructor + WHY it's skipped → apply the same fix to HK. dispatch rejection at macrunner_hb.c 19913-19979 (pc must be in a guest IMAGE or native builtin; an uninit-slot target = neither). COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · ★★★SHARED-ROOT CANDIDATE FOUND (engine-side): NON-ATOMIC lock-prefixed RMW. The HB lifter (hb_lift_x64.c 2174-2181) marks LOCK-prefixed ops is_locked; the codegen (hb_arm64_codegen.c 8071/8073/8367-8374) only DMB-WRAPS them (ordering) — and block_has_atomic_ir routes ONLY CMPXCHG/XCHG/XADD through the true-atomic helper (hb_jit_helper_exec_atomic_ir → __atomic_fetch_* on hb_jit_atomic_host_ptr). So `lock inc/dec/add/sub/and/or/xor [mem]` (which lift to is_locked HB_IR_ADD/SUB/AND/OR/XOR, NOT XADD) execute as DMB-wrapped NON-ATOMIC load-modify-store → CONCURRENT increments LOSE updates → UNDER-INCREMENTED counter = EXACTLY ABZU's relayed root (CS-protected counter under-incremented → singleton init skipped → cascade). A lost-increment → an init-guard/registration counter under-counts → the branch on it takes the SKIP path = ABZU's "early skipped registration / mistranslated branch" (the branch is correct; its INPUT is data-corrupted). Matches my uninit [r15+0x10] (constructor not run) too. ★VERIFY-FIRST FIX-TEST: make is_locked non-CMPXCHG/XADD/XCHG RMW truly atomic (serialize via split-lock OR __atomic_fetch_add/and/or/xor) → does the c000007b vanish? If yes → SHARED CASCADE ROOT (one fix, HK+ABZU). Note: InterlockedIncrement→lock xadd is already atomic; only lock inc/add/or/and (no-return-value forms) are broken — so the question is whether the counter uses inc/add vs xadd. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · ★FIX-TEST IN FLIGHT (verify-first, the cascade-root test). Implemented env-gated serialization of non-atomic lock-RMW: hb_arm64_codegen.c hb_arm64_codegen_instr now brackets is_locked non-(cmpxchg/xadd/xchg) RMW with hb_jit_split_lock_acquire/release when MACRUNNER_HB_LOCK_RMW_ATOMIC=1 (preserves lazy-flags-correct codegen_instr, just serialized → no lost updates). Built libhyperbridge.a (-Werror clean) + relinked ntdll + installed to dist. Running 3× HK with the env. VERDICT MAP: c000007b 0/3 (vs ~80% baseline) + scene-load advances past the wall (furthest>UnloadTime / present markers) → non-atomic lock-RMW WAS the cascade root → unblocks HK (+likely ABZU). Persists 3/3 → not the cause → control-flow divergence (ABZU differential). ★ABZU CROSS-CHECK (decisive, cheap): does the under-incremented counter's accessor (0x5844a0) increment via `lock inc`/`lock add` (no-return → MY non-atomic bug) or `lock xadd`/`lock cmpxchg` (Interlocked* → already atomic in the HB helper)? One disasm fact confirms/refutes shared root. NB I lack CrossOver tooling for the differential; the fix-test is the more-decisive engine-level verification. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8 (the env-gated fix is UNCOMMITTED, default-off).
2026-06-22 · ★FIX-TEST REFUTED: MACRUNNER_HB_LOCK_RMW_ATOMIC=1 did NOT cure HK's c000007b (2/3 fired ≈ baseline, deadlock 3/3, 0 present, NO advancement; + regressed boot — none reached UnloadTime, the global split-lock is slow). ⇒ the non-atomic lock-RMW is a REAL but SEPARATE bug, NOT HK's c000007b cause. Verify-first saved a wrong commit; env-gated change stays default-off (inert). ★OPEN DISTINCTION never verified: is the rejected target [r15+0x10] (a) GARBAGE/uninit slot (constructor-not-run = ABZU's class = control-flow divergence) or (b) a VALID Mono-JIT'd method on the executable HEAP that the dispatch OVER-REJECTS (dispatch at 19913 requires guest-IMAGE membership or native builtin; Mono code is heap RX, not a module → rejected)? Earlier dump hint: [r15+0]=0 (null vtable) leans (a)-uninit, but the rejected TARGET's executability was never checked. NEXT: instrument the badtarget to query mach_vm_region(ctx->pc=target) prot — RX→(b) Mono over-rejection (HK-specific dispatch fix: accept executable guest regions); non-exec/garbage→(a) constructor-not-run (shared w/ ABZU's cascade → ABZU's differential root applies). COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8 (atomic-RMW fix UNCOMMITTED, default-off, NOT the cure).
2026-06-22 · DISAMBIGUATION in flight: added macrunner-hb-badtarget-prot (mach_vm_region on the rejected target ctx->pc) to the badtarget. Retry-until-c000007b probe running (atomic env OFF = baseline). Verdict: target prot 'x'(executable) → valid Mono method OVER-REJECTED by the dispatch (HK-specific fix: accept executable guest regions, NOT shared with ABZU) | target '---'/'rw-'/kr-fail → garbage/uninit slot = constructor-not-run = ABZU's class (shared cascade root, ABZU's differential applies). COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · ★★★CONFIRMED HK SHARES ABZU'S CASCADE CLASS. badtarget-prot: rejected target=0x727c96100 prot=rw- (READ-WRITE DATA region, kr=0) = NOT executable → NOT a Mono method (over-rejection REFUTED) → GARBAGE/UNINIT slot. + [r15+0]=0 (null vtable) = the C++ constructor NEVER STARTED. ⇒ zeroed-but-allocated object, constructor SKIPPED = ABZU's class. HK shares the cascade root. NEXT (no CrossOver): does HK's _initterm run the FULL static-init table or exit early (cap may be incomplete)? COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · _INITTERM SKIP TRACE in flight: instrumented the _initterm shim (macrunner_hb.c 11160) to log skipped static-init callbacks — path1 "no-module"/"not-amd64" (macrunner_hb_module_from_pc fails → SILENT skip) + path2 "dispatch-fail" (callback run_x64 errored → skip+continue, non-_e) + a per-call summary (slots/run/skip counts). Env MACRUNNER_HB_TRACE_BLOCK. The skipped callbacks = the cascade producer-gaps (a skipped ctor = a null-vtable object = my c000007b). Reason tells WHY: no-module=module-detection gap (relocation/load-addr), dispatch-fail=nested c000007b (cascade). NEXT: scan macrunner-hb-initterm-skip / -summary → the FIRST skip = the root. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · ★_INITTERM RULED OUT: 0 initterm-skip + 0 summary (the shim runs the FULL static-init table, no path1/path2 skips) — yet c000007b still fired (badtarget:1, prot=rw- uninit confirmed 3rd time, target varies per run 0x17ffb43eb/0x727c96100/0x8a5875160 = ASLR heap). ⇒ the skipped constructor is NOT a static initializer → it's a RUNTIME LAZY/ONE-TIME-INIT (exactly ABZU's counter-gated singleton pattern, not _initterm). Narrows the root: an EARLY RUNTIME main-init control-flow divergence (ABZU's reframe) — likely an ENGINE flag/branch mistranslation making a registration loop/guard diverge → under-count → skipped lazy-init → uninit object → c000007b → producer dies → deadlock. Shared HK+ABZU. ★STATE: HK confirmed shares ABZU's cascade class; ruled out (this session) throughput/TSO/atomic-RMW/Mono-over-rejection/_initterm. The earliest runtime divergence = ABZU's CrossOver DIFFERENTIAL (I lack CrossOver tooling). PLAN: apply ABZU's differential-root fix to HK + fix-test (does c000007b vanish?); offer engine-side help if the divergence is a specific flag/branch/registration mechanism. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8 (all session instrumentation + atomic-RMW fix UNCOMMITTED + env-gated default-off = inert).
2026-06-22 · MAGIC-STATICS NOT SHIMMED: _Init_thread_header/footer/epoch absent from engine → the guest ucrtbase magic-static guard (function-local static / singleton one-time-init, epoch-counter-gated = ABZU's "CS-protected counter") runs as JIT'd GUEST CODE. ⇒ the divergence is a JIT MISTRANSLATION inside the runtime-init guard (a branch/flag/atomic on the epoch), NOT a shim. = ABZU's "early main-init control-flow divergence." ★MAXIMAL MacRunner-side narrowing reached: HK's c000007b = uninit singleton from a SKIPPED runtime lazy-init whose guard is JIT-mistranslated; shares ABZU's exact root. Ruled out (this session): throughput, TSO, atomic-RMW, Mono-over-rejection, _initterm static-init. The EXACT divergent block needs a reference trace — ABZU's CrossOver differential (Unicorn oracle can't do a full-boot differential). HANDOFF: ABZU pins the first divergent block; I apply + fix-test on HK. OPTION if operator wants me to keep driving: shim/trace _Init_thread_header/footer (intercept like _initterm) to log which magic-static guard SKIPS its constructor = HK's earliest skipped singleton, MacRunner-side. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · MAGIC-STATIC SKIP-TRACE built (operator: drive own angle, don't wait on ABZU). Instrumented _Init_thread_header/_Init_thread_footer in the CRT import handler (macrunner_hb.c after is_crt_runtime gate) — INSTRUMENT-ONLY (log guard ptr + *guard + caller, FALL THROUGH so the real ucrtbase guard runs unaltered: epoch-compare + thread-wait preserved). Env MACRUNNER_HB_TRACE_BLOCK, cap 60000. Run in flight. ANALYSIS: match header↔footer by guard ptr; a guard with header(s) but NO footer = construct never completed = producer-gap; caller names the static. The EARLIEST unmatched = cascade root → cross-ref the c000007b object → name the JIT mistranslation in the guard/init-body → cross-check ABZU's bracket (same HB code path / instruction class = shared-core, one fix). COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · MAGIC-STATIC IMPORT ANGLE EXHAUSTED: 0 _Init_thread_header/footer captured even with the trace moved BEFORE the is_crt_runtime gate (catches any dll) → these are NEVER imported → UnityPlayer STATICALLY LINKS the MSVC runtime (/MT) → the magic-static/one-time-init guard is LOCAL JIT'd code, not a CRT import → import-hook interception CANNOT reach it. Combined with _initterm ruled out, ALL import-based init mechanisms are exhausted: HK's skipped ctor is gated by LOCAL/inlined guest code. The divergence is a SILENT wrong branch in that local gate (only 1 c000007b/run = the downstream USE, no fault at the gate). Side-clue: last 3 c000007b targets 0x17ffb43eb/0x17ffe43eb/0x17fff43eb share low 0x43eb (structured, not random) — possible relocation/base-computation pattern, unconfirmed. ★STATE: independent naming of HK's instruction now needs RE+oracle of the LOCAL gate (disasm UnityPlayer at the r15-load + construct-gate → oracle that block HB-vs-Unicorn) — involved; OR converge with ABZU's CrossOver differential (names the shared diverging instruction-class; I verify HK's gate uses the same x86 op + fix-test). Ruled out this session: throughput, TSO, atomic-RMW, Mono-over-rejection, _initterm, magic-static-import. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · USE-BEFORE-INIT DETECTOR built (operator's pick). macrunner_hb.c run_x64 per-block point: on r15-CHANGE (cheap — r15 callee-saved, changes only when code loads a new object), check [r15+0]==0 (null vtable) AND ([r15+8]||[r15+0x10] set) (partially-filled = allocated-but-not-constructed, not a zero buffer) → log macrunner-hb-ubi #N (r15, fields, block_pc), dedup + cap 24/thread. Env MACRUNNER_HB_TRACE_BLOCK. Catches the FIRST uninit-object USES generically, EARLIER than the downstream fatal c000007b (which always aborts, so it's not the earliest use). Run in flight. ANALYSIS: earliest ubi #0 block_pc = the use site nearest the producer-gap → RE/trace back to the construct-gate → name the JIT mistranslation; cross-ref the c000007b object. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · USE-BEFORE-INIT DETECTOR = NOISE-DOMINATED (doesn't isolate the gap). 60 detections were math vectors ([r15+0x10]=0x3f800000=1.0f) + string buffers ([r15+0x10]=0x75632e696778642a="*dxgi.cu") — any data with a zero first word. The cap filled with early-boot noise (L1301-1318); the actual c000007b object (region 0x1679e...) never appeared. ROOT ISSUE: an uninit object's garbage slot (e.g. c000007b 0x1679e43eb, UNALIGNED) is INDISTINGUISHABLE from data (floats/strings) with a zero first word — no cheap per-block filter separates them; alignment fails too (garbage slot is unaligned, like strings). ⇒ the silent producer-gap (a construct-skip BRANCH, no fault at the gate) needs a REFERENCE of what-should-happen = a differential. ALL cheap MacRunner-side independent angles exhausted this session: throughput, TSO, atomic-RMW, Mono-over-rejection, _initterm, magic-static-import, use-before-init. The remaining independent path = RE the singleton-global (disasm UnityPlayer at the r15-load 0x87ef11f67bf → find the global → instrument its writer → the construct that should follow) — INVOLVED; OR converge with ABZU's CrossOver differential (names the shared diverging instruction; I verify HK's gate + fix-test). COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · ★★★CROSSOVER REFERENCE STOOD UP (operator's unlock). HK runs under CrossOver (bottle MacRunnerGames, `wine start /unix` to handle the space in "Hollow Knight.exe", Rosetta x86_64). Player.log (bottle AppData/LocalLow/Team Cherry/Hollow Knight/): reaches Direct3D 11.0 device + MonoManager + PhysX(PhysX 4.1.2) + <RI> Input initialized + UnloadTime + scene-load — ★NO c000007b/producer-death★ ⇒ CONFIRMS MacRunner's c000007b IS an HB/Wine bug (the producer runs correctly under CrossOver), like ABZU. Reference VALID for the c000007b differential. ⚠NEW DOWNSTREAM WALL (separate from c000007b): HK does NOT reach the menu under CrossOver either — "GOG authorization failed: FAILURE_REASON_GALAXY_SERVICE_NOT_AVAILABLE" + "Couldn't find a UIManager" loop → stalls (54% CPU, log frozen). This is the GOG build's online-subsystem (GOGGalaxyOnlineSubsystem) needing GOG Galaxy. ⇒ (a) MacRunner's c000007b is in the POST-UnloadTime init (the GOG/online-subsystem + scene-load region) — NEW LEAD it may be GOG-subsystem-related; (b) fixing the c000007b may surface the GOG wall next (CrossOver hits it — likely environmental: no GOG Galaxy service). Engine version 6000.0.61f1 (Unity 6). NEXT: cross-ref MacRunner's c000007b vs the GOG markers (is it the GOG subsystem?), then memwatch the singleton + differential-chainwatch per the operator's plan. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · GOG cross-ref in flight: first MacRunner run stalled at input-init (no UnloadTime/GOG, no c000007b — boot is variable: some runs stall pre-UnloadTime, some reach UnloadTime+c000007b). Retry probe (up to 4×, early-stop on c000007b OR "Selected online subsystem") to capture the marker timeline + whether the c000007b is at/after GOGGalaxy (= GOG-subsystem-related). Then: (step 2) find+memwatch the singleton global (the r15-load source), differential-chainwatch MacRunner-vs-CrossOver to the first diverging branch; reframe = trace what the gate READS (faithful gate, wrong data — ABZU's shape). Will also need UnityPlayer load base to translate the r15-load RVA (0x87ef11f67bf) for CrossOver winedbg. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · ★GOG REFUTED as the c000007b cause: MacRunner reaches <RI> Input initialized (L5367) then c000007b (L8466) — NEVER reaches UnloadTime or GOG markers. On CrossOver UnloadTime is 2ms after input-init + GOG right after. ⇒ the c000007b fires in the input-init→UnloadTime window = the EARLY SCENE-LOAD, BEFORE GOG → a SCENE-LOAD producer-gap (a Unity scene-load worker), NOT the GOG subsystem. (GOG wall is a separate DOWNSTREAM issue CrossOver also hits.) NEXT: capture UnityPlayer load base + the c000007b RVAs (caller last_block + r15-load) → break at those RVAs under CrossOver winedbg (where HK runs correctly) to trace the producer's construct + what the gate READS; differential to the first diverging branch. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · RE of the c000007b region (UnityPlayer base=0x87ef0000000, CONFIRMED via byte-match 0x11f67bf=48 8b d7=mov rdx,rdi): the c000007b is in a REGISTERED-HANDLER/SUBSYSTEM LOOP (function ~0x11f6850; 3 indirect `call rax` sites 0x11f694c/0x11f6b9f/0x11f6d09 + nested element loops, stride 0x58/0x60) = the producer-gap shape (a handler whose method-ptr slot is uninit). BUT offline LINEAR disasm DRIFTS (ABZU's lesson — 32MB .text; 0x11f6e79 lands mid-instruction in linear stream though it's a real JIT block boundary). ⇒ exact indirect-call instruction needs RUNTIME inspection. PLAN (operator's): CrossOver winedbg — UnityPlayer RVAs are real execution addresses (targetable despite offline drift); break at the c000007b block where HK runs CORRECTLY, inspect the VALID handler/object + what the gate READS, vs MacRunner's garbage. Capturing CrossOver UnityPlayer base next (via +loaddll). COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · ★★★CROSSOVER DIFFERENTIAL — CONTROL-FLOW DIVERGENCE CONFIRMED. winedbg setup solved: real-copy HKDBG.exe (space workaround), $BreakOnFirstChance=0 (HK uses first-chance exceptions liberally; without this winedbg terminates it), $CanDeferOnBPByAddr=1. CrossOver UnityPlayer base=0x6ffffc620000 (via break UnityMain + info share). Set bp at the c000007b block UnityPlayer+0x11f6e79 (=0x6ffffd816e79): bp ARMED but NEVER HIT in 440s — yet Player.log shows HK reached the FULL scene-load (UnloadTime/GOG/UIManager-loop, same depth as normal). ⇒ CrossOver does NOT execute 0x11f6e79; MacRunner DOES (+ c000007b's there). A CONTROL-FLOW DIVERGENCE: MacRunner takes a path to 0x11f6e79 that CrossOver doesn't = ABZU's shape (a gate steers wrong). The gate (upstream of 0x11f6e79) reads data that differs MacRunner-vs-CrossOver. NEXT: bisect — break along the MacRunner r15-ring path (0x11f67bf→0x11f6978→0x11f6a64→0x11f6e24→0x11f6e49→0x11f6e79) on CrossOver; the LAST block CrossOver executes before the non-executed ones = the diverging gate. Then inspect what that gate READS. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · ★BISECT RESULT + REFRAME: ALL 6 r15-ring path blocks (0x11f67bf..0x11f6e79) armed but NONE hit on CrossOver (HK reached full scene-load). ⇒ the WHOLE MacRunner c000007b path is MacRunner-only. + the c000007b LOCATION VARIES across runs (0x11f6e79 / 0x727c96100 / 0x17ff..43eb / 0x87efc7c5605=FMOD). ⇒ NOT a single fixed-block divergence — a CASCADE of producer-gaps (multiple uninit objects, like ABZU's 3+), fault hits whichever vcalled first. The per-block differential (0x11f6e79) addresses one run's manifestation, not the root. ROOT = the EARLIEST divergence (deep iterative differential, ABZU-scale: ABZU took ~10 iterations to reach its malformed-path root). ★★CRITICAL STRATEGIC FINDING: the CrossOver REFERENCE ITSELF does NOT reach the menu — GOG auth failure (GALAXY_SERVICE_NOT_AVAILABLE) + UIManager-loop stall. So even a perfect HB (matching CrossOver) would NOT reach a frame → fixing the c000007b alone CANNOT yield a first frame for THIS (GOG) HK build without ALSO resolving the GOG wall (non-GOG build / offline config / GOG Galaxy service). ★DELIVERABLE: reusable CrossOver winedbg differential harness (real-copy exe for space, $BreakOnFirstChance=0, deferred bp, base via UnityMain+info-share) — works for any future Unity bug. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · ★REFRAME ACCEPTED (operator): GOG-block premise REFUTED — operator has a CrossOver screenshot of HK rendering the LANGUAGE-SELECT (a real frame); GALAXY_SERVICE_NOT_AVAILABLE is NON-fatal (offline). My "UIManager stall/no frame" = a winedbg/$BreakOnFirstChance artifact + killed-too-early. First-frame = ANY rendered frame (logo/language-select), BEFORE any GOG gate. ⇒ GOG is IRRELEVANT; the c000007b (init crash, before any frame) IS the real+only blocker; fixing it DOES yield a first frame. DIFFERENTIAL (earliest-divergence, ABZU-scale): the c000007b path (0x11f6xxx) is MacRunner-only on CrossOver → divergence is UPSTREAM (a caller/gate). Added a guest CALL-CHAIN stack-scan to the badtarget (UnityPlayer RVAs on the stack = the callers). Capturing now (retry until c000007b) → then bisect those caller RVAs on CrossOver (base 0x6ffffc620000) to the LAST-common block = the earliest divergence; inspect what its gate READS (ABZU's shape). Step3: cross-check ABZU task#19 (shared-core Wine/runtime-state divergence — may clear HK too). COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · CALL-CHAIN captured (run base 0x87ef0f70000, ASLR-randomized): target=dynamic-builtin.dll+0x4b4db0 (=HB FALLBACK label for an unidentifiable module, macrunner_hb.c:1047 — NOT a real mechanism; the target is a rw- heap region = uninit slot, confirms producer-gap). UnityPlayer RVAs on stack: +0x286e8e/+0x53fa70/+0x28602e/+0x89c8b/+0x8a02c/+0x286ec7/+0x1c8764/+0x1c8850 (mix of return-addrs + data ptrs; offline disasm DRIFTS, can't cleanly ID return-addrs). NEXT: bisect these on CrossOver. ★FRICTION (honest): the differential is ABZU-scale (~10 iters) but EACH iter fights — ASLR randomizes BOTH bases per run (cross-run RVA align fragile), offline RE drifts, stack-scan noisy, winedbg slow (~8min/run), c000007b intermittent+varying-location. STRATEGIC: HK confirmed SAME CLASS as ABZU (producer-gap cascade, control-flow divergence). Operator's step3 (ABZU task#19 = shared-core Wine/runtime-state divergence) is the likely SHORTCUT — if its root lands, test on HK first (one fix, both) before grinding HK's ~10-iter friction-hunt. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · BISECTION iter done (HOLD per operator's (a)): CrossOver UnityPlayer base STABLE 0x6ffffc620000 (no re-ASLR — good). None of 5 call-chain RVAs (0x1c8850/0x1c8764/0x89c8b/0x286e8e/0x28602e) hit on CrossOver in 470s post-UnityMain → either data-ptr noise or MacRunner-only (divergence even higher up). Bisection not converging cleanly = the friction justifying (a). HOLDING the grind, banked. ★ABZU task#19 CONVERGED (operator): root = a UE4 FArchive deserialize reading WRONG 4 BYTES (HB 0xb8631f47 vs CrossOver magic 0x5a6f12e1) → format-check fail → error path → the NULL crash object is the ERROR-PATH LOGGER singleton (red herring). task#20 (cause classification) imminent. PLAN: hold HK grind; shared-structure check running (does HK log a data/deserialize/format error just before the c000007b = ABZU's shape?); when task#20 lands: Wine-file-IO-wrong-bytes→almost-certainly-shared→apply+fix-test HK 1 run; HB-codegen→maybe→fix-test; UE4-pak→UE4-specific→resume bisection. HK's c000007b path being MacRunner-ONLY = a divergent/error branch = consistent with ABZU's faithful-gate-fed-wrong-data shape. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · SHARED-STRUCTURE CHECK = CONSISTENT WITH ABZU. c000007b fires RIGHT AFTER UnloadTime (L9305 vs UnloadTime L9261, 44 lines) = at the scene-LOAD/DESERIALIZE point (UnloadTime=235ms MacRunner vs 2ms CrossOver). NO explicit data/format/deserialize error logged in the 600 lines before the c000007b (sparse Unity log there, mostly HB trace) = consistent with ABZU's LOGGER-CRASH pattern (error not logged because the crash object IS the error-path logger). + structural: c000007b path is MacRunner-ONLY (divergent/error branch). ⇒ matches ABZU's signature: scene-load deserialize → format-check fail → error path → crash. (a) well-founded. POSTURE: HOLD HK grind; standing by for ABZU task#20 classification → 1-run fix-test on HK (Wine-file-IO-wrong-bytes→apply+test, almost-certainly-shared since Unity reads assets via same Wine file-IO; HB-codegen→test; UE4-pak→resume bisection). Bisection + CrossOver winedbg harness banked. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · ★ABZU ROOT >4GB-SPECIFIC (operator): reads a 4.767GB pak footer from the wrong offset → off_t/>4GB bug. HK is SMALL (<4GB) → does NOT hit that path → ABZU's fix likely WON'T clear HK. STRUCTURE shared (scene-load deserialize → error-path logger-NULL, MacRunner-only — confirmed) but CAUSE differs. ⇒ find HK's OWN cause via ABZU's method: SOURCE-CAPTURE the scene-load deserialize's reads (file/offset/bytes) on BOTH MacRunner+CrossOver, diff → first difference = HK's cause (wrong bytes@right offset=Wine file-IO/decompress bug; wrong file/offset=path/seek bug). NOW: MacRunner WINEDEBUG=+file capture of HK's asset-file reads (deterministic — the wrong read happens regardless of the intermittent downstream crash). Then CrossOver same (HKDBG.exe absolute, +file) + diff. CrossOver UnityPlayer base stable 0x6ffffc620000; winedbg harness banked. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · SOURCE-CAPTURE (file-read layer) findings: F_GETPATH WORKS (0 fails). First 140 FD_TYPE_FILE reads = ALL kernelbase.dll (140× full 9.37MB reads = ~1.3GB; a Wine boot re-read inefficiency, NOT the bug — note for perf). Unity asset reads NOT captured via NtReadFile. ★KEY: c000007b fires 16 lines AFTER input-init (L20644 vs L20628), BEFORE UnloadTime + before the scene-load reads assets ⇒ the gate's wrong-data is NOT a freshly-read scene asset (file-read layer is WRONG for this) — it's a boot-time read or a computed/memory value. (c000007b position VARIES run-to-run: sometimes pre-UnloadTime, sometimes post — intermittent.) ⇒ to capture what the gate reads needs a recent-read RING dumped at the c000007b (the gate's actual data, regardless of mmap/timing), not an asset-name filter — moderate cross-file plumbing (ring in file.c + extern dump from macrunner_hb.c run-exit). ★HONEST STATE: ~68 turns; structure CONFIRMED (ABZU's producer-gap/error-path class) + reusable CrossOver winedbg harness banked; but the exact CAUSE has resisted differential/bisection/source-capture/RE through layered friction (offline drift, ASLR-both-sides, winedbg slowness, mmap, varying-location c000007b, wrong-layer reframes). COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · ★READ-RING (the bounded shot, operator's option 1): per-thread last-32-file-reads ring in file.c (name/off/len/first-16-bytes, env MACRUNNER_TRACE_FILEINFO), dumped at the c000007b via macrunner_dump_read_ring() called from the HB run-exit-indirect (macrunner_hb.c). BYPASSES the friction that walled every prior method (no disasm/ASLR/winedbg). NB: Wine POISONS strncpy in ntdll → used memcpy+strlen. Built clean, ntdll installed. Retry-until-c000007b probe running (MACRUNNER_HB_TRACE_BLOCK=1 + MACRUNNER_TRACE_FILEINFO=1). OUTCOMES (all informative, then BOUND=stop): (1) a recent read has WRONG bytes/offset vs CrossOver → HK's cause (ABZU-analog) → name+fix-test; (2) reads match CrossOver but gate crashes → in-memory value not a file → escalate option 3 (memory-value capture); (3) no relevant read precedes crash → in-memory → option 3; (walled) neither → STOP, pause HK (bank confirmed structure + CrossOver winedbg harness). COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8.
2026-06-22 · ★★★SHARED-CORE FIX FOUND + APPLIED (operator: ABZU's bug is NOT >4GB-specific — HB ReadFile ignored lpOverlapped, reading at the fd's CURRENT position → corrupts ANY offset-mismatched overlapped read). FOUND HK's exact spot: macrunner_hb.c ReadFile handler (10414) did `read(fd, buffer, request)` at the current position, IGNORING lpOverlapped (args[4]). ★This explains the read-ring miss: the guest's ReadFile goes through THIS HB handler (raw read(fd)), BYPASSING NtReadFile — so my NtReadFile ring only saw DLL loads. FIX (macrunner_hb.c): if args[4] (lpOverlapped) present → pread at the OVERLAPPED offset (Offset@+16/OffsetHigh@+20 in x64 OVERLAPPED) instead of read@current; same for WriteFile→pwrite. Built clean, installed. TEST 1 running: does the c000007b vanish + HK reach PAST-UnloadTime markers (GOG/UIManager/language — NEVER reached before, died at c000007b pre-UnloadTime) + render the first frame? If yes → shared-core win on BOTH HK+ABZU. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8 (overlapped fix UNCOMMITTED).
2026-06-22 · TEST 1 RESULT: overlapped fix did NOT clear HK — c000007b still fired (1), no progress past the early scene-load (input-init t=360s, died before UnloadTime, same as pre-fix). ⇒ either HK's reads aren't overlapped, the fix isn't on HK's read path, or it's a different bug. TEST 2 proper (right layer): added macrunner-guestread log at the HB ReadFile handler (10414 — where the guest's ReadFile is served via raw read/pread, BYPASSING NtReadFile; this is why my NtReadFile ring only saw DLLs). Logs non-DLL guest reads: file/ovl(overlapped?)/offset/bytes. → tells me if HK uses overlapped reads (ovl=1 → fix should apply → bug elsewhere/offset wrong; ovl=0 → non-overlapped → bug is a seek/position or in-memory). COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8 (overlapped fix + guestread log UNCOMMITTED).
2026-06-22 · ★★★TEST 2 VERDICT (right-layer guest-read capture at HB ReadFile handler): HK's reads are 100% NON-OVERLAPPED (0/299 ovl=1; all off=-1 current-position) + SUCCEED (got=req) + return VALID Unity SerializedFile v22 data (globalgamemanagers ×232, sharedassets0.assets/.resS/.resource, globalgamemanagers.assets, level0, boot.config/app.info/config/json — sane headers '00000016', advancing sequential content). level0 (the first scene, req=83952 got=83952, valid header) is the LAST data read before the c000007b (read L8595, c000007b L8923). ⇒ CONCLUSIONS: (1) the overlapped fix (ABZU's shared-core bug) is IRRELEVANT to HK — HK does NOT trigger it (0 overlapped reads). The fix is still CORRECT + benefits ABZU/any overlapped reader (kept UNCOMMITTED). (2) HK's read DATA is CORRECT (valid Unity v22, reads succeed) → the c000007b is downstream of correct reads = an IN-MEMORY/CODEGEN issue (the null-vtable uninit-object vcall = a constructor/dispatch NOT run), NOT a file-read bug. This matches ABZU's ORIGINAL Sess18-19 class ("creator/constructor never dispatched → singleton NULL → crash" = static-init/dispatch divergence), NOT the overlapped-read mechanism. (3) Per the operator's BOUND: the read-layer is cleared (reads correct), pointing to in-memory; option 3 (memory-value capture at the gate) needs the gate block PINNED = the friction wall (drift/ASLR) that walled every prior method → STOP, pause HK. BANKED: findings + CrossOver winedbg harness + guestread instrument + the (correct) overlapped fix for ABZU. COMMITS(HEAD): 3eeefc6, dfc2148, 51605cd, 2b9fbe8 (overlapped fix + guestread log UNCOMMITTED).
2026-06-22 · ★BYTE-DIFF (airtight, ground-truth = the actual file bytes, which CrossOver also reads): HK's captured pos-0 reads MATCH the on-disk files BYTE-FOR-BYTE — globalgamemanagers + level0 = 00000000000000000000001600000000 (Unity SerializedFile v22 header), boot.config = 6766782d... ("gfx-threading-mo"), app.info = 5465616d... ("Team Cherry"). ⇒ READS DEFINITIVELY RULED OUT. Bank airtight. ★PAUSED HK per the bound. BANKED: (a) verdict (HK c000007b = in-memory/dispatch bug, ctor-never-ran null-vtable, downstream of correct reads; ABZU=read bug, HK=dispatch bug — diverge); (b) CrossOver winedbg harness + Player.log reference; (c) guest-read instrument (HB ReadFile handler, MACRUNNER_HB_TRACE_BLOCK) + read-ring (file.c, MACRUNNER_TRACE_FILEINFO) — env-gated/inert; (d) overlapped ReadFile/WriteFile fix (correct, benefits ABZU, irrelevant to HK), UNCOMMITTED. RESUME when codegen/EC-unwind tooling matures (PE32 EC-unwind + uc_lockstep) — same JIT-dispatch class. Memory: project-hk-firstframe-paused.md. golden + HK game install UNTOUCHED.
