2026-07-26 19:21 +10 · C1 magenta-control lane started · validating current HK launcher/prefix/artifact state before run · next: synchronize and byte-verify winemetal.so in the selected prefix
2026-07-26 19:40 +10 · C1 preflight artifact audit · requested cf093… is absent; specified build+dist are f44ad641…; live HK JIT run PID 3551/9863 detected, so no dist/prefix mutation · next: inspect completed C1 evidence and wait for verified owner-run completion
2026-07-26 19:44 +10 · C1/coverage/final-surface evidence reconciliation · C1=655360/655360 magenta, coverage now partial/131072 missing, actual first drawable has one draw/no clear yet 786432 black; report PRESENTED-DRAWABLE-BOUNDARY-REVIEW-20260726.md · next: inspect VS-constant instrument while PID 3551 remains live
2026-07-26 19:48 +10 · VS-CB instrument verified + C0 runner prepared · current x86_64 d3d11 SHA=336c76d… contains probe; classifier self-control=13/13 PASS; syntax-checked read-only ordinal-200 runner · next: await PID 3551 C1 ordinal-200 control before preflight/launch
2026-07-26 19:52 +10 · Late-C1 monitor + source replay · selector parsed (`phase=selector result=ok ordinal=200`) but that line is emitted on first Present configuration, not proof ordinal 200 occurred; no presented-surface completion yet · next: continue bounded monitor; do not treat absence as failure before 40 min
2026-07-26 19:55 +10 · Late-C1 monitor · PID 3551/9863 remains live at 18:49/18:25; logs last grew at 19:50 and contain no `checkpoint=presented-surface` or schedule result; <40 min gate not reached · next: retain run and resume monitor in next autonomous iteration
2026-07-30 18:00 +10 · SPEED-DIG · retired a provably-dead 1 MB import-table linear scan that ran on EVERY guest block dispatch (`macrunner_hb_find_import_thunk(ctx->pc)`, top self-time item: 84/536 + 99/501 samples on one instruction): `Restored language` 828.7s → 448.4s / 490.3s (n=2, baseline 593±78s), Mono phase 284.2s → 150.5s / 135.5s; default config verified on shipped ntdll 7a3f1a44, avg_chain=1.0000 · block chaining measured as capped at 7.4% of terminals (jcc is 71.1%) and dies exit=5 5/5 incl. with CHAIN_WIDE=0, so the widening + PC guard are innocent and the defect predates this lane; BLOCK_CHAIN stays default-off · RETRACTED my own claim that hb_test_runner reproduced a chaining ctx->pc bug — a SINGLE_LOOKUP=1 control arm shows all 5 pc failures come from dispatch_fastpath (already the production default), leaving only 8 benign code-size-budget asserts · next: split the chain flag into emit-side vs patch-side to bisect the exit=5 in one 45s run
LOOP-STATUS: CONTINUE import-scan fix delivered and verified (n=2, 828.7s→469.4s mean); chaining still exit=5 5/5 with the widening ruled out — next bisect is emit-side vs patch-side chain gate
2026-07-30 18:30 +10 · SPEED-DIG · ROOT-CAUSED the block-chaining exit=5 (5/5 runs) by three-arm bisection on new gates MACRUNNER_HB_CHAIN_PATCH / MACRUNNER_HB_CHAIN_WRITE: emit side alone (4-NOP slot + counter accounting) BOOTS to Begin MonoManager with 19.15M dispatches, but merely COMMITTING a trampoline — never branched to — dies exit=5, isolating `chain_trampoline_for`, which wrote 20 instruction words into a page `jit_commit_blob` had already re-protected via hb_jit_buffer_commit (a store to non-writable JIT memory kills the process with no guest exception, matching zero SIGSEGV/SIGBUS/quarantine markers across all five deaths); fixed by bracketing the build in make_writable/commit · VERIFIED: CHAIN-WXFIX (ntdll f5aee510) now passes the death point — gate +49.661s then 450k dispatches at +56.662s, vs exit=5 within 0.5s of the gate in all five prior arms (run cut by my own wrapper timeout, not the guest; no orphans left) · avg_chain still exactly 1.0000 with chainable_pct=5.21, so chaining is now safe but INERT · next: find why patch_block_tail declines — suspect its `meta->target_code == next->native_code + 16` early return (compares a trampoline address to a block address, never true), entry_has_chain_slot, and arm64_branch_reaches across a 128MB arena whose B range is exactly ±128MB
LOOP-STATUS: CONTINUE chaining crash root-caused and fixed (missing W^X bracket in chain_trampoline_for; guest now survives 450k dispatches past the 5/5 death point) but avg_chain still 1.0000 — mechanism safe and inert, next is why patch_block_tail declines
2026-07-30 18:40 +10 · SPEED-DIG · counted patch_block_tail's decline reasons per exit (new macrunner-hb-chaindecline): site_called=389480 already=336061 slot_next=47054 PATCHED=6365, zero for gate/invalid/nometa/terminal/slot_cur/tramp/reach/wprot/xprot — so after the W^X fix chaining IS installed (6365 tails patched, and already=336061 proves those predecessors keep re-executing), REFUTING my own iteration-3 "safe but inert" reading · avg_chain=1.0000 is an INSTRUMENT DEFECT proven by construction: the patch overwrites 2 of the 4 NOPs (hb_runtime.c:3158-3159) while entry_has_chain_slot demands all four still be NOP, and that predicate gates native_accounting (:6011,:6234), so chained blocks fall back to blocks_executed++ and avg_chain can never exceed 1.0000; the same predicate rejects already-patched blocks as chain TARGETS (slot_next=47054), self-throttling coverage · chaining now stable (exit=124 at +203s on its own budget, no exit=5) · third instrument defect this lane found in its own measurements, after the eb89b54d denominator and dispatch_stats_register's per-dispatch clock_gettime · next: record the chain-slot offset at emit time in the cache entry / hb_block_chain_meta instead of sniffing bytes chaining itself overwrites — that fixes the blindness and the self-throttling together; chaining's payoff remains UNMEASURED and no speed claim is made for it
LOOP-STATUS: CONTINUE chaining installed and stable (6365 tails patched) but its payoff is unmeasurable — avg_chain is blind by construction because patching destroys the 4-NOP signature that gates native_accounting; next is recording the slot at emit time
2026-07-30 18:55 +10 · SPEED-DIG · fixed the blind chain-slot predicate (accept the PATCHED shape MOV X0,X19/B/NOP/NOP beside the pristine 4 NOPs, keeping byte-sniffing so the persistent cache keeps working) and MEASURED BLOCK CHAINING FOR THE FIRST TIME: avg_chain 1.0000 -> 5.1886..5.9293, slot_next declines 47054 -> 0, PATCHED 6365 -> 12033 — a dispatcher round trip now retires ~5.2 guest blocks · BUT the same run shows it is a NET LOSS: blocks/s 2,243,438 (chaining off) -> 188,785 (on), a ~12x drop in guest throughput, and the boot WEDGES deterministically 2/2 at ~+53s (identical PATCHED=12033/site_called=43810 both runs; last log line +52.7s, only `Mono path`, never `Initialize engine version`, dies on the wrapper budget), where the same lineage with patching disabled reached Begin MonoManager at +61.3s · BLOCK_CHAIN therefore STAYS default-off and no speed claim is made for it · my wedge-sample helper matched no pid and wrote nothing — logged as a failed instrument, not evidence · next: refuse to chain BACKWARD edges (next->guest_addr <= cur->guest_addr, the loop-backedge shape) — two lines + one 150s run discriminates "chained cycle never returns to the dispatcher" from "the 5x larger steps/blocks deltas feed back into out->steps_executed and trip a budget in macrunner_hb_run_x64"
LOOP-STATUS: CONTINUE block chaining now measurable and working (avg_chain 5.19, 12033 tails patched) but it costs 12x guest throughput and wedges the boot deterministically at ~+53s, so it stays default-off; next is forward-edge-only chaining to test the chained-cycle hypothesis
2026-07-30 20:35 +10 · SPEED-DIG · bisected the chaining wedge on QUANTITY after both kind-splits failed, using new gates MACRUNNER_HB_CHAIN_MAX_PATCHES + MACRUNNER_HB_TRACE_CHAIN_EDGE: cap=8 wedges, cap=1 WEDGES (PATCHED=1, capped=3003092), and the single fatal edge is logged deterministically as a SELF-CHAIN cur=next=0x87ef3e43250 — but forward-only+cap=8 (self and backward edges refused, backedge=956801) ALSO wedges on 8 strictly-forward distinct edges, so the self-chain is a real hazard yet NOT the cause; retracted · four arms therefore force the conclusion that EXECUTING ANY CHAINED TRANSITION wedges the guest (no patch boots to Begin MonoManager; 1, 8 and 11458 patches all wedge), which retires the cycle/backedge/self-chain hypotheses with evidence and re-poses the defect as "the chained-execution path is broken on first use" · also RETRACTED my iteration-5 claim that chaining is a ~12x net loss: it compared a 6.9s cold-boot window against a 233s window incl. steady state; conditioned on comparable earliest windows chaining is if anything fastest (1,041,814 blocks/s at avg_chain 5.83 vs 854,701-862,069 with chaining off), and no throughput verdict is supportable while every chaining arm wedges before steady state · BLOCK_CHAIN stays default-off; import-scan fix untouched (Restored language 828.7s -> 448.4/490.3s) · next: dispatcher-side trace when run_block_delta>1 printing ctx->pc + predecessor guest_addr + expected successor, to tell trampoline control-flow corruption from skipped per-dispatch host work
LOOP-STATUS: CONTINUE chaining wedge narrowed to "any single chained transition is fatal" (cap=1 wedges; self-chain and backedge hypotheses both refuted by forward-only+cap=8); next is tracing guest state across one chained transition
2026-07-30 20:50 +10 · SPEED-DIG · traced one chained transition with cap=1: `chaintransit from=0x87ef3e43250 pc_after=0x87ef3e4325c blocks=4 steps=16` — the self-chained block ran 4 iterations in ONE native dispatch and returned to a sane guest PC, so the trampoline provably does NOT corrupt control flow (encodings clang-verified, native_code+12 entry verified, now the runtime behaviour too) · yet that run still wedges, and the guest kept dispatching 3.4M blocks afterwards, so the chained jump is not the wedge · rebuilt chain_trampoline_build_at to assemble into a LOCAL buffer committed by a single jit_commit_blob (with a dest!=predicted refuse-guard) so nothing outside jit_commit_blob toggles the arena — this REFUTED my own W^X hypothesis by its own prediction: CHAIN_WRITE=0 (write_off=3003120, zero patches, zero extra toggles) still wedges · also REFUTED arena exhaustion: zero jit-buffer-full/code-cache-full/interp-fallback markers in any of the four runs incl. the booting one · isolated variable is now `cache->chain_meta` being ALLOCATED: the booting arm (CHAIN_PATCH=0) never calls block_cache_chain_meta(create=true), and that allocation is the enable flag for the eviction-time unchain machinery at hb_runtime.c:516-590, which writes arm64_nop back over block tails (:525-527) and flips trampoline literals — pre-existing code, consistent with chaining never having worked · BLOCK_CHAIN stays default-off; import-scan fix untouched · next: gate the eviction unchain writes while keeping chain_meta allocated — the trampoline design's premise is that eviction is ONE store to a literal, not a rewrite of predecessor tails
LOOP-STATUS: CONTINUE chained transitions proven correct at runtime; W^X and arena-exhaustion hypotheses both refuted by their own predictions; wedge isolated to chain_meta allocation enabling the eviction unchain path, which is the next gate
2026-07-30 21:10 +10 · SPEED-DIG · found and fixed a genuine SECOND instance of the unbracketed-arena-write bug: block_cache_prepare_replace_entry stored the eviction trampoline literal (__atomic_store_n slot=bail) in the first if-block while hb_jit_buffer_make_writable was only called in the second, i.e. a store into read+execute JIT memory whenever the evicted block had a trampoline — both writes now share one bracket, ordering preserved · BUT REFUTED as the wedge: CHAIN-EVICTFIX (ntdll a7de44ab, full chaining) still wedges with avg_chain=5.6543, PATCHED=11003, Mono path only, dispatch stops +53.1s · three mechanisms now proposed and killed by their own predictions: trampoline W^X bracket, arena exhaustion (zero jit-buffer-full/code-cache-full/interp-fallback markers in any run incl. the booting one), and the eviction literal store · also established the per-eviction W^X cycle was never a candidate — it is gated only on runtime_block_chain_enabled(), so the booting CHAIN_PATCH=0 arm performs it too · wedge remains isolated to CREATING A TRAMPOLINE AT ALL (0 patches wedge, 1 wedges, 11003 wedge, no trampoline boots) with chained transitions proven correct at runtime; mechanism NOT found and I have stopped guessing · NON-MEASUREMENT: my wedge sample used `pgrep -f extracted-hollow-knight`, which matched a WRAPPER command line — the profile was /bin/bash (Path:/bin/bash, ??? addresses inside bash's own load range), so its convincing "spinning in arena code, no dispatcher frame" reading is discarded; the documented matcher is `ps -Ao pid,comm | grep -i hollow` plus verifying the sample's Path: line before reading any count · BLOCK_CHAIN stays default-off; import-scan fix untouched · next: re-sample the deterministic +53s wedge with the verified matcher
LOOP-STATUS: CONTINUE second unbracketed-arena-write bug found and fixed but refuted as the wedge (3 hypotheses now killed by their own predictions); wedge still isolated to trampoline creation, and my sampling attempt profiled bash so it proved nothing — next is a verified-target sample
2026-07-30 21:20 +10 · SPEED-DIG · sampled the chaining wedge with a target-VERIFIED profile (matched on comm, then checked the sample header says Process: wine [22923] / Path: .../wine before reading any count — last iteration's pgrep -f matcher had profiled /bin/bash): the wedged guest has 8 threads and EVERY ONE IS PARKED — 1x mach_msg2_trap + 7x __ulock_wait2, stable across two samples 70s apart, with ZERO hb_jit_runtime_run frames, ZERO unsymbolized ??? frames and nothing running · so the wedge is a LOST WAKEUP / DEADLOCK, not a spin: this retires the whole spinning family incl. my original chained-cycle idea AND the discarded bash reading that had claimed "spinning in arena code, no dispatcher frame" · connects to known ground: all-threads-in-__ulock_wait2 is the signature in project_hb_two_futex_queues_20260729 (HB and wine keep separate futex queues; sync.c builds SRWLock/condvar/critsect on PE-INTERNAL calls HB cannot intercept, equal=0 proves a lost wake) — plausibly chaining removes the dispatcher visit at which a wake would be observed, but that stays a HYPOTHESIS after three offline guesses already failed · BLOCK_CHAIN stays default-off; import-scan fix untouched (Restored language 828.7s -> 448.4/490.3s) · next: run the documented instrument for this symptom rather than a new guess — WAIT_WAKE_TRACE + ARM_FILE on a chaining run and read whether equal=0 (NOT TRACE_WAITADDR, which logs only n<64 || n%8192 and so can never show the final wait)
LOOP-STATUS: CONTINUE chaining wedge measured as a lost wakeup — all 8 guest threads parked in __ulock_wait2/mach_msg with no JIT frames, retiring every spin hypothesis; next is WAIT_WAKE_TRACE to find the lost wake
2026-07-30 21:30 +10 · SPEED-DIG · WEDGE NAMED from the samples I already had plus the run log: all 8 threads of the wedged guest are 1x Cocoa main thread idle in CFRunLoopRun/mach_msg2_trap and 7x AssetGarbageCollectorHelper parked identically in macrunner_hb_call_import_thunk -> try_kernel32_handle_semantic -> NtWaitForSingleObject -> inproc_wait -> msync_wait_single -> __ulock_wait2, and NO thread has an hb_jit_runtime_run frame — the guest's MAIN thread is absent · the log says why: at +48.076s `macrunner-hb-run-exit: label=thread status=c000007b reason=runtime pc=0x87ef2469a30` with fail-bt slots in mono-2.0-bdwgc.dll rva=0x3cc250, so with chaining armed the main thread dies c000007b inside Mono and the helpers are innocent bystanders parked on a dead producer (process survives via the Cocoa thread, hence exit=124 not a crash) · CORRECTION to my previous entry: "lost wakeup/deadlock" was the wrong interpretation of a correct measurement — one thread dies, everything else parks behind it · CORRECTION: my W^X + eviction fixes DID change the failure mode (earliest arms died exit=5 within a second of first dispatch; every arm after the W^X fix reaches +48s, loses one thread to c000007b, then hangs exit=124) and I wrongly filed them together as "still wedging identically" — should have separated the modes by exit code · STALE MEMORY corrected: WAIT_WAKE_TRACE/ARM_FILE from project_hb_two_futex_queues do NOT exist in this tree (grep over engine/ returns nothing, same for TRACE_WAITADDR/rtl_wait_on_address) · c000007b-in-Mono matches project_monojit_target_validation_unified_root (validator rejects unregistered indirect-call targets); HYPOTHESIS that chaining skips the dispatcher visit which registers such targets, untested · next: find what maps a runtime failure to c000007b on the run-exit path (not raised literally in hb_validate.c/macrunner_hb.c), then gate it and rerun chaining
LOOP-STATUS: CONTINUE chaining wedge named — main guest thread dies c000007b inside mono-2.0-bdwgc.dll at +48s and the 7 helper threads merely park behind it; next is locating the emitter of that status and gating it
2026-07-30 21:35 +10 · SPEED-DIG · c000007b turned out to carry NO information — macrunner_hb.c:39761 sets STATUS_INVALID_IMAGE_FORMAT/"runtime" for ANY failed HyperBridge run, and the informative ERR() above it was invisible because the run script sets WINEDEBUG=-all (suppresses ERR, keeps raw fprintf markers); my Mono-validator reading from last pass was built on that empty code and is WITHDRAWN · re-ran with MACRUNNER_HK_WINEDEBUG=err+all and the failure names itself: `MacRunner HyperBridge run failed thread pc=0x87ef2469a30 ret=OK out=MEMORY_FAULT reason=JIT helper fault` followed by BaseThreadInitThunk thread-callback-failed — the main thread dies on a GUEST MEMORY FAULT INSIDE A JIT HELPER, not on any image/target rejection · CONTROL RUN (same binary, same err+all, only MACRUNNER_HB_CHAIN_PATCH differing) establishes attribution rather than asserting it: emit-side-only reaches Initialize engine version AND Begin MonoManager with ZERO "run failed" lines, full chaining reaches only Mono path with 1 — so the helper fault occurs only when tails are patched; the first-chance c0000005 at 0x1119DA440 appears ONCE IN BOTH arms so it is pre-existing background and is excluded as a suspect, likewise the two c0000135 LdrLoadDll failures · MACRUNNER_HB_TRACE_JIT_HELPER_FAIL printed nothing (0 macrunner-hb-block-fault-pc lines in both arms), so this fault does not reach the ctx->last_result branch that gate gu 
ards — it arrives via the signal-guard path setting out->faulted · BLOCK_CHAIN stays default-off; import-scan fix untouched · next: print the faulting guest ADDRESS where set_helper_fault_result is actually reached, and map it against the chained edge
LOOP-STATUS: CONTINUE chaining failure named and ATTRIBUTED by control run — patched tails cause a guest memory fault inside a JIT helper that kills the main thread (out=MEMORY_FAULT), while c000007b and the c0000005 both proved to be noise; next is the faulting address
2026-07-30 21:45 +10 · SPEED-DIG · removed a real latent hazard: emit_return_if_helper_failed emits a full emit_epilogue INSIDE the block and every epilogue carried a 4-NOP chain slot, so for any block whose LAST epilogue was the failure one, patch_block_tail would have patched the FAILURE path (continuing into a successor instead of returning with ctx->last_result set); mid-block epilogues now emit no slot via emit_epilogue_ex(buf,false) · REFUTED as the cause, and airtight because PATCHED=11003 is BIT-IDENTICAL to the previous run (site_called 36018 vs 36028, already 25015 vs 25025) — if any block had ended with a helper-failure epilogue the chainable population would have shrunk, so no block here ends that way and the change is a no-op in this workload; kept anyway since it closes the hazard for differently-shaped blocks · the fault is highly DETERMINISTIC, which is the real lead: same guest pc 0x87ef2469a30 with identical rcx=0xf0e0993f rdx=0x320eec31 across two runs (only rax differs), same out=MEMORY_FAULT reason="JIT helper fault" · TALLY: five mechanism guesses now killed on this wedge (chained cycle/backedge/self-chain, trampoline W^X bracket, arena exhaustion, eviction literal store, mid-block epilogue slot) while the only two things that produced knowledge were direct observations — the verified sample (all threads parked, main thread absent) and re-enabling err (out=MEMORY_FAULT) — so I am not proposing a sixth · BLOCK_CHAIN stays default-off; import-scan fix untouched · next: observation only — resolve module+RVA of 0x87ef2469a30 (the fail-bt named mono-2.0-bdwgc.dll rva=0x3cc250 but that is the 0x87ef27c region, a DIFFERENT module from the failing pc), and log chained edges filtered to that address to name the exact edge that breaks the guest
LOOP-STATUS: CONTINUE mid-block-epilogue chain slot fixed but refuted as the cause (PATCHED unchanged at 11003 proves it was inapplicable); fault is deterministic at guest pc 0x87ef2469a30 — next step is observation only, resolving that pc and the chained edge reaching it
2026-07-30 22:00 +10 · SPEED-DIG · MY ERROR corrected: the helper-fault trace "printed 0 lines" only because I set MACRUNNER_HB_TRACE_JIT_HELPER_FAIL in the CONTROL arm (which does not fault) and then reported its absence from the TEST arm; set in a faulting arm it fires at once — `block-fault-pc: pc=0x87ef2469a30 last=-8 guest_addr=0x87ef2469a30`, and pc==guest_addr means the fault lands on the block's FIRST memory access · resolved the pc with no run: fail-bt val=0x87ef246adc2 rva=0x6adc2 fixes the base at 0x87ef2400000, so the faulting block is mono-2.0-bdwgc.dll+0x69a30 and every backtrace frame is in that same DLL (Boehm GC — the same subsystem as the 7 parked AssetGarbageCollectorHelper threads) · replaced the first-64 chainedge cap with an address filter (new MACRUNNER_HB_CHAIN_EDGE_NEAR) and got the first direct tie between the chain mechanism and the fault site: the faulting block IS A CHAIN TARGET 3x and never a source (as next:3, as cur:0) — edges 0x87ef246aee7->0x2469a30, 0x87ef246adb6->0x2469a30, 0x87ef246adc9->0x2469a30, all via one per-target trampoline 0x2000086a0840, and TWO of those predecessors are exactly the frames the failure names (fail-bt[3] val=0x87ef246aee7 and run-exit-prev pc=0x87ef246adb6) · OBSERVATION unexplained: chained sequences routinely exit with ctx->pc at SYNTHETIC IMPORT addresses (pc_after=0x6f0000002240 / 0x6f00000024e0, IMPORT_BASE=0x6f0000000000) after 2-6 blocks; should be benign since the thunk is not a cached block and the O(1) index resolves it, but I have not verified that path equals reaching the same thunk unchained · BLOCK_CHAIN stays default-off; import-scan fix untouched · next: dump rcx/rdx immediately before and after the chained transition into 0x2469a30 (the two registers identically garbage in every failing run) to separate "chaining corrupts them" from "chaining merely reaches a block already doomed"
LOOP-STATUS: CONTINUE faulting block identified as mono-2.0-bdwgc.dll+0x69a30 and proven to be a CHAIN TARGET whose predecessors are the failure backtrace itself; next is dumping rcx/rdx across that one transition
2026-07-30 22:15 +10 · SPEED-DIG · PROVEN by direct before/after + control comparison that block chaining CORRUPTS GUEST REGISTERS: the chained transition landing on the faulting block is `chaintransit NEAR from=0x87ef246adb6 pc_after=0x87ef2469a30 blocks=2 steps=71` with rcx 5000000000000->f0e0993f and rdx 11d66db60->320eec31 — post-values identical to what the failure reports (rcx=0xf0e0993f rdx=0x320eec31) in every run · the CONTROL (same binary, CHAIN_PATCH=0, new per-block entry probe) shows what they should be: over 8 dispatches of that exact block rcx=0x11dc1d9d0/0x11dc1dbb0 and rdx=0x11dc1d9ec/0x11dc1dbcc with rdx=rcx+0x1c, i.e. TWO GUEST STACK POINTERS into the caller's frame (rsp=0x11dc1d9xx), and that arm reaches Begin MonoManager with ZERO "run failed" lines · so chained the block receives 32-bit non-addresses and faults on its first memory access, which kills the main thread and parks the 7 GC helpers behind it — every element of that chain of custody is now measured (transition, before/after regs, unchained baseline, fault site mono-2.0-bdwgc.dll+0x69a30, thread states) rather than inferred · BLOCK_CHAIN stays default-off; import-scan fix untouched (Restored language 828.7s -> 448.4/490.3s) · next: bisect INSIDE that one chained run — (a) log each block boundary within it so the intermediate block is named (2 blocks / 71 steps is suspiciously many instructions and the count comes from the emitted counter, unverified), (b) refuse to chain from that single predecessor and see whether the fault vanishes or merely relocates, which separates a block-pair defect from a general chaining defect
LOOP-STATUS: CONTINUE chaining proven to corrupt guest rcx/rdx (control shows they should be stack pointers rdx=rcx+0x1c; chained they arrive as 32-bit non-addresses) so the GC block faults on first access; next is bisecting inside that 2-block/71-step chained run
2026-07-30 22:25 +10 · SPEED-DIG · ran the pre-registered discriminator MACRUNNER_HB_CHAIN_REFUSE_NEAR=1 (declines every edge in the ±64KB window around the faulting block: refused_near=130241, chaining still fully active elsewhere with PATCHED=13933 avg_chain=3.5817): the MEMORY FAULT VANISHED (HyperBridge-run-failed count 0, no block-fault-pc at all) but the boot still did not progress (Mono path only, exit=124) — and the counters show a STALL not slow progress: 800,000 dispatches with dispatch-stats stopping at wall_s=7.09 and nothing for the remaining ~230s, against 482,000,000 dispatches over 172.6s in the CHAIN_PATCH=0 control · so chaining has TWO independent fatal effects (register corruption on some edges, removable in isolation; and execution stopping outright), and per my pre-registration the defect is NOT that block pair but general to chaining · DECISION: park chaining and return to the profile — seven mechanism hypotheses proposed and refuted on it (cycle/backedge/self-chain, trampoline W^X, arena exhaustion, eviction literal store, mid-block epilogue slot), two fatal modes still live, and its avg_chain 5.2-5.9 has never had an observable SPEED effect because no chaining arm ever reached steady state; meanwhile the post-import-fix profile has an untouched 15.1% item (_platform_memmove = the 1592-byte per-dispatch context snapshot in run_jit_block_with_signal_guard) plus hb_jit_runtime_run 12.5%, the guard 10.6%, _tlv_get_addr 8.8%, guest MMU ~10% — contained single-mechanism targets of the same kind as the import scan that already delivered 828.7s -> 448.4/490.3s · BLOCK_CHAIN stays default-off with all gates/instruments left in place so resuming costs nothing · next: measure how many of those 1592 bytes the fault path actually needs and how often a fault consumes the snapshot (brief records ZERO faults on the critical thread), then narrow the copy behind a gate and A/B on the marker
LOOP-STATUS: CONTINUE refusing the suspect edges removed the fault but the run still stalls at 800k dispatches vs 482M in control, so the chaining defect is general — parking chaining default-off and attacking the measured 15.1% per-dispatch context snapshot next
2026-07-30 22:40 +10 · SPEED-DIG · ★ SECOND WIN: per-dispatch context snapshot 2616 -> 760 bytes · CORRECTION first: I had said the snapshot copies 1592 bytes, but MACRUNNER_HB_SNAPSHOT_SKIP_INTERP is DEFAULT OFF, so every run this week took `*dst = *src` and copied the FULL 2616 — the profile's 15.1% _platform_memmove was the whole-struct cost, i.e. another already-written optimisation that had simply never been armed (same pattern as BLOCK_CHAIN/INDIRECT_IC) · widened the skip window from xmm_ext to ymm_hi after an EXHAUSTIVE safety check: the interpreter-only AVX region is ymm_hi[640,896)+zmm_hi[896,1408)+opmask k[1408,1472)+xmm_ext[1472,1728)+ymm_hi_ext[1728,1984)+zmm_hi_ext[1984,2496) = one unbroken 1856-byte run, and the COMPLETE set of offsetof(hb_context_t,..) in hb_arm64_codegen.c (block_count, flags, guest32_base, indirect_ic_guest_addr, indirect_ic_native_code, last_result, lazy_flags, pc, step_count + the regs union) all lies OUTSIDE [640,2496); ymm_hi/zmm_hi/k are referenced 0 times in the emitter · MEASURED within one run: critical-thread _platform_memmove self time 81/536=15.1% -> 16/498=3.2%, and run_jit_block_with_signal_guard's own self 10.6% -> 7.8% · wall clock, all new bests: Restored language 828.7 -> 448.4/490.3 -> 369.009s, Mono phase 284.2 -> 150.5/135.5 -> 109.7s, vs a 593±78s baseline (369.0s is 1.61x under the baseline mean, 2.25x under this lane's start) with ZERO run-failed lines and 758M dispatches over 478s · flipped the gate default ON (0->1) and redeployed ntdll 3b9ab74bb6e9aef4; marker time is n=1 so the load-bearing evidence is the profile share not the clock, and =0 restores the full copy in one env var · next: the profile moved — now hb_jit_runtime_run 12.7%, _tlv_get_addr 8.2% (pure overhead, lazy TLS accessor), guard 7.8%, find_region_normalized 6.8% + hb_memory_read 6.4% (MMU pair = 13.2%, largest group), guest code 5.0%, dispatch_stats_add 2.4% (my own instrument — disable for a clean baseline)
LOOP-STATUS: CONTINUE second win delivered and deployed — per-dispatch snapshot 2616->760 bytes cut memmove 15.1%->3.2% and took Restored language to 369.0s (best; baseline 593±78s); next target is the MMU pair at 13.2% or _tlv_get_addr at 8.2%
2026-07-30 22:55 +10 · SPEED-DIG · CORRECTION to my own last entry: the snapshot fix's WALL-CLOCK claim does not survive n=2 — a second post-snapshot run (CLEANPROF, same deployed binary, instruments off) reached Restored language at 457.049s vs the 369.009s I reported, so post-snapshot is 369.0/457.0 (mean 413s) against post-import-fix 448.4/490.3 (mean 469s), an ~12% difference sitting well INSIDE the documented ±78s spread; I had labelled the marker n=1 but also wrote "1.61x under the baseline" and that comparison was not supportable · defensible statement: the snapshot change is proven by the PROFILE (_platform_memmove 15.1% -> 3.2%/5.1%) and its effect on time-to-menu is not yet resolvable against variance (Mono phase likewise 109.7s then 139.0s) · CLEAN critical-thread profile (instruments off, 534 samples): hb_jit_runtime_run 17.0%, hb_flags_read_operand_value 7.7%, hb_memory_read 5.4%, guard 5.1%, _platform_memmove 5.1%, translated guest code 5.1%, _tlv_get_addr 4.9%, _platform_memset 3.7%, find_region_normalized 3.0%, hb_memory_write 2.1%, hb_context_write/read_reg_value 1.9% each, extend_operand_lazy 1.7% — groups: dispatch machinery 30.9%, MMU 10.5%, my own dispatch_stats* 0 · quantified the contamination the previous profile carried: _tlv_get_addr was 8.2% over 22 callers of which 11/41 samples were my dispatch_stats instrument, plus its own 2.4% self; clean it is 4.9% spread over ~20 callers incl. a long tail of macrunner_hb_*_probe_*_block per-block probes, so there is no single fix there · NEXT TARGET chosen on clean data: the lazy-flags group = hb_flags_read_operand_value 7.7% + hb_context_read_reg_value 1.9% + hb_context_write_reg_value 1.9% + extend_operand_lazy 1.7% = 13.2%, reached from hb_jit_helper_eval_cond_lazy which the emitter calls for EVERY Jcc — and this lane measured Jcc at 71.1% of all dispatched terminals, so the histogram and the profile now agree from opposite directions; hb_jit_runtime_run's 17.0% is bigger as a single symbol but it IS the dispatcher loop, so cutting it means fewer dispatches i.e. chaining, which is parked · next: read what hb_flags_read_operand_value does per call, count Jcc helper calls per dispatch and which cc codes dominate (one gated counter, one run), then judge whether common conditions can be evaluated inline in ARM64 instead of calling into C
LOOP-STATUS: CONTINUE clean uncontaminated profile obtained; snapshot clock claim retracted as within-variance (369.0/457.0 vs 448.4/490.3) while its profile proof stands; next target is the 13.2% lazy-flags group behind every Jcc
2026-07-30 23:05 +10 · SPEED-DIG · instrumented hb_flags_read_operand_value by operand type and measured 560,000,001 calls on the critical thread: reg 189,184,228 (33.8%), imm 41,781,127 (7.5%), MEMORY 329,034,646 (58.8%) — stable at 58.7x% across the last three reports — so the majority of these operand reads go through mem_read_size -> hb_memory_read -> find_region_normalized, which is why the MMU sits at 10.5% right beside this function's 7.7% · CORRECTION before it propagates: I introduced the counter as "size the lazy-flags re-read" and wrote that those 329M memory reads "exist purely to re-materialize x86 flags" — WRONG attribution. Checking callers instead of assuming: every caller outside hb_flags.c is in hb_arm64_codegen.c (L5339,5349,5642,5821,5847,5900,5903,6299,6354,6382,6405,6434), i.e. the hb_jit_helper_exec_* family that emitted code calls when the JIT could NOT emit an operation natively; hb_lazy_flags_note already takes VALUES (lhs,rhs,result) so materialisation does not re-read. The number is real, the label was wrong · the finding is therefore LARGER than what I was chasing: a very large number of guest instructions are still executed by C helpers rather than emitted ARM64, which is the brief's own "hot loops still interpreted — dig there" arriving via the helper path, and it explains the clean profile without invoking chaining (flags 7.7% + MMU 10.5% + context_read/write_reg_value 3.8% + extend_operand_lazy 1.7% are all the cost of not having emitted the instruction) · BLOCK_CHAIN still default-off; snapshot fix deployed (ntdll 922213c31d2ee210 now carries the flagops counter too, gated off by default) · next: this fits the standing BULK-OVER-REACTIVE rule exactly — the op set is finite (163 ops in hb_ir.h) with the interpreter as reference, so add one gated histogram keyed by instr->op at the helper entry points, rank helper invocations by op from a single run, emit the top ones natively, and re-run the same counter to measure the coverage shift (a matrix, not a per-failure fix)
LOOP-STATUS: CONTINUE measured 560M JIT-helper operand reads with 58.8% going through the software MMU, and corrected my own mis-attribution of them to flag re-materialisation; next is a per-IR-op histogram of helper invocations to pick native-emission targets in bulk
2026-07-30 23:25 +10 · SPEED-DIG · built the ranked helper-op COVERAGE MATRIX the bulk-over-reactive rule asks for: 152,000,001 helper invocations on the critical thread, six ops = 99.3% — STORE(46) 64,164,260 42.2%, SIGN_EXTEND(79) 20,584,762 13.5%, MULSS(183) 20,525,952 13.5%, CVTTSS2SI(191) 20,430,848 13.4%, LOAD(45) 19,723,877 13.0%, FADD(174) 5,136,561 3.4%, then CMP/ZERO_EXTEND/IMUL/CALL/DIV/SUB all <=0.3%; LOAD+STORE = 55.2%, matching the independently measured 58.8% of operand reads coming from guest memory · MY ERROR caught by a compile-time check: my first decoding of these op numbers came from a regex over hb_ir.h and named the head PUSH/CMPS/COMISS/VEC_PACKED — wrong by 1-3 indices (compiler says HB_IR_STORE=46, PUSH=47, CMPS=82, COMISS=186, VEC_PACKED=194), so I rebuilt the map by emitting one printf per enum name and compiling it (271 entries); the counts were always right but I nearly spent a pass emitting native code for PUSH, an op that never appears · the head lands on a lever the harness explicitly disables: hk-run-try12-config.sh:100 forces MACRUNNER_HB_JIT_DIRECT_MEM=0 unless MACRUNNER_HK_FORCE_JIT_DIRECT_MEM is set, which is why LOAD/STORE reach a C helper; armed it (direct_mem=1 echoed, 0 faults) and FOUR helper op classes went to exactly ZERO — SIGN_EXTEND, MULSS, CVTTSS2SI, FADD (46.3M baseline calls' worth) — a qualitative class elimination, not a rate · CONFOUND stated: the LOAD/STORE totals are NOT comparable between the two runs (152M->424M, LOAD 19.7M->225.2M) because this run progressed FURTHER in less time (UnloadTime +177.059s vs +262.313s, Mono phase 120.5s) and the counters are cumulative, so no direction is claimed for LOAD/STORE · next: re-run both arms with MACRUNNER_HB_TRACE_DISPATCH_STATS=1 to normalise helper calls per DISPATCH (the same fix that turned avg_chain from a comparison into a single-run number), then the direct-mem default is decidable on evidence and the remaining LOAD/STORE head is the native-emission target
LOOP-STATUS: CONTINUE helper-op coverage matrix built and op names verified compile-time after my regex mis-decoded them; arming the disabled direct-mem lever zeroed four helper classes, but LOAD/STORE totals are phase-confounded so next step is normalising per dispatch
2026-07-30 23:40 +10 · SPEED-DIG · normalised the helper metric so phase drops out (helper calls / dispatches, both from the same run) and it REFUTES the direct-mem lever: direct_mem OFF = 160,000,001 helpers / 486,000,000 dispatches = 329.2 per 1000 dispatches (68.1 per 1000 guest steps), direct_mem ON = 352,000,001 / 504,000,000 = 698.4 per 1000 (144.3 per 1000 steps) — arming it is 112% WORSE · per op per 1000 dispatches it trades four eliminated classes (SIGN_EXTEND 44.6->0, MULSS 44.5->0, CVTTSS2SI 44.2->0, FADD 11.1->0) for large increases in integer work (LOAD 42.7->367.4 = 8.6x, STORE 139.0->203.7, CMP 1.0->63.5, IMUL 0.4->26.8) plus new ops only present with it on (op4 6.1, TEST 4.3, op19 2.8, op156 2.6) · this REFUTES the standing expectation in project_hk_directmem_lever_20260628 ("JIT direct-mem is default-OFF so loads/stores hit the interpreter lazy-helper", read as an opportunity): on this workload the harness default MACRUNNER_HB_JIT_DIRECT_MEM=0 is the BETTER setting by 2.1x, so the default is left alone; the ON arm's shorter Mono phase (193.1s vs 223.3s) is n=1 inside a ±78s spread and is NOT treated as evidence · it also settles last iteration's confound honestly: the direction the raw totals suggested turns out to be real, but it could not be known until the denominator existed · NATIVE-EMISSION TARGET LIST now fixed and ranked for the shipping configuration (direct_mem OFF), per 1000 dispatches: STORE 139.0 (42% of all helper calls), SIGN_EXTEND 44.6, MULSS 44.5, CVTTSS2SI 44.2, LOAD 42.7, FADD 11.1 — six ops = 326.1 of 329.2 = 99% of helper traffic, with verified op numbering and a phase-independent unit · next: read the HB_IR_STORE case in hb_arm64_codegen.c to find which operand shapes it declines to emit (MACRUNNER_HB_JIT_DIRECT_SCALAR_MEM/_SCAN are already on by default, so something specific makes STORE fall through), emit the common shapes natively, and re-run the same normalised counter to measure the coverage shift without any cross-run clock claim
LOOP-STATUS: CONTINUE normalised helper metric built (329.2 vs 698.4 per 1000 dispatches) and it refutes the direct-mem lever, so the default stays off; native-emission target list is now ranked with STORE at 42% of all helper traffic
2026-07-30 23:50 +10 · SPEED-DIG · found WHY STORE+LOAD reach a C helper: a fifth default-off gate. The emitter has a native path for both (hb_arm64_codegen.c:4563 for STORE, same predicate in LOAD) guarded by jit_native_mem_ir_enabled() = MACRUNNER_HB_JIT_NATIVE_MEM_IR, default 0, set by NO script in the tree (only docs/ACTIVE-INVESTIGATION.md mentions it, as =0); the sibling predicate direct_user_mem_allowed -> jit_direct_scalar_mem_enabled() IS on by default, so this one flag routes STORE 139.0 + LOAD 42.7 = 181.7 of 329.2 per 1000 dispatches = 55% of ALL helper traffic into C · ARMED IT AND IT BREAKS THE GUEST: exit=29, c000001d (illegal instruction) at +52.3s with macrunner-hb-seh-nonmod-stk dumps and ZERO of the three boot markers — so unlike the snapshot window this is not correct-but-unarmed, the native memory emission produces bad code, which is presumably why it has been off; the 55% cannot be reclaimed by flipping a flag, the emitter must be fixed · CORRECTION caught immediately: the per-op "-> 0.0" column in my own comparison was a DIVISION ARTIFACT (run died before any dispatch report, so dispatches=0 and every per-1000 figure printed 0.0, reading exactly like "all helper traffic eliminated") — a normalised metric needs its denominator checked for zero before the ratio is believed, same discipline the avg_chain denominator needed · UNIT SUITE CLEAN so the defect needs the real guest: hb_test_runner 3 runs per arm gives 449/35 stable fails with the flag off and 447/35 with it on, failures unique to the ON arm = 0 — same pattern as block chaining, stated so the next attempt does not begin by re-running the suite · FIVE LEVERS NOW CATALOGUED: BLOCK_CHAIN works (avg_chain 5.2-5.9) but breaks the guest two ways, parked; INDIRECT_IC still never armed; SNAPSHOT_SKIP_INTERP correct+unarmed, now widened and DEFAULT ON (second win); JIT_DIRECT_MEM armed and measured WORSE (698.4 vs 329.2 helpers/1000 dispatches), default correctly off; JIT_NATIVE_MEM_IR is the big one at 55% and is BROKEN (c000001d) · next: find the illegal instruction the native memory path emits — compile blocks through the emitter with NATIVE_MEM_IR=1 for the operand shapes the suite misses (indexed, large displacement, sub-register sizes, the x22-clobber case the XMM store at :4548 already documents as a real corruption bug) and disassemble against the helper path's semantics; emit_direct_mem_store_from_x20_tso / emit_direct_mem_addr is where to look
LOOP-STATUS: CONTINUE located the gate behind 55% of helper traffic (JIT_NATIVE_MEM_IR, default off, never set) but arming it breaks the guest with c000001d at +52s and the unit suite is clean, so the next step is finding the bad emission rather than flipping the flag
2026-07-31 00:00 +10 · SPEED-DIG · ruled out the unpatched-branch theory (emit_b_deferred emits B #0 = a HANG not an illegal instruction; and the SEH dump bytes are x86 `48 89 85 ..`, so c000001d is the GUEST executing garbage after corruption, not a malformed ARM64 stream) — noted a latent trap anyway: emit_direct_mem_store_from_x20_tso patches its second branch only `if (done_branch)` while emit_b_deferred returns buf->size, so offset 0 would silently skip the patch (unreachable today because every block starts with a prologue) · FOUND a genuine statically-proven defect: RIP-relative operands took the direct path with a STALE RIP. is_direct_user_mem_operand admits any base below HB_REG_XMM0 and compile-time values are HB_REG_RIP=16, HB_REG_XMM0=17, so RIP passed and reached emit_direct_mem_addr which has no RIP case and emitted `ldr x21,[x19,#reg_off(RIP)]`, reading ctx->regs.x64.rip as an ordinary GPR — that field is synced only at block boundaries so mid-block it is stale, while x86 RIP-relative addressing is defined against the NEXT instruction's address, which the sibling emit_direct_mem_addr_for_instr computes correctly and which only 2 of 21 call sites use; since RIP-relative access is how x86-64 reaches globals this fires constantly. Excluded RIP (base and index) from the direct path so those fall back to the helper — correct by construction, kept regardless · REFUTED as the blocker: NATIVEMEM-NORIP still dies identically (exit=29, c000001d, no boot markers, no dispatch report), so the native memory path has at least one more hole · PATTERN worth naming: four times now a statically-proven defect has turned out not to be the blocker (mid-block epilogue slot, eviction literal store, unbracketed trampoline build — that one WAS — and now RIP-as-GPR); reading code finds real bugs here but has a poor record at finding THE bug, whereas bisection with the guest as oracle has worked every time · next: bisect the operand SHAPES rather than hunt more defects — gate the native path to the narrowest useful shape (size==64, no index, disp in [0,4096)) and widen in steps (add index, add large disp, add sub-register sizes), one 300s run per arm with the same normalised counter; the first surviving arm gives a working subset and by difference names the breaking shape, the technique that localised chaining to a single edge in two runs
LOOP-STATUS: CONTINUE found and fixed a real RIP-as-GPR defect in the direct-mem address path but it is not the blocker (c000001d unchanged), so switching from code-reading to shape bisection with the guest as oracle
2026-07-31 00:12 +10 · SPEED-DIG · ★ SHAPE BISECT LOCALISES THE NATIVE_MEM_IR CRASH TO SUB-64-BIT MEMORY OPERANDS. Added MACRUNNER_HB_JIT_NATIVE_MEM_SHAPE (0=unrestricted default) and walked the levels, one run each: SHAPE=1 (64-bit, NO index, disp<4096) BOOTS — Begin MonoManager + UnloadTime, exit=124, c000001d=0, 580,000,000 dispatches; SHAPE=2 (+index) BOOTS — 576,000,000; SHAPE=3 (+arbitrary displacement) BOOTS — 564,000,000; SHAPE=0/4 unrestricted (the only added clause being op->size != HB_SIZE_64) CRASHES — no markers, exit=29, c000001d=1, 0 dispatches · so 8/16/32-bit memory operands are the breaking shape and index registers + large displacements are INNOCENT, the opposite of where the XMM path's x22 comment pointed · this is what the bisect was for: four rounds of code-reading each produced a real defect that was not the blocker, three runs of population-halving localised it; mechanism now has an obvious shape (a narrow store must write only its low bytes, a narrow load must extend correctly, so emit_direct_mem_store_from_x20(buf,size) mishandling those sizes would put full 64-bit values into memory = writes landing wrong = guest executes garbage = c000001d) but that is a HYPOTHESIS until the emitted narrow-size code is read; the localisation itself is measured · CAVEAT on the helper numbers from these arms: I put the shape filter in direct_user_mem_allowed which has 32 call sites, so SHAPE=1 narrowed every op using it and helper traffic rose 329.2 -> 924.1 per 1000 dispatches (LOAD 42.7->427.4, CMP 1.0->107.5, ZERO_EXTEND 0.5->87.1) — that rise is an artifact of my gate's breadth and says nothing about NATIVE_MEM_IR's value; the crash/no-crash result is unaffected, which is why the bisect still works · next: read emit_direct_mem_store_from_x20 for HB_SIZE_8/16/32 (STRB/STRH/STR W vs a full 64-bit STR?) and the load side's extension, fix, then run NATIVE_MEM_IR=1 with SHAPE=0 and compare helpers/1000 dispatches against the 329.2 baseline — that number decides whether the 55% of helper traffic is reclaimable
LOOP-STATUS: CONTINUE shape bisect localised the NATIVE_MEM_IR crash to sub-64-bit memory operands (64-bit with index and arbitrary displacement all boot cleanly at ~570M dispatches); next is reading and fixing the narrow-size store/load emission
2026-07-31 00:20 +10 · SPEED-DIG · NARROWED THE NATIVE_MEM_IR CRASH TO THE NARROW-SIZE STORE PATH, and refuted two more code-reading hypotheses along the way: (a) "narrow stores emit a full 64-bit STR" is FALSE — emit_stlr_from_reg honours size exactly (STLRB/STLRH/STLR Wt/STLR Xt); (b) "narrow loads zero the destination's upper bits" is FALSE — emit_store_x20_to_gpr_sized uses STRB W20 / STRH W20 at reg_off+reg_offset (so AH-style sub-registers work) and zero-extends 32-bit via UBFM+STR X, exactly x86 semantics · RE-FRAMES the previous size bisect: the LOAD case has its OWN extra gate `src1.size != HB_SIZE_64 || jit_native_mem_ir_qword_loads_enabled()` and MACRUNNER_HB_JIT_NATIVE_MEM_IR_QWORD_LOADS is ALSO default 0, so in SHAPE=1..3 the 64-bit loads still went to the helper and those arms were really testing native STORES — "sub-64-bit breaks it" was ambiguous between narrowness and the load side · added MACRUNNER_HB_JIT_NATIVE_MEM_SIDE (0 both, 1 stores only, 2 loads only) and ran stores-only with sizes unrestricted: it CRASHES (exit=29, c000001d, no markers) while SHAPE=3 (64-bit only, both sides) BOOTS — so the defect is in the narrow-size (8/16/32-bit) native STORE path and the LOAD side is exonerated, established in two runs with no code-reading needed for the conclusion · TALLY: six code-reading hypotheses refuted on this defect (unpatched deferred branch, RIP-as-GPR which was a real bug but not the blocker, full-64-bit narrow store, narrow-load upper-bit clobber, plus two earlier chaining ones) while every localisation that stuck came from bisection — recording it as a reliable prior for this codebase · next: bisect WHICH narrow width breaks by extending the shape levels to 64+32 then 64+32+16 with SIDE=1; HB_SIZE_8 is the structural suspect because direct_mem_alignment_mask(HB_SIZE_8) returns 0, making byte stores the only size with no alignment check and no helper fallback emitted, so they take the inline STLRB unconditionally
LOOP-STATUS: CONTINUE crash localised to the narrow-size native STORE path (stores-only reproduces it, 64-bit-only boots, load side exonerated); next is bisecting which of 8/16/32-bit breaks, with byte stores the structural suspect
2026-07-31 00:35 +10 · SPEED-DIG · ★ NATIVE_MEM_IR CRASH FULLY LOCALISED TO THE INLINE BYTE STORE, address path exonerated · width bisect (stores only): 64-bit only BOOTS (564M dispatches), everything-except-8-bit BOOTS (364M), all widths incl. 8-bit CRASHES (exit=29, c000001d) — so HB_SIZE_8 stores are the defect · REFUTED a seventh code-reading hypothesis on the way ("the 8-bit source register's reg_offset is ignored for AH/BH/CH/DH"): emit_load_gpr_sized_to_reg computes off = reg_off(op->reg) + op->reg_offset and emits LDRB from there, exactly as the writeback side does · FINAL CUT used a diagnostic that already existed, MACRUNNER_HB_FORCE_LAZY_STORE=1, which keeps emit_direct_mem_addr inline but routes the store through hb_jit_helper_store_sized: all widths with the INLINE store crashes (c000001d), all widths with inline ADDR + helper store BOOTS (552M dispatches, Begin MonoManager + UnloadTime, 0 illegal instructions) — so the inline ADDRESS COMPUTATION IS CORRECT and the INLINE BYTE STORE INSTRUCTION is what breaks the guest; not arithmetic, not the encoding (STLRB Wt,[Xn] = 0x089ffc00|rn<<5|rt, verified), but the fact that the inline store skips whatever write_u64_tso does around the write · HYPOTHESIS now targeted rather than speculative: the helper performs SMC / translation-cache invalidation on writes into pages holding translated code, and Mono's code patching + write barriers are exactly byte-store traffic, which would explain why only this width shows it while aligned 16/32/64-bit inline stores are fine — that is a read of ONE function with a named question · SHIPPABLE SUBSET: NATIVE_MEM_IR=1 with 8-bit stores excluded boots, but its speed value is UNMEASURED because my shape gate sits in direct_user_mem_allowed (32 call sites) and inflates the helper metric; measuring it needs the exclusion moved into the STORE case itself, so nothing is defaulted on yet · TALLY: seven code-reading hypotheses refuted, five bisection arms and five decisive answers — the defect went from "arming a flag kills the boot" to "the inline byte store, address path fine" in five runs
LOOP-STATUS: CONTINUE inline byte store identified as the NATIVE_MEM_IR defect (inline-addr + helper-store boots with 552M dispatches, proving the address path correct); next is reading write_u64_tso for the SMC invalidation the raw STLRB skips
2026-07-31 00:48 +10 · SPEED-DIG · read what the store helper does that the raw store does not: hb_jit_helper_store_sized -> hb_jit_helper_write_u8_tso calls hb_memory_host_ptr(ctx->memory, addr, size, HB_PERM_WRITE) — guest->host translation WITH a permission check — then an Apple hb_jit_live_host_ptr fallback, then the full software hb_memory_write_u8; the inline path does STLRB W20,[X21] where on x64 x21 IS the guest address (emit_x86_ea_to_host returns immediately unless arch==HB_ARCH_X86), so it works only because guest VA == host VA and performs NO permission check and NO software fallback, and byte stores are the only width that can never reach that helper because their alignment mask is 0 · applied the byte exclusion in the predicate (all 32 call sites) and it does exactly what it should to store traffic: STORE 139.0 -> 39.5 per 1000 dispatches (-72%), MULSS/CVTTSS2SI/FADD still eliminated, so the native store path demonstrably works for 16/32/64-bit · ★ BUT the run still crashes, which exposes a LOGIC ERROR in my previous entry: I wrote that the side bisect "exonerated" loads — it did not. SIDE=1 (stores only) crashing shows stores are SUFFICIENT to crash, not that loads are safe, and the arm I called proof-of-boot (SHAPE=6) ALSO carried SIDE=1, so it had the native LOAD path switched off entirely; its success was "native stores, no bytes, NO LOADS". With loads re-enabled and bytes excluded the guest dies again (exit=29, c000001d, Begin MonoManager only, 324M dispatches). Sufficient != necessary and I conflated them · next: the arm I never ran — SIDE=2, loads only — which tests the load path independently and is the missing cell of the table; if loads alone crash then NATIVE_MEM_IR has two independent defects and none of the 55% is reclaimable until the load side is fixed too · KEEPING the byte-store guard and its comment (mechanism measured, FORCE_LAZY_STORE booting proves the address arithmetic is fine) with NATIVE_MEM_IR still default-off so shipping behaviour is unchanged
LOOP-STATUS: CONTINUE byte-store guard cuts STORE helper traffic 139.0->39.5 but the run still crashes, and I corrected my own logic error (stores being sufficient to crash never exonerated loads); next is the loads-only arm I never ran
2026-07-31 01:05 +10 · SPEED-DIG · ran the missing cell and COMPLETED the NATIVE_MEM_IR map: each side is safe ALONE but the pair is not — baseline off boots (329.2 helpers/1000 disp), stores-only all-widths CRASHES, stores-only no-bytes BOOTS, LOADS-ONLY no-bytes BOOTS (338.0), BOTH sides no-bytes CRASHES (790.1). So it is an INTERACTION, not a per-side defect: the shape of shared state (x20/x21/x22 or deferred lazy-flag materialisation that each side relies on the other's helper path to perform) — recorded as the open question rather than chased by reading after seven refutations · ★ DECISIVE NUMBER: the best SAFE configuration is worth 2.2%. Arming the load side with bytes excluded plus MACRUNNER_HB_JIT_NATIVE_MEM_IR_QWORD_LOADS=1 (a further never-armed gate found while reading the LOAD case) boots cleanly: total 329.2 -> 322.1, LOAD 42.7 -> 34.3 (-20%), STORE unchanged. Helper traffic is ~13% of the critical thread, so that is ~0.3% of thread time — measurable but far too small to justify defaulting a gate that crashes in three of its five configurations · CONCLUSION: the "55% of helper traffic" was addressable only on paper. The histogram was right that LOAD+STORE are 55% of helper CALLS; the runs establish that the emitter can absorb only a fraction — byte operands must stay on the helper (the inline path has no permission check and no software fallback) and the two sides cannot both be native. NATIVE_MEM_IR stays default-off; the byte guard and diagnostic gates stay in · NEXT: back to the clean profile where the remaining size is — hb_jit_runtime_run 17.0% (the dispatcher loop; reducing it means fewer dispatches = chaining, parked), guest-MMU group find_region_normalized + hb_memory_read + hb_memory_write = 10.5% (largest actionable item), hb_flags_read_operand_value 7.7%, _tlv_get_addr 4.9%. find_region_normalized already has a treap plus a per-thread MRU hot cache, so measure that cache's HIT RATE and how often hot_cache_reset_tls wipes it (it clears every slot whenever the region epoch changes, and Mono maps/unmaps constantly) — one gated counter, one run
LOOP-STATUS: CONTINUE NATIVE_MEM_IR measured to exhaustion — safe subsets exist but the best is worth only 2.2% of helper traffic (~0.3% of thread time) and both sides together crash, so it stays default-off; next is the guest-MMU hot-cache hit rate at 10.5%

### 2026-07-31 — MMU: negative cache lands; an ABA use-after-free found on the way

- Epoch churn REFUTED as the hot-cache problem: 0.04 resets per 1k lookups over 808 M lookups.
- Capacity REFUTED by a 64-deep LRU shadow priced in-run: 32 slots would give 76.04 % vs the real 75.82 %.
- 97 % of misses resolve to NULL — ~166 M lookups/run (~25 % of all) for addresses no region contains, each
  paying a 16-slot scan plus a failed treap walk over ~24 000 regions.
- Fix: negative (gap) cache built from the failed walk's predecessor/successor bracket, 8 per-thread entries,
  gated `MACRUNNER_HB_NO_NEG_CACHE`. Measured on a live boot: **154.98 M gap hits vs 155.89 M null misses =
  99.4 % of failed walks eliminated**.
- Required a real correctness fix: region adds never bumped `hot_gen`. That also exposed an ABA hazard the
  POSITIVE cache already had — a reused `hb_memory_t` address with `hot_gen` restarting at 0 matches stale TLS
  entries holding pointers to freed regions. `hb_memory_create` now seeds `hot_gen` from a global counter.
- Unit suite 3+3 arms on my own gate: stable regressions NONE (19370 now fails only in the off arm = ASLR noise).
- Wall-clock NOT established — the instruments-off sample landed at t≈153 s, not the baseline's ~240 s. Stated
  as open rather than claimed.
- Uncommitted for the coordinator: `hb_memory.c` (new), plus `hb_arm64_codegen.c`, `hb_flags.c`, `hb_runtime.c`.

LOOP-STATUS: CONTINUE negative cache eliminates 99.4% of failed treap walks (154.98M/run over a ~24000-region tree) and fixed an ABA use-after-free in the positive cache; wall-clock A/B at equal guest-relative offsets is the next step

### 2026-07-31 (later) — negative cache: correct, but measured slower; default flipped to opt-in

- Measured the mechanism's scale in-run: `nodes_per_failed_walk=17.2` × 158.3 M failed walks = **2.72 G
  dependent cache-cold loads/run**, 99.4 % of which the gap cache removes.
- A/B at an identical guest-relative offset (+164 s), same build: ON is WORSE — `find_region*` 4.05 % vs
  1.96 %, dispatches at 300 s 532 M vs 560 M. Flipped the gate to opt-in `MACRUNNER_HB_NEG_CACHE=1`.
- Cause identified in my own code: a second `__atomic_load_n(ACQUIRE)` per miss on the shared `hot_gen` line,
  plus region adds WRITING that line (the `clear_hot_cache` I added last pass) — coherence ping-pong across
  54 threads. Fixed by splitting `hot_gen` (positive) from a new `add_gen` (negative): adds can't invalidate a
  region, removes can't invalidate a gap. One acquire load per resolve now.
- Correctness verified independently: `MACRUNNER_HB_VERIFY_NEG_CACHE` re-walks the treap on every gap hit —
  **0 violations in 3 suite runs**. RETRACTED my own "stable regression 19316": it appears in 2/3 runs, i.e.
  suite ASLR noise, n=3 too few.
- Uncommitted: `hb_memory.c`, `hb_memory.h`, `hb_arm64_codegen.c`, `hb_flags.c`, `hb_runtime.c`.

LOOP-STATUS: CONTINUE negative cache proven correct (0 verify violations) and proven large (2.72G loads removed) but measured 5% SLOWER at equal guest-relative offset, so it ships opt-in; the coherence cost that explains it is now fixed by splitting hot_gen/add_gen and the re-A/B is the next step

### 2026-07-31 (III) — split-epoch A/B: regression fixed, but the win is a spin, not speed

- Split-epoch build reverses every profile number vs the previous A/B: `find_region*` 0.00 % ON vs 0.63 % OFF
  (was +2.09 pts the wrong way), MMU group −0.94 pts, `hb_memory_read` −1.74 pts. The coherence diagnosis
  (duplicate acquire loads + adds writing the shared `hot_gen` line) was correct.
- Dispatch rate ON 2355 k/s vs OFF 2052 k/s = **+15 %** — `total_dispatches` verified to be the GLOBAL atomic
  accumulator, so this is comparable across arms.
- BUT work-per-progress kills it: three arms reach `UnloadTime` on ~460 M dispatches (440/468/472); the ON arm
  burned **644 M and never reached it**, still running at 2355 k/s at budget end. No crash, no fault pins.
- Signature of a spin: the 166 M null lookups look like a probe/retry loop that was rate-limited by the failed
  walk. Making it cheap buys iterations, not progress. Alternative (variance) is live — the two OFF arms alone
  differ by 37 s on `UnloadTime` — n=1 cannot separate them. Default stays opt-in.
- Caveat recorded: the arms' hottest threads were different named threads, so per-symbol shares compare
  different work; only the process-wide rate is a clean cross-arm number.
- Uncommitted: `hb_memory.c`, `hb_memory.h`, `hb_arm64_codegen.c`, `hb_flags.c`, `hb_runtime.c`.

LOOP-STATUS: CONTINUE split-epoch fix reversed the regression (find_region 0.00% vs 0.63%, +15% dispatch rate) but the ON arm burned 644M dispatches vs ~460M for the same marker and never reached it, so the saving is feeding a spin rather than progress; next step is a guest-PC histogram of the null lookups to find what issues them

### 2026-07-31 (IV) — null lookups localized: a double-resolve in our own miss path, now removed

- Guest-PC histogram over 112.9 M nulls: top block ≤4 % (1.5 % measured, evicted=3.36 M) → the nulls are
  DIFFUSE. Last pass's spin explanation for the 644 M-dispatch anomaly is REFUTED.
- Call-site attribution: `hb_memory_find_region` = 47.8 % of nulls at a **94.7 % null rate**; external probes
  (`jit_codegen`, `interp`) zero. Arithmetic closes exactly: its 63.0 M nulls = read 49.9 M + write 12.3 M.
  Every failed read/write resolves the same address twice — directly, then again via `can_read_span` →
  `check_perm_span`, which bails on its first null and so can only return false when region is already NULL.
- Removed it (gate `MACRUNNER_HB_NULL_SPAN_RECHECK=1` restores). Per dispatch: `find_region` lookups
  0.1868 → 0.0120 (**−93.6 %**), total region lookups 1.573 → 1.402 (**−10.9 %**), nulls 63.0 M → 0.88 M.
- Suite 5 runs per arm (not 3): stable 33 both, **NONE introduced**. Default ON.
- NOT claiming speed: rate +9.4 % but neither run reached `UnloadTime` in 260 s, and rate ≠ progress per the
  negative-cache result.
- Uncommitted: `hb_memory.c`, `hb_memory.h`, `hb_arm64_codegen.c`, `hb_interpreter.c`, `hb_flags.c`,
  `hb_runtime.c`.

LOOP-STATUS: CONTINUE nulls localized to a double-resolve in our own read/write miss path (63.0M = read 49.9M + write 12.3M, provably-NULL recheck) and removed — find_region lookups/dispatch -93.6%, total -10.9%, no suite regressions at n=5; next is re-A/B'ing the negative cache which may now be obsolete

### 2026-07-31 (V) — negative cache was CORRUPTING MEMORY ANSWERS on the real workload; fixed and verified

- Third replication of the ON-arm signature (higher rate, ~650 M dispatches, never reaches `UnloadTime`;
  3/3 OFF arms reach it) made it a pattern, not noise.
- Ran the verify pass on a live HK boot for the first time — it had only ever run in the SINGLE-THREADED unit
  suite: **2118 violations, 267 distinct addresses**, the gap cache calling mapped memory unmapped.
- Root cause is my own epoch split: not every add goes through `insert_region_head`. The VM-map sync path
  appends directly to `mem->regions` + `rebuild_region_tree`, bumping only `hot_gen`, so regions appeared
  inside cached gaps with nothing to invalidate them.
- Fix: `clear_hot_cache` bumps `add_gen` too (conservative). **Re-verified live: 2118 → 0**, and that arm
  reached `UnloadTime` at 227.9 s vs 260.0 s before.
- RETRACTS pass (III)'s "spin" explanation — the ON arms were executing on corrupted lookups, not spinning.
- Shipping profile now: MMU group **10.10 %**, `find_region*` 1.98 %; largest non-dispatch item is
  `_tlv_get_addr` at **7.5 %**.
- Uncommitted: `hb_memory.c`, `hb_memory.h`, `hb_arm64_codegen.c`, `hb_interpreter.c`, `hb_flags.c`,
  `hb_runtime.c`.

LOOP-STATUS: CONTINUE negative cache was UNSOUND on the real workload (2118 verified violations from the VM-map sync path bumping only hot_gen, invisible to the single-threaded unit suite) — fixed via clear_hot_cache bumping add_gen, re-verified 2118 to 0; next target is _tlv_get_addr at 7.5% of the critical thread

### 2026-07-31 (VI) — static ceiling measured: RA is worth ≤9 %, per-block overhead is 34 %

- Per the updated brief, did the STATIC first step with zero HK runs: decoded 1 096 350 real emitted blocks
  (37 825 816 ARM64 instructions) from a persisted translation cache.
- `emitted = 11.8 + 7.2 × guest_instructions`. Fixed per-block overhead **34 % of all emitted code**;
  guest-GPR memory traffic **9.0 %** (0.99 per guest instruction, not the 4 the brief assumed); XMM ctx 7.0 %;
  rip/rflags ctx traffic zero.
- Blocks are tiny: mean **3.14** guest instructions, 44.8 % hold ≤2.
- Conclusion inverts the brief's ordering: RA must flush at every unchained block boundary, so with
  3.14-instruction blocks its flush traffic is comparable to what it removes. Chaining/larger blocks attack
  34 % AND are the precondition for the 9 %.
- Report: `reports/research/SPEED-DIG-STATIC-REGISTER-TRAFFIC-20260731.md`.
- Carried over unmeasured: a TLS-address hoist in `hot_cache_lookup/insert` is built and deployed
  (ntdll 251b37ac) but its measuring run was interrupted — treat as unverified.
- Uncommitted: `hb_memory.c`, `hb_memory.h`, `hb_arm64_codegen.c`, `hb_interpreter.c`, `hb_flags.c`,
  `hb_runtime.c`.

LOOP-STATUS: CONTINUE static ceiling measured on 1.1M real blocks — register allocation is worth at most 9.0% of emitted instructions (0.99 guest-GPR accesses per guest instruction, not 4) while fixed per-block overhead is 34%, and with 3.14-instruction blocks RA's mandatory boundary flushes eat most of its own win, so chaining/larger blocks should come first

### 2026-07-31 (VII) — fixed overhead identified: a callee-saved frame that 48.3 % of blocks do not need

- Modal prologue (4 instrs, 100 % of blocks) + epilogue (5 instrs, 63.9 %) = 9 instructions, of which **8 are a
  callee-saved register frame = 12 memory accesses per dispatch** on 3.14-guest-instruction blocks.
- All guest-register traffic is 3.1 accesses/block, so the frame costs ~4× the register-allocation target.
- **48.3 % of blocks (529 347) make ZERO calls** (calls/block is only 0 or 1, mean 0.52) — for those the frame
  preserves registers against a call that never happens.
- Eliding it there: **6 352 164 memory accesses = 1.87× the entire RA ceiling**; 4 234 776 instructions =
  **11.2 % of all emitted code**.
- Feasible: a call-free block needs 5 scratch registers and AAPCS offers 7 caller-saved (x9–x15) requiring no
  preservation; epilogue collapses to `STR x21,[x19,#544]; RET`.
- Revised lane ordering: (1) elide frame on call-free blocks 11.2 %; (2) chaining/larger blocks; (3) register
  allocation 9.0 %.
- Zero HK runs this pass (the brief's static step); last completed run CLEANPROF2 classified BLOCKED /
  GENERIC_GRAPHICS_FAULT, exit=124, reaches UnloadTime.

LOOP-STATUS: CONTINUE the 34% fixed per-block overhead is a callee-saved frame (12 memory accesses per dispatch) and 48.3% of blocks make zero calls so never need it — eliding it there is worth 6.35M memory accesses and 11.2% of emitted code, 1.87x the entire register-allocation ceiling, and is the next implementation step

### 2026-07-31 (VIII) — lean-frame preconditions verified; implementation designed

- All 529 347 call-free blocks: **0** touch the stack outside the frame, **0** write x24–x28, **0** write x30,
  **0** end in anything but `RET`. Body needs ≤5 registers; AAPCS offers 7 caller-saved (x9–x15).
  → a call-free block can run with **no frame at all**.
- Design settled: remap belongs in the **42 encoder primitives** (`rmap[32]` on the codegen buffer, identity by
  default), not at the 596 hard-coded call sites. Relocations are recorded inside a primitive, so applying the
  remap before `relocs[].reg = rd` keeps patching consistent with no extra pass.
- Call-free detection via two-pass emit; re-emitting ~52 % of blocks costs ~0.35 s against the 0.69 s codegen
  budget already measured and refuted as a cost.
- Zero HK runs this pass (static work per the brief). Last completed run CLEANPROF2: BLOCKED /
  GENERIC_GRAPHICS_FAULT, exit=124, reaches UnloadTime.

LOOP-STATUS: CONTINUE lean-frame preconditions all verified on 529347 call-free blocks (zero stack use, zero x24-x28, zero x30, all end in RET) and the implementation is designed — remap in the 42 encoder primitives before reloc recording, two-pass emit, gated MACRUNNER_HB_LEAN_FRAME; next pass writes it and verifies statically from the cache dump

### 2026-07-31 (IX) — lean frame built and gated; two of my own bugs found; baseline restored

- Implemented frameless blocks behind `MACRUNNER_HB_LEAN_FRAME`: `rmap[32]` applied in the 31 leaf encoders
  (composites excluded to avoid double substitution), frame skipped when armed, two-pass re-emit if a call
  appears. Relocations verified to be recorded AFTER the remap, so patching stays consistent.
- Static verification from a real cache: 81.8 % frameless, **0** frameless blocks with a call, **0** still
  using x19 as ctx base (136 on the remapped x11).
- **Bug 1 (fixed):** mapping x19→x9 collided with `emit_mov_reg(buf, 9, 20)` at `:3453` and overwrote the ctx
  pointer. Moved to x11–x15.
- **Bug 2 (fixed):** `hb_codegen_buffer_t` is never zero-initialised, so `rmap_active` read uninitialised stack
  and armed the remap **with the gate off**, breaking the default path. Now guarded by a file-scope flag only
  `lean_frame_arm()` sets, plus an 0xA5 sentinel. Baseline re-verified: MonoManager 56.2 s, **UnloadTime
  152.2 s** — the fastest today.
- **All lean-frame timing results this pass are VOID** (they ran on the broken build). Also recorded: enabling
  the translation cache to get a dump changed two variables at once; the control run exposed it.
- Uncommitted: `hb_arm64_codegen.c`, `hb_codegen.h`, `hb_memory.c`, `hb_memory.h`, `hb_interpreter.c`,
  `hb_flags.c`, `hb_runtime.c`.

LOOP-STATUS: CONTINUE lean frame implemented and statically verified (81.8% frameless, zero with calls, zero stale x19 bases) but its timing runs are VOID because an uninitialised rmap_active armed the remap even with the gate off and broke the default path — fixed with a file-scope flag plus 0xA5 sentinel, baseline restored at UnloadTime 152.2s; next pass redoes the measurement on the fixed build

### 2026-07-31 (X) — lean frame measured cleanly: it breaks the boot; bisection is next

- Valid A/B at last (same build, only the env var differs): baseline reaches MonoManager 56.2 s and
  **UnloadTime 152.2 s**; with `MACRUNNER_HB_LEAN_FRAME=1` the run produces 0.28 MB and **no markers at all**.
- Unit suite 5 runs per arm on the fixed build: **no** stable failures introduced → the defect is on a path the
  suite does not exercise (signal/fault or dispatch), not in block lowering.
- Emitted code verified correct and can be excluded from the search: 81.8 % frameless, 0 frameless blocks with
  a call, 0 with a stale x19 ctx base (136 on x11), relocations recorded after the remap.
- Latent but NOT the cause: `hb_runtime.c:3010/:3162` identify the block tail by exact encoding
  (`MOV X0,X19`, `LDP X19,X20,[SP],#48`). Both are in the chaining machinery (default-off); they must learn
  about frameless blocks before chaining + lean frame can coexist.
- Gate stays OFF; baseline re-verified on this build.
- Uncommitted: `hb_arm64_codegen.c`, `hb_codegen.h`, `hb_memory.c`, `hb_memory.h`, `hb_interpreter.c`,
  `hb_flags.c`, `hb_runtime.c`.

LOOP-STATUS: CONTINUE lean frame now measured cleanly and it BREAKS the boot (baseline UnloadTime 152.2s vs no markers at all) while the unit suite shows nothing and the emitted code verifies correct, so the defect is on the signal/fault or dispatch path; next step is the two-way bisect — remap-only with the frame kept, which is semantically valid and isolates which half is at fault

### 2026-07-31 (XI) — bisect result: the register remap is at fault, frame elision exonerated

- New gate `MACRUNNER_HB_LEAN_REMAP_ONLY` (remap applied, frame still emitted — semantically valid) splits the
  change. Baseline: UnloadTime **152.2 s**. Frame-elided: no markers. **Remap-only: no markers too** → the
  defect is the remap, not the frame elision.
- Mechanism confirmed: `hb_arm64_codegen.c:4527` builds `ADD x20,x20,x21,LSL#s` inline via `emit_u32` with
  literal register numbers, bypassing the leaf encoders where `hb_rm` lives. 57 expression-built `emit_u32`
  sites exist; a grep only finds literal-digit ones.
- My earlier "0 stale x19 bases" check was too narrow — it inspected only the `Rn` field of loads/stores, so
  `Rd`/`Rm` bypasses were invisible. The audit has to scan every register field of every emitted instruction.
- Also caught by the artifact gate: an `hb` build failed (rc=2) while wine relinked the old library; the
  `strings` check reported gate=0 and blocked a meaningless measurement.
- Gate stays OFF; baseline verified on this build.

LOOP-STATUS: CONTINUE bisect isolates the fault to the register remap itself (remap-only with the frame kept also produces no markers, while baseline reaches UnloadTime 152.2s) — cause is emitters building encodings inline via emit_u32 with literal registers, bypassing the leaf encoders; next is an empirical all-register-field scan of a remap-only cache dump to enumerate every bypass

### 2026-07-31 (XII) — remap fixed and now complete; boots again

- Two bypass classes fixed: (1) an inline `emit_u32` encoding at `:4527`, routed through `emit_add_reg_lsl`;
  (2) **12 leaf encoders my own transform silently skipped** — all declared
  `static void __attribute__((unused)) emit_…`, which my regex's character class excluded. The tool reported
  "31 transformed" and looked fine.
- Found by scanning the EMITTED ARTIFACT (decode every register field of every instruction in remapped blocks),
  not by re-reading source — the source grep had already produced a wrong answer twice.
- **Scan now returns zero survivors**: 47 705 remapped blocks, 1 098 656 instructions decoded, no x19–x23
  references (caveat: 5.9 % of forms undecoded).
- remap-only now boots: **MonoManager 55.4 s, UnloadTime 210.5 s** (baseline 56.2 s / 152.2 s). Slower at n=1
  but inside today's 152–245 s spread — not a speed verdict yet.
- Uncommitted: `hb_arm64_codegen.c`, `hb_codegen.h`, `hb_memory.c`, `hb_memory.h`, `hb_interpreter.c`,
  `hb_flags.c`, `hb_runtime.c`.

LOOP-STATUS: CONTINUE remap now complete and booting (zero x19-x23 survivors across 47705 remapped blocks) after fixing 12 leaf encoders my own transform silently skipped because they carry __attribute__((unused)); next is re-testing the full lean frame, the half that carries the 11.2%, on this sound remap

### 2026-07-31 (XIII) — lean frame works end to end; timing inconclusive at n=2

- Full lean frame on the sound remap: **boots**, both markers reached, no new failure class.
  UnloadTime lean 150.6 / 229.0 s vs baseline 152.2 / 218.3 s → means 189.8 vs 185.3, within-arm spread 66–78 s.
  **No speed claim** — separating ~11 % from that noise needs ~8 runs per arm.
- Unit suite (5 runs/arm) clean; remap verified complete (0 x19–x23 survivors over 47 705 remapped blocks).
- Next measurement is within-run per Rule 1: mean emitted instructions per block, lean vs baseline, from cache
  dumps over the same guest addresses — confirms or refutes the predicted 11.2 % without touching the clock.
- Operational: the Bash tool caps at 10 min and killed a 4-run loop mid-flight; one run per call now. No
  orphans left (mr-clean verified 0 wine procs); the partial run dir was discarded, not read.
- Uncommitted: `hb_arm64_codegen.c`, `hb_codegen.h`, `hb_memory.c`, `hb_memory.h`, `hb_interpreter.c`,
  `hb_flags.c`, `hb_runtime.c`.

LOOP-STATUS: CONTINUE full lean frame now boots end to end with both markers and no new failure class, but timing is inconclusive at n=2 per arm (means 189.8 vs 185.3 s against a 66-78 s spread) so no speed claim is made; next is the within-run confirmation — mean emitted instructions per block from cache dumps of both arms

### 2026-07-31 (XIV) — lean frame delivers the predicted size reduction (11.03 % vs 11.2 % predicted)

- Within-run measurement (Rule 1): caches from both arms on the same build, intersected on identical cache
  keys → **53 414 identical guest blocks**. Mean emitted instrs/block **36.03 → 32.06 = −11.03 %**, total
  1 924 765 → 1 712 491, **0 blocks larger**, 66.2 % smaller.
- Static prediction made before the code existed was 11.2 % — measured 11.03 %.
- Cross-check: 3.10 guest instrs/block here vs 3.14 on the 1.1 M-block corpus.
- Wall-clock STILL unproven: UnloadTime n=2/arm, baseline 152.2/218.3 s vs lean 150.6/229.0 s, spread 66–78 s.
  Gate stays OFF; no speed claim.
- Uncommitted: `hb_arm64_codegen.c`, `hb_codegen.h`, `hb_memory.c`, `hb_memory.h`, `hb_interpreter.c`,
  `hb_flags.c`, `hb_runtime.c`.

LOOP-STATUS: CONTINUE lean frame verified within-run to cut emitted code 11.03% over 53414 identical guest blocks with zero blocks larger, matching the 11.2% predicted before it was written; wall-clock remains unproven at n=2 per arm against a 66-78s spread so the gate stays off pending a timing campaign or a move to chaining

### 2026-07-31 (XV) — cheap marker refuted as a discriminator; lean frame parked verified, moving to chaining

- Tried `Begin MonoManager` (≈55 s, ~1 s spread) as a cheap speed marker: baseline mean 55.15 s vs lean
  55.93 s → **no signal**. That marker is in the load-dominated prefix, not JIT-throughput-bound. Refuted
  experiment design, not a result about the change.
- Expected wall-clock effect computed: the frame is 6 of ~34.5 instructions, all L1 stack accesses; dispatcher
  is 16.0 % and guest code ~5 % of the critical thread → plausible **3–6 %**, i.e. 6–11 s on a 185 s mean
  against a 66–78 s spread. That needs ~30 runs/arm, not 8 — **not fundable**.
- **Lean frame parked**: gated, correct (unit suite clean, remap verified complete), and measured at
  **−11.03 % emitted code** over 53 414 identical guest blocks. No speed claim.
- Next target is the bigger item from the same corpus: **chaining** removes the dispatch round-trip
  (~20 % of the critical thread in dispatch machinery) and is the precondition for register allocation.
- Uncommitted: `hb_arm64_codegen.c`, `hb_codegen.h`, `hb_memory.c`, `hb_memory.h`, `hb_interpreter.c`,
  `hb_flags.c`, `hb_runtime.c`.

LOOP-STATUS: CONTINUE cheap-marker experiment refuted (Begin MonoManager shows no signal because it is load-bound, not JIT-bound) and the honest arithmetic puts the lean frame's wall-clock effect at 3-6% needing ~30 runs per arm to detect, so it is parked gated with its verified -11.03% code-size result; next target is chaining, which removes the ~20% dispatch round-trip and unblocks register allocation

### 2026-07-31 (XVI) — chaining: corrupted register identified as RBP (=0x8), instrument was watching rcx/rdx

- Static, free: the stack-leak hypothesis is **refuted** — the trampoline targets `native_code + 12`, skipping
  the successor's frame push and reusing the predecessor's frame (`hb_runtime.c:3276`).
- Same line pins the lean-frame conflict precisely: a frameless block's `+12` is mid-body, so chaining and
  `MACRUNNER_HB_LEAN_FRAME` are mutually exclusive until the chain target uses the real entry offset.
- Run with chaining + edge trace: 64 edges, 32 transits, then a **guest failure dump at +50.9 s** and a wedge
  to timeout. Control: that dump appears **0×** in baseline and lean runs → it belongs to chaining.
- Dump says **`rbp=0x8`** (garbage frame pointer) at `mono-2.0-bdwgc.dll rva=0x6adc2` — the same function as
  the 30.07 localisation (rva 0x6adb6). Same defect, corrupted register now named.
- **My trace was watching the wrong registers**: `chaintransit` prints only rcx/rdx, 0/32 looked wrong, and I
  nearly filed "not reproducing". Also recorded: `macrunner-hb-fail-rbp-chain` is the failure-backtrace
  frame-pointer section, NOT a chaining marker.
- Uncommitted: `hb_arm64_codegen.c`, `hb_codegen.h`, `hb_memory.c`, `hb_memory.h`, `hb_interpreter.c`,
  `hb_flags.c`, `hb_runtime.c`.

LOOP-STATUS: CONTINUE chaining defect advanced — the corrupted guest register is RBP (=0x8) at mono-2.0-bdwgc rva 0x6adc2, matching the 30.07 neighbourhood, and the failure dump is chaining-exclusive (0x in baseline and lean controls); my chaintransit trace only printed rcx/rdx which is why 32 transits looked clean, so next is adding rbp/rsp to it and catching the edge where rbp becomes 0x8

### 2026-07-31 (XVII) — rbp/rsp added to the chain trace; 21 transitions hit rbp=0x8, control still missing

- Extended `trace_chain_transition` with rbp/rsp and an **uncapped** alert (the old 32-line cap is why the
  first 32 transitions looked clean).
- **21 chain transitions land on `rbp=0x8`** — the exact value in the failure dump at mono rva 0x6adc2. The
  corruption is visible at a chain boundary, not just post-mortem.
- Not yet a verdict: the alert fired 7232× over 171 source blocks (2524 of them rbp→0), and normal transits
  show `rbp 1->1`, so small rbp occurs legitimately. **No unchained control exists** because the trace only
  runs with chaining on.
- Next: sample `block_delta == 1` (unchained) dispatches with the same instrument to get the in-run control
  rate — same run, same workload, same thread. That separates corruption from normal guest behaviour.
- Uncommitted: `hb_runtime.c` (+rbp/rsp trace), `hb_arm64_codegen.c`, `hb_codegen.h`, `hb_memory.c`,
  `hb_memory.h`, `hb_interpreter.c`, `hb_flags.c`.

LOOP-STATUS: CONTINUE chain trace now carries rbp/rsp and catches 21 transitions landing on exactly rbp=0x8, matching the failure dump at mono rva 0x6adc2, but the alert fired 7232 times over 171 blocks and small rbp occurs legitimately so it cannot convict without an unchained control; next is sampling block_delta==1 dispatches with the same instrument to get that control rate inside one run
