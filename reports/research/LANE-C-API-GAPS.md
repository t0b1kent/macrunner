# Lane C API Gaps

Running ledger for Win32/NT loader, virtual memory, and DLL API gaps found while bringing up real applications.

Lane C owns fixes in `engine/wine/dlls/ntdll/unix/loader.c`, `engine/wine/dlls/ntdll/unix/virtual.c`, and the real DLL implementations under `engine/wine/dlls/**`.

Do not edit `engine/wine/dlls/ntdll/unix/macrunner_hb.c`, `engine/wine/dlls/ntdll/unix/signal_arm64.c`, `engine/hyperbridge/**`, or `engine/dxmt/**`. If a required API belongs in the HyperBridge thunk dispatcher, record it here for Lane A/coordinator.

## Open Gaps

- 2026-06-04: x86_64 PE DLL relinks for some updated Wine DLLs can fail on the existing `___chkstk_ms` unresolved-symbol blocker. The WinMM ACM alias source fix compiles for x86_64 but `reports/build/lane-c-winmm-msacm-alias-rebuild-20260604103126.log` stops while linking `winmm.dll` at `dlls/winmm/x86_64-windows/mci.o:(MCI_SendCommand)` with `undefined symbol: ___chkstk_ms`, matching the earlier `msvcrt.dll` relink class. This blocks installing the x64 `winmm.dll` artifact from that checkpoint until the toolchain/runtime stack-probe symbol route is fixed; the aarch64 artifact was rebuilt/installed and dynamically validated.
- 2026-06-04: `ws2_32!WSAIoctl` static body scans can surface unobserved multiplexer cases from any app importing the API. A focused GUP probe (`reports/lane-c-npp-gup-wsaioctl-probe-20260604101017/summary.txt`) did not call `WSAIoctl` in the 20s run (`wsaioctl_lines=0`), so do not patch `SIO_UDP_CONNRESET`, `SIO_ENABLE_CIRCULAR_QUEUEING`, or the default unknown-ioctl path from static import evidence alone. If a future app/game dynamically hits `SIO_UDP_CONNRESET`, the real behavior needs socket/AFD state to control UDP ICMP reset delivery; record/route any server or thunk boundary need before changing the DLL facade.
- 2026-06-04: Minimal x64 CLI fixture (`fixtures/x64/hello_x64.exe`) cannot currently serve as a Lane C pass/fail gate because it stops before user output in the same excluded SEH continuation loop. Evidence: `reports/lane-c-cli-hello-x64-20260604095922/summary.txt` has `rc=143`, empty stdout, `missing_imports=0`, `fixme_stub=0`, `hb_runtime_fail=0`, and repeated `macrunner-hb-seh-invalid: reason=unwind-metadata-missing` / `RtlRaiseStatus status=c0000026`. No loader/virtual/real-DLL body gap is exposed.
- 2026-06-04: WOW64 `memory_x86` after the ARM64 ID-register fallback cleanup still does not reach the 32-bit/WOW64 layer. Evidence: `reports/lane-c-wow64-memory-post-arm64-id-reg-20260604095647/summary.txt` times out at 45s with `wow64=0`, `missing_imports=0`, `fixme_stub=0`, `core_id_stub=0`, `fourgb_err=0`, one expected 4GB skip trace, and no `UNSUPPORTED`/`macrunner-hb-runtime-fail`; the repeating marker remains `macrunner-hb-seh-invalid: reason=unwind-metadata-missing` with `RtlRaiseStatus status=c0000026`. This leaves the current WOW64 continuation blocker outside Lane C after the Lane-C-owned VM and ntdll stub markers are clean.
- 2026-06-04: Win7 Calc runtime after the NTDLL SQM and MSVCRT locale checkpoints no longer exposes a Lane-C-owned DLL/API gap. The active dist was updated with rebuilt x64/aarch64 `ntdll.dll` exports (`reports/build/lane-c-install-rebuilt-ntdll-pair-20260604095151.log`), then `scripts/mr-run.sh` launched `artifacts/phase-h/win7-calc-rebased2/calc.exe` in `reports/lane-c-win7-calc-runtime-post-dll-breadth-20260604095217/`. The run times out at 45s (`rc=143`) with `missing_imports=0`, `fixme_stub=0`, `sqm=0`, `msvcrt_locale=0`, and `hb_runtime_fail=0`; the repeated marker is `macrunner-hb-seh-invalid: reason=unwind-metadata-missing` followed by `RtlRaiseStatus status=c0000026`. This is an excluded SEH/runtime continuation blocker, not a loader/virtual/real-DLL body fault. `msvcrt.dll` dist relink remains blocked by existing `___chkstk_ms` toolchain/runtime linkage (`reports/build/lane-c-calc-dll-rebuild-x64-llvm-mingw-20260604094815.log`), but the source-level wrappers and static owning-DLL scan are clean.
- 2026-06-04: WOW64/x86 runtime remains blocked before `BTCpu`/`cpu_simulate` after the 4GB VM source fix can be built but not installed as a matched runtime pair. Evidence before the fix: installed-dist `memory_x86` probe `reports/lane-c-wow64-memory-post-npp-dll-breadth-20260604092000/memory_x86.log` reached `wow64.dll`/`wow64win.dll` mapping (`wow64=34`) and exposed Lane-C-owned `virtual.c` failures at the 4GB boundary (`mmap() error Cannot allocate memory, range 0x100000000-...`) while still showing `btcpu=0`/`cpu_simulate=0` and no unsupported/API markers. Source fix build validation passes, but matched runtime validation is blocked because `engine/wine/build-arm64ec-spike` has no `dlls/ntdll/ntdll.dll` target (`reports/build/lane-c-wow64-4gb-probe-skip-arm64ec-pe-ntdll-20260604092519.log`); a Unix-`ntdll.so`-only swap (`reports/lane-c-wow64-memory-post-4gb-skip-20260604092356/memory_x86.log`) no longer reaches WOW64 and is not a valid behavioral gate for this fix.
- 2026-06-04: Fresh post-VM WOW64 `memory_x86` probe with the rebuilt ARM64EC Unix `ntdll.so` still does not reach the 32-bit/WOW64 layer. Evidence: `reports/lane-c-wow64-memory-post-vm-20260604083843/memory_x86.log` times out after 60s with rc=143, 12 `macrunner-hb-ldr-init` markers, 41 ARM64EC relocation markers, and zero `Wow64`/`BTCpu`/`cpu_simulate`/BOP markers; there are no `UNSUPPORTED`, unimplemented/missing import, `status=c...`, `macrunner-hb-runtime-fail`, or Lane-C-owned `virtual.c` failure signatures. This remains a pre-WOW64 ARM64EC loader/runtime continuation blocker outside Lane C; do not patch VM/DLLs without a new Lane-C-owned failing signature.
- 2026-06-03: ARM64 Phase 3 DLL breadth probe after remote-VM checkpoints did not isolate a new Lane-C-owned real-DLL body gap. `reports/lane-c-phase3-arm64-breadth-20260603232740/summary.tsv` shows `common_controls`, `msi_detection_fixture`, and `gdi_font` pass; several GUI/OLE/audio/input probes hit fresh-prefix 35s timeouts with no missing/unimplemented markers; `shell_known_folder` exits `2` after `SHGetFolderPath failed` while also logging `Unhandled alignment`, and a focused `+shell,+reg` rerun (`shell_known_folder_shelltrace.log`) times out in repeated `RtlRaiseStatus status=c0000026` without shell32/registry diagnostics. Do not patch shell32 on this evidence; classify the immediate blocker as the signal/runtime alignment path unless a shell32-local HRESULT trace appears.
- 2026-06-03: Post remote-VM-query WOW64 real-app probe (`artifacts/phase-h/npp-x86/notepad++.exe`) remains blocked before the 32-bit/WOW64 path reaches Lane C. Evidence: `reports/lane-c-phase5-npp-x86-post-vm-20260603232505/npp_x86.log` times out after 90s with rc=143, 39 `macrunner-hb-ldr-init` markers, ARM64EC relocation markers, and zero `Wow64`/`BTCpu`/`cpu_simulate`/BOP markers; no `UNSUPPORTED`, missing import, unimplemented API, or `macrunner-hb-runtime-fail` signatures. This confirms the current x86 real-app blocker is still the excluded pre-WOW64 ARM64EC loader/runtime continuation path, not `virtual.c` or a real-DLL body.
- 2026-06-03: Post remote-VM-query Phase 3 registry probe still does not expose a Lane-C-owned DLL/API gap. ARM64 comparison passes (`reports/lane-c-phase3-single-20260603232210/registry_arm64.log`: `registry ok`, rc=0), while x64 registry times out after 90s (`reports/lane-c-phase3-single-20260603232210/registry_x64.log`, rc=143) before fixture output, with no `UNSUPPORTED`, missing import, unimplemented API, or `macrunner-hb-runtime-fail`; focused markers are only `macrunner-hb-ldr-init` and ARM64EC PE relocation lines. This remains an excluded loader/runtime continuation blocker rather than `advapi32`/registry DLL body work.
- 2026-06-03: Current clean Phase 5 WOW64/x86 fixture probe is blocked before `wow64.dll` startup by the excluded ARM64 signal/runtime loader callback path. Evidence: after refreshing `engine/wine/dist-arm64ec-spike/lib/wine/aarch64-windows/ntdll.dll` from the clean source, `reports/lane-c-wow64-memory-clean-20260603230141/memory_x86.log` times out with rc=143 after repeated `macrunner-hb-ldr-init: phase=before/after-loader` and ARM64EC relocation markers, with no `Wow64LdrpInitialize`, `cpu_simulate`, `NtMapViewOfSection`, unsupported opcode, missing import, or real-DLL failure signature. Read-only source ownership check maps the emitted `macrunner-hb-ldr-init` markers to excluded `engine/wine/dlls/ntdll/signal_arm64.c`, so Lane A/coordinator must clear this pre-WOW64 continuation loop before Lane C can see further 32-bit VM/DLL gaps.
- 2026-06-03: x64 process-spawn fixtures are blocked by cross-arch CRT argv state, not Lane C `CreateProcess` real-DLL code. Evidence: `reports/lane-c-process-x64-debug-20260603144900/process_spawn_exe_x64.log` shows the fixture building `CreateProcessInternalW` cmdline `L"\"(null)\" --child"` before `CreateProcessA`, after x64 import-thunk registration for `__p___argv`/`__p___argc`; ARM64 self/env/cwd/cmd variants pass under `dist-pure-arm64` in `reports/lane-c-process-compare-arm64-20260603144312/summary.tsv`. The involved x64 CRT argv/import bridge lives in excluded `engine/wine/dlls/ntdll/unix/macrunner_hb.c`, so this needs Lane A/coordinator work before Lane C process API fixes are meaningful.
- 2026-06-03: x64 simple-DLL fixtures are blocked after successful Lane C loader/import work by cross-arch callback dispatch, not by a `LoadLibrary` or PE loader body gap. Evidence: `reports/lane-c-api-slice-x64-20260603151528/summary.tsv` shows `dll_import_x64` and `dll_loadlibrary_x64` timing out while the ARM64 comparison in `reports/lane-c-api-slice-arm64-compare-20260603151528/summary.tsv` passes both DLL fixtures; the focused x64 log `reports/lane-c-dll-load-x64-debug-20260603152118/dll_loadlibrary_x64.log` shows `simpledll_x64.dll` loaded, `MODULE_InitDLL` returning success, and `GetProcAddress(simple_add)` returning a guest target before repeated `macrunner_hb_dispatch_x64_callback rejected non-x64 callback target` and `RtlRaiseStatus status=c0000026`. The failing dispatcher lives in excluded `engine/wine/dlls/ntdll/unix/macrunner_hb.c`, so Lane C records this for Lane A/coordinator instead of patching loader code.
- 2026-06-03: x64 `CreatePipe` fixtures fail in the Lane A thunk path, not Lane C real DLL code. Evidence: `reports/lane-c-mr-api-pack-x64-202606030448/overlapped_file_io_x64.log` and `named_pipe_x64.log` both exit `2` with `CreatePipe failed`; the same real-DLL path passes for ARM64 in `reports/lane-c-mr-pipe-compare-202606030452/summary.tsv` (`named_pipe_arm64` rc=0, `named pipe ok`). Read-only audit shows x64 `CreatePipe` is handled in `engine/wine/dlls/ntdll/unix/macrunner_hb.c` (Lane A-owned), while `kernelbase/sync.c` has the standard Lane C real-DLL implementation.
- 2026-06-03: Phase 3 x64 DLL probe pack under `scripts/mr-run.sh` found no Lane-C-owned DLL fix in common controls/audio/XInput/controller smoke (`reports/lane-c-phase3-probes2-20260603-101631/summary.tsv`: all PASS). `shell_known_folder_x64` fails `SHGetFolderPathA(NULL, CSIDL_PERSONAL, ...)` after entering the native `SHELL32.dll!SHGetFolderPathA` import thunk; `+shell/+module/+seh` evidence in `reports/lane-c-phase3-shell-known-20260603-101505/` maps rejected callback targets to ARM64 PE `kernel32.dll`, `advapi32.dll`, `shlwapi.dll`, and `ucrtbase.dll`, with no `UNSUPPORTED_OPCODE` or missing DLL signature. This is a Lane A cross-arch callback-routing gap, not a shell32 function-body fix.
- 2026-06-03: Post-VM rebuild Phase 3 probe batches did not isolate a Lane-C-owned DLL body gap. `reports/build/lane-c-ntdll-pure-arm64-rebuild-20260603203925.log` rebuilt and codesigned the updated `ntdll.so`; x64 COM/shell/timer/network probe summaries in `reports/lane-c-phase3-probes3-20260603-204158/summary.tsv` and `reports/lane-c-phase3-noncom-20260603-204659/summary.tsv` mostly time out, while ARM64 comparisons in `reports/lane-c-phase3-arm64-compare-20260603-204425/summary.tsv` and `reports/lane-c-phase3-arm64-noncom-compare-20260603-204927/summary.tsv` reproduce the COM/timer timeouts or pass the real-DLL cases (`file_association_arm64`, `udp_loopback_arm64`). The focused x64 threadpool trace in `reports/lane-c-phase3-threadpool-seh-20260603-205032/threadpool_timer_x64.log` shows repeated `EXCEPTION_ACCESS_VIOLATION` dispatch at `00006FFFFFCB7184` plus the user-visible unhandled-alignment diagnostic, with no missing import/API signature; this points at the excluded cross-arch signal/runtime callback boundary rather than a fixable Lane C DLL implementation.
- 2026-06-03: Phase 4 Hollow Knight smoke after the ARM64EC spike `ntdll.so` target rebuild (`reports/build/lane-c-ntdll-arm64ec-spike-target-20260603210006.log`, rc=0; full `install` remains blocked by unrelated i386 `appwiz.cpl` `__alloca` link in `reports/build/lane-c-ntdll-arm64ec-spike-rebuild-20260603205615.log`) still does not expose a Lane-C-owned DLL/loader/VM API gap. The bounded run in `reports/phase4-hollow-knight/lane-c-20260603-210028-ntdll-spike180/` reaches Unity/Mono startup, then stdout reports Mono invalid-vtable assertions for `System.RuntimeType`/`MemberInfo:get_Name`; stderr records `macrunner-hb-runtime-fail ... module=mono-2.0-bdwgc.dll ... reason=JIT helper fault` and `macrunner-hb-run-exit ... status=c000007b`. The blocker is in the excluded HyperBridge/JIT runtime path, not a Wine DLL implementation Lane C can patch.
- 2026-06-03: Post-transfer-count Phase 3 probe sample under a fresh `mr-run.sh` prefix found no Lane-C-owned DLL/API gap. `reports/lane-c-phase3-post-vm-20260603-213913/summary.tsv` has `xinput_probe_x64` and `msi_detection_fixture_x64` PASS; `common_controls_x64` and `udp_loopback_x64` time out at x64 loader/runtime-cache startup with no `UNSUPPORTED`, missing import, unimplemented API, or Win32 DLL error marker in the bounded log tails. The first harness attempt in `reports/lane-c-phase3-post-vm-20260603-213855/` failed with rc=127 because the host shell lacks `timeout`; it is discarded as harness error.
- 2026-06-03: Phase 4 non-engine x64 app probe (`Калькулятор из Windows 7/Calc/calc.exe`) under a fresh `mr-run.sh` prefix found no Lane-C-owned gap. `reports/lane-c-phase4-nonengine-calc-20260603-214204/summary.tsv` times out after 35s, but bounded log markers show x64 loader `ProcessInit/ThreadInit` completed, `macrunner-hb-runtime-fail` count is zero, and there are no `UNSUPPORTED`, missing import, unimplemented API, or Win32 DLL error markers. This is not actionable in `loader.c`/`virtual.c`/real DLLs without a new failing signature.
- 2026-06-03: Phase 5 WOW64 x86 fixture sample after the regular NT transfer-count fix still does not expose a Lane-C-owned loader/VM/API gap. `reports/lane-c-phase5-wow64-post-vm-20260603-214337/summary.tsv` has `memory_x86`, `installer_like_x86`, and `registry_installer_x86` failing with rc=123 after `Wow64LdrpInitialize`; bounded markers show CPU module exports, `BTCpuProcessInit`, `BTCpuThreadInit`, and `Wow64LdrpInitialize before cpu_simulate`, with no `UNSUPPORTED`, missing import, unimplemented API, `status=c000`, or `macrunner-hb-runtime-fail` signature. The blocker remains the excluded x86 CPU/BOP continuation path, not `virtual.c` or a real DLL body.
- 2026-06-03: WOW64/x86 app probes (`lane-c-npp-x86-20260603-054654.log`, `lane-c-keepass-setup-20260603-054833.log`) both exit `123` immediately after `Wow64LdrpInitialize before cpu_simulate`; `NtMapViewOfSection` returns success-class `STATUS_IMAGE_NOT_AT_BASE (0x40000003)` and there are no `err:`, missing import, unsupported, or crash signatures. This does not point to a Lane C DLL/loader/VM API gap; needs Lane A/coordinator investigation of the x86 CPU/BOP continuation path if x86 bring-up is prioritized.
- 2026-06-02: `engine/wine/include/winnt.h` defines `MEM_RESET_UNDO` as `0x10000000`, but real Win32/llvm-mingw guests use `0x01000000`. Lane C runtime accepts both in `ntdll/unix/virtual.c`; header correction is outside Lane C's owned DLL/loader/virtual file set.

## Closed In Checkpoints

- 2026-06-04: NTDLL resource-language runtime refresh for Mac/user32 resources. A focused ARM64 Notepad++ installer probe (`reports/lane-c-npp-arm64-macdrv-resource-20260604103946/summary.txt`) reproduced 36 `macdrv:load_strings Failed to load string ID 0x0100..0x0114` diagnostics and 3 `menu:copy_sys_popup Unable to load default system menu` failures even though installed `winemac.drv`/`user32.dll` resources contained the required `RT_STRING`/`SYSMENU` entries. The active behavior was an installed `ntdll.dll` resource lookup mismatch, not a `winemac.drv` or `user32` resource-authoring bug: rebuilding/installing the current aarch64/ARM64EC `ntdll.dll` from existing `ntdll/resource.c` (`reports/build/lane-c-ntdll-resource-rebuild-sourceonly-20260604104622.log`, `make_rc=0`, `install_rc=0`) cleared the family. Validation: `reports/lane-c-npp-arm64-macdrv-resource-post-ntdll-20260604104639/summary.txt` reports `load_string_fail=0`, `loadstring_trace_fail=0`, `copy_sys_popup=0`, `module_load_fail=0`, and `unsupported=0`.
- 2026-06-04: WinMM ACM driver alias resolution in `winmm/driver.c`. ARM64 Notepad++ installer startup attempted `OpenDriver(L"msacm.imaadpcm"...)`, `msacm.msadpcm`, `msacm.msg711`, `msacm.l3acm`, and `msacm.msgsm610`; the modules actually ship as `imaadp32.acm`, `msadp32.acm`, `msg711.acm`, `l3codeca.acm`, and `msgsm32.acm` via the `Drivers32` aliases from `wine.inf`. `OpenDriver()` now skips direct module loading for `msacm.*` aliases and resolves them through `Drivers32`, avoiding false loader failures while preserving direct loads for real module names. Validation: `reports/build/lane-c-winmm-msacm-alias-compiledb-20260604103113.log` compiles `driver.c` for aarch64, arm64ec, x86_64, and i386; `reports/build/lane-c-winmm-msacm-alias-rebuild-20260604103126.log` rebuilds the aarch64 `winmm.dll` and records the separate x86_64 `___chkstk_ms` relink blocker; the installed aarch64 DLL is validated by `reports/lane-c-npp-arm64-installer-post-winmm-msacm-20260604103200/summary.txt` with `msacm_alias_load_fail=0` and `module_load_fail=0`.
- 2026-06-04: Common-Controls v6 SxS dependency fallback in `ntdll/actctx.c`. The ARM64 Notepad++ installer manifest requests `Microsoft.Windows.Common-Controls` v6 (`processorArchitecture="*"`, public key `6595b64144ccf1df`); throwaway prefixes can lack the physical WinSxS manifest early in startup, producing `fixme:actctx:parse_depend_manifests Could not find dependent assembly`. `lookup_assembly()` now preserves real WinSxS preference but falls back to Wine's bundled `comctl32_v6.dll` manifest from `windows\system32` for compatible v6 requests, creating the normal comctl32/window-class activation context instead of failing SxS generation. Validation: `reports/build/lane-c-actctx-comctl32-v6-compiledb-20260604102644.log` compiles `actctx.c` for aarch64, arm64ec, x86_64, and i386; `reports/build/lane-c-actctx-comctl32-v6-ntdll-rebuild-20260604102705.log` rebuilds/installs the active aarch64/x64 PE ntdll pair; `reports/lane-c-npp-arm64-installer-post-comctl32-v6-20260604102726/summary.txt` reports `common_controls_missing=0`, `fixme_actctx=0`, and `module_load_fail=0`.
- 2026-06-04: `ws2_32!WSAIoctl` expected-case diagnostics in `socket.c`. The Notepad++ updater import scan first flagged `SIO_GET_EXTENSION_FUNCTION_POINTER` unknown GUID handling and then non-IPv4 `SIO_ROUTING_INTERFACE_QUERY`; both paths already returned the expected WinSock errors (`WSAEINVAL` for unknown extension GUIDs and `WSAEAFNOSUPPORT` for non-IPv4 routing queries) and Wine tests cover the same error-family behavior, so the diagnostics now use `TRACE` instead of stub/unsupported `FIXME`. Validation: `reports/build/lane-c-ws2-wsaioctl-family-compiledb-20260604100555.log` compiles `ws2_32/socket.c` for arm64ec, aarch64, i386, and x86_64; dynamic GUP probe `reports/lane-c-npp-gup-wsaioctl-probe-20260604101017/summary.txt` confirms the app did not hit WSAIoctl during the trace window, leaving remaining body-level ioctl stubs unobserved rather than app blockers.
- 2026-06-04: ARM64 SMBIOS ID-register fallback diagnostics in `ntdll/unix/system.c`. The WOW64 `memory_x86` probe surfaced `fixme:ntdll:get_core_id_regs_arm64 stub` from the non-Linux ARM64 path while building SMBIOS processor additional-info records. Linux has a sysfs/auxval implementation for ID registers; macOS does not expose that register population path, so the existing zero-register fallback is now explicit `TRACE` output instead of a stub marker. Validation: `reports/build/lane-c-arm64-id-reg-fallback-ntdllso-20260604095613.log` rebuilds `dlls/ntdll/ntdll.so` successfully; `reports/lane-c-wow64-memory-post-arm64-id-reg-20260604095647/summary.txt` reports `fixme_stub=0` and `core_id_stub=0`.
- 2026-06-04: MSVCRT Unicode locale wrapper diagnostics in `msvcrt/locale.c`. Win7 Calc imports `__crtLCMapStringW` and `__crtGetStringTypeW`; both bodies already delegated to `LCMapStringW`/`GetStringTypeW` but still emitted `partial stub` diagnostics that surfaced as Lane C DLL-breadth hits. The wrappers now trace the delegated calls without changing the NLS behavior, leaving unsupported handling to the real Unicode APIs. Validation: available PE compilers `i686-w64-mingw32-gcc` and `x86_64-w64-mingw32-gcc` compile `msvcrt/locale.c` in `reports/build/lane-c-msvcrt-locale-wrappers-mingw-compile-20260604094347.log`; focused Win7 Calc owning-DLL import scan `reports/build/lane-c-win7-calc-owning-dll-scan-20260603234415.txt` reports `owning_dll_stub_hits=0`.
- 2026-06-04: NTDLL SQM telemetry no-op family in `ntdll/misc.c`/`ntdll.spec`. Win7 Calc imports `WinSqmAddToStream`, `WinSqmAddToStreamEx`, and `WinSqmIncrementDWORD`; the local spec lacked the stream exports and the existing SQM bodies logged stubs/returned `STATUS_NOT_IMPLEMENTED`. The implemented subset now follows MacRunner's telemetry-disabled policy: opt-out is reported, session start returns an invalid telemetry handle, end/session/data mutation calls are successful no-ops, and all diagnostics are `TRACE` instead of stub failures. `WinSqmAddToStream`/`WinSqmAddToStreamEx` are exported with the 4-slot/5-slot decorations recorded by the mingw-w64 NTDLL DEF metadata. Validation: available PE compilers `x86_64-w64-mingw32-gcc` and `i686-w64-mingw32-gcc` compile `ntdll/misc.c` in `reports/build/lane-c-ntdll-sqm-family-mingw-compile-20260604094138.log` (ARM Windows wrappers are unavailable in this environment); the corrected Win7 Calc owning-DLL import scan now reports no `ntdll` SQM hits and leaves only the two `msvcrt` locale partial stubs.
- 2026-06-04: Application Recovery registration family in `kernel32/process.c`. Win7 Calc imports `ApplicationRecoveryFinished`, `ApplicationRecoveryInProgress`, and `RegisterApplicationRecoveryCallback`; the local bodies reported stubs/`ERROR_CALL_NOT_IMPLEMENTED`. Kernel32 now stores process-local recovery callback metadata, validates callback/flags/ping interval per documented constraints, reports `E_FAIL` for `ApplicationRecoveryInProgress` outside a WER-invoked recovery callback, clears in-progress state on `ApplicationRecoveryFinished`, and exports/implements `UnregisterApplicationRecoveryCallback` to close the family. Validation: manual `/usr/bin/clang` compiles of `kernel32/process.c` for aarch64/arm64ec/x86_64/i386 Windows targets pass in `reports/build/lane-c-app-recovery-family-manual-clang-20260604092848.log`; focused Win7 Calc owning-DLL import/stub scan no longer reports Kernel32 recovery APIs.
- 2026-06-04: macOS ARM64 4GB free-area probe handling in `ntdll/unix/virtual.c`. A refreshed installed-dist WOW64 `memory_x86` run reached `wow64.dll`/`wow64win.dll` mapping and then logged `try_map_free_area mmap() error Cannot allocate memory` for free-area probes starting exactly at `0x100000000`; macOS can report that fixed probe as `ENOMEM` even though the VM search should treat the 4GB boundary as an occupied/unusable candidate and keep scanning. `try_map_free_area()` now normalizes `ENOMEM` at exactly `limit_4g` to `EEXIST` on macOS/ARM64/Win64, preserving fatal handling for other addresses and errors. Validation: `reports/build/lane-c-wow64-4gb-probe-skip-arm64ec-20260604092321.log` (ARM64EC `virtual.o` and `ntdll.so` relink rc=0) and `reports/build/lane-c-wow64-4gb-probe-skip-pure-arm64-20260604092332.log` (pure ARM64 `virtual.o` and `ntdll.so` relink rc=0). Matched runtime validation is blocked by the absent ARM64EC PE `ntdll.dll` target; see open gap above.
- 2026-06-04: `FindFirstFileExW` argument-family cleanup in `kernelbase/file.c`. NPP x86 imports `FindFirstFileExW`; after the stream/file-info checkpoints, the remaining local scan hit was diagnostic-only `FIXME` output for defined flags/search arguments. The implementation now accepts both defined flags in this tree (`FIND_FIRST_EX_CASE_SENSITIVE`, which Wine tests note is commonly ignored on Windows, and `FIND_FIRST_EX_LARGE_FETCH`), rejects unknown flags with `ERROR_INVALID_PARAMETER`, and reports unsupported search-op/info-level arguments as warnings while preserving the existing invalid-parameter behavior. Validation: manual `/usr/bin/clang` compiles of `kernelbase/file.c` for aarch64/arm64ec/x86_64/i386 Windows targets all pass in `reports/build/lane-c-findfirstfileex-argument-family-manual-clang-20260604091639.log`; focused NPP import/stub scan no longer reports `kernelbase/file.c`.
- 2026-06-04: `GetFileInformationByHandleEx` query-class fallbacks in `kernelbase/file.c`. NPP x86 imports `GetFileInformationByHandleEx`; the local body returned `ERROR_CALL_NOT_IMPLEMENTED` for the `FileRemoteProtocolInfo`, `FileStorageInfo`, `FileCaseSensitiveInfo`, and `FileNormalizedNameInfo` query classes, and grouped set-only Ex classes into the same unsupported branch. Local files now report non-remote protocol info, conservative 512-byte aligned storage info, case-sensitive flags 0, and normalized names via the existing NT `FileNameInformation` path; `FileDispositionInfoEx`/`FileRenameInfoEx` are treated as invalid query classes instead of unsupported APIs. Validation: manual `/usr/bin/clang` compiles of `kernelbase/file.c` for aarch64/arm64ec/x86_64/i386 Windows targets all pass in `reports/build/lane-c-file-info-class-fallbacks-manual-clang-20260604091156.log`; focused NPP import/stub scan no longer reports `GetFileInformationByHandleEx`.
- 2026-06-04: Default stream enumeration in `kernelbase/file.c`. NPP x86 imports `FindFirstStreamW`; the local implementation returned `ERROR_HANDLE_EOF` for every path with a `stub!` log. The family now validates level/flags/data, opens the target for attributes, returns `ERROR_HANDLE_EOF` for directories/no alternate-stream enumeration, returns a default `::$DATA` stream and file size for regular files, makes `FindNextStreamW` a traced EOF after the default stream, and teaches `FindClose` to free stream search handles. Existing Wine test expectation for `C:\\windows` remains `INVALID_HANDLE_VALUE` with `ERROR_HANDLE_EOF`. Validation: manual `/usr/bin/clang` compiles of `kernelbase/file.c` for aarch64/arm64ec/x86_64/i386 Windows targets all pass in `reports/build/lane-c-findfirststream-default-data-manual-clang-20260604090654.log`; focused NPP import/stub scan no longer reports `FindFirstStreamW`.
- 2026-06-04: Application Restart state family in `kernelbase/process.c` and `kernel32` spec routing. NPP x86 imports `GetApplicationRestartSettings`, `RegisterApplicationRestart`, and `UnregisterApplicationRestart`; historical NPP trace evidence in `reports/installer-e2e/open-dialog-enumtrace-20260524-105407/last-run.json` shows those calls emitted `fixme:process` during startup. `kernelbase` now owns process-local restart command-line/flag state, `GetApplicationRestartSettings` returns documented `S_OK`/`ERROR_NOT_FOUND`/`ERROR_INSUFFICIENT_BUFFER`/`E_INVALIDARG` results, and `kernel32` register/unregister exports import the shared `kernelbase` implementation instead of the old local stubs. Validation: manual `/usr/bin/clang` compiles of edited `kernelbase/process.c` and `kernel32/process.c` for aarch64/arm64ec/x86_64/i386 Windows targets all pass in `reports/build/lane-c-app-restart-family-manual-clang-20260604090056.log`; generated spec-object targets are absent in this configured tree, logged in `reports/build/lane-c-app-restart-family-generated-build-20260604090119.log`.
- 2026-06-04: DWM colorization/window-attribute fallback in `dwmapi/dwmapi_main.c`. NPP x86 static import scan showed `DwmGetColorizationColor` and `DwmSetWindowAttribute`; the local colorization path still returned `E_NOTIMPL`, while the window-attribute path already accepted attributes but logged as a stub. `DwmGetColorizationColor` now returns a deterministic ARGB caption color from `COLOR_ACTIVECAPTION` plus opaque blending, with `E_INVALIDARG` for invalid output pointers; `DwmSetWindowAttribute` remains a successful no-op but is traced instead of reported as unsupported. Validation: manual `/usr/bin/clang` compiles of `dwmapi_main.c` for aarch64/arm64ec/x86_64/i386 Windows targets all pass in `reports/build/lane-c-dwmapi-colorization-window-attr-manual-clang-20260604085224.log`.
- 2026-06-04: UXTheme buffered-animation no-animation fallback in `uxtheme/buffer.c`. NPP x86 static import scan showed `BeginBufferedAnimation`, `BufferedPaintRenderAnimation`, `BufferedPaintStopAllAnimations`, and `EndBufferedAnimation`; the local implementation already could only run without tracked animations, but still returned/logged `E_NOTIMPL`/`Stub` for stop/end paths. The family now consistently reports no animation handle (`BeginBufferedAnimation` returns `NULL` with output HDCs cleared), paints normally when no animation is active (`BufferedPaintRenderAnimation` returns `FALSE`), treats stop-all as successful no-op (`S_OK`), and reports invalid animation handles as `E_INVALIDARG` rather than unsupported. Validation: generated uxtheme make target is blocked by the existing missing `aarch64-w64-mingw32-clang` wrapper (`reports/build/lane-c-uxtheme-stop-all-animations-20260604084615.log`); manual `/usr/bin/clang` compiles of `uxtheme/buffer.c` for aarch64/arm64ec/x86_64/i386 Windows targets all pass in `reports/build/lane-c-uxtheme-buffered-animation-family-manual-clang-20260604084615.log`.
- 2026-06-04: Reserved-area end helper reuse in `ntdll/unix/virtual.c`. Remaining reserved-address-space walks now use `reserved_area_limit()` instead of open-coded `area->base + area->size` in unmap, fixed-map, large-address-space release, and i386/WOW64 query-region emulation paths, keeping the 32-bit reserved-area boundary family on the same clamped end-pointer helper. Validation: `reports/build/lane-c-reserved-area-limit-helper-virtual-20260604083712.log` (ARM64EC `unix/virtual.c` object compile rc=0, ARM64EC `ntdll.so` relink rc=0) and `reports/build/lane-c-reserved-area-limit-helper-virtual-pure-20260604083712.log` (pure-ARM64 `unix/virtual.c` object compile rc=0, pure-ARM64 `ntdll.so` relink rc=0).
- 2026-06-04: Builtin RVA delta arithmetic bounds in `ntdll/unix/loader.c`. `fixup_rva_dwords()` now rejects DWORD overflow/underflow while converting builtin data-directory, import, export, and delay-import RVAs by the SO-to-image delta, so wrapped metadata cannot become plausible in-image RVAs before the later string/thunk/table validators run. Validation: `reports/build/lane-c-builtin-rva-dword-fixup-bounds-20260604082640.log` (ARM64EC `unix/loader.c` object compile rc=0, ARM64EC `ntdll.so` relink rc=0) and `reports/build/lane-c-builtin-rva-dword-fixup-bounds-pure-build-20260604082640.log` (pure-ARM64 `unix/loader.c` object compile and `ntdll.so` relink completed; `ntdll.dll` target then stopped on missing `aarch64-w64-mingw32-clang`). Runtime gate is currently not attributable to this loader change: pure-ARM64 `hello_arm64_minimal` times out before user code in the pre-existing alignment/debugger path (`reports/build/lane-c-builtin-rva-dword-fixup-bounds-pure-harness-check-20260604082640.log`), and ARM64EC dist validation times out in the existing HB loader continuation path (`reports/build/lane-c-builtin-rva-dword-fixup-bounds-arm64ec-runtime-20260604082640.log`).
- 2026-06-04: Builtin export pointer-to-RVA bounds in `ntdll/unix/loader.c`. `map_so_dll()` now validates the builtin DLL entrypoint pointer and every exported-function pointer against the constructed builtin image before converting those pointers into 32-bit RVAs, rejecting out-of-image or wrapped builtin metadata instead of publishing invalid export/entry RVAs. Validation: `reports/build/lane-c-builtin-export-pointer-rva-bounds-20260604082152.log` (`unix/loader.c` ARM64EC object compile rc=0, ARM64EC `ntdll.so` relink rc=0) and `reports/build/lane-c-builtin-export-pointer-rva-bounds-runtime-20260604082152.log` (`dll_loadlibrary_arm64` under `mr-run.sh`, rc=0).
- 2026-06-03: Remote-process `MemoryWorkingSetExInformation` in `ntdll/unix/virtual.c`. Cross-process `NtQueryVirtualMemory(..., MemoryWorkingSetExInformation, ...)` now answers from the existing remote `MemoryBasicInformation` APC path instead of returning `STATUS_INVALID_INFO_CLASS`, zeroing unmapped entries and reporting valid/protection/shared attributes for committed pages. Validation: `reports/build/lane-c-remote-working-set-ex-20260603231514.log` (`unix/virtual.c` ARM64EC object compile rc=0, ARM64EC `ntdll.so` relink rc=0; generated ntdll per-test make targets are absent/disabled in this configured tree).
- 2026-06-03: Remote-process `MemoryRegionInformation` in `ntdll/unix/virtual.c`. Cross-process `NtQueryVirtualMemory(..., MemoryRegionInformation, ...)` now walks the target process' `MemoryBasicInformation` ranges from `AllocationBase` to return allocation-scope `RegionSize` and `CommitSize` instead of `STATUS_NOT_IMPLEMENTED`. Validation: `reports/build/lane-c-remote-memory-region-info-20260603231313.log` (`unix/virtual.c` ARM64EC object compile rc=0, ARM64EC `ntdll.so` relink rc=0; generated ntdll per-test make targets are absent/disabled in this configured tree).
- 2026-06-03: Regular NT virtual-memory transfer counts in `ntdll/unix/virtual.c`. Cross-process `NtReadVirtualMemory()` now reports the actual server reply size, and `NtWriteVirtualMemory()` now reports zero bytes on server failure instead of trusting `reply->written`. Validation: `reports/build/lane-c-virtual-nt-transfer-counts-20260603213723.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Zero-size `NtFlushVirtualMemory` range clipping in `ntdll/unix/virtual.c`. Current-process flush now derives zero-length requests from the rounded address to the checked containing view end instead of expanding to the full view size and potentially flushing past the view. Validation: `reports/build/lane-c-virtual-flush-zero-size-view-end-20260603213529.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Loader compatibility-DB path allocation handling in `ntdll/unix/loader.c`. Startup now fail-fasts if formatting the optional `cxcompatdb.so` probe path runs out of memory, while preserving the existing behavior for absent files or `dlopen()` failures. Validation: `reports/build/lane-c-loader-cxcompatdb-asprintf-20260603213042.log` (`unix/loader.c` temp-object compile rc=0; remaining warning is pre-existing macro redefinition).
- 2026-06-03: Loader builtin-EXE probe allocation handling in `ntdll/unix/loader.c`. `check_command_line()` now fail-fasts if the builtin executable probe path cannot be formatted, and iterates `dll_paths` with a `size_t` index to match the bounded path-array sizing. Validation: `reports/build/lane-c-loader-builtin-probe-allocation-20260603212910.log` (`unix/loader.c` temp-object compile rc=0; remaining warning is pre-existing macro redefinition).
- 2026-06-03: Loader reexec argv-vector bounds in `ntdll/unix/loader.c`. `reexec_loader()` now rejects impossible argument counts before computing the reexec argv allocation size, and copies the inherited argv tail with a bounded `size_t` byte count. Validation: `reports/build/lane-c-loader-reexec-argv-bounds-20260603212746.log` (`unix/loader.c` temp-object compile rc=0; remaining warning is pre-existing macro redefinition).
- 2026-06-03: DLL search-path array bounds in `ntdll/unix/loader.c`. `prepend_dll_path()`, `set_dll_path()`, and `set_system_dll_path()` now use `size_t` entry counts and reject pointer-array allocation overflow before allocating or prepending DLL search paths. Validation: `reports/build/lane-c-loader-dll-path-array-bounds-compiledb-20260603212430.log` (`unix/loader.c` temp-object compile rc=0; remaining warning is pre-existing macro redefinition).
- 2026-06-03: Loader path allocation overflow guards in `ntdll/unix/loader.c`. Shared path builders now validate `dir/name`, relative-path `..` component expansion, and temporary `ntdll.so` path sizing before allocating startup/re-exec paths, preventing environment/build paths from wrapping allocation sizes. Validation: `reports/build/lane-c-loader-path-allocation-overflow-20260603212017.log` (`unix/loader.c` temp-object compile rc=0; remaining warning is pre-existing macro redefinition).
- 2026-06-03: Mandatory loader path formatting checks in `ntdll/unix/loader.c`. `load_apiset_dll()` now fail-fasts on `apisetschema.dll` path formatting allocation failure before opening the file, and macOS build-tree `pre_exec()` now checks `DYLD_LIBRARY_PATH` formatting failure and frees the temporary formatted path after `setenv()`. Validation: `reports/build/lane-c-loader-mandatory-asprintf-20260603211651.log` (`unix/loader.c` temp-object compile rc=0; remaining warning is pre-existing macro redefinition).
- 2026-06-03: Alternate wineloader path allocation cleanup in `ntdll/unix/loader.c`. Build-tree WOW64 loader discovery now frees the temporary `loader-wow64` path after `realpath_dirname()`, and `get_alternate_wineloader()` now checks `asprintf()` failures before returning alternate loader paths. Validation: `reports/build/lane-c-loader-alt-wineloader-allocation-20260603211428.log` (`unix/loader.c` temp-object compile rc=0; remaining warning is pre-existing macro redefinition).
- 2026-06-03: WOW64 64-bit virtual-memory transfer counts in `ntdll/unix/virtual.c`. `NtWow64ReadVirtualMemory64()` now reports the actual `read_process_memory` reply size, and `NtWow64WriteVirtualMemory64()` now reports the server's `reply->written` count instead of the requested byte count when a transfer completes short. Validation: `reports/build/lane-c-wow64-vm-transfer-counts-virtual-manual-clang-20260603210828.log` (`unix/virtual.c` temp-object compile rc=0); narrow `dlls/ntdll/tests/wow64` make targets are absent in this build tree (`reports/build/lane-c-wow64-test-target-probe-20260603210904.log`), and x86 runtime execution is still gated by the open CPU/BOP continuation blocker.
- 2026-06-03: Thread-stack view-limit bounds in `ntdll/unix/virtual.c`. `virtual_alloc_thread_stack()` now derives the mapped stack end through the checked view helper, computes guard/stack-limit addresses with overflow checks, and releases the mapped view on impossible post-map bound failures before publishing `INITIAL_TEB` stack bounds. Validation: `reports/build/lane-c-thread-stack-view-limit-bounds-manual-clang-20260603203527.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Preload limit end helper in `ntdll/unix/virtual.c`. 32-bit preload reserve scanning now computes each preload span end through the saturated range helper before moving `user_space_limit`, avoiding raw `preload_info.addr + preload_info.size` arithmetic during VM initialization. Validation: `reports/build/lane-c-preload-limit-end-helper-manual-clang-20260603203343.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Reserved-area entry-limit helper in `ntdll/unix/virtual.c`. Reserved-area add/remove/query paths now compute existing entry ends through an immutable wrapper around the saturated reserved-area end helper, avoiding repeated raw `area->base + area->size` arithmetic while preserving list merge/split semantics. Validation: `reports/build/lane-c-reserved-area-entry-limit-helper-manual-clang-20260603203249.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Working-set view-end helper reuse in `ntdll/unix/virtual.c`. `MemoryWorkingSetExInformation` iteration now uses the shared checked view-end helper when backing up to a previous overlapping view and when walking current-view dirty/committed references, avoiding raw `view->base + view->size` loop bounds. Validation: `reports/build/lane-c-working-set-view-end-helper-manual-clang-20260603203142.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Public free/query view-end bounds in `ntdll/unix/virtual.c`. `NtFreeVirtualMemory()` now validates the found view end before release-range sizing, and `get_memory_region_size()` now uses the shared checked view-end helper for region starts/ends instead of raw `view->base + view->size` arithmetic. Validation: `reports/build/lane-c-public-free-region-view-end-manual-clang-20260603203040.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Placeholder coalesce bounds in `ntdll/unix/virtual.c`. Placeholder coalescing now checks each candidate view end through the shared helper and bounds accumulated placeholder size before adding it, so adjacency and target-size checks do not rely on raw `view->base + view->size` or wrapped `views_size` arithmetic. Validation: `reports/build/lane-c-placeholder-coalesce-bounds-manual-clang-20260603202908.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Free/decommit view-end helper reuse in `ntdll/unix/virtual.c`. Decommit, page-removal, placeholder-preserving free, and normal free paths now compute checked `base_end`/`view_end` values before split decisions, diagnostics, and host unmap spans instead of repeating raw `base + size` and `view->base + view->size` arithmetic. Validation: `reports/build/lane-c-free-decommit-view-end-helper-manual-clang-20260603202807.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Map-file view helper reuse in `ntdll/unix/virtual.c`. `map_file_into_view()` now derives the containing view end through the shared checked helper and computes the requested data address once before page/host-page rounding, instead of repeating local `view->base + view->size` and `view->base + start` arithmetic. Validation: `reports/build/lane-c-map-file-view-helper-reuse-manual-clang-20260603202639.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: ARM64EC bitmap view-end helper reuse in `ntdll/unix/virtual.c`. `commit_arm64ec_map()` now derives the view end through the shared checked file-view helper and reuses that bounded end in bitmap span/base overflow diagnostics, with the bitmap base address formed only after the existing overflow check. Validation: `reports/build/lane-c-arm64ec-bitmap-view-end-helper-manual-clang-20260603202545.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Remove/create view-end helper reuse in `ntdll/unix/virtual.c`. `remove_reserved_area()` and `create_view()` now use the shared checked file-view end helper for covered-view unmap gaps and system-view overlap diagnostics instead of duplicating local `view->base + view->size` guards. Validation: `reports/build/lane-c-remove-create-view-end-helper-manual-clang-20260603202437.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: View lookup/free-placement end bounds in `ntdll/unix/virtual.c`. `find_view()`, `find_view_range()`, `find_view_inside_range()`, and `map_free_area()` now reuse a checked file-view end helper for tree comparisons and free-area placement instead of repeating raw `view->base + view->size` arithmetic. Validation: `reports/build/lane-c-view-lookup-end-bounds-manual-clang-20260603202319.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Free-range view-end bounds in `ntdll/unix/virtual.c`. Free-range insert/remove bookkeeping now derives granularity-aligned view ranges through a checked helper, including neighbor-view clipping and trace end pointers, instead of forming raw `view->base + view->size + granularity_mask` spans. Validation: `reports/build/lane-c-free-range-view-end-bounds-manual-clang-20260603202035.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Reset-write-watch range-end bounds in `ntdll/unix/virtual.c`. `NtResetWriteWatch()` now validates the rounded `base + size` end before tracing or resetting write-watch metadata, matching the `NtGetWriteWatch()` range guard. Validation: `reports/build/lane-c-reset-write-watch-end-bounds-manual-clang-20260603201625.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Public VM range-end bounds in `ntdll/unix/virtual.c`. `NtGetWriteWatch()` now validates the rounded `base + size` end before tracing or walking write-watch pages, and `NtFlushInstructionCache()` rejects wrapped current-process cache-flush ranges before `__clear_cache()`. Validation: `reports/build/lane-c-public-range-end-bounds-manual-clang-20260603201521.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Allocation wrapper wrap-helper cleanup in `ntdll/unix/virtual.c`. `allocate_virtual_memory()` now relies on the integer-based `is_beyond_limit()` helper for wrapped requested-base ranges instead of performing a raw `base + size` pointer comparison first. Validation: `reports/build/lane-c-allocate-vm-wrap-helper-manual-clang-20260603201348.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: File-backed map-view end bounds in `ntdll/unix/virtual.c`. `map_file_into_view()` now computes checked view/map/data ends once and reuses them for host-size selection plus mmap/read-fallback diagnostics, avoiding repeated raw `map_addr + size` and `map_addr + map_size` arithmetic. Validation: `reports/build/lane-c-map-file-view-end-bounds-manual-clang-20260603201239.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: x64 guest range-overlap bounds in `ntdll/unix/virtual.c`. The MacRunner x64 guest range overlap helper now validates both candidate spans before forming end pointers, avoiding wrapped overlap positives in host-exec protection decisions. Validation: `reports/build/lane-c-x64-guest-range-overlap-bounds-manual-clang-20260603201112.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Reserved/fixed map span bounds in `ntdll/unix/virtual.c`. `map_reserved_area()` and `map_fixed_area()` now validate reserved-area entry spans before forming `area->base + area->size`, and fixed-map diagnostics reuse the checked host-rounded map end. Validation: `reports/build/lane-c-reserved-fixed-map-bounds-manual-clang-20260603200931.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Write-watch update bounds in `ntdll/unix/virtual.c`. `update_write_watches()` now clamps accessed bytes to the caller range and validates the final `base + size` span before clearing write-watch metadata or restoring page protections. Validation: `reports/build/lane-c-update-write-watches-bounds-manual-clang-20260603200813.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: View creation range bounds in `ntdll/unix/virtual.c`. `create_view()` now rejects wrapped `base + size` ranges before overlap scans or vprot metadata allocation, and uses checked existing-view ends for overlap diagnostics. Validation: `reports/build/lane-c-create-view-range-bounds-manual-clang-20260603200645.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Reserved-area removal bounds in `ntdll/unix/virtual.c`. `remove_reserved_area()` now validates the removal end once, reuses that immutable boundary while walking views, and checks view end arithmetic before unmapping uncovered gaps. Validation: `reports/build/lane-c-remove-reserved-area-bounds-manual-clang-20260603200457.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Reserved free-area placement bounds in `ntdll/unix/virtual.c`. `find_reserved_free_area()` now validates base/end span and alignment additions before rounding, and only forms `range->end - size` or `range->base + align_mask` after proving the specific free-range can hold the request. Validation: `reports/build/lane-c-reserved-free-area-bounds-manual-clang-20260603200334.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Free-area mapper fit bounds in `ntdll/unix/virtual.c`. `try_map_free_area()` now proves each candidate fits inside the requested range before forming `start + size`, and `map_free_area()` rejects impossible base/end spans and alignment additions before top-down or bottom-up placement arithmetic. Validation: `reports/build/lane-c-map-free-area-fit-bounds-manual-clang-20260603200222.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Reserved-area query bounds in `ntdll/unix/virtual.c`. `mmap_is_in_reserved_area()` now reuses the saturated reserved-area end calculation before containment checks, avoiding fresh `addr + size` arithmetic while preserving complete/partial/not-reserved return semantics for view and DOS-area callers. Validation: `reports/build/lane-c-reserved-area-query-bounds-manual-clang-20260603200019.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Reserved-area end arithmetic bounds in `ntdll/unix/virtual.c`. Reserved-area add/remove bookkeeping now computes a single saturated range end before list comparisons, avoiding signed wrap checks and repeated `addr + size` pointer arithmetic while preserving zero-length no-ops and max-address reservations. Validation: `reports/build/lane-c-reserved-area-end-bounds-manual-clang-20260603195854.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: VM page-range metadata bounds in `ntdll/unix/virtual.c`. Page protection and ARM64EC bitmap helpers now derive exclusive page ends from checked last-byte arithmetic instead of raw `addr + size + page_mask`, preserving zero-length no-ops and rejecting wrapped ranges before vprot table allocation or metadata mutation. Validation: `reports/build/lane-c-vm-page-range-bounds-manual-clang-20260603195542.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing macro redefine/unused diagnostics).
- 2026-06-03: Image-directory DOS-header validation cleanup in `ntdll/loader.c`. `read_image_directory()` now drops the dead negative `e_lfanew` test for the unsigned DOS-header field and keeps validation on exact reads plus optional-header/data-directory bounds. Validation: `reports/build/lane-c-read-image-directory-lfanew-manual-clang-20260603151327.log` (`loader.c` temp-object compile rc=0 for aarch64/x64/x86; remaining warnings are pre-existing tautological width checks and unused MacRunner helpers).
- 2026-06-03: Disk section-table bounds in `ntdll/loader.c`. MacRunner disk PE helpers now use a shared bounded section-table resolver that validates `SizeOfOptionalHeader` and `NumberOfSections` against the mapped file before export, COFF alias lookup/sync, or ARM64X native-entry probes walk sections; the same disk helpers no longer carry dead unsigned `e_lfanew < 0` checks. Validation: `reports/build/lane-c-disk-section-table-bounds-manual-clang-20260603151150.log` (`loader.c` temp-object compile rc=0 for aarch64/x64/x86; remaining warnings are pre-existing tautological width checks and unused MacRunner helpers).
- 2026-06-03: API-set NT-header offset validation cleanup in `ntdll/unix/loader.c`. `load_apiset_dll()` now relies on the bounded `e_lfanew` upper check for this unsigned DOS-header field instead of a dead negative comparison, keeping the mapped apisetschema header validator warning-clean apart from the pre-existing macro redefine. Validation: `reports/build/lane-c-apiset-lfanew-validation-manual-clang-20260603150757.log` (`unix/loader.c` temp-object compile rc=0; only warning is pre-existing `WINE_NT_MAP_VIEW` macro redefinition).
- 2026-06-03: Builtin SO header rounding bounds in `ntdll/unix/loader.c`. `map_so_dll()` now computes the synthesized PE header span in 64-bit arithmetic and rejects `SectionAlignment` rounding overflow before narrowing the code-section start to `DWORD`. Validation: `reports/build/lane-c-builtin-header-rounding-bounds-manual-clang-20260603150644.log` (`unix/loader.c` temp-object compile rc=0; warnings are pre-existing `WINE_NT_MAP_VIEW` macro redefinition and unsigned `e_lfanew < 0` comparison).
- 2026-06-03: Reserved-area limit rounding bounds in `ntdll/unix/virtual.c`. `map_reserved_area()` now checks host-page round-up of `limit_low` in both top-down and bottom-up reserved-area scans before searching/mapping inside the adjusted range. Validation: `reports/build/lane-c-reserved-area-limit-rounding-bounds-manual-clang-20260603150504.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing `WINE_NT_MAP_VIEW` macro redefinition and unused `inherit`).
- 2026-06-03: Unmap-area rounding bounds in `ntdll/unix/virtual.c`. `remove_reserved_area()` now validates rounded view spans before subtracting them from reserved holes, and `unmap_area()` rejects host-page rounding or `start + size` overflow before unmapping or replacing host VM. Validation: `reports/build/lane-c-unmap-area-rounding-bounds-manual-clang-20260603150236.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing `WINE_NT_MAP_VIEW` macro redefinition and unused `inherit`).
- 2026-06-03: Free/decommit rounding bounds in `ntdll/unix/virtual.c`. `decommit_pages()`, `free_pages_preserve_placeholder()`, and `free_pages()` now validate base round-up, `base + size`, and placeholder host remap sizes before changing vprot state or unmapping host pages. Validation: `reports/build/lane-c-free-decommit-rounding-bounds-manual-clang-20260603150106.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing `WINE_NT_MAP_VIEW` macro redefinition and unused `inherit`).
- 2026-06-03: Preload-reserve range bounds in `ntdll/unix/virtual.c`. `virtual_init()` now validates `WINEPRELOADRESERVE` ranges before publishing reserve start/end boundaries, rejecting reversed or host-page-rounding-overflow ranges instead of letting environment input wrap reserved-area placement. Validation: `reports/build/lane-c-preload-reserve-range-bounds-manual-clang-20260603145933.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing `WINE_NT_MAP_VIEW` macro redefinition and unused `inherit`).
- 2026-06-03: ARM64EC bitmap commit-span bounds in `ntdll/unix/virtual.c`. `commit_arm64ec_map()` now validates view end arithmetic, bitmap byte span rounding, and bitmap-base pointer addition before marking the view as ARM64EC and committing the bitmap pages. Validation: `reports/build/lane-c-arm64ec-commit-map-bounds-manual-clang-20260603145823.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing `WINE_NT_MAP_VIEW` macro redefinition and unused `inherit`).
- 2026-06-03: ARM64EC bitmap map rounding bounds in `ntdll/unix/virtual.c`. `alloc_arm64ec_map()` now checks host-page rounding for the address-space-derived EC code bitmap size before reserving the bitmap view, failing startup explicitly on impossible overflow. Validation: `reports/build/lane-c-arm64ec-map-rounding-bounds-manual-clang-20260603145652.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing `WINE_NT_MAP_VIEW` macro redefinition and unused `inherit`).
- 2026-06-03: Protection-range rounding bounds in `ntdll/unix/virtual.c`. `mprotect_range()` now rejects wrapped host-page rounding before walking vprot bytes or applying host `mprotect`, so protection updates from VM mapping, write-watch, and buffer-access paths cannot collapse overflowing spans. Validation: `reports/build/lane-c-mprotect-range-rounding-bounds-manual-clang-20260603145600.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing `WINE_NT_MAP_VIEW` macro redefinition and unused `inherit`).
- 2026-06-03: Write-access buffer rounding bounds in `ntdll/unix/virtual.c`. `check_write_access()` now rejects wrapped host-page rounding for caller buffers before walking vprot bytes or temporarily disabling write-watch protections in locked read/pread/recv and uninterrupted-write paths. Validation: `reports/build/lane-c-write-access-rounding-bounds-manual-clang-20260603145509.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing `WINE_NT_MAP_VIEW` macro redefinition and unused `inherit`).
- 2026-06-03: Virtual-heap size bounds in `ntdll/unix/virtual.c`. `virtual_init()` now checks the derived pages-protection table heap size before adding it to the view/free-range blocks, and `alloc_virtual_heap()` rejects host-page rounding overflow before searching reserved areas or mmaping the heap. Validation: `reports/build/lane-c-virtual-heap-size-bounds-manual-clang-20260603145352.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing `WINE_NT_MAP_VIEW` macro redefinition and unused `inherit`).
- 2026-06-03: Section-map view rounding bounds in `ntdll/unix/virtual.c`. Non-image `NtMapViewOfSection` now uses checked page rounding for the requested or file-derived view size, rejecting all wrap cases before `map_view()` and `map_file_into_view()` consume the span. Validation: `reports/build/lane-c-section-map-rounding-bounds-manual-clang-20260603145219.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing `WINE_NT_MAP_VIEW` macro redefinition and unused `inherit`).
- 2026-06-03: Map-view host rounding bounds in `ntdll/unix/virtual.c`. `map_fixed_area()` now rejects host-page rounding or fixed-base end overflow before mapping a fixed VM span, and null-base `map_view()` now checks host-size rounding plus alignment slack before reserving the overmapped search view. Validation: `reports/build/lane-c-map-view-host-rounding-bounds-manual-clang-20260603145056.log` (`unix/virtual.c` temp-object compile rc=0; warnings are pre-existing `WINE_NT_MAP_VIEW` macro redefinition and unused `inherit`).
- 2026-06-03: VM stack rounding bounds in `ntdll/unix/virtual.c`. `virtual_alloc_thread_stack()` now uses checked granularity rounding for reserve/commit-derived stack size, rejecting wrapped stack reservations before `map_view()` creates a too-small stack. Validation: `reports/build/lane-c-vm-stack-rounding-bounds-manual-clang-20260603144720.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: VM allocation null-base rounding bounds in `ntdll/unix/virtual.c`. The `allocate_virtual_memory()` path where the kernel chooses the base now uses `round_size_checked()` instead of raw `ROUND_SIZE(0, size, page_mask)`, rejecting wrapped page rounding before `map_view()`. Validation: `reports/build/lane-c-vm-alloc-null-rounding-bounds-manual-clang-20260603144623.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: VM allocation address rounding bounds in `ntdll/unix/virtual.c`. `allocate_virtual_memory()` now uses checked page rounding for caller-supplied base addresses and separately bounds the 64K reserve-prefix span, avoiding raw `addr + size + page_mask` wrap before low-address/kernel-limit checks. Validation: `reports/build/lane-c-vm-alloc-address-rounding-bounds-manual-clang-20260603144527.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: Builtin resource RVA bounds in `ntdll/unix/loader.c`. `fixup_so_resources()` now validates named resource string offsets inside the resource directory and validates each adjusted resource data entry `OffsetToData`/`Size` range against `SizeOfImage`, matching the later `access_resource()` image-bounds contract. Validation: `reports/build/lane-c-builtin-resource-rva-bounds-manual-clang-20260603144311.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Builtin export-name RVA bounds in `ntdll/unix/loader.c`. `map_so_dll()` now validates the adjusted export module name and every adjusted exported-symbol name string before rewriting the builtin export directory, rejecting zero, unterminated, or out-of-image names. Validation: `reports/build/lane-c-builtin-export-name-rva-bounds-manual-clang-20260603144036.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Builtin import-name RVA bounds in `ntdll/unix/loader.c`. `fixup_rva_names()` now validates each adjusted non-ordinal `IMAGE_IMPORT_BY_NAME` target in normal and delay import thunk arrays before rewriting it, rejecting unterminated or out-of-image names instead of leaving malformed builtin import metadata for later resolver paths. Validation: `reports/build/lane-c-builtin-import-name-rva-bounds-manual-clang-20260603143903.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Builtin delay-import RVA bounds in `ntdll/unix/loader.c`. `map_so_dll()` now validates adjusted delay-load module-handle, IAT, bound-IAT, and unload-IAT RVAs before later resolver paths consume them, matching the existing DLL-name/INT checks. Validation: `reports/build/lane-c-builtin-delay-import-rva-bounds-manual-clang-20260603143526.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Virtual relocation image-size bounds in `ntdll/unix/virtual.c`. `virtual_relocate_module()` now checks `SizeOfImage` page rounding and rejects rounded values that would overflow the function's 32-bit image-size path before relocation directory and section protection math. Validation: `reports/build/lane-c-virtual-relocate-size-bounds-manual-clang-202606030442.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: VM reset-write-watch rounding bounds in `ntdll/unix/virtual.c`. `NtResetWriteWatch()` now rejects wrapped caller ranges before tracing or resetting write-watch state. Validation: `reports/build/lane-c-vm-reset-writewatch-rounding-bounds-manual-clang-202606030439.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: VM write-watch range rounding bounds in `ntdll/unix/virtual.c`. `NtGetWriteWatch()`, `VmPrefetchInformation`, and dirty-state range handling now reject wrapped caller ranges before write-watch scans, `madvise()`, or dirty-state reset view lookups. Validation: `reports/build/lane-c-vm-writewatch-rounding-bounds-manual-clang-202606030435.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: VM flush rounding bounds in `ntdll/unix/virtual.c`. Local `NtFlushVirtualMemory()` now checks host-page rounding after the zero-size-to-view-size expansion before calling `msync()`, while preserving remote APC behavior and existing view lookup semantics. Validation: `reports/build/lane-c-vm-flush-rounding-bounds-manual-clang-202606030431.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: VM lock/unlock rounding bounds in `ntdll/unix/virtual.c`. Local `NtLockVirtualMemory()` and `NtUnlockVirtualMemory()` now validate guest-page and host-page rounded spans before updating the in/out address and size or calling `mlock`/`munlock`, while leaving remote APC behavior unchanged. Validation: `reports/build/lane-c-vm-lock-rounding-bounds-manual-clang-202606030427.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: VM free/protect rounding bounds in `ntdll/unix/virtual.c`. Local `NtFreeVirtualMemory()` and `NtProtectVirtualMemory()` now reject wrapped address/size page rounding before view lookup or protection changes, preserving remote-process APC behavior. Validation: `reports/build/lane-c-vm-free-protect-rounding-bounds-manual-clang-202606030424.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: Map-file view span bounds in `ntdll/unix/virtual.c`. `map_file_into_view()` now enforces `start/size` against the view at runtime, uses checked page rounding, and guards `map_addr + map_size` / `view_base + view_size` before mmap/read fallback math. Validation: `reports/build/lane-c-map-file-view-span-bounds-manual-clang-202606030421.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: Map-image rounding completion in `ntdll/unix/virtual.c`. The remaining image-controlled `ROUND_SIZE()` uses inside `map_image_into_view()` now use checked rounding for flat-image size, import remap spans, zero-fill spans, and final section/header protection spans. Validation: `reports/build/lane-c-map-image-rounding-complete-manual-clang-202606030416.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: Map-image rounding bounds in `ntdll/unix/virtual.c`. `map_image_into_view()` now uses checked rounding for PE-controlled header, file, and section spans before pointer arithmetic or zero-fill/mapping math, rejecting wrapped `ROUND_SIZE()` results instead of letting them collapse into smaller ranges. Validation: `reports/build/lane-c-map-image-rounding-bounds-manual-clang-202606030411.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: Resource entry containment in `ntdll/resource.c`. `access_resource()` now verifies the supplied `IMAGE_RESOURCE_DATA_ENTRY` pointer is inside the loaded resource directory before reading `OffsetToData` and `Size`, rather than relying on the SEH wrapper for arbitrary pointers. Validation: `reports/build/lane-c-resource-entry-containment-manual-clang-202606030407.log` (`ntdll/resource.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: RVA cached-section bounds in `ntdll/loader.c`. `RtlImageRvaToVa()` now verifies a caller-supplied cached section pointer belongs to the validated section table before dereferencing it, then falls back to the normal bounded section lookup if the cache is stale or hostile. Validation: `reports/build/lane-c-rva-cached-section-bounds-manual-clang-202606030404.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: RVA section-table bounds in `ntdll/loader.c`. `RtlImageRvaToSection()` now validates the section table span against `SizeOfHeaders`/`SizeOfImage` before walking `NumberOfSections`, preventing malformed headers from driving the exported helper past the mapped image headers. Validation: `reports/build/lane-c-rva-section-table-bounds-manual-clang-202606030401.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: Data-directory index bounds in `ntdll/loader.c`, `ntdll/unix/loader.c`, and `ntdll/unix/virtual.c`. PE directory helpers now reject indexes beyond the fixed `DataDirectory` array and reject optional headers too short to contain the requested directory entry before reading it. Validation: `reports/build/lane-c-data-directory-index-bounds-manual-clang-202606030357.log` (`ntdll/loader.c` aarch64/x64/x86 + `unix/loader.c` + `unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: Relocation target bounds in `ntdll/loader.c` and `ntdll/unix/virtual.c`. Both relocation processors now validate every relocation entry's write span against the image size before applying a block, closing the case where a valid block base RVA could still use offsets past the mapped image end. Validation: `reports/build/lane-c-relocation-target-bounds-manual-clang-202606030352.log` (`ntdll/loader.c` aarch64/x64/x86 + `unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: TLS callback slot bounds in `ntdll/loader.c`. `call_tls_callbacks()` now validates each callback-table slot against the loaded image before dereferencing it, instead of relying on SEH to catch a malformed or unterminated TLS callback array. Validation: `reports/build/lane-c-tls-callback-slot-bounds-manual-clang-202606030348.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: ARM64EC redirection destination bounds in `ntdll/unix/loader.c` and `ntdll/unix/virtual.c`. `redirect_ntdll_functions()` now rejects redirected ntdll procedure targets outside the mapped image, and `update_arm64ec_ranges()` rejects redirected entry RVAs outside the view before publishing them to image metadata. Validation: `reports/build/lane-c-arm64ec-redirection-destination-bounds-manual-clang-202606030344.log` (`unix/loader.c` + `unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: Bootstrap reloc-probe bounds in `ntdll/loader.c`. The winemetal bootstrap relocation trace now resolves the hard-coded probe RVA through `image_rva_range()` before dereferencing, so tracing cannot fault on small or malformed modules. Validation: `reports/build/lane-c-bootstrap-reloc-probe-bounds-manual-clang-20260603034051.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: ARM64EC CodeMap range bounds in `ntdll/unix/virtual.c`. `update_arm64ec_ranges()` now validates each CodeMap `StartOffset + Length` against the mapped view and rejects wrapped `base + StartOffset` pointers before marking ARM64EC ranges. Validation: `reports/build/lane-c-arm64ec-codemap-range-bounds-manual-clang-20260603033918.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: Builtin view section bounds in `ntdll/unix/virtual.c`. `virtual_create_builtin_view()` now rejects wrapped builtin view ranges, validates section virtual ranges before setting page protections, and skips the server map call if section metadata is malformed. Validation: `reports/build/lane-c-builtin-view-section-bounds-manual-clang-20260603033743.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: Load-config pointer bounds in `ntdll/loader.c`. `update_load_config()` now rejects wrapped image-end calculations before validating security cookie and CHPE metadata pointers against the loaded image range. Validation: `reports/build/lane-c-load-config-pointer-bounds-manual-clang-20260603033530.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: Disk-export return bounds in `ntdll/loader.c`. MacRunner disk export lookup now verifies returned function RVAs fit inside the loaded target module and reject wrapped `DllBase + FunctionRva` pointers before returning cross-arch export targets. Validation: `reports/build/lane-c-disk-export-return-bounds-manual-clang-20260603033409.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: Image-directory disk read bounds in `ntdll/loader.c`. `read_image_directory()` now rejects negative PE header offsets, validates data-directory indexes and section-table byte counts, and ensures directory reads fit inside the containing raw section before issuing the file read. Validation: `reports/build/lane-c-read-image-directory-bounds-manual-clang-20260603033246.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: PE64 conversion header bounds in `ntdll/loader.c`. `convert_to_pe64()` now validates the mapped header end and section-table byte span with integer bounds before making headers writable and moving section headers. Validation: `reports/build/lane-c-pe64-conversion-header-bounds-manual-clang-20260603033116.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: Build-module map-size bounds in `ntdll/loader.c`. `build_module()` now rejects overflowing `SizeOfImage` page rounding and wrapped mapped-module end pointers before relocation and PE-loaded notifications consume `map_size`. Validation: `reports/build/lane-c-build-module-map-size-bounds-manual-clang-20260603033000.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: Module address comparator bounds in `ntdll/loader.c`. `module_address_search_compare()` now uses integer address arithmetic and rejects wrapping `DllBase + SizeOfImage` ranges before deciding an address belongs to a module. Validation: `reports/build/lane-c-module-address-compare-bounds-manual-clang-20260603032852.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: Bootstrap entrypoint RVA bounds in `ntdll/loader.c`. The AMD64-on-ARM64 DLL-entry bootstrap fixup now resolves header `AddressOfEntryPoint` through the bounded image RVA helper before overriding the loader entry pointer. Validation: `reports/build/lane-c-bootstrap-entrypoint-rva-bounds-manual-clang-20260603032735.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: Module entrypoint RVA bounds in `ntdll/loader.c`. `alloc_module()` now resolves `AddressOfEntryPoint` through the bounded image RVA helper before storing `LDR_DATA_TABLE_ENTRY.EntryPoint`, avoiding wrapped entry pointers from malformed images. Validation: `reports/build/lane-c-module-entrypoint-rva-bounds-manual-clang-20260603032623.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: Unix data-directory RVA pointer bounds in `ntdll/unix/loader.c`. Unix-side `get_module_data_dir()` now returns directory pointers through the bounded module RVA helper after validating directory ranges, rejecting wrapped `module + VirtualAddress` results. Validation: `reports/build/lane-c-unix-data-dir-rva-ptr-manual-clang-20260603032510.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Unix export RVA pointer bounds in `ntdll/unix/loader.c`. Unix-side export lookup now resolves export arrays, names, and function RVAs through a bounded `module_rva_ptr()` helper so wrapped `module + RVA` values are rejected before dereference or return. Validation: `reports/build/lane-c-unix-export-rva-ptr-bounds-manual-clang-20260603032406.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Virtual relocation protection bounds in `ntdll/unix/virtual.c`. `virtual_relocate_module()` now validates raw section ranges before making relocation sections writable, records old protections only after successful changes, and restores only captured protections. Validation: `reports/build/lane-c-virtual-reloc-protect-bounds-manual-clang-20260603032244.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: Section protection bounds in `ntdll/unix/virtual.c`. The PE image protection pass now verifies each section `VirtualAddress + rounded size` fits inside the mapped image before setting vprot or WOW64 guest protections. Validation: `reports/build/lane-c-section-protection-bounds-manual-clang-20260603032118.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: Shared import remap bounds in `ntdll/unix/virtual.c`. PE section mapping now checks import-directory containment with subtraction, clamps page-aligned import remap bases to the owning shared section, and avoids underflowing the shared-file offset when remapping import pages. Validation: `reports/build/lane-c-shared-import-remap-bounds-manual-clang-20260603032023.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: API-set lookup bounds in `ntdll/loader.c`. Runtime API-set entry/target resolution now bounds hash, namespace-entry, value-entry, host-name, and target-name offsets against `API_SET_NAMESPACE.Size` before dereferencing namespace map data. Validation: `reports/build/lane-c-apiset-lookup-bounds-manual-clang-20260603031900.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: Image RVA helper bounds in `ntdll/loader.c`. Import/image helper code now rejects wrapped `DllBase + SizeOfImage` ranges, resolves image RVAs through a bounded helper, and avoids forming import-name pointers before proving the import header and name string fit inside the image. Validation: `reports/build/lane-c-image-rva-helper-bounds-manual-clang-20260603031629.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: ARM64X entry RVA bounds in `ntdll/loader.c`. MacRunner native ARM64X entry discovery now validates `AddressOfEntryPoint` against `SizeOfImage` and rejects wrapped `DllBase + EntryRva` calculations before returning an entry pointer. Validation: `reports/build/lane-c-arm64x-entry-rva-bounds-manual-clang-20260603031455.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: ARM64X thunk resolver bounds in `ntdll/loader.c`. MacRunner ARM64X redirection now verifies candidate targets are inside the module image before subtracting RVAs, and the thunk-chain resolver validates code bytes plus RIP-relative pointer slots before dereferencing them. Validation: `reports/build/lane-c-arm64x-thunk-resolver-bounds-manual-clang-20260603031339.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: ARM64X dispatch section bounds in `ntdll/loader.c`. MacRunner ARM64X dispatch metadata patching now uses bounded virtual-section containment before writes/protection changes, rejects wrapped slot pointers, and guards module address range comparisons against `base + SizeOfImage` overflow. Validation: `reports/build/lane-c-arm64x-dispatch-section-bounds-manual-clang-20260603031150.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: ARM64EC native-entry bounds in `ntdll/loader.c`. MacRunner disk native-entry discovery now rejects negative/overflowing PE header offsets and out-of-image entry RVAs before returning a native entry pointer, and in-memory CHPE metadata lookup now guards `module + SizeOfImage` overflow. Validation: `reports/build/lane-c-arm64ec-native-entry-bounds-manual-clang-20260603031030.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: COFF symbol alias bounds in `ntdll/loader.c`. MacRunner disk-symbol alias lookup/sync now validates symbol-table byte counts without overflow, requires long symbol names to NUL-terminate inside the COFF string table, and computes alias-slot RVAs with bounded section/value arithmetic. Validation: `reports/build/lane-c-coff-symbol-bounds-manual-clang-20260603030902.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: Disk-export string bounds in `ntdll/loader.c`. MacRunner disk export lookup now resolves export-name RVAs through a bounded NUL-terminated string helper, limiting `strcmp()` to names that terminate inside the containing raw section and mapped file. Validation: `reports/build/lane-c-disk-export-string-bounds-manual-clang-20260603030715.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: Disk-export metadata bounds in `ntdll/loader.c`. MacRunner disk export lookup now rejects negative/overflowing PE header offsets, validates export array byte-count products before resolving name/ordinal/function arrays, and checks forwarder RVAs with subtraction instead of `VirtualAddress + Size`. Validation: `reports/build/lane-c-disk-export-metadata-bounds-manual-clang-20260603030611.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: File-RVA helper bounds in `ntdll/loader.c`. MacRunner disk-export helpers now validate raw-section spans with subtraction-based checks and require `PointerToRawData + delta + len` to fit both the section raw size and mapped file size before returning a pointer. Validation: `reports/build/lane-c-file-rva-helper-bounds-manual-clang-20260603030448.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: Image directory entry bounds in `ntdll/loader.c`. `RtlImageDirectoryEntryToData()` now validates directory `VirtualAddress + Size` against image size, header size, or containing raw section before returning pointers, and rejects wrapped direct `module + RVA` results. Validation: `reports/build/lane-c-image-directory-bounds-manual-clang-20260603030331.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: RVA-to-section range overflow guard in `ntdll/loader.c`. `RtlImageRvaToSection()` and the cached-section path in `RtlImageRvaToVa()` now use subtraction-based raw-section containment and reject wrapped raw-file pointer offsets before returning mapped addresses. Validation: `reports/build/lane-c-rtlimage-rva-section-range-manual-clang-20260603030144.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: Relocation section protection bounds in `ntdll/loader.c`. `perform_relocations()` now validates each section `VirtualAddress + SizeOfRawData` against the loaded image size before making relocation sections writable or restoring protections. Validation: `reports/build/lane-c-reloc-section-protect-range-manual-clang-20260603030030.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: Thunk section range overflow guard in `ntdll/loader.c`. MacRunner thunk/executable-section predicates now reject wrapped `base + VirtualAddress` and `start + section-size` calculations before comparing callback targets against guest PE section ranges. Validation: `reports/build/lane-c-thunk-section-range-manual-clang-20260603025922.log` (`ntdll/loader.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: Message-resource entry offset helper in `ntdll/resource.c`. `RtlFindMessage()` now resolves `OffsetToEntries` through the bounded resource offset helper before reading the first `MESSAGE_RESOURCE_ENTRY`, completing the resource offset-pointer guard family for message tables. Validation: `reports/build/lane-c-message-entry-offset-helper-manual-clang-20260603025801.log` (`resource.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: API-set raw section bounds in `ntdll/unix/loader.c`. `load_apiset_dll()` now validates the mapped apisetschema DOS/NT/section headers and requires the `.apiset` raw section to contain a full `API_SET_NAMESPACE` before reading version/size fields. Validation: `reports/build/lane-c-apiset-section-bounds-manual-clang-20260603025639.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Resource data RVA bounds in `ntdll/resource.c`. `access_resource()` now validates normal-module resource data `OffsetToData + Size` against `SizeOfImage`, checks data-file `RtlImageRvaToVa()` results, and rejects malformed resource data entries before returning pointers or sizes. Validation: `reports/build/lane-c-resource-data-rva-bounds-manual-clang-20260603-025211.log` (`resource.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: Resource offset pointer guards in `ntdll/resource.c`. Resource traversal now validates name and child/data entry offsets before forming `root + offset` pointers, keeping resource directory walks inside the bounded resource section. Validation: `reports/build/lane-c-resource-offset-pointer-manual-clang-20260603-025039.log` (`resource.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: Message-resource table bounds in `ntdll/resource.c`. `RtlFindMessage()` now uses `LdrAccessResource()` size output to bound `MESSAGE_RESOURCE_DATA` blocks, message-entry offsets, entry lengths, and target message walks before returning a message entry from malformed resources. Validation: `reports/build/lane-c-message-resource-bounds-manual-clang-20260603-024914.log` (`resource.c` temp-object compile rc=0 for aarch64/x64/x86 via `/usr/bin/clang` compile-db commands).
- 2026-06-03: Builtin SO map status propagation in `ntdll/unix/loader.c`. `dlopen_dll()` now returns the specific `map_so_dll()` NTSTATUS, preserving invalid-image results from builtin metadata guards instead of reporting every map failure as `STATUS_NO_MEMORY`. Validation: `reports/build/lane-c-map-so-status-propagation-manual-clang-20260603-024618.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Builtin SO invalid-header cleanup in `ntdll/unix/loader.c`. `dlopen_dll()` now closes the `dlopen()` handle when `__wine_spec_nt_header` is missing before returning `STATUS_INVALID_IMAGE_FORMAT`. Validation: `reports/build/lane-c-dlopen-header-dlclose-manual-clang-20260603-024507.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Builtin image-base rounding guard in `ntdll/unix/loader.c`. `dlopen_dll()` now rejects builtin PE image bases that would overflow pointer-width arithmetic when rounded up to the synthetic module alignment, and closes the SO handle on that invalid-image path. Validation: `reports/build/lane-c-builtin-imagebase-rounding-manual-clang-20260603-024412.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Builtin section-range overflow guard in `ntdll/unix/loader.c`. `map_so_dll()` now computes synthesized code/data section ends in 64-bit arithmetic, validates macOS `__DATA` segment offsets, rejects DWORD overflow, and rejects underflowing section ranges before writing PE section sizes. Validation: `reports/build/lane-c-builtin-section-range-manual-clang-20260603-024317.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Builtin RVA delta sign guard in `ntdll/unix/loader.c`. `map_so_dll()` now rejects negative SO-header-to-module deltas before deriving the builtin data-section start, preventing malformed builtin headers from producing wrapped synthetic section ranges. Validation: `reports/build/lane-c-builtin-delta-nonnegative-manual-clang-20260603-024139.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Builtin RVA delta range guard in `ntdll/unix/loader.c`. `map_so_dll()` now computes the SO-header-to-module delta in pointer-width arithmetic and rejects values that cannot be represented by the existing `int` RVA fixup helpers, preventing truncated builtin metadata rewrites. Validation: `reports/build/lane-c-builtin-delta-range-manual-clang-20260603-024046.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Builtin section-alignment guard in `ntdll/unix/loader.c`. `map_so_dll()` now rejects zero or non-power-of-two builtin `SectionAlignment` before deriving `alignment - 1` for synthesized header/data layout rounding. Validation: `reports/build/lane-c-builtin-section-alignment-manual-clang-20260603-023955.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Builtin data-directory count bounds in `ntdll/unix/loader.c`. `map_so_dll()` now caps copied builtin `NumberOfRvaAndSizes` to the fixed PE data-directory array length and skips absent import/resource/export/delay slots through a bounded accessor before indexing directory entries. Validation: `reports/build/lane-c-builtin-data-dir-count-manual-clang-20260603-023906.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Builtin resource fixup bounds in `ntdll/unix/loader.c`. `map_so_dll()` now validates the builtin resource directory range and `fixup_so_resources()` bounds directory headers, entry arrays, leaf data entries, and recursion depth before rewriting resource data RVAs. Validation: `reports/build/lane-c-builtin-resource-fixup-bounds-manual-clang-20260603-023748.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Builtin delay-import fixup bounds in `ntdll/unix/loader.c`. `map_so_dll()` now bounds the builtin delay-import descriptor directory, requires a descriptor terminator inside the directory, validates delayed DLL names, and verifies delay INT thunk arrays are terminated inside the synthesized image before mutating their RVAs. Validation: `reports/build/lane-c-builtin-delay-fixup-bounds-manual-clang-20260603-023613.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Builtin import fixup bounds in `ntdll/unix/loader.c`. `map_so_dll()` now bounds the builtin import descriptor directory, requires a terminator inside the directory, validates imported DLL names, and verifies INT/IAT thunk arrays are terminated inside the synthesized image before mutating their RVAs. Validation: `reports/build/lane-c-builtin-import-fixup-bounds-manual-clang-20260603-023444.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Builtin export fixup bounds in `ntdll/unix/loader.c`. `map_so_dll()` now validates the builtin export directory and export function/name/ordinal arrays against the synthesized image size before rewriting export RVAs and pointer tables. Validation: `reports/build/lane-c-builtin-export-fixup-bounds-manual-clang-20260603-023226.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Unix bootstrap export fail-fast handling in `ntdll/unix/loader.c`. Mandatory ntdll and WOW64 ntdll export lookup failures now stop startup with `fatal_error()` after bounded lookup rejects malformed or missing export metadata, avoiding partially initialized Unix dispatcher globals. Validation: `reports/build/lane-c-unix-export-failfast-manual-clang-20260603-022927.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Unix bootstrap export lookup bounds in `ntdll/unix/loader.c`. Unix-side ntdll export lookup now validates export function/name/ordinal arrays and requires export names to be NUL-terminated inside `SizeOfImage`, so malformed export metadata is rejected before bootstrap callers consume it. Validation: `reports/build/lane-c-unix-export-bounds-manual-clang-20260603-022803.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Unix ARM64EC redirection metadata bounds in `ntdll/unix/loader.c`. `redirect_ntdll_functions()` now verifies load-config field size, CHPE metadata pointer containment, redirection count width, and redirection table bounds before redirecting bootstrap ntdll entrypoints through image-supplied ARM64EC metadata. Validation: `reports/build/lane-c-unix-arm64ec-redirect-bounds-manual-clang-20260603-022326.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: Unix loader data-directory bounds in `ntdll/unix/loader.c`. `get_module_data_dir()` now rejects absent directory slots and directory ranges outside `SizeOfImage` before returning export/load-config pointers to Unix-side ntdll bootstrap callers. Validation: `reports/build/lane-c-unix-data-dir-bounds-manual-clang-20260603-021958.log` (`unix/loader.c` temp-object compile rc=0).
- 2026-06-03: ARM64X dynamic relocation bounds in `ntdll/unix/virtual.c`. The Unix image mapper now verifies dynamic relocation table placement, V1/V2 record sizes, ARM64X fixup payload spans, inline value data, and target image ranges before applying ARM64X relocation fixups from malformed PE metadata. Validation: `reports/build/lane-c-arm64x-dynamic-reloc-bounds-manual-clang-20260603-021807.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: ARM64EC metadata range validation in `ntdll/unix/virtual.c`. The ARM64EC range updater now verifies the CHPE metadata VA-to-RVA conversion, metadata struct bounds, redirection-map bounds, and code-map bounds before walking malformed image-supplied metadata. Validation: `reports/build/lane-c-arm64ec-metadata-bounds-manual-clang-20260603-021426.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: ARM64X/ARM64EC load-config size guards in `ntdll/unix/virtual.c`. The image mapper now verifies the PE load-config directory is large enough to contain `Size` before using it to bound CHPE metadata and dynamic relocation table walks. Validation: `reports/build/lane-c-load-config-size-guard-manual-clang-20260603-120658.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: PE header-map-size consistency in `ntdll/unix/virtual.c`. Section-table preservation now compares against the capped `header_map_size` actually used for header mapping, not the uncapped server `image_info->header_map_size`, so truncated header mappings still copy section metadata before section remaps can overwrite it. Validation: `reports/build/lane-c-pe-header-map-size-manual-clang-20260603-120548.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: PE header read failure handling in `ntdll/unix/virtual.c`. `map_pe_header()` now checks both mmap-tail and fallback `pread()` results, returning `errno_to_status()` or `STATUS_INVALID_IMAGE_FORMAT` on failed/short reads before later PE parsing touches partially initialized header bytes. Validation: `reports/build/lane-c-pe-header-read-status-manual-clang-20260603-120459.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: PE zero section-alignment guard in `ntdll/unix/virtual.c`. The Unix image mapper now rejects a zero `image_info->alignment` before deriving `alignment - 1` as a mask, preventing malformed x86 PE metadata from underflowing the section/header rounding path. Validation: `reports/build/lane-c-pe-section-alignment-zero-manual-clang-20260603-120349.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: PE mapper `fstat()` failure handling in `ntdll/unix/virtual.c`. Image header mapping now returns `errno_to_status(errno)` if the backing fd cannot be statted instead of deriving header map sizes from an uninitialized `struct stat`. Validation: `reports/build/lane-c-pe-fstat-status-manual-clang-20260603-120224.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: PE NT-header offset validation in `ntdll/unix/virtual.c`. The Unix image mapper now computes the mapped header boundary before forming `ptr + e_lfanew`, and rejects NT headers that do not fit inside the mapped PE header. Validation: `reports/build/lane-c-pe-nt-header-offset-manual-clang-20260603-120137.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: `is_beyond_limit()` overflow hardening in `ntdll/unix/virtual.c`. Shared VM limit checks now use integer start/end arithmetic and treat wrapped range ends as beyond-limit, covering allocation, map, and query callers that validate user-supplied address spans through the helper. Validation: `reports/build/lane-c-is-beyond-limit-overflow-manual-clang-20260603-115952.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: `find_view_range()` overflow hardening in `ntdll/unix/virtual.c`. The range-overlap helper now rejects wrapped `addr + size` inputs before comparing view bounds, matching the existing `find_view()` guard for exact-view lookups. Validation: `reports/build/lane-c-find-view-range-overflow-manual-clang-20260603-115857.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: `MemoryWorkingSetExInformation` range clipping in `ntdll/unix/virtual.c`. Working-set queries now cap sorted caller address ranges at `working_set_limit` before subtracting `end - start`, preserving zero attributes for out-of-range pages without wrapped pointer arithmetic. Validation: `reports/build/lane-c-working-set-ex-range-manual-clang-20260603-115740.log` (`unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: Relocation block validation in `ntdll/loader.c` and `ntdll/unix/virtual.c`. Base relocation directories are checked against image size, and each relocation block now requires a header-sized, USHORT-aligned `SizeOfBlock` fully inside the relocation directory before PE-side or Unix-side relocation walkers process entries. Validation: `reports/build/lane-c-relocation-block-validation-manual-clang-20260603-114649.log` (`loader.c` aarch64/x64/x86 and `unix/virtual.c` temp-object compile rc=0).
- 2026-06-03: EXE vs DLL manifest resource selection in `ntdll/loader.c`. Main/non-DLL modules now probe `CREATEPROCESS_MANIFEST_RESOURCE_ID` (1) while DLL modules keep `ISOLATIONAWARE_MANIFEST_RESOURCE_ID` (2), so `fixup_imports()` can create activation contexts for normal embedded EXE manifests. Validation: `reports/build/lane-c-exe-manifest-resource-id-manual-clang-20260603-114353.log` (`loader.c` temp-object compile rc=0 for aarch64/x64/x86).
- 2026-06-03: Resource directory traversal bounds in `ntdll/resource.c`. Resource lookup now checks directory headers, entry arrays, named resource strings, and returned child/data entries against the PE resource directory size before traversing type/name/language levels used by manifests and resource APIs. Validation: `reports/build/lane-c-resource-directory-bounds-manual-clang-20260603-114126.log` (`resource.c` temp-object compile rc=0 for aarch64/x64/x86).
- 2026-06-03: TLS directory validation in `ntdll/loader.c`. TLS raw-data ranges, zero-fill size addition, index pointers, and callback-array pointers are validated against the mapped image before module/thread TLS allocation, unload cleanup, or callback dispatch can dereference them. Validation: `reports/build/lane-c-tls-directory-validation-manual-clang-20260603-113804.log` (`loader.c` temp-object compile rc=0 for aarch64/x64/x86).
- 2026-06-03: Delay-load import metadata bounds in `ntdll/loader.c`. `LdrResolveDelayLoadedAPI()` now validates delay-load DLL names, module-handle RVAs, INT/IAT thunk arrays, IAT thunk index alignment, and import-by-name entries inside the mapped image before resolving or invoking failure hooks. Validation: `reports/build/lane-c-delay-import-metadata-manual-clang-20260603-113419.log` (`loader.c` temp-object compile rc=0 for aarch64/x64/x86).
- 2026-06-03: Import metadata bounds in `ntdll/loader.c`. Import descriptor arrays now must terminate within the PE import directory, imported DLL names must be NUL-terminated inside the image, and INT/IAT thunk walks are bounded by `SizeOfImage`; malformed import metadata returns loader invalid-image handling instead of unbounded scans. Validation: `reports/build/lane-c-import-metadata-manual-clang-20260603-113011.log` (`loader.c` temp-object compile rc=0 for aarch64/x64/x86).
- 2026-06-03: TLS callback array guard in `ntdll/loader.c`. `call_tls_callbacks()` now reads each callback pointer inside the existing SEH protection path, so malformed TLS callback arrays fail as loader-handled callback exceptions instead of dereferencing before the guard.
- 2026-06-03: HIGHADJ relocation support in `ntdll/loader.c` and `ntdll/unix/virtual.c`. Both PE relocation paths now handle standard two-slot `IMAGE_REL_BASED_HIGHADJ` fixups and reject truncated HIGHADJ blocks instead of treating the relocation type as unsupported.
- 2026-06-03: `NtQueryVirtualMemory` buffer validation in `ntdll/unix/virtual.c`. Supported query classes now reject null result buffers with `STATUS_ACCESS_VIOLATION` before filling basic, region, working-set, image, or Wine Unix function outputs.
- 2026-06-03: Core VM in/out pointer validation in `ntdll/unix/virtual.c`. `NtAllocateVirtualMemory`, `NtAllocateVirtualMemoryEx`, `NtFreeVirtualMemory`, `NtProtectVirtualMemory`, `NtMapViewOfSection`, `NtMapViewOfSectionEx`, and `NtWow64AllocateVirtualMemory64` now reject null mandatory address/size/protection pointers before trace/APC/local dereferences.
- 2026-06-03: Lock/unlock/flush VM pointer validation in `ntdll/unix/virtual.c`. `NtLockVirtualMemory`, `NtUnlockVirtualMemory`, and `NtFlushVirtualMemory` now reject null address/size pointer arguments with `STATUS_ACCESS_VIOLATION` before dereferencing them or queuing remote APCs.
- 2026-06-03: Forwarded-export string hardening in `ntdll/loader.c`. Forwarder strings are now required to be NUL-terminated inside the export directory before parsing `DLL.Name` / `DLL.#ordinal`, preventing malformed export data from driving unbounded loader string scans.
- 2026-06-03: Unix loader path-format allocation hardening in `ntdll/unix/loader.c`. Startup ntdll path construction now fails explicitly on `asprintf()` failure, and reexec probing skips paths that could not be formatted instead of calling `access()` on null.
- 2026-06-03: Loader environment-buffer allocation hardening in `ntdll/loader.c`. `get_env_var()` now returns `STATUS_NO_MEMORY` if the query buffer allocation fails instead of passing a null buffer to `RtlQueryEnvironmentVariable()`.
- 2026-06-03: System-directory NT name allocation hardening in `ntdll/loader.c`. KnownDll and bootstrap builtin search now return `STATUS_NO_MEMORY` and close opened section mappings if the synthesized system32 NT path cannot be allocated.
- 2026-06-03: `NtSetInformationVirtualMemory` range-array validation in `ntdll/unix/virtual.c`. Supported classes now return `STATUS_ACCESS_VIOLATION` for null `MEMORY_RANGE_ENTRY` arrays when `count > 0`, avoiding Unix-side null dereferences while preserving existing parameter-order status results.
- 2026-06-03: Delay-load failure-hook descriptor fix in `ntdll/loader.c`. `LdrResolveDelayLoadedAPI()` now fills the `DELAYLOAD_INFO` name union with the actual import name for name-described failures instead of storing the low word of the import-name RVA as an ordinal.
- 2026-06-03: Placeholder allocation validation in `ntdll/unix/virtual.c`. `MEM_RESERVE_PLACEHOLDER` creation now rejects contradictory commit/reset/replace combinations before creating a committed or reset placeholder view.
- 2026-06-03: Runtime delay-load no-INT fallback in `ntdll/loader.c`. `LdrResolveDelayLoadedAPI()` now mirrors normal import binding and uses the delay IAT as the import descriptor source when `ImportNameTableRVA` is absent.
- 2026-06-03: `NtAllocateVirtualMemoryEx` address-requirements validation in `ntdll/unix/virtual.c`. Null address-requirement pointers are rejected before dereference, and `HighestEndingAddress + 1` now uses the full page mask alignment check.
- 2026-06-02: Large-page allocation semantics in `ntdll/unix/virtual.c`. `MEM_LARGE_PAGES` is now accepted by the public type masks and returns explicit invalid-parameter vs privilege-not-held results instead of falling through the unknown-bit path.
- 2026-06-02: Write-watch allocation validation in `ntdll/unix/virtual.c`. `MEM_WRITE_WATCH` now requires `MEM_RESERVE` so commit-only calls cannot implicitly create write-watch regions.
- 2026-06-02: Reserved-area split hardening in `ntdll/unix/virtual.c`. Reserved-area removal now reports split bookkeeping allocation failure atomically, with virtual-heap callers restoring the reserved mapping before falling back.
- 2026-06-02: Loader startup/path allocation hardening in `ntdll/unix/loader.c`. DLL path setup, prefix/path helpers, re-exec argv, WOW64 ntdll path, macOS temp-link creation, preloader exec formatting, wineserver PATH probing, and Android JNI argv construction now handle allocation failure instead of dereferencing null buffers.
- 2026-06-02: `MEM_RESET_UNDO` support in `ntdll/unix/virtual.c`. Both the real Win32 bit (`0x01000000`) and the current Wine header bit are accepted, with Darwin `MADV_FREE_REUSE` handling when available.
- 2026-06-02: Working-set query allocation hardening in `ntdll/unix/virtual.c`. `MemoryWorkingSetExInformation` now returns `STATUS_NO_MEMORY` if the temporary reference array allocation fails.
- 2026-06-02: LargeAddressAware registry lookup hardening in `ntdll/unix/virtual.c`. The AppDefaults key lookup now handles failed app-name allocation and closes the root key on all paths.
- 2026-06-02: Relocation protection hardening in `ntdll/loader.c`. PE relocation now checks section `NtProtectVirtualMemory()` failures and restores successfully changed section protections through a common cleanup path.
- 2026-06-02: TLS allocation failure hardening in `ntdll/loader.c`. `alloc_tls_slot()` no longer reports success when per-thread TLS data allocation fails, and it closes the enumerated thread handle on early allocation failures.
- 2026-06-02: IAT protection hardening in `ntdll/loader.c`. Static import binding now fails explicitly if the loader cannot make a read-only import address table writable or restore its original protection.
- 2026-06-02: `MEM_RESET` error propagation in `ntdll/unix/virtual.c`. `NtAllocateVirtualMemory` now returns an NTSTATUS for failed `madvise(MADV_DONTNEED)` instead of silently reporting success.
- 2026-06-02: Delay-import RVA fixup hardening in `ntdll/unix/loader.c`. The builtin PE mapper now mirrors normal import handling and skips the import-name table walk when a delay descriptor has no INT.
- 2026-06-02: Loader hook protection hardening in `ntdll/unix/loader.c`. The Apple x86_64 hook helper now fails loudly if `mprotect()` cannot make the target page writable before patching bytes.
