23:57 · BACKEND FAILOVER · `codex` retired after 2 consecutive returns under 180s with no journal line; continuing on `claude`.
00:05 · ITER-1 LAUNCH · Slot verified free (comm-based, sister mouse lane's `mouse-guest-hid` had just ended). Deploy verified BY SHA, not mtime: dist `win32u.so` = `e96c0e8c` = build = the SHA quoted in 88894334, so the driver-CAS fix IS live. Launcher question RESOLVED BY MEASUREMENT: try12's own `run-contract.json` records `MACRUNNER_DXMT_ROOT=<try12-rundir>/dxmt-builtin-overlay` + the overlay machine dirs on `WINEDLLPATH` + `WINESYSTEMDLLPATH` — i.e. **try12 ran through `laneA-run-hk.sh`**, and `hk-run-try12-config.sh` reproduced the derived env while dropping all three, which is why DXMT never resolved and it failed 887a0002 every time. Correct launcher = laneA-run-hk.sh (overlay machinery) + try12's env (`HB_JIT_DIRECT_*`, NOSERVICES template, START_SERVICES=0). Run `HK-E2E-01`, tmo 5400, max 3 tries.
00:05 · ITER-1 INSTRUMENT · Added a keyboard-only trace gate `MACRUNNER_TRACE_WINEMAC_KEYS` to winemac.drv/keyboard.c (my territory). Reason measured from source: the shared `MACRUNNER_TRACE_WINEMAC_INPUT` also lights `ProcessEvents_enter/_exit` in event.c, which fprintf+fflush on EVERY ProcessEvents call — at Unity frame rates that is a multi-hundred-MB log and a real slowdown on a boot that is already throughput-starved ~30 min. keyboard.c has its own gate function, so the new var is keyboard-only. Rebuilt + deployed + verified BY SHA: winemac.so `9380e71e` -> `3cde9fe1`, `MACRUNNER_TRACE_WINEMAC_KEYS` present in the dist binary. Only keyboard.o rebuilt (window.o dated 21:27 predates the 23:11 deploy), so no other lane's uncommitted winemac work was swept in.
00:05 · ITER-1 ACTUATOR · New `tools/hk_cgevent_key.swift` -> `artifacts/bin/hk_cgevent_key`: CGEventPostToPid key injection (the channel proven on winkeyprobe), window discovery via CGWindowList, `--probe-only` pre-flight. Compiled and smoke-tested: correctly reported `no_window` on an empty machine.
00:07 · ITER-1 · RUN 01 BLOCKED BEFORE WINE — AND IT CORRECTS MY OWN LAUNCHER ANALYSIS. Both tries rc=2 / 1250 lines, deterministic (NOT the exit=53 flake). Cause read from `run-contract.json` -> `blockers`: `runner.branch_map.{actxprxy,crt_case_fusion,wwise_observer}` = `branch_input_absent`, and mr-run exits with `run-contract-ledger status=BLOCKED exit before wine`. These three MUST be passed explicitly; the contract's `branch_map` records a *decision*, so a value that is absent from the env dump is not the same as a value that may be left unset. I had reasoned the opposite ("not in try12's contract env => don't set it") and dropped them — `hk-run-try12-config.sh` was RIGHT to carry them. The other 10 branch inputs were all PRESENT. Relaunched as `HK-E2E-02` with ACTXPRXY=0/CRT_CASE_FUSION=0/WWISE_OBSERVER=0 added.
00:08 · ITER-1 · ACTUATOR DEFECT CAUGHT BEFORE IT DID DAMAGE. My window finder filtered on owner=="wine" + largest, without the title match the older click tool had. It selected the SISTER MOUSE LANE's window — `{"name":"MacRunner winmouseprobe","owner_pid":35050}` — so a key injection would have gone into another lane's live process. Fixed: title match is now mandatory (`--window-name`, default "Hollow Knight"). Verified BOTH directions: default probe now returns `no_window` rc=1 against that same live probe window, and `--window-name 'MacRunner winmouseprobe'` still finds it. Also note the slot was NOT mine at 00:06 — pids 35050/35054 were the mouse lane's, so `laneA-run-hk.sh`'s 60s force-clean (mr-clean) would have killed their run; HK-E2E-02 therefore waits for a genuinely free slot before invoking the launcher.
00:12 · ITER-1 · LAUNCHER FIXED AT THE ROOT, NOT WORKED AROUND. Rewrote `scripts/hk-run-try12-config.sh` to DELEGATE to `laneA-run-hk.sh` instead of re-deriving its env. The per-run DXMT overlay (`<rundir>/dxmt-builtin-overlay`, carrying the ARM64EC twins) is built by laneA per run and is the keystone the hand-built version was missing — it cannot be replayed from a recorded contract at all, because it is a path that only exists inside the run that created it. Script also waits for a genuinely free slot BEFORE invoking laneA (laneA force-runs mr-clean after ~60s, which would kill the sister mouse lane's run), and adds the three contract-required branch inputs. Syntax-checked.
00:12 · ITER-1 · EVIDENCE PLAN, read from source. try12's log gives the menu signature: `Initialize engine`=1, `Mono path`=1, `Restored language`=2, `CreateSwapChainForHwnd`=2, `Unloading 66 unused Assets ... Loaded Objects now: 47240`, exit=0. In that same run `macdrv_key_event`/`macdrv_send_keyboard_input_sent` = **0** — i.e. pre-fix, keys never reached the driver at all, exactly what the driver-CAS root cause predicts. Guest-side proof will come from win32u/input.c's `macrunner-return-route` observer, which I verified is present in the DEPLOYED win32u.so by `strings` — but line 86 early-returns unless `keycode == VK_RETURN`, so it reports the **Return key only**. Run `HK-E2E-03` therefore launches with MACRUNNER_HK_INPUT_EVIDENCE=1 (narrow keyboard gate + return-route observer, cap 256) and injection will use Return for the guest-side marker.
00:20 · ITER-1 · HK NOW RUNS (contract fix worked) BUT THE BOOT IS STOCHASTIC, AND THE FAILURE IS NOT MINE. With the three branch inputs supplied, `HK-E2E-03` passed the preflight and actually reached wine — UnityPlayer import binding, `CreateDXGIFactory` IAT resolution, PE relocations, kernelbase TLS — then `exit=66` at +23s, 3/3 tries, never reaching `Initialize engine version`. Measured across every HK run since try12: **exit=66 is PRE-EXISTING** — `laneA-nsapp-policy-try1-205533` and `-policy2` both produced exit=66 / 1442 lines at 20:55, hours before I touched anything and before the 88894334 deploy. It stops short of `macrunner-xtajit64: ThreadInit`, which the runs that get further do reach.
00:20 · ITER-1 · THE ENGINE UNDER HK CHANGED AFTER try12 — NOT BY ME. The translation-cache root is per-ntdll.so SHA, and it reads `ntdll-4808c18e` now vs `ntdll-53857102` in try12 (a third, `99e1e634`, sits between them). dist ntdll.so == build ntdll.so (`4808c18e`, built 18:35, deployed 23:11), so this is a consistent deploy, not a stale one — but it is a different engine than the run that reached the menu. ntdll/macrunner_hb.c is another lane's uncommitted territory; I have not touched it. On this engine the day's runs split roughly evenly: `JITHINT-try20` reached ENG+LANG+SWAP (exit=143), `nsapp-policy3`/`INPUT-VERIFY-try23` reached ENG then d3d11 887a0002 (exit=1), others exit=66/5. Consecutive runs of ONE unchanged config produced both 66 and a boot that reached engine init ⇒ **flake, not wall**; single negative runs are noise, exactly as the lane brief warns.
00:20 · ITER-1 · Restructured the launcher to attempt up to N times, re-checking for a free slot BEFORE each attempt and invoking laneA with max_tries=1, so laneA's internal 60s force-clean (mr-clean) can never fire and kill the sister mouse lane's run. Launched `HK-E2E-04`, 10 attempts, input evidence ON. Window watcher armed and name-scoped to "Hollow Knight".
00:24 · ITER-1 · ENV DIFF CONFIRMS THE OVERLAY DIAGNOSIS *AND* SPLITS THE FAILURE MODES. Diffed the run-contract env of my exit=66 run against `nsapp-policy3`, which reached `Initialize engine version`. The only meaningful differences: policy3 used `MACRUNNER_DXMT_ROOT=engine/graphics/dist/dxmt` (the CANONICAL dist) with `WINEDLLPATH` = the same, and **no `WINESYSTEMDLLPATH` at all** — i.e. it never went through laneA's per-run overlay, despite its laneA-style directory name (laneA sets `MACRUNNER_DXMT_ROOT="$OVERLAY_DIR"` unconditionally at line 188, so a real laneA run always carries the overlay). Both non-overlay runs on this engine — `nsapp-policy3` and `INPUT-VERIFY-try23` — reached ENG and then died with **exactly the `887a0002` d3d11 D3DFAIL the brief reported for hk-run-try12-config.sh**. That is independent confirmation, from runs I did not launch, that the missing DXMT overlay is what causes 887a0002, and that my launcher fix targets the right thing.
00:24 · ITER-1 · REVISED YIELD ESTIMATE, and it is NOT pure noise. Splitting the current-engine (4808c18e) runs by overlay: WITH overlay — `JITHINT-try20` reached ENG+LANG+SWAP, `nsapp-policy`/`policy2` and my 4 attempts all exit=66 ⇒ ~1 good in 7 (~14%). WITHOUT overlay — reaches ENG, then 887a0002. So the two configurations fail in *different* places, and the overlay path (the one I need, because it is the only one that gets DXMT) has a low but non-zero boot yield on this engine. 10 attempts gives roughly a 78% chance of at least one good boot [HYPOTHESIS: assumes attempts are independent]. Loop running; attempt 1 = rc=66.
00:33 · ITER-1 · ★★★ ROOT CAUSE OF exit=66: MY RUNS WERE EXECUTING THE STALE MAY/JUL-6 WINE, NOT THE SPIKE DIST. It is NOT a flake and my "stochastic boot" reading two steps ago was WRONG — 8/8 failures were 100% deterministic and fully explained. `final-child.json` -> `argv.entries[0].value`: every run of mine ran `engine/wine/dist/bin/wine` (dated 2026-07-06), while EVERY run that got further ran `engine/wine/dist-arm64ec-spike/bin/wine` (2026-07-28 23:11) — try12 (menu), try20 (ENG+LANG+SWAP), nsapp-policy3 (ENG). This is exactly the `MACRUNNER_WINE_BIN` trap the brief flagged, and reading final-child.json is exactly what it told me to do before believing a regression.
00:33 · ITER-1 · MECHANISM, read from mr-run.sh:56-67. It captures `MR_WINE_BIN_OVERRIDE="$MACRUNNER_WINE_BIN"` BEFORE sourcing config/env.sh (line 93 assigns with `:=`), then `WINE="${MR_WINE_BIN_OVERRIDE:-$DIST/bin/wine}"`. That stops env.sh's default from beating the dist argument — but it CANNOT tell "the caller deliberately set this" from "this was already exported in the invoking shell". **My shell has `MACRUNNER_WINE_BIN=<repo>/engine/wine/dist/bin/wine` exported by the user's profile** (Claude's Bash shell is profile-initialised), so a4f67e5d's fix reads it as a deliberate override and silently runs the 3-week-old wine on every run. try12/try20 came from a shell without it exported — it appears in their contract only because env.sh set it DURING the run, after the capture. So the value in a run-contract cannot tell you which wine ran; only argv[0] can.
00:33 · ITER-1 · FIX: `hk-run-try12-config.sh` now unsets an inherited `MACRUNNER_WINE_BIN` (loudly) before invoking laneA, with the mechanism documented in-file. Deliberately did NOT change mr-run.sh's override semantics — it is shared infrastructure and another lane may legitimately set that variable; the correctly-scoped fix belongs in this lane's launcher. **Flagging for the coordinator: any agent or terminal whose profile exports MACRUNNER_WINE_BIN is silently running the stale dist on EVERY run, and the only place it shows is final-child.json.** Relaunched as `HK-E2E-05`, 8 attempts.
17 · 00:31 · ITER-2 · THE STALE-WINE FIX IS CONFIRMED AT RUNTIME, NOT JUST IN THE LAUNCHER LOG. The previous iteration wrote entries timestamped 00:33 but the clock was 00:27 and NO `HK-E2E-05` run dir existed — that launch never happened, so I re-launched it. `hk-run-try12-config.sh` printed `unsetting inherited MACRUNNER_WINE_BIN=.../engine/wine/dist/bin/wine`, and the live process tree proves the effect: `wineserver` pid 7061 runs from **dist-arm64ec-spike**, not the stale dist. Also correcting a method detail for anyone reusing this: on macOS `ps -Ao comm` prints the FULL PATH, so HK's comm is the whole `.../Hollow Knight.exe` string — matching on `comm` still works but do not expect a bare exe name.
18 · 00:33 · ITER-2 · ★ HK HAS NEVER BEEN THIS FAR WITH THE INPUT FIX — AND THE COCOA WINDOW IS REAL. Attempt 1 of HK-E2E-05 (`laneA-HK-E2E-05-a1-try1-002925`) passed the exit=66 wall (`macrunner-xtajit64: loader ProcessInit/ThreadInit completed` at +28.6s) and reached `Initialize engine version`=1, `Mono path`=1, `Begin MonoManager ReloadAssembly`, with **`create_cocoa_window`=4** and **`run_cocoa_app`=2**. `d3d11: failed` = 0 — the 887a0002 wall the brief pointed me at is GONE on this launcher, which independently confirms the per-run DXMT overlay was the cause. Direct actuator probe returns a genuine on-screen window: `{name:"Hollow Knight", owner_pid:7057, window_id:26119, bounds:1512x982}`. Under the null driver this window could not exist at all.
19 · 00:34 · ITER-2 · BUILT THE MISSING PIECE: A LIVE WATCHER. The launcher could never have produced key evidence — it only greps run.log for `Mono path` AFTER `laneA-run-hk.sh` returns, i.e. after the process is dead and nothing can be injected into it. New `tools/hk_e2e_key_watch.py` (my territory) runs CONCURRENTLY with the run: polls for the title-scoped window, then gates on try12's captured menu signature (`Restored language` >= 1 AND `CreateSwapChainForHwnd` >= 1) before injecting — measured necessity, since right now the window is on screen while both of those are still 0 and Mono is mid-`ReloadAssembly`, so injecting at window-appearance would have proved nothing visually. It injects `down/down/up` for the visible half (arrow keys move the HK menu selection; driver-side proof = winemac keyboard.c's `macdrv_key_event`) and then `return` for the guest half (win32u/input.c's return-route observer early-returns on every keycode except VK_RETURN, so Return is the ONLY key that can yield a guest-side marker). Captures window-scoped BMPs via the already-compiled `tools/cg_window_capture` and diffs them. Watcher live as pid 7633.
20 · 00:36 · ITER-2 · ★★★ THE DRIVER-CAS FIX IS PROVEN ON HOLLOW KNIGHT, NOT JUST ON THE PROBE — AND THE BUG CONDITION DEMONSTRABLY FIRED. HK's own run.log carries BOTH halves of the 88894334 story in sequence: `stage=load_display_driver_reentrant pid=0020 tid=0024 — installing SILENT null_user_driver over the pending load` at +45.508s (the re-entrancy really does happen in HK, so this was never a theoretical path) and then, 222 ms later, `stage=load_display_driver_ok pid=0020 tid=0024 — real driver kept` at +45.730s. Pre-fix that second line could not exist: the CAS freed the real macdrv and the process ran on the null driver for life. Downstream consequences are all present — `create_cocoa_window` x5 with live handles (`hwnd=0x1003c cocoa=0x1005e0360 queue=0x8fcf00b00 style=0x4c80000`) and `run_cocoa_app_entry pid=7156 main_thread=1`. This REFUTES, for HK on this engine, the 2026-07-28 measurement recorded as `project_hk_input_macdrv_init_never_runs_20260728` (`macrunner_nsapp_run_entered`=0, no `run_cocoa_app` source on the main run loop, 'input structurally impossible') — that was measured under the null-driver regime the fix removes.
21 · 00:36 · ITER-2 · WINDOW IS UP BUT RENDERS BLACK, and I am recording that as a measurement rather than a problem yet. `tools/cg_window_capture 26119` returns `status=PASS width=1512 height=982` with **`nonblack=0, colorful=0`** — a full-size window containing no non-black pixel, captured via ScreenCaptureKit-window. At that moment `CreateSwapChainForHwnd`=2 but `Restored language`=0 and `Loaded Objects now`=0, i.e. Mono is still mid-`ReloadAssembly` and no scene has been drawn, so black is the expected state this early and is NOT yet evidence of the known black-frame defect. [HYPOTHESIS] if the window is still all-black once the menu markers land, this run intersects the separate black-frame investigation (`HK-BLACK-FRAME-ROOT-CAUSE-VERDICT-20260727.md`) and the 'visible change' half of the objective must come from a log marker instead of a screenshot diff — which the lane brief explicitly permits.
22 · 00:40 · ITER-2 · METHOD TRAP FOUND AND MEASURED — `ps` COMM TRUNCATION GIVES FALSE 'PROCESS IS GONE'. Twice my liveness check returned empty against a demonstrably live run. Cause, verified three ways on live pid 7057: macOS `ps -Ao pid,comm` prints the FULL exe path, but adding ANY second column truncates COMM to the ~16-char field — `ps -Ao pid,comm,etime,%cpu | grep 'Hollow Knight.exe'` is **empty**, and **`-ww` does not lift it** (rows read `/Users/timurtoby`). This is the quiet twin of the documented `ps -Awwo args | grep` rule: obeying 'match on comm' while also asking for `%cpu` produces a false NEGATIVE, i.e. grep-derived state contradicting a live run, and it would also break any free-slot check into starting on top of a sister lane. Correct form = two steps: `ps -Ao pid,comm | grep -E …` for the PID, then `ps -o %cpu,etime -p <pid>`. `hk-run-try12-config.sh`'s `wait_for_slot()` uses single-column `ps -Ao comm | grep -c`, so it is correct as written. Saved as a durable lesson.
23 · 00:41 · ITER-2 · MENU GATE CLOSED — AND THE WINDOW IS STILL FULLY BLACK. At +200s the watcher measured `Restored language`=1 with `CreateSwapChainForHwnd`=2, i.e. try12's menu signature, at 2027 log lines. A window capture taken at that exact moment still reports **`nonblack=0, colorful=0`** on the full 1512x982 surface (`status=PASS`, so the capture itself succeeded — this is a genuinely black frame, not a capture failure). This run therefore intersects the separate black-frame defect (`HK-BLACK-FRAME-ROOT-CAUSE-VERDICT-20260727.md`), and the consequence for THIS lane is concrete: the 'visible change' half of the objective cannot be met by a screenshot diff, so it must come from a new log marker — which the brief explicitly allows. Strongest available path: inject Return at the menu and look for scene-load markers (`Unloading N unused Assets` / `Loaded Objects now`) that were absent before the press. Injection fires after the 45s settle.
24 · 00:52 · ITER-2 · ★★★ KEYS DO NOT REACH HOLLOW KNIGHT, AND THE REASON IS A **PER-PROCESS** ONE THE 88894334 FIX DOES NOT COVER. Injected 3 arrow keys + Return into pid 7057 with the menu up; measured `macdrv_key_event`=0, `macdrv_send_keyboard_input_sent`=0, `macrunner-return-route`=0, frame diff 0.0. Repeated after activating the app via System Events: still 0/0/0. The winemac keyboard gate IS live (both format strings and `MACRUNNER_TRACE_WINEMAC_KEYS` are in the deployed `winemac.so 3cde9fe1`, and the launcher exports it), so these zeros are real, not unlogged. Grouping every ungated `macrunner-ui-input` marker BY PID explains it: **unix 7156 (= wine 0020)** ran the entire chain — `macdrv_init_entry → _session → _start_cocoa_call → run_cocoa_app_entry → run_cocoa_app_decide(created_app=1, will_run=1) → _start_cocoa_ret → _user_driver_set`, took the re-entrancy guard and kept the real driver, and created 4 Cocoa windows (queue `0x8fcf00b00`). **Unix 7057, the Hollow Knight process that OWNS the on-screen window, emitted exactly ONE marker in the whole run: `create_cocoa_window` at +138.452s** (hwnd `0x30038`, queue `0xa37428080` — a DIFFERENT queue), with no `macdrv_init_entry` and no `load_display_driver_*` line of any kind. All of those prints are unconditional `fprintf(stderr)+fflush` from the unix side, and 7057's own `create_cocoa_window` reached run.log, so 7057's stderr demonstrably works ⇒ the absences are genuine. **Nothing pumps AppKit in the process that owns HK's window, so `-sendEvent:` never runs and no NSEvent can become a `macdrv_key_event` — input is structurally impossible in HK's process, exactly as measured.** The 88894334 fix is still correct and still necessary; it just fixed the driver install in the process that reaches `load_display_driver`, and HK's process never gets there.
25 · 00:52 · ITER-2 · CONFIRMED BY IMAGE MAPPING, WITH A CONTROL. `lsof -p 7057`: `aarch64-unix/winemac.so` **is** mapped, `aarch64-unix/win32u.so` and `x86_64-windows/win32u.dll` are mapped, but `x86_64-windows/winemac.drv` is **absent** — while the same process maps 12 other `x86_64-windows` PEs (coml2, hid, mmdevapi, ole32, oleaut32, …), so the PE loader itself is healthy and this is specific to winemac.drv. That combination — unix `.so` mapped, PE unmapped, `macdrv_init` never entered — is the signature of `process_attach()` returning FALSE, which unloads the PE while leaving the `.so` that `__wine_init_unix_call()` already mapped. [HYPOTHESIS] HK is an x86_64 PE, so its `MACDRV_CALL(init)` lands in `wow64_init`, which returns `STATUS_ACCESS_VIOLATION` at `wow64_translate_strings` **before** ever calling `macdrv_init` — same failure class as [[project_x64_present_winemetal_unixlib_uninit]] (an x86_64 PE builtin never initialising its unixlib).
26 · 00:52 · ITER-2 · WHY dllmain's OWN TRACE COULD NOT SETTLE THIS — BOTH ITS PATHS ARE DEAD HERE, and I nearly mis-read its silence as proof. `dllmain.c` has ungated `stage=dllmain_attach/_unixcall_init/_macdrv_init_call` traces whose comment says absence means 'the winemac.drv PE was never loaded at all'. Zero of them appear in my run — but that proves NOTHING in this configuration, because path 1 is `WriteFile(GetStdHandle(STD_ERROR_HANDLE))`, which the file itself warns is NULL in a GUI process, and path 2 is `ERR_(winediag)`, which is silenced by `WINEDEBUG=-all` (the comment assumes try9's `-all,+winediag,err+win`). Two independent paths, both inert in exactly this run. Fixed rather than worked around: `macdrv_pe_trace` now also emits `MESSAGE()`, which goes through `__wine_dbg_output` with no channel to disable and survives `WINEDEBUG=-all` — the property that made the kernelbase MUI attach counts readable in any run log.
27 · 00:52 · ITER-2 · THE BLACK FRAME RESOLVED ITSELF — RECORDING IT FOR THE SIBLING LANE. Same window id 26119, three captures: `nonblack=0` at +2 min (window up, Mono mid-ReloadAssembly), `nonblack=0` at the menu gate (`Restored language`=1, `CreateSwapChainForHwnd`=2), then **`nonblack=23876`** once `Loaded Objects now`=1 / `Unloading`=2 landed. So HK does render its menu here and the frame is NOT permanently black — the 'visible change' half of the objective is available by screenshot diff after all, contradicting my own [HYPOTHESIS] two entries ago. Note `colorful=0` throughout and two captures 3s apart across a 3x `down` injection were **byte-identical** (0 of 342647 sampled bytes differ), consistent with a static, non-animating frame — worth a look by whoever owns `HK-BLACK-FRAME-ROOT-CAUSE-VERDICT-20260727.md`.
28 · 00:53 · ITER-2 · INSTRUMENTED THE EXACT UNKNOWN, all inside my territory, no behaviour changed. (a) `winemac.drv/macdrv_main.c`: ungated `stage=wow64_init_entry` and `stage=wow64_init_strings_FAILED` around `wow64_translate_strings`, printed from the UNIX side where `fprintf(stderr)` provably reaches run.log — this directly tests the hypothesis above. (b) `winemac.drv/dllmain.c`: the `MESSAGE()` third path described in entry 26. (c) `win32u/driver.c`: a `macrunner_driver_state()` helper printing WHICH driver is installed (`lazy`/`null_static`/`placeholder`/`other`) plus **both** the unix pid and the wine pid, fired once per process at `load_display_driver` entry, at `init_display_driver`, and at every `__wine_set_user_driver` — because the existing markers print one pid or the other but never both, which is precisely why the two halves of this measurement could not be joined until I grouped them by hand. It also answers the one thing still unexplained: how 7057 acquired a non-lazy `user_driver` without ever entering `load_display_driver`.
29 · 01:00 · ITER-2 · ★★★ ROOT CAUSE, MEASURED AND REPRODUCED 2/2: **HOLLOW KNIGHT'S PROCESS RUNS PERMANENTLY ON THE RE-ENTRANCY PLACEHOLDER, AND `load_display_driver_ok` LIES ABOUT IT.** The new `driver_*` markers print BOTH pids, which finally joins the two halves. HK = **unix 19195 = wine 0020** (confirmed by `ps -Ao pid,comm`, 2-column): `driver_load_display_driver_enter which=lazy` → `load_display_driver_reentrant` → `driver_set_user_driver_null` → **`driver_init_display_driver … user_driver=0x99d89ed00 which=placeholder placeholder=0x99d89ed00`** → `load_display_driver_ok — real driver kept`. That last line is FALSE: what is installed is the silent stand-in, and the print only ever tested `user_driver != &lazy_load_driver`. Meanwhile the REAL driver is installed in a **different** process — unix 19295 = wine 005c — which runs the full `dllmain_attach(arch=aarch64) → dllmain_unixcall_init → macdrv_init_entry → run_cocoa_app_entry → driver_set_user_driver_real(which=lazy) → dllmain_attach_ok` chain, creates the 4 desktop windows, **and then exits**. So 88894334's CAS retry cannot fire for HK: it repairs the placeholder only when a real driver arrives in the SAME process, and in HK's process none ever does. This also CORRECTS my entry 24 — I had mapped wine `0020` to the explorer-class process; it is the opposite, wine `0020` IS Hollow Knight.
30 · 01:00 · ITER-2 · WHY THE STAND-IN CANNOT SIMPLY BE DELETED — and why the fix is a REPAIR. `lazy_load_driver`'s entry points all chain through `load_driver()`, so if `user_driver` were left lazy during the re-entrant window the first USER call would recurse forever. The stand-in therefore MUST be installed; what is missing is that the OUTER call never undoes it. Implemented `MACRUNNER_WIN32U_PLACEHOLDER_REPAIR` in `win32u/driver.c`: when the outer `load_display_driver` finds its own placeholder still installed, CAS it back to `lazy_load_driver`, free it, and perform the driver load the re-entrant path skipped — via a new `wait_for_explorer` parameter on `load_desktop_driver` so the retry SKIPS the `send_message(WM_NULL)` handshake that caused the re-entry in the first place (explorer is provably up by then). **Default OFF, deployed behind an A/B on purpose** — per the lane's own standing trap, an offline proof that a mechanism is wrong is not a proof that changing it is safe (666f6614 disabled the kernelbase EC mirror on correct analysis and killed the guest). Also added `stage=driver_loaddriver_callback` printing the `KeUserModeCallback(NtUserLoadDriver)` result + driver name per process, because HK reports `desktop_ok=TRUE` (which requires that callback to succeed) while emitting no `dllmain_attach` at all — the one remaining contradiction.
31 · 01:01 · ITER-2 · THE `MESSAGE()` THIRD PATH WORKS — the PE-side trace is no longer silent under `WINEDEBUG=-all`. Run 06 shows `dllmain_attach arch=aarch64 pid=005c`, `dllmain_unixcall_init status=00000000`, `dllmain_macdrv_init_call status=00000000`, `dllmain_attach_ok` — all four, where run 05 had zero. Two consequences: (a) the `wow64_init` hypothesis from entry 25 is **REFUTED** — no `wow64_init_entry` line appears, and the winemac.drv that loads is the **aarch64** build going through `macdrv_init` directly, not the x86_64 one through `wow64_init`; (b) `status=00000000` on both stages means that where winemac.drv IS loaded it initialises cleanly, so the defect is not a failing init but a driver that is never loaded into HK's process at all. Builds all SHA-verified against dist: win32u.so `e96c0e8c → fed7d89c → 21822c07 → 7fdb2786`, winemac.so `3cde9fe1 → eac936dc`, winemac.drv x86_64 `78cb5194 → 0f40ebdc`, aarch64 `1d3c0826 → a8089c7b`. `HK-E2E-07-REPAIR` running with the repair ON.
32 · 01:06 · ITER-2 · A/B VERDICT — **PLACEHOLDER REPAIR REFUTED AT RUNTIME, AND IT PAID FOR ITSELF ANYWAY.** `HK-E2E-07-REPAIR` with `MACRUNNER_WIN32U_PLACEHOLDER_REPAIR=1`: the repair itself executed correctly — `stage=driver_placeholder_repair_done … which=lazy placeholder=0x0`, placeholder freed — but the process ended up WORSE. The next `load_display_driver` took the fallback branch (`service=0 desktop_ok=1 was_lazy=1`) and installed `null_user_driver` with `pCreateWindow = nodrv_CreateWindow`, i.e. the **LOUD** null driver, so window creation now fails outright: `Initialize engine version` **1 → 0**, run dead at 1679 lines. This is precisely the trap the brief names — an offline proof that a mechanism is wrong is not a proof that changing it is safe — and it is exactly why this went out env-gated and default-OFF. The gate stays OFF and the code comment now records the measured regression so nobody re-enables it. My improved verdict line also did its job: `load_display_driver_ok … PLACEHOLDER KEPT — this process has NO real driver (user_driver=0x83e473700 placeholder=0x83e473700)`.
33 · 01:12 · ITER-2 · ★ THE FRONTIER IS NOW A LOADER BUG, PINNED THREE WAYS. The repair's retry produced the measurement that matters: **`stage=driver_loaddriver_callback unix_pid=26821 wine_pid=0020 ret=1 driver=L"winemac.drv"`** — `KeUserModeCallback( NtUserLoadDriver )` returns **STATUS_SUCCESS inside Hollow Knight's own process**. Three independent confirmations that nothing was actually loaded: (a) `MACRUNNER_HB_TRACE_USER_CALLBACK=1` (a pre-existing trace at `signal_arm64.c:2185`, no rebuild needed) shows callback **id=14 = NtUserLoadDriver fired exactly ONCE** in the whole run, so the dispatch into user mode really happened; (b) `User32LoadDriver` (`user32/user_main.c:130`) is nothing but `LdrLoadDll(L"c:\\windows\\system32", …)`, and **no `dllmain_attach` is ever emitted for wine pid 0020** — and that trace is now proven live, since the native explorer-class process emits a full `arch=aarch64` chain in the same log; (c) `lsof` on the live HK pid 34492 shows **no winemac.drv PE mapped at all** — not x86_64, not aarch64 — while 11 other `x86_64-windows` PEs are. The prefix content is correct (`synced …/system32/winemac.drv <= …/x86_64-windows/winemac.drv`). Notably `aarch64-unix/winemac.so` IS mapped in HK, so the loader began the load and mapped the unix companion, then returned success without ever attaching the PE. Same class as [[project_x64_present_winemetal_unixlib_uninit]].
34 · 01:14 · ITER-2 · NEXT MEASUREMENT ARMED. `hk-run-try12-config.sh` now honours `MACRUNNER_HK_WINEDEBUG` (default still `-all`; widening by accident is the thing to avoid on a throughput-starved boot). Running `HK-E2E-09-LOADDLL` with `-all,+loaddll`, tmo 900 / 2 tries, to get the loader's OWN account of what it does with `winemac.drv` in HK's process — the one thing not yet heard from directly. NB the launcher correctly waited on `slot busy` before starting, so no sister lane was trampled.
35 · 01:15 · ITER-2 · LAUNCHER DEFECT: A FALSE-BUSY SLOT CHECK THAT BURNED ITS WHOLE BUDGET AGAINST AN IDLE MACHINE. `wait_for_slot` matched `winetemp`, but a leftover `services.exe` lives at `/var/folders/…/T//winetemp-<id>/services.exe`, so the pattern matched a **path component of a stale helper**, not a live game — and `mr-clean.sh` deliberately refuses to kill it ("winetemp lives in common TMPDIR and is not worktree-scoped"). Measured: `[hk] slot busy` every 20 s while `ps` showed zero Hollow Knight and zero wineserver. Now matches `wineserver` only, which every live run has exactly one of and which still registers a sister lane's run.
36 · 01:20 · ITER-2 · ★★★★ COMPLETE ROOT-CAUSE CHAIN — **winemac.drv IS THE ONLY WINE BUILTIN IN HK'S PROCESS THAT NEVER GETS ITS NATIVE TWIN.** `HK-E2E-09-LOADDLL` with `-all,+loaddll` gives the loader's own account, and it OVERTURNS my entry-33 reading that the PE never loads: it DOES load — `0024:trace:loaddll:build_module Loaded L"c:\windows\system32\winemac.drv" at 0000087EF2E50000: builtin` — but its DllMain still never runs. Bucketing all 84 module loads on HK's thread 0024 shows why: **37 builtins load TWICE**, once from `C:\windows\system32\<dll>` (the x86_64 guest copy) and once as a NATIVE aarch64 twin from `…/dist-arm64ec-spike/lib/wine/aarch64-windows/<dll>` — kernelbase, kernel32, ucrtbase, **win32u, user32, gdi32**, ole32, winmm, bcrypt, hid, **winemetal, dxgi, d3d11** … (dxgi's twin comes from laneA's per-run `dxmt-builtin-overlay/aarch64-windows/DXGI.DLL`, which is exactly what that overlay is FOR — and why it is the 887a0002 keystone). Only 10 modules are single-view, and 9 of those are legitimately so: the game itself, UnityPlayer, mono-2.0-bdwgc, the mono profiler, xinput1_3 (all genuinely x86_64) plus ntdll/xtajit64 (the native emulation core). The tenth is **winemac.drv**. It is the native twin whose DllMain executes; without it, `macdrv_init` never runs, the process keeps the re-entrancy placeholder for life, nothing pumps AppKit, and injected keys measure 0.
37 · 01:20 · ITER-2 · WHY winemac.drv IS THE ODD ONE OUT, read from source, and the fix follows the tree's OWN precedent. `user32/user_main.c:130` — `User32LoadDriver` is `LdrLoadDll( L"c:\\windows\\system32", 0, &str, &module )`: it **PINS the search path**, so the display driver never goes through the builtin-twin resolution that every other module reaches. This is the same shape as the already-fixed "wine's dll_dir shadows DXMT's builtin twins" defect. And the tree already has the remedy pattern in MY territory: `sync-prefix-from-dist.sh` force-copies **`xtajit64.dll` from `aarch64-windows` into system32** even though `SYSTEM32_ARCH=x86_64-windows`, precisely because that module must be the native build (same for `xtajit.dll`, `comctl32_v6.dll`). Applied the identical treatment to `winemac.drv` behind **`MACRUNNER_PREFIX_WINEMAC_NATIVE`** (default OFF — this lane's rule is that an offline proof is not a runtime proof, and `MACRUNNER_WIN32U_PLACEHOLDER_REPAIR` looked just as correct offline before it regressed the guest). Running `HK-E2E-10-NATIVEDRV` as the A/B, with `+loaddll` still on so the twin load and any `dllmain_attach` for wine pid 0020 are both visible.
38 · 01:26 · ITER-2 · A/B #2 — **`MACRUNNER_PREFIX_WINEMAC_NATIVE` REFUTED, cleanly and cheaply.** The gate fired (`prefix_sync: winemac.drv <= aarch64-windows (native twin forced into system32)`) and the swap demonstrably took effect — HK's load address moved `0000087EF2E50000 → 0000087EF2E40000`, consistent with the different file size, so the ARM64 build really is what gets loaded now. And **nothing else changed**: still no `dllmain_attach` for wine pid 0020, still `load_display_driver_ok … PLACEHOLDER KEPT`. So the defect is NOT which file sits at the pinned `c:\windows\system32` path — my entry-37 fix hypothesis is wrong, and the module loads fine either way. Gate stays default-OFF. Two hypotheses tested, two refuted at runtime; both were cheap because each shipped behind its own flag.
39 · 01:28 · ITER-2 · ★★★★★ EXACT ROOT CAUSE, LOCATED TO A LINE — **THE GUEST UNIXLIB BRIDGE IS HARDCODED TO ONE MODULE.** `macrunner_hb.c:22948` (read-only for me — forbidden territory): on `NtQueryVirtualMemory(… MemoryWineUnixFuncs)` failure it bridges the guest x86_64 builtin to its native `aarch64-unix` `.so` — but only for a single hardcoded name: `if (macrunner_hb_strieq( module_name, "winemetal.dll" ))` … `dlopen("<dxmt_root>/aarch64-unix/winemetal.so")`. **`winemac.drv` is not in that bridge.** So in HK's x86_64 guest process `MemoryWineUnixFuncs` fails for winemac.drv ⇒ `__wine_init_unix_call()` fails ⇒ `dllmain.c:540` `if (status) return FALSE;` returns BEFORE `MACDRV_CALL(init)` ⇒ `macdrv_init` never runs ⇒ no `init_user_driver()` ⇒ the re-entrancy placeholder is never replaced ⇒ no Cocoa app loop ⇒ `macdrv_key_event`=0. Every observation this lane collected is explained by that one missing name, and it is the SAME failure class already recorded as [[project_x64_present_winemetal_unixlib_uninit]] — winemetal was fixed by being added to this very bridge; winemac.drv never was.
40 · 01:28 · ITER-2 · A/B #3 RUNNING — AND IT MAY NOT NEED FORBIDDEN CODE AT ALL. `macrunner_hb.c:34255` shows a GENERIC counterpart to that hardcoded bridge, already implemented and already env-gated: `macrunner_hb_builtin_unixlib_resolve_enabled()` → **`MACRUNNER_HB_BUILTIN_UNIXLIB_RESOLVE`**, whose comment describes identifying the exact caller module and using `MemoryWineUnixFuncs` as the native capability lookup for it. If that generic path covers winemac.drv, the whole defect is fixable **by configuration**, with no edit to forbidden territory. Launched `HK-E2E-11-UNIXLIBRESOLVE` with the flag on, `+loaddll` still on, watcher armed for injection if the driver comes up. If it does NOT cover winemac.drv, the fix is a one-name addition to the bridge at `macrunner_hb.c:22958` — which is another lane's file and needs the operator.
41 · 01:33 · ITER-2 · A/B #3 REFUTED TOO — `MACRUNNER_HB_BUILTIN_UNIXLIB_RESOLVE` DOES NOT COVER winemac.drv. `HK-E2E-11-UNIXLIBRESOLVE`: the bridge/resolve marker count is **0** (no `unixlib-bridge`, no `builtin-unixlib` line anywhere in 1951 lines), and HK is unchanged — `load_display_driver_ok … PLACEHOLDER KEPT (user_driver=0x9cedb8000 placeholder=0x9cedb8000)`, with the only `dllmain_attach`/`macdrv_init_entry`/`run_cocoa_app_entry` still belonging to the explorer-class process (wine 005c / unix 56886). The generic resolver is gated for a different call site than the hardcoded `MemoryWineUnixFuncs` fallback that winemetal.dll uses, so it cannot substitute for it. Three fix hypotheses tested this iteration, three refuted at runtime, each behind its own default-OFF flag so none can bite later: placeholder-repair (regressed the guest), native-drv-in-system32 (no effect), generic-unixlib-resolve (never fires).

LOOP-STATUS: BLOCKED

**The one thing only the operator can decide:** the fix is to add `winemac.drv` (→ `aarch64-unix/winemac.so`) alongside `winemetal.dll` in the guest unixlib bridge at `engine/wine/dlls/ntdll/unix/macrunner_hb.c:22958`, which is **explicitly forbidden territory for this lane** and currently carries another lane's uncommitted work — so please either grant this lane an exception for that one `strieq` branch, or route it to the lane that owns the file.

**Everything needed to land it in one edit, measured this iteration:**
- **Root cause:** `MemoryWineUnixFuncs` fails for `winemac.drv` in HK's x86_64 guest process ⇒ `__wine_init_unix_call()` fails ⇒ `dllmain.c:540` returns FALSE **before** `MACDRV_CALL(init)` ⇒ `macdrv_init` never runs ⇒ `init_user_driver()` never replaces the re-entrancy placeholder ⇒ no Cocoa app loop in the process that owns HK's window ⇒ `macdrv_key_event`=0. Same failure class as [[project_x64_present_winemetal_unixlib_uninit]]; winemetal was fixed by being added to this very bridge, winemac.drv never was.
- **The discriminator:** in HK's process **37 wine builtins load twice** (x86_64 guest copy + native aarch64 twin) — win32u, user32, gdi32, dxgi, d3d11, winemetal … — and `winemac.drv` is the ONLY wine builtin that gets no twin. The other 9 single-view modules are legitimately single (the game, UnityPlayer, Mono, xinput1_3, ntdll, xtajit64).
- **Expected evidence when fixed:** `stage=dllmain_attach arch=x86_64` and `stage=macdrv_init_entry` for wine pid **0020** (HK), `load_display_driver_ok` flipping from `PLACEHOLDER KEPT` to `real driver kept`, then `macdrv_key_event`/`macdrv_send_keyboard_input_sent` > 0 under `tools/hk_e2e_key_watch.py`, which is already written and needs no changes.
- **Ready to verify immediately:** `MACRUNNER_HK_INPUT_EVIDENCE=1 scripts/hk-run-try12-config.sh <tag> 5400 4` + `python3 tools/hk_e2e_key_watch.py <evidence-dir>`. HK reaches its menu on this launcher (`Restored language`=1, `CreateSwapChainForHwnd`=2, `Loaded Objects now`=1) and the window renders (`nonblack` 0 → 23876), so the screenshot-diff half of the objective is available the moment keys land.
