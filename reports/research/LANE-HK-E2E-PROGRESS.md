03:41 · BACKEND FAILOVER · `codex` retired after 2 consecutive returns under 180s with no journal line; continuing on `claude`.

03:45 · ITER-3 · ★ THE PREVIOUS ROOT CAUSE IS INCOMPLETE, AND THE MEASUREMENT THAT SETTLES IT IS ALREADY ON DISK. The coordinator's ungated probe answered its own question: in `laneA-MISSPROBE-try25-a1-try1-033806` (the freshest run, dist ntdll.so verified to CONTAIN the string) `macrunner-hb-unixfuncs-miss` = **0** across 2129 lines. So it is not "the name did not match" — `MemoryWineUnixFuncs` never fails for winemac.drv in HK's process because that query is never made there at all. Confirmed independently from the unix side: `stage=wow64_init_entry` = **0** in all 20 of the day's HK run dirs, and HK is an x86_64 PE whose `MACDRV_CALL(init)` would land in `wow64_init`. The PE-side init never starts, so the unixlib bridge it was supposed to need is downstream of a wall that is never reached.
03:45 · ITER-3 · ★★★ AND THE DRIVER IS NOT ACTUALLY ABSENT FROM HK'S PROCESS — ONLY UNINITIALISED. Same runs, HK's own unix pid (96301 = wine-child.pid): `macrunner-get-win-data`=1, `macrunner-winrealize: d3d_on_demand hwnd=0x30038 result=0x7d5ab2c30 cocoa=0x7dc1c0000 … rects={ window (0,0)-(1512,982) }`=1, `stage=create_cocoa_window pid=96301`=1. Those all live in **winemac.so** (window.c / d3dmetal.c are unixlib sources — `Makefile.in`: `UNIXLIB = winemac.so`, only dllmain.c is the PE). DXMT reaches them through the exported `macdrv_functions` table at `d3dmetal.c:424`, which needs neither `__wine_init_unix_call` nor a DllMain. So winemac.so is mapped, live and executing in HK's process; what never runs is `macdrv_init` — hence no `macdrv_start_cocoa_app`, no `init_user_driver()`, and win32u keeps `load_display_driver`'s re-entrancy placeholder for life (`PLACEHOLDER KEPT`=1, measured again this run).
03:52 · ITER-3 · FIX IMPLEMENTED, ENTIRELY IN THIS LANE'S TERRITORY — unix-side self-init. `macdrv_main.c`: split `macdrv_init`'s body into `macdrv_init_core(strings, origin)` (once-only under a pthread mutex, `strings` may be NULL) and added `macdrv_process_selfinit()`, gated `MACRUNNER_MACDRV_UNIX_SELFINIT=0` for the A/B arm. `d3dmetal.c`: call it at the top of `my_get_win_data` — the FIRST winemac.so entry HK's process demonstrably reaches. `cocoa_app.m`: `WineLocalizedString()` now falls back to the winemac.rc English defaults, because the self-init path has no PE to `LoadStringW` from and every caller feeds the result to `-[NSMenu initWithTitle:]` / `-[NSMenuItem setTitle:]`, which raise on nil. No forbidden file touched; the granted `macrunner_hb.c` exception was NOT needed and that file is left exactly as the coordinator left it.
03:56 · ITER-3 · DEPLOY VERIFIED BY SHA, NOT EXIT CODE. `make` + `make install` rc=0; `dist-arm64ec-spike/lib/wine/aarch64-unix/winemac.so` `eac936dc…` → `6fabe675…`, and the binary contains `macdrv_selfinit_entry`, `MACRUNNER_MACDRV_UNIX_SELFINIT`, `macdrv_init_core_already` and the English menu fallbacks. The PE twins are byte-identical (`winemac.drv` aarch64 `a8089c7b`, x86_64 `0f40ebdc` unchanged), which is the expected signature of a unixlib-only change.
03:58 · ITER-3 · LAUNCHER DEFECT FIXED BEFORE RUNNING: the GOOD-BOOT gate was `grep -q 'Mono path'`, which every boot prints at ~+50 s — including the ones that die at ~+150 s right after the first swapchain (E2E-08, MISSPROBE). It therefore declared success for menu-less runs AND stopped the retry loop from retrying. Gate is now `Restored language`, try12's own menu signature (E2E-05: +370 s, `Loaded Objects now` +424 s, run alive to +1061 s), overridable via `MACRUNNER_HK_GOOD_BOOT_MARKER`. Launched `HK-E2E-12-SELFINIT`, tmo 1800, up to 6 attempts, with `tools/hk_e2e_key_watch.py` supervised per attempt so a dead boot cannot overwrite a good capture.

04:12 · ITER-3 · ★★★★★ THE FIX WORKS AT THE DRIVER LEVEL — HK'S PROCESS HAS THE REAL MAC DRIVER FOR THE FIRST TIME. `laneA-HK-E2E-12-SELFINIT-a1-try1-040039`, HK's own unix pid 24355 = wine pid 0020: `stage=macdrv_selfinit_entry` (+140.752s) → `macdrv_init_session … graphic=1 origin=selfinit` → `run_cocoa_app_entry pid=24355 nsapp=0x0 main_thread=1` → `run_cocoa_app_decide … class=NSKVONotifying_WineApplication created_app=1 success=1 already_entered=0 will_run=1` → `macdrv_init_user_driver_set origin=selfinit` → **`stage=user_driver_placeholder_replaced pid=0020 tid=0024 — real driver installed over the re-entrancy stand-in`**. Then `create_cocoa_window pid=24355 hwnd=0x30038 cocoa=0x99c234000 queue=0x9965448c0 style=0x94000000` and a 1512×982 window the watcher independently found on screen via CGWindowList (`owner_pid 24355`, `window_id 26180`). Every prior run of this lane ended `PLACEHOLDER KEPT` for that pid; this one ends `placeholder_replaced`, and 88894334's CAS retry is what let the late driver win.
04:12 · ITER-3 · AND THE BOOT WENT FURTHER THAN THE BASELINE, NOT LESS FAR. Baseline (MISSPROBE, same launcher, same dist minus this change) died/timed out with `Restored language`=0. This run reached `Restored language code 'EN'` at **+591 s** — the menu signature — having passed +153 s, where E2E-08 and MISSPROBE both ended. So the self-init did not regress the guest on this sample [HYPOTHESIS: one sample; HK's boot is stochastic across exit=66/5/53/1].
04:12 · ITER-3 · BUT INPUT IS STILL 0, AND THE FRAME IS BLACK — both measured, neither explained yet. Injection ran at the menu gate (`down`,`down`,`up`,`return`; the injector reports 4 posted CGEvents each): `macdrv_key_event`=0, `macdrv_send_keyboard_input_sent`=0, `macrunner-return-route`=0, and all four window-scoped captures come back `nonblack=0 colorful=0` (1512×982, `status: PASS`), so there is no visible menu to change either. `MACRUNNER_TRACE_WINEMAC_KEYS` IS in the run's `final-child.json` environment, so the 0 is a real measurement, not a dark gate. Run ended +661 s, `Abort trap: 6` / exit=134, on a Mono worker (`macrunner-hb-run-exit status=c000007b pc=…mono-2.0-bdwgc.dll`, `last_bytes=cc` = int3) — the documented HK c000007b worker-thread death class, ~19 s after the first injection [HYPOTHESIS: correlation not causation; not A/B'd].
04:20 · ITER-3 · INSTRUMENTING THE ONE EDGE THAT DECIDES THE NEXT FIX. `-sendEvent:` has never been observable for keys: `trace_ui_input_event()` early-returns on anything that is not a mouse BUTTON event, so "AppKit delivered the key and it was dropped for want of a key window" and "the key never arrived" are currently indistinguishable — and they need opposite fixes. Added `trace_key_input_event()` (cocoa_app.m) on the same narrow KEYS gate, budgeted to 60 lines, printing `window / keyWindow / mainWindow / isActive / activationPolicy` with each key event, called from `-[WineApplication sendEvent:]` and `-[WineApplicationController handleEvent:]`; plus an ungated one-shot `stage=nsapp_run_entering pid=… policy=… active=… keyWindow=… windows=…` in cocoa_main.m, because `will_run=1` is a decision and `nsapp_run_returned=0` is equally consistent with "running" and "never entered". Also added the default-OFF lever `MACRUNNER_MACDRV_FORCE_KEY_WINDOW` on the on-demand realize path, ready to A/B the moment the trace says `keyWindow=(nil)`, and turned on `MACRUNNER_HB_TRACE_WINSHOW` in the evidence block so the show/activate transitions are visible at all.

04:20 · COORDINATOR — ★★★ THE LOSS POINT IS ONE LINK EARLIER THAN THE UNIXLIB BRIDGE. Measured with two UNGATED probes now in
the shipped ntdll (`macrunner-hb-ntdll-memory-semantic`, once per process after the ntdll/NtQueryVirtualMemory filters, and
`macrunner-hb-unixfuncs-query`, every MemoryWineUnixFuncs query with module+status, deliberately OUTSIDE the failure branch):
BOTH read 0 in HK-E2E-13-KEYTRACE, which ran on the artifact containing them (built 04:16:52, run started 04:17:41,
strings-verified 1/1/1 in dist ntdll.so).
And the direct measurement: in that run `stage=dllmain_attach` for winemac.drv occurs EXACTLY ONCE — `arch=aarch64 pid=005c`,
the explorer-class process — and `dllmain_unixcall_init` there returns `status=00000000`. There is NO dllmain_attach for
wine pid **0020**, which is HK (both pids appear in the log). So in Hollow Knight's process winemac.drv's **DllMain never runs
at all**; `__wine_init_unix_call()` is INSIDE DllMain, so it cannot be failing — it is never reached, and no unixlib bridge
can help. My `winemac.drv` branch in the MemoryWineUnixFuncs bridge is therefore aimed one link too late. It is harmless
(default-ON, `MACRUNNER_HB_WINEMAC_UNIXLIB_BRIDGE=0` to disable) and can stay while the real question is answered.
▶ **THE QUESTION, narrowed:** the loader reports winemac.drv LOADED in HK (`+loaddll`: `Loaded L"c:\windows\system32\winemac.drv"
… builtin`, measured in iteration 2) yet its DllMain does not execute — in an x86_64 guest process, while the same module's
DllMain runs fine in the native aarch64 explorer. Why does a loaded builtin PE's process-attach not run in the x86_64 guest?
That is a loader/ARM64EC question, not a unixlib one.
▶ **Note on my own instrument, so it is not repeated:** the first version of the probe sat inside
`if (status && info_class == MemoryWineUnixFuncs)`, so its zero could equally mean "never ran" or "ran and succeeded".
Both probes are now outside that branch. And an "ARTIFACT UNCHANGED" SHA guard gave a false abort at 03:57 — the SHA was
unchanged because a sister lane had already built the identical sources. Verify by CONTENT (`strings` for the probe) as well
as by SHA.

04:48 · ITER-3 · ★★★★★ THE LAST BREAK IN THE KEYBOARD CHAIN, MEASURED TO A BRANCH. `laneA-HK-E2E-13-KEYTRACE-a1-try1-041741` — the injected keys DO reach Wine. 14 trace lines, all four keys, down and up: `stage=app_sendEvent_key_enter pid=38127 type=10 keycode=125 window=0x876864000 class=WineWindow keyWindow=0x0 mainWindow=0x0 active=0 policy=0` and the identical `controller_handleEvent_key`. So AppKit delivers them, they carry the correct WineWindow, `[NSApp run]` is genuinely entered (`stage=nsapp_run_entering pid=38127`, which retires the `will_run=1`-is-only-a-decision ambiguity), the process IS a regular app (`policy=0`) — and `macdrv_key_event` is STILL 0. The reason is `-[WineApplicationController handleEvent:]`: its `NSEventTypeKeyDown` branch only bypasses AppKit for `kVK_Help`; everything else leaves `ret = FALSE`, so `-[WineApplication sendEvent:]` falls through to `-[NSApplication sendEvent:]`, whose keyDown routing targets the **key window** — and there is none (`keyWindow=0x0`), so the event is dropped before `-[WineWindow keyDown:]` → `postKeyEvent:`. The KeyUp branch then drops the up too, because `isKeyPressed:` is false without the down.
04:48 · ITER-3 · WHY "NO KEY WINDOW" IS NOT AN UPSTREAM BUG TO FIX INSTEAD. The show path demonstrably ran and did everything it could: `f2-before-async-show … activate=1`, `objc-before/after-order-front`, `objc-before/after-window-got-focus`, and the new `MACRUNNER_MACDRV_FORCE_KEY_WINDOW=1` lever fired (`macrunner-winrealize: forced_key_window hwnd=0x30038 cocoa=0x876864000 on_screen=1`) — and 22 minutes later `keyWindow` is still nil with `active=0`. macOS will not hand an inactive, non-frontmost process a key window, and `CGEventPostToPid` exists precisely so injection does NOT need the app to be frontmost. The event names its target window; the fix is to deliver it there.
04:48 · ITER-3 · FIX #2 IMPLEMENTED (cocoa_app.m, my territory): in `handleEvent:`'s KeyDown branch, when `[NSApp keyWindow]` is nil AND `[anEvent window]` is a WineWindow, `[anEvent.window sendEvent:anEvent]` and return handled — the same shape as the existing kVK_Help bypass and the KeyUp branch, reusing `-[WineWindow keyDown:]` so repeat handling and `noteKey:` bookkeeping stay identical (which also makes the existing KeyUp branch start working). Deliberately narrow: when a key window DOES exist, AppKit's routing is untouched. A/B: `MACRUNNER_MACDRV_KEYDOWN_NO_KEYWINDOW=0`.
04:48 · ITER-3 · ALSO MEASURED THIS RUN, both recorded as open items: the boot STALLED — log frozen at +342 s while the process burned 204% CPU for 18 min (`Restored language`=0), so no menu; and every window capture is `nonblack=0` in BOTH runs, i.e. the window is black even in the run that reached `Restored language`. The visible-change half of the objective needs the frame, not just the key. Run 12 (no force-key-window) reached the menu at +591 s; run 13 (with it) did not [HYPOTHESIS: 1 sample each, HK's boot is stochastic — not attributed to the lever].

05:15 · ITER-3 · FIX #2 FIRES BUT THE CHAIN STILL ENDS SHORT — AND THE NEXT TWO TRACES WERE DARK. `laneA-HK-E2E-14-KEYDOWN-a1-try1-044620`: `controller_keydown_no_keywindow`=4 (the bypass runs for every injected keyDown, `keyWindow=0x0` confirmed at each), `app_sendEvent_key_enter`=7 — and `macdrv_key_event` still 0. The two traces that would say whether `[anEvent.window sendEvent:]` reached `-[WineWindow keyDown:]` (`wine_window_keyDown`, `postKey_posted`) are in cocoa_window.m and were gated on `MACRUNNER_TRACE_WINEMAC_INPUT`, which no HK run sets — so their silence proved nothing, exactly the trap this lane keeps hitting. Moved both to a new `macrunner_key_trace_enabled()` (KEYS gate, key events only) and added `stage=wine_window_sendEvent_keydown … firstResponder=%p class=%s isKey=%d`, because `-[WineWindow sendEvent:]` dispatches keyDown via `[[self firstResponder] keyDown:]` and a nil/absorbing first responder is the remaining candidate. Deployed, `winemac.so` `fb250849…` → `90c47289…`.
05:15 · ITER-3 · ALSO CONFOUNDED, RECORDED HONESTLY: that run's injection happened while the guest was STALLED (log frozen at 1910 lines for ~20 min at 204% CPU), and `macdrv_key_event` is only reached when the guest drains the macdrv queue — so a 0 there is not evidence against the Cocoa-side fix. The run then un-stalled and printed `Restored language` at +1574 s, 58 s AFTER the watcher had finished, and died at +1613 s (`Abort trap: 6`, exit=134, same Mono c000007b class as run 12). Manual injection at that moment posted (`verdict: POSTED`) but the process was already dying.
05:30 · ITER-3 · WATCHER DEFECT FIXED: `hk_e2e_key_watch.py` returns 0 after "injecting anyway for driver-side counts", so the supervisor treated a menu-less attempt as success and stopped retrying while the launcher was still going — that is how run 14's actual menu was missed. The supervisor now ends the loop only when `evidence.json` reports `menu_ready: true`, and `HK_MENU_WAIT` is raised to 1500 s (measured menu arrivals: +591 s and +1574 s).
05:30 · ITER-3 · 40-SECOND HARNESS ATTEMPT — ABANDONED, WITH THE REASON. Built `tools/winkeyprobe.c` both as x86_64 and aarch64 and added the test-only lever `MACRUNNER_MACDRV_TEST_NO_KEYWINDOW=1` (makes `-[WineWindow canBecomeKeyWindow]` return NO) to reproduce HK's state in seconds. Two blockers, both measured: mr-run.sh's contract preflight refuses a non-HK exe (`canonical_{config,data,save}_path_authority_absent`, then `final child capture failed: ValueError` even with the three paths supplied), and run standalone in a seeded prefix the probe reproduces a DIFFERENT wall — the x86_64 build never triggers `macdrv_process_selfinit` because that hook is on DXMT's `get_win_data`, which a GDI-only program never calls, and the aarch64 build ends at `load_display_driver_reentrant` with no explorer to hand off to. The lever is committed and harmless (default OFF) but the harness needs mr-run parity to be worth more time. Relaunched HK as `HK-E2E-15-KEYCHAIN` with the full key-chain trace instead.

05:33 · ITER-3 · ★★★★★ THE COCOA-SIDE KEYBOARD CHAIN NOW COMPLETES IN HOLLOW KNIGHT'S PROCESS. `laneA-HK-E2E-15-KEYCHAIN-a1-try1-052550`, pid 73498, every injected key, with `keyWindow=0x0` and `active=0` throughout:
  `app_sendEvent_key_enter` → `controller_handleEvent_key` → `controller_keydown_no_keywindow` → `wine_window_sendEvent_keydown hwnd=0x30038 firstResponder=0x939b0a400 class=WineContentView isKey=0` → `wine_window_keyDown hwnd=0x30038 keycode=125 repeat=0 key=0 app_active=0` → **`postKey_posted hwnd=0x30038 keycode=125 pressed=1 queue=0x93868c880`**.
KeyUp posts too (`pressed=0`), which confirms the designed side effect: delivering the KeyDown restores `noteKey:` bookkeeping so the pre-existing KeyUp branch starts working. So `MACRUNNER_MACDRV_KEYDOWN_NO_KEYWINDOW` is VERIFIED at runtime, not just offline — the key reaches Wine's macdrv event queue in a process that has no key window and is not active, which is precisely what `CGEventPostToPid` needs and what AppKit's key-window routing structurally cannot do. `isKey=0` and `firstResponder=WineContentView` also retire the last alternative hypothesis: the responder chain was never the problem.
05:33 · ITER-3 · CAVEAT ON THIS SAMPLE, STATED PLAINLY: those keys were injected at +142 s by a leftover `until`-loop from the abandoned probe harness, i.e. ~7 minutes before HK's menu exists, so `macdrv_key_event` (which only runs when the GUEST drains the queue in ProcessEvents) is still 0 and the guest-side half is NOT yet proven. The Cocoa half is. The run is still live with the watcher armed for a menu-gated injection.

05:55 · ITER-4 · STATE ON ENTRY, MEASURED NOT ASSUMED. Nothing was running: run 15 (`laneA-HK-E2E-15-KEYCHAIN-a1-try1-052550`) died at **+143.6 s** with its log truncated mid-line and `macrunner-hb-run-exit`=0 — i.e. killed, not crashed, when the previous iteration's session ended. So the 05:33 note "the run is still live with the watcher armed" was already false when it was written; the menu-gated injection it promised never happened. Confirmed counts in that run: `postKey_posted`=6, `wine_window_keyDown`=4, `controller_keydown_no_keywindow`=4, `app_sendEvent_key_enter`=7, `macdrv_key_event`=**0**, `Restored language`=**0** (died 7 min before any menu).
05:55 · ITER-4 · WHY `macdrv_key_event`=0 IS STILL UNDECIDED, AND THE PROBE THAT DECIDES IT. Three different bugs are all consistent with that zero: (a) the guest never pumps messages, (b) it pumps but nobody drains the queue `postKeyEvent:` wrote to — HK's window is realized on demand from DXMT's `get_win_data`, i.e. on whichever thread asks first, not necessarily the thread that pumps — or (c) it was injected 7 minutes before the guest was ready, which is what run 15 actually did. They need opposite fixes. Added `macrunner_note_drain()` to `event.c`: one line per (tid, queue) pair on first sight plus log-spaced call counts (~7 lines per pumping thread, so it is affordable on a full boot, unlike `ProcessEvents_enter/_exit` which fires twice per frame), and an unconditional `ProcessEvents_key_dequeued` for KEY_PRESS/KEY_RELEASE. Gate `MACRUNNER_TRACE_WINEMAC_DRAIN`, default-on under the existing KEYS gate.
05:55 · ITER-4 · DEPLOY VERIFIED BY SHA **AND** CONTENT. `dist-arm64ec-spike/lib/wine/aarch64-unix/winemac.so` `90c47289…` → `d5c25f9c…`, and `strings` finds `ProcessEvents_drain`, `ProcessEvents_key_dequeued`, `MACRUNNER_TRACE_WINEMAC_DRAIN` = 1/1/1 alongside the retained `postKey_posted` / `controller_keydown_no_keywindow` / `macdrv_selfinit_entry`.
05:55 · ITER-4 · SUPERVISOR REWRITTEN SO A RUN CANNOT BE SCORED AGAINST ITS PREDECESSOR. New `scripts/hk-e2e-supervise.sh`: deletes `/tmp/laneA-current-rundir.txt` before each attempt and refuses to start the watcher until a NEW one appears (it holds the previous run's dir for ~40 s while mr-run boots — that is how run 14's real menu was missed); scores ONLY on `evidence.json.menu_ready == true`, never on the watcher's exit status (the watcher returns 0 after "injecting anyway for driver-side counts"); tears down by walking the PID tree of the launcher it started, never by name. On success it LEAVES THE RUN ALIVE — the menu costs 10–26 min to reach and further evidence is far cheaper from a live process. Watcher also now dumps the run.log delta after every injection (`delta-*.log`), because the objective accepts "a new marker in the log" as the observable change and Return at a menu produces a scene load.
05:55 · ITER-4 · LAUNCHED `HK-E2E-16-DRAIN`, 4 attempts × 2700 s, detached (nohup) so it survives this iteration's boundary — the defect that killed run 15. Run dir `laneA-HK-E2E-16-DRAIN-t1-a1-try1-055535`. Slot was verified free (`wineserver` count 0) before launch.

06:05 · ITER-4 · PROBE HOLE FOUND AND CLOSED BEFORE IT COULD MISLEAD. `macdrv_ProcessEvents` early-returns `if (!data) return FALSE;` ABOVE the new drain probe, so "HK never pumps messages" and "HK pumps but the pumping thread never ran `macdrv_init_thread_data`" would both have read as zero drain lines — the same class of ambiguity that cost this lane the `MemoryWineUnixFuncs` detour. Added `macrunner_note_drain(NULL,-1,-1)` on that branch (`queue=(nil) fd=-1 handled=-1` is the no-thread-data signature). Rebuilt and deployed ATOMICALLY (`cp` to a temp name then `mv`) so the live attempt-1 process keeps its old inode: `winemac.so` `d5c25f9c…` → `b4580efc…`. Attempt 1 therefore runs WITHOUT the entry probe and attempt 2+ with it — recorded so the two are not compared naively.
06:05 · ITER-4 · THE ORPHANED-QUEUE HYPOTHESIS IS REFUTED FROM DATA ALREADY ON DISK, so no fix is needed for it. In run 15, `create_cocoa_window pid=73498 hwnd=0x30038 … queue=0x93868c880` and `postKey_posted … queue=0x93868c880` are the SAME queue, and `window.c:707` makes that structural, not lucky: `macdrv_create_win_data` returns NULL unless `NtUserGetWindowThread(hwnd) == GetCurrentThreadId()`, so the on-demand realize can only ever run on the HWND-owning thread and `create_cocoa_window` takes that thread's queue. The key is posted to the queue of the thread that owns the window.
06:05 · ITER-4 · AND THE GUEST-SIDE INSTRUMENT IS BETTER THAN "WM_KEYDOWN" FOR A UNITY TITLE. Read `win32u/input.c` (read-only — forbidden territory): `macrunner_return_route_observe` is called from `NtUserGetAsyncKeyState`'s two return paths (`unity-async-state-query`, line 872/891) and from `unity-key-state-query`, and only when the result is NON-ZERO. So the marker fires exactly when the GUEST asks for VK_RETURN and the shared desktop keystate reads it as pressed. Unity polls input rather than consuming WM_KEYDOWN, so this is the true guest-receipt proof for this title. Note also `input.c:862`: `NtUserGetAsyncKeyState` calls `check_for_events(QS_ALLINPUT)` → `process_driver_events` → `user_driver->pProcessEvents` — i.e. a polling Unity game drains the macdrv queue itself, which is why the drain probe should light up once HK starts polling input, and why run 15's injection at +142 s (Mono init, before any input polling) could not have produced `macdrv_key_event` regardless of the Cocoa fix.
06:05 · ITER-4 · ★ THE BLACK FRAME IS REAL, NOT A CAPTURE-PERMISSION ARTIFACT — CONTROL RUN. `tools/cg_window_capture` on two non-Wine windows in the same session, same binary, same TCC context: Terminal `nonblack=228486`, Finder `nonblack=400581 colorful=540`, both `capture_mode=ScreenCaptureKit-window status=PASS`. HK's live window (id 26250, 1512×982) in the same minute: `nonblack=0 colorful=0`. So every prior `nonblack=0` in this lane measured the game, not a missing Screen Recording grant [caveat: HK was mid-boot at capture time, so black is EXPECTED here — the control only retires the tooling explanation, it does not yet say the menu is black].
06:05 · ITER-4 · SELF-INFLICTED INJECTION, RECORDED BECAUSE IT IS IN THE RUN'S COUNTERS. `hk_cgevent_key --help` is not a recognised flag and the tool fell through to its default action, posting one real `down` keypress into live attempt 1 at ~+348 s. It produced `postKey_posted hwnd=0x30038 window=0x6fb7c8000 keycode=125 pressed=1/0 queue=0x6e5840a00` — so the Cocoa chain is confirmed a second time, in a second process — but attempt 1's `postKey_posted` count now includes 2 events that the watcher did not schedule.
06:20 · ITER-4 · ★ THE OBSERVABLE-CHANGE HALF NOW HAS AN ORACLE-ANCHORED MARKER, NOT A PIXEL DIFF. From `HK-BLACK-FRAME-ROOT-CAUSE-VERDICT-20260727.md` (lane HK-MONO, callers decompiled): `Making UI menu lean.` comes from `UIManager::MakeMenuLean`, whose ONLY callers are `<RunStartNewGame>d__359` / `<RunContinueGame>d__361`, and every caller of `StartNewGame` is an input handler (`StartGameEventTrigger::OnSubmit`, `SaveSlotButton::OnSubmit`, scene-wired UnityEvents) — no timer, autostart or command-line path exists. The same report measures that our runs ALREADY load the menu scene (`Loaded Objects now: 47240` vs oracle `47392` = 99.68 %) and that the first oracle line we lack is the first line that needed a button press. So that marker is the strongest available proof of guest-visible input, and it is independent of the VS-constant-buffer defect (`DEGENERATE,NAN_OR_INF` at draw=546) that makes the menu render black — which is in forbidden territory and which the objective explicitly does not require me to fix ("a new marker in the log, or a screenshot diff"). Watcher now counts these markers as a DELTA across the injection, presses `return`→`space`→`return` (Unity's StandaloneInputModule submits on both; HK's flow is Start Game → save slot → confirm) and stops the moment the marker appears; `tools/hk_menu_prove.py` added as the one-shot equivalent for an already-live run.
06:20 · ITER-4 · BOOT TRAJECTORY OF THE LIVE RUN IS WITHIN NOISE OF THE RUN THAT REACHED THE MENU: `Mono path` +46.0 s vs +46.7 s, `Initialize engine version` +50.7 s vs +50.5 s, `CreateSwapChainForHwnd` +141.4 s vs +140.8 s, lines at +523 s = 1905 vs 2005 (run 12 printed `Restored language` at +591.4 s, line 2096). Counters at +523 s: `ProcessEvents_drain`=1 and that one is **pid 88323, not HK's 88263** — so HK's own process has not called `macdrv_ProcessEvents` yet, exactly as expected while it is still in Mono init and not yet polling input. Report: `reports/phase4-hollow-knight/HK-E2E-ITER4-KEYCHAIN-DRAIN-20260729.md`.

06:14 · ITER-5 · ★★★★★ THE GUEST-SIDE HALF OF THE KEYBOARD CHAIN IS NOW PROVEN IN HOLLOW KNIGHT'S OWN PROCESS — `macdrv_key_event` IS NO LONGER 0. Live run `laneA-HK-E2E-16-DRAIN-t1-a1-try1-055535`, HK's own pid 88263, at +1024.671s, six consecutive lines on ONE thread (tid 29913353):
  `ProcessEvents_key_dequeued pid=88263 tid=29913353 queue=0x6e5840a00 type=8/KEY_PRESS window=0x6fb7c8000`
  → `macdrv_key_event hwnd=0x30038 window=0x6fb7c8000 keycode=125 pressed=1 vkey=0x28 scan=0x150`
  → `macdrv_send_keyboard_input_sent hwnd=0x30038 vkey=0x28 scan=0x50 flags=0x1`
  → the identical triple for `type=9/KEY_RELEASE` (`pressed=0`, `flags=0x3`)
  → `ProcessEvents_drain pid=88263 tid=29913353 queue=0x6e5840a00 fd=220 calls=1 handled=3`.
Counts measured, not expected: `macdrv_key_event`=2, `macdrv_send_keyboard_input_sent`=2, `ProcessEvents_key_dequeued`=2, `ProcessEvents_drain`=2. keycode 125 = kVK_DownArrow mapped to vkey 0x28 = VK_DOWN, i.e. `macdrv_compute_keyboard_layout` produced a correct table on the self-init'd thread — a risk this lane had flagged and never tested. The chain host→guest is therefore complete from `CGEventPostToPid` all the way to `NtUserSendHardwareInput`, in a process with `keyWindow=0x0` and `active=0`.
06:14 · ITER-5 · AND THE DRAIN PROBE ANSWERED ITS DESIGNED QUESTION THE OTHER WAY ROUND. `calls=1` on that line says this was HK's **first ever** `macdrv_ProcessEvents` call, at +1024s. The key it delivered had been posted at **+348s** — 676 seconds earlier, by the accidental `--help` injection recorded at 06:05. So the macdrv queue RETAINED it across the whole Mono-init stall and handed it over the moment the guest first pumped. That retires the "injected too early" failure mode outright: **keys injected before the guest is ready are queued, not lost**, which is why every previous run's `macdrv_key_event`=0 was a statement about the guest never pumping, never about the Cocoa side. No `queue=(nil) fd=-1 handled=-1` line was ever emitted for pid 88263, so the no-thread-data branch is also ruled out.
06:14 · ITER-5 · WHAT IS STILL NOT PROVEN, STATED PLAINLY. `macrunner-return-route`=0 and `Making UI menu lean`=0, so the GUEST-VISIBLE half — WM_KEYDOWN reaching HK's message loop and changing something — is NOT yet demonstrated; the chain is proven only as far as `NtUserSendHardwareInput`. `Restored language`=0 at +1030s, i.e. no menu yet in this run. The window (id 26250, 1512×982) captures `nonblack=0 colorful=0` at +1030s — consistent with the known VS-constant-buffer black-frame defect in forbidden territory, which is why the objective's "new marker in the log" branch is the one this lane must land.
06:14 · ITER-5 · THE MENU SIGNATURE AND THE SIZE OF THE WINDOW TO HIT IT, read from run 12's log rather than assumed: `Couldn't find a Game Manager` +583s → `Discovered supported languages: DE, EN, …` +589s → `Loaded system language 'English'` +591s → `Restored language code 'EN'` +591.4s — and then run 12 DIED at +660s, `macrunner-hb-jit-fallback label=thread pc=0x87ef28ca08f out=UNSUPPORTED_OPCODE reason='JIT codegen failed' steps=0 gbytes=cc eb 92 48`, module `mono-2.0-bdwgc.dll` rva 0x4ea08f. So the post-menu window in the one run that reached the menu was **69 seconds**, and the thing that closed it is the documented Mono int3/c000007b class in FORBIDDEN territory (hyperbridge). The watcher polls at 20 s, so it can fit inside that window, but it has little margin [HYPOTHESIS: one sample of the menu→death interval].

06:35 · ITER-5 · ★ RUN 16 IS THE FIRST RUN IN THIS PROJECT'S HISTORY WHERE A KEY REACHED THE DRIVER INSIDE HK — SWEEP OVER ALL 34 HK RUNS ON DISK THAT SHOW ANY MENU MARKER. `stage=macdrv_key_event` is **2 in `laneA-HK-E2E-16-DRAIN-t1-a1-try1-055535` and 0 in the other 33**. Also measured across the same 34: `Making UI menu lean`=**0 everywhere**, `Opening sequience`=0 everywhere, `Levels are ready`=0 everywhere, `macrunner-return-route`=**0 everywhere**. So the guest has never once been observed receiving VK_RETURN, and the Start-Game handler has never once run — the objective's observable-change marker has no precedent to regress from.
06:35 · ITER-5 · THE MENU-SCENE MARKER SEPARATES THE OLD RUNS FROM THE NEW ONES, AND THAT IS A REGRESSION SIGNAL I AM NOT GOING TO BURY. `Loaded Objects now` (Unity's post-scene-load line) is present in ~30 of the 34 runs — including `laneA-HK-E2E-05` (00:29 today, LO=1) and `laneA-JITHINT-try20` (LO=1) — but is **0 in all three runs built on the unix-side self-init**: E2E-12 (04:00), E2E-14 (04:46), E2E-16 (05:55). 3/3 after the change, ~30/31 before it. [HYPOTHESIS, 3 samples, not A/B'd: standing up a real Cocoa app + NSApp run loop + window inside HK's process — which had never happened before selfinit — costs enough throughput or perturbs timing enough that HK dies in Mono before the menu scene finishes loading. The lever to test it is already built and default-on: `MACRUNNER_MACDRV_UNIX_SELFINIT=0`.] Note the tradeoff is real, not free: with selfinit off there is no driver and therefore no input at all, so this is a measurement to understand the cost, not a candidate fix.
06:35 · ITER-5 · THE DEATH IS DETERMINISTIC TO THE BYTE, TWO INDEPENDENT RUNS. Run 12 and run 16 both end at `macrunner-hb-jit-fallback label=thread pc=0x87ef28ca08f out=UNSUPPORTED_OPCODE reason='JIT codegen failed' steps=0 gbytes=cc eb 92 48 …`, `macrunner-hb-runtime-fail … module=mono-2.0-bdwgc.dll rva=0x4ea08f`, then `Abort trap: 6` / exit=134. Same pc, same rva, same leading bytes; `cc` is int3, so a block is being entered at a breakpoint/padding byte — the documented Mono-JIT indirect-target class. Interval from `Restored language` to death: run 12 **69.1 s** (+591.4→+660.5), run 16 **43.2 s** (+1327.9→+1371.1). This is in FORBIDDEN territory (`engine/hyperbridge/**`) and I did not touch it; I checked for an env-only lever and there is none — the only `MACRUNNER_HB_*` vars near this path are trace-only (`TRACE_INTERP_FAIL`, `TRACE_FAULTS`), with no fallback/force-interp/tolerate switch.
06:35 · ITER-5 · ★★ HARNESS DEFECT, AND IT INVALIDATES EVERY INJECTION RUN 16 RECORDED: **the watcher injected into a corpse.** HK died at +1372.4 s; the watcher's four injections are timestamped +1386.0, +1392.5, +1398.8 and +1439.0 — all AFTER death. Every one logged `{"action":"no_window","note":"no on-screen wine window found"}` and fell back to `--pid`, and every capture failed `ScreenCaptureKit … "window id is not shareable"`. `CGEventPostToPid` still reports `verdict: POSTED` against a dead pid, which is exactly why the injector's own success line proves nothing. My own 5-`return` burst at 06:26 has the same defect and the same status: posted after death, worthless. Root cause: the `menu_ready` gate is a LOG-MARKER poll with no liveness check, and `Restored language` is discovered 10–60 s after it is written. The supervisor then logged `SUCCESS on attempt 1 … leaving the run ALIVE for follow-up` about a process that had exited with rc=134, and stopped the remaining 3 attempts.
06:35 · ITER-5 · AND THE GATE ITSELF WAS THE WRONG MARKER. `Restored language` is language init, not the menu: in run 16 `Loaded Objects now`=0, i.e. HK died BEFORE the menu scene finished loading. Gating on it guarantees injecting into a game that is not at a menu even when the timing works. The correct gate is `Loaded Objects now` AND a live pid.

06:52 · ITER-5 · ★★★★★ THE MENU WALL IS AN ntdll-BUILD REGRESSION, AND THE CORRELATION IS PERFECT OVER ALL 34 RUNS WITH ZERO EXCEPTIONS. `MACRUNNER_HB_TRANSLATION_CACHE_ROOT` embeds the deployed `ntdll.so`'s SHA256 prefix (verified: dist `ntdll.so` = `bd7b00f9e0b35dc3b8e6`, cache root `ntdll-bd7b00f9e0b35dc3`), so every run dir records which ntdll it ran on. Grouping all 34 HK runs by that hash:
  • `5385710288` 22/22 reached `Loaded Objects now`, 0 deaths at rva 0x4ea08f
  • `b9be322674` 3/3, `4808c18ee8` 2/2, `dc12e96d8e` 1/1, `7b483b61bf` 1/1, `43daa23f6e` 1/1 — all reached the scene load, 0 deaths
  • **`99e1e6348e` 0/1 and `bd7b00f9e0` 0/3 — NEVER reached the scene load, and died at rva 0x4ea08f every single time**
30 runs across 6 ntdll builds reach the menu scene and never hit 0x4ea08f; 4 runs across 2 ntdll builds never reach it and always hit 0x4ea08f. The split is on the ntdll binary, not on the launcher, not on the graphics config, and not on wall-clock speed.
06:52 · ITER-5 · WHICH RETIRES MY OWN 06:35 SELF-INIT HYPOTHESIS — I AM WITHDRAWING IT. `laneA-INPUT-MUIFIX-try18` (yesterday 17:28, ntdll `99e1e6348e`) shows LO=0 and the identical 0x4ea08f death, and it predates the unix-side self-init (03:52 today) entirely. The 3/3 "regression after selfinit" I flagged was the ntdll hash travelling with those runs, not the driver change. Self-init is not implicated.
06:52 · ITER-5 · THE BISECT WINDOW, NARROWED TO ONE FILE AND ONE TIMESTAMP. The last run on a good ntdll is `laneA-HK-E2E-05` at **00:29 today** (`4808c18ee8`, LO=1). The first on the bad one is `laneA-HK-E2E-12` at **04:00** (`bd7b00f9e0`). Of the three modified ntdll sources, `loader.c` was last touched 07-19 and `signal_arm64.c` 07-27; **`unix/macrunner_hb.c` was modified 07-29 03:52** — inside the window — and `git diff --stat` shows it carrying **1105 insertions** of uncommitted work belonging to another lane. No known-good `ntdll.so` exists anywhere on disk to A/B against: `engine/wine/build-arm64ec-spike/dlls/ntdll/ntdll.so` is the same `bd7b00f9e0`, and every archived copy is from June/early July.
06:52 · ITER-5 · PREDICTION REGISTERED BEFORE THE FACT, so the correlation gets tested rather than admired: run 17 (`laneA-HK-E2E-17-LIVEGATE-t1-a1-try1-062402`) is on ntdll `bd7b00f9e0`, therefore it should reach `Restored language`, NOT reach `Loaded Objects now`, and die at `mono-2.0-bdwgc.dll rva=0x4ea08f` with `Abort trap: 6`. If it does, the failing class is 5/5.
06:52 · ITER-5 · HARNESS FIXES LANDED THIS ITERATION (in-territory, both syntax-checked). `tools/hk_e2e_key_watch.py`: new `alive(pid)` guard (`os.kill(pid,0)`) at the gate, before the first injection and before every individual key, with verdicts `PROCESS_DIED_BEFORE_MENU` / `PROCESS_DIED_BEFORE_INJECTION` and `target_alive_at_end` recorded so an all-zero result can no longer be confused with "the guest ignored the keys"; menu gate `Restored language` → `Loaded Objects now` (with the old marker kept as an explicitly recorded fallback in a new `menu_gate` field); poll 20s→3s; **pre-injection settle 45s→0** (that settle alone consumed the entire measured 43.2s live window); per-press settle 40s→12s and 3→6 presses; `s` pre-bound so an immediate liveness break cannot raise `NameError`. `scripts/hk-e2e-supervise.sh`: success now requires `menu_ready` AND `target_alive_at_end` AND ≥1 delivered key event — the old gate scored attempt 1 of run 16 SUCCESS for a process that had already exited rc=134 and skipped the remaining 3 attempts.

07:00 · ITER-5 · ★★★ THE ntdll CHANGE HAS A NAME AND A DOCUMENTED KILL SWITCH, SO THIS IS TESTABLE FROM MY OWN TERRITORY AFTER ALL. Reading (not editing) the uncommitted diff in `engine/wine/dlls/ntdll/unix/macrunner_hb.c`: the work that landed 07-29 03:52 is an **SMC (self-modifying-code) guard for the IR cache** — `macrunner_hb_ir_smc_enabled()`, `macrunner_hb_ir_smc_hash()`, `macrunner_hb_ir_func_guest_span()`, new `smc_span_start` / `smc_span_len` / `smc_hash` fields on `macrunner_hb_ir_cache_entry`, and counters `smc_tracked` / `smc_checked` / `smc_stale` / `smc_retire_overflow`. Its own comment: *"This cache is address-keyed and, until now, patching a callsite/trampoline left the lifted IR stale for the rest of the run_x64 dispatch even though the block cache correctly evicted its translation"* — see `HK-SMC-STALE-IR-CACHE-SECOND-CACHE-GAP-20260728.md`. **Kill switch `MACRUNNER_HB_SMC_RELIFT=0`**, and the comment states both halves are required (the other half is `engine/hyperbridge/src/hb_runtime.c:1006`, `runtime_env_flag_cached(&cached, "MACRUNNER_HB_SMC_RELIFT", 1)`).
07:00 · ITER-5 · WHY THIS IS THE PRIME SUSPECT, AND WHERE THE ARGUMENT IS WEAK — BOTH STATED. Strong: the failure is a block **entered at an `int3`/padding byte** (`gbytes=cc eb 92 48`), which is precisely the shape of a stale-or-mis-boundaried re-lift; Mono patches its own trampolines constantly, so an IR-cache invalidation policy meets HK's hottest code; and the ntdll half landed 03:52, inside the 00:29→04:00 bisect window. Weak, and I am not hiding it: BOTH halves default to **ON** (`cached = (e && e[0]=='0') ? 0 : 1` and the `,1` default), and **all 119 runs with a `final-child.json` left `MACRUNNER_HB_SMC_RELIFT` unset**, so the older `hb_runtime.c` half was also ON in the 30 good runs. What is new on 03:52 is the *second* (IR-cache) half — before it, the IR cache was simply never invalidated on SMC. So `=0` is not a perfect restoration of the good state; it disables both halves at once. Either outcome is informative, which is why it is worth exactly one run.
07:00 · ITER-5 · REGISTERED PREDICTION #1 IS STILL OPEN, NOT QUIETLY DROPPED. Run 17 (ntdll `bd7b00f9e0`) is at 1931 lines with the log frozen at +279.4 s while the process burns 106 % CPU at 7 m 55 s elapsed — the same stall shape as run 16. `Restored language`=0, `Loaded Objects now`=0, `0x4ea08f`=0 so far. Note its boot was FAST before the stall (`UnloadTime` +279 s, versus +252 s and +273 s in the two good runs and +805 s in run 16), which already kills "the bad ntdll is just slower" as an explanation for the menu wall.
07:00 · ITER-5 · AND THE KEY CHAIN REPRODUCED IN A SECOND, INDEPENDENT PROCESS. Run 17, HK pid 2814: `stage=macdrv_key_event`=2 and `ProcessEvents_drain`=2 (`tid=29954876 queue=0xb04334900 fd=220 calls=1`), driven by the continuous injector rather than by the accidental single press that produced run 16's pair. So the 06:14 result is n=2 processes, not a one-off. Both runs show `calls=1`: HK calls `macdrv_ProcessEvents` exactly ONCE in its whole life, which is the signature of a guest that never reaches an input-polling player loop — consistent with dying before the menu scene, and the reason `macrunner-return-route` stays 0.

07:08 · ITER-5 · ★ THE SMC GUARD IS THE **ONLY** DEFAULT-ON BEHAVIOUR CHANGE IN THE ENTIRE UNCOMMITTED ntdll DIFF, WHICH IS WHY THE A/B IS AIMED THERE AND NOWHERE ELSE. Enumerated every env gate the diff adds and read each one's default polarity: `MACRUNNER_HB_MONO_ALLOC_ORACLE` and `MACRUNNER_HB_GFX_RING_LEDGER` (plus its three `*_RVA` / `*_MAX_SAMPLES` parameters) go through `macrunner_hb_cached_env_flag()` = **default OFF**, so they are inert in every run; `MACRUNNER_HB_WINEMAC_UNIXLIB_BRIDGE` is default-on but measured never to fire (`macrunner-hb-winemac-unixlib-bridge`=0, `PLACEHOLDER KEPT`=1); `MACRUNNER_HB_IR_RETIRE_MAX` is a tuning constant of the SMC retire path itself. That leaves `MACRUNNER_HB_SMC_RELIFT` — `cached = (e && e[0]=='0') ? 0 : 1`, i.e. **default ON** — as the single new always-active code path in the build that fails 4/4.
07:08 · ITER-5 · A/B LAUNCHED AND ITS PROPAGATION VERIFIED IN THE RUN'S OWN CONTRACT, NOT ASSUMED. `laneA-HK-E2E-18-SMCOFF-t1-a1-try1-063459`: `final-child.json` → `MACRUNNER_HB_SMC_RELIFT=0`, `ntdll-bd7b00f9e0` (the SAME failing build), `argv[0]=…/dist-arm64ec-spike/bin/wine`. One variable changed against run 16/17. Decision rule fixed BEFORE the result: reaches `Loaded Objects now` ⇒ the SMC guard is the regression and the operator gets a one-line fix; still dies at `0x4ea08f` ⇒ the cause is elsewhere in the 1105 lines and restoring a good ntdll is an operator call, since that file holds another lane's uncommitted work I must not revert.
07:08 · ITER-5 · RUN 17 STOPPED DELIBERATELY, AND ITS PREDICTION IS RECORDED AS **UNTESTED** RATHER THAN QUIETLY CLAIMED. It was killed mid-stall (scoped by verified PIDs — supervisor first so it could not start attempt 2, then the helpers, then the HK pid read from the run's own `wine-child.pid`; slot confirmed clear afterwards) to free the serial JIT slot for the A/B, which is worth more than a 5th confirmation of a 4/4 correlation. Final counts at kill: lines=1931, `Restored language`=0, `Loaded Objects now`=0, `0x4ea08f`=0.

06:41 · ITER-6 · ★★ TWO HK INSTANCES WERE RUNNING AT ONCE — LAST ITERATION'S "RUN 17 STOPPED" KILLED THE RUN BUT NOT ITS SUPERVISOR. Found live at 06:39: supervisor pid 96177 (`hk-e2e-supervise.sh HK-E2E-17-LIVEGATE 4 2700`) had survived and started **attempt 2** (`laneA-HK-E2E-17-LIVEGATE-t2-a1-try1-063353`, HK pid 12154) 66 seconds BEFORE the A/B run 18 launched (063459). So the registered SMC A/B spent its whole life sharing the machine — and the single JIT title slot — with an unwanted control. Run 17-t2 was wedged at **0.0 % CPU** with its log frozen at +59.7 s on `macrunner-hb-guest-alloc-fail: import=VirtualAlloc status=ffffffffc0000018 base=0x1131c0000` = STATUS_CONFLICTING_ADDRESSES, i.e. the documented arena-base collision that two concurrent HB processes cause. Killed scoped by verified PID, supervisor FIRST this time (96177 → 5822, 8212 watcher, 5861 mr-run → HK 12154), then confirmed all five gone; run 18's tree (8276 sup / 14060 watcher / 12412 mr-run / 18676 HK @105.9 % / 18680 wineserver) verified still alive. Slot is now clean and run 18 is uncontended.
06:41 · ITER-6 · A/B PROPAGATION RE-VERIFIED FROM RUN 18'S OWN CONTRACT, NOT INHERITED ON TRUST: `MACRUNNER_HB_SMC_RELIFT=0`, `MACRUNNER_HB_TRANSLATION_CACHE_ROOT=…/ntdll-bd7b00f9e0b35dc3` (the SAME failing build), `argv.entries[0].value=…/dist-arm64ec-spike/bin/wine`. Also present and worth recording because it was never called out: `MACRUNNER_HB_JIT_DIRECT_MEM=0`.
06:41 · ITER-6 · ★★★ I AM CORRECTING MY OWN LANE'S 06:52 CLAIM OF A "PERFECT CORRELATION, ZERO EXCEPTIONS" — ITS DENOMINATORS CAME FROM A SET SELECTED ON THE OUTCOME. Re-swept **132** run dirs that have BOTH `final-child.json` and `run.log` (the 06:52 sweep used 34 "runs that show any menu marker", i.e. it filtered on the thing it then measured). Unconditional `Loaded Objects now` rates per ntdll build are nothing like 100 %: `5385710288` **22/36**, `4808c18ee8` **2/22**, `b9be322674` 3/3, `99e1e6348e` 0/4, `bd7b00f9e0` **0/8**. Against a 2/22 = 9 % base rate, 0/8 has probability 0.91^8 ≈ **0.47** — completely unremarkable. So stated that way the "regression" is not evidence at all.
06:41 · ITER-6 · BUT THE CORRELATION SURVIVES WHEN CONDITIONED ON A **PRIOR** MILESTONE INSTEAD, AND THAT IS THE DEFENSIBLE FORM. `Restored language` (language init) is emitted BEFORE the menu scene load, so conditioning on it is legitimate where conditioning on a menu marker is not. Among all runs that reached `Restored language`: LO = **30/30** across the 6 good builds, **0/4** across `99e1e6348e` + `bd7b00f9e0`, and all 4 of those deaths are the `rva=0x4ea08f` class. Hypergeometric p = 1/C(34,4) ≈ **2.2e-5**. The bulk of the "failures" I found in the raw sweep are the documented exit=53 boot flake (clusters of 1449-line runs 30 s apart) and deliberately short probes, which is exactly why the unconditional rate is misleading in the other direction. Net: the build-linked wall is real, my lane's stated evidence for it was not.
06:41 · ITER-6 · AND THE HASH IS AN EPOCH PROXY, NOT AN ntdll ATTRIBUTION — A DISTINCTION THIS LANE HAD COLLAPSED. `MACRUNNER_HB_TRANSLATION_CACHE_ROOT` embeds the SHA of `ntdll.so`, but a rebuild redeploys the whole dist, so the hash identifies the BUILD EPOCH of every binary, not ntdll alone. The timeline (local): `99e1e6348e` 17:28 Jul-28 BAD → `4808c18ee8` 18:35 Jul-28 GOOD → `5385710288` … → `7d152f6fc7` 03:35 → `84fbb707eb` 03:38 → `bd7b00f9e0` 04:00 BAD. A bad epoch was un-broken by the very next build one hour later, which is a **non-monotonic** pattern and therefore not a simple forward regression in one file. [HYPOTHESIS, untested: the 17:28 bad epoch is the `666f6614` kernelbase EC-mirror-disable era and `4808c18ee8` is post-revert `08b006dc`; that would put the suspect outside ntdll entirely.] Recorded so the SMC A/B is not over-read whichever way it lands.
06:52 · ITER-6 · NO ENV VARIABLE EXPLAINS THE MENU SPLIT — THE ENV-ONLY ESCAPE IS DEAD. Pulled every `final-child.json` env var for the 34 recent-era runs past `Restored language`, normalised the per-run-unique paths (rundir, `_mr-run.X` prefix, flight/DXMT/log paths) and asked which variables have NO shared value between LO=Y (30) and LO=N (4). Answer: only `MACRUNNER_HB_TRANSLATION_CACHE_ROOT` (which IS the ntdll hash) and `LANEA_RUN_START_EPOCH` (which is the clock). A first pass that skipped normalisation reported 10 "perfect separators" — all of them per-run unique paths, i.e. artifacts of every run having its own directory; recorded so nobody re-derives that false positive.
06:52 · ITER-6 · ORDERING VERIFIED RATHER THAN ASSUMED, BECAUSE MY WHOLE CONDITIONAL RESTS ON IT: `Restored language` → `Loaded Objects now` = **+54.0 s** (`laneA-HK-E2E-05`: +370.4→+424.4) and **+54.9 s** (`laneA-JITHINT-try20`: +386.8→+441.7). RL genuinely precedes LO, so conditioning on it is not conditioning on the outcome. Both bad epochs die 43–69 s after RL, i.e. INSIDE that ~54 s scene-load window.
06:52 · ITER-6 · NO KNOWN-GOOD ntdll.so EXISTS — CONFIRMED INDEPENDENTLY BY HASHING EVERY COPY ON DISK across both worktrees. None of `5385710288 / 4808c18ee8 / b9be322674 / dc12e96d8e / 7b483b61bf / 43daa23f6e` is present anywhere; newest non-current copy is `4c2c34a7` (07-24). Restoring a good epoch by file copy is impossible, so the A/B levers are the only in-territory route.
06:52 · ITER-6 · THE FIRST BAD EPOCH HAS A DIFFERENT, ALREADY-FIXED CAUSE — SO IT SHOULD NOT BE POOLED WITH THE CURRENT ONE. `git log` local times: `99e1e6348e` last bad run 18:27 Jul-28 → **e4b90849 18:31 "kernelbase: list_init the reg_mui_cache ARM64X twin — unblocks all guest input"** → `4808c18ee8` first good run 18:35. The fix lands exactly in the gap. `99e1e6348e`'s RL-conditioned sample is n=1 anyway. Dropping it, the live evidence is `bd7b00f9e0` **0/3** vs good **30/30** (p = 1/C(33,3) ≈ 1.8e-4) and the only question is what re-broke it between 18:35 Jul-28 and 04:00 Jul-29.
06:52 · ITER-6 · ★★ THERE IS A **THIRD** DEFAULT-ON SMC GATE AND ITER-5 DID NOT ENUMERATE IT — WHICH MEANS RUN 18 IS TESTING ONLY PART OF THE MECHANISM. Reading `hb_runtime.c` (read-only; hyperbridge is forbidden to edit): `MACRUNNER_HB_SMC_RELIFT` @1006 default ON (what run 18 disables, both halves), **`MACRUNNER_HB_SMC_REVERIFY` @921 default ON — hash at translate time, re-verify on EVERY cache hit, evict on mismatch — never tested in any run**, and `MACRUNNER_HB_TRACE_SMC_REVERIFY` @926 default OFF (inert). The death line carries **`steps=0`**, and `smc_relift_should_exit(uint64_t steps)` @1034 accumulates a streak on exactly zero-progress exits, capped by `SMC_RELIFT_MAX_CONSECUTIVE 8`, after which it stops exiting and falls through to the "degraded (possibly stale)" in-place retranslate. `gbytes=cc eb 92 48` begins with `cc` = int3. [HYPOTHESIS, untested: Mono patches a trampoline → REVERIFY evicts → the re-lift reads bytes mid-patch → `UNSUPPORTED_OPCODE steps=0` → the zero-progress streak drives the fallback into the abort.] `MACRUNNER_HB_SMC_REVERIFY=0` is therefore the queued outcome-B lever and it is **env-only: no build, no forbidden edit**.
06:52 · ITER-6 · AND THE OTHER PRIME SUSPECT HAS NO KILL SWITCH, WHICH BOUNDS WHAT I CAN DO ALONE. `d9dad4bc 18:38 Jul-28 "hyperbridge: place JIT arenas above the guest address space"` is **32 unconditional lines in `hb_jit.c` with zero env gates** — it landed after the last good build and was first baked into a dist at 03:35 today. It has no A/B short of a rebuild, and `engine/hyperbridge/**` additionally carries another lane's uncommitted work (`hb_runtime.c`, `hb_arm64_codegen.c`, `hb_interpreter.c`, `hb_runtime.h`) that any rebuild of mine would silently link — note `build-wine-arm64ec-spike.sh` does not build hyperbridge, so the .a is whatever was last `make`d. If both SMC A/Bs fail, this is an operator call, not mine.

07:00 · ITER-6 · ★★★★★ THE MENU WALL IS **MONO'S GC STOP-THE-WORLD `SuspendThread` LOOP TIMING OUT** — AND THE `cc`/UNSUPPORTED_OPCODE THIS LANE HAS CHASED FOR TWO ITERATIONS IS NOT A MISTRANSLATION AT ALL. Read `mono-2.0-bdwgc.dll` off disk, parsed its PE (imageBase 0x180000000, .text rva 0x1000 raw 0x400) and mapped rva 0x4ea08f → file offset 0x4e948f. The bytes there are `cc eb 92 48 8b 84 24 98 00 00 00 48` — **byte-identical to the `gbytes=` the runtime printed**, so guest memory was never corrupted and HB read exactly the right bytes at exactly the right address. Decoding the enclosing block:
  `0x4ea025 mov rcx,[rdi+0x10]` (thread handle) → `0x4ea029 call [rip+0x8b4e2]` = **SuspendThread** → `cmp eax,-1` → `0x4ea034 lea rdx,[rsp+0x20]; mov dword[rsp+0x50],0x100003` (CONTEXT + ContextFlags = CONTEXT_AMD64|CONTROL|INTEGER) → `0x4ea045 call [rip+0x8b506]` = **GetThreadContext** → `test eax,eax; jnz` = **the only loop exit** → `0x4ea053 call [rip+0x8b238]` = **ResumeThread** → `0x4ea073 call [rip+0x8b270]` = Sleep/yield → `inc ebx; cmp ebx,0xf4240` = **retry up to 1,000,000** → `jl 0x4ea024` → on exhaustion `0x4ea082 lea rcx,[rip+0x8d7bf]` → **the string at rva 0x577848 is literally `"SuspendThread loop failed"`** → `0x4ea089 call [rip+0x24ffc9]` (noreturn g_error, IAT slot rva 0x73a058) → `0x4ea08f cc`.
  So **`0x4ea08f` is simply the RETURN ADDRESS of a noreturn call, and MSVC put an `int3` there as padding.** HB is not failing to translate `cc`; it is arriving at `cc` because the guest called a function that never returns. The real event is one rung up: **Mono's stop-the-world could not suspend-and-read-context of some thread within 1,000,000 retries.**
07:00 · ITER-6 · WHICH RETROACTIVELY EXPLAINS THE "STALL" THIS LANE HAS LOGGED FIVE TIMES AND NEVER IDENTIFIED. A 1,000,000-iteration `SuspendThread/GetThreadContext/ResumeThread/yield` spin is exactly a process pegged near 100 % CPU emitting ZERO log lines. Run 18 is doing it **right now**: log frozen at +307.688 s since 06:40:07 while pid 18676 holds 106–107 % CPU for 22 minutes. Runs 14 and 16 showed the identical signature before dying at 0x4ea08f. Stall and death are therefore ONE phenomenon (spin → exhaust → abort), not two, and "the bad epoch is just slower" was the wrong frame — boot is in fact NORMAL speed in the bad epoch (`init` med 53 s vs 54 s good, `swap` 145 s vs 157 s, `UnloadTime` **280 s vs 280 s**); only the post-UnloadTime segment diverges (RL med 1328 s vs 407 s), because that is where the GC first has to stop the world.
07:00 · ITER-6 · AND THE PATH IS HB'S OWN, WITH AN ENV-ONLY INSTRUMENT ALREADY BUILT INTO IT. `macrunner_hb.c:23545` intercepts `SuspendThread`/`ResumeThread` → `NtSuspendThread`/`NtResumeThread`, and `:23579` intercepts `GetThreadContext`/`SetThreadContext`/`NtGetContextThread`/`NtSetContextThread` → `NtGetContextThread`/`NtSetContextThread`; both already `fprintf` a `macrunner-hb-wait-semantic:` line carrying `handle=`, `status=`, `previous=`, `ret=`, `last_error=` and (for the context call) `requested_flags=`. Gate = **`MACRUNNER_HB_TRACE_WAIT_SEMANTIC=1`** with **`MACRUNNER_HB_TRACE_WAIT_SEMANTIC_BUDGET=<n>`** (default 400, `<=0` = unlimited), `macrunner_hb.c:1658/1677`. So the next run can NAME the failing NT status and the failing thread handle **without a build and without touching forbidden territory**. Caveat recorded up front: with the gate on, `budget_allows()` pays an unmemoized `getenv` per call (its own comment measures ~13 % of a spinning thread), so the instrumented run will be slower and the budget must be finite.
07:00 · ITER-6 · WHAT THIS DOES **NOT** YET SAY, STATED BEFORE ANYONE READS IT AS SOLVED. It does not say WHY the suspend/context pair fails, nor on WHICH thread, nor whether that thread is new. The leading candidate is that the epoch-correlated change put a thread in HK's process that cannot be suspended or whose amd64 CONTEXT cannot be synthesised — and the obvious suspect is the Cocoa/NSApp thread the unix-side self-init now stands up, since `selfinit=4` in exactly the three `bd7b00f9e0` runs and 0 in every good run. **[HYPOTHESIS — and it is CONFOUNDED, because selfinit and the build epoch travel together in every run on disk, so only an A/B can separate them; `MACRUNNER_MACDRV_UNIX_SELFINIT=0` is the lever and it is in my territory.]** Note the counter-evidence I am not burying: `laneA-INPUT-MUIFIX-try18` died the same way with `selfinit=0`, though it is n=1 and on the separately-explained `99e1e6348e` epoch.
07:00 · ITER-6 · NEGATIVE RESULTS THAT CLOSE OFF ROUTES, RECORDED SO THEY ARE NOT RE-RUN. (a) **No force-interpreter / tolerate-unsupported-opcode env lever exists** — `force_interp` appears nowhere in `hb_runtime.c`, and the only `*FORCE*` gates are `FORCE_ISFOCUSED`, `FORCE_LAZY_LOAD/STORE` and three `TEST_FORCE_*` fault injectors. ITER-5's claim holds. (b) The four `laneA-smc-reverify-*` runs set only `TRACE_SMC_REVERIFY=1`, i.e. they ran with reverify **ON**, and all reached `Loaded Objects now`; `SMC_REVERIFY=0` was tried once (`laneA-smc-off-bisect-15`) and also reached it. So **SMC reverify is exonerated as a sole cause** — both polarities reach the menu on good builds, which also demotes my own 06:52 outcome-B lever.
07:05 · ITER-6 · HARNESS: KILLED RUN 18'S SUPERVISOR **LOOP ONLY**, PRE-EMPTIVELY, SO THE MORNING'S CONTENTION BUG CANNOT REPEAT. `hk-e2e-supervise.sh HK-E2E-18-SMCOFF 3 2700` had 2 attempts left and would have launched attempt 2 the instant attempt 1 ended — which is exactly how run 17's supervisor produced the second HK I found at 06:39. Killed pid 8276 alone (not its tree): confirmed gone, while the launcher 8297, mr-run 12412, watcher 14060 and HK 18676 all survive reparented to init, so attempt 1 still completes with its watcher able to inject if a menu appears. Note the launcher's own `wait_for_slot` (`hk-run-try12-config.sh:57`) polls `ps -Ao comm | grep -cE 'wineserver'` for up to 2400 s, so it guards a LATER start but cannot guard two launchers that start within the same minute — which is precisely the 06:33:53/06:34:59 race. The durable rule is one supervisor at a time, verified by PID.
07:05 · ITER-6 · TRACE BUDGET SEMANTICS READ BEFORE USING THEM (`macrunner_hb.c:1665`): default **400**, `limit <= 0` = UNLIMITED, and it is a simple PREFIX counter (`count < limit`) shared across all call sites — including the `NtSetContextThread` guest-context-restore hot path, so boot can eat it before the GC loop ever runs. It prints `macrunner-hb-wait-semantic: trace budget exhausted at N entries, silencing`, so truncation is DETECTABLE rather than silent. Next run will use `MACRUNNER_HB_TRACE_WAIT_SEMANTIC=1` + `MACRUNNER_HB_TRACE_WAIT_SEMANTIC_BUDGET=300000` and I will check for that exhaustion line before believing any absence of suspend traffic.
07:08 · ITER-6 · ★ THE FUNCTION IS IDENTIFIED BY NAME, NOT INFERRED: IT IS **BOEHM GC's `GC_suspend()` INSIDE `GC_stop_world()`** (`win32_threads.c`). The three strings adjacent to the one the failing `lea` loads are, in address order, `0x577830 "ResumeThread failed"`, `0x577848 "SuspendThread loop failed"`, `0x577868 "GC_stop_world() called before GC_thr_init()"`, `0x5778a0 "GC Warning: Thread stack pointer %p out of range, pushing everything"`. That is bdwgc's own abort-message cluster, and the decoded block matches its published shape one-for-one: suspend → set `ContextFlags` → `GetThreadContext` → **break ONLY on success** → else `ResumeThread` (abort `"ResumeThread failed"` if that fails) → retry, and `if (i > 1000000) ABORT("SuspendThread loop failed")`. HK is a Mono-BDWGC build (`mono-2.0-bdwgc.dll`), so this is the garbage collector stopping the world — which is why it first bites during scene load, the first heavy-allocation phase.
07:08 · ITER-6 · AND THAT NARROWS THE FAILURE TO EXACTLY TWO STEADY STATES, DISTINGUISHABLE BY THE TRACE I HAVE NOW FOUND. The abort we hit is the loop-exhaustion one at `0x4ea08f`, NOT the `"ResumeThread failed"` abort at `0x4ea06b` — so whichever path is being taken 1,000,000 times never hits a failing ResumeThread. Either **(A)** `SuspendThread` returns `(DWORD)-1` every time (`cmp eax,-1; je` skips both GetThreadContext and ResumeThread — the shape of suspending a thread that no longer exists), or **(B)** `SuspendThread` succeeds, `GetThreadContext` returns 0 every time, and `ResumeThread` succeeds — the shape of a live thread whose amd64 CONTEXT cannot be produced. `macrunner-hb-wait-semantic:` prints `status=`/`previous=`/`ret=` for the suspend/resume pair and `requested_flags=` for the context call, so ONE instrumented run separates A from B and names the handle. Registering both readings now so neither can be claimed after the fact.
07:12 · ITER-6 · WHY NOBODY SAW THIS FOR TWO ITERATIONS: **THE ABORT MESSAGE NEVER REACHES OUR LOG.** Swept all **371** `run.log` files under `reports/`: `"SuspendThread loop failed"` = 0, `"ResumeThread failed"` = 0, `"GC_stop_world"` = 0, `"GC Warning"` = 0, `"GC_thr_init"` = 0. bdwgc's abort text is simply never captured, so the only surface this failure ever presented was the `int3` return address `0x4ea08f` — which reads like an opcode bug and was chased as one. The diagnosis was reachable only by decoding the shipped DLL. Also measured in the same sweep: `wait-semantic` = 0 across all 371, i.e. `MACRUNNER_HB_TRACE_WAIT_SEMANTIC` has never been enabled in this project's history, so the instrument is genuinely fresh rather than something already tried.
07:12 · ITER-6 · THE ONE ALTERNATIVE READING I AM KEEPING OPEN, BECAUSE THE EVIDENCE DOES NOT YET EXCLUDE IT. HB reports `block_pc = next_pc = 0x4ea08f`, i.e. control ARRIVED at the int3 — which under reading (A) means the noreturn `g_error` RETURNED rather than aborting. Reading **(B)** is that no loop ran at all and an indirect branch simply landed one byte low, on `0x4ea08f` instead of `0x4ea090` (which holds `eb 92`, the loop's back-edge). (A) explains the 25-minute 107 %-CPU silent stall that runs 14, 16, 17 and 18 all show; (B) explains nothing about the stall. So (A) is much better supported, but the trace settles it outright: a long burst of `SuspendThread`/`GetThreadContext` pairs before the death confirms (A), their absence confirms (B). Recorded before the run, not after.
07:16 · ITER-6 · ★★ FIXED A HARNESS DEFECT THAT WOULD HAVE PRODUCED A **FALSE `LOOP-STATUS: GOAL`** — FOUND BY AUDIT, NOT BY IT FIRING. `hk_e2e_key_watch.py:239` had a fallback `elif Restored language >= 1 and CreateSwapChainForHwnd >= 1: menu_ready = True` sharing the same `if menu_ready: break`, so the FIRST poll after RL appeared closed the gate and stopped waiting. But I measured today that RL precedes `Loaded Objects now` by **+54.0 s** (E2E-05) and **+54.9 s** (JITHINT-try20) — RL is language init, the menu scene is not loaded yet — and the failing epoch dies INSIDE that gap. Combined with `macdrv_key_event` now being reliably ≥2 (proven in runs 16, 17 and 18) and the pid still alive at that instant, the supervisor's success test `menu_ready AND target_alive_at_end AND delivered>0` would have passed **all three** for a run that never reached a menu — i.e. the lane would have declared the objective met on a game sitting in Mono init. ITER-5 introduced this while fixing the opposite bug and labelled it "an explicitly recorded fallback"; it was not merely recorded, it gated. Now RL sets only `evidence["restored_language_seen"]` and logs `recorded, NOT gating`; `menu_ready` is reachable ONLY via `Loaded Objects now`. `python3 -m py_compile` clean. This is the third time this lane has scored a menu-less run as good (run 16's corpse, ITER-5's gate marker, this) — the standing rule is that the success gate must be the scene-load marker AND a live pid, never a boot marker.
07:16 · ITER-6 · RUN 18 (SMC A/B) AT 29:40 ELAPSED: still frozen at +307.688 s, 106 % CPU, `Restored language`=0, `Loaded Objects now`=0, `0x4ea08f`=0 — i.e. the SMC relift kill switch did NOT prevent the silent 100 %-CPU spin, which under the GC_suspend reading is the 1,000,000-iteration loop itself. Its supervisor loop is already killed so no attempt 2 can start; the 2700 s mr-run timeout lands ~07:20 and the waiter will report the final marker counts.
07:20 · ITER-7 · THE DEPLOYED ntdll WAS THE CONFIRMED BAD BUILD, VERIFIED BY SHA BEFORE ANYTHING ELSE. `dist-arm64ec-spike/lib/wine/aarch64-unix/ntdll.so` = `bd7b00f9e0b35dc3` — the exact hash the 34/34 bisect scores **0/4 reaching `Loaded Objects now`**. So every run this lane has made since 04:00, including 14/16/17/18, has been on a binary that is *known* never to reach the menu scene. That reframes ITER-4..6 entirely: they were diagnosing the death of a build already predicted to die, and no amount of env A/B on that binary can produce a menu.
07:20 · ITER-7 · NO KNOWN-GOOD ntdll EXISTS ON DISK — MEASURED, NOT ASSUMED, ACROSS ALL 56 COPIES. Hashed every `ntdll.so` in the repo (56 files): **zero** match any of the six good hashes (`5385710288`, `b9be322674`, `4808c18ee8`, `dc12e96d8e`, `7b483b61bf`, `43daa23f6e`). Newest archived copies are 07-21..07-24. A rebuild was therefore mandatory, not optional.
07:20 · ITER-7 · AND THE BUILD TREE WAS NOT WHERE ITER-6 LOOKED. The shipping tree is **`engine/wine/build-arm64ec-spike/`** (out-of-tree, nested inside `engine/wine`), whose `dlls/ntdll/ntdll.so` is `bd7b00f9e0` @07-29 03:56. The in-tree `engine/wine/dlls/ntdll/ntdll.so` is `4c2c34a7de` @**07-24** — a five-day-old decoy that ships nothing. My first probe for a build dir returned "none" only because zsh aborts an entire `ls` when the FIRST glob of several fails to match; the durable rule is one glob per `ls`, or `2>/dev/null` per pattern.
07:20 · ITER-7 · ★★ LIVE SAMPLE OF THE SPINNING PROCESS REFUTES ITER-6'S CENTRAL INFERENCE. Rather than pay for another run, I sampled run 18 *while* it held 107 % CPU (`sample 18676 6 10`, 536 samples/thread, ~60 threads). Frames found: `macrunner_hb_run_x64` 58, `macrunner_hb_call_import_thunk` 55, `macrunner_hb_try_wait_address_semantic` 28, `hb_jit_helper_exec_interp_ir` 12. Frames **NOT** found, count **0** each: `NtSuspendThread`, `NtGetContextThread`, `macrunner_hb_get_x64_thread_context`, `GC_stop_world`. The single hot thread (tid 29992408) is in HB's **interpreter** (`hb_jit_helper_exec_interp_ir`, `exec_instr_unlocked`, `hb_lazy_flags_materialize`, `_platform_memmove`). And `ps -M 18676` gives **STIME 29:54 vs UTIME 4:20 — 87 % of the process's CPU is in the KERNEL**, the signature of mprotect/mmap thrash (the known `sync_virtual_region`/`hb_memory_protect`-per-HeapAlloc path), not a user-space retry loop. ITER-6's "the silent 100 %-CPU stall IS the 1,000,000-iteration SuspendThread loop" is therefore **not supported** for run 18.
07:20 · ITER-7 · THE LIMIT OF THAT REFUTATION, STATED SO IT IS NOT OVER-READ. Run 18 is the off-nominal `MACRUNNER_HB_SMC_RELIFT=0` arm and it never reached `Restored language` at all, so its stall is at an earlier point than the 0x4ea08f deaths of runs 14/16/17. The sample refutes the GC-loop reading **for run 18**; it does not by itself refute it for the runs that died at 0x4ea08f after `Restored language`. [HYPOTHESIS: one sample, one run.] What it does establish is that "silent + ~100 % CPU" is NOT a unique fingerprint of the GC loop — this lane has been treating it as one across five iterations.
07:20 · ITER-7 · RUN 18 VERDICT (the SMC A/B this lane launched at 06:34): **`MACRUNNER_HB_SMC_RELIFT=0` does not help and is worse.** Final counts at 41:40 elapsed, log frozen at +307.688 s since 06:40:07: `Restored language`=**0**, `Loaded Objects now`=**0**, `0x4ea08f`=**0**, `macrunner-hb-run-exit`=**0**. Killed by verified PID (18676 + its wineserver 18680 + two services.exe of the same `winetemp-47583261` prefix); no pkill, and the sister prefix `winetemp-47461737` was left alone. Slot confirmed clear.
07:24 · ITER-7 · ★★★ BUILT A CLEAN-HEAD ntdll SURGICALLY, WITHOUT WRITING TO ANY SHARED SOURCE FILE. The bisect window from the memory note is `macrunner_hb.c` (modified 07-29 03:52, ~1105 uncommitted insertions from another lane); `signal_arm64.c` (07-27) and `loader.c` (07-19) are byte-identical in the last-good and first-bad builds, so they are excluded and I left them alone. Method: `git show HEAD:…/macrunner_hb.c` into the scratchpad (**38 956** lines vs the worktree's **39 939**), compiled it there with the Makefile's exact flags (+`-I…/dlls/ntdll/unix`, needed only because the source sits outside its own directory) — **it compiles clean against the current headers** — then swapped only the *object* inside the build dir and let `make dlls/ntdll/ntdll.so` do the link with its own recipe. Result: **`f6493bbcf8277be6`**. The build dir was then restored byte-exactly (`macrunner_hb.o` back to `85af24e020309d81`, `ntdll.so` back to `bd7b00f9e0b35dc3`, both re-verified by SHA) and `git status` confirms the other lane's three modified files are still modified and untouched. No forbidden file was edited; the granted `macrunner_hb.c` exception was not used.
07:26 · ITER-7 · RUN 19 LAUNCHED AS THE A/B. Arm A (`bd7b00f9e0`) and arm B (`f6493bbcf8`) are both archived under `reports/phase4-hollow-knight/HK-E2E-19-CLEANNTDLL-evidence/`; arm B is deployed. `winemac.so` is **unchanged** at `b4580efc18dc532c`, so the unix-side self-init and the keyDown bypass are still in play and a menu-reaching run can be injected into immediately. Gate corrected at the launcher too: `MACRUNNER_HK_GOOD_BOOT_MARKER='Loaded Objects now'` — `hk-run-try12-config.sh:160` still defaults to `Restored language`, which ITER-6 proved is language init ~54 s before the scene load. Supervisor pid 31625, 2 attempts, 2700 s each. **Registered prediction before the result:** if arm B reaches `Loaded Objects now`, the regression is inside the uncommitted `macrunner_hb.c` work and the ntdll-build correlation is causal; if arm B dies at 0x4ea08f too, the correlation is confounded and the cause is elsewhere.

08:35 · ITER-8 · ★★★ REGISTERED PREDICTION #2 RESOLVED — **NEGATIVE. THE ntdll ATTRIBUTION IS REFUTED BY THE EXPERIMENT ITER-7 DESIGNED TO TEST IT.** Run 19 arm B (`laneA-HK-E2E-19-CLEANNTDLL-t1-a1-try1-071747`) ran on the clean-HEAD ntdll `f6493bbcf8` — verified in the run's own contract (`ntdll-f6493bbcf8`) and by SHA of the deployed `aarch64-unix/ntdll.so` — and it did **NOT** reach the menu: `Loaded Objects now`=**0**, `Restored language`=**0**, `0x4ea08f`=0, `macrunner-hb-run-exit`=0. Its log froze at **+308.154 s** and stayed frozen for 66 minutes at ~107 % CPU until `Terminated: 15` at +4284.8 s. ITER-7's rule was stated before the fact: *"if arm B dies at 0x4ea08f too, the correlation is confounded"* — arm B did not even get that far, so reverting the other lane's 1105 uncommitted `macrunner_hb.c` lines does not restore the menu. **The uncommitted ntdll work is exonerated as the sole cause, and ITER-5/6's build-epoch bisect is confounded** — self-init (03:52) and the ntdll rebuild (03:56) travel together in every run on disk, and this run separates them for the first time.
08:35 · ITER-8 · ★★★★ AND THE FREEZE HAS A PRECISE, REPEATED ONSET THAT NOBODY HAS NAMED: **HK'S OWN FIRST `macdrv_ProcessEvents` CALL.** Last timestamped guest line, three consecutive runs, each then silent at ~100 % CPU until killed:
  • run 17-t1 `+279.427 s` -> `ProcessEvents_drain pid=2814 tid=29954876 queue=0xb04334900 fd=220 calls=1` (silence to the +576.8 s kill)
  • run 18   `+307.688 s` -> `ProcessEvents_drain pid=18676 tid=29991660 queue=0x77c490b80 fd=220 calls=1` (silence to the +2518.3 s SIGTERM)
  • run 19-t1 `+308.154 s` -> `ProcessEvents_drain pid=37898 tid=30047928 queue=0xa64a08b00 fd=220 calls=1 handled=1` (silence to the +4284.8 s SIGTERM)
`calls=1` on all three is the probe's first-call marker, so in each case HK drained the Cocoa queue exactly ONCE in its whole life and the log ends on that line. This is the same `+280…+310 s` window in which `UnloadTime` prints (run 19: +279.5 s), i.e. immediately after the first scene unload — where Unity's player loop starts polling input and therefore where the real driver's `pProcessEvents` / `pMsgWaitForMultipleObjectsEx` first carry traffic. Under the null driver (every run before self-init) those paths were stubs. **[HYPOTHESIS — the correlation is 3/3 but not yet causal, and run 16 is the honest counter-example: it drained at +1024 s and kept running to +1372 s.]**
08:35 · ITER-8 · WHY THIS IS NOT SIMPLY ITER-6'S "GC SuspendThread LOOP" AND NOT SIMPLY A DEADLOCK EITHER. `macrunner_note_drain()` is called at `event.c:812`, *after* the handler loop, so the drain line printing means `macdrv_ProcessEvents` completed its work — the stall is at or after its return, not inside `macdrv_handle_event`. ITER-7's live sample of run 18 in this exact state found `NtSuspendThread` / `NtGetContextThread` / `GC_stop_world` = **0 frames** and the hot thread in HB's interpreter with **87 % of process CPU in the kernel** — the mprotect/mmap-thrash signature. So "deadlock" and "catastrophic slowdown" are both still live, they need opposite fixes, and neither is settled.
08:35 · ITER-8 · INSTRUMENT ARMED **BEFORE** THE EVENT, ON A LIVE RUN. Attempt 2 (`laneA-HK-E2E-19-CLEANNTDLL-t2-a1-try1-082927`, HK pid 49456, same arm-B ntdll `f6493bbcf8`) was already past +190 s when this iteration started, i.e. ~2 minutes short of the freeze window. Wrote `tools/hk_stall_sample.sh` (syntax-checked, liveness by PID only, never by name) and launched it against that run: it snapshots `sample` + `ps -M` the moment HK's own `ProcessEvents_drain` appears, then again after every 90 s of log silence, up to 3 stall snapshots. Output under `reports/phase4-hollow-knight/HK-E2E-19-STALL-SAMPLE/`. This gets the at-drain and in-stall thread stacks from the SAME process, which is what separates the two readings above.

08:50 · ITER-8 · ★★★★★ **ROOT CAUSE OF THE MENU WALL, FOUND IN THE ACT AND NAMED TO THE INSTRUCTION: THE COCOA MAIN THREAD DIES IN A HYPERBRIDGE FAULT HANDLER WHILE DELIVERING A *RETURN* KEY — AND IT IS THIS LANE'S OWN TRACE HOOK THAT KILLS IT.** `tools/hk_stall_sample.sh` caught live run 19-t2 (HK pid 49456) at the stall. The process main thread — the one `[NSApp run]` lives on — has this stack, **identical PC `0x1148c5d68` in three samples 15 minutes apart, 894/895 samples each, ~107 % CPU throughout**:
  `main → __wine_main → CFRunLoopRun → __CFRunLoopDoSource0 → run_cocoa_app → -[NSApplication run] → -[NSApplication _handleEvent:] → -[WineApplication sendEvent:] → -[WineApplicationController handleEvent:] → -[WineWindow postKey:pressed:modifiers:event:] → macdrv_return_route_observe + 492 → _sigtramp → macrunner_hb_primary_signal_handler → macrunner_hb_route_x64_callback_fault → macrunner_hb_redirect_arm64x_hexpthk_sigill → macrunner_hb_pc_is_x64_guest_code_module_no_lock → macrunner_hb_pc_in_graphics_arm64x_x64_range → macrunner_hb_ldr_entry_from_pc`
Disassembling the deployed `winemac.so` at that offset: `+488 bl <plt>` (the only undefined `_NtCurrentTeb` import) followed by `+492 ldr w8, [x0, #0x48]` — the ARM64 TEB's `ClientId.UniqueThread`. That is `GetCurrentThreadId()` at **`event.c:217`**, sitting in the `fprintf` argument list **outside** the `pthread_main_np()` guard that the very same function already carries for this exact hazard (its comment names the 2026-07-20 `_MetalLayer_setProps dispatch_sync` boot deadlock). The Cocoa main thread is not a Wine thread — `main → __wine_main → CFRunLoopRun` parks the process's original thread in the run loop while Wine's threads are created separately — so it has no TEB and that load faults; HB's handler then never returns.
08:50 · ITER-8 · **THE DISCRIMINATOR THAT MAKES THIS 6/6 WITH ZERO EXCEPTIONS, AND IT WAS SITTING IN THE LOGS ALL DAY: keycode 36 vs keycode 125.** `macdrv_return_route_observe` early-returns unless `keycode == 36 || vkey == VK_RETURN`, so arrow keys never reach the faulting line and Return always does. Counting `wine_window_keyDown` against its matching `postKey_posted` per keycode, over every run since the trace existed:
  • 15 KEYCHAIN — 125@142.5,142.7,143.5 all posted (6 lines) → **36@143.674 never posted** → log ends +145.4
  • 16 DRAIN — 125@348.4, 125@1239.3 posted → **36@1259.437 never posted** → log ends +1373.4
  • 17-t1 LIVEGATE — 125@138.7 posted → **36@158.891 never posted** → log ends +577.7
  • 18 SMCOFF — 125@165.2 posted → **36@185.359 never posted** → log ends +2519.2
  • 19-t1 CLEANNTDLL — **36@156.213 never posted** → log ends +4284.8
  • 19-t2 CLEANNTDLL — **36@174.116 never posted** → log ends +308.7 (caught live)
**Every Return wedges; every arrow posts down and up cleanly. 6/6, no exceptions.** Because `[NSApp run]` is on that thread, losing it kills the process: every later `dispatch_sync` onto the main queue — how DXMT sets Metal layer properties — blocks forever, which is the `+280…+310 s` freeze ITER-8 opened with. Stall and menu wall are ONE phenomenon.
08:50 · ITER-8 · WHICH ALSO EXPLAINS WHY THIS ONLY STARTED AT 03:52 AND WHY EVERY OTHER SUSPECT WAS INNOCENT. Before the unix-side self-init there was no `[NSApp run]` in HK's process at all, so `postKey:` could never run on the main thread — the wall arrived with self-init, but self-init is not the bug, this one line is. It retires, with evidence rather than assertion: the ntdll/build-epoch bisect (run 19 arm B ran clean-HEAD ntdll `f6493bbcf8` and wedged identically), the SMC guards (run 18 had `MACRUNNER_HB_SMC_RELIFT=0` and wedged identically), and ITER-6's `GC_stop_world` reading (`NtSuspendThread`/`GC_stop_world` = **0 frames** in all three samples). ITER-5's withdrawn self-init hypothesis was directionally right and mechanistically wrong.
08:50 · ITER-8 · FIX LANDED, IN TERRITORY, AND VERIFIED BY DISASSEMBLY RATHER THAN BY EXIT CODE. `event.c`: added `DWORD wine_tid = 0;`, moved `GetCurrentThreadId()` into the existing non-main-thread `else` branch beside the `NtUser*` calls, and the trace now prints `wine_tid=0` on the Cocoa thread instead of faulting. Comment records the measurement so the next reader cannot re-introduce it. Build `make dlls/winemac.drv/winemac.so` rc=0, **0 errors**; `winemac.so` `b4580efc18dc532c` → `278189b4fa293d0a`, deployed atomically (`cp` + `mv`), dist SHA re-read equals build SHA. **Content proof:** the `bl`-then-`ldr wN,[x0,#0x48]` pair is present at +492 in the OLD binary and **absent from the new one**, with a new `bl` appearing at +340 inside the `NtUser*` cluster — i.e. the call moved into the guarded branch, exactly as intended. All prior features still present in the shipped binary (`macdrv_selfinit_entry`, `MACRUNNER_MACDRV_KEYDOWN_NO_KEYWINDOW`, `postKey_posted`, `ProcessEvents_drain`, `macrunner-return-route` = 1/1/1/1/1).
08:50 · ITER-8 · BULK SWEEP OF THE CLASS, BECAUSE ONE INSTANCE IS NEVER THE WHOLE SET. Enumerated every C function defined in `winemac.drv/*.c` that is called from the Cocoa `*.m` files (10 of them) and scanned each body for TEB/win32u access (`GetCurrentThreadId|NtCurrentTeb|NtUser*|NtGdi*|__wine_unix_call`). Exactly **2** hit: `display.c: is_skyrim_se_launcher()` (a title-specific quirk, not on HK's path) and `event.c: macdrv_return_route_observe()` — the one just fixed, and the only one carrying the full `NtUser*` set. The Cocoa/win32u boundary is otherwise clean, so this is a bounded class with one live instance, not the first of many.
08:50 · ITER-8 · RUN 20 LAUNCHED AS THE TEST, SLOT VERIFIED CLEAN FIRST. Killed run 19-t2's tree scoped by verified PID in supervisor-first order (31625 → 43985 watcher → 41866 launcher → 41904 mr-run → 49456 HK → 49460 wineserver → 49487 services), confirmed all gone and zero `wineserver` remaining; no `pkill`. Launched `HK-E2E-20-TEBFIX`, 3 attempts × 2700 s, detached, with `MACRUNNER_HK_GOOD_BOOT_MARKER='Loaded Objects now'`; run dir `laneA-HK-E2E-20-TEBFIX-t1-a1-try1-084701`, HK pid 71804, stall sampler armed on it. **Registered prediction before the result:** a Return key should now produce a matching `postKey_posted`, the main thread should stay in the run loop, and HK should reach `Loaded Objects now` for the first time since 00:29. If it wedges again at the same PC the fix is incomplete; if it reaches the scene load, the menu wall is closed and the objective's remaining half is the injected Return at the menu.
08:50 · ITER-8 · OPEN ITEM, RECORDED NOT HIDDEN: **nothing in the harness explains the early Return.** `hk_e2e_key_watch.py`'s `watch.out` for both run-19 attempts shows only `poll`/`menu wait` lines and zero injections up to the wedge; `hk_cgevent_key.swift:124` exits on `--probe-only` before posting; `laneA-run-hk.sh` and `mr-run.sh` contain no injector. Yet a keycode-36 keyDown lands at +156…+185 s in five separate runs, each within ~10 s of the watcher's `WINDOW FOUND`. [HYPOTHESIS, untested: AppKit drains events queued to the pid once `[NSApp run]` starts at self-init.] It is now harmless and is in fact a free early probe, but it is not yet a key **I** chose to send, so it cannot serve as the objective's evidence.

09:10 · ITER-8 · ★★ RUN 20 IS NOT A TEST OF MY FIX — A SISTER LANE OWNS THE SLOT AND REDEPLOYED ntdll UNDER IT, AND I AM SAYING SO RATHER THAN READING ITS FAILURE AS MINE. Run 20 froze at **+45.946 s** on `stage=dllmain_unixcall_init arch=x86_64`, far earlier than anything this lane has seen, with `dllmain_x64=2` and `winemac-initdll=2` — counters that are **0 in all six preceding runs**. Cause, measured not guessed: `x86_64-windows/ntdll.dll` and `aarch64-windows/ntdll.dll` were rewritten at **08:41:39**, six minutes before run 20 started (08:47:01) and by a lane that is not me — I touched only `aarch64-unix/winemac.so` at 08:45:53. The `hk-dllmain` autoloop (`scripts/lane-autoloop-failover.sh hk-dllmain`) had made winemac.drv's x86_64 DllMain run in HK's process for the first time. Its OWN run `laneA-hk-dllmain-x64entry-i2-a1-try1-085721` then froze at **+45.852 s** on the identical last line — so the early wedge reproduces without my winemac.so being the variable, and run 20 says nothing either way about `event.c`.
09:10 · ITER-8 · SLOT DISCIPLINE: I TOOK THE SLOT WHILE IT WAS OCCUPIED, AND STOOD BACK DOWN. `ps -Ao comm | grep -c wineserver` returned **1** immediately before I launched run 21 and I launched anyway — that count was the sister lane's live run, and the correct reading was "busy". Tracing HK pid 81588's ancestry showed `mr-run.sh ← laneA-run-hk.sh ← hk-run-try12-config.sh hk-dllmain-x64entry-i2 ← claude -p (hk-dllmain lane) ← lane-autoloop-failover.sh`. Killed my own supervisor 81641 and its orphaned launcher 81653 by verified PID, confirmed zero `HK-E2E-21` processes remain, and confirmed pid 81588 still alive and untouched. No `pkill`. Their run keeps the slot; mine waits.
09:10 · ITER-8 · AND I REMOVED MY OWN EXPERIMENT BINARY FROM THE SHARED DIST, WHICH SHOULD NOT HAVE OUTLIVED ITS PREDICTION. ITER-7's surgical clean-HEAD `ntdll.so` `f6493bbcf8` (07:17) was still deployed while the ntdll **PE twins** were rebuilt at 08:41 — a mismatched pair that any lane launching in between inherited silently. `make dlls/ntdll/ntdll.so` from the current worktree reports the tree's real build is `bd7b00f9e0b35dc3` (byte-identical to ITER-7's archived arm A), so I deployed that atomically: dist `f6493bbcf8` → **`bd7b00f9e0`**, with `winemac.so` confirmed still `278189b4fa293d0a` (my fix persists) and the PE twins left exactly as the other lane deployed them. Since ITER-8 refuted the ntdll-epoch theory, running my fix on `bd7b00f9e0` — the build the old theory scored 0/4 — is now the *strongest* available test, not a compromise.
09:10 · ITER-8 · ★ WHAT MY SAMPLE KNOWS THAT THE SISTER LANE'S LOGS DO NOT, HANDED OVER RATHER THAN KEPT. Their `loader.c:618-639` comment (default already flipped OFF, opt-in `MACRUNNER_HB_WINEMAC_X64_DLLMAIN=1`, and it correctly cites my run 20) records only "the process still alive and its log frozen" — it does not say where. My live sample does: `macrunner_hb_call_arm64_pe_import12_for_ctx + 1624 → <guest PE 0x87efb5fba54> → _sigtramp → macrunner_hb_primary_signal_handler + 628 → macrunner_hb_route_x64_callback_fault + 704 → macrunner_hb_pc_in_executable_section + 4`, **stuck 413/413 samples**, while the process main thread is healthy in `CFRunLoopRun → mach_msg2_trap` and no other thread is in a handler. **★★ And that is the SAME endpoint family as my own Return-key hang** (`…primary_signal_handler + 628 → …route_x64_callback_fault + 380 → …ldr_entry_from_pc`): two unrelated faults, two threads, two classification helpers, both spinning a core forever. [HYPOTHESIS, untested and in FORBIDDEN territory so I did not touch it: the PC-classification helpers reached from `macrunner_hb_route_x64_callback_fault` contain an unbounded walk, so any fault whose PC fails to classify becomes a silent CPU-burning hang instead of an error — which is exactly why "the log just stops" has cost these lanes six iterations.] Written up with both stacks, the artifact paths, and two cheap asks (bound the walk + print on overflow) in `reports/phase4-hollow-knight/HK-HB-FAULT-ROUTER-HANGS-TWO-CALLSITES-20260729.md`.
09:10 · ITER-8 · BLOCKED ON THE SLOT, NOT ON A QUESTION — SO THE LOOP CONTINUES RATHER THAN TERMINATING. The deployed `ntdll.dll` predates the sister lane's own default-OFF fix (`MACRUNNER_HB_WINEMAC_X64_DLLMAIN` is absent from both PE twins even as UTF-16, i.e. the shipped build runs that DllMain unconditionally with no env escape), so **every** run on this dist currently dies at +46 s, theirs included. I deliberately did NOT rebuild their `ntdll.dll` to unblock myself: `loader.c` is their lane's in-flight uncommitted work and shipping a mid-edit state is exactly the hazard the guardrails name. Their autoloop will redeploy. Next action when the slot frees AND a fresh `ntdll.dll` lands: relaunch `HK-E2E-21-TEBFIX-CLEAN` on `winemac.so 278189b4` + `ntdll.so bd7b00f9e0`, prediction unchanged — a Return must now produce a matching `postKey_posted` and HK must reach `Loaded Objects now`.

09:16 · ITER-8 · SISTER LANE'S FIX VERIFIED FROM THEIR OWN RUN, AND IT VINDICATES THE STAND-DOWN. They redeployed `ntdll.dll` at 09:02:56 (and again 09:06:52); their `laneA-hk-dllmain-defaultoff-verify-a1-try1-090308` shows `dllmain_attach arch=x86_64`=**0** (default-off holds) and boots normally past the +46 s wall — `Initialize engine version`=1, `create_cocoa_window`=5, and, on **my** `winemac.so 278189b4`, `selfinit`=4 with `user_driver_placeholder_replaced`=**1**. So the unix-side self-init route still delivers the real driver with their change in place, which is the interop question neither lane had answered. Had I rebuilt their `loader.c` to unblock myself I would have shipped their mid-edit state and learned none of this.
09:16 · ITER-8 · AND THEIR RUN INCIDENTALLY NARROWS MY OPEN "MYSTERY RETURN" ITEM. At +161 s their run has `wine_window_keyDown`=**0**, where every run of mine has one at +156…+185 s. The difference is env, not physics: my supervisor exports `MACRUNNER_MACDRV_KEYDOWN_NO_KEYWINDOW=1`, and without that bypass an ambient Return is dropped by AppKit's key-window routing before `postKey:` and leaves no trace. [HYPOTHESIS, still untested: the event is ambient in both processes; only my lane's bypass makes it observable.] So it is not an injector of mine misfiring — which is what I had feared — and it remains unusable as the objective's evidence because I did not choose to send it.
09:16 · ITER-8 · ★ RUN 21 IS LIVE ON A SELF-CONSISTENT TREE — THE FIRST CLEAN TEST OF THE `event.c` FIX. `tools/hk_e2e_wait_and_launch.sh` (written this iteration, syntax-checked, liveness matched on `comm` and on the launcher script path, never on a pattern that can match its own argv) held the slot through the sister lane's run and released it the moment BOTH gates cleared — a `ntdll.dll` newer than the known-bad 08:41 build AND an empty slot — with a 20 s settle + re-check so a foreign autoloop about to claim the slot wins the race instead of colliding. It launched `laneA-HK-E2E-21-TEBFIX-CLEAN-t1-a1-try1-091347` at 09:13:47, HK pid 4746. Deployed at launch: `winemac.so` **278189b4fa293d0a** (my TEB fix), `ntdll.so` **bd7b00f9e0b35dc3** (the tree's real build — the very hash ITER-5's now-refuted theory scored 0/4), `ntdll.dll` the sister lane's default-off build. Stall sampler armed. **Prediction, unchanged and still registered:** a Return delivered to the Cocoa main thread must now produce a matching `postKey_posted`, the main thread must stay in `CFRunLoopRun`, and HK must reach `Loaded Objects now` for the first time since 00:29.
09:16 · ITER-8 · ITERATION STATUS: **CONTINUE.** The menu wall has a named, measured, fixed root cause and the verification run is in flight; the objective's second half (a Return *I* choose to send, at the menu, producing an observable change) is untouched until a menu exists. Not BLOCKED — nothing here needs an operator decision, only the slot, which the waiter now arbitrates.

09:22 · ITER-8 · ★★★★★ **THE FIX IS PROVEN AT RUNTIME, AND THE PROOF IS THE EXACT COUNTER THAT WAS 0/6 BEFORE IT.** `laneA-HK-E2E-21-TEBFIX-CLEAN-t1-a1-try1-091347`, HK pid 4746, on `winemac.so 278189b4` + `ntdll.so bd7b00f9e0` + the sister lane's default-off `ntdll.dll`. Something is injecting alternating Return/DownArrow every ~20.2 s from +147 s, and **every single Return now completes**:
  `wine_window_keyDown` keycodes: `36@147.200 125@167.350 36@187.501 125@207.690 36@227.861 125@248.042 36@268.215 125@288.392 36@308.581 125@328.764 36@348.955 125@369.138 36@389.321 125@409.505 36@429.686`
  `postKey_posted`: **30 lines — a `pressed=1` and a `pressed=0` for every one of those 15 keyDowns, Returns included** (`36/p1@147.200 36/p0@147.270 … 36/p1@429.686 36/p0@429.755`).
**Before the fix: Return keyDowns 6, matching `postKey_posted` 0 — six runs, zero exceptions. After: 8 Returns, 8 completions.** And `macrunner-return-route: stage=macdrv-nsevent seq=16 pid=4746 native_tid=30211686 **wine_tid=0**` shows the guarded branch doing exactly what it was written to do — reporting 0 on the Cocoa thread instead of reading a TEB that isn't there.
09:22 · ITER-8 · AND THE PROCESS IS PAST THE WALL, MEASURED AGAINST THE RUNS THAT DIED THERE. Run 21 is **alive and still logging at +429.8 s / 2042 lines**. The four wedged runs stopped dead at 1914–1936 lines and +279…+308 s, every one of them within seconds of an unmatched Return; run 21 sailed through +308.581 s with a Return **and posted it**, then kept going for another two minutes and counting. The `+280…+310 s` freeze that this lane chased for five iterations as a GC loop, an SMC bug, an ntdll regression and a self-init cost was one un-guarded `GetCurrentThreadId()` in a trace hook.
09:22 · ITER-8 · WHAT IS STILL NOT PROVEN, STATED BEFORE ANYONE READS THIS AS THE OBJECTIVE. `Restored language`=0, `Loaded Objects now`=**0**, `macdrv_key_event`=0 and `macrunner-return-route` (the GUEST-side marker from win32u) =0 at +430 s — so HK has not reached the menu in this run yet and no key has been proven to reach the guest. The menu costs 10–26 min historically; the run is 7 min old. The objective needs the menu, a Return **I** choose to send, and an observable change — none of that is claimed here. What IS claimed, and now has runtime evidence rather than a disassembly argument: the Cocoa main thread no longer dies on a Return, which is the precondition for all of it.

09:25 · COORDINATOR — ★★★ THE SHARED HANG ENDPOINT IS FIXED AT THE ROUTER, AND THE MECHANISM IS NOT WHAT THE ASK ASSUMED.
The HK-E2E lane asked for "bound the walk + print on overflow". The walk is ALREADY bounded:
`macrunner_hb_ldr_entry_from_pc` (macrunner_hb.c:1331) walks the PEB list under `guard++ < 4096`, so the loop was never
inside it and bounding it harder would have changed nothing. The real shape is RECURSION: that function opens with
`NtCurrentTeb()->Peb` (line 1311), and on a thread with no Wine TEB — a Cocoa callback thread is exactly that — the read
itself faults. The fault handler calls the router, the router calls the classifier, the classifier faults again. Every
fault begets the next. That is why both sampled hangs pin at `+20` into that function, i.e. at its very first statement,
and why the process stays alive at ~107 % CPU with the log simply stopping.
Second finding, and it is why nothing protected us: `macrunner_hb_callback_loop_route_enter` DOES keep a depth counter,
but it early-returns on `!macrunner_hb_callback_loop_trace_enabled()`. In an ordinary run nothing counts; and even with
the trace on it only EMITS the depth — it never breaks the cycle.
FIX (signal_arm64.c, ALWAYS ON, independent of every env gate): a `__thread` re-entrancy depth with a
`__attribute__((cleanup))` release, checked at the top of `macrunner_hb_route_x64_callback_fault`. On re-entry it writes
`macrunner-hb-fault-reentry-break` once via the async-signal-safe writer and returns FALSE, so the fault falls through to
normal handling — a diagnosable crash instead of a hang. `MACRUNNER_HB_FAULT_REENTRY_LIMIT_OFF=1` restores the old
behaviour for an A/B. Compiles clean (`make dlls/ntdll/unix/signal_arm64.o` rc=0, 8 pre-existing warnings, none from the
edit). NOT deployed yet — the slot is busy; `scripts/faultreentry-deploy-when-slot-free.sh` will link, deploy, and verify
by CONTENT (`strings` for the marker) as well as by SHA, because an SHA-only guard already gave one false abort today.
NOTE for both lanes: this does not fix the underlying faults, it stops them from becoming silent hangs. Expect runs that
previously froze to now die with a real error and a printed PC. That is the point — six iterations were spent because a
fault loop in us reads exactly like a livelock in the guest.

09:35 · ITER-8 · ★★★★★ **RETURN NOW REACHES THE GUEST INPUT BOUNDARY — 39 TIMES. THE PREVIOUS BEST IN THIS PROJECT'S HISTORY WAS 2, AND THEY WERE ARROW KEYS.** Run 21, HK pid 4746: `stage=macdrv_key_event hwnd=0x30038 window=0x8691e4000 keycode=36 pressed=1 **vkey=0xd**` from +517.035 s onward — `macdrv_key_event`=**39** and `macdrv_send_keyboard_input_sent`=**39**, every one of them **VK_RETURN**. ITER-5's sweep over all 34 HK runs on disk found `macdrv_key_event` = 2 in exactly one run and 0 in the other 33, and those 2 were `vkey=0x28` (VK_DOWN). So the chain `CGEventPostToPid → AppKit → -[WineApplicationController handleEvent:] → keyDown bypass → postKey: → macdrv queue → guest ProcessEvents → macdrv_key_event → NtUserSendHardwareInput` is now complete **for the Return key specifically**, which is the key the objective needs and the one that used to kill the process on contact.
09:35 · ITER-8 · RUN 21 ENDED, AND I AM NAMING THE ENDPOINT RATHER THAN THE PROGRESS. It died at **+715.830 s**: `macrunner-hb-run-exit: label=thread status=c000007b reason=runtime pc=0x87efd7edc9e`, `run-exit-indirect … q0=0x0/MEMORY_FAULT`, with `macrunner-hb-mem-pressure: virtualalloc_ok=128 virtualalloc_fail=12`. That is the documented HK Mono worker-thread `c000007b` class — but **not** the 0x4ea08f site (`0x4ea08f`=0 this run), a different pc. `label=thread` means a thread died, not the process, which is why pid 4746 kept burning ~133 % CPU with its log frozen at +715.8. Counts at death: `Restored language`=**0**, `Loaded Objects now`=**0**, `macrunner-return-route` (the guest-side win32u marker) =**0**. So the input half advanced enormously and the **menu half did not move**: HK still does not reach the scene load, and no key has yet been proven to reach guest *code* — only the guest input boundary.
09:35 · ITER-8 · THE INJECTION CADENCE IS NOW EXPLAINED, AND IT IS NOT MINE TO KEEP. The log shows a metronomic alternation — Return, DownArrow, Return, DownArrow — at **20.1 s** intervals from +147.2 s to +470.1 s, i.e. a periodic injector that is not the watcher (which gates on `Loaded Objects now` and logged only `poll`/`menu wait`). It is a leftover loop from an earlier iteration of this lane. It is currently doing useful work — it is what proved the fix — but it means run 21's key counts are NOT from keys this lane deliberately timed, so they cannot serve as the objective's evidence. Finding and owning that injector is the next harness task, before any GOAL claim.
09:35 · ITER-8 · NET FOR THE ITERATION, HONESTLY SCOPED. Closed: the `+280…+310 s` freeze that killed runs 15–19 (one un-guarded `GetCurrentThreadId()` in `event.c`, fixed, verified by disassembly AND by 8/8 Returns completing at runtime where 0/6 completed before). Retired with evidence: the ntdll build-epoch theory, the SMC guards, the `GC_stop_world` reading, and "self-init costs throughput". Handed off: two live samples showing HB's fault router spinning forever at two unrelated call sites (`HK-HB-FAULT-ROUTER-HANGS-TWO-CALLSITES-20260729.md`). Still open and unmoved: **HK has not reached `Loaded Objects now` since 00:29 today**, and the current endpoint is a Mono `c000007b` at +716 s. The objective is not met and this remains CONTINUE.

09:50 · ITER-9 · ★★★★★ **THE MENU WALL IS AN ENGINE-EPOCH REGRESSION AND I CAN DATE IT TO A 15-MINUTE WINDOW.** Every run dir records its engine identity for free in `MACRUNNER_HB_TRANSLATION_CACHE_ROOT` (basename = `ntdll-<sha16>`), so this is a log read, not an experiment. Over all 102 run dirs that have both a contract and a log, **conditioned on `Initialize engine version`>=1** (20 runs that died before the engine started — the d3d11 887a0002 class and the +46s DllMain class — are excluded; they cannot reach a menu and would have inflated the failure rate of whatever build was deployed): builds up to `5385710288b50361` = **28/36 booted runs reached `Loaded Objects now` (78%)**; builds from `99e1e6348ee46957` onward = **2/27 (7%)**. **Fisher one-sided p = 1.3e-08.** Last run on the good build started 07-28 **17:12:35**; first run on the bad build started 07-28 **17:28:02**.
09:50 · ITER-9 · AND I DECLARED THE CONFOUND RATHER THAN BANKING THE BIGGER NUMBER. The last two builds (`bd7b00f9e0`, `f6493bbcf8`, 12 runs) are polluted by ITER-8's TEB bug, which killed the Cocoa main thread on the ambient Return that arrives at +147…+185s in every run of this lane — several of those 12 never had a chance at a menu, so their 0% is not independent evidence. Dropping them entirely and using only the pre-self-init post-boundary builds the TEB bug could not touch (`99e1e634` + `4808c18e`): **2/13 = 15% vs 78%, p = 1.3e-04.** The regression survives its own confound.
09:50 · ITER-9 · TWO EXPLANATIONS KILLED WITH DATA, SO NOBODY RE-CHASES THEM. **(a) Self-init is innocent — it arrives 10 hours late.** `macdrv_selfinit` first appears in `bd7b00f9e0` at 07-29 04:11; the regression is already complete at 07-28 17:28 on `99e1e6348ee46957`, which is 0/4 with `selfinit`=0 in all four. Every post-boundary build before 04:11 has selfinit=0 and already fails. **(b) Cold translation cache is innocent.** Each build does get its own cache dir and `5385710288b50361` is a 216 MB outlier warmed over 30+ runs — a real alternative explanation, and it is refuted by the small-cache builds on both sides: 07-27 builds at **10.7–18.2 MB → 6/6 = 100%**, 07-28/29 builds at **10.0–19.7 MB → 0/8 = 0%**. Same warmth, opposite outcome; the build date separates them, cache size does not. (The old memory that this cache "is 16 bytes = never persisted" does not hold for this path — 1.7 GB across 170 dirs.)
09:50 · ITER-9 · SUSPECT NAMED, AND NAMED AS A SUSPECT. In the window sits the work later committed as `e4b90849` "kernelbase: list_init the reg_mui_cache ARM64X twin — **unblocks all guest input**" (committed 18:31, but in-tree and being built against from 17:22 — the first bad-build run is literally tagged `laneA-INPUT-MUIFIX-try18-172802`). **[HYPOTHESIS, untested]** the change that gave this project input is the change that cost it the menu: before it, explorer.exe died in kernelbase's DllMain, nothing loaded winemac.drv, and HK ran on the null driver doing none of the Cocoa/window/swapchain work; after it HK's process does substantially more work on the way to its first scene. I did NOT test it by reverting — `kernelbase/locale.c` and `engine/hyperbridge/**` are FORBIDDEN to this lane and another lane holds uncommitted work there. That A/B needs an owner who may touch kernelbase.
09:50 · ITER-9 · RUN 21 DID NOT STALL, IT DIED, AND IT DIED FAR EARLIER THAN "almost at the menu". `macrunner-hb-run-exit: label=thread status=c000007b pc=0x87efd7edc9e` at **+715.830s**, 32 frames deep in UnityPlayer, `q0=0x0/MEMORY_FAULT`, `virtualalloc_fail=12`; `label=thread` is why pid 4746 then burned 133% CPU for 20 min with a frozen log. Against the last run that DID reach a menu (`HK-E2E-05`): engine-init +50.2s ✓, MonoManager +52.0s ✓, then **`UnloadTime` +252.7s NEVER, `Restored language` +370.4s NEVER, `Loaded Objects now` +424.4s NEVER**. It never cleared assembly loading and died 291s past the point where a healthy run has already shown its menu.
09:50 · ITER-9 · THE AMBIENT INJECTOR: TWO SUSPECTS CLEARED, AND THE PROBLEM ROUTED AROUND INSTEAD OF HUNTED. `hk_e2e_key_watch.py` never reached its injection phase (run-21 `watch.out` = 72 lines of poll/menu-wait, gate never closed); `hk_cgevent_key.swift` `--probe-only` calls `exit()` at line 124 **before** the `postToPid` at 142/146, so the per-poll window probe cannot post a key; `hk-e2e-supervise.sh` contains no injection; no live process carries an injector in its argv. Origin still unknown — but the metronome only ever sends keycode **36 (Return)** and **125 (Down)**, so the objective's evidence will ride on keys it never sends: **`up` (126)** for the selection change and **`space` (49)** for submit (Unity's StandaloneInputModule fires OnSubmit for Space too). Attribution becomes structural instead of circumstantial, which is better than finding the injector.
09:50 · ITER-9 · SLOT FREED IN ORDER, QUEUE DRAINED, ENGINE VERIFIED BY CONTENT. Run 21's HK was dead-in-fact but its supervisor still held the slot on a 2700s budget; killed scoped by verified PID supervisor-first (94321 → 740 watcher → 98080 → 98092/98126 → 98125 → 98127, then guest 4746/4799/4750) plus an 8.5-hour-old orphaned `services.exe` 34529. No `pkill`; zero wine-family processes left; 37 GB free; zero orphaned `_mr-run.*` prefixes. That released the queue correctly: the coordinator's deploy fired at 09:43:25 and landed `ntdll.so bd7b00f9e0b35dc3 → **968d1dafcbcc3b81** with `macrunner-hb-fault-reentry-break` present=1` (was 0), and **`winemac.so` came out of the full rebuild byte-identical at `278189b4fa293d0a`** — my ITER-8 TEB fix survives, markers 5/5. Also settled: `macrunner-return-route` is shipped in `aarch64-unix/win32u.so` as well as `winemac.so`, so ITER-8's "guest-side marker = 0" means win32u never completed delivery, NOT that the instrument is missing. Sister lane `hk-dllmain-i4` then took the slot.
09:50 · ITER-9 · REGISTERED PREDICTION BEFORE THE NEXT RUNS, SO IT CANNOT BE WRITTEN AFTERWARDS. Run 21 is the ONLY post-boundary run that carried the TEB fix — n=1 against a 73%-era baseline of n=30 — so one negative proves nothing. Next: 3–4 attempts on `ntdll.so 968d1daf` + `winemac.so 278189b4`. **If >=1 of 4 reaches `Loaded Objects now`, the regression is a rate problem and the objective is reachable by retrying; if 0/4, the epoch genuinely gates the menu** and the §3 suspect needs an owner who may touch kernelbase. Full write-up: `reports/phase4-hollow-knight/HK-E2E-ITER9-MENU-EPOCH-REGRESSION-20260729.md`. Objective NOT met — no menu, and no key I chose to send. **LOOP-STATUS: CONTINUE**

10:05 · ITER-9 · ★★ **I AM RETRACTING MY OWN HEADLINE FROM 20 MINUTES AGO — THE DENOMINATOR WAS WRONG AND IT FLIPPED THE CONCLUSION.** The "78% → 7% at a 15-minute build window, p=1.3e-08" counted every engine-booted run, but a large share of post-boundary runs were SHORT diagnostic probes (94s, 118s, 131s, 142s, 145s) launched to answer input/driver questions. A run killed at 94s cannot reach a marker that lands at +424s, so scoring it as a menu failure is my error. Re-conditioned on runs with the wall-clock to succeed (last timestamp >=600s) and split at self-init's arrival as well as at the build boundary: **PRE (<07-28 17:20) 12/12 = 100%; MID (17:20→07-29 04:00, no self-init) 2/4 = 50%, p=0.05; LATE (>=07-29 04:00, self-init) 0/8 = 0%, p<0.0001.**
10:05 · ITER-9 · ★★★ **AND THAT REVERSES "SELF-INIT IS INNOCENT" — IT IS THE STRONGEST SINGLE CORRELATE IN THE DATA.** Measuring the `Begin MonoManager ReloadAssembly → UnloadTime` phase across every run that has both markers: **`macdrv_selfinit` absent → n=19, median 219.7s, MAX 237.7s, runs>300s = 0. `macdrv_selfinit` present → n=9, median 228.4s, MAX 928.5s, runs>300s = 4 (390.5, 437.7, 753.1, 928.5).** Zero of nineteen without it ever exceeded 237.7s; four of nine with it exceeded 300s. What I read this morning as a pre-self-init regression (`99e1e6348ee46957`, 0/4) is **0/1** once the three short probes are removed. **The 07-28 17:20 build boundary is NOT established** — it rests on 2/4 vs 12/12, n=4, p=0.05. The only firmly established regression is the self-init era. My §3 kernelbase/`e4b90849` suspect is correspondingly demoted from "prime suspect" to "unproven and now poorly supported"; nobody should spend a kernelbase revert on it.
10:05 · ITER-9 · AND THE MEDIAN IS UNCHANGED IN EVERY ERA (202–228s), SO THIS IS NOT A SLOWDOWN — IT IS A HEAVY TAIL. Most self-init runs boot at normal speed; a subset stalls 2–4×. That shape has a ready mechanism, and it is the coordinator's 09:25 finding: self-init is what first put `[NSApp run]` inside HK's own process, which is what made ITER-8's un-guarded `GetCurrentThreadId()` reachable, and a fault on a thread with no Wine TEB makes `macrunner_hb_route_x64_callback_fault` RECURSE — a core pinned at ~107% forever. A core spinning in a fault loop starves the guest's JIT threads: same median, occasional 2–4× tail, no clean blocking gate. **[HYPOTHESIS]**, but note **every run in this dataset predates the fix** — ITER-8's `event.c` change closed one fault site (run 21 still tailed at 437.7s, so others remained) and the general re-entrancy guard landed only at **09:43:36 today**.
10:05 · ITER-9 · CORRECTION TO MY OWN RUN-21 POST-MORTEM: IT DID REACH `UnloadTime`, AT +491.6s. I wrote "never" from `watch.out`, which does not track that marker, instead of reading the log. The corrected reading is more useful: run 21 was **not blocked, it was late** — engine-init +51.8s and MonoManager +53.9s are within 2s of the good run, and the entire divergence sits in `MonoManager → UnloadTime` (437.7s vs a pre-self-init max of 237.7s over 19 runs). It then died at +715.8s still short of `Restored language`.
10:05 · ITER-9 · ★ SHARPENED PREDICTION FOR THE QUEUED RUNS, REGISTERED BEFORE THEY START. `HK-E2E-22-EPOCHTEST`, 4 attempts × 1500s, waiter pid 37288 polling politely behind the sister lane's `hk-dllmain-i4` run (it backs off if they take a second attempt). Engine: `ntdll.so 968d1dafcbcc3b81` (fault-reentry guard present=1) + `winemac.so 278189b4fa293d0a` (TEB fix, byte-identical through the full rebuild). **The measurement that decides it is now a continuous one, not a coin flip: `MonoManager → UnloadTime` must come back under ~240s with no run over 300s.** If it does and menus return, the fault loop was the whole story. If it still tails, the fault loop was not the cause and self-init costs throughput some other way — and that is a far better question than the one I started the iteration with. Watcher edited (`tools/hk_e2e_key_watch.py`, syntax-checked) to lead its arrow phase with `up` (126), a key the ambient metronome never sends, so the objective's evidence is attributable by construction.

10:20 · ITER-10 · ★★★ I AM RETRACTING THIS LANE'S CLAIM THAT WIN32U NEVER DELIVERS THE KEY — IT DOES, AND THE CHAIN IS PROVEN TO THE WINESERVER. ITER-8 recorded `macrunner-return-route`=0 and ITER-9 built on it ("win32u never completed delivery"). Measured from run 21's log: **104 records across five stages** — `macdrv-nsevent` 24, `macdrv-event-dequeue` 20, `macdrv-vkey-map` 20, **`win32u-send-request` 20**, **`server-raw-enqueue` 20**, alongside `macdrv_key_event`=39 and `macdrv_send_keyboard_input_sent`=39. And the zero was NOT a budget artefact: `input.c:86/:88` gate on `MACRUNNER_HB_RETURN_ROUTE_OBSERVER` and cap at 256; run 21's contract sets MAX=256 and the per-process seq maxima were 64 and 20, nowhere near it. (The 84 records under one pid against a 64-long sequence are two independent copies of the static counter — the observer ships in BOTH `winemac.so` and `win32u.so`.) So the objective's first half is plumbed and evidenced as far as the server raw-input queue; the only unproven hop left is HK's own message loop dispatching it, which cannot be tested without a menu.
10:20 · ITER-10 · A LEAD I RAISED AND THEN KILLED WITH MEASUREMENT RATHER THAN SHIPPING. `hk_e2e_key_watch.py:82` re-scans all of run.log every 3s for 20 substrings; on the repo's biggest log (1962.8 MB) that is 20.29s/pass = 676% of a core, which would be a serious tax on a guest this lane believes is starved. It does not apply: this lane's HK logs are n=36, median **0.35 MB**, max **0.50 MB** → 0.005s/pass = **0.17%** of one core. No change made. The 1.9 GB file was a `wx-census` run, not ours — I had measured the wrong denominator and corrected it before drawing anything from it.
10:20 · ITER-10 · ★★ THE MENU WALL RESTATED, AND IT IS NOT "AN EPOCH REGRESSION": **A WINDOW AND A MENU HAVE NEVER CO-OCCURRED IN THE CURRENT BUILD.** Conditioned on wall-clock >=600s up front (the denominator error that flipped ITER-9's headline): self-init ABSENT n=16 → **14/16** reached `Loaded Objects now`, MonoManager→UnloadTime median **224.0s**, max **237.7s**, zero >300s. Self-init PRESENT n=8 → **0/8** reached it, median **390.5s**, max **928.5s**, four >300s. But the decisive row is attribution: **of the 14 fast-era runs that reached the scene load, only 2 had a Cocoa window.** The fast era was fast largely because HK was doing no window work — so "restore the fast era" is not a goal, the objective needs the window. `CreateSwapChainForHwnd` is 16/16 even with no window, so the swapchain is not the cost; the real Cocoa window is. [HYPOTHESIS, not isolated.]
10:20 · ITER-10 · THE SELF-INIT ERA FAILS IN TWO DISTINCT MODES, AND ONE OF THEM IS A HANG, NOT SLOWNESS. **Mode A (dies): runs 12/14/16/21** — reach `UnloadTime`, 3 of 4 reach `Restored language`, then die **38–69s later**, i.e. inside the scene-load window. **Mode B (stalls): runs 13/18/19-t1/19-t2** — `UnloadTime` at a strikingly consistent **277–281s**, then `Restored language` NEVER, for up to **4285s (71 min)**. Run 19-t1 ran 71 minutes past UnloadTime producing no further phase marker.
10:20 · ITER-10 · ★ THE MODE-A DEATH IS BYTE-IDENTICAL ACROSS THREE INDEPENDENT RUNS — HANDED OVER, NOT TOUCHED. Runs 12, 14, 16 all die at pc **`0x87ef28ca08f`** with identical `last_bytes` (`cc eb 92 48 8b 84 24 98 …`), identical `prev_bytes`, identical `rbp=0x87ef2b26d40`, `r15=0x0`, all of `q0/q8/q10/q18/q20 = 0x0/MEMORY_FAULT`; status `c000007b`, `label=thread`. `prev_bytes` decodes as two IAT-indirect calls (`ff 15 …`) and the pc lands on the following `cc` (int3/padding). Run 21 dies at a different pc (`0x87efd7edc9e`). A probably-same-site death appears **once** pre-self-init (`INPUT-MUIFIX-try18`, pc `0x87ef28ea08f`, differing by 0x20000 = different image base), so the site is not new — its **frequency** is (3/8 vs 1/16) and it now lands BEFORE the menu instead of after (the one fast-era run that died after a menu did so +6333s later). HyperBridge is FORBIDDEN to this lane; written up, not edited.
10:20 · ITER-10 · ★★★★ **EXISTENCE PROOF THAT BOTH CAN CO-OCCUR, FULLY ATTRIBUTED TO HK'S OWN PID.** `laneA-HK-E2E-05-a1-try1-002925`: `wine-child.pid`=**7057**, and `stage=create_cocoa_window pid=7057 hwnd=0x30038 cocoa=0xa47f28000` at **+138.452s** — HK's own process, its own window, the same hwnd the self-init runs get — then `Loaded Objects now: 4274` at **+424.386s**, with `macdrv_selfinit`=**0** and `user_driver_placeholder_replaced`=**0**. Attribution mattered: the other 4 fast-era runs with Cocoa windows had them under **pid 7156**, not HK, so "4/16 had a window" overstates it and "2/14" is the real number.
10:20 · ITER-10 · AND IT IS NOT THE ENVIRONMENT — IT IS THE DEPLOYED BINARIES. Full env diff run 05 vs run 22 (`final-child.json`, 125 vs 129 keys): **zero** keys only in run 05; only in run 22 are `MACRUNNER_HB_TRACE_WINSHOW`, `MACRUNNER_TRACE_WINEMAC_DRAIN`, `MACRUNNER_HK_GOOD_BOOT_MARKER` (trace/harness) and `MACRUNNER_MACDRV_KEYDOWN_NO_KEYWINDOW=1`. Everything else differing is a path, tag, session id or epoch. So the gap between "menu+window" and "window, no menu" is the deployed engine, and the biggest in-territory change across it is winemac.drv's unix self-init.
10:20 · ITER-10 · REGISTERED PREDICTION **FAILED**, STATED AS SUCH. ITER-9 registered "MonoManager→UnloadTime must come back under ~240s, no run over 300s" on `ntdll.so 968d1daf` (guard verified present in the deployed file) + `winemac.so 278189b4`. Run 22 attempt 1: MonoManager +53.958s, `UnloadTime` **+838.077s** → phase **784.1s**, second-worst on record. **And `macrunner-hb-fault-reentry-break`=0** — the coordinator's guard is deployed and never fired while the tail got worse, which is evidence AGAINST ITER-9's "a fault loop starves the JIT threads" mechanism for this tail. The guard fixes a real hang; it is just not what gates the menu.
10:20 · ITER-10 · NEXT EXPERIMENT REGISTERED BEFORE IT RUNS, AND IT IS FULLY IN TERRITORY. `macdrv_main.c:564` `macdrv_process_selfinit()` is gated — **`MACRUNNER_MACDRV_UNIX_SELFINIT=0` restores the previous behaviour exactly** (`:567`, documented `macdrv.h:292`); it is called from `d3dmetal.c:116` on the DXMT swapchain path, which is why it lands at ~+140s. `hk-run-try12-config.sh` never sets it, so an exported value passes through. **Arm A = SELFINIT=0**, control = the 8 historical self-init runs (0/8). Predictions: **A1** MonoManager→UnloadTime returns under ~240s; **A2** HK reaches `Loaded Objects now`; **A3 — the one that decides it — does HK still get its OWN Cocoa window without self-init?** Run 05 says yes and needed neither self-init nor the placeholder replacement, and `88894334` has since fixed the driver-install CAS, which should make the normal path more reliable. If A3 fails, self-init is load-bearing, there is no injection target, and the wall is the byte-identical `c000007b` above — which belongs to the HyperBridge owner.
10:20 · ITER-10 · SLOT: I DID NOT KILL A RUN I WAS ABOUT TO CALL DEAD. Run 22 attempt 1 (HK pid 55123) was at +738s with `UnloadTime`=0 and I had drafted the teardown; it then reached `UnloadTime` at +838.077s and is still alive and logging at +868.6s. Historically `Restored language` follows UnloadTime by 149–589s, so it can still land inside the 1500s budget. Letting it play out; the A/B takes the slot only when this attempt ends. Full write-up: `reports/phase4-hollow-knight/HK-E2E-ITER10-KEYCHAIN-PROVEN-WINDOW-VS-MENU-20260729.md`. Objective not met — no menu, no key I chose to send. **LOOP-STATUS: CONTINUE**

10:34 · ITER-10 · ★★★ **I AM RETRACTING MY OWN EXISTENCE PROOF FROM 15 MINUTES AGO — THE WINDOW WAS INERT, AND I FOUND IT BY CHECKING WHICH PID INITIALISED THE DRIVER.** I claimed run 05 showed HK holding a real Cocoa window AND finishing the menu scene load. Wrong. In run 05 the ONLY process that ever brought the Mac driver up was **pid 7156 — explorer, not HK**: `macdrv_init_session pid=7156` / `macdrv_init_user_driver_set pid=7156`, and NOTHING for HK's pid 7057. HK's single `create_cocoa_window pid=7057` came through DXMT's direct `macdrv_functions` table — exactly what `macdrv_main.c:470-474` documents, winemac.so alive in the process and able to make a window object **without the driver being initialised**. That is why `macdrv_key_event`=0 in run 05: input was structurally impossible there, not merely untried. Run 21 by contrast shows BOTH `pid=4900 origin=dllmain` AND `pid=4746 origin=selfinit`, and 39 key events. **Corrected conclusion: there is no run on record where HK had an initialised user driver AND reached the menu.** Both halves of the objective are individually demonstrated and have never once been demonstrated together — the key chain works when HK has a driver (run 21), the menu loads when HK has none (run 05).
10:34 · ITER-10 · AND THAT REFUTES MY OWN PREDICTION A3 FROM THE SOURCE, BEFORE SPENDING THE SLOT ON IT. `macdrv_process_selfinit()` early-returns on `macdrv_process_initialised` (`macdrv_main.c:569`) and its header states that with the gate off "this function then does nothing and **the process keeps win32u's re-entrancy placeholder**" (`:554-556`). Nothing else initialises the driver in HK's process. So `MACRUNNER_MACDRV_UNIX_SELFINIT=0` gives HK no driver, no injection target, no input — **the arm CANNOT satisfy the objective**, and I am recording that rather than deleting the prediction, because the version of this experiment I nearly launched would have been read as a path to GOAL. It still runs, as diagnostics only, scored on A1/A2 alone: menu returns with the gate off → HK *paying for a real user driver* is the regression's mechanism and the fix (make that path cheaper or later) is in this lane's territory; menu does not return → self-init is innocent and the wall is the byte-identical `c000007b`, which is the HyperBridge owner's. [HYPOTHESIS for the cost, not isolated: with the placeholder every user-driver call in HK is a no-op stub, and after `init_user_driver()` each becomes real Cocoa work on a loading loop that pumps messages continuously.]
10:34 · ITER-10 · HANDOFF ARMED SO THE A/B CANNOT REPEAT THIS LANE'S TWO KNOWN SLOT MISTAKES. `scripts/hk-e2e-selfinit-ab.sh` (syntax-checked, pid 62185, detached so it survives a thread relaunch) waits on the incumbent **HK pid**, not a name pattern; stands down entirely if the incumbent scored `menu_ready` (a success is meant to be left alive for follow-up, and tearing it down would destroy the objective's evidence); kills only supervisor 37288's own tree by verified PID; then waits for `wineserver` to clear and re-checks after a 20s settle so a foreign autoloop claiming the slot wins the race instead of colliding. No `pkill` anywhere.

10:41 · ITER-11 · ★★★★ **THE SELF-INIT STALL IS DETERMINISTIC, NOT THROUGHPUT STARVATION — TWO RUNS LANDED 3.2 s APART ON A 780 s PHASE WHILE ONE OF THEM HAD 9 MINUTES OF CPU CONTENTION AND THE OTHER HAD NONE.** Run 22 attempt 1: `Begin MonoManager` +53.958s → `UnloadTime` **+838.077s** = phase **784.119s**. Attempt 2: +57.980s → **+838.900s** = phase **780.920s**. Attempt 2 shared the machine with the sister dllmain lane's HK (pid 71811, `hk-dllmain-i7-twinattach-a1`, 10:20:33–~10:30) for its first ~9 min; attempt 1 ran alone. A throughput-starved boot cannot absorb a second HK instance and land within 0.4 % of the uncontended run. Against the healthy self-init-absent era (median **224.0s**, max **237.7s**) the excess is **≈558 ± 2 s** — a constant, which is the signature of a wait or a fixed-count cost, not of a rate. This retires "the driver costs throughput" as this lane's working model.
10:41 · ITER-11 · TOP CANDIDATE FOR THE CONSTANT, NAMED AS A HYPOTHESIS. Sampling attempt 2 live at +1050s, one guest thread (tid 30389849) spends **2973/4098 = 72.5 %** of its samples in `macrunner_hb_call_import_thunk → macrunner_hb_call_arm64_pe_import12_for_ctx → __wine_syscall_dispatcher → **NtSetTimer** → wine_server_call → server_call_unlocked → write(2309)/read(647)` — i.e. a **wineserver round-trip per timer set**, thousands of them. The same thread class shows in attempt 1 (tid 30332065, `NtSetTimer` 1156/4858 = 24 %). **[HYPOTHESIS, not isolated]** a fixed number of timer/message operations become real wineserver traffic once the real user driver replaces win32u's no-op placeholder, adding a constant to the load path. Not yet attributed to the driver: `NtSetTimer` also has guest-side callers, and I have no placeholder-arm sample to difference against.
10:41 · ITER-11 · ★★★ I FOUND A PINNED HOST THREAD AND THEN REFUTED IT MYSELF INSIDE THE SAME ITERATION. Attempt 1 sampled at ~+1400s had exactly one thread pinned **4862/4862 samples, fanout 0**, in `macrunner_hb_run_x64 → _sigtramp → macrunner_hb_primary_signal_handler → **macrunner_hb_route_x64_callback_fault** → macrunner_hb_pc_is_x64_guest_code_module_no_lock`; `_sigtramp` appears **once**, and `macrunner-hb-fault-reentry-break`=0 in the log, so this is NOT the nested recursion `signal_arm64.c:1454`'s always-on guard was written for. It looked like the answer. It is not: `tools/hk_wedge_monitor.sh` sampled attempt 2 on a 90 s cadence **through the identical stall** (+601s, +696s, +791s, `UnloadTime` still absent) and found **0 wedges every time**. The stall reproduces with no wedged thread; the wedge arrives ~560 s after the stall starts. Recorded, handed to the HyperBridge owner, and explicitly NOT the menu blocker.
10:41 · ITER-11 · TWO READINGS KILLED BEFORE THEY COULD BE BANKED. **(a) `[NSApp run]` is not a cost** — HK's process main thread is `__wine_main → CFRunLoopRun → run_cocoa_app → -[NSApplication run] → mach_msg2_trap`, **4862/4862 samples blocked**, zero CPU; any story where self-init's Cocoa loop burns time is wrong. **(b) My own "attempt 2 is idle at 0.0 % CPU, so it is waiting" was a transient `ps` sample** — re-measured 40 s later it was **60.0 %** with 4 running threads. Deleted rather than shipped.
10:41 · ITER-11 · INPUT IS STRUCTURALLY DOWNSTREAM OF THE MENU, SO THERE IS NO SHORTCUT. Attempt 1: `postKey_posted`=**18**, `macdrv_key_event`=**0**, `ProcessEvents_drain`=**2** (last +1048.7s). The ambient injector's keys reach HK's Cocoa queue and are never converted to guest input because the guest never pumps while stuck in the Mono load — run 21 got `macdrv_key_event`=39 on this same code once it was pumping. The key half cannot be demonstrated before the menu half.
10:41 · ITER-11 · LEVER BUILT AND DEPLOYED, VERIFIED BY CONTENT NOT BY EXIT CODE. **`MACRUNNER_MACDRV_SELFINIT_DELAY_MS`** withholds the unix-side driver install for N ms from HK's first winemac.so entry; unset/0 is byte-for-byte today's behaviour. `macdrv_main.c` gets `macdrv_selfinit_delay_elapsed()` checked inside `macdrv_process_selfinit()`; `d3dmetal.c` gets retries at `my_macdrv_get_cocoa_window`, `my_macdrv_view_get_metal_layer`, `my_GetWindowRect` — required, because `my_get_win_data` is a swapchain one-shot and a deferred install would otherwise never happen. Deliberately not a timer thread (a TEB-less pthread reaching win32u is the documented way into the fault router). Deployed `winemac.so` **278189b4fa293d0a → 023394a181bb225a**, `macdrv_selfinit_deferred` present in the shipped binary, `ntdll.so` unchanged at `968d1daf` as intended. Rationale: both halves of the objective are proven and have never co-occurred, and deferral wins under either reading of the cost — remove it from the load window, or move it past the menu, which is when input is needed anyway.
10:41 · ITER-11 · REGISTERED BEFORE THE ARM RUNS. `MACRUNNER_MACDRV_SELFINIT_DELAY_MS=420000`; controls = 8 historical self-init runs (0/8 menu) and 16 self-init-absent runs (14/16). **P1** `stage=macdrv_selfinit_deferred` prints and `macdrv_selfinit_entry` does not, before +420s. **P2** `UnloadTime` under ~300s (vs 838.1 / 838.9 in the two runs above). **P3** HK reaches `Loaded Objects now`. **P4 — decides it** — after the menu a retry site fires, `macdrv_selfinit_entry` prints, and `macdrv_key_event`>0 for a key I inject. P2/P3 pass + P4 fail ⇒ widen the retry set (in territory). P2 fail ⇒ self-init timing is innocent and the wall is the constant in the load path.
10:41 · ITER-11 · SLOT, STATED PLAINLY. A collision I did not cause: the sister dllmain lane started its own HK at 10:20:33 into my still-live attempt 1, and my supervisor started attempt 2 at 10:21:49 into that. I did **not** kill the sister lane's run — not my territory — and it ended on its own budget. Attempt 2's *timing* is therefore confounded and is not scored as clean data; its *wedge counts* are unaffected, a stack shape not a rate. New instruments: `tools/hk_wedge_classify.js` (dominant-spine thread classifier — my first cut matched wait symbols anywhere in the subtree and called all 54 threads blocked, hiding the wedge completely) and `tools/hk_wedge_monitor.sh` (read-only cadence sampler, writes `wedge-timeseries.tsv` beside the run log). Report: `reports/phase4-hollow-knight/HK-E2E-ITER11-WEDGE-MEASURED-NOT-CAUSE-20260729.md`. No menu, no key I chose to send. **LOOP-STATUS: CONTINUE**

10:58 · ITER-11 · ★★★★★ **DEFERRING THE DRIVER INSTALL RESTORED THE MENU, AND IT IS A CAUSAL RESULT, NOT A CORRELATION — SAME BUILD, SAME LAUNCHER, ONE ENV VAR.** `MACRUNNER_MACDRV_SELFINIT_DELAY_MS=420000`, run `laneA-HK-E2E-23-SELFINITDELAY-t1-a1-try1-104755`. **P1 PASSED:** `stage=macdrv_selfinit_deferred` at **+143.955s** — the same instant self-init used to fire (+141.383s / +147.963s in the two controls) — with `macdrv_selfinit_entry`=**0** and `user_driver_placeholder_replaced`=**0**, so the driver really was withheld. **P2 PASSED, and not marginally:** `Begin MonoManager` +55.977s → `UnloadTime` **+268.608s** = phase **212.631s**, against **784.119s** and **780.920s** in the two self-init controls and a healthy-era max of 237.7s. The ≈558 s constant is **gone**. **P3 PASSED:** `Restored language` **+384.833s**, `Loaded Objects now` **+440.262s**, `menu_ready=true` (gate `Loaded Objects now`) in `evidence.json`. **This lane has never had a menu with the current build.** The ITER-10 statement "no run on record where HK had an initialised user driver AND reached the menu" now has its other half: withhold the driver through the load window and the menu comes back.
10:58 · ITER-11 · **P4 FAILED, AND THE RUN WAS TOO SHORT TO HAVE TESTED IT.** `macdrv_selfinit_entry`=0, `macdrv_key_event`=0, `postKey_posted`=0, `target_alive_at_end=False`, `delivered=0`. But the honest reading is that P4 never got its chance: the delay expires at +420s, `Loaded Objects now` lands at +440.262s, and the supervisor tore the attempt down at +478s — **38 seconds of post-menu life**, during which nothing on the DXMT render path happened to call a retry site. So this is not yet evidence that the retry set is wrong. Two things need separating and I will not guess between them: (a) no retry site is called post-menu at all, (b) they are called but the window was too short. **Correction to my own diagnostic:** I first read `get_win_data`=0 / `macdrv_get_cocoa_window`=0 / `get_metal_layer`=0 as "the retry sites were never reached" — wrong twice over. The unconditional probe prints `macrunner-get-win-data` with HYPHENS and I grepped underscores, and the other two are `TRACE()`, channel-gated off. `macdrv_selfinit_deferred` printing at +143.955s proves `my_get_win_data` *was* called. Zeros from a gated or misspelled pattern are not measurements.
10:58 · ITER-11 · WHAT THE SCREENSHOT SAYS, STATED BEFORE ANYONE CELEBRATES. `shot-00-before.bmp` 1512×982, **nonblack=0, colorful=0** — the menu SCENE loaded (`Loaded Objects now`, `Restored language`) but the window renders BLACK. That is this branch's other open problem (`hk/black-frame-root-cause`) and it is orthogonal to input. The objective admits a log marker as the observable change, so it does not block GOAL, but no screenshot diff will be available until the black frame is fixed.
10:58 · ITER-11 · NEXT, NARROWED TO TWO EDITS, BOTH IN TERRITORY. (1) Instrument the gate: count calls to `macdrv_selfinit_delay_elapsed()` while it is declining and print periodically, so "no retry site fires post-menu" and "the window was 38 s" stop being indistinguishable. (2) Move the delay to ~**300000 ms** — after the expensive window closes (`UnloadTime` +268.6s) but ~140 s before the menu, so the driver is in and its Cocoa app running by the time `Loaded Objects now` lands and the watcher injects. Attempts 2–3 of the current series keep running at 420000 unchanged: they replicate P1–P3 at n=3, which this lane needs given its history of single-run noise. Full write-up: `reports/phase4-hollow-knight/HK-E2E-ITER11-WEDGE-MEASURED-NOT-CAUSE-20260729.md`. Menu reached; no key delivered. **LOOP-STATUS: CONTINUE**

11:32 · ITER-11 · ★★★★★ **HOLLOW KNIGHT REACHED ITS MENU *AND* GOT A REAL USER DRIVER IN THE SAME PROCESS — THE FIRST TIME IN THIS LANE'S HISTORY. THE OBJECTIVE IS STILL NOT MET, AND I AM NOT CALLING IT MET.** Run `laneA-HK-E2E-24-WIN32UHOOK-t1-a1-try1-111616`, `MACRUNNER_MACDRV_SELFINIT_DELAY_MS=300000` + the new win32u hook. The whole chain fired in order: `stage=nulldrv_selfinit_hook pid=34395 **resolved=1**` at **+307.858s** (the dlsym found winemac.so's newly-exported `macdrv_process_selfinit`), gate ladder `declines=4 elapsed_ms=155553`, `UnloadTime` **+275.132s**, `Restored language` +392.462s, `Loaded Objects now` **+452.600s**, then `macdrv_selfinit_entry` + `user_driver_placeholder_replaced` at **+453.232s** — driver installed 0.6 s after the menu, exactly as designed — and `postKey_posted` **n=18** from +453.605s.
11:32 · ITER-11 · **WHERE IT STOPS, NAMED EXACTLY: THE ROUTE DIES AT STAGE 1 OF 5.** `macrunner-return-route` n=8, and tallying the stage field gives **`macdrv-nsevent` 8 and NOTHING else** — no `macdrv-event-dequeue`, no `macdrv-vkey-map`, no win32u stage. `macdrv_key_event`=**0**, `macdrv_send_keyboard_input_sent`=**0**, and every nsevent record carries **`vkey=0x0`** and `wine_tid=0`. So the synthetic keys reach macdrv's NSEvent handler in HK's own process and are never dequeued or mapped. **[HYPOTHESIS, untested]** the driver had been up for **0.6 s** when the first key arrived, against ~6 minutes in run 21 (the only run that ever reached `macdrv_key_event`=39, with `vkey=0xd` mapped) — so the keyboard-layout/vkey map may simply not be built yet. Cheap next test: inject on a schedule that starts 60–120 s AFTER `macdrv_selfinit_entry`, not on the menu gate.
11:32 · ITER-11 · ⚠ **THE SUPERVISOR DECLARED SUCCESS AND IT IS A FALSE POSITIVE — FIX THIS BEFORE IT CAUSES A FALSE GOAL.** `[sup 11:25:12] SUCCESS on attempt 1 — menu gate closed, target still alive, 8 key event(s) delivered`. Its `delivered` is `max(guest_wm_keydown_return_route, driver_key_events)`, but the first of those counts **`macrunner-return-route` records of ANY stage**, and all 8 were stage-1 `macdrv-nsevent` — a Cocoa-side arrival, not a guest `WM_KEYDOWN`. The objective requires guest `WM_KEYDOWN` plus an observable change; this run had **neither**. `hk-e2e-supervise.sh:105` must count only `macdrv_key_event` / `macdrv_send_keyboard_input_sent`, or a future iteration will read this banner and stop.
11:32 · ITER-11 · THE RUN THEN DIED, AND THE WINDOW TO INJECT MYSELF CLOSED WHILE I WAS COMPILING THE ACTUATOR. `exit=5` at **+787.446s**, last line `macrunner-hb-nullcall-iat: UnityPlayer module not found` — a post-menu crash, ~334 s after the menu. The supervisor had deliberately left it alive for follow-up and I had `swiftc` running on `tools/hk_cgevent_key.swift` when it went; live HK count went 1 → 0. Lesson for next time: **build the actuator BEFORE the run, not after the menu lands** — the post-menu window here was ~5 minutes and a Swift compile ate it. Screenshot remains `nonblack=0, colorful=0`: the menu scene loads, the frame is still black, so a screenshot diff is unavailable and a log marker must serve as the objective's observable change.
11:32 · ITER-11 · WHAT LANDED, BUILT AND SHA-VERIFIED. `winemac.so` `278189b4` → `023394a1` → **`dae6b4e7f1da607c`**, `win32u.so` → **`5ee1ef280c8a0f35`**, `ntdll.so` **unchanged** at `968d1daf` (I touched no HyperBridge). `nm -gU` confirms `_macdrv_process_selfinit` is now exported **T** — winemac.so exported only 3 symbols and this was not one, so the dlsym would have silently resolved NULL without that change. Edits: `macdrv_main.c` (delay gate + decline ladder + getenv hoisted behind the initialised check, since the hook now calls it per message pump), `macdrv.h` (export + the measured rationale), `d3dmetal.c` (3 retry sites — kept, but measured inadequate: they all fire at swapchain creation ~+144s and never again), `win32u/driver.c` (`nulldrv_ProcessEvents` → dlsym'd self-init; this is the hook that actually worked). New instruments: `tools/hk_wedge_classify.js`, `tools/hk_wedge_monitor.sh`, `tools/hk_syscall_share.{sh,js}`, `scripts/hk-e2e-selfinit-delay-arm.sh`.
11:32 · ITER-11 · NEXT ITERATION, IN PRIORITY ORDER. (1) Fix `hk-e2e-supervise.sh:105` so `delivered` counts only real guest/driver key events. (2) Pre-build `hk_cgevent_key` and inject **60–120 s after `macdrv_selfinit_entry`**, not at the menu gate, to test the vkey-map-not-ready hypothesis. (3) If keys still stop at `macdrv-nsevent`, the question is why the Cocoa queue is not drained into win32u — note `ProcessEvents_drain` is a **bounded per-(thread,queue) probe**, so its n=2 does NOT mean two calls and must not be read as "not pumping". Report: `reports/phase4-hollow-knight/HK-E2E-ITER11-WEDGE-MEASURED-NOT-CAUSE-20260729.md`. Menu ✓, driver ✓, guest WM_KEYDOWN ✗. **LOOP-STATUS: CONTINUE**

11:35 · COORDINATOR — ★★★ AUDIO: HK HAS BEEN RUNNING WITH NO SOUND, AND THE CAUSE IS A MISSING FILE, NOT A BROKEN STACK.
The operator asked whether sound was ever checked. It was not, and it is measurably broken: `laneA-HK-E2E-23-SELFINITDELAY-t1`
(the run that reached the menu) logs at **+55.744 s** — `FMOD failed to initialize any audio devices, running on emulated
software output with no sound.` Root cause, read from `scripts/sync-prefix-from-dist.sh`: `copy_arch_set()` globs
`"$WINE_LIB/$arch"/*.dll`, so **any `.drv` is invisible to the bulk copy** and must appear in `CORE_SYSTEM32_MODULES` by hand
— where `winemac.drv` was the ONLY driver listed. The prefix therefore received dsound / mmdevapi / winmm / the full xaudio2
row (all `.dll`) but never `winecoreaudio.drv`, the CoreAudio backend they all sit on. No backend ⇒ mmdevapi enumerates zero
endpoints ⇒ FMOD goes silent. Exactly the GraphicsDriver shape: whole stack present, host-facing module absent, omission
silent because nothing verifies a driver arrived. FIXED (commit): added `winecoreaudio.drv`, `msacm32.drv`, `winspool.drv`.
Verified by running the script into a disposable prefix and confirming the files land — not by exit code.
**For both lanes:** this changes the prefix contents of every future run. If a run now behaves differently at audio init,
that is why. The narrow claim is only that the driver reaches the prefix; whether sound works is the next measurement and
nobody should assert it yet.
11:36 · ITER-11 · FALSE-POSITIVE SCORING FIXED IN THIS ITERATION, NOT DEFERRED TO THE NEXT. `tools/hk_e2e_key_watch.py:388` assigned the RAW `macrunner-return-route` count — every stage, including the Cocoa-side `macdrv-nsevent` — to a field named `guest_wm_keydown_return_route`; renamed to **`return_route_records_all_stages`** with the measurement recorded inline. `scripts/hk-e2e-supervise.sh` now scores `delivered` on **`driver_key_events` / `driver_input_sent` only** and deliberately does not consult the renamed field. Both syntax-checked; re-scoring run 24's own `evidence.json` through the corrected gate gives `delivered=0 → NOT A SUCCESS`, which is the honest verdict. Stage names for anyone scoring this by hand: `macdrv-nsevent` = cocoa_window.m:2215 (arrival), `macdrv-vkey-map` = keyboard.c:1038 (driver), `unity-*-state-query` = win32u/input.c (the guest actually reading the key). Slot: the live HK now belongs to the sister **hk-dllmain** lane (`hk-dllmain-i9-nativeaep`); my supervisor exited, I am not colliding, and I did not touch theirs. **LOOP-STATUS: CONTINUE**

11:41 · ITER-12 · START. Slot was NOT free on entry: 4 orphaned wine tails (ppid=1, 0.0% CPU) — 3 `services.exe` in `winetemp-*` and one `wineserver` in my own dist — killed scoped by verified PID, never by name. Disk 35 GB free (above the 30 GB stop line, but only just).
11:41 · ITER-12 · ★ RE-READ ITER-11's OWN DATA BEFORE SPENDING A SLOT, AND IT ALREADY NAMES THE WALL — THE COCOA SIDE IS FINE AND THE WINE SIDE NEVER DRAINS. From run 24's log, not from a new run: `postKey_posted` **18** times with `queue=0xbc93e8140` on every one, and `ProcessEvents_drain` prints `tid=30524717 queue=0xbc93e8140 fd=220 calls=1 handled=1` — the **same queue pointer**. So the "keys stop at stage 1 of 5" result is NOT a queue mismatch and NOT a lost event: `-[WineWindow postKey:]` (`cocoa_window.m:2221`) posts to exactly the queue the pumping thread owns.
11:41 · ITER-12 · ★★ THE DRAIN PROBE IS LOG-SPACED (first sight, then calls==10/100/1000/…, `event.c:126`), AND ONLY THE FIRST-SIGHT LINE EVER PRINTED. That is a measurement, not an absence: between its first call at **+453.234 s** and process death at **+787 s** — 334 s — the pumping thread called `macdrv_ProcessEvents` **fewer than 10 times**. `ProcessEvents_key_dequeued` (printed unconditionally under the drain gate, `event.c:811`) = **0**. The first drain landed **0.371 s BEFORE** the first injected key, so every one of the 8 keys arrived after the pump had already gone quiet.
11:41 · ITER-12 · WHY IT CANNOT DRAIN, READ FROM SOURCE: `win32u/message.c:3431` is the **only** call site of `user_driver->pProcessEvents` in the whole of win32u, and it is gated — `if (driver_signaled) drained = user_driver->pProcessEvents(events_mask);` where `driver_signaled = check_internal_bits(QS_DRIVER)`. Reached from `check_for_driver_events()` (`:3480`, throttled to one 8 kHz tick) on every `NtUserPeekMessage`/`NtUserGetMessage` (`:3735/3759/3793/3814`), and from `wait_message` (`:3532`, `:3564`). So <10 drains in 334 s means either the guest stopped peeking, or `QS_DRIVER` stopped being set — **two different bugs with two different fixes**, and run 24 cannot tell them apart.
11:41 · ITER-12 · THE BLIND SPOT IS AN ENV VAR, NOT MISSING CODE. `win32u/message.c:47` gates every one of its probes on `MACRUNNER_TRACE_UI_INPUT`, which `hk-e2e-supervise.sh` never set — that is why run 24 has **0** `process_driver_events_enter` lines despite the code printing `qs_driver=%d` on entry. winemac.drv's own gate (`event.c:45`) honours the same variable. No rebuild is needed to see this.
11:41 · ITER-12 · KILLED MY OWN 2.5-MINUTE-OLD RUN TO GET IT. Attempt `HK-E2E-25-HIDKEY-t1` was already booting when I found the above; it carried the identical blind spot, so I tore it down scoped by PID (supervisor first so it could not start attempt 2) and relaunched as `HK-E2E-26-PUMPTRACE` with `MACRUNNER_TRACE_UI_INPUT=1` + the proven `MACRUNNER_MACDRV_SELFINIT_DELAY_MS=300000` arm. Cost ~2.5 min; the alternative was a 20-minute run that reproduces ITER-11 exactly.
11:41 · ITER-12 · SUPERVISOR CHANGE, AND WHY IT IS NOT A LOOSENED GOAL. `hk-e2e-supervise.sh` now honours `HK_SUP_HOLD_ON_MENU=1`: menu + live target stops the loop and **leaves the run alive**, without consulting `delivered`. Under the strict rule ITER-11 correctly installed, a run that reaches the menu but delivers 0 keys — exactly the state this lane is trying to fix — was torn down and the expensive half (10–25 min) thrown away. The hold is logged as `MENU HOLD ... (NOT scored as goal)`; the success path with its `delivered>0` requirement is untouched below it.
11:41 · ITER-12 · [HYPOTHESIS, NOT YET TESTED] The `.cghidEventTap` channel a sister lane proved for the mouse is **not** this lane's lever. Its finding is that `CGEventPostToPid` delivers `window=nil`, which breaks `handleMouseButton:`; keyboard does not consult that field, and run 24 proves it — the keys reached `postKey` and were posted. Everything downstream of `[queue postEvent:]` is wine-side, so no injector change can move it. Recording this so the next iteration does not spend a slot on it.
12:08 · ITER-12 · ★★★ THE HISTORICAL SWEEP OVERTURNS "THE KEYS STOP AT STAGE 1". Tallying every `laneA-HK-E2E-*` run dir (39 of them) instead of only the newest: **run 21 `TEBFIX-CLEAN` drove the FULL five-stage return route** — `macdrv-nsevent 24 → macdrv-event-dequeue 20 → macdrv-vkey-map 20 → win32u-send-request 20 → server-raw-enqueue 20`, with `macdrv_key_event`=39 and `macdrv_send_keyboard_input_sent`=39, `target_pid=0020 target_wine_tid=0024`. Runs 16/17-t1/18 did the same at n=2. So the Cocoa→winemac→win32u→wineserver chain is **not** broken and has never needed a fix; ITER-11's "stage 1 of 5" was a property of that one run, not of the build.
12:08 · ITER-12 · ★★★ AND THE REAL PATTERN IS AN ANTI-CORRELATION NOBODY HAD PLOTTED. Runs that dequeue keys (16, 17-t1, 18, 21) have `Loaded Objects now`=**0** — no menu. Runs that reach the menu (05, 23-t1, 23-t2, 24) have `key_dequeued`=**0**. **Four and four, overlap zero.** The lane has had the keys and it has had the menu; it has never had both in one process.
12:08 · ITER-12 · WHY, MECHANICALLY: `ProcessEvents_drain` reports `calls=1` in **every** run in the table — the guest's message pump reaches `macdrv_ProcessEvents` a handful of times in a whole boot, and one call drains everything pending (run 21: `handled=41`, all 20 Return records in the same 1 ms). Run 21's single pump landed *after* the injections; run 24's landed **0.371 s before** them and never fired again in the remaining 334 s. So which side of the pump the keys land on is currently **luck**, not plumbing. [HYPOTHESIS] the pump is one-shot because HK's main loop is not calling PeekMessage/GetMessage at menu time; that is the next thing to measure, not to assume.
12:08 · ITER-12 · ⚠ MY OWN win32u PROBE WAS KILLING HOLLOW KNIGHT — CAUGHT BY A/B, NOT BY A HUNCH. A bounded `MACRUNNER_TRACE_DRIVER_EVENTS` counter added to `process_driver_events` (win32u/message.c) shipped as `win32u.so 5ee1ef28→09f6362a`. Every run on it died at **+42–46 s** with `Killed: 9` (SIGKILL) right after `win32u.dll` entry-prot: run 27, run 28-t1, and a third with the env var **unset** so the probe body never ran. Removing the probe and rebuilding reproduced the previous artifact **byte-identically (`5ee1ef280c8a0f35`)** — proving the only delta was mine — and that build boots past `Initialize engine version` and is alive at +64 s. Dist restored to `5ee1ef280c8a0f35`. **Mechanism unexplained** (`getenv`+`fprintf` on a path that returned early should not SIGKILL); recorded as a hazard, not as an understood bug. Do not re-add instrumentation to win32u/message.c without this A/B.
12:08 · ITER-12 · AND THE INSTRUMENT I NEEDED ALREADY EXISTS, COMPILED IN, NO REBUILD. `macrunner_hb.c:33931 macrunner_hb_trace_user32_message_import` reads the guest's `MSG` buffer after `PeekMessageW/GetMessageW` and prints `hwnd/msg(name)/wparam/lparam` — so it shows **both** whether HK pumps at all **and** a guest `WM_KEYDOWN` if one ever arrives, which is exactly the proof this lane's objective demands. Gates: `MACRUNNER_HB_TRACE_USER32_MESSAGE=1`, `..._POST_SWAPCHAIN_ONLY=1` (keeps boot quiet), `..._BUDGET=N` (default 2000; `0` = unlimited — do not use `0`, see the 820 MB note below).
12:08 · ITER-12 · ⚠ TWO TRAPS THAT COST ME RUNS TODAY, BOTH NOW WRITTEN DOWN. (1) `MACRUNNER_TRACE_UI_INPUT=1` is **not** a win32u-only switch: it also lights HyperBridge's per-guest-call `hb_run_x` trace — **5,661,915 lines / 820 MB in 80 s**, and the run stopped producing output at +80 s. Never set it on HK. (2) **Never launch a long run from `ctx_execute`**: its sandbox sets `TMPDIR` to a directory that is deleted when the call returns, and wineserver died with `mkdir …/.ctx-mode-a1TbfD/.wine-501: No such file or directory`. Launch runs from the Bash tool.
12:22 · COORDINATOR — LANE FOLDED INTO hk-master, not stopped for cause. Its 42 runs, its refutations and its trap list are carried over verbatim into prompts/hk-master-lane.md and remain readable here. Consolidated because the operator is right that only one process can hold the engine, and two slot-bound lanes bought no parallelism while costing a three-way collision.
LOOP-STATUS: BLOCKED
Folded into hk-master by the coordinator.
12:36 · ITER-12 · ★★★ THE ONE THAT MATTERS OPERATIONALLY: **I HAD LEAKED TWO HOLLOW KNIGHTS AND THEY WERE EATING THE MACHINE.** At 12:26 `ps` showed **three** `Hollow Knight.exe`: pid 85440 running **44 minutes** at 99.3% CPU (run 26's guest, "killed" at 11:46), pid 44013 running **16 minutes** at 100.0% CPU (run 29's guest, "killed" at 12:19), and only pid 60825 (RSS 2.5 GB) was the live run. `hk-e2e-supervise.sh`'s `kill_tree` walks down from the **launcher**, and the guest is not in that tree — so every torn-down attempt leaked one. **Every timing, freeze and SIGKILL I measured between 11:42 and 12:26 was taken on a machine running one or two extra full-speed Hollow Knights**, including the win32u A/B. The sister DLLMAIN lane logged at 12:18 that "two HK processes run" while investigating its own starvation — those were mine, and I owe it that correction.
12:36 · ITER-12 · FIXED AT THE SOURCE, BY PID, NEVER BY NAME. `hk-e2e-supervise.sh` teardown now also kills the wine child that `mr-run.sh` records in `$RUNDIR/wine-child.pid`, then any `Hollow Knight.exe` that is orphaned (`ppid 1`) or descends from it. Name-matching alone is forbidden here: the sister lane runs Hollow Knight too and a `killall` would destroy its work.
12:36 · ITER-12 · ⚠ AND THE win32u VERDICT IS DOWNGRADED ACCORDINGLY. The artefact delta stays proven — removing my probe rebuilt **`5ee1ef280c8a0f35`, byte-identical** to what was deployed before I touched it, so the only difference in the failing build was mine, and dist is restored. But `Killed: 9` is also what jetsam does under memory pressure, and those three deaths happened while leaked guests were pinning the CPU. So "my probe killed HK" is **[UNCONFIRMED]**, 3-vs-1 on a machine that was never quiet. Rule carried forward: A/B win32u only on a verified-quiet machine.
12:36 · ITER-12 · ⚠ TWO OF MY OWN INTERMEDIATE READINGS WERE WRONG AND ARE RETRACTED. (1) "run 29 deadlocked in DLL detach — 6 threads, 2 in `__ulock_wait2`" — I sampled the **wrong process**: matching `winetemp` in `comm` selected a helper whose stacks are `CFRunLoopRun` + `wine_rpcrt4_server`, with no Unity/Mono/HyperBridge frames at all. (2) `macrunner-msvcrt-exit: status=0x3` at ~+64 s is that helper exiting, **not** HK — it appears in runs whose guest demonstrably kept running 90 s longer. Neither says anything about Hollow Knight.
12:36 · ITER-12 · THE STALL DETECTOR I ADDED THEN ABORTED A HEALTHY RUN, SO IT NEEDED A SECOND FIX. Run 30's guest emitted nothing for >300 s while sitting at **98.7% CPU / 2.5 GB RSS** — slow because it had spent its first five minutes sharing the CPU with the two leaked guests. `hk_e2e_key_watch.py` now aborts only on log silence **AND** `guest_busy()` false (no `Hollow Knight.exe` above 20% CPU, matched on `comm`). Silence is not death; silence with no CPU is.
12:36 · ITER-12 · RELAUNCHED AS `HK-E2E-31-CLEANMACHINE` (4 attempts) — the first run this iteration to start with `hk=0 wineserver=0` verified. Same proven arm as run 24 (`SELFINIT_DELAY_MS=300000`) plus the HyperBridge user32 message trace, `HK_SUP_HOLD_ON_MENU=1`, `HK_STALL_ABORT=600`. Report: `reports/phase4-hollow-knight/HK-E2E-ITER12-MENU-AND-KEYS-NEVER-COEXIST-20260729.md`.
12:40 · ITER-12 · ⚠⚠ AND THE SUPERVISOR WAS ADOPTING OTHER LANES' RUNS — CAUGHT AND KILLED WITHIN SECONDS. `/tmp/laneA-current-rundir.txt` is written by **every** lane's `mr-run.sh`. At 12:36:41 my freshly launched supervisor latched `laneA-hk-dllmain-i10-entryprobe-a1-try1-123641` — the sister lane's run — and ten seconds later a third lane's `laneA-AUDIOCHECK-a1-try1-123651` overwrote the file again. Scoring a foreign run would only be wrong; **combined with the guest-kill I had just added, it would have killed the pid in that lane's `wine-child.pid` and destroyed its run.** I killed my supervisor and watcher immediately, before the teardown path ran, and confirmed the sister's run untouched. Fixed: the wait loop now requires the run dir name to contain **this attempt's own tag**, and the watcher is pinned via a new `HK_RUNDIR` env instead of re-reading the shared latch on every poll.
12:40 · ITER-12 · NOTE ON EDITING A RUNNING SCRIPT: I added the guest-kill block to `hk-e2e-supervise.sh` **while it was executing**, which bash can resume mid-token. It survived, but the correct move is to stop the supervisor first; I stopped and relaunched rather than trust it.
12:41 · ITER-12 · QUEUED, NOT BARGED. `HK-E2E-32-QUEUED` launched 12:38:21 with the fixed harness; another lane holds the title slot right now (its `Hollow Knight.exe` live), so `hk-run-try12-config.sh`'s `wait_for_slot` is blocking until it finishes. That is the cooperative behaviour and I am leaving it that way rather than taking the slot.
12:41 · ITER-12 · STATE FOR THE NEXT ITERATION. Dist is on the proven artefacts: `win32u.so 5ee1ef280c8a0f35`, `winemac.so dae6b4e7f1da607c`, `ntdll.so 968d1dafcbcc3b81` (PE `ntdll.dll` was redeployed by the sister lane at 11:42 — aarch64 `11b4a6dadd94e641`, x86_64 `0497785d68320dd7` — and no run has yet reached the menu on it, so **the current build has no menu baseline**; run 24 at 11:16 is the last menu and it predates that deploy). The objective is unchanged and unmet. First thing to do next: **verify `ps -Ao comm | grep -c 'Hollow Knight.exe'` is 0 before launching**, then land a menu and key-storm it.
12:42 · ITER-12 · VERDICT: **continuing** (deliberately NOT writing a column-0 `LOOP-STATUS:` token — `lane-autoloop2.sh` anchors its grep at `^LOOP-STATUS` and a `CONTINUE` line there risks false-terminating the loop). Objective unmet: no menu was reached this iteration, no guest `WM_KEYDOWN` was captured. What this iteration bought the next one: the key chain is proven end-to-end from an old run rather than assumed broken, the menu/keys anti-correlation is named with its mechanism, and the harness no longer leaks guests, no longer adopts other lanes' runs, no longer waits 40 minutes on a dead one, and no longer aborts a live one.
12:45 · ITER-12 · ACKNOWLEDGING THE COORDINATOR'S FOLD (12:22, `LOOP-STATUS: BLOCKED` at line 288). I did not see it until the end of this iteration — it landed while I was mid-run — so everything I appended after 12:22 was written by an iteration that was already folded. **I have stood down:** `HK-E2E-32-QUEUED` supervisor and launcher are stopped, no run of mine holds or is queued for the title slot, and `ps` shows only the other lane's `Hollow Knight.exe` (pid 30695). `lane-autoloop-failover.sh hk-master` is confirmed running.
12:45 · ITER-12 · WHAT hk-master INHERITS FROM THIS ITERATION (none of it is in the 12:22 hand-over, which predates it). **Findings:** the Cocoa→winemac→win32u→wineserver key chain is proven end-to-end by run 21 (five stages, 20 Return records) so it needs no fix; menu and keys have never coexisted (4 runs vs 4, overlap 0); `ProcessEvents_drain` is `calls=1` in all 39 run dirs, so the guest's pump is effectively one-shot and injection timing is currently luck. **Harness fixes, all in place:** guest kill by `$RUNDIR/wine-child.pid` (I had leaked two 100%-CPU Hollow Knights for 44 and 16 minutes); run-dir adoption gated on the attempt's own tag plus `HK_RUNDIR` pinning (my supervisor latched two other lanes' run dirs); `run_finished()` and a CPU-gated `HK_STALL_ABORT`; `HK_SUP_HOLD_ON_MENU`; and `scripts/hk-key-storm.sh`. **Traps:** never `MACRUNNER_TRACE_UI_INPUT=1` on HK (820 MB / 80 s), never launch a run from `ctx_execute` (TMPDIR is deleted under it), never instrument `win32u/message.c` without a quiet-machine A/B. Full report: `reports/phase4-hollow-knight/HK-E2E-ITER12-MENU-AND-KEYS-NEVER-COEXIST-20260729.md`.
