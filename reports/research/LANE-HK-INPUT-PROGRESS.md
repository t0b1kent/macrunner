# LANE HK-INPUT — progress journal

08:42 · COORDINATOR — RETRACT THE PREMISE THIS LANE WAS OPENED ON. MY TEST WAS INVALID. · I opened you on "input is a proven blocker, measured by the operator". That is NOT established and I am withdrawing it before you spend another minute on it. What actually happened: I disabled `MACRUNNER_HB_START_GAME_ACTUATOR` but the LANGUAGE one-shot is a DIFFERENT knob and it was still armed — `run.log` contains `oneshot-sequence=armed`, and at seq=36/37 `macrunner-hb-language-flow: event=confirm-language method=ConfirmLanguage phase=return call=1` / `event=oneshot-sequence status=ok`. **Our own actuator pressed the button, not the operator's keyboard.** So the observation "menu did not respond to keyboard or mouse" stands as an observation, but the gate release afterwards was ours, and I cannot claim either that input works or that it does not. Zero `macdrv_key`/`macdrv_mouse` lines in the log proves nothing either — the driver may not log them at all, which is the very gate I wrote into your prompt. ★ WHAT IS STILL TRUE AND WORTH YOUR TIME: the operator pressed keys and clicked for minutes on a game that was demonstrably alive (110% CPU, menu rendered, `Loaded system language` x2) and nothing visibly responded until OUR actuator fired. That is suggestive, not proof. ★ THE CLEAN EXPERIMENT, and it is cheap: re-run with BOTH actuators off — `MACRUNNER_HB_START_GAME_ACTUATOR=0` AND whatever arms the language one-shot (find it; `oneshot-sequence=armed` is its fingerprint in run.log, and `tools/hk_language_observer/` is the source). Prefix: `artifacts/hk-prefix-template-NOLANG-inputtest` by ABSOLUTE path so the menu appears. Then the operator presses keys and the answer is unambiguous: menu selection moves = input works; nothing = input is the blocker. Before that run, do the offline half — add the counted event trace on `macdrv_key_event`/`macdrv_mouse_button` and prove it fires for a control, so the run yields evidence either way instead of another silence. The `GA_ROOT=0` early-return at `winemac.drv/window.c:812` remains the leading [HYPOTHESIS] and is worth reading now, offline, at zero cost.
08:49 · instrument+deploy winemac ui-input trace · 12 новых stage-строк byte-verified в dist-arm64ec-spike winemac.so (keyDown/postKey/key+focus events/set_focus GA_ROOT/app activate) · далее: try4 с MACRUNNER_TRACE_UI_INPUT=1 после освобождения слота (try3 до ~08:54)
08:56 · try4 armed + slot claim · try4-slotwatch.sh (10s poll) за FASTJIT-084929 (~09:29); claim записан в ACTIVE-INVESTIGATION · ждём слот; статика: клики не view-routed, Metal view не ест события
09:01 · tools ready · hk_cgevent_click (swift, --probe-only verified compiled) + hk_nsevent_inject.sh (/usr/bin/lldb, focus-state + Return injection) · try4 = try3 + TRACE_UI_INPUT (winemac.so с новыми точками — DIAGNOSTIC_ONLY контракт) · ждём слот ~09:29
10:42 · watcher v1 умер молча (exit -1, setsid отсутствует на macOS) · watcher v2 с логом запущен; слот занят BISECT-nomem-100824 (~10:48, тоже без TRACE_UI_INPUT — координатор гоняет JIT-бисекты в той же серии)
10:56 · try4 = FIREHOSE trap: MACRUNNER_TRACE_UI_INPUT включал ntdll flood (hb_run_guest_return + NtWaitForMultipleObjects per-call) → 5.9GB/4мин, ран убит · фикс: выделенный гейт MACRUNNER_TRACE_WINEMAC_INPUT во всех 8 winemac-файлах, redeploy+verified · try5 armed, watcher ждёт слот (wx-census-34 до ~11:35)
11:57 · ROOT DEFECT: NSApp в игровом процессе = stock NSApplication (delegate=nil, [NSApp run] не запущен) → sendEvent/handleEvent мертвы → ВВОД не входит в Wine вообще · Wine-сторона очереди жива (lldb keyDown→postKey→queue_post KEY_PRESS доказано), но consumer не пумпит (JIT-spin, MONO lane) · try6: thiefwatch lldb bp на +[NSApplication sharedApplication] + run_cocoa_app_entry trace
12:13 · fix готов и deployed: stock-NSApp recovery (delegate + sendEvent swizzle + [NSApp run]) + thief hook (+[NSApplication sharedApplication] first-caller stack) · try7 armed за run35A (~12:47) · журнал обновлён
12:38 · BACKEND FAILOVER · `kimi` retired after 2 consecutive returns under 180s with no journal line; continuing on `claude`.
12:55 · ★ ROOT CAUSE PROVEN ON THE LIVE PROCESS — `[NSApp run]` IS NEVER ENTERED; the 12:13 fix did NOT take. Measured on live pid 75428 (run FAST4of5-LONG-123136, winemac.so 12:12 = the fixed build, confirmed loaded via `lsof` and containing `run_cocoa_app_stock_recovery`). THREE instruments, each with a passing control:
  (1) **Main-thread stack, 758/758 samples** (`sample 75428 1`): `start → main (wine) → __wine_main (ntdll.so) → CFRunLoopRun → __CFRunLoopServiceMachPort → mach_msg`. **Zero AppKit frames.** CONTROL Safari: `SafariMain → NSApplicationMain → -[NSApplication run] → _DPSNextEvent → …`. `grep -c 'NSApplication run'` = Safari 1, HK **0**. The main thread is parked in the bare `CFRunLoopRun()` at `engine/wine/dlls/ntdll/unix/loader.c:3276` — i.e. it never became the Cocoa app.
  (2) **Accessibility window count**: game = **0**, CONTROLS Safari/Finder/Telegram/Claude = 1 each — while CGWindowList shows the game DOES own a real on-screen window (`wid=25150 "Hollow Knight" 1024x768 layer=0 alpha=1.0 onscreen=true`). AppKit publishes AX windows only for an NSApp that is running; 0-with-a-window = AppKit is not managing/pumping it.
  (3) `_NSEventThread` **is** alive in AppKit and AppKit+CoreGraphics are loaded → the WindowServer connection exists and events are being *queued*, but `-[NSApplication run]`/`nextEventMatchingMask` never dequeues them, so **`-sendEvent:` is never called**.
  ⇒ **No macOS key or mouse event can reach `macdrv_key_event`/`macdrv_mouse_button` at all.** Input is structurally impossible in this configuration — this is the blocker, and it is upstream of every Wine-side path.
  ★ **REFUTES the lane's leading hypothesis**: the `GA_ROOT(hwnd)==0` early-return at `winemac.drv/window.c:812` is NOT what kills input — events never get far enough to reach that line. (It may still bite later, once events flow; not the wall now.)
  ★ **MECHANISM, narrowed by elimination (proven):** the window exists ⇒ `macdrv_init` (macdrv_main.c:466) succeeded ⇒ `macdrv_start_cocoa_app` returned 0 ⇒ `ret = !startup_info.success` ⇒ **`success == TRUE`** ⇒ `run_cocoa_app` DID run. `[NSApp run]` is gated at `cocoa_main.m:239` by `if (created_app && startup_info->success)`. success is TRUE and `[NSApp run]` is absent ⇒ **`created_app == FALSE`**. `created_app` is FALSE only when NSApp was already non-nil AND already `isKindOfClass:[WineApplication class]` — so BOTH the `!NSApp` branch and the 12:13 stock-recovery `else if` were skipped. The 12:13 fix targeted the *stock-NSApplication* case; the live process is NOT in that case.
  ★ [HYPOTHESIS] NSApp was already a `WineApplication` when `run_cocoa_app` ran (implies an earlier invocation or another creator) — the `run_cocoa_app_entry nsapp=%p class=%s` trace line settles it in one run, and that trace is already in the deployed binary but its gate `MACRUNNER_TRACE_WINEMAC_INPUT` was OFF in try7/FAST4of5/FAST4of5-LONG (0 `macrunner-ui-input` lines; try6 at 11:56 had it ON and fired 8 stages, so the gate works).
  ★ REPORT: `reports/research/HK-INPUT-ROOT-CAUSE-NSAPP-RUN-20260728.md`
  ★ NEXT: (a) fix the `created_app &&` guard so `[NSApp run]` is entered whenever this process still needs to pump AppKit, with a static re-entry guard against a nested run loop; (b) rebuild + **deploy only after the live run ends (~14:01)** — the binary is mmap'd by pid 75428; (c) re-run with `MACRUNNER_TRACE_WINEMAC_INPUT=1` to capture `run_cocoa_app_entry` and confirm the branch. Live run left untouched.
13:00 · FIX WRITTEN + BUILT + VERIFICATION ARMED · `cocoa_main.m`: guard `created_app && success` → `success && !macrunner_nsapp_run_entered` (new static blocks a nested run loop on re-entry), so `[NSApp run]` is entered in ALL three cases (created-here / pre-existing stock / pre-existing WineApplication). SECOND defect fixed in the same place: `startup_info` points into `macdrv_start_cocoa_app`'s STACK FRAME and the unlock releases the thread owning it — reading `startup_info->success` AFTER the unlock (old line 239) is a use-after-return that decides whether input works; the decision is now captured BEFORE the unlock. Added gated `stage=run_cocoa_app_decide` trace (created_app/success/already_entered/will_run). Built clean into `build-arm64ec-spike/dlls/winemac.drv/winemac.so` sha `75938c99…`, **content-verified** (contains `run_cocoa_app_decide`); dist deliberately NOT touched — pid 75428 has it mmap'd. `run-INPUT-TEST-try8-NSAPPRUN.sh` = FAST4of5-LONG + `MACRUNNER_TRACE_WINEMAC_INPUT=1` (only new var; dedicated gate, ntdll doesn't read it → cannot repeat try4's 5.9GB flood). Detached watcher pid 85926 waits for the slot → deploys (atomic mv + adhoc re-sign + content verify) → launches → writes `NSAPPRUN-VERDICT.txt` (A: main-thread stack, B: AX window count, C: the decide line). Liveness detector validated against the live run (reported 4≠0), counts into a variable, never `ps|grep -q` under pipefail.
  ⚠ **UNTRACKED LOAD-BEARING SOURCE:** `.gitignore:60` ignores all of `engine/`, so `cocoa_main.m` (and the 7 other instrumented winemac sources) are NOT tracked — only `macdrv.h`/`window.c` were force-added. Lane forbids `git add`; **operator must force-add them** or this fix can be lost.
  ▶ **NEXT ITERATION, read this first:** watcher pid 88592 (`scratchpad/wait-deploy-launch-try8.sh`, log `try8-watcher.log`) is waiting for the slot. When it fires it writes `NSAPPRUN-VERDICT.txt` into `reports/phase4-hollow-knight/laneA-INPUT-TEST-nsapprun-try8-*/`. **Check that file first.** Read: **A** `-[NSApplication run]` frames ≥1 (was 0) · **B** AX windows ≥1 (was 0, controls 1) · **C** `stage=run_cocoa_app_decide … created_app=? success=? will_run=?` — `created_app=0 success=1 will_run=1` confirms the diagnosis exactly. If A and B flip, drive input (`tools/hk_cgevent_click`, or the operator) and watch for `event=confirm-language` in run.log — the observer is armed with `activation-control=excluded`, so it CANNOT self-fire; any hit = real input reached the game = GOAL. If A/B flip but input still dead, the next gate is already instrumented: `stage=canBecomeKeyWindow_NO` and `transformProcessToForeground` (cocoa_app.m:367/400, cocoa_window.m:1779), then the `GA_ROOT==0` early-return at window.c:812 — and GA_ROOT really is 0, confirmed live this run (`macrunner-winrealize: … root=0x0 … reason=GA_ROOT_null`).
  ⚠ Separate, unresolved: the on-screen window was still fully black (`nonblack=0`) 24:49 into the live run while DXMT frame dumps exist and a prior dump shows the menu rendering. Not an input finding.
13:05 · OFFLINE VERIFY PASS while the slot is held (live run 75428 still up, ends ~14:01; watcher 88592 parked and untouched). Nothing launched, nothing deployed, nothing rebuilt. Four results:
  ① **The 12:55 elimination chain is SOUND — the timeout escape I suspected is REFUTED.** I checked whether `macdrv_start_cocoa_app` could have hit its 5s `lockWhenCondition:beforeDate:` timeout (which would return -1 while `run_cocoa_app` still ran later against a dead frame). `macdrv_main.c:487-491`: `if (macdrv_start_cocoa_app(...)) { ERR("Failed to start Cocoa app main loop"); return STATUS_UNSUCCESSFUL; }` — and `init_user_driver()` is called only on the success path. So a window existing really does prove the call returned 0, i.e. `startup_info.success` was TRUE when the *waiting* thread read it, and `run_cocoa_app` ran within 5s. Chain holds.
  ② ★ **REFINEMENT — `created_app == FALSE` is NOT proven, and no "thief" is needed.** The 12:55 inference (`success==TRUE` ⇒ the guard's `startup_info->success` was TRUE ⇒ `created_app` must have been FALSE ⇒ someone pre-created a WineApplication) silently assumes the guard's read returns the same value the waiting thread saw. It does not — **that read IS the use-after-return** documented in the 13:00 fix. Consistent alternative, requiring no thief: `!NSApp` was true, `created_app=TRUE`, `success=TRUE`; the waiting thread woke, read TRUE from its own *live* frame (→ window created ✓), returned, and `init_user_driver()` burned stack over the popped frame; `run_cocoa_app` then drained its autoreleasepool and read `startup_info->success` from that dead frame as **0** → `[NSApp run]` skipped. This explains every measurement (window exists ✓, success TRUE at the caller ✓, zero AppKit frames ✓). Supporting evidence: `[WineApplication sharedApplication]` has **exactly one** call site in the whole tree (`cocoa_main.m:184`, inside `run_cocoa_app` itself; repo-wide grep for `sharedApplication`/`NSApplicationLoad`/`NSApp =` across `engine/**` + `tools/**` finds no other creator), so a thief would itself require a *prior* `run_cocoa_app` that also skipped `[NSApp run]` — which needs the same use-after-return. **Both stories therefore reduce to the same root cause, so the 13:00 fix is right either way.** The `decide` line discriminates: `created_app=1` ⇒ use-after-return alone (no thief); `created_app=0` ⇒ a real pre-creator exists and `stage=first_sharedApplication` names it. Fix reviewed line-by-line: all four `startup_info` derefs (`:168`, `:218`, `:233`, `:261`) are now before the unlock ✓; `[NSApp run]` is reached for all three branches ✓; the `macrunner_nsapp_run_entered` static cannot race (both invocations are main-thread serial) ✓.
  ③ ★★ **DEFECT FOUND AND FIXED IN THE TRY8 LAUNCHER — it would have repeated the exact 08:42 invalidation.** `run-INPUT-TEST-try8-NSAPPRUN.sh` still exported `MACRUNNER_HB_LANGUAGE_ONESHOT_SEQUENCE='1'`. The 13:00 entry's claim that "the observer is armed with `activation-control=excluded`, so it CANNOT self-fire" is **wrong**: `activation-control=excluded` is a descriptive **string inside the armed log banner** (`hk_language_observer.c:2570`), not a code gate. The real path is `METHOD_HIGHLIGHT_DEFAULT → invoke_oneshot_sequence()` (`:2267`), gated at `:981` by *only* `oneshot_sequence` (the env var) + a one-time CAS + a foreground-thread-identity check — i.e. armed, it calls managed `ConfirmLanguage` **itself**, which is precisely the `event=confirm-language` line the 13:00 entry designated as this run's GOAL signal. Left as-is, try8 would have "passed" on our own actuator for the second time in one day. **Set to `'0'`** (with the reasoning in-file). Verified disarming arms nothing else: `diagnostic_direct_confirm` (`:2445`) becomes *eligible* but its own env var `MACRUNNER_HB_LANGUAGE_DIAGNOSTIC_DIRECT_CONFIRM` is unset → still OFF (its body early-returns at the `!diagnostic_direct_confirm` guard); `start_game_actuator`/`newobj_probe` already off; `env_enabled()` = `value[0] && value[0] != '0'` so `'0'` is genuinely disabled. `sh -n` clean. Launcher otherwise verified: `MACRUNNER_TRACE_WINEMAC_INPUT=1` present, NOLANG prefix template by absolute path, RUNDIR pattern matches the watcher's glob. Safe to edit because the watcher launches it later — the *watcher* script was deliberately NOT touched (bash reads a running script by byte offset; editing it mid-run corrupts execution).
  ④ **The charter's "one broken hierarchy, two symptoms" is PARTLY REFUTED.** Line numbers first: the charter's `window.c:812` is now **`:825`** (`set_focus`) — instrumentation shifted them, and `:812` today is inside `is_all_the_way_front`. `set_focus` is the *only* winemac GA_ROOT site that early-returns (already instrumented, `stage=set_focus_garoot_null`). The **mouse** path does **not**: `send_mouse_input` (`mouse.c:149`) uses `top_level_hwnd` solely to fill the `update_window_zorder` server request and then sends the input regardless. So `GA_ROOT==0` **cannot** explain dead mouse input; it can explain dead *keyboard* input (focus never handed to the guest, and key events follow focus). The observed "both dead" is better explained by the `[NSApp run]` root cause, which kills both — with GA_ROOT==0 as a **second, keyboard-specific** defect queued behind it. GA_ROOT really is 0 live (`macrunner-winrealize: root=0x0 reason=GA_ROOT_null`), so expect it to bite once events flow.
13:22 · SECOND OFFLINE PASS — slot still held (live run 75428, 47 min in, ends ~14:02; watcher 88592 ALIVE and parked, untouched). Nothing launched, deployed, or rebuilt. Confirmed the watcher's artifact is still pristine: `build-arm64ec-spike/dlls/winemac.drv/winemac.so` sha `75938c99…`, mtime 12:51, `window.o` mtime 10:55 — unchanged by everything below.
  ① **Independently re-verified the armed try8 pipeline** (did not take the 13:00/13:05 entries on trust). `cocoa_main.m` read line-by-line: `should_run_nsapp` is computed at **:261**, the unlock is at **:276-279**, `[NSApp run]` at **:288** — the decision and the `decide` trace (**:263-272**) both read `startup_info->success` BEFORE the unlock, so the reported `success=` is the live value, not the dead-frame one. The three branches (`!NSApp` :182, stock-recovery :195, pre-existing WineApplication fall-through) all reach the guard. Architecture check: `run_cocoa_app` is a **CFRunLoopSource perform callback** (`source_context.perform`, :329), so `[NSApp run]` nests inside the main thread's existing `CFRunLoopRun` — which is exactly upstream Wine's design and precisely matches the observed broken stack (`__wine_main → CFRunLoopRun → mach_msg`, i.e. the source returned without entering AppKit). Launcher re-verified myself: actuator `'0'` (:108), `START_GAME_ACTUATOR='0'` (:183), NOLANG template by absolute path (:123), `MACRUNNER_TRACE_WINEMAC_INPUT=1` (:209). Watcher re-verified: bracketed-pattern liveness counted into a variable, no `pipefail`, content-verify on both build and dist, atomic same-dir `mv` + re-sign. **No defects found; left completely alone.**
  ② ★★ **GA_ROOT==0 IS NARROWER THAN THE CHARTER ASSUMED — read out of win32u, not guessed.** `NtUserGetAncestor(hwnd, GA_ROOT)` (`win32u/window.c:881`) returns 0 **only** when `list_window_parents()` (`:759`) returns NULL. Critically, a normal top-level window does **not** return 0 — `:884` (`if (!list[0] || !list[1]) ret = get_full_window_handle(hwnd)`) returns the window **ITSELF**. So `root=0x0` is never "this window has no parent"; it is a hard failure with exactly three possible causes:
   **(a)** `get_win_ptr(hwnd) == NULL` (`:770 goto empty`) — the handle is invalid *in this process's view*;
   **(b)** hwnd **IS** the desktop window (`:772-774`, the `if (!pos) goto empty`) — by design;
   **(c)** `win == WND_OTHER_PROCESS` (`:771`) → server `get_window_parents` replied `count==0` (`:803`) — the window is real but the **server's window tree disagrees**.
   Case (c) is the interesting one and has never been considered: it would mean a server-side hierarchy inconsistency, not a dead handle — and it is consistent with the window being genuinely on screen (CGWindowList `wid=25150`) while its parent walk fails.
  ③ **Instrument written to discriminate (a)/(b)/(c) — `window.c`, source only, NOT built.** New `macrunner_trace_garoot_null(site, hwnd)` logs `is_window` / `is_desktop` / `ga_parent` / `case=`. It discriminates because **`NtUserIsWindow` goes through `NtUserCallHwnd`** (`ntuser.h:1389`) — a *different* validity path from `get_win_ptr` — so `is_window=1` together with `root=0` positively identifies case (c), and a `ga_parent` that resolves while `ga_root` does not is the smoking gun (the two answers then disagree about the same handle). Wired at **both** GA_ROOT-null sites — `set_focus` and `client_surface_update` — so one run explains the keyboard and the black-frame symptom together.
  ④ **`set_focus` GA_ROOT fallback written, DEFAULT OFF** behind new `MACRUNNER_WINEMAC_GAROOT_FALLBACK` (`env && env[0] && env[0] != '0'`), so it cannot ride along silently while the NSApp fix is the variable under test. ⚠ Noted limitation, deliberately not hidden: `set_focus`'s caller at `:947` passes `info.hwndFocus`, which may be a **child** window; the fallback then sets `root = child` and `get_win_data(child)` will simply fail and return — no worse than today's early return, but it is **not** a universal fix, and the `garoot_null` trace is what tells us which case we are actually in. The `client_surface_update` site keeps its existing unconditional fallback (behaviour unchanged); only the classifier was added there.
  ⑤ **Verified by real compilation, not by eye:** extracted the exact command for `winemac.drv/window.c` from `build-arm64ec-spike/compile_commands.json`, stripped `-o …window.o`, ran `clang -fsyntax-only` with the project's true flags → **EXIT=0, 0 errors**, 15 warnings *all* pre-existing `%lx`-vs-DWORD ones in the older `macrunner-winrealize`/`macrunner-secondary` traces (grep for `garoot` in the diagnostics: **no hits**). Because `-o` was stripped, this wrote **nothing** into the build tree — re-checked `window.o`/`winemac.so` mtimes and sha afterwards. ⚠ This is a **syntax/type check, not a link** — `winemac.so` must still be genuinely rebuilt + content-verified before any run that depends on ③/④.
  ▶ **NEXT ITERATION:** ① read `NSAPPRUN-VERDICT.txt` in the newest `reports/phase4-hollow-knight/laneA-INPUT-TEST-nsapprun-try8-*` (watcher writes it ~3 min after launch). A = `-[NSApplication run]` frames ≥1, B = AX windows ≥1, C = the `decide` line. ② ALSO grep the run.log for `stage=first_sharedApplication` and `stage=winemac_so_load` — the verdict script's regex omits them and they settle ②'s thief-vs-use-after-return question in the same run. ③ If A/B flip, the input test is now UNAMBIGUOUS (no actuator can fire): operator presses a key / `tools/hk_cgevent_click`, and any `event=confirm-language` is real input = GOAL. ④ If A/B flip but keyboard stays dead while mouse works, that is exactly ④'s prediction → apply the `set_focus` GA_ROOT toplevel fallback, same shape as the proven `macdrv_client_surface_update` one (`window.c:1446-1451`), **behind a default-OFF env gate** so it stays one variable per run. Deliberately NOT built into try8's binary: the NSApp fix is the single variable under test, and rebuilding now would race the watcher's content-verified artifact. **The 13:22 pass has now WRITTEN that fallback (④) plus the case-classifier (③) into `window.c` — source only, syntax-clean, unbuilt.** ⑤ **DO NOT `make` winemac.drv until the watcher has deployed AND launched try8** (it deploys `build-arm64ec-spike/dlls/winemac.drv/winemac.so`, sha `75938c99…`; a rebuild before then silently swaps the artifact under a content-verified deploy). Once try8 is up, rebuilding is safe — the dist copy is already mmap'd and the build tree is no longer read. Then try9 = rebuild (must contain `stage=garoot_null`, verify by `strings`, not by SHA delta) + `MACRUNNER_TRACE_WINEMAC_INPUT=1`, with `MACRUNNER_WINEMAC_GAROOT_FALLBACK` left UNSET on the first pass so the classifier reports the untouched truth. ⑥ ⚠ `window.c` IS git-tracked (unlike `cocoa_main.m` — see the 13:00 untracked-source warning), so ③/④ survive; `cocoa_main.m` still does not and **still needs the operator to force-add it**.
13:27 · READINESS + ONE UNFLAGGED RISK (still offline; live run 75428 up, waiter armed).
  ⑦ **Untracked load-bearing sources are WORSE than the 13:00 note said.** Checked each file with `git ls-files --error-unmatch`: TRACKED = `window.c`, `macdrv.h` only. **UNTRACKED = `cocoa_main.m`, `event.c`, `mouse.c`, `keyboard.c`, `cocoa_app.m`, `cocoa_window.m`** — six instrumented sources, including the `[NSApp run]` fix itself and every `macrunner-ui-input` stage the verdict depends on. `.gitignore:60` ignores all of `engine/`. **OPERATOR ACTION: force-add those six** (`git add -f`) — the lane is forbidden from `git add`, and a tree clean would erase the entire day's work.
  ⑧ **Input tooling verified ready, not just present.** `tools/hk_cgevent_click --probe-only` run now: exit 0, resolved the live window (`pid=75428 window_id=25150 bounds=1024x768@y33 center=512,417`) and clicked nothing — so the moment try8 flips A/B we can drive a click without first debugging the tool. `tools/hk_nsevent_inject.sh` present + executable. ⚠ Not run against the coordinator's live run beyond the read-only probe.
  ⑨ ⚠ **RISK NOT PREVIOUSLY FLAGGED — the NSApp fix changes main-thread behaviour process-wide.** Today the main thread sits in ntdll's bare `CFRunLoopRun` (`loader.c:3276`); after the fix it enters `-[NSApplication run]` and never comes back. AppKit runs the same main run loop underneath, so run-loop sources should still be serviced — but this is the first time this process has had AppKit own the main thread, and it is the single most likely cause if try8 shows a **boot regression** rather than an input change. If try8 dies earlier than FAST4of5-LONG did, suspect this before anything else; it is not evidence against the diagnosis.
  ⑩ **This iteration deliberately did NOT:** rebuild (would swap the watcher's content-verified `75938c99…` artifact mid-flight), deploy, launch, touch the watcher script (bash reads a running script by byte offset), kill anything, or `git add`.
13:31 · THIRD OFFLINE PASS — attacked a risk nobody had tested, not a re-verify. Slot still held (live run 75428, 51 min in, ends ~14:02; watcher 88592 ALIVE, untouched). Nothing launched, deployed, rebuilt, or killed.
  ⑪ ★ **NEW RISK RAISED AND THEN REFUTED BY LIVE MEASUREMENT — activation policy is NOT a blocker.** Standing worry the lane had never tested: a macOS process that is not a *foreground* app (LSUIElement / never `TransformProcessType`d) can never take keyboard focus, so try8 could flip A/B and still show a dead keyboard. Measured read-only on live pid 75428 with `lsappinfo`: the process **is** LaunchServices-registered — `"wine" ASN:0x0-0xb10b10, type="Foreground"`, checked in 12:34:48. CONTROLS Finder/Safari = `type="Foreground"` likewise. ⇒ the app already has `NSApplicationActivationPolicyRegular`, i.e. **`transformProcessToForeground` ALREADY RAN without `[NSApp run]`** (it is driven from `cocoa_window.m:1779` on window order-front, which is Cocoa-thread work independent of the app run loop). This **eliminates** activation policy as a post-fix failure mode and independently corroborates that the Cocoa *window* machinery is healthy while only the *event pump* is dead — exactly the 12:55 diagnosis.
  ⑫ **A candidate corroborator was tested and DISCARDED before it could be believed.** The game's lsappinfo line carries `!signalled`, which Finder/Safari do not — tempting to read as "never called `-[NSApplication finishLaunching]`, hence never entered `[NSApp run]`", i.e. a fourth independent witness. **Refuted by the control set:** enumerating every registered app, `!signalled` is also carried by **TextEdit, Calendar, Universal Control, AuthenticationServicesAgent** and all the Safari/AutoCAD helpers — ordinary working AppKit apps. The flag is not diagnostic and is **not** cited as evidence. (Prove the tool before trusting the number: it failed the control, so it is out.)
  ⑬ ★ **RESIDUAL POST-FIX RISK CHAIN, read out of source — this is what to grep in try8 if A/B flip but keyboard stays dead.** `transformProcessToForeground` (`cocoa_app.m:367`) wraps its **entire** body — menu build, `setActivationPolicy`, *and* its `tryToActivateIgnoringOtherApps:` at `:403` — in `if ([NSApp activationPolicy] != Regular)`. Policy is already Regular ⇒ **every future call is a total no-op**, including the activation. So post-fix activation rests solely on the *separate* `if (activate) [controller tryToActivateIgnoringOtherApps:YES]` at `cocoa_window.m:1784-1785`; and that helper itself early-returns at `cocoa_app.m:2282` `if ([NSApp isActive]) return;`. Then the window must pass `canBecomeKeyWindow` (`cocoa_window.m:2501`), which returns **NO** on `disabled`, `noForeground`, or `isExcludedFromWindowsMenu`. All three points are already instrumented (`applicationDidBecomeActive`, `windowDidBecomeKey`, `canBecomeKeyWindow_NO`) ⇒ try8 can name the failing rung without another build.
  ⑭ **PATH CORRECTION + artifact re-verified intact.** The 13:00/13:22 entries wrote the build artifact as `build-arm64ec-spike/dlls/winemac.drv/winemac.so`; the real path is **`engine/wine/build-arm64ec-spike/…`** (the shorter one does not exist — I hit the error myself). The watcher uses the correct absolute `$ROOT/engine/wine/…` (`:14`), so nothing is broken. Enumerated every `winemac.so` on disk: build = `75938c9953cf` mtime 12:51 (**unchanged**), dist = `c09d79366a90` mtime 12:12 (the old stock-recovery build, mmap'd by the live run) — exactly the intended pre-deploy state.
  ⑮ **Watcher double-launch hazard checked and CLEAR.** `try8-watcher.log` shows *two* `watcher up` lines (12:57, 13:00), which would be a real hazard (two deploys + two launches into one slot). Counted into a variable, never `ps|grep -q`: **exactly 1** live watcher — pid 88592, `ppid=1`, 26 min. The 12:57 line belongs to the dead 85926. No hazard.
  ⑯ **try8 artifact content-verified rung-by-rung** (`strings`, not SHA): all 10 stages the verdict needs are PRESENT (`run_cocoa_app_decide`, `run_cocoa_app_entry`, `first_sharedApplication`, `winemac_so_load`, `macdrv_key_event`, `macdrv_mouse_button`, `windowDidBecomeKey`, `applicationDidBecomeActive`, `canBecomeKeyWindow_NO`, `wine_window_keyDown`), and the two 13:22 additions (`garoot_null`, `set_focus_garoot_fallback`) are correctly **ABSENT** — confirming the NSApp fix really is the single variable under test.
  ⑰ ★ **NEW TOOL, controls-validated: `tools/hk_input_ladder.sh <run-dir>`.** Reads the whole input chain out of a run.log as a 20-rung ladder (counts only, bounded output — never dumps the log), prints the observed `stage=` histogram so a confident `0` can't hide a wrong pattern, and **hard-refuses to report GOAL when `oneshot-sequence=armed`** — the 08:42 trap encoded mechanically so it cannot be repeated a third time. The controls found **three bugs in the tool itself** before any number was trusted: (1) `grep -c` prints `0` *and* exits 1, so a `|| echo 0` fallback double-printed every zero; (2) substring matching counted `stage=queue_post` = 4 by also matching `queue_post_signal_done` (truth 2) → now exact-token via `grep -o | grep -cx`; (3) try6 is only a **partial** control — its log contains **no cocoa-side stages at all**, because `run_cocoa_app_entry`/`decide` were first deployed at 12:13, *after* try6 ran at 11:56. Validation status, stated honestly: gate detection + queue rungs = **runtime-validated** (try6: 38 ui-input lines, `queue_post`=2); gate-OFF detection = **runtime-validated** (live run: 0 lines → prints `MEANINGLESS`); cocoa-side rungs = **content-validated against the try8 binary only** (⑯), never yet observed in a run.
  ⑱ ⚠ **THE LIVE COORDINATOR RUN IS INPUT-INVALID — do not cite it.** Running the new ladder on `FAST4of5-LONG-123136`: `oneshot-sequence=armed` = 1 **and** `event=confirm-language` = 2. Its language gate was released by **our own actuator** again, precisely the 08:42 trap. It is a JIT/memory-series run, not an input run, and carries zero input evidence. The interlock earned its keep on its first real use.
  ▶ **NEXT ITERATION:** ① `bash tools/hk_input_ladder.sh reports/phase4-hollow-knight/laneA-INPUT-TEST-nsapprun-try8-*` — one command now gives the full chain; then `NSAPPRUN-VERDICT.txt` for A (`-[NSApplication run]` frames ≥1, was 0) and B (AX windows ≥1, was 0). ② Expect `1b run_cocoa_app DECIDE` ≥1; `created_app=1` ⇒ use-after-return alone (no thief, per 13:05 ②), `created_app=0` ⇒ check rung `1c first_sharedApplication` for a real pre-creator. ③ If A/B flip, drive input (`tools/hk_cgevent_click`, probe-verified 13:27 ⑧, or the operator) — the ladder's GOAL line is now trustworthy because the actuator is disarmed in try8 (13:05 ③). ④ If A/B flip but keyboard stays dead, walk ⑬'s chain: rung 2 `applicationDidBecomeActive` = 0 ⇒ activation (`tryToActivateIgnoringOtherApps` early-return); rung 3b `canBecomeKeyWindow_NO` ≥1 ⇒ `disabled`/`noForeground`/`excludedFromWindowsMenu`; both fine but rung 5a = 0 ⇒ back to the Wine side and build the 13:22 GA_ROOT classifier (try9). ⑤ Build path is `engine/wine/build-arm64ec-spike/…` (⑭). ⑥ Still outstanding for the operator: force-add the **six untracked** winemac sources (13:27 ⑦) — `cocoa_main.m`, `event.c`, `mouse.c`, `keyboard.c`, `cocoa_app.m`, `cocoa_window.m`.
13:26 · COORDINATOR — THE PREMISE IS NOW CONFIRMED, NOT RETRACTED. THIS LANE IS THE PROJECT'S BLOCKER. · At 08:42 I withdrew the "input is broken" premise because our own language actuator had fired and I could not separate its effect from the operator's clicks. That confound is now gone and the result is clean. Today at 13:18 Hollow Knight reached its **MAIN MENU** — full title screen, HOLLOW KNIGHT logo, START GAME / OPTIONS / ACHIEVEMENTS / EXTRAS / QUIT GAME, version 1.5.12620, team cherry emblem — rendered in a real macOS window, run dir `laneA-INPUT-TEST-manual-language-FAST4of5-LONG-123136`, frame preserved at `reports/phase4-hollow-knight/checkpoints/20260728-MAIN-MENU-REACHED/evidence/HK-MAIN-MENU.png`. The operator then pressed keys and clicked **many times over several minutes** and the game did not respond: menu selection never moved, no scene change, and `run.log` gained no input-related line. ★ THE GAME WAS DEMONSTRABLY ALIVE THROUGHOUT: CPU 217-261%, RSS 1.4 GB, the menu scene fully loaded (`Unloading 66 unused Assets … Loaded Objects now: 47240` with an 11.5 s GC, versus 4274 objects before), `dxmt-frame-dump frame=200` and `macrunner-hb-present-surface phase=schedule result=ok ordinal=200` still cycling. So this is not a hang and not a render failure — the process renders and presents while receiving nothing. **No actuator was armed in this run**, so nothing else can explain the absence of a response. ★ WHAT THIS MEANS FOR PRIORITY: everything else we fixed today (registry W-shim, LDP-fusion stale base, the language gate) got us to a menu that cannot be used. Input is now the single thing between MacRunner and a playable title. Your original brief stands unchanged and the `GA_ROOT=0` early-return at `winemac.drv/window.c:812` remains the leading [HYPOTHESIS] — the same null-root that stopped the Metal view from being parented would also drop events, and yesterday's toplevel fallback fixed only the rendering path. ★ FIRST STEP IS STILL OFFLINE AND STILL CHEAP: instrument `macdrv_key_event` (`keyboard.c:967`) and `macdrv_mouse_button` (`event.c:572`) with a counter, prove the counter fires for a control, and only then read its silence as evidence. `not logged != did not happen` is exactly why we could not settle this in the morning. The operator is willing to test interactively and has done so twice today — a run with your counter armed plus his keypresses answers it in one slot.
13:33 · ★★ REPLY TO THE 13:26 COORDINATOR ENTRY — **your conclusion is CORRECT and now much stronger than your own argument for it, but one supporting claim is factually wrong and the "leading hypothesis" must change.** Measured, not assumed.
  ⑲ ⚠ **"No actuator was armed in this run" is FALSE for `FAST4of5-LONG-123136`** — the exact run you cite. Read verbatim out of its own `run.log`: `seq=1 event=observer-init status=armed … oneshot-sequence=armed source=accepted-145129 activation-control=excluded`, and it then ran the **full sequence**: `seq=11 phase=resolve` → `seq=13 phase=argument method=SetLanguage language_code=EN` → `seq=14 phase=invoke status=begin` → `seq=18 event=confirm-language phase=enter` → `seq=36 event=confirm-language phase=return`. So **our actuator, not the operator, released the language gate** and is why the game reached the menu. (Note `confirmedLanguage=unavailable reason=no-this` on both confirm calls — the managed call had no `this`, worth its own look later.) This is the *third* time today the actuator's state has been misread; `tools/hk_input_ladder.sh` (⑰) now reports it mechanically so it cannot recur.
  ⑳ ★ **BUT YOUR CONCLUSION SURVIVES, on a sounder footing.** An armed actuator can manufacture a false **positive** (the 08:42 trap) — it **cannot** manufacture a false **negative**. Nothing the actuator does can cause the operator's keypresses and clicks to be ignored. So "input is dead" stands as a clean observation; only the sentence "nothing else can explain the absence of a response" was doing unnecessary work. **The lane's premise is CONFIRMED.** What the actuator does explain is the *progress to the menu*, which you attributed to the gate fix.
  ㉑ ★★ **YOUR NEW EVIDENCE DISCRIMINATES BETWEEN THE TWO HYPOTHESES — AND IT REFUTES GA_ROOT AS THE PRIMARY CAUSE.** The 13:26 entry still names `GA_ROOT=0` at `window.c:812` the leading [HYPOTHESIS]. It cannot be, and the decisive fact is one **you** supplied: the operator reported **mouse dead as well as keyboard**. Verified in source myself this pass rather than taken from the 13:05 note — `send_mouse_input` (`mouse.c:143-179`): `top_level_hwnd = NtUserGetAncestor(hwnd, GA_ROOT)` at **:149** has **no early return**; the value is used *only* to fill `req->window` for `update_window_zorder`, and the function sends the input regardless. ⇒ **GA_ROOT==0 predicts "keyboard dead, mouse WORKING".** Observed: both dead. ⇒ GA_ROOT is **not** the primary blocker; it stays queued as a secondary, keyboard-specific defect (and it *is* live: `root=0x0 reason=GA_ROOT_null`). The `[NSApp run]`-never-entered root cause (12:55, three instruments each with a passing control) predicts **both** dead — it is the only candidate consistent with the operator's full observation. Your test did not just re-confirm the premise; **it selected the root cause.**
  ㉒ **YOUR "FIRST STEP" IS ALREADY DONE AND SHIPPED — the lane is further along than the 13:26 entry assumes.** You asked for a counter on `macdrv_key_event`/`macdrv_mouse_button`, proven against a control before its silence is trusted. Both stages have been instrumented since 08:49 and are **content-verified present in the try8 artifact** (`strings`, ⑯), the gate `MACRUNNER_TRACE_WINEMAC_INPUT` is runtime-proven to fire (try6: 38 `macrunner-ui-input` lines), and the **fix** for the root cause is built and staged (`75938c99…`, 12:51). Watcher pid 88592 deploys + launches try8 the moment your run frees the slot (~14:02); try8 has the actuator **disarmed** (`MACRUNNER_HB_LANGUAGE_ONESHOT_SEQUENCE='0'`, `:108`) precisely so the next confirm-language is unambiguous. **No new offline work is required from this lane before that run.**
  ㉓ **What the operator should do at ~14:02:** when try8 comes up, press keys / click the menu exactly as at 13:18. Then `bash tools/hk_input_ladder.sh reports/phase4-hollow-knight/laneA-INPUT-TEST-nsapprun-try8-*` reads the whole chain in one command, and its GOAL line is trustworthy this time because the actuator is off.
13:47 · ★★ **THE `[NSApp run]` FIX IS LIVE AND IT DID NOT WORK — measured, with controls, on a binary proven loaded.** This iteration set out to do offline prep and instead caught the fix failing in production. Also: **the run currently in the slot is NOT try8**, despite its name.
  ㉔ ★★ **VERDICT A AND B ARE UNCHANGED WITH THE FIX DEPLOYED AND LOADED. The 13:00 fix does not enter `-[NSApplication run]`.** Measured on live pid 13773 (run dir `…-nsapprun-try8-133818`, 11 min in, window on screen `wid=25271` 1024x768):
   • **Provenance first, so this cannot be a stale-binary story:** `lsof -p 13773` shows the process mapped **exactly one** winemac.so — `engine/wine/dist-arm64ec-spike/lib/wine/aarch64-unix/winemac.so`, sha `e7033303b30314b8`, and `strings` confirms it **contains `stage=run_cocoa_app_decide`** = the fixed code. The run dir has **no** `dxmt-builtin-overlay/winemac*`, so nothing shadowed it. Verified by content, not by SHA delta.
   • **A — main-thread stack, 2145/2145 samples:** `start → main (wine) → __wine_main → CFRunLoopRun → __CFRunLoopServiceMachPort → mach_msg2_trap`. **Zero AppKit frames.** `NSApplication run`=0, `_DPSNextEvent`=0, `nextEventMatchingMask`=0. CONTROL Finder: `NSApplication run`=1, `_DPSNextEvent`=1 — the instrument works.
   • **B — Accessibility window count:** game **0**, CONTROL Finder **2**, while CGWindowList shows the game's real on-screen window. Identical to the pre-fix 12:55 reading.
   ⇒ The 12:55 diagnosis (nothing pumps AppKit ⇒ `-sendEvent:` never called ⇒ no input can reach Wine) still **holds**; the *remedy* for it does not.
  ㉕ ★ **AND THE FIX IS CORRECT AS WRITTEN — so a premise of the 12:55/13:05 elimination chain is FALSE.** Re-read `cocoa_main.m:175-300` line by line this pass: there is **no early return** between `run_cocoa_app_entry` and the guard; `should_run_nsapp = startup_info->success && !macrunner_nsapp_run_entered` is computed **before** the unlock; `if (should_run_nsapp) { … [NSApp run]; }` follows. All three branches reach it. Yet the contradiction is now sharp and worth stating explicitly, because it is the next lever: `success==FALSE` ⇒ `macdrv_start_cocoa_app` returns non-zero ⇒ `macdrv_init` returns `STATUS_UNSUCCESSFUL` ⇒ **no window** — but the window exists. So `success` cannot have been FALSE, `should_run_nsapp` should have been TRUE, and `[NSApp run]` should have been entered. It was not. **[HYPOTHESIS] the remaining escape is that `run_cocoa_app` never ran at all in this process** (the CFRunLoopSource perform callback never fired, or the driver initialised down a path that does not go through `macdrv_start_cocoa_app`) — in which case the chain's step "window exists ⇒ run_cocoa_app ran" is simply wrong. **Rung `1a run_cocoa_app entry` settles this in one gated run: absent ⇒ never ran; present with `will_run=0` ⇒ read the `decide` values.**
  ㉖ ⚠⚠ **THE RUN IN THE SLOT IS NAMED "nsapprun-try8" BUT IS NOT TRY8 — do not read its result as the verdict.** Run `…-manual-language-nsapprun-try8-133818`, launched 13:38:18 by someone other than this lane's watcher. Read out of its own `run-contract.json` and `run.log`: **`MACRUNNER_TRACE_WINEMAC_INPUT` = ABSENT** (so every winemac rung is dark — 0 `macrunner-ui-input` lines, and rung `1a`/`1b` therefore **cannot** be read from it) and **`oneshot-sequence=armed` = 1** (the language actuator is ARMED). The real launcher `run-INPUT-TEST-try8-NSAPPRUN.sh` exports `MACRUNNER_TRACE_WINEMAC_INPUT='1'` (`:209`) and `MACRUNNER_HB_LANGUAGE_ONESHOT_SEQUENCE='0'` (`:108`), and names its dir `laneA-INPUT-TEST-nsapprun-try8-$STAMP` **without** `manual-language-`. ⇒ a different script copied try8's tag but not its configuration. **Its eventual `event=confirm-language` will be our own actuator for the fourth time today.** A/B above are still valid from it because they are external measurements that do not need the gate.
  ㉗ **The build artifact moved under the plan — the journal's ⑯ content-verification is STALE.** At **13:37** someone rebuilt (`window.o` mtime 13:37, so the 13:22 *uncommitted* `window.c` work linked in; `cocoa_main.o` stayed 12:51) and deployed at 13:38. Build **and** dist are now `e7033303b30314b8`, **not** the `75938c99…` recorded at 13:00/13:22. Content-verified: it carries `run_cocoa_app_decide` + `run_cocoa_app_entry` + `first_sharedApplication` **and** the 13:22 additions `garoot_null` + `set_focus_garoot_fallback`. **Behaviourally still one variable** (the classifier is trace-only; the fallback is default-OFF behind `MACRUNNER_WINEMAC_GAROOT_FALLBACK`), so try8 stays interpretable — and it now answers the GA_ROOT (a)/(b)/(c) question in the same run, which is a bonus. State it as content-changed, not as unchanged.
  ㉘ **Watcher 88592 re-verified and deliberately LEFT ALIVE.** It behaved correctly: `13:38:03 slot FREE … settling 20s` → `13:38:24 a run reappeared during settle (live=2) — keep waiting`. Its liveness detector counts into a variable and did **not** double-deploy under the new run. Its glob `laneA-INPUT-TEST-nsapprun-try8-*` is **correct** for the real launcher (I suspected a mismatch against the live dir name and disproved it — the live dir came from a different script, ㉖). It verifies the deploy by **content** (`strings … | grep -c "$MARKER"`), not by a hard-coded SHA, so the artifact swap in ㉗ does not block it. It will deploy + launch the real try8 when this slot frees.
  ㉙ ★ **`tools/hk_input_ladder.sh` UPGRADED — it could not read the pump, and could not tell "didn't fire" from "not instrumented".** Two additions, both in lane territory (`tools/**`):
   **(1) Pump rungs 5e-5h.** `macdrv_ProcessEvents` is the **only** bridge from the Cocoa queue into Wine's message system, and win32u calls it from exactly one site (`process_driver_events`, `message.c:3431`) which runs only inside `wait_message`/`peek_message` — i.e. **only when the guest pumps**. Its four traces were already compiled in and **nothing was reading them**. New `--- PUMP adjudication ---` block gives a three-way verdict: `5e=0` ⇒ guest never pumped (upstream of every Wine-side rung); `5e>0, 5f=0` ⇒ guest pumps but the Cocoa queue was empty; `5f>0` ⇒ events cross into Wine, look downstream.
   **(2) `n/a` vs `0`.** Each rung's stage token is now looked up in a **reference binary** (default: deployed winemac.so, override `--binary <path>`); a rung with no hits prints `0` only if the trace is **proven present** in that binary, otherwise `n/a  <-- trace NOT in binary: silence is MEANINGLESS`. Plus a banner warning when the reference is newer than the log. This is the `not logged != did not happen` gate encoded mechanically, like the actuator interlock in ⑰.
  ㉚ **Controls run BEFORE trusting the tool — and one of them found a real defect in my own tool.** (C0) `sh -n`/`bash -n` clean. (C1) try6 regression: `queue_post` still **2** = the known truth from ⑰, and the newer-reference banner fires. (C2) `n/a` path exercised against a genuinely different reference (`engine/wine/dist/…/winemac.so`, Jul-6, 37 stage tokens): `run_cocoa_app_decide` → `n/a` ✓ (absent there), `queue_post` → still 2 ✓ (a hit short-circuits the binary check). **(C2 also FAILED the tool:** it printed a loud `*** GUEST NEVER PUMPED ***` from a mismatched reference, without restating the provenance caveat printed 40 lines above — precisely the false-confidence class this lane keeps getting burned by. **Fixed:** the strong verdict is now suppressed to `UNDETERMINED` whenever the reference is newer than the log. (C3) re-run confirms the suppression fires; (C4) `queue_post`=2 unregressed.
  ㉛ **[HYPOTHESIS] the guest may never pump — which would mean `[NSApp run]` is necessary but NOT sufficient.** try6 (gate ON, 38 `macrunner-ui-input` lines, lldb-injected events proven reaching `queue_post`=2) contains **zero** `ProcessEvents_*` tokens. I cannot content-verify try6's 11:56 binary (it was overwritten), so this is **not** proof — but the trace exists in **both** a Jul-6 build and today's, so its absence from that generation is unlikely. If true it independently corroborates the 11:57 "consumer doesn't pump" note and predicts that fixing `[NSApp run]` alone still yields dead input. **Rung 5e in try8 decides it**, and the ladder will now report it.
  ▶ **NEXT ITERATION:** ① The blocker is no longer "is the diagnosis right" but **"why does the corrected guard still not run"** — and only a **gated** run answers it. Wait for the real try8 (watcher 88592 will launch it), then `bash tools/hk_input_ladder.sh <try8-dir>` and read **rung 1a first**: absent ⇒ `run_cocoa_app` never ran and ㉕'s premise-failure is confirmed; present ⇒ read `1b`'s `created_app/success/already_entered/will_run` verbatim. ② Then rung **5e** for ㉛. ③ Do **not** cite `…-nsapprun-try8-133818` for anything except A/B (㉖). ④ Artifact is `e7033303`, not `75938c99` (㉗). ⑤ Still outstanding for the **operator**: force-add the six untracked winemac sources (13:27 ⑦) — `cocoa_main.m`, `event.c`, `mouse.c`, `keyboard.c`, `cocoa_app.m`, `cocoa_window.m`; `.gitignore:60` ignores all of `engine/`, and the `[NSApp run]` fix itself lives in one of them. ⚠ Checked this pass: **`tools/hk_input_ladder.sh` and this journal are ALSO untracked** (`git status` reports both `??`) — add them to the same force-add list, or the lane's only input instrument and its entire record are one `git clean` from gone.

14:05 · ★★★ **㉕'s ESCAPE HATCH IS REFUTED FROM SOURCE: `run_cocoa_app` DID run. `[NSApp run]` WAS entered — and it RETURNED. That edge had NO trace, which is why it was invisible.** No run needed; this is forced by code the lane had not yet read.
  ㉜ ★★ **THE DEDUCTION, and every step is source-verified this pass.** ㉕ proposed "the remaining escape is that `run_cocoa_app` never ran at all" and queued a gated run to settle it. It cannot be that, and `macdrv_main.c:487-494` is the reason — it is stock, unpatched:
   `if (macdrv_start_cocoa_app(NtGetTickCount())) { ERR(...); return STATUS_UNSUCCESSFUL; }` and only then `init_user_driver();`.
   • A non-zero return ⇒ **`init_user_driver()` is never called** ⇒ macdrv is not the user driver ⇒ **no Mac window at all**. The window exists (`wid=25271`, CGWindowList, `macdrv_client_surface_create` ran). ⇒ **`macdrv_start_cocoa_app` returned 0.**
   • `ret = !startup_info.success` is only reached inside `lockWhenCondition:COCOA_APP_RUNNING` (`cocoa_main.m:338-342`), and that condition is set **only** by `run_cocoa_app` at `:278`. ⇒ **`run_cocoa_app` ran**, and **`success == TRUE`**.
   • Every branch in `run_cocoa_app` sets `success=TRUE` (`:220` stock-recovery, `:234` `setWineController:`), and `macrunner_nsapp_run_entered` is FALSE on the only invocation. ⇒ `should_run_nsapp = TRUE` ⇒ **`[NSApp run]` was entered.**
   • The `sample` stack is `__wine_main → CFRunLoopRun` = ntdll `apple_main_thread`'s own loop (`loader.c:3245-3277`), **not** inside a `__CFRunLoopDoSources0 → run_cocoa_app` frame. ⇒ the perform callback **completed**. ⇒ **`[NSApp run]` returned.**
  ㉝ ★ **AND NOTHING IN THE DRIVER ASKS IT TO STOP.** Grepped all of winemac.drv: **zero** `[NSApp stop:]` / `stopModal` / `abortModal`. The only quit path is `[NSApp terminate:NSApp]` (`cocoa_app.m:2354`), which would end the process — it is alive at 191% CPU. So the return is **spurious**, not a requested shutdown. Why it returns is still open; `ran_ms` in the new trace discriminates (≈0 ⇒ NSApp was already in a bad state before `run`; large ⇒ something stopped the loop mid-flight).
  ㉞ ★★ **THE EDGE WAS UNINSTRUMENTED — this is the lane's own `not logged != did not happen` gate, and we were on the wrong side of it.** Upstream annotates the call `/* Never returns */` and the code trusted that comment; `grep -c nsapp_run_returned cocoa_main.m` = **0**. Every rung the ladder measured was *downstream* of an edge nothing was watching. Fixed:
   **(1) `stage=nsapp_run_returned`** immediately after `[NSApp run]`, logging `isrunning`, `ran_ms`, `reentry`, `fast_returns`.
   **(2) Bounded re-entry, default ON** (`MACRUNNER_WINEMAC_NSAPP_REENTER=0` restores old behaviour for an A/B). One spurious return currently kills input for the life of the process; re-entering is the same semantic the 13:00 fix already states ("what matters is whether this process still owes AppKit an event loop"). Bounded so an instantly-returning loop cannot hot-spin: 8 consecutive returns under 100 ms, or 1024 total, ⇒ `stage=nsapp_run_reenter_giving_up`.
   **(3) `run_cocoa_app_entry` / `_decide` / `nsapp_run_returned` are now UNGATED.** Three lines, once per process. ㉖ wasted an entire 90-minute run because it was launched without `MACRUNNER_TRACE_WINEMAC_INPUT` and every rung went dark; that failure mode is now structurally impossible for the rungs that matter most.
  ㉟ **Build `45e50a68` — content-verified, and deliberately NOT deployed.** `make dlls/winemac.drv/winemac.so` clean, zero warnings. `strings` confirms all six new tokens present **and** the watcher's marker `run_cocoa_app_decide` still present (so watcher 88592's content check will pass). **`dist` deliberately left at `e7033303`** — a run is live (pid 13773, 22 min into a 5400 s budget); nothing was deployed under it. Watcher 88592 (alive, 59 min) deploys `45e50a68` and launches the real try8 when the slot frees, so **try8 now answers 1a/1b AND the new edge in one run** instead of costing a second 90-minute cycle.
  ㊱ **`tools/hk_input_ladder.sh`: rungs `1e/1f/1g` + an `[NSApp run] adjudication` block — and the controls caught a real defect in my own block.** C0 `sh -n`/`bash -n` clean. C1 try6 regression: `queue_post` still **2**, and 1e correctly reads `n/a` against the still-deployed old binary. **C2 FAILED the tool exactly as ㉚ predicted it would**: pointed at the *new* reference binary, it asserted `run_cocoa_app NEVER RAN` for a try6 log recorded two hours *before* that binary existed. **Fixed** — the `1a=0` verdict is now suppressed to `UNDETERMINED` whenever the reference is newer than the log, matching the pump block's guard. C3 confirms the suppression fires; C4 `queue_post`=2 unregressed; **C5 positive control** (synthetic fixture, result written down in advance) returns `1e=1` and `*** CONFIRMED ***`. The tool is now proven in **both** directions before it is trusted on a real run.
  ㊲ ★ **PRE-REGISTERED PREDICTIONS FOR TRY8 — written before the run, so it can falsify me.** ① rung `1a` **PRESENT**. ② rung `1b` = `success=1 already_entered=0 will_run=1`. ③ rung `1e` **PRESENT, ≥1** — this is the whole deduction. ④ If `1e` is absent *and* `sample` still shows zero AppKit frames, **㉜ is wrong** and the driver-init chain must be re-read from `init_user_driver` outward. ⑤ If re-entry sticks (one `nsapp_run_returned`, then AppKit frames appear and AX windows ≥ 1), **input may simply work** — that is the GOAL condition, and it needs the operator at the keyboard.
  ▶ **NEXT ITERATION:** ① Do **not** deploy or launch by hand — watcher 88592 owns the slot handoff and its liveness check counts into a variable (no `grep -q`/pipefail misfire). ② When try8 lands: `bash tools/hk_input_ladder.sh <try8-dir>` and read the **`[NSApp run] adjudication`** block first, then `1e`'s verbatim `ran_ms`. ③ If `1e` fires and re-entry holds, ask the operator to press keys — that is the GOAL test. ④ If re-entry gives up, `ran_ms≈0` says NSApp was already unusable before `run`, and the next lever is *why* (rung `1c first_sharedApplication` names the thief and whether it ran off the main thread). ⑤ Artifact is now **`45e50a68`**, not `e7033303` (㉟). ⑥ Still outstanding for the **operator**: force-add the untracked load-bearing files — `cocoa_main.m`, `event.c`, `mouse.c`, `keyboard.c`, `cocoa_app.m`, `cocoa_window.m`, plus `tools/hk_input_ladder.sh` and this journal.
14:24 · COORDINATOR — try8 RESULT: INPUT STILL DEAD, BUT YOUR FIX WAS NEVER EXERCISED — `run_cocoa_app` IS NOT CALLED AT ALL · Run `laneA-INPUT-TEST-manual-language-nsapprun-try8-133818`, built and verified BY CONTENT before launch: `winemac.so` c09d7936 -> **e7033303**, `strings` confirms `stage=run_cocoa_app_decide` present (the 12:12 artifact lacked it — the source edit landed 12:50, so the previous binary was stale and would have proved nothing). `MACRUNNER_WINEMAC_NSAPP_REENTER` defaults ON, so the fix was active. `START_GAME_ACTUATOR=0`. Operator pressed keys and clicked at the menu. ★ RESULT: no response — `unity-selection HighlightDefault` x3 only (the menu's own default highlight), `Loaded Objects now: 47240` unchanged, zero markers past the menu, run.log flat, CPU 272%. Same as 13:18. ★ BUT THE INTERESTING PART IS WHY: **`run_cocoa_app` never appears in the log — not once.** That line is marked "UNGATED on purpose" in your own code, and the stream is not the problem: `toplevel_fallback` fires twice in the same run and it prints from `window.c` in the SAME `winemac.so`, so unix-side stderr reaches run.log fine. Counts for the whole run: `toplevel_fallback` 2, `GA_ROOT` 2, `client_surface` 2, `macdrv` 3, **`run_cocoa_app` 0**. ★ SO THE PREMISE NEEDS REVISING, AND THIS IS GOOD NEWS FOR YOUR SEARCH: your fix targets "NSApp was pre-created as a stock NSApplication before run_cocoa_app got to run" — i.e. it assumes `run_cocoa_app` eventually runs and must not start a nested loop. Our case is harder: **that function is not in the chain at all.** The driver is demonstrably alive — it creates the window, parents the Metal view via yesterday's toplevel fallback, and renders — but the process never enters the Cocoa application main loop, so nothing pumps AppKit, `-sendEvent:` is never called, and both keyboard and mouse are dead for identical reasons. That matches every observation we have, including the operator's report that the two die together. ★ THE QUESTION IS NOW NARROWER: who is supposed to call `run_cocoa_app`, on which thread, and what skips it in this configuration? Worth checking whether the game process reaches `macdrv` process-attach at all, whether the main thread is claimed by something else before the driver initialises, and whether a headless/no-tty launch path bypasses it. ★ MY ERROR TO NOT REPEAT: I set `MACRUNNER_HB_UI_INPUT_TRACE=1`, which does not exist — the real gates are `MACRUNNER_TRACE_WINEMAC_INPUT` / `MACRUNNER_TRACE_UI_INPUT`. The ungated `run_cocoa_app_decide` line makes the conclusion above safe regardless, but the next run should set the real variable so the gated stages are visible too. Operator is willing to test again and has now done so three times today.

14:25 · ★★★ **㉜ IS REFUTED BY DIRECT MEASUREMENT ON THE LIVE PROCESS. `[NSApp run]` was never entered — because `macdrv_init` never ran. The queued try8 fix targets an edge that cannot execute.** This iteration stopped deducing and read the live process with lldb (controls first). Nothing was launched; the slot was never touched.
  ㊳ ★★ **CONTROL FIRST, and it caught a real reading subtlety.** Wrote `scratchpad/nsapp_control.m` — a Cocoa app whose NSApp is a subclass named `MRControlApplication`, deliberately non-obvious so a probe that merely prints the generic "NSApplication" FAILS instead of accidentally passing. Predictions written into the source BEFORE running. Probe returned `isRunning=1`, delegate `MRDelegate`, and class **`NSKVONotifying_MRControlApplication`** — KVO renames the dynamic class, so class identity must be matched by SUBSTRING, not equality. The probe is proven in both directions before it was pointed at the game.
  ㊴ ★★★ **PROVENANCE GATE PASSED — the reads are against the binary the process actually maps.** lldb resolves symbol addresses from the on-disk file; the dist was written at 13:38, the same minute the run started, so a stale-file/mapped-inode mismatch would have silently produced all-zero reads. `otool -l` on-disk UUID = **`F7D17437-2A7B-3C32-8164-E155BF082335`** = the UUID lldb reports for the mapped image. Identical ⇒ addresses valid. (Build 45e50a68 UUID `4ADC038A…` differs, as it must.) Exactly **one** winemac.so image is loaded (`image list`), and `class_getImageName` puts both `WineWindow` and `WineApplication` in it — so there is no second copy of the statics.
  ㊵ ★★★★ **MEASURED ON LIVE HK pid 13773** (run `…-nsapprun-try8-133818`, dist e7033303, 42 min in):
   • `object_getClassName(NSApp)` = **`NSApplication`** — plain stock, not KVO-wrapped; `isKindOfClass:WineApplication` = **0**.
   • `[NSApp delegate]` = **nil**, `isRunning` = **0**, `isActive` = **0**, `keyWindow` = **nil**.
   • `macrunner_nsapp_run_entered` = **0**, `macrunner_orig_sendEvent` = **NULL**, `macrunner_orig_sharedApplication` = **NULL**.
   • `app_icon_callback` / `app_quit_request_callback` / `regcreateopenkeyexa_callback` / `regqueryvalueexa_callback` = **all NULL**; `macdrv_err_on` = 0.
   • `CFRunLoopGetMain()` full description: 10 common-mode items, and **`run_cocoa_app` appears 0 times** — the only winemac source that should be there is absent. ntdll's `apple_create_wine_thread` source IS present, so this is the right run loop.
   • main-thread bt: `dyld start → wine main → ntdll __wine_main → CFRunLoopRun → __CFRunLoopServiceMachPort → mach_msg2_trap`. Zero AppKit, no `run_cocoa_app` frame.
  ㊶ ★★★ **THE CHAIN, and each link is independent of the others.** `run_cocoa_app` cannot have run: whichever branch it takes it either creates a `WineApplication` or sets the delegate (`cocoa_main.m:243`), and NSApp is stock with a nil delegate. `macdrv_start_cocoa_app` cannot have run: it does `CFRunLoopAddSource(CFRunLoopGetMain(), …, kCFRunLoopCommonModes)`, **nothing ever removes that source**, and it is not in the loop. `macdrv_init` cannot have run: its FIRST statement is `app_icon_callback = params->app_icon_callback`, and `dllmain.c:486` always passes `(UINT_PTR)macdrv_app_icon` — a real function pointer, never NULL. ⇒ **the unix-side driver init never executed, so the Cocoa application half of winemac.drv was never started, and no macOS key or mouse event can reach `macdrv_key_event`/`macdrv_mouse_button`.**
  ㊷ ⚠ **SELF-CORRECTION, logged so it is not re-derived:** my first pass treated `app_icon_callback == NULL` as proof on its own. It is not — those statics are copied from `params`, so NULL is also what a caller passing NULL would produce. The claim rests on `dllmain.c:486`, which passes a real pointer. The run-loop and delegate evidence are independent of it and would stand alone.
  ㊸ ★★ **CONSEQUENCE — THE ARMED FIX IS AIMED AT AN UNREACHABLE EDGE.** 45e50a68's bounded `[NSApp run]` re-entry sits inside `if (should_run_nsapp)`, reached only from `run_cocoa_app`, which never runs. **Pre-registered: try8 will show rungs `1a`, `1b`, `1e` all at 0.** The re-entry loop is harmless and worth keeping (it becomes live the moment init is fixed), but it cannot restore input by itself. ㉞'s instrumentation was correct and necessary — it just watched the second-deepest edge, not the deepest.
  ㊹ ⚠ **ONE MEASUREMENT STILL CONTRADICTS THE CHAIN AND I AM NOT PAPERING OVER IT.** `[[NSApp windows] objectAtIndex:0]` is a real **`WineWindow`**, `windowNumber 25271`, `isVisible=1`, `canBecomeKeyWindow=1`, its own delegate. `window.c:620` is the ONLY site that builds one, and it is reachable only through the user-driver vtable, installed only by `init_user_driver()` — which runs only at the end of `macdrv_init`. So either the window was created by a path nobody has mapped, or one of the two measurements is wrong. **This is the fork try8 must settle, and it is now instrumented rather than argued.**
  ㊺ **INSTRUMENTED THE DECIDING EDGE (unix side only, on purpose).** `macdrv_main.c` `macdrv_init`: `stage=macdrv_init_entry` (+`icon_cb`), `…_session` (SessionGetInfo status/attrs), `…_no_graphic_access`, `…_start_cocoa_call`, `…_start_cocoa_ret rc=`, `…_user_driver_set`. `window.c:620`: `stage=create_cocoa_window` (first 4 windows, bounded). All UNGATED — same reasoning as ㉞(3). **Deliberately NOT `dllmain.c`:** that is the PE side and ships as `winemac.drv`, which watcher 88592 does **not** deploy — a trace there would silently never reach the run.
  ㊻ **Artifact is now `fd29b8ef` (UUID `3B30907E-9A65-39E7-BF4A-E300C365B723`), NOT 45e50a68.** `make dlls/winemac.drv/winemac.so` RC=0, no warnings on the new lines (the remaining `%lx` warnings are pre-existing at window.c:613/698/704/783). Content-verified by `strings`: all 7 new stages present **and** the watcher's gate marker `run_cocoa_app_decide` still present, so watcher 88592's check passes and it will deploy this build. dist deliberately left at e7033303 — a run is live and nothing was deployed under it.
  ㊼ **`tools/hk_input_ladder.sh`: new rungs `0a–0f`, controls run in BOTH directions.** `bash -n` clean. Against the still-deployed e7033303 the new rungs correctly read `n/a  <-- trace NOT in binary` (silence meaningless); against `--binary` fd29b8ef they read `0` (instrumented, absent from that old log). So the tool cannot confuse "not built" with "did not fire" for the rungs that now matter most.
  ㊽ **The run in the slot is confirmed worthless for input** — ladder: GATE `macrunner-ui-input` = **0** lines, and GOAL adjudication `*** INVALID FOR INPUT ***` (`oneshot-sequence=armed`=1, `confirm-language`=2). ㉖ was right. It ends ~15:08 on its 5400 s budget; it was NOT killed (charter: one title run at a time, never pkill).
  ㊾ **try8 launcher re-verified line by line** (it is what the watcher execs): `MACRUNNER_HB_START_GAME_ACTUATOR=0`, `…_MAX=0`, `MACRUNNER_HB_LANGUAGE_ONESHOT_SEQUENCE=0`, `MACRUNNER_TRACE_WINEMAC_INPUT=1`, and `MACRUNNER_MR_RUN_PREFIX_TEMPLATE` = the **absolute** `artifacts/hk-prefix-template-NOLANG-inputtest`. The 08:42 trap cannot repeat in try8.
  ㊿ ⚠ **Watcher defect found, deliberately NOT fixed — do not edit that script.** `wait-deploy-launch-try8.sh:116` globs `laneA-INPUT-TEST-nsapprun-try8-*` but the launcher creates `laneA-INPUT-TEST-nsapprun-try8-$STAMP` (line 9) — that matches, so RUNDIR resolves. However its section C greps only `run_cocoa_app_(entry|decide|created|stock_recovery)`, so the auto-verdict will NOT show the new init rungs. Harmless: run the ladder by hand instead. **Editing a live bash script is unsafe** (bash re-reads by byte offset; the watcher is parked mid-loop), which is why it was left alone — exactly as ⑩ decided.
  ▶ **NEXT ITERATION:** ① Do **not** launch or deploy by hand — watcher 88592 (alive 1h20m) owns the handoff and deploys `fd29b8ef` when the slot frees ~15:08. ② When try8 lands: `bash tools/hk_input_ladder.sh <try8-dir>` and read **rungs 0a–0f FIRST** — they are upstream of everything else. ③ The fork: `0a=0` ⇒ the unix init call never arrives, so the next lever is the PE side (`dllmain.c:481` `__wine_init_unix_call()` / `:492 MACDRV_CALL(init)`) and its x86_64→ARM64EC unix-call routing, incl. `wow64_init`'s `wow64_translate_strings` NULL return at `macdrv_main.c:741`. `0a=1` but `0c` fires ⇒ `SessionGetInfo`/`sessionHasGraphicAccess` denies graphic access to the process — a session/entitlement problem, not a Wine one. `0a=1, 0d2 rc!=0` ⇒ back to the 5 s `lockWhenCondition` timeout and ㉜'s territory. ④ If `0f>0` while `0a=0`, ㊹'s contradiction is real and the window path must be traced from `init_user_driver` outward. ⑤ Do NOT ask the operator to press keys yet — input is structurally impossible until the init chain is repaired; a manual test now can only produce another false negative. ⑥ Still outstanding for the **operator**: force-add the untracked load-bearing winemac sources (`.gitignore:60` ignores all of `engine/`) — `cocoa_main.m`, `macdrv_main.c`, `window.c`, `event.c`, `mouse.c`, `keyboard.c`, `cocoa_app.m`, `cocoa_window.m`, plus `tools/hk_input_ladder.sh` and this journal.
  ⓪ ★ **CORRECTION + STRENGTHENING of ㊵/㊶, same iteration (14:32).** ㊵ said "the only winemac source that should be there is absent"; that phrasing was too strong and is corrected here. The main run loop **does** carry winemac.so sources: `callout = PerformRequest (0x11c8d4778)` ×3, and `nm` puts `_PerformRequest` at exactly offset **0x4778** in winemac.so — Wine's `OnMainThread`/`OnMainThreadAsync` request source (`cocoa_app.m`). So winemac.so demonstrably **can and does** add sources to this exact `CFRunLoopGetMain()`. That makes the conclusion **stronger**, not weaker: the absence of `run_cocoa_app` (winemac.so+0x1160c) cannot be explained away as "the source went to a different run loop". Also checked: all sources — winemac's 3 and ntdll's 2 — read `signalled = No, valid = Yes`, so nothing is stuck waiting on a starved main thread; the loop is healthy and being serviced. ⇒ if `macdrv_start_cocoa_app` had ever run, its source would be registered here and would have been performed.

14:40 · ★★★★ **ROOT CAUSE FOUND, AND IT IS UPSTREAM OF EVERYTHING THIS LANE HAS TOUCHED: win32u is running on the NULL USER DRIVER. `winemac.drv` (the PE) is never loaded into the process, so `macdrv_init` never runs, so there is no Cocoa app, no event pump, and input is structurally impossible.** Measured on the REAL try8 (`laneA-INPUT-TEST-nsapprun-try8-143122`, watcher 88592 deployed + launched it at 14:31:22 while the previous slot-holder ended), plus two independent measurements and a fully source-verified chain. ㊸'s pre-registration is CONFIRMED and ㊹'s contradiction is RESOLVED.
  ① ★★ **RUNG 0a IS ZERO, WITH A PASSING CONTROL IN THE SAME STREAM.** try8 is the first run with the gate actually on and the artifact content-verified (dist `c934cd07` = build `fd29b8ef` + adhoc signature; `strings` confirms all 10 stage tokens present, incl. `macdrv_init_entry`). Counts: `macdrv_init_entry` **0**, `macdrv_init_session` **0**, `macdrv_init_no_graphic_access` **0**, `macdrv_init_start_cocoa_call` **0**, `macdrv_init_start_cocoa_ret` **0**, `macdrv_init_user_driver_set` **0**, `run_cocoa_app_entry` **0**, `run_cocoa_app_decide` **0**, `nsapp_run_returned` **0**. **CONTROL:** the same run carries **43** `macrunner-ui-input` lines from the same `fprintf(stderr)` mechanism in the same `winemac.so` (`winemac_so_load`, `queue_create`, `queue_post`×2, `onmainthread_*`, `set_queue_display_fd`, `macdrv_init_thread_data`, `create_cocoa_window`, `garoot_null`×2, `first_sharedApplication`). So the silence at 0a is REAL, not a dark gate — `not logged != did not happen` is satisfied in the right direction for the first time.
  ② ★★ **INDEPENDENT CONFIRMATION, NO lldb, NO INSTRUMENT: the PE is not mapped.** `lsof -p 43099` lists `aarch64-unix/win32u.so`, `x86_64-windows/win32u.dll`, `aarch64-unix/winemac.so` and the DXMT overlay's `winemetal.so`/`winemetal.dll` — and **no `winemac.drv` at all**, of any architecture. CONTROL: the same `lsof` lists **23** dist images, so it sees PE modules fine (it found `win32u.dll`). ⇒ winemac.drv's `DllMain` never ran ⇒ `dllmain.c:492 MACDRV_CALL(init, &params)` was never issued. This is a third, method-independent witness for ①, and it does not depend on any static read (so ㊷'s caveat does not apply).
  ③ ★★★ **THE CHAIN, every link read out of source this pass.** `win32u/driver.c:1031` — `if (is_service_process() || !load_desktop_driver( get_desktop_window() ) || user_driver == &lazy_load_driver)` → `:1038 __wine_set_user_driver( &null_user_driver, … )`. `load_desktop_driver` (`:943`) opens `System\CurrentControlSet\Control\Video\{GUID}\0000` (`:982`) and reads `GraphicsDriver` (`:984`); only then does it do `KeUserModeCallback( NtUserLoadDriver, … )` (`:993`), which is the ONLY thing that ever loads the winemac.drv PE. **MEASURED on the live prefix `artifacts/_mr-run.VRGrfo`: `system.reg` contains ZERO `Control\Video` keys** — so `reg_open_key` at `:982` returns NULL and the function returns FALSE before it even looks for the value. The value is written by **explorer.exe** and by nothing else (`programs/explorer/desktop.c:998` builds that exact key, `:1063 RegSetValueExW(… "GraphicsDriver" …)`; repo-wide, `driver.c:984` is the only reader and `desktop.c:1063` the only writer). **MEASURED: no wine `explorer.exe` process is running.** ⇒ no explorer ⇒ no `Control\Video` ⇒ `load_desktop_driver` FALSE ⇒ **null user driver** ⇒ winemac.drv PE never loaded ⇒ `macdrv_init` never runs ⇒ `macdrv_start_cocoa_app` never runs ⇒ no `WineApplication`, no `[NSApp run]`, no `-sendEvent:` ⇒ **no key or mouse event can reach `macdrv_key_event`/`macdrv_mouse_button`.** Keyboard and mouse die together, for one reason — exactly the operator's observation.
  ④ ★★★ **㊹'s CONTRADICTION IS RESOLVED — a real WineWindow CAN exist with `macdrv_init` never having run, and here is the path.** `create_cocoa_window` (`window.c:580`) is called at **`window.c:741`**, inside the MacRunner `d3d_on_demand` realize path (`:781/:791/:806`), whose entry point is `macdrv_client_surface_create` (`window.c:1576`) — and that is called **directly** from **`d3dmetal.c:112`**, an ordinary intra-`winemac.so` function call from the DXMT/Metal side, **not** through the user-driver vtable. Repo-wide, `macdrv_client_surface_create` has exactly four callers (`vulkan.c:54`, `opengl.c:1479`, `d3dmetal.c:112`, and its own recursive use at `:1591`) — none of them needs `init_user_driver()`. **Measured confirmation in the same run:** `stage=create_cocoa_window pid=43099 hwnd=0x2002e cocoa=0x710e5bc000` fires **once** while every `macdrv_init` rung is **zero**. So winemac.so's unix half is being used **piecemeal by the graphics path while the driver was never initialised** — the game gets a real, visible, rendering NSWindow and no input machinery whatsoever. **㊹ was a real contradiction and it is now closed by measurement, not by argument.**
  ⑤ ★★ **THE "THIEF" IS REAL, IS NAMED, AND CAME WITH A STACK TRACE — but it is not a rival `run_cocoa_app`.** try8's log carries `stage=first_sharedApplication caller_class=NSApplication app=0x9bdb3d800 appclass=NSApplication main_thread=1` **and** the hook's backtrace: `macrunner_sharedApplication_hook ← -[WineContentView initWithFrame:] ← __macdrv_create_view_block_invoke ← __OnMainThread_block_invoke ← PerformRequest`. ⇒ **AppKit lazily instantiates the stock `NSApplication` when our Metal content view is created** (`macdrv_create_view` → `WineContentView`), on the main thread, long before anything would have created a `WineApplication`. This **refutes 13:05②'s "no thief is needed"** (a pre-creator does exist) while leaving its conclusion intact for the wrong reason: the pre-creator is AppKit itself, reached through the same graphics-only path as ④. It also explains ⓪'s `PerformRequest` sources on the main run loop — `[WineApplicationController sharedController]` has 30 call sites in `cocoa_window.m` alone, so the controller (and its request source) is created lazily by the graphics path, with `run_cocoa_app` never involved.
  ⑥ ★★ **GA_ROOT: the classifier fired and named case (c) — the one 13:22② flagged as never considered.** `stage=garoot_null site=client_surface_update hwnd=0x2002e is_window=1 is_desktop=0 ga_parent=0x10020 desktop=0x10020 case=c_server_no_parents` (×2). So the handle is valid in this process (`is_window=1`), it is not the desktop, `GA_PARENT` resolves to the desktop — and the **server's `get_window_parents` still replies count==0**. That is not a coincidence: with the null driver, `nodrv_CreateWindow` (`driver.c:763-775`) **returns FALSE** for any window with a real parent, so the window is never properly realised on the driver/server side. ⇒ **`GA_ROOT==0` is a SYMPTOM of the null driver, not an independent defect.** The charter's "one broken window hierarchy, two symptoms" instinct was RIGHT; the hierarchy is broken because there is no graphics driver at all. Yesterday's black frame — the Metal view never parented because `GA_ROOT` was 0 — has the same single cause, and the 19-line toplevel fallback was compensating for it downstream.
  ⑦ ⚠ **CONSEQUENCE FOR TODAY'S WORK: every fix this lane built is aimed at code that cannot execute.** The `[NSApp run]` guard rewrite, the `startup_info` use-after-return fix, the bounded re-entry, the `nsapp_run_returned` edge — all sit inside `run_cocoa_app`, which is only ever reached from `macdrv_start_cocoa_app`, which is only ever reached from `macdrv_init`. **Keep them** (they are correct and become live the moment init is repaired, and the use-after-return was a genuine latent bug), but **none of them can restore input**. ㊸ pre-registered exactly this and try8 confirmed it: rungs 1a/1b/1e all 0.
  ⑧ ★ **PRE-REGISTERED FALSIFIABLE PREDICTION — costs one env var, no code, and it can prove me wrong.** `nodrv_CreateWindow` ERRs **once** on the `winediag` channel: `"Application tried to create a window, but no driver could be loaded."` followed by `driver_load_error`, whose default (set at `driver.c:962`, before anything can overwrite it) is literally **`"The explorer process failed to start."`**. Every run so far used `WINEDEBUG=-all`, which suppresses it — which is precisely why this sat undetected for two days. **A run with `winediag` enabled MUST contain both lines. If they are absent, this diagnosis is WRONG.**
  ⑨ **TWO CANDIDATE MECHANISMS FOR "no explorer", NOT YET DISCRIMINATED — [HYPOTHESIS] both.** **M1 — never spawned:** `get_desktop_window()` (`winstation.c:806`) only spawns `C:\windows\system32\explorer.exe /desktop` (`:833-893`) when the server's `get_desktop_window` reply leaves `top_window == 0` (`:831`); if anything created the desktop first, the branch is skipped silently and forever. **M2 — spawned and died:** `mr-run.sh:321` defaults `SYSTEM32_ARCH=x86_64-windows`, and the run.log confirms `synced …/system32/explorer.exe <= …/x86_64-windows/explorer.exe`, so explorer would run **as an x86_64 PE under HyperBridge** (the aarch64 build exists in dist but is not the one installed); a failure is reported only by `ERR_(win)( "failed to start explorer %x" )` (`winstation.c:902`) — suppressed by `WINEDEBUG=-all`. **The same `winediag`/`err` run in ⑧ discriminates M1 from M2**, so one run answers both.
  ⑩ **FIX CANDIDATES, in the order I would try them.** **(A) Registry seed — a probe first, a workaround second, and it needs ZERO engine code (stays in `artifacts/` + `tools/`, i.e. lane territory):** create `HKLM\System\CurrentControlSet\Control\Video\{00000000-0000-0000-0000-000000000000}\0000` with `GraphicsDriver="mac"` in the prefix template. The **null GUID is exactly the right one**: `driver.c:966-973` falls back to `guid_nullW` when the desktop window carries no `__wine_display_device_guid` prop, and that prop is set by explorer — which never ran. If seeding it makes rung 0a fire, the chain in ③ is proven end-to-end and input may simply start working. **(B) The correct fix — make explorer actually run** (and/or install the aarch64 explorer.exe), once ⑨ says which of M1/M2 is true. ⚠ `win32u/**` and `scripts/mr-run.sh` are **outside this lane's declared territory** (`winemac.drv/**`, `tools/**`, `reports/**`); (A) deliberately avoids both.
  ⑪ **Slot + housekeeping.** try8 is LIVE — pid 43099, launched 14:31:22 on a 5400 s budget, so the slot is held until **~16:01**. Nothing was launched, deployed, rebuilt, or killed this pass; the only writes were to `reports/`. Watcher 88592 has **done its job** (deployed `fd29b8ef`→dist `c934cd07`, content-verified, launched the real try8) and is now redundant — it can be retired by the operator. ⚠ **Do NOT ask the operator to press keys on this run**: with the null driver, input is structurally impossible and a manual test can only manufacture a fourth false negative today.
  ▶ **NEXT ITERATION:** ① Build the (A) registry-seed prefix template + launcher **now, offline**, so it is ready the moment the slot frees; set `WINEDEBUG` to let `winediag`/`err` through so ⑧'s prediction and ⑨'s M1/M2 fork are answered by the **same** run. ② Read rung `0a macdrv_init_entry` first — it is now the single number that matters; `0a≥1` = the driver is initialising = the whole lane moves downstream in one step. ③ If 0a fires, the next rungs in order are `0d2 macdrv_init_start_cocoa_ret rc=`, `1a run_cocoa_app_entry`, `1b … will_run=`, `1e nsapp_run_returned` — all already instrumented and content-verified in the deployed binary, so no rebuild is needed to read them. ④ Only after `0a` fires is it worth asking the operator for a keypress. ⑤ `tools/hk_input_ladder.sh` needs rungs 0a-0f promoted to the top of its output and a new adjudication line "NULL USER DRIVER — input structurally impossible" when `0a=0` while `create_cocoa_window≥1`, so this state can never again be mistaken for a Cocoa-side problem. ⑥ Still outstanding for the **operator**: force-add the untracked load-bearing files (`.gitignore:60` ignores all of `engine/`) — `cocoa_main.m`, `macdrv_main.c`, `event.c`, `mouse.c`, `keyboard.c`, `cocoa_app.m`, `cocoa_window.m`, plus `tools/hk_input_ladder.sh` and this journal. (`window.c`/`macdrv.h` are tracked.)
14:50 · **SAME ITERATION — one CORRECTION to my own 14:40 entry, one INVALID artifact caught, and try9 built + armed.** Nothing launched, deployed, rebuilt, or killed; the live try8 was not touched.
  ⑫ ⚠ **CORRECTION TO ③ — the registry observation is NOT evidence, and I am striking it.** I wrote "MEASURED: `system.reg` contains ZERO `Control\Video` keys" as if it proved explorer never ran. It proves nothing: `explorer/desktop.c:1059` creates that key with **`REG_OPTION_VOLATILE`**, so it lives only in the wineserver session and is **never written to `system.reg`** — the file would be empty of it even after a perfectly successful explorer run. **The conclusion is unaffected**, because it rests on three other legs that are each independently sufficient: rung `macdrv_init_entry` = 0 with 43 passing-control lines in the same stream (①), the winemac.drv PE not being mapped at all (②), and no explorer process running. But the claim as phrased was wrong and would have misled the next reader. Also corrected while I was in `desktop.c`: the value explorer writes is **`"winemac.drv"`**, not `"mac"` — `:1036` builds `libname = L"wine" + name + L".drv"` — which is exactly what the seed in ⑩(A) has to contain to be loadable by the `LoadLibraryW` on the other side of `NtUserLoadDriver`.
  ⑬ ★★ **`NSAPPRUN-VERDICT.txt` FROM THE try8 WATCHER IS INVALID FOR A AND B — DO NOT CITE IT.** It reports `game pid: 36810`, which is **`mr-run.sh` (bash)**, not the game (43099): its selector `pgrep -f 'extracted-hollow-knight.*Hollow Knight.exe'` matches mr-run's *argv*, which carries the game path, and returns it first. So its "A: `-[NSApplication run]` frames: 0" was **sampled from a shell** — a stack of `??? (in bash)` frames, visibly so in the file — and its B is not a number at all but the error text `Can't get application process 1 whose unix id = 36810`. A zero that agrees with the diagnosis for entirely the wrong reason is the most dangerous kind, so: **re-measured on the correct pid 43099, each leg with a live control.** **A** — HK `-[NSApplication run]` = **0**, `_DPSNextEvent` = **0**, `CFRunLoopRun` = 11; CONTROL Finder `-[NSApplication run]` = **1** ⇒ instrument works. **B** — HK AX windows = **0**; CONTROL Finder = **2**. Both exactly as the null-driver root cause predicts, and now they are actually evidence. (My 14:40 conclusion never used the verdict file — it used the run.log rungs and `lsof` on 43099 — so nothing above it moves.)
  ⑭ ★★ **try9 IS BUILT AND ARMED — the ⑩(A) fix, ready to fire the moment the slot frees. No rebuild and no deploy are needed.** Three artifacts, each verified by content:
   **(1) Prefix template `artifacts/hk-prefix-template-NOLANG-GRAPHICSDRIVER`** — `cp -a` of the NOLANG input-test template (1.9 MB, so no disk-hygiene concern; `system.reg` verified byte-identical before the edit, and the oracle save snapshot / `.valid` / `WINDOWS-ORACLE-IMPORT.json` carried over intact, so the run contract's `save_snapshot_manifest_sha256` still resolves). Appended, and read back: `[System\\CurrentControlSet\\Control\\Video\\{00000000-0000-0000-0000-000000000000}\\0000]` → `"GraphicsDriver"="winemac.drv"`. **The key path was verified against the code that reads it, not assumed:** `driver.c:932-940 guid_key_prefixW` is `\Registry\Machine\System\CurrentControlSet\Control\Video\{`, and `system.reg`'s header is `;; All keys relative to REGISTRY\\Machine` — so the seeded path resolves to exactly the string `reg_open_key` builds. The **null GUID is the correct one** precisely *because* explorer never ran: `driver.c:966-973` falls back to `guid_nullW` when the desktop window has no `__wine_display_device_guid` prop, and only explorer sets that prop. Seeding it as a *persistent* key is strictly better than explorer's volatile one (⑫) — it survives the wineserver session.
   **(2) Launcher `reports/phase4-hollow-knight/run-INPUT-TEST-try9-GRAPHICSDRIVER.sh`** — try8 with **exactly three functional lines changed** (`diff` = 4 lines incl. the banner): the run-dir name (deliberately **not** matching the `…nsapprun-try8-*` glob), `MACRUNNER_MR_RUN_PREFIX_TEMPLATE` → the seeded template by **absolute** path, and `WINEDEBUG` `-all` → **`-all,+winediag,err+win`**. Everything else byte-identical — re-verified in place: `MACRUNNER_HB_LANGUAGE_ONESHOT_SEQUENCE='0'`, `MACRUNNER_HB_START_GAME_ACTUATOR='0'`, `MACRUNNER_TRACE_WINEMAC_INPUT='1'`. `sh -n` and `bash -n` clean. The `WINEDEBUG` change is **not** a second variable in the confounding sense — it adds no behaviour, and it is what makes a *failed* fix self-explaining: it is the only way ⑧'s prediction and ⑨'s M1/M2 fork can be read, and both are dark under `-all`. Flood risk assessed: `winediag` is a rarely-used channel and `err+win` is the err class on one channel — neither can reproduce try4's 5.9 GB ntdll flood.
   **(3) Watcher `scratchpad/wait-launch-try9.sh` (pid 49071, alive, reparented to init).** Waits for the slot (counts into a variable, **no `ps | grep -q` under pipefail**, bracketed patterns so its own `grep` argv cannot self-match), requires **3 consecutive free polls** before launching (a run reappeared mid-handoff once today), then launches try9 and writes `TRY9-VERDICT.txt` into the run dir: all 14 rungs, the verbatim `macdrv_init_*` lines, the winediag/explorer lines for the 0a=0 branch, the actuator interlock, and A/B with Finder controls. **It deliberately does NOT deploy** — the deployed `winemac.so` already content-verifies as carrying every rung try9 reads, so a rebuild would add a variable rather than remove one.
  ⑮ ★ **THE WATCHER'S SELFTEST CAUGHT A REAL BUG IN MY OWN FIX BEFORE IT SHIPPED — controls with answers written down first, again earning their keep.** Having just caught ⑬'s wrong-pid defect, I replaced the selector with `ps -Ao pid,comm | awk '$2 ~ /Hollow Knight\.exe$/'` and ran it against the live run with the answer pre-registered (**must return 43099, must not return 36810**). It returned **EMPTY** — because the executable name **contains a space**, so awk field-splitting puts `Hollow` in `$2` and `Knight.exe` in `$3`. Fixed to match the end of the whole line (`/Hollow Knight\.exe$/`); re-run: **C1 live_count = 3** (predicted ≥2 ✓), **C2 game_pid = 43099** (✓, and the old `pgrep` demonstrably still returns 36810 in the same test, so the defect is reproduced alongside its fix). A watcher shipped with the first "fix" would have exited with "game process never appeared" 10 minutes into the next slot and cost the whole cycle.
  ⑯ **Housekeeping.** The try8 watcher (88592) finished on its own at 14:34 and exited — nothing to kill, and no double-launch hazard with 49071 (verified: exactly one `wait-launch-try9` process, ppid 1). try8 remains live (pid 43099, ends **~16:01** on its 5400 s budget).
  ▶ **NEXT ITERATION:** ① Check `reports/phase4-hollow-knight/laneA-INPUT-TEST-try9-GRAPHICSDRIVER-*/TRY9-VERDICT.txt` — the watcher writes it ~8 min after launch. **Read `macdrv_init_entry` first and nothing else matters until you have it.** ② `≥1` ⇒ the null-driver root cause is proven end-to-end and the fix works: walk on to `0d2 …start_cocoa_ret rc=`, then `1a/1b/1e`, then A/B (must flip to ≥1 from the baseline 0, Finder controls 1 and 2), and **only then** ask the operator for a keypress — with the actuator off, any `event=confirm-language` is real input and that is GOAL. ③ `0` ⇒ read the winediag block in the same verdict: `"no driver could be loaded"` + `"The explorer process failed to start."` confirms null-driver-but-seed-not-taken (next lever: why `reg_open_key` still misses — check the key actually loaded into the running prefix); `"failed to start explorer"` names M2; **neither line present ⇒ the 14:40 diagnosis is wrong** and ⑧ has falsified it, so re-open from `load_display_driver` (`driver.c:1018`) outward. ④ ⚠ **The operator can save 70 minutes**: try8 has been fully read out (rungs, lsof, A/B all captured above) and carries **no** remaining input value — ending it early lets watcher 49071 fire try9 immediately. That is his call, not mine; I did not touch it. ⑤ Still outstanding for the **operator**: force-add the untracked load-bearing files (`.gitignore:60` ignores all of `engine/`) — `cocoa_main.m`, `macdrv_main.c`, `event.c`, `mouse.c`, `keyboard.c`, `cocoa_app.m`, `cocoa_window.m`, plus `tools/hk_input_ladder.sh`, the two try8/try9 launchers, and this journal.
  ⑰ **`tools/hk_input_ladder.sh`: new `--- DRIVER adjudication ---` block, placed UPSTREAM of every other block, controls run in both directions.** The ladder already carried rungs 0a-0f (㊼) but nothing *adjudicated* them, so today's state read as a wall of zeros that looked like a Cocoa-side failure. The block now names it mechanically: `0a=0` **with** `0f>0` ⇒ `*** NULL USER DRIVER — INPUT IS STRUCTURALLY IMPOSSIBLE ***`, plus the independent `lsof`/winediag confirmations and the exact registry fix lever; it also distinguishes 0a=0-with-no-window (plain driver-load failure), `no_graphic_access`, init-entered-but-incomplete, and init-completed. It explicitly says **do not ask the operator to press keys** in the null-driver state — the interlock for the failure mode that burned three of today's manual tests. **Controls, predictions written first: (C0)** `sh -n`/`bash -n` clean. **(C1)** try8 log + the deployed binary → `*** NULL USER DRIVER ***`, `0a=0 … 0f=1` ✓ as predicted. **(C2)** the *same* log against the Jul-6 reference binary → correctly degrades to `UNDETERMINED — the macdrv_init_entry trace is NOT in the reference binary` ✓, so the tool cannot confuse "not built" with "did not fire". **(C3)** try6 regression → `queue_post` still **2** ✓ unregressed, and the stale-reference guard fires there too. One implementation hazard caught while writing it: the registry key ends in `\0000`, which `printf` would have eaten as an **octal escape** — the path is held in a variable and printed with `%s`, and C1 confirms it renders intact.
15:12 · **try9's CENTRAL ASSUMPTION IS NOW MEASURED, NOT ASSUMED — the seed really is readable at the driver's exact path. Two more links in the same chain closed. And a CORRECTION: try9 cannot discriminate M1 from M2 the way 14:40 ⑨ claimed, so I built the instrument that can.** Offline pass; nothing launched, deployed, rebuilt, or killed; live try8 untouched.
  ⑱ ★★ **THE SEED'S KEY PATH WAS A REAL RISK, AND IT IS NOW PROVEN.** The seed is written under `System\\CurrentControlSet\\…` — and that is the *only* key of its kind in the file: the unseeded template carries **185** `[System\\ControlSet001\\…]` headers and exactly **one** `[System\\CurrentControlSet]`, which `server/registry.c:1940-41` turns into a `KEY_SYMLINK` whose `SymbolicLinkValue` is `ControlSet001`. So try9's key is the single key in that prefix written *through the link*, and if the loader did not follow it the value would exist but be invisible to `reg_open_key` — a silent 90-minute loss. Source says it resolves: `load_key` (`:1452`) → `create_key_recursive` (`:1489`) walks path elements one at a time via `create_key_object( …, OBJ_OPENIF, … )` (`:888`); `OBJ_OPENLINK` is absent, so `follow_symlink` (`:496-531`) redirects `CurrentControlSet` → `ControlSet001` before the rest of the path is created. **But "source says" is not this lane's standard.** New tool **`tools/hk_regseed_probe.sh`**, three controls, predictions written into the script *before* it ran:
   • **C1 positive control** — `…\\Control\\Class\\{4D36E968-…}\\0000 /v DriverDesc` read through the same symlink → **`Apple M1 Pro`** ✓ (proves reg.exe works *and* that reads traverse the link).
   • **C2 negative control** — the Video key on the **unseeded** template → **`reg: Unable to find the specified registry key`** ✓ (proves the probe can say no).
   • **C3 the test** — the Video key on the **seeded** template → **`HKEY_LOCAL_MACHINE\System\CurrentControlSet\Control\Video\{00000000-0000-0000-0000-000000000000}\0000` / `GraphicsDriver  REG_SZ  winemac.drv`** ✓.
   ⇒ **the seed is reachable at exactly the string `driver.c:932-982` builds.** Not a title run: native **aarch64** `reg.exe` (no HyperBridge), 1.9 MB throwaway prefixes, scoped `wineserver -k`, no `pkill`; afterwards only the live run's wineserver (43079) remained and zero temp prefixes were left. Three defects the controls caught before any number was believed: **`timeout` does not exist on macOS** (C1 → rc=127, and the adjudicator refused to report rather than reading the failure as "not found"); **a relative `WINEPREFIX` is rejected outright** (`invalid directory … not an absolute path` — the charter's absolute-path trap, hit by my own script); and the templates carry **no `drive_c/windows/system32`**, so `wine` began a full prefix update — fixed with `printf 'disable' > .update-timestamp` on the **scratch copies only** (`wineboot.c:137` skips the update on that marker). The real templates were not touched.
  ⑲ ★ **THE NEXT TWO LINKS ARE ALSO CLOSED.** **(a)** `KeUserModeCallback( NtUserLoadDriver, … )` lands on `User32LoadDriver` (`user32/user_main.c:130`) = `LdrLoadDll( L"c:\\windows\\system32", 0, "winemac.drv", … )`, and **`winemac.drv` IS present in the live prefix's system32** (122880 B = the x86_64 build, size-identical to `dist/lib/wine/x86_64-windows/winemac.drv`) because `scripts/sync-prefix-from-dist.sh:59` lists it by name. The load has a file to find. **(b)** ★ **The null GUID is *unconditionally* correct in this tree — stronger than 14:50 ⑭ claimed.** `__wine_display_device_guid` is SET at exactly one site, `win32u/defwnd.c:3034`, inside `desktop_window_proc`'s `WM_NCCREATE`, and only when `cs->lpCreateParams` carries a GUID — which only explorer passes. Searched both the literal-string and char-array spellings; every other occurrence in the tree is a **reader** (`driver.c:957`, `winecfg/x11drvdlg.c:109`). ⇒ with no explorer the prop cannot exist, `guid_atom`=0, `NtQueryInformationAtom` fails, `guid_nullW` is taken — the exact key we seeded.
  ⑳ ⚠ **THE ONE WAY THE SEED CAN STILL MISS — pre-registered so try9 stays interpretable.** If explorer gets far enough to create the desktop window **with** a GUID (setting the prop) but dies before `RegSetValueExW(…"GraphicsDriver"…)` (`explorer/desktop.c:1063`), `load_desktop_driver` consults the **real-GUID** key, the null-GUID seed is never read, and `0a` stays 0 **while the 14:40 diagnosis is still right**. That outcome is not silent: `0a=0` **with** winediag `"no driver could be loaded"` ⇒ seed missed, diagnosis intact. `0a=0` with **no** winediag at all ⇒ the gate never opened and the run says nothing.
  ㉑ ★★ **CORRECTION TO 14:40 ⑨ — try9 CANNOT discriminate M1 from M2, and the instrument that can is now running.** The claim "the same winediag/err run discriminates M1 from M2" is **wrong as configured**. try9 uses `WINEDEBUG='-all,+winediag,err+win'`: `+winediag` opens all classes on that channel ✓, and `err+win` opens the **ERR class only** on `win` — so `ERR_(win)( "failed to start explorer %x" )` (`winstation.c:902`) does appear, but `TRACE_(win)( "starting explorer for desktop" )` (`:858`) and `TRACE_(win)( "started explorer" )` (`:897`) are **TRACE class and stay dark**. ⇒ try9 can CONFIRM a spawn failure but can NOT confirm a successful spawn, so **M1 (never spawned) and "spawned, started, died quietly" are indistinguishable in it** — and "died quietly" is entirely plausible, since explorer is installed as the **x86_64** PE (`0aeacc9a…`, 638976 B, confirmed in the live prefix) running under HyperBridge. Closed from outside the process: new **`tools/hk_explorer_watch.sh`**, a read-only 1 s `ps` poll answering "did `explorer.exe` EVER exist", independent of WINEDEBUG — **any sighting refutes M1 outright**. Validated in both directions *before* launch: **C1** (no explorer) → 0 sightings while correctly logging the live mr-run/wineserver pids and `game process present`; **C2** (`exec -a "C:\windows\system32\explorer.exe /desktop" sleep`) → **detected**, pid/ppid/argv captured. Detached as **pid 62853**, log `reports/research/try9-explorer-watch.log`, 3.5 h hard stop. Charter-compliant: counts into a variable, no `ps | grep -q`, no pipefail, bracketed patterns, never signals or kills.
  ㉒ ⚠ **A LAUNCH THAT REPORTED SUCCESS AND HAD NOT HAPPENED — caught only by checking the artifact.** My first launch printed `watchers after launch: 2` and started **nothing**. The guard counted my own interactive shell, whose argv contains `tools/hk_explorer_watch.sh` from the `nohup` line — the bracket trick does not help when the self-match is in the *shell wrapper* rather than the grep. The give-away was that the log file did not exist. Detector fixed (`| grep -vc 'zsh -c'`), relaunch verified by the log's existence and contents rather than by a count. **Same failure family as ⑬'s wrong-pid verdict and ⑮'s awk-space bug: three times today a status has disagreed with reality, and every time only an artifact check caught it.**
  ㉓ **`tools/hk_input_ladder.sh` — new `driver-load cause (winediag / explorer)` block, controls in three directions.** The ladder *advised* looking for the winediag line but never counted it; under try9 that line becomes real evidence. It now counts `"no driver could be loaded"`, `"The explorer process failed to start."`, `"failed to start explorer"` — and, critically, prints `GATE LOOKS CLOSED … these zeros carry NO information` when there is no winediag output at all, so a `-all` run can never be misread as "explorer was fine". ㉑'s limit is printed inline. Uses the file's own `c()` helper rather than a raw `grep -c` (the double-printed-zero defect in this tool's own history). Controls, predictions first: **(C0)** `bash -n` clean; **(C1)** real try8 log → `GATE LOOKS CLOSED` ✓ with the pre-existing `*** NULL USER DRIVER ***` verdict unregressed ✓; **(C2)** synthetic M2 fixture → `=> M2: explorer WAS spawned and NtCreateUserProcess FAILED` ✓; **(C3)** synthetic M1 fixture → `=> … either M1 or died silently` ✓.
  ㉔ **Logged, not acted on, and not part of any conclusion.** The live prefix's `system.reg` differs from its template by 183 lines: `[System\\ControlSet001\\Control\\Class\\{4D36E968-E325-11CE-BFC1-08002BE10318}\\0000]` (the display-adapter class) had its `#time` and `DriverDate` rewritten **this run** (`6-26-2026` → `7-28-2026`), and a `Software\\NVIDIA Corporation\\Global\\NGXCore` key appeared. So something registers display devices at runtime **even on the null driver**. **[HYPOTHESIS]** that is win32u's own device-registration fallback and/or the DXMT path — *not* evidence that macdrv ran; `DriverDesc="Apple M1 Pro"` was already in the June template. Flagged only so the next reader does not mistake it for driver activity. It does not touch the 14:40 chain, which rests on `0a=0` with 43 passing-control lines, the PE not being mapped, and no explorer process.
  ㉕ **Slot + housekeeping.** try8 still live (pid 43099, ends **~16:01**). Watcher **49071 alive and untouched** — it owns the try9 handoff. try9 launcher re-verified by content this pass: seeded template by **absolute** path (`:123`), `ONESHOT_SEQUENCE='0'` (`:108`), `START_GAME_ACTUATOR='0'` (`:183`), `TRACE_WINEMAC_INPUT='1'` (`:209`), `WINEDEBUG='-all,+winediag,err+win'` (`:153`), run dir deliberately outside the try8 glob (`:9`).
  ▶ **NEXT ITERATION:** ① `TRY9-VERDICT.txt` — **`macdrv_init_entry` first, nothing else matters until you have it.** ② `bash tools/hk_input_ladder.sh <try9-dir>` — read the new **driver-load cause** block; if it says `GATE LOOKS CLOSED`, the run was misconfigured and its zeros mean nothing. ③ `reports/research/try9-explorer-watch.log` — an `EXPLORER.EXE SEEN` line **refutes M1** and points the real fix (B) at *why explorer dies* rather than *why it never starts*. ④ `0a≥1` ⇒ walk `0d2` → `1a` → `1b` → `1e`, then A/B (must flip from 0; Finder controls 1 and 2), and **only then** ask the operator for a keypress — actuator is off, so any `event=confirm-language` is real input = GOAL. ⑤ `0a=0` ⇒ use ⑳'s fork before concluding anything. ⑥ Still outstanding for the **operator**: force-add the untracked load-bearing files (`.gitignore:60` ignores all of `engine/`) — `cocoa_main.m`, `macdrv_main.c`, `event.c`, `mouse.c`, `keyboard.c`, `cocoa_app.m`, `cocoa_window.m`, plus `tools/hk_input_ladder.sh`, **`tools/hk_regseed_probe.sh`**, **`tools/hk_explorer_watch.sh`**, the try8/try9 launchers, and this journal.
14:56 · COORDINATOR — THE BREAK IS ABOVE macdrv_init AND THE INSTRUMENT TO SPLIT IT IS 3 LINES IN dllmain.c (offline, 0 slots) · Following try8 I traced the call chain. Your own `stage=macdrv_init_entry` is UNGATED and prints as the function's first statement — and in try8 it is **absent**, along with `macdrv_init_session` and `macdrv_init_no_graphic_access`, all zero. So `macdrv_init` did not merely fail the `sessionHasGraphicAccess` check: **it was never entered.** That rules out the graphic-access hypothesis, my own GA_ROOT hypothesis, and the "run loop exited early" reading in one shot. The break is strictly upstream. ★ THE CHAIN: `dllmain.c` (PE side) does `if (__wine_init_unix_call()) return FALSE;` then builds params (`app_icon_callback = macdrv_app_icon`, always non-NULL, which is exactly why you concluded the statics being NULL means the function never ran) and finally `if (MACDRV_CALL(init, &params)) return FALSE;`. On the unix side `macdrv_init` sits at `__wine_unix_call_funcs[0]` (macdrv_main.c:728) with a WoW64 twin `wow64_init` at :783 in `__wine_unix_call_wow64_funcs[]`. Our guest is x86-64 under ARM64 translation, so which dispatcher table is used is itself a live question. ★ WHAT IS MISSING: **`dllmain.c` has no tracing at all.** Silence there cannot distinguish three very different failures — (a) the PE-side `winemac.drv` never loaded or its DllMain never ran; (b) it ran but `__wine_init_unix_call()` returned non-zero, so the function bailed before ever calling init; (c) the call was made and crossed to the unix side but the x86-64 -> aarch64 unixlib dispatch failed or landed in the wrong table. All three look identical from where you are standing, and this is the same `not logged != did not happen` trap that cost us the morning. ★ SUGGESTED, and it is about five lines: an UNGATED `fprintf` at DllMain/process-attach entry printing pid and machine, one immediately after `__wine_init_unix_call()` printing its return, and one around `MACDRV_CALL(init, &params)` printing the status. Then a single run says which of (a)/(b)/(c) it is, instead of another 45 minutes of the same silence. Note both `system32/winemac.drv` and `syswow64/winemac.drv` are synced in our runs, so print the machine word too — knowing WHICH winemac.drv ran is half the answer. ★ CONTEXT THAT SURVIVES: yesterday's compositor fix is healthy in the same run (`toplevel_fallback` fires twice, window visible, menu rendered), so this is purely the event path. And the operator has now tested interactively three times today and is willing again — but only spend his attention once the instrument can tell him something either way.
15:20 · ★★ **THE COORDINATOR'S 14:56 REQUEST IS IMPLEMENTED, BUILT, DEPLOYED AND CONTENT-VERIFIED — and it matters more than the request assumed: it is the only thing that will separate "the seed did not take" from "the PE loaded but its unixlib init failed", which is try9's most likely PARTIAL-success outcome.** Live try8 untouched; dist `winemac.so` deliberately unchanged.
  ㉖ **ANSWERING 14:56 DIRECTLY — leg (a) is ALREADY MEASURED and needs no instrument.** The ask is to split (a) the PE/DllMain never ran, (b) `__wine_init_unix_call()` failed, (c) the x86-64→aarch64 unixlib dispatch landed wrong. **(a) was settled at 14:40 ② by a method that needs no tracing at all:** `lsof -p 43099` lists **no `winemac.drv` of any architecture**, while listing 23 other dist images *including* `win32u.dll` — a passing control inside the same command. Re-confirmed this pass from a wider angle: **`lsof | grep -c 'winemac\.drv'` machine-wide = 0** — not one process on this Mac has any winemac.drv mapped. So we are in case (a), and (b)/(c) have not yet had the chance to happen. The instrument's value is therefore **forward-looking**, which is why I built it anyway (㉘).
  ㉗ ⚠ **AND 14:25 ㊺'s OBJECTION TO INSTRUMENTING `dllmain.c` WAS CORRECT, SO I REMOVED ITS CAUSE.** ㊺ deliberately skipped `dllmain.c` because "it ships as `winemac.drv`, which watcher 88592 does **not** deploy — a trace there would silently never reach the run." Exactly right: a trace in an **undeployed** PE is indistinguishable from case (a) itself, and would have manufactured the very silence it exists to rule out. **So I deployed the PE myself** instead of leaving the trace dark. Safe under a live run precisely *because* of ㉖ — nothing has winemac.drv mapped, re-checked with an `lsof` count immediately before each write and an abort-on-nonzero guard in the deploy loop — so replacing it cannot race an mmap. `winemac.so`, which **is** mapped, was not touched.
  ㉘ ★★ **THE INSTRUMENT, and why it earns a build even though (a) is already known.** Four **ungated** rungs on the process-attach path in `dllmain.c`: `stage=dllmain_attach` (the FIRST statement of `DllMain` on `DLL_PROCESS_ATTACH`, with `arch=` and pid), `stage=dllmain_unixcall_init status=` (the return of `__wine_init_unix_call()`, previously **discarded** by `if (__wine_init_unix_call()) return FALSE;`), `stage=dllmain_macdrv_init_call status=` (the return of `MACDRV_CALL(init, &params)`, likewise discarded), and `stage=dllmain_attach_ok`. ★ **The forward-looking point: the moment try9's seed works, the PE loads and the very next unknown is (b)/(c)** — and this project already has a documented x86-64-PE-unixlib failure class (`winemetal.dll` never initialising its unixlib). Without these rungs, "seed did not take" and "PE loaded, unixlib init failed" **both present as `0a=0`** and are indistinguishable; with them, try9 answers both in one run. `arch=` is printed as the coordinator asked, with `__arm64ec__`/`_M_ARM64EC` tested **before** `__aarch64__` (ARM64EC defines both).
  ㉙ ★ **THE TRACE MECHANISM WAS ITSELF A RISK AND I DID NOT LEAVE IT SINGLE-PATHED.** `ERR()`/`TRACE()` are silenced by `WINEDEBUG=-all` — which is *how this entire state hid for two days* — so the first implementation wrote straight to `GetStdHandle(STD_ERROR_HANDLE)`. But that handle can legitimately be NULL in a GUI process, and **this mechanism has never been exercised in a PE in this process type**, so trusting it alone risked manufacturing exactly the silence it exists to disprove. Rebuilt to emit on **two independent paths**: the raw stderr handle **and** `ERR_(winediag)` (try9 opens `+winediag`). If one path is dead the other still reports, and the two agreeing cross-validates the mechanism for every later run.
  ㉚ **BUILD + DEPLOY — content-verified at every step, with the single-variable invariant held.** `make` of all three PE arches, RC=0, and the error/warning-filtered output was empty. Deployed **only** the PEs (atomic same-dir `mv`): `x86_64-windows` **78cb5194**, `aarch64-windows` **1d3c0826** (ARM64X — hence its doubled token counts), `i386-windows` **e007081d**; all three verified **in dist** by `strings` (4 / 8 / 4 `stage=dllmain_` tokens, winediag channel present). ★ **INVARIANT HELD: dist `winemac.so` is still `c934cd07` and the build artifact still `fd29b8ef`** — byte-identical to what watcher 49071 content-verified, so **try9's single variable is still the registry seed**; the PE rungs add no behaviour. All three arches were instrumented deliberately: if only x86_64 carried the trace and another arch loaded, the silence would be misread — the coordinator's "knowing WHICH winemac.drv ran is half the answer".
  ㉛ **`tools/hk_input_ladder.sh`: new `PE LOAD` block above the DRIVER block — and its own controls caught TWO defects in it.** Rungs Pa–Pd with a four-way verdict (never loaded / unixlib-init failed / MACDRV_CALL failed / attach OK). **Defect 1:** the ladder classifies rungs against a reference binary that was `winemac.so`, but these tokens live in `winemac.drv` — so every new rung would have reported a false `n/a`. Fixed by unioning the deployed PE's tokens into the reference set. **Defect 2, the more instructive one:** the block printed a confident `=> the winemac.drv PE NEVER LOADED` for **try8**, whose log predates the instrumentation — a verdict that is *true* (㉖ proved it by `lsof`) but that the log had no way to earn. I added a stale-reference guard **and it silently did nothing**, because `[ ref -nt log ]` can never fire against a **live** run whose log mtime keeps advancing to now. Fixed to compare the reference's mtime against the log's **BIRTH** time (`stat -f %B`) — and the same latent bug in the pre-existing `REF_STALE` guard was fixed with it. Controls, predictions first: **(C0)** `bash -n` clean; **(C1)** try8 → `UNDETERMINED` ✓ (replacing the unearned strong verdict); **(C4)** both synthetic fixtures → strong verdicts unchanged ✓; **(C5)** pre-existing `NULL USER DRIVER` + `GATE LOOKS CLOSED` unregressed ✓; **(C6)** the long-standing try6 regression `queue_post` = **2** ✓.
  ㉜ **State.** try8 live (pid 43099, 48 min in, ends **~16:01**) — not touched, not killed. Watcher **49071** alive and untouched (owns the try9 handoff; deploys nothing). Explorer watcher **62853** alive. ★ **try9 now answers FOUR questions in ONE run:** the seed (`0a`), the PE-load fork (`Pa`–`Pd`), the M1/M2 winediag fork, and — from outside the log entirely — whether `explorer.exe` ever existed.
  ▶ **NEXT ITERATION (SUPERSEDED by the 15:40 list at the bottom):** ① `bash tools/hk_input_ladder.sh <try9-dir>` and read **`Pa dllmain_attach` FIRST** — it is now the most upstream number in the whole ladder. `Pa=0` ⇒ the PE still never loaded ⇒ the seed did not take (go to the driver-load-cause block, then ⑳'s fork). `Pa≥1` ⇒ **the seed WORKED** and the fight has moved downstream — read `Pb`/`Pc` `status=` verbatim. ② Then `0a macdrv_init_entry`, then `0d2`/`1a`/`1b`/`1e`. ③ `reports/research/try9-explorer-watch.log` — an `EXPLORER.EXE SEEN` line **refutes M1** and re-aims the real fix (B) at *why explorer dies* rather than *why it never starts*. ④ Ask the operator for a keypress **only** once `0a≥1`; the actuator is off, so any `event=confirm-language` is real input = GOAL. ⑤ Artifacts: PE **78cb5194** (x86_64), unix `winemac.so` **c934cd07** — unchanged on purpose. ⑥ Operator: force-add the untracked load-bearing files — now also **`engine/wine/dlls/winemac.drv/dllmain.c`**, plus `tools/hk_regseed_probe.sh` and `tools/hk_explorer_watch.sh`.
15:40 · ★★★ **THE LADDER PRINTED `VERDICT: GOAL` ON try9 AND IT WAS FALSE — our own launcher banner was being counted as evidence for four different rungs. Defect found, fixed, and proven in BOTH directions. Separately: try9's real numbers are in, the seed is live in the prefix, and explorer.exe WAS spawned (M1 refuted).** Live try9 untouched — nothing launched, deployed, rebuilt or killed this pass; the only writes were `tools/hk_input_ladder.sh` and this journal.
  ㉝ ★★★ **THE FALSE GOAL, and it would have ended this lane on a lie.** Read the ladder on the live try9 dir and it printed `VERDICT: GOAL — confirm-language with the actuator DISARMED = real input`, plus `0a macdrv_init ENTERED = 2`, `"no driver could be loaded" = 1`, `"failed to start explorer" = 1` and `=> M2: explorer WAS spawned and NtCreateUserProcess FAILED`. **Every one of those five numbers came from ONE line of our own commentary.** run.log **L4** is the launcher's `echo` note (1673 chars), and a per-line scan proves it is the *only* line in the whole log containing `confirm-language`, `macdrv_init_entry`, `no driver could be loaded` or `failed to start explorer` (each: `-> lines 4  n=1`). The note exists precisely because it tells the next reader what to grep for — so it names every rung, and `c() { grep -c … "$LOG"; }` (`:69`) counted it as a hit for all of them. A GOAL verdict two minutes into a run whose menu needs ~30 minutes is what made me look; **the charter's "prove the tool before trusting the number" is the only reason this was caught.**
  ㉞ ★★ **THE FIX — a prose guard upstream of every count, and it cannot be silent.** New block in `tools/hk_input_ladder.sh` before `c()`. The invariant it rests on: **a machine-emitted trace line names exactly ONE rung; prose names several.** So any line matching **≥2 distinct rung markers** (16 literal substrings — `macdrv_init_entry`, `dllmain_attach`, `confirm-language`, `run_cocoa_app`, `queue_post`, `garoot_null`, … — deliberately mutually-exclusive concepts, so no real line can reach 2) is diverted to a prose file and excluded from **all** counts. `$LOG` is repointed at the sanitised stream so every pre-existing count inherits the guard with no other edit; `$LOG_DISPLAY` keeps the real path for the header. It **prints** `⚠ PROSE GUARD: N line(s) excluded from ALL counts` plus the first excluded line, so the filtering can never happen invisibly.
  ㉟ ★★ **CONTROLS, predictions written down BEFORE running — including the one that matters, which is the over-drop direction.** **(C0)** `bash -n` + `sh -n` clean ✓. **(C1 the defect, try9)** predicted guard-drops=1, `0a` 2→**0**, nodrv 1→**0**, expfail 1→**0**, GOAL→"no confirm-language yet": **all four exactly as predicted** ✓, and the correct `*** NULL USER DRIVER — INPUT IS STRUCTURALLY IMPOSSIBLE ***` + `GATE LOOKS CLOSED` verdicts now fire for try9. **(C2 unregressed, try8)** `*** NULL USER DRIVER ***` and `GATE LOOKS CLOSED` still fire ✓. **(C3 BOTH DIRECTIONS — the control that earns the fix)** synthetic fixture = 1 prose line + 4 real single-marker lines: prose dropped ✓, `stage=macdrv_init_entry` **counted** (0a=1) ✓, `queue_post` **counted** ✓, a real `event=confirm-language` **counted AND `VERDICT: GOAL` still fires** ✓. ⇒ the guard removes the false positive **without** being able to suppress a true one. ⚠ **Fifth time today a status disagreed with reality and only an artifact check caught it** (⑬ wrong pid, ⑮ awk-space, ㉒ phantom launch, ⑫ my own registry claim, now this).
  ㊱ ★ **THE GOAL TOKEN IS REAL — C3 raised the question, so I closed it.** `event=confirm-language` is emitted by `tools/hk_language_observer/hk_language_observer.c:2283` and `:2375` via `observer_log()`, which writes `macrunner-hb-language-flow: event=<event> …` (`:399-414`), shipped as `mono-profiler-hk_language.dll` and loaded by `WINEDLLOVERRIDES=mono-profiler-hk_language=n`. It carries exactly one rung marker, so the new guard provably cannot drop it. **NB `macrunner_hb.c` contains ZERO occurrences** — the lane's termination token does not come from the engine, so the FORBIDDEN engine lane cannot break it.
  ㊲ ★★★ **try9's REAL numbers — and the run has moved, in two measurable ways.** Game pid **84849** (⚠ the `Hollow Knight.exe` argv has a **trailing space**, so a `$`-anchored `ps` selector silently returns the *mr-run bash* instead — ⑮'s bug in a new costume; hit it once this pass and fixed by dropping the anchor + excluding `mr-run`).
   **(a) ★ THE SEED LANDED AND IS LIVE.** `artifacts/_mr-run.RZ1osZ/system.reg` L48854-48856: `[System\\ControlSet001\\Control\\Video\\{00000000-0000-0000-0000-000000000000}\\0000]` → `"GraphicsDriver"="winemac.drv"`. Note **ControlSet001** — ⑱ predicted from `server/registry.c` that the `CurrentControlSet` symlink would be followed at load; that is now confirmed in a real run, not just in source.
   **(b) ★★ EXPLORER.EXE WAS SPAWNED — M1 IS REFUTED.** Watcher 62853 has been up since 15:09:29, i.e. **before** try9 launched at 15:20:33, so this positive sighting is earned: `15:21:00 *** EXPLORER.EXE SEEN (count=1) — M1 REFUTED, explorer WAS spawned ***`, **pid 85189, ppid 1, argv `C:\windows\system32\explorer.exe /desktop`, etime 00:01**. It was seen once, at one second old, and **no explorer process exists now** ⇒ spawned and died young. ⚠ **CORRECTION to that same watcher's own try8 verdict: its `15:20:07 => M1 SUPPORTED` line is UNEARNED and must not be cited** — the watcher started 38 minutes into try8 and explorer spawns at startup. **A negative from an instrument that was not present is not a negative** (the `not logged != did not happen` gate, applied to my own tool).
   **(c) ★ winemac.drv IS BEING OPENED THIS RUN.** Machine-wide `lsof … winemac.drv` = **1** — wineserver 84712 holds `x86_64-windows/winemac.drv` **fd 140r**. In try8 the same machine-wide count was **0** (㉖). [HYPOTHESIS] opened on explorer's behalf, not the game's.
   **(d) BUT THE GAME IS STILL ON THE NULL DRIVER.** `lsof -p 84849`: `winemac.drv` **0**, `winemac.so` 1, **CONTROL 24 dist images incl. `x86_64-windows/win32u.dll`** (so lsof sees PE modules fine). Rungs after the guard: `Pa dllmain_attach` **0**, `0a macdrv_init_entry` **0**, `garoot_null … case=c_server_no_parents` ×2.
   **★ `Pa=0` IS A REAL ZERO, CONTENT-VERIFIED, NOT A DARK GATE:** the *live prefix's* `system32/winemac.drv` is sha **78cb5194** = byte-identical to dist, and `strings` finds **4** `stage=dllmain` tokens in it (syswow64: **e007081d**, 4 tokens). The instrument is physically in the file the run loads, and it is silent.
  ㊳ ⚠ **THE winediag GATE NEVER OPENED — ⑧'s prediction is UNTESTED, not falsified. Do not read try9's winediag zeros as evidence.** Body of run.log with the banner excluded, 1815 lines: `err:` **0**, `winediag` **0**, `fixme:` **0**, `trace:` **0** (only one `warn:` line, DXMT's own `MakeWindowAssociation`). The launcher does `export WINEDEBUG='-all,+winediag,err+win'` (`:153`) and `mr-run.sh:642` honours `WINEDEBUG="${WINEDEBUG:--all}"`, so it *should* have passed through — and it produced not one line. The rebuilt ladder correctly refuses these zeros (`GATE LOOKS CLOSED — these zeros carry NO information`). ⇒ **⑳'s seed-missed-vs-diagnosis-wrong fork cannot be resolved from this run**; M1/M2 was answered from outside the log instead (㊲b). **Any next run must carry a control that proves the gate is open before its silence is read.**
  ㊴ ★★ **THE LEADING HYPOTHESIS NOW, and it is ⑳'s pre-registered miss case — [HYPOTHESIS].** explorer *did* run (㊲b). `explorer/desktop.c` creates the desktop window **with a real GUID** (setting `__wine_display_device_guid`, the prop `driver.c:957` reads) *before* `RegSetValueExW(…"GraphicsDriver"…)` at `:1063`. If explorer died in that window — and it died at ~1 s — then the desktop carries a **real** GUID, `driver.c:966-973` does **not** fall back to `guid_nullW`, our null-GUID seed is **never read**, and `0a` stays 0 **with the 14:40 null-driver diagnosis fully intact**. That is exactly ⑳, and ㊲b is the first evidence for it.
  ㊵ ★★ **THE NEXT LEVER IS IN-LANE, CHEAP, AND NEEDS NO GAME SLOT — explorer is running as the x86_64 PE under HyperBridge, and a native aarch64 build already exists.** Measured: `mr-run.sh:321 SYSTEM32_ARCH="${MACRUNNER_PREFIX_SYSTEM32_ARCH:-x86_64-windows}"` — **an env knob**, so the launcher can change it without touching `scripts/**`. The live prefix's `system32/explorer.exe` = **0aeacc9a**, 638976 B = the **x86_64** build; dist also ships **aarch64-windows/explorer.exe** = **f48473b1**, 1245184 B. ⚠ **Do NOT flip `MACRUNNER_PREFIX_SYSTEM32_ARCH` wholesale** — HK is an x64 PE and loads its DLLs from system32; that would confound everything. **Targeted move: overwrite ONLY `system32/explorer.exe` with the aarch64 build** — explorer is a separate process, so its architecture is independent of the guest's.
  ㊶ ★ **AND THE WHOLE QUESTION REPRODUCES WITHOUT THE 90-MINUTE TITLE RUN.** `load_desktop_driver` is taken by *every* Wine GUI process, not by HK specifically. So `explorer.exe /desktop` on a throwaway copy of the seeded template, with the debug channels open, answers "does explorer die, and with what error" in **seconds** — the ⑱ pattern (native binary, 1.9 MB throwaway prefix, scoped `wineserver -k`, no `pkill`) that already worked twice today. That is where the next iteration should start; the title slot is the expensive instrument and it is not needed for this question.
  ㊷ **Slot + housekeeping.** try9 **LIVE** — pid 84849, launched 15:20:10 on a 5400 s budget ⇒ slot held until **~16:50**. Watchers 49071 (handoff, its job done) and 62853 (explorer poll, still useful) both alive and untouched. Deployed artifacts unchanged this pass: unix `winemac.so` **c934cd07**, PE **78cb5194**/**1d3c0826**/**e007081d**. ⚠ **Still do NOT ask the operator for a keypress** — `0a=0`, so input remains structurally impossible and a manual test can only manufacture another false negative.
  ▶ **NEXT ITERATION:** ① **Start offline, not on the slot** (㊶): copy the seeded template, run `explorer.exe /desktop` under it with debug channels open, and **prove the gate is open with a control before reading any silence** (㊳) — then read why explorer exits. ② A/B that against the **aarch64** explorer (㊵), overwriting *only* `system32/explorer.exe`. ③ Resolve ㊴ directly if a cheap read exists: whether the desktop window carries a **real** `__wine_display_device_guid` (⇒ our null-GUID seed is bypassed and the seed must move to that GUID, or explorer must be kept alive). ④ Re-read try9 with the **fixed** ladder before quoting any number from it — every pre-15:40 ladder figure for try9 in this journal above is contaminated by ㉝ and must be re-derived. ⑤ Ask the operator for a keypress **only** once `0a≥1`. ⑥ Still outstanding for the **operator**: force-add the untracked load-bearing files (`.gitignore:60` ignores all of `engine/`) — `cocoa_main.m`, `macdrv_main.c`, `event.c`, `mouse.c`, `keyboard.c`, `cocoa_app.m`, `cocoa_window.m`, `dllmain.c`, plus `tools/hk_input_ladder.sh`, `tools/hk_regseed_probe.sh`, `tools/hk_explorer_watch.sh`, the try8/try9 launchers, and this journal.
15:55 · **ONE ATTEMPTED MEASUREMENT, FAILED ITS OWN CONTROLS, NO CONCLUSION DRAWN — recorded so the next iteration does not repeat it.** Live try9 still healthy and untouched (pid 84849, 246.9% CPU at t+16 min).
  ㊸ ⚠ **A LIVE-PREFIX REGISTRY PROBE FOR ㊴ DOES NOT WORK — and the controls, not the result, are what told me.** To resolve ㊴ (does the desktop carry a *real* `__wine_display_device_guid`, so the null-GUID seed is bypassed?) I tried enumerating `HKLM\System\CurrentControlSet\Control\Video` **on the live prefix**, where a volatile real-GUID key written by explorer would be visible to any client joining the same wineserver. **All three queries returned zero bytes — including both controls** (C1 `DriverDesc`, predicted `Apple M1 Pro`; C2 our seeded `GraphicsDriver`, predicted `winemac.drv`, and that one is *persistent in system.reg* so it cannot legitimately be missing). `wine --version` against the same prefix returns `wine-11.0` rc=0, so the wine binary is fine; the query exits **rc=1 with 0 bytes on both stdout and stderr**. ⇒ `wine reg.exe` against a **busy live prefix** behaves differently from the throwaway prefixes `tools/hk_regseed_probe.sh` validated at ⑱, cause unknown. **Because the controls failed, the TEST result is void and I drew no conclusion from it — ㊴ remains an open [HYPOTHESIS].** I did not push further: the risk budget of poking the operator's 90-minute slot is not worth a measurement the offline explorer repro (㊶) answers better anyway.
  ㊹ **Live run verified unharmed after the probe** — game pid 84849 at 246.9% CPU, wineserver 84712 alive, mr-run 78190 alive. Nothing killed, no `wineserver -k`, no `pkill`. Disk 40 GB free (above the 30 GB stop line).
  ㊺ ⚠ **AND THE CLEANUP CHECK ITSELF LIED — sixth time today.** `ps -Ao args | grep -c 'reg\.exe'` reported **2 stray reg.exe processes**; a direct `ps -Ao pid,ppid,%cpu,etime,args | grep 'reg\.exe'` returned **nothing at all**. The 2 were the **shell wrapper's own argv**, which contains the command text — exactly ㉒'s documented failure ("the bracket trick does not help when the self-match is in the shell wrapper"), and this time it fired on a *counting* check rather than a launch guard. **Rule for this lane: a bare count is never evidence of a process; print the rows.**
15:50 · COORDINATOR — try9 RESULT: THE GraphicsDriver SEED CHANGED NOTHING, and two of my own hypotheses died with it (not a loop status) · I launched try9 (`laneA-INPUT-TEST-try9-GRAPHICSDRIVER-152010`) the moment the slot freed and the operator pressed keys at the language menu. **Verdict: negative, and cleanly so.** Stage-by-stage against try8, counting only real traces (`stage=X pid=N`, never the banner): `macdrv_init_entry` 0/0, `run_cocoa_app_entry` 0/0, `winemac_so_load` 0/0, `first_sharedApplication` 0/0 — and the stages that DO fire are identical in both: `macdrv_init_thread_data` 1/1, `create_cocoa_window` 1/1, `queue_create` 1/1, `set_queue_display_fd` 1/1, `onmainthread_enter` 10/10. Your pre-registration was honest and the answer is no; the seed is readable at the driver's exact path, as you measured at 15:12, and the driver still does not load. ★ MY OWN ERROR, LOGGED SO IT IS NOT REPEATED: I first reported `macdrv_init_entry = 1` as a breakthrough. It was `grep` matching **my own launcher banner**, which lists the stage names in prose. Exactly the shape you warned about at 14:50 — a number that agrees with the hoped-for outcome for entirely the wrong reason. Count `stage=NAME pid=` only. ★ TWO HYPOTHESES REFUTED CHEAPLY, both mine: (a) architecture — I suspected `system32/winemac.drv` was the wrong arch because the run syncs `x86_64-windows/winemac.drv` there while an `aarch64-windows/winemac.drv` also exists in dist. **Refuted:** the whole prefix is a coherent x86-64 world (`kernel32.dll`, `win32u.dll`, `user32.dll` in system32 are all PE32+ x86-64), so winemac.drv matches its neighbours. (b) `SessionGetInfo` hanging between entry and session — refuted, since entry never fires at all. ★ CONFIRMED INDEPENDENTLY, and it sharpens your 14:40 conclusion: `lsof` on the live game pid shows **exactly one winemac artefact open — `aarch64-unix/winemac.so`, the UNIX side. The PE `winemac.drv` is not mapped.** That explains the otherwise-contradictory stage list: `macdrv_init_thread_data` and `create_cocoa_window` are reachable from unix-side code (`keyboard.c:1064/1358/1409`, `clipboard.c:1367`) without the PE ever loading, which is why a partial driver appears alive while `macdrv_init` never runs. ★ THE THREAD I WOULD PULL NEXT, offline: `explorer.exe` **is named in the run log but no explorer process is alive** — it is started and does not survive. `win32u/driver.c:984` reads `GraphicsDriver` from exactly the key you seeded, so the value source is fixed; what is not fixed is whoever is supposed to come and ask for it. If the driver load is reached via desktop creation and the desktop never exists because explorer dies, the seed is necessary but not sufficient. Worth finding why explorer exits — that failure may be the single upstream cause of the null driver, and it is diagnosable from the existing run dirs without a slot.
16:10 · ★★★★ **ROOT CAUSE OF THE NULL USER DRIVER FOUND, AND IT IS NOT IN winemac.drv AT ALL: `explorer.exe` never reaches `main()` — it is KILLED inside `kernelbase.dll`'s `DllMain(PROCESS_ATTACH)` by a NULL ARM64X `.data` twin. Proven offline 9 runs + confirmed in the LIVE run, and a workaround that lives entirely in `artifacts/` removes it — after which explorer loads `winemac.drv` and EXECUTES `run_cocoa_app`.** Full report: `reports/research/HK-INPUT-ROOT-CAUSE-KERNELBASE-MUI-20260728.md`. Live title run never touched; no engine file modified; no commit, no `git add`.
  ㊻ ★★★ **THE DEFECT, every link read out of source.** `kernelbase/main.c:47` DllMain → `init_locale()` → the timezone init at `kernelbase/locale.c:5991/6639` calls `RegLoadMUIStringW(key,"MUI_Std"/"MUI_Dlt")` → `kernelbase/registry.c:2845 load_mui_string` → `LIST_FOR_EACH_ENTRY` over `reg_mui_cache` (`registry.c:82`) → at kernelbase RVA **0x5CCC8**: `ldr w8,[x25,#0x18]` with **x25==0** → `c0000005 info0=0 info1=0x18` → `ntdll/loader.c:10025` `process_attach(node_kernel32)` fails → **`NtTerminateProcess`**. Disassembled, not guessed: the loop loads the head, compares against `&head`, and never NULL-checks — because a `LIST_INIT`ed head *cannot* be NULL. **Why it is NULL:** `llvm-nm` shows `reg_mui_cache` **TWICE** — VA 0x180150ab0 and 0x1801525b0, **delta 0x1b00 = exactly `MACRUNNER_HB_LOCALE_ENTRY_EC_DELTA`** (`locale.c:703`). It is an ARM64X dual-`.data` twin and it is **NOT** in `macrunner_hb_sync_locale_ec_copies`'s mirror list (`grep -c reg_mui_cache locale.c` = **0**). This is the SECOND casualty of a class already fixed once — `locale.c:751` records the first: *"RegSetKeyValueW faulted on an unmirrored `entry_sintlsymbol` subkey pointer (kernelbase+0x59930)"*. ⚠ **A `memcpy` mirror would be WRONG here**: `LIST_INIT` is self-referential, so copying bytes leaves the twin pointing at the *native* address and `cmp x25,x24` never matches — the twin must be `list_init`ed **at its own address**.
  ㊼ ★★ **EVIDENCE — offline, deterministic, and it REFUTED MY OWN PRE-REGISTERED PREDICTION.** 15 process-attaches across 9 runs: `init_locale done` = **0**, 13 faults, **one single faulting address** every time. E2 carries the direct line `err:module:loader_init Initializing system dll for L"C:\windows\system32\explorer.exe" failed, status c0000005`. The failing module is named verbatim by ntdll's own env-gated bootstrap trace (`MACRUNNER_HB_TRACE_BOOTSTRAP=1`, already in the shipped binary — env only, no rebuild): `process-attach-after-initdll: module=L"kernelbase.dll" status=c0000005`, with `after-ntdll-attach` present and `after-kernel32-attach` ABSENT. ★ **I predicted the native aarch64 build would survive where x86_64 died. It does not** — aarch64 `wineboot.exe` (K2) and aarch64 `reg.exe` (R) die identically. **Architecture is NOT the variable**, so 15:40 ㊵'s "install the aarch64 explorer" candidate would NOT have worked; that is now closed by measurement instead of being carried forward as a hope.
  ㊽ ★★★ **LIVE CONFIRMATION IN THE RUNNING TITLE — AND IT NEEDED NO DEBUG GATE.** `MESSAGE()` is not suppressed by `WINEDEBUG=-all`, unlike `ERR_()`. In `laneA-INPUT-TEST-manual-language-try10-DESKTOP-155020` (the coordinator's run, read-only): **`process-attach` = 3, `init_locale done` = 1.** Ordered against the harness markers: L1253 game launches → L1260 attach #1 (never finishes, dies) → L1306 attach #2 → **L1310 `init_locale DONE`** (the survivor = the game) → L1397 attach #3, arriving *after* the game is up and never finishing — **that is explorer**, and it matches the independent watcher's live sighting (`pid 85189, etime 00:01`, gone by the next 1 s poll). **Two of three processes in the live run die exactly as they die offline.** This closes 15:40 ㊳'s complaint that the gate was shut: the decisive counters were readable all along on a channel nothing gates.
  ㊾ ★★★ **THE WORKAROUND — `artifacts/` ONLY, NO ENGINE CODE, AND IT WORKS.** `RegLoadMUIStringW` returns *before* `load_mui_string` unless the value exists (`registry.c:2959-2960`) **and** starts with `'@'` (`:2974-2977`). The prefix template carries **278** such values, all `"@tzres.dll,-NNNNN"` under `…\Time Zones\*`. Deleting them makes the faulting call unreachable. New template **`artifacts/hk-prefix-template-NOLANG-GRAPHICSDRIVER-NOMUI`** (system.reg 49501→49223 lines, delta exactly 278; `.valid` / `WINDOWS-ORACLE-IMPORT.json` / `user.reg` / `userdef.reg` carried over so the run contract's save snapshot still resolves). Paired A/B, predictions written first: **CTL (stock) `init_locale done`=0 faults=1 · FIX (NOMUI) `init_locale done`=2 faults=0.** Under FIX, wineboot and services.exe complete init and go on to load rpcrt4/setupapi/shell32/ole32/wininet instead of dying at 1 s.
  ㊿ ★★★★ **AND THE CHAIN COMES APART — `run_cocoa_app` EXECUTES.** Re-ran the A/B with `MACRUNNER_HB_X64_LOADER=1` added (the one env var my harness lacked; without it x86_64 PEs cannot start at all — `wine: ntdll export __wine_unix_call_dispatcher_arm64ec not found`). explorer.exe now launches in BOTH arms, so the template is the only difference. **A (stock): `init_locale done`=0, faults=2, winemac mentions=0, 22 loaddll lines**, with the verbatim `loader_init … explorer.exe failed c0000005`. **B (NOMUI): `init_locale done`=5, winemac mentions=4, 117 loaddll lines**, and the log contains `Loaded L"…\aarch64-windows\winemac.drv"`, `macrunner-ui-input: stage=winemac_so_load`, and a backtrace reading **`macrunner_sharedApplication_hook +200 ← run_cocoa_app +208`**. ⇒ **`winemac.drv` loads and `run_cocoa_app` RUNS** — the function this lane spent the entire day proving is never reached. It is reached the moment explorer survives kernelbase.
  ㊿b ⚠ **THE NEXT WALL, NAMED — a second, independent defect, and it is engine-lane.** Arm B then dies on: `code=c0000005 flags=0 addr=0x1119DC210 **info0=8**` (an EXECUTE fault) + `macrunner-hb-native-dispatch-boundary-reject: tid=0024 pc=0x1119DC210 **reason=not-x64-main-process**` + `arm64-unwind-unsafe-boundary … action=stop-unwind` → `NtRaiseException` unhandled → rc=5. Spawned helpers (explorer/services/rpcss) are not the x64 main process, so a native-dispatch callback in them is rejected and kills them. That is `macrunner_hb.c` = **FORBIDDEN to this lane**; cross-reference the existing repo-root `LANE-A-FIX-SPEC-native-dispatch-callback-exception.md`.
  ⓫ ⚠ **THREE OF MY OWN INSTRUMENTS LIED TODAY AND ONLY ARTIFACT CHECKS CAUGHT THEM — the count is now nine for the lane.** (1) `mkprefix` echoed its log line into the captured stdout, so `WINEPREFIX` became a two-line string and wine refused it — **every run "died at 1 s", looking exactly like the defect I came to measure**; caught ONLY because the `+loaddll` gate control read 0 when it must read ≥1. The C1 control earned its keep on first use. (2) `local tag="$1" pfx="…$tag"` — bash expands all `local` arguments **before** assigning any, so `$tag` was unbound under `set -u`; the script exited 1 having done nothing while reporting nothing. (3) **b5 was a bad test**: I checked the prefix `system.reg` for a `GraphicsDriver` value explorer had written — but explorer creates that key `REG_OPTION_VOLATILE` (`explorer/desktop.c:1059`), so it is *never* written to `system.reg`. Both arms showed exactly the 1 seeded value and it proves nothing. **This lane already recorded that exact trap at note-12 and I walked into it again.**
  ⓬ **Housekeeping + territory.** Live title run untouched throughout (verified by printed `ps` rows before and after every experiment, never a bare count); every teardown a prefix-scoped `wineserver -k`; **no `pkill`**; all throwaway prefixes removed (`artifacts/_mr-run.*` left with only the live run's own). Disk 38 GB free (above the 30 GB line). ⚠ The real-harness attempt `reports/phase4-hollow-knight/run-EXPLORER-REPRO-realharness.sh` (try9 clone → explorer) is **BLOCKED by the run contract** — `canonical_save_path_authority_absent`, because explorer is not the title; kept for reference, not usable as-is. ⚠ **The try9 slot ended and `try10-DESKTOP` (another agent: "virtual desktop + GraphicsDriver + saved language") started at 15:50:20, BEFORE my first experiment at 15:53 — my runs did not cause it** (mr-run's cleanup is `pkill -f "$PREFIX"`, scoped to its own throwaway prefix).
  ▶ **NEXT ITERATION:** ① **One title run on `artifacts/hk-prefix-template-NOLANG-GRAPHICSDRIVER-NOMUI`** (absolute path), gate open with `WINEDEBUG=+loaddll,+winediag` so a failure explains itself. Read in order: `init_locale done` (expect ≥2, was 1) → `stage=dllmain_attach` → `stage=macdrv_init_entry` → and **only then** ask the operator for a keypress. ② Expect ㊿b's `not-x64-main-process` reject to be the thing that still kills explorer — if so, input stays blocked until the engine lane fixes it, and that is now a *named* blocker rather than a mystery. ③ The kernelbase fix (`list_init` the `reg_mui_cache` twin at its own address, or a defensive `if (!head->next) list_init(head)` in `load_mui_string`) is **outside this lane's territory** (`winemac.drv/**`, `tools/**`, `reports/**`) and is deliberately NOT applied. ④ Sweep the class rather than fixing a third item one crash at a time. ⑤ Operator force-add list unchanged, plus this report.

LOOP-STATUS: BLOCKED
Blocked on two operator decisions: the fix belongs in `kernelbase`/`macrunner_hb.c`, both outside this lane's declared territory, and the decisive verification needs one title slot on the NOMUI template while another agent's try10 currently holds it.
16:14 · COORDINATOR — ★★★★ MECHANISM CLOSED END TO END: `is_service_process()` SHORT-CIRCUITS THE DRIVER LOAD. try10 (desktop seed) also negative. · try10 (`laneA-INPUT-TEST-manual-language-try10-DESKTOP-155020`) seeded a virtual desktop (`HKCU\Software\Wine\Explorer` `"Desktop"="Default"`, `Desktops\"Default"="1024x768"`) ON TOP of your GraphicsDriver seed, with the saved language restored so the boot passes the language gate on its own. Result: `macdrv_init_entry` 0, `winemac_so_load` 0, `run_cocoa_app_entry` 0, and `lsof` on the live pid again shows only `aarch64-unix/winemac.so` with **no PE `winemac.drv` mapped**. Seventh hypothesis refuted. (Two side-observations worth keeping: the registry fix works — `Loaded saved language code` = 1, the game passed the language screen unaided; and CPU on the opening screen was **14.3%** here versus 180-270% in earlier runs, which is a real difference and may be a desktop-seed side effect worth a separate look.) ★ THE ACTUAL MECHANISM, read from the code rather than inferred: `win32u/driver.c:1031` is `if (is_service_process() || !load_desktop_driver( get_desktop_window() ) || ...)`. **`is_service_process()` is FIRST in the `||`, so when it is true `load_desktop_driver` is never called at all** — the `GraphicsDriver` value is never read, which is exactly why seeding it changed nothing, and why it changed nothing again with a desktop present. And `winstation.c:373-380`: `is_service_process()` returns true iff the process window station is named **`__wine_service_winstation`**. ★ FULL CHAIN, now closed with no gaps: game lands on the service window station -> `is_service_process()` true -> `load_desktop_driver` short-circuited -> no user driver selected -> null driver -> PE `winemac.drv` never loaded -> its DllMain never runs -> `macdrv_init` never called -> no Cocoa app -> `-sendEvent:` never called -> **no keyboard and no mouse, for one shared reason**. This explains every observation we have, including the ones that looked contradictory: the picture is fine because DXMT/Metal renders past the driver entirely; the event queue exists and takes thousands of posts because the unix-side `.so` is loaded and its own paths run; and all seven earlier hypotheses failed because every one of them sat downstream of this edge. ★ THE REMAINING QUESTION IS NARROW AND LIKELY OURS: why does the game process end up on `__wine_service_winstation`? `mr-run.sh` explicitly starts `services.exe` (:505) and `rpcss.exe` (:523) before launching the title at :646, so inheritance from that startup is the first thing to check. Instrument `NtUserGetProcessWindowStation` / the station name at process start, or simply print what `is_service_process()` returns — one line, and it converts this from a strong inference into a measurement. If it is inheritance, the fix may be in how we launch rather than in any driver code.
16:50 · COORDINATOR — try11/try12 NEGATIVE: `services.exe` IS NOT THE SOURCE OF THE SERVICE STATION. And a process correction I am making on myself. · try12 (`laneA-INPUT-TEST-manual-language-try12-NOSERVICES-163630`) is the run that REACHED THE MAIN MENU with exactly one variable changed: `MACRUNNER_MR_RUN_START_SERVICES=0`. Result at +10 min: `macdrv_init_entry` 0, `winemac_so_load` 0, `run_cocoa_app_entry` 0, and `lsof` still shows only `aarch64-unix/winemac.so` with no PE `winemac.drv`. So the short-circuit at `driver.c:1031` is real and correctly described, but `services.exe` is not what puts the process on `__wineservice_winstation`. ★ ONE USEFUL SIDE RESULT: **turning services.exe off cost nothing** — 0 RPC errors, no `RPC_S_SERVER_UNAVAILABLE`, no OLE breakage. The comment at `mr-run.sh:513-518` warns that without it `ncalrpc:[irpcss]` is missing and the guest's first CoInitialize dies; that did not happen here. Worth knowing before anyone treats that startup as load-bearing. ★ ALSO KILLED: try11 was scrapped before it could mislead — I had built it from try10 and it carried the desktop seed, so it varied TWO things against the baseline. And the desktop seed itself is a REGRESSION: try10 differed from the menu-reaching run only by that template and stopped earlier (`Performing automatic level start.` 0 vs 1, no `Loaded Objects now: 47240`, CPU 14% vs 180-270%). Do not carry it forward. ★ THE CORRECTION THAT MATTERS, and it is about method, not this bug: **I have now spent eight hypotheses and roughly eight 40-minute runs guessing at why the station is what it is, instead of measuring it.** GA_ROOT, focus, sessionHasGraphicAccess, SessionGetInfo hang, winemac.drv architecture, the GraphicsDriver seed, the desktop seed, services.exe — all refuted, all downstream or adjacent, none measured first. The station name is knowable with **one line**: print `NtUserGetObjectInformation(NtUserGetProcessWindowStation(), UOI_NAME, …)` and `is_service_process()` at process start, ungated, next to your existing `macrunner-ui-input` stages. That single print turns this from a guessing game into a read. It is exactly the discipline I have been pressing on every lane today and did not apply to myself. ★ REQUEST: add that one print. Until it exists I am not going to spend another title slot or another minute of the operator's attention on a ninth guess — he has tested interactively five times today and deserves a measurement, not another maybe.

17:0x · COORDINATOR — ★★★★ **THE LANE WAS RIGHT AND I WAS WRONG; THE kernelbase FIX IS NOW APPLIED.** My eight-hypothesis
`is_service_process()` chain is **withdrawn**. It rested on a measurement artifact: I read "PE `winemac.drv` absent from `lsof`"
as "never loaded", when Wine maps x86-64 PEs into anonymous memory on ARM64 — so PE absence proves nothing, while the mapped
`aarch64-unix/winemac.so` *is* proof the PE ran (`dllmain.c:537 __wine_init_unix_call()` is the only thing that loads it). And every
`macdrv_init_entry = 0` I quoted came from a trace gated behind `MACRUNNER_TRACE_WINEMAC_INPUT`, which I never set — zero meant
"printer off", not "code did not run". That is the exact `не залогировано ≠ не произошло` gate I have been enforcing on the lanes.
**FIX APPLIED** (`engine/wine/dlls/kernelbase/registry.c`): `reg_mui_cache_ensure_init()` lazily `list_init()`s the head, called
from `dump_mui_cache` / `reg_mui_cache_get` / `reg_mui_cache_put`. Deliberately NOT a `memcpy` entry in
`macrunner_hb_sync_locale_ec_copies` — a list head is self-referential, so mirroring bytes would leave the twin pointing at the
NATIVE head and the compare against the EC `&head` would never match. Lazy init is correct from whichever `.data` view runs first
and needs no cross-module plumbing. **NOT BUILT**: a title run is live (pid 58487), and a full `build-wine-arm64ec-spike.sh` would
also compile three other lanes' uncommitted `engine/wine` edits into the artifact — that is exactly how the mixed `ntdll.so`
(99e1e634) happened. `scripts/muicache-fix-finish-when-slot-free.sh` is written and executable but **deliberately not armed**;
it waits for the slot, builds, and verifies kernelbase.dll changed by SHA before claiming anything.
**TERRITORY GRANTED** to this lane, on my authority as coordinator: `engine/wine/dlls/kernelbase/**` is now YOURS for the
`reg_mui_cache` class sweep you asked for in ④. `engine/wine/dlls/ntdll/unix/macrunner_hb.c` stays FORBIDDEN — the
`not-x64-main-process` native-dispatch reject is engine-lane work and is being handed over separately with
`LANE-A-FIX-SPEC-native-dispatch-callback-exception.md`.
**DO NOT re-derive**: the station/`services.exe`/desktop-seed branch is closed. try11/try12 were negative, and the desktop seed is
a REGRESSION — try10 stalled before `automatic level start`. Drop that template.

18:30 · COORDINATOR — ★★★★ **INPUT BLOCKER IS FIXED AND THE FIX IS REPRODUCIBLE (4 runs).** The `reg_mui_cache`
lazy-`list_init` in `kernelbase/registry.c` is built and deployed (kernelbase.dll 092fbb9f -> 5b6139d5, artifact verified
changed by SHA, 0 build errors). With it, on the STOCK template and with NO prefix hackery: `init_locale done` 0 -> 4,
`macdrv_init_entry` 0 -> 1, `macdrv_init_start_cocoa_call` 0 -> 1, `run_cocoa_app_entry` 0 -> 1,
`run_cocoa_app_created nsapp=… class=WineApplication`, `macdrv_init_session … graphic=1`. **The tell is `queue_get`: 60+ per
run now, and ZERO in every earlier run** — events were being posted and nobody was draining them. The NOMUI prefix
workaround is no longer needed; the defect is fixed at source.
**THREE PROCESS ERRORS OF MINE, recorded so nobody repeats them.** (1) `exit=53` is the KNOWN xtajit64-c0000135 boot flake,
documented in `laneA-run-hk.sh`'s own header, and that wrapper retries it — I called `mr-run.sh` directly, lost the retry,
and read three flakes as a regression. (2) A slot check that greps `ps -Awwo args` matches THIS SCRIPT'S OWN command line;
it read "busy=2" against an empty machine. Count on `comm` instead. (3) I hand-rebuilt try12's env into
`scripts/hk-run-try12-config.sh` and pre-set `MACRUNNER_DXMT_ROOT`/`FLIGHT_RECORDER_PATH`, which mr-run DERIVES — the
contract records the FINAL child env, so those values are outputs, not inputs. Result: `d3d11: failed to create device
(887a0002)`. On the unmodified `laneA-run-hk.sh` config graphics is clean (`d3d11_fail=0`).
▶ **NEXT WALL, measured once (try18):** with the Cocoa loop up the game reaches `Restored language` + `Unloading 5 Unused`
and then a Mono thread dies — `macrunner-hb-run-exit status=c000007b` (STATUS_INVALID_IMAGE_FORMAT) at
`mono-2.0-bdwgc.dll+0x4ea08f`, alongside `virtualalloc_ok=130 virtualalloc_fail=12`. That is the managed/engine frontier,
not input. try19 is in flight with 6 retries.

19:00 · COORDINATOR — ★★★ **A 30-SECOND REPRODUCTION, AND IT IS NOT HOLLOW KNIGHT.** `winecfg`, launched through the plain
`dist/bin/wine` with `MACRUNNER_TRACE_WINEMAC_INPUT=1`, shows the IDENTICAL signature to HK: `macdrv_init_entry`=6,
`run_cocoa_app_entry`=6, `queue_get`=62 (the Cocoa loop IS up) and `APP_ACTIVATED`(0)=0, `WINDOW_GOT_FOCUS`(29)=0,
`KEY_PRESS`(8)=0, `MOUSE_BUTTON`(12)=0; frontmost stayed Finder. **Stop debugging input with 45-minute title runs.**
**Event decode on HK try20, operator pressing keys throughout:** 2346 x type3 CLIENT_SURFACE_PRESENTED (frames ARE being
presented), 4 x 28 WINDOW_FRAME_CHANGED, 2 x 29 WINDOW_GOT_FOCUS, and ZERO of types 8/9/12/13/14. The window takes focus
when clicked; the PROCESS never becomes the active macOS application, and macOS delivers keys only to the active app.
**Named cause, already in the tree.** cocoa_window.m ~3788 carries a prior lane's comment: "an explicit
NSRunningApplication activation request was accepted but not honored" and "The controller foreground-transform helper is
NOT usable here: its NSMenu initWithTitle: throws on a bundle-less process (bundleName == nil)".
`tryToActivateIgnoringOtherApps:` (cocoa_app.m:2276) bottoms out in `activateIgnoringOtherApps:`, which modern macOS
cooperative activation ignores.
**Bundle experiment — DEAD END, do not repeat.** Built `artifacts/MacRunnerWine.app` (real binary copy +
Contents/{lib,share,bin} symlinks; `wine --version` works from it) and added `WINE="${MACRUNNER_WINE_BIN:-...}"` at
mr-run.sh:57 so it can be selected. It cannot help alone: `loader.c:678 create_tempdir` re-execs the loader from
$TMPDIR/winetemp-<inode>-<size>-<mtime>/<exename>, so the final process image is outside any bundle no matter what was
launched. A bundle would have to be built INSIDE that tempdir. The override is kept because it is additive.
NEXT: iterate on the winecfg harness, not the game. Narrow question: what makes a Wine process become the ACTIVE macOS
app on this OS version. `System Events` cannot even see the process ("Can't get process whose unix id = ..."), which is
itself a measurement worth explaining before any more activation code gets written.
