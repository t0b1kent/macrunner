# LANE HK-MANAGED — progress journal

12:35 · session start · dnfile+dncil present; building IL reader; target strings located next
12:52 · IL reader working (tools/hk_managed_il/hk_il.py) · all boot strings located in Assembly-CSharp.dll + TeamCherry.Localization.dll · dump gates next
13:05 · Q1 decoded · <Start>d__25.MoveNext (0x060041d2) = 4 sequential poll gates; IsPlayerPrefsLoaded hardcoded true on desktop (0x06001071: ldc.i4.1;ret) · park = while(!confirmedLanguage) yield null
13:12 · Q2 decoded · GameLangSet written only by ConfirmLanguage (0x06000e6c), read only by CheckIsLanguageSet (0x06000e69); saved/system branch (RestoreLanguageSelection) is cosmetic — zero gate influence, explains run15
13:18 · Q3 resolved · menu shows iff PlayerPrefs.GetInt("GameLangSet",0)==0; run10 menu + "Loaded system language" (saved=0 in run6/8/10/12) prove PlayerPrefs reads miss template registry values · boundary = native PlayerPrefs→registry read
13:24 · Q4 answered · release = public void StartManager::ConfirmLanguage() (0x06000e6c), zero IL callers = UI-event-only; mono_runtime_invoke from HighlightDefault is the actionable path
13:26 · 4 reports written · reports/phase4-hollow-knight/HK-MANAGED-Q{1,2,3,4}-*-20260727.md + IL dumps in managed-decompile/
LOOP-STATUS: GOAL — Hollow Knight is waiting on `StartManager.confirmedLanguage` (field 0x04000DE2), polled per-frame by `<Start>d__25.MoveNext` (token 0x060041d2, IL_00da: `while(!confirmedLanguage) yield return null;`); satisfied only by `public void StartManager::ConfirmLanguage()` (token 0x06000e6c, zero IL callers = UI-click-only, so mono_runtime_invoke from the HighlightDefault trigger is the path). The menu appears because `PlayerPrefs.GetInt("GameLangSet",0)` returns 0 despite the template registry carrying the value — "Loaded system language" (not "saved") in run6/8/10/12 proves PlayerPrefs.HasKey misses too; the native PlayerPrefs→registry read is the boundary for the engine lane. Reports: reports/phase4-hollow-knight/HK-MANAGED-Q1-PLAYERPREFS-WAIT-PATH-20260727.md, -Q2-LANGUAGE-GATE-MAP-, -Q3-MENU-CONTRADICTION-, -Q4-SATISFY-THE-WAIT-.
14:34 · round2 start · tooling live; locating 'Restored language code' emitter
14:20 · R2-Q1 decoded · "Restored language code" = TCL Language::LoadLanguage (0x06000008) IL_0012, called from Language..cctor on FIRST access — pre-gate, not gate-pass evidence
14:25 · R2 premise corrected · parked run12 HAS the triple (+374s, log lines 1815-1819) and still parked; runs 14/15/16 have none (stalled pre-Language-access) — string discriminates idx38+2s, not gate pass
14:30 · R2 run17 resolved · actuator invoked SetLanguage (+939s, 435s inside) then ConfirmLanguage (+1374s, status=ok) → idx40 +1425s → level start +1550s; round-1 path worked, no contradiction
14:35 · R2-Q2 predicate · menu iff !CheckIsLanguageSet() && Platform.ShowLanguageSelect; on desktop reduces to PlayerPrefs/registry GameLangSet read==0 (DesktopPlatform.ShowLanguageSelect=const true 0x06000f82; poll skipped entirely on path A)
14:40 · R2-Q3 scan · single stfld confirmedLanguage writer (ConfirmLanguage IL_005e) across 4 assemblies; zero UTF-16 name literals (no IL reflection); wiring = serialized UnityEvent in level0
14:45 · R2-Q4 post-gate · LanguageSettingDone coroutine is SILENT (fade+SetActive(false)+SaveConfig); first post-gate log = idx40 "Didn't need to wait..."; then allowSceneActivation → Menu_Title → level start
14:52 · R2 report written · reports/phase4-hollow-knight/HK-MANAGED-R2-RESTORED-LANGUAGE-GATE-BYPASS-20260727.md + 3 new IL dumps in managed-decompile/
LOOP-STATUS: GOAL — Round 2 resolved with code behind it. (1) `Restored language code 'EN'` is emitted by `TeamCherry.Localization.Language::LoadLanguage` (TCL.dll 0x06000008, IL_0012) and in run17 it came from the `Language..cctor` (0x06000007) on FIRST class access at +408s — ~2s after idx38, BEFORE the confirmedLanguage gate; it sets nothing on the gate path (no confirmedLanguage/GameLangSet writes anywhere in the TCL restore path). The brief's premise was wrong: parked run12 logged the same triple at +374s and still parked — the string marks "first Language access", not "gate passed". (2) run17 passed because the actuator invoked `StartManager.SetLanguage("EN")` (+939s) and `StartManager.ConfirmLanguage()` (+1374s, status=ok) — round 1's prescribed mono_runtime_invoke path; idx40 followed at +1425s, level start +1550s. No contradiction. (3) Menu predicate: `!CheckIsLanguageSet() && Platform.Current.ShowLanguageSelect` = on desktop exactly "PlayerPrefs→registry GameLangSet read returns 0" (DesktopPlatform.ShowLanguageSelect is const true, 0x06000f82); when false, the boot SKIPS the confirmedLanguage poll entirely (only menu path polls). (4) confirmedLanguage has exactly one writer (ConfirmLanguage IL_005e) in all 4 game assemblies; no IL reflection (zero UTF-16 name literals); trigger wiring is a serialized UnityEvent in scene level0. (5) Post-gate: LanguageSettingDone coroutine is silent (fade menu, SetActive(false), ConfigManager.SaveConfig) — first post-gate log is idx40 "Didn't need to wait for PlayerPrefs load.", then allowSceneActivation=true and Menu_Title activation; ~50s invoke→idx40 is normal. Report: reports/phase4-hollow-knight/HK-MANAGED-R2-RESTORED-LANGUAGE-GATE-BYPASS-20260727.md
