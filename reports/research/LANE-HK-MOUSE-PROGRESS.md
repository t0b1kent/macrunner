03:41 · BACKEND FAILOVER · `codex` retired after 2 consecutive returns under 180s with no journal line; continuing on `claude`.
03:45 · iter3 start · prior lane claimed GOAL (hid8/hid9 `VERDICT mousemove=5 lbuttondown=3 lbuttonup=3`), but the dist was REDEPLOYED at 03:38 by the e2e lane — deployed `winemac.so eac936dc…` / `win32u.so 7fdb2786…` differ from the SHAs the positive was measured on. Re-measure required, not optional.
03:45 · fixed a live hazard in `tools/winmouse_run.sh`: `wine_pids()` matched EVERY lane's `winetemp-` proc (comment claimed otherwise); its teardown would have killed the e2e lane's in-flight run (foreign pids 34529, 97003). Now snapshots foreign pids before launch and never touches them.
03:45 · rebuilt `winmouseprobe.exe` (was missing from /tmp) sha `b74539f8…`. SLOT BUSY: Hollow Knight live fullscreen pid 96301 @207% CPU — a `.cghidEventTap` post would land in their run. Waiting for the slot before injecting.
03:52 · staged and blocked on slot only. Verified the foreign-pid fix (with the live snapshot `wine_pids()` returns EMPTY, so no foreign kill; with a fake snapshot it still finds ours). Harness now also stamps the deployed winemac/win32u/probe SHAs into run.log so a counter can never again be read against an unknown build. Oracle binary + probe exe both present. Waiting on HK pid 96301 (08:51 elapsed, 207% CPU) to release the screen; a hid-tap post while it is fullscreen would either be refused or land in the e2e lane's run.
04:0x · iter4 · SLOT RE-EVALUATED, not blocked. Read-only window list: HK pid 96301 owns a FULLSCREEN layer-0 window (0,0 1512x982, z16) but is NOT frontmost (frontmost = Claude pid 28023). Re-read `mouse_inject.swift:272-381`: the guard derives occluders from windows strictly IN FRONT of the target, screen-captures the aim point to confirm the visible pixel is ours, then re-checks topmost and `exit(3)`s otherwise. A hid post therefore CANNOT reach HK: if our probe is above HK the WindowServer routes to us, and if it is below, the guard refuses. Proceeding to measure instead of waiting out HK's 45 min.
03:55 · iter4 · injector RE-VALIDATED on the live desktop with HK fullscreen on screen: oracle `btnWithWindow=6 btnNilWindow=0 viewDown=3 viewUp=3 viewMoved=9`, aim point verified ours, HK sat at z2 behind it. Guard confirmed sound by reading `mouse_inject.swift:272-381` (occluders taken only from windows in front of target; screen-capture confirms the visible pixel is ours; `exit(3)` otherwise).
03:55 · iter4 · first wine run on the REDEPLOYED build (`winemac.so eac936dc` / `win32u.so 7fdb2786`) = NO MEASUREMENT, not a negative: `WINDOW NEVER APPEARED (polls=70)` and ZERO `winmouseprobe:` lines in run.log. Cause identified: the 03:38 redeploy changed the wine build, so wineboot ran a full prefix UPDATE (`system.reg` rewritten 03:53, `.update-timestamp` 03:52) and ate the whole 70s poll budget before the probe exe ever started. Driver itself is healthy on the new build — `run_cocoa_app_entry=3`, `first_sharedApplication=3`, `create_cocoa_window=8`. Prefix now warm; re-running with secs=90.
03:57 · iter4 · SLOT FREE — HK pid 96301 exited on its own (was 204% CPU / ~14 min). The injection-safety question is now moot for this iteration; the oracle validation above was nevertheless run with HK fullscreen on screen and the guard held.
04:05 · iter4 · two instrument bugs found and fixed, both of which turn a REFUSAL into a fake zero.
  (a) `WS_EX_TOPMOST` is NOT usable as an aiming aid: with it set the probe window never appeared in CGWindowList at ANY layer across 70 polls (run `mouse-guest-iter4c-topmost`, exstyle=0x108 confirmed applied, guest-side hwnd valid/visible=1/fg=1), where the identical normal-level window registers at layer 0. winemac.drv maps it to `state->floating` (window.c:257). Kept behind an argv flag; [HYPOTHESIS] the floating path loses the window — a winemac.drv bug worth its own chase, not this lane's goal.
  (b) new `mouse_inject --free-spot WxH` picks the probe's window origin from the live window list. Two real bugs in writing it: summing intersection AREAS double-counts overlapping windows so every score fell below the initial best (silently returned the 120,120 default); and counting ALL layer-0 windows as occluders gives uncovered=0 everywhere because AutoCAD is fullscreen, when the measured z-order puts the probe at index 1, above everything except the frontmost app. Now scored by point-sampling against the frontmost app's windows only: origin 968,600 uncovered_frac=0.457.
Rebuilt: mouse_inject sha c23a138f6b4e1e20, winmouseprobe.exe sha 46bcd529b3043596. Runs 4b/4c produced NO mouse measurement (4b refused, 4c never registered) — neither is evidence about the mouse path.
04:12 · iter4 · **GOAL — re-measured on the redeployed build, twice.** `winemac.so 6fabe6759ef00b7a` / `win32u.so 7fdb27860bd05504` (both 03:57), probe `46bcd529b3043596`, injector `c23a138f6b4e1e20`.
  · run `mouse-guest-iter4d-freespot`: `VERDICT mousemove=6 lbuttondown=3 lbuttonup=3`
  · run `mouse-guest-iter4e-confirm`:  `VERDICT mousemove=7 lbuttondown=3 lbuttonup=3`
  · coordinates CHECKED not asserted: injected `1410.7,705.55` → guest client `438,75` → guest's own `ClientToScreen` = `1410,705`; window CG bounds `972,598 512x358`, so `972+438=1410` and `598+32+75=705`.
  · stages both runs: `app_sendEvent_enter=6 handleMouseButton_enter=6 decision=6 post=6 no_post=0 macdrv_mouse_button=12 send_mouse_input=75`; decision line carries `window=0x1005e6a10 process=1`, i.e. a real WineWindow.
  · NO winemac.drv/cocoa change was needed — the driver path was already correct. What changed is the instrument: `mouse_inject --free-spot` now picks the probe's window origin from the live window list (968,600) instead of hoping a strip is left exposed at 120,120. The 03:54 refusal (`blocked_not_topmost`, exit 3) was the operator's window covering the probe, not a mouse failure.
  · Report: `reports/phase4-hollow-knight/HK-MOUSE-GUEST-DELIVERY-VERIFIED-20260729.md`.
LOOP-STATUS: GOAL
