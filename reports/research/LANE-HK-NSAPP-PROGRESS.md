20:14 · BACKEND FAILOVER · `codex` retired after 2 consecutive returns under 180s with no journal line; continuing on `claude`.
20:17 · Reproduced the defect on the winecfg probe (40s, not a 45min title run): 8 Cocoa apps, `app_sendEvent_enter` = **0**.
20:18 · `run_cocoa_app_decide` reads `created_app=1 success=1 already_entered=0 will_run=1` on every app; `nsapp_run_returned` = **0**. `[NSApp run]` is entered and does not return — matches the brief.
20:23 · **`sample` of a live Cocoa process REFUTES the "AppKit is not pumped" framing.** Main-thread stack, 920/920 samples: `__wine_main → CFRunLoopRun → __CFRunLoopDoSource0 → run_cocoa_app (winemac.so) → -[NSApplication run] → nextEventMatchingMask: → _DPSNextEvent → _BlockUntilNextEventMatchingListInMode → ReceiveNextEventCommon → mach_msg`. `com.apple.NSEventThread` EXISTS (thread 28871429). AppKit is healthy and correctly blocked waiting for events. The nesting inside a CFRunLoopSource callback is NOT the problem.
20:26 · **ROOT-CAUSE CLASS FOUND, measured from outside the process** (`tools/ls_activation_probe.swift`): the live Cocoa process reports `activationPolicy=prohibited active=false`. macOS delivers **no key events** to a Prohibited-policy app and it can never become active — that alone explains zero `app_sendEvent_enter`, zero `applicationDidBecomeActive`, and zero KEY_PRESS.
20:26 · `tools/cg_window_list_probe.swift`: the process's only WindowServer window is `onscreen=false`. So on this probe the window was never ordered in.
20:30 · Build trap confirmed exactly as the brief warned: `make -C dlls/winemac.drv winemac.so` reports success and ships the OLD binary (`make: 'winemac.so' is up to date`, SHA unchanged). Must build from the TOP of `build-arm64ec-spike`. Rebuilt + deployed, SHA-verified `ed124f0c…`.
20:31 · Instrumented the only path that lifts the policy — `transform_to_foreground_entry`, `transform_to_foreground` (logs `setActivationPolicy:` **return value**, which upstream discards), and `order_in` in `-[WineWindow orderBelow:orAbove:activate:]`.
20:35 · Probe runs flaking on `err:virtual:try_map_free_area mmap() Cannot allocate memory` at the top of the guest address space (another lane's arena placement; swap 3.0/4.0 GB used). Wrapped launch+wait+probe+scoped-kill into `tools/nsapp_probe.sh` with retries so a flake no longer measures nothing.
20:45 · **My instrumented `winemac.so` (`ed124f0c…`) does not LOAD** — `winemac_so_load`=0, `macdrv_init_entry`=0, session dies just after `dllmain_attach`, 4/4 attempts. Reverted sources rebuild **byte-identically** to `fd29b8ef…` (so the top-level make IS the right build path) and that binary succeeds on attempt 1. Not the mmap noise — that is present in the *working* runs too (17× in probe6). REVERTED and redeployed `fd29b8ef…`. Unexplained; bisect one trace at a time next.
20:50 · Probe honesty check: in EVERY probe run the only process that loads winemac is `explorer.exe`, and its 4 windows are wine's internal shell windows (hwnd 0x1003c/0x10046/0x10048/0x1004a) that are legitimately never ordered in. `winecfg` and `notepad` (native ARM64 PE) never reach `macdrv_init_entry` at all. So the probe reproduces `app_sendEvent_enter=0` but CANNOT yet promote `prohibited` from lead to root cause — that needs a process with an on-screen window.
20:55 · **★ `MACRUNNER_WINE_BIN` trap — affects EVERY lane.** `mr-run.sh:56` sources `config/env.sh` itself; env.sh:93 sets `MACRUNNER_WINE_BIN` with `:=`, so `mr-run.sh:65`'s `${MACRUNNER_WINE_BIN:-$DIST/bin/wine}` fallback can NEVER fire and every run uses the **May 13** `engine/wine/dist` regardless of the dist argument. Unsetting in the caller does not help (mr-run re-sources). Measured back-to-back on HK: May-13 binary → **ladder rung 1**, correct spike binary → **rung 8**. Check `final-child.json` → `argv.entries[0].value` before believing any `LADDER_REGRESSION`. Fix belongs in env.sh/mr-run.sh = outside this lane's territory → flagged for the coordinator.
20:58 · HK title runs ×3: (1) `laneA-run-hk.sh` bare → ledger `BLOCKED exit before wine` (3× `branch_input_absent`; use `scripts/hk-run-try12-config.sh` instead — it replicates the run that reached the main menu today); (2) try12 + the wine-bin trap → rung 1; (3) try12 + correct binary → **rung 8 (gfxdevice), exit 1 at ~60s**. HK never created its own game window, so the decisive measurement — HK's `activationPolicy` while rendering — is STILL OPEN.
20:58 · Report: `reports/phase4-hollow-knight/WHY-DOES-A-RUNNING-NSAPP-RUN-DISPATCH-NO-EVENTS-20260728.md`. Next: bisect the trace-induced load failure → get any wine window on screen → then `tools/hk_activation_watch.sh` (carries the wine-bin workaround) for the one number that settles it.
21:11 · **Both brief hypotheses REFUTED by a 3-second standalone experiment** (`tools/nsapp_nest_repro.m`, no wine, no guest): `[NSApp run]` entered from inside a CFRunLoopSource perform callback on top of `CFRunLoopRun` — wine's exact shape — gives `setActivationPolicy ret=1 / policy=regular / isActive=1 / applicationDidBecomeActive=1 / sendEvent=6`, identical to the direct control, with `bundleid=(nil)`. Nesting is fine and bundle-lessness is fine. AppKit was never the problem.
21:16 · **The winecfg probe cannot reproduce the defect and never could.** Four runs: the only guest reaching `macdrv_init` is explorer, whose 4 shell windows are styles `0x4c80000/0x8c000000/0x84000000/0x84000000` — **none WS_VISIBLE**, so `show_window` is never called and `app_sendEvent_enter=0` there is trivially true. Built the missing instrument: `tools/winkeyprobe.c`, one WS_VISIBLE overlapped top-level window + message loop + counters, `winegcc --target=aarch64-windows`, ~40s per run.
21:20 · winkeyprobe reproduces it exactly: `visible=1 foreground=1(self=1) focus=1 active=1` and **keydown=0 keyup=0 mousemove=0 for 40s**. Its own hwnd `0x10052` gets **no `create_cocoa_window`** — all 4 belong to explorer. In-guest introspection: `WinSta0`, `WSF_VISIBLE=1`, `cxscreen=1512`, `ga_parent==desktop`, `ga_root==self`, `iswindow=1` → **`GA_PARENT==0` and `is_service_process` both REFUTED for the app's own window.**
21:28 · Ungated counter at the top of `macdrv_WindowPosChanging`: **8 calls from explorer, 0 from the app process** — the entry point is genuinely never dispatched there, not merely untraced. (The instrumented `winemac.so` loaded fine, so iteration 1's "instrumented build does not load" did not reproduce.)
21:34 · **ROOT CAUSE, captured live in one run.** Ungated traces at both install sites in `win32u/driver.c`, app process, one thread (tid `0024`): `load_display_driver_reentrant` (installs the SILENT `null_user_driver` — `pCreateWindow` left as `nulldrv_CreateWindow`, hence no winediag ERR and CreateWindow succeeds) → `macdrv_init_user_driver_set` (real driver arrives) → `load_display_driver_ok`. `__wine_set_user_driver`'s CAS (`driver.c:1491`) only installs while `user_driver == &lazy_load_driver`, so **the real macdrv funcs are `free()`d and silently discarded**; the process runs on the null driver for life. No `win_data`, no `WineWindow`, no Cocoa queue ⇒ input structurally impossible, while win32u still reports visible/foreground/focus/active. `macdrv_init_user_driver_set` prints *upstream* of the CAS — which is why the lane trusted it for days.
21:40 · **FIX landed + verified.** `win32u/driver.c`: record the re-entrancy placeholder, retry the CAS against it so a real driver wins; driver-vs-driver races still first-wins. Before→after on the app process: `WindowPosChanging` 0→8, `create_cocoa_window` for `0x10052` 0→1, WindowServer window none→`onscreen=true 512x358`, policy `prohibited`→**`regular`**, `app_sendEvent_enter` 0→**6**, `queue_post` **KEY_PRESS(8) 0→5** / KEY_RELEASE(9) 0→5, guest **WM_KEYDOWN/WM_KEYUP/WM_CHAR 0→5/5/5**. Keys injected with `CGEventPostToPid` (no human needed). SHA-verified: `win32u.so e96c0e8c…`, `winemac.so 9380e71e…`.
21:42 · Not proven, stated plainly: **HK itself has not been re-run** (fix is proven on winkeyprobe; one title run away). **Mouse is not proven** — synthetic `CGEventPostToPid` events carry `window=0x0`, so `handleMouseButton_decision` never fires; keyboard only. Also: `app_sendEvent_enter` is a **mouse-button-only** counter (`cocoa_app.m:92` early-returns on every other event type) — it read 0 in the *fixed* build too until a click was injected, so the lane's headline metric never meant "zero NSEvents dispatched". Report: `reports/phase4-hollow-knight/WHY-NO-NSEVENT-APP-PROCESS-NEVER-GETS-THE-MAC-DRIVER-20260728.md`. **Coordinator flag:** the fix is in `win32u/driver.c`, outside this lane's stated territory (file was clean in git; the other win32u lane holds `input.c` only).
LOOP-STATUS: GOAL
