LANE-HK-TWINSWEEP — ARM64X dual-.data twin sweep

2026-07-28T10:15:22Z iter1 step1: lane start; progress file was empty. Goal = derive true twin set from shipped kernelbase.dll, diff vs mirror list, rank reachability, classify memcpy vs init-in-place.
2026-07-28T10:19:20Z iter1 step2: derived twin set from engine/wine/dist-arm64ec-spike/lib/wine/aarch64-windows/kernelbase.dll sha b7e96fb9 (IMAGE_FILE_MACHINE_ARM64X, single .data RVA 0x14F000 vsz 0x8678) via llvm-nm: 301 data syms, 141 twin pairs in 4 strictly non-overlapping zones (EC .data 14f030-150b18 / native .data 150b28-152618 delta 0x1b00|0x1af8; EC .bss 152638-154d00 / native .bss 154e20-1574e8 delta 0x27e8; .mrdiag pair delta 0xc). Zone-disjointness = the twin-vs-name-collision discriminator; 0 collisions. Mirror list = 57 entries, all real twins, all deltas correct => 84 unmirrored.
2026-07-28T10:25:05Z iter1 step3: KEY RESULT — exhaustive byte-compare of all 75 .data twin pairs in shipped kernelbase (sha b7e96fb9): 23 IDENTICAL, 52 PTR-SHIFTED-self-consistent, **0 EC-ZERO**. Every self-referential static (7 CRITICAL_SECTIONs + 4 critsect_debug + 2 struct lists incl. reg_mui_cache) is correctly initialised in BOTH views: EC reg_mui_cache = {0x180150ab0,0x180150ab0} = its OWN address. Base relocs cover both zones (162 EC + 158 native). DVRT = 152 bytes, ZERO .data entries (loader never rewrites .data per view). => the load-time-initialiser hazard class does NOT exist in this artifact; the surviving class is runtime-populated globals (.bss). Contradiction with e4b90849's stated mechanism logged for the coordinator.
2026-07-28T10:31:46Z iter1 step4: MODULE SWEEP. Build uses --enable-archs=arm64ec,aarch64 (scripts/build-wine-arm64ec-spike.sh:44) => ALL 644 shipped aarch64-windows builtins are dual-compiled ARM64X; the twin condition is universal, not kernelbase-specific. Ran the authoritative llvm-nm+byte-compare on 10 load-bearing modules (kernelbase/ntdll/kernel32/user32/win32u/advapi32/combase/ucrtbase/gdi32/ole32) = 978 twin pairs: **0 EC-ZERO in every one**. Dominant delta family per module: ntdll 0xb40, user32 0xd50, ole32 0x1680, ucrtbase 0x1838, gdi32 0x7b0, combase 0x3e8. NOT checked individually: the other 634 builtins (screened structurally only).
2026-07-28T10:36:36Z iter1 step5: ROOT CAUSE OF THE WHOLE CLASS FOUND — the mirror function IS the bug. RUNTIME PROOF from laneA-INPUT-MUIFIX-try16-172249/run.log: base=0x87FFF830000, 'verify native_sort=0x87FFF982AD8' => RVA 0x152AD8 = the **EC** copy of sort (native copy is RVA 0x1552C0). So macrunner_hb_sync_locale_ec_copies executes in the ARM64EC view, NOT the native view as it assumes; every '&g' is the EC copy and 'dest = &g - delta' writes BELOW it instead of into the twin. Log's 'ec_sort=0x87FFF9802F0' => RVA 0x1502F0 = inside EC .data, exactly the predicted clobber address. MR_SYNC_CORE_EC(codepages) (CPTABLEINFO[128] = 8192 B) memcpy's 0x1503F0..0x1523F0 from EC .bss (all zeros at that moment), zeroing 30 EC .data symbols incl. reg_mui_cache, reg_mui_cs, reg_mui_cs_debug and entry_sintlsymbol, then continuing into most of native .data. => BOTH recorded 'casualties' of the ARM64X twin class were self-inflicted by the mirror. reg_mui_cache reads {0,0} => LIST_FOR_EACH_ENTRY derefs NULL => c0000005 at kernelbase+0x5CCC8. Exactly the reported fault.
2026-07-28T10:53:10Z iter1 step6: FIX LANDED (not deployed). locale.c: mirror disabled (MACRUNNER_HB_LOCALE_EC_MIRROR_ENABLED 0) + direction now derived from the CHPE code map + region invariant (a .data object's twin is in .data, a .bss object's twin is in .bss). Disable rather than sign-flip because the deltas are unmaintainable: my own edit moved CORE 0x27E8 -> 0x2808 (65 pairs -> 86 pairs, llvm-nm on b7e96fb9 vs rebuild), a shift that stays in the right half so no bounds check catches it; and the delta is not even uniform within a build (5 pairs at 0x1AF8 vs 0x1B00, shifted by EC-only __imp_aux_RaiseException). ARTIFACT-VERIFIED: macrunner_hb_mirror_ec_copy = 2 copies in shipped b7e96fb9 -> 0 in rebuild 05fa3326 (linker eliminated the memcpy as dead code); 0 EC-zero twin pairs; both views compile clean under -Wall. registry.c comment corrected (its stated root cause was refuted). Sibling-mirror sweep: git grep finds the construct ONLY in locale.c. Residual class documented = runtime-split state (top_filter/global_data/oem_file_apis/is_wow64/RegOverridePredefKey), latent, NOT fixable by mirroring. dist untouched (still b7e96fb9) - another lane owns the title slot. Report: reports/phase4-hollow-knight/ARM64X-TWIN-SWEEP-THE-MIRROR-WAS-THE-BUG-20260728.md

LOOP-STATUS: GOAL
