# Журнал лейна ROSETTA2


## 2026-08-04 итерация 1 — карта заведена, три раздела заполнены измерениями

Первым делом проверил утверждение брифа «наработок нет вовсе» — **оно неверно**: в
`reports/research/` лежит `ROSETTA-DEEP-ANALYSIS.md` на 323 строки от 31.07 с разобранным
AOT-конвейером, схемой трансляции 1:1, картами x86↔ARM, возвратами через хостовый RAS, флагами и
TSO, всё со ссылками на Champollion part 1/2 и dougallj. Не переписываю, ссылаюсь.

Создан `ROSETTA2-FULL-MAP.md` — восемь разделов брифа, каждое утверждение помечено.

**Новые измерения на этой машине (macOS 26.5.2, 25F84):**

* `/usr/libexec/rosetta/` — шесть бинарей; `oahd` и `translate_tool` собраны **arm64e**, а
  `runtime` (тот, что живёт внутри транслированного процесса) — обычный **arm64**;
* **AOT-кэш системных библиотек переехал и вырос**: файла `/System/Library/dyld/aot_shared_cache`,
  описанного в публичных разборах, на системе НЕТ; кэш лежит в
  `/System/Volumes/Preboot/Cryptexes/OS/System/Library/dyld/aot_shared_cache.{0..6}` — семь
  файлов, суммарно **5.70 ГБ** против ~2.4 ГБ у Champollion. Магия `AotCache` в первых восьми
  байтах сохранилась, то есть структура заголовка та же;
* `/var/db/oah/` не читается вовсе (`Operation not permitted`) — всё о нём остаётся источником,
  не измерением;
* права `oahd`: `com.apple.private.oahd`, `...coalition-spawn`, `...kernel.global-proc-info`,
  `...security.storage.oahd`, `...ecosystemanalytics.rosetta`, песочница `com.apple.oahd`;
* **у `runtime` прав НЕТ ни одного** — значит право исполнять свежесозданный код транслированный
  процесс получает не через `com.apple.security.cs.allow-jit`. Это прямо касается нашего
  раздела 4 и делает версию с entitlement маловероятной.

**Пробелы, объявленные явно:** разделы 4 (память и W^X), 7 (исключения), 8 (потоки) пусты;
в разделе 5 есть флаги, но нет x18; в разделе 6 нет имени управляющего бита TSO.

Слот прогонов не трогал, игру не запускал, в системе ничего не менял.

LOOP-STATUS: CONTINUE|проверил и опроверг посылку брифа об отсутствии наработок — ROSETTA-DEEP-ANALYSIS.md на 323 строки уже покрывает AOT-конвейер, трансляцию 1:1, карты, возвраты, флаги и TSO со ссылками, поэтому ссылаюсь, а не переписываю; создан ROSETTA2-FULL-MAP.md со всеми восемью разделами и пометками на каждом утверждении; новые измерения на macOS 26.5.2: шесть бинарей в /usr/libexec/rosetta, oahd и translate_tool арм64e против обычного arm64 у runtime, AOT-кэш системных библиотек ПЕРЕЕХАЛ в Cryptexes и разбит на семь файлов суммарно 5.70 ГБ против 2.4 ГБ в публичных разборах, магия AotCache сохранилась, /var/db/oah не читается по SIP, у oahd шесть приватных прав включая com.apple.private.oahd, а у runtime прав НЕТ ни одного, что делает версию про allow-jit маловероятной и прямо касается нашего раздела про W^X; дальше — закрывать пробелы 4, 7, 8 и x18 поиском в интернете и разбором runtime, слот прогонов не трогал

## 2026-08-04 15:48 — УКАЗАНИЕ ОПЕРАТОРА: карта ПОЛНАЯ, не по восьми разделам

Восемь разделов в задании — **минимальный костяк, а не граница**. ROSETTA2 это целый стек
запуска x86-64 на ARM, и карта должна покрыть его весь.

**Порядок изменён:** сначала ПЕРЕПИСЬ ПОВЕРХНОСТИ (все модули, файлы, экспортируемые
функции, службы, переменные окружения, ключи, форматы), и только потом глубина.
Пока перепись не сделана — вглубь не уходить.

**Каждая итерация обязана заканчиваться разделом «ЧТО ЕЩЁ НЕ ПОКРЫТО»** — явным
списком белых пятен. Пустой он бывает только в самом конце.

Задание в `prompts/` обновлено, перечитай его.
