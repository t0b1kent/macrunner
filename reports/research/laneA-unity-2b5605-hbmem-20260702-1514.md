# Lane A Unity+0x2b5605 / HB Memory Bookkeeping

Date: 2026-07-02

## Snapshot

Fresh pre-diagnostic full-closure snapshot:

`artifacts/milestone-dist/hk-rung11-latest-glm-postswap-20260702-141251-20260702-141251`

## Raw Fault

Source run:

`reports/phase4-hollow-knight/laneA-hk-glm-latest-postswap-long-132919-try1-132920`

Observed once:

- `UnityPlayer.dll+0x2b5605`
- bytes: `49 8b 84 f0 88 04 00 00`
- instruction: `mov rax, qword ptr [r8 + rsi*8 + 0x488]`
- regs: `r8=0x4c1302cb0`, `rsi=0x107a0a4a0`
- EA: `0xcfe355638`
- `hb_memory_read_u64(EA) -> MEMORY_FAULT`

Storm verdict: one-shot. The run has one `macrunner-hb-runtime-fail` at `rva=0x2b5605`, then `macrunner-hb-run-exit status=c000007b`. There is no fault/recover/fault livelock at this PC in that log.

Address-class verdict: not closed. The new `macrunner-hb-unity-addrclass` instrumentation was added, but subsequent runs did not replay `UnityPlayer+0x2b5605`, so no HB/virtual/Mach mapping line exists yet. The raw regfile leans toward class B (bad producer/register/data: `rsi` is pointer-sized, not a plausible small index), but this remains a hypothesis until the mapping dump fires.

## HB Fixes

Sampled post-device/post-swapchain runs were dominated by HB bookkeeping:

- Before fix: `macrunner_hb_sync_virtual_region -> hb_memory_protect -> rebuild_region_tree -> tree_insert`
- After incremental tree insertion: `rebuild_region_tree/tree_insert` disappeared; next hotspot was `split_all_regions_at`
- Final fix: `split_all_regions_at` uses `hb_memory_find_region` for the normal non-overlap invariant

Deployed `ntdll.so`:

`a12a7823b7071c248d3bac578255a18ad90bc0d85abcdd29233d8ab90e0ea70a`

## Verification

Filtered classify was run on:

`reports/phase4-hollow-knight/laneA-laneA-hk-addrclass-splitfast-20260702-150106-try1-150106`

Result: rung 11 preserved. Raw markers include:

- `CreateSwapChainForHwnd slot=15 ... hwnd=0x20054 ... rc=0x0`
- `MakeWindowAssociation` follows
- no `GetBuffer`
- no real swapchain `Present`
- no replay of `UnityPlayer+0x2b5605`

Next lever: continue from the post-swapchain no-GetBuffer window. If `UnityPlayer+0x2b5605` replays, use the new `macrunner-hb-unity-addrclass` line to choose class A/B/C.
