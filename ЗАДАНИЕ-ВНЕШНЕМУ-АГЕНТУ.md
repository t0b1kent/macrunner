# Задание внешнему агенту

**ПЕРВЫМ ДЕЛОМ: `git pull`.** Трижды подряд ты работал на застывшем клоне и трижды докладывал,
что файлов нет, когда они были. Начинай с обновления.

Дальше — `ОРИЕНТИР-ДЛЯ-АГЕНТА.md` и `УКАЗАТЕЛЬ.md`.

**Собрать и прогнать проект в твоём окружении нельзя** — нужны macOS на Apple Silicon, Metal,
Mach, ARM64EC, многогигабайтный дист Wine и сама игра. Их там нет физически, поэтому не заявляй
«собрано» или «проверено»: это будет неправдой по устройству окружения. Прогоны делает оператор.

---

## ЗАДАЧА 1 (текущая) — семнадцать точек входа x64-транслятора Prism

Файлы выложены: `docs/prism-binaries/xtajit64.dll`, `xtajit64se.dll`, `xtajit.dll`.

**Уже установлено разбором заголовков 07.08 — не переоткрывай:**

- `xtajit64.dll` и `xtajit64se.dll` **не экспортируют ничего**: каталог экспорта есть, в нём
  только имя модуля, функций 0, имён 0.
- Подключение идёт через **ARM64X**. Секция `.a64xrm` (144 байта, RVA `0x15F000`) — таблица пар
  RVA, **17 записей, шаг 8 байт** (две RVA по 4 байта). 144 = 18 слотов, восемнадцатый нулевой —
  это ограничитель, а не запись. Ранее здесь стояло «шаг 16 байт»: при таком шаге 17 записей
  не помещаются (17×16 = 272 > 144), и чтение с шагом 16 даёт 9 пар — каждую вторую. Таблица
  ниже верна, ошибка была только в подписи к ней.

```
0x2000 -> 0x4000     0x2090 -> 0x41C4
0x2010 -> 0x4300     0x20A0 -> 0x42F0
0x2020 -> 0x4380     0x20B0 -> 0x4220
0x2030 -> 0x427C     0x20C0 -> 0x4360
0x2040 -> 0x4054     0x20D0 -> 0x4310
0x2050 -> 0x4324     0x20E0 -> 0x4320
0x2060 -> 0x40B0     0x20F0 -> 0x42E0
0x2070 -> 0x410C     0x2100 -> 0x750D0
0x2080 -> 0x4168
```

- Левая колонка лежит внутри `.hexpthk` (RVA `0x1000`, размер `0x1110`) — слоты заглушек.
  Правая указывает в `.text`. `CHPEMetadataPointer = 0`, то есть ARM64X, а не CHPE.

**Выяснить: что представляют собой 17 функций по адресам правой колонки.**

Разбирай код ARM64 по этим RVA. Опознавай по признакам: сколько аргументов используется, какие
регистры читаются, что возвращается, какие строки и импорты рядом. Сверяй с контрактом x86 —
`xtajit.dll` экспортирует 25 функций `BTCpu*` с известными именами, часть ролей должна совпасть
(инициализация процесса и потока, вход в исполнение, уведомления о памяти).

Отдельно: **`0x2100 -> 0x750D0`** резко выбивается из остальных (`0x4000`-`0x4380`). Возможно,
это не заглушка. Не подгоняй под общую схему.

Деливерабл `reports/research/PRISM-X64-ТОЧКИ-ВХОДА.md`: по каждой из 17 — RVA, что опознано,
**на чём основано** (конкретные инструкции, строки, импорты), и честная пометка «не опознано»,
где не вышло. Догадка под видом факта здесь хуже пустой строки: по этому контракту будут писать код.

## ЗАДАЧА 2 — четвёртая категория мёртвых приборов

В `reports/research/МЁРТВЫЕ-ПРИБОРЫ.md` у тебя 8 живых маркеров против 78 молчащих. Добавь
категорию, которой нет: **упомянуты в комментариях, но НЕ реализованы**.

Пример, стоивший дня 07.08: `macrunner-hb-nullcall-vtable` трижды назначался критерием прогона.
Печати с таким именем **в коде нет** — только в комментариях; реально написаны `nullcall-site`,
`-engine`, `-frame`, `-iat`, `-map`. Три прогона по 900 с ждали строку, которую печатать некому.

Пройди комментарии в `engine/` на упоминания `macrunner-*` и сверь с реальными `fprintf`.
Каждое несовпадение — потенциально день чужой работы.

---

## Как оформлять результат

- **Механизм — факт со ссылкой на файл и строку.** Величина — оценка; нет замера, пиши «нужен
  замер» и назови какой.
- **Не ссылайся на файлы, которых не создавал.** Отчёт, где такие ссылки нашлись, потерял доверие
  целиком, вместе с верной частью.
- **Отрицательный результат записывай наравне.** Шесть правок подряд не сдвинули часы — это
  знание.
- Правки кода предлагай ветками `bridge/<имя>` с файлом `ЗАЯВКА.md` по форме
  `ФОРМА-ЗАЯВКИ.md` — тогда мост на машине оператора соберёт их и вернёт вердикт.
